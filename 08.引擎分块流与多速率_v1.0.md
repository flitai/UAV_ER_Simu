# 08. 引擎分块流与多速率

> **版本** v2.1，2026-09-07。本版按技术设计文档的文体重写全文：章节标题改为名词短语，取消设问句、
> 讲解式自指与口语化表述，统一规范性用语（应 / 必须 / 不得），六张图编号并在正文中引用。
> v2.0（同日）以可读性为目标重排结构：新增 §0 前言，为各节补写前置说明与论证段落，
> 并订正 v1.1 遗留的过时事实。**章节编号与主题自 v1.1 起未变**，因为代码与规范中有十四处注释
> 按 §9.2、§9.3、§9.4、§15 引用本报告。v1.1（2026-09-06）按 G-1 至 G-3 的实现回填 §9；
> v1.0（同日）在切片 ① 收口后写出。修订记录见 §17。
> **状态** 定稿。§2 至 §6、§9 至 §11 为既成实现的设计记录，均有对应代码与测试；§7、§8、§12、§13
> 规定首期不实现的内容及其将来的实现位置，逐项在 §15 列明拍板依据；§9 涉及的场景线为 04 之外的
> PROVISIONAL 扩展（D-030）。
> **依据** 04 §5.2（主产品定义）、§5.4（内部观测点 S0–S6）、§6.3（技术选型与同进程共享内存）、
> §8.3（运行引擎十一项职责）、§8.4（模型组件规范）、§8.6（错误码与标识符）、§9.2（最小元数据）、
> §12（采样率与带宽）、§13.3（P1 退出条件）、§15（验证分级）；03 §11.6（十一种标准数据端口）、
> §11.7（组件生命周期）、§11.3.1 与 §11.4（MATLAB 定位与 Coder）；01 §3.1（M/E/V 三维声明）；
> `CLAUDE.md` 铁律 3、4、5、8、9、10、15、16、17，决策 D-009、D-012、D-013、D-020、D-030、D-031、
> D-033、D-036、D-037、D-040、D-041、D-042、D-046、D-047、D-049；06 备忘录 §9 P1-0 / P1-2 / P1-4、
> §9A、§9C、§9D；`docs/{iq-format, diagram-format, component-catalog, display-products, api-versions,
> scenario-format}.md`。依据条款与本报告各节的对应关系见附录 C。
> **用途** 引擎与模型组件的实现依据；M-2、M-3 与后续场景线步骤的接口契约来源；`engine/tests` 判据的
> 出处；评审与交付时说明信号链路计算过程的对内材料。
> **范围界定** 本报告不规定文件格式的字段。字段已在 `docs/` 冻结，字段变更应先修改该处并记录决策。
> 本报告规定的是结构、时序、边界与不变量。与上游条款的逐条对照见 §14，速查表见附录 A 与附录 B。
> **阅读指引** 首次阅读本报告应按 §0.2 给出的路线进行，不宜自 §1 顺序通读。

---

## 0. 前言

### 0.1 问题定义与设计约束

04 §8.3 对运行引擎的要求可概括为一句总则加十一项职责。总则为：「运行引擎采用有向数据流和分块 IQ 处理。
每个 IQ 数据块同时携带样本和元数据。」十一项职责中与本报告直接相关的包括：按拓扑顺序调度模型、
检查采样率与中心频率、执行分块与缓存与背压与流结束处理、管理随机种子和确定性复现、
支持文件流和内存流、将运行状态与降采样显示产品发布给应用服务、在采样率变化时处理时间和群时延关系、
避免无必要的 IQ 内存复制。

该总则中的两个概念决定了引擎的整体形态，需要先行界定。

**其一为分块。** 本系统的主数据产品是 DDC 后、信道化前的宽带复基带 IQ，即 04 §5.2 定义的 S4 观测点。
首期常用采样率为 5 至 50 MS/s，文件兼容口径达到 100 MS/s。以 100 MS/s 计，内部使用的复 float32
每秒产生 800 MB 数据，一分钟即为 48 GB。将一次仿真的完整 IQ 一次性载入内存再逐级处理，
在任何目标机上均不可行，且无法支持运行过程中的实时观察。因此引擎必须将数据流切分为若干片段，
逐段推过整条链路。由此得到本报告的核心数据结构：

> **分块流**：将一条连续的 IQ 流切分为若干个块（`Block`），每个块由一段连续样点与描述该段样点的
> 元数据（`BlockMeta`）构成。块是调度的最小单位，也是元数据的携带单位。链路上的各组件均按块处理，
> 不假设块长为定值，也不假设自身被调用的次数。

该定义附带一条必须同时给出的边界条件：**分块是调度行为，不改变时间连续性**。一条数据流被切分为
一万块或一百块，其描述的仍是同一段连续采集或连续仿真的信号。采集过程本身的不连续，
即失锁、补零、重对齐等情形，由 `BlockMeta` 中独立的 `continuous_with_previous` 标志表达，
两者不得混用（铁律 3）。§2.1 将该边界条件落实为三条不变量。

**其二为有向数据流。** 用户在浏览器中编辑的对象是一张框图，即若干模型组件经连线构成的图，
信号自源节点流向观测点。引擎将该图作为有向无环图（DAG）执行，按拓扑顺序逐节点调度。
无环为硬性要求，含环的框图在装载阶段即被拒绝，因为环意味着某节点的输入依赖其自身的输出，
在分块流中无法确定求值顺序。调度机制见 §6，图的装载过程见 §5。

**引擎的职责边界。** 引擎不实现界面、HTTP 服务、任务队列与数据库访问，亦不解析服务器上的文件路径。
上述功能均由应用服务承担。引擎是一个以框图 JSON 与被注入的文件路径为输入、
以产品文件与事件流为输出的子进程。该边界的完整表述见 §1。

### 0.2 适用读者与阅读指引

本报告服务三类读者，各自的阅读路线不同。首次阅读应按下表选择路线，不宜自 §1 顺序通读。

| 读者 | 关注问题 | 建议路线 |
|---|---|---|
| 引擎实现者 | 调度机制、块的流转、错误报告方式 | §0.4 → §2 → §3 → §6 → §5 → §10 → 附录 A |
| 模型组件作者（含 Coder 产物的封装层） | 需实现的接口函数与禁止事项 | §0.4 → §4 → §2 → §8 → §13 → §11 → 附录 B |
| 评审、集成方与场景线使用者 | 链路的计算过程与结果可信度 | §0.1 → §0.5 → §9 → §10 → §11 → §14 |

三类读者的共同前置内容为 §0.3 的术语与编号对照表。本报告引用项目内部的步骤编号与决策编号，
该表给出统一展开，正文中首次出现时另给一句说明。

### 0.3 术语、缩略语与内部编号

#### 0.3.1 信号与数字信号处理术语

| 术语 | 含义 |
|---|---|
| IQ | 复基带信号的同相与正交两路分量。系统内部统一采用复 float32，文件交换采用复 int16 交织（`docs/iq-format.md`） |
| DDC | 数字下变频。将采样数据搬移至零中频并抽取至目标采样率 |
| 抽取 | 降采样。每 D 个样点保留一个，采样率变为原值的 1/D。抽取前必须先经抗混叠滤波（铁律 4） |
| 抗混叠 | 在抽取前以低通滤波器抑制超出新奈奎斯特频率的成分，避免其折叠进有用频段 |
| 群时延 | 滤波器引入的整体延迟。线性相位 FIR 滤波器的群时延为常数，等于 `(N−1)/2` 个输入样点，N 为抽头数 |
| 零阶保持 | 以分段常数表示慢变量的方式：两个更新时刻之间取值恒为前一时刻的值，不作插值 |
| 背压 | 下游处理速率低于上游产出速率时，由下游反向约束上游产出速度的机制 |
| 拓扑序 | 有向无环图的一种节点排序，使每条边均自排序在前的节点指向排序在后的节点 |
| 扇贝损耗 | 谱线未落于 FFT 频点中心时读取的峰值偏低量。hann 窗最坏情形为 1.42 dB |
| 等效噪声带宽 | 加窗使谱线能量分布至邻近若干频点。hann 窗下将谱线邻域在线性域求和，所得为单音功率的 1.5 倍，即 `10·log10(1.5) = 1.761 dB` |

#### 0.3.2 本项目术语

| 术语 | 含义 | 出处 |
|---|---|---|
| S4 观测点 | 04 §5.4 定义的七个内部观测点 S0–S6 之一，位于 DDC 后、信道化前，为本系统唯一的主数据产品 | 04 §5.2、§5.4 |
| 观测点（框图中的） | 挂接于某 IQ 输出口的抽头，将流经的 IQ 归约为频谱与包络两种定长行产品并写盘 | §10.2 |
| 分块流 / 块 / 块长 | 见 §0.1。块长指一个块所含的复样点数 | §0.1、§2 |
| 时间锚 | 块首样点在整条流中的序号 `start_sample`。引擎内唯一的权威时间量，逻辑时间由其与采样率相除派生 | §2.1 |
| 参数帧 | 场景侧慢变量（几何、路损、多普勒、时延、噪声底）的一次取值，更新率 10 至 100 Hz，帧内零阶保持 | §9 |
| 慢变 / 快变 | 慢变指参数帧一侧（10–100 Hz），快变指 IQ 一侧（5–50 MS/s），两者相差五至六个数量级 | §9.1 |
| 结果四态 | `valid` / `degraded` / `invalid` / `not_applicable`。不得以默认值替代缺失数据 | 铁律 15 |
| 溯源八件套 | `model_id`、`model_version`、`model_level`(E)、`model_layer`(M)、`credibility`(V)、`parameter_version`、`confidence`、`trace_id`，随每个数据产物传递 | 铁律 8、D-012 |
| M / E / V / L | 模型层次 M1–M3、同层内精度 E1–E4、验证程度 V1–V5、IQ 模型等效档 L1–L3。四者同时声明 | 01 §3.1、04 |

#### 0.3.3 内部编号

本报告引用的项目内部编号统一展开如下。

| 编号 | 含义 |
|---|---|
| 切片 ① | 一条链到频谱：合成源经观测点输出频谱与包络并在前端显示。2026-09-06 收口 |
| 切片 ② | 场景里飞起来：无人机沿航线运动，链路读数与瀑布随距离变化。2026-09-06 收口 |
| 切片 ③ / ④ / ⑤ | 拖出框图（画布交互）/ 合流与厚化（下变频、信道化、噪声注入）/ 遮挡接入。均未开工 |
| B-1 至 B-7 | 引擎运行器与服务桥接七步：注册表、JSON 装载、观察者与产品、`cuav_run`、任务管理、WebSocket 补取、视窗抽取 |
| G-0 至 G-5 | 场景线六步：示例场景、`geo/` 运动学与链路预算、场景源组件、施加类信道、态势图层、场景编辑器 |
| M-2 / M-3 | MATLAB 线：Coder 生成 DDC、生成信道化与接收滤波。均未开工，为 §8 与 §13 的首个使用者 |
| P1-0 | 06 备忘录中本报告自身的步骤编号 |
| D-0xx | `CLAUDE.md` 决策日志条目号。本报告主要涉及 D-013（端口不得直连）、D-031（引擎为子进程）、D-033（参数帧按样点序号推进）、D-036（DSP 组件采用 Coder）、D-037（浏览器不接触服务器路径）、D-040（装载器四项约定）、D-041（事件与退出码）、D-042（服务任务管理器）、D-047（功率单位与 dBm）、D-049（切片 ② 场景运行时约定） |
| 铁律 N | `CLAUDE.md` 所列十七条项目铁律。本报告主要涉及 3（时间不混用）、4（IQ 语义）、5（单位与 dB）、8（溯源）、9（种子与复现）、10（基准不得静默变）、15（不静默降级）、16（目标平台）、17（路径可迁移） |

### 0.4 典型运行流程

图 1 给出一次完整任务从提交到结果读取的处理流程。后续各章分别展开其中的环节。

```
 ① 浏览器提交框图 JSON
        │
        ▼
 ② 应用服务：最小结构检查 → 将 data_id / scenario_id 解析为服务端文件路径
        │    结果写入解析旁挂 diagram.resolved.json，框图副本自身不含任何路径（§5.1、D-037）
        ▼
 ③ cuav_run --validate：装载一次，只校验不落盘，未通过则返回 400（§5）
        │
        ▼
 ④ cuav_run --run：经装载五步得到可执行的 DAG（§5）
        │
        ▼
 ⑤ 调度轮转：按拓扑序逐节点调用 process()，每条边设一个深度 1 的缓冲（§6）
        │    ├─ 源节点按 block_samples 切块并维护 start_sample（§2.1）
        │    ├─ 场景参数源每轮产出覆盖本轮样点窗口的全部帧（§9.3）
        │    ├─ 施加类信道按样点序号取帧，施加增益、多普勒与时延（§9.5）
        │    └─ 观测点将流经的 IQ 归约为频谱行与包络行，逐行追加写盘（§10.2）
        ▼
 ⑥ 输出分两路
        ├─ 产品文件 data/runs/<task_id>/<op_id>/{spectrum.f32, envelope.f32, index.json}
        │      定长行，读端按 row_index × row_len × 4 定位（§10.3）
        └─ stdout JSON 事件（task.state / progress / product_row / entity / link / log）
               应用服务脱敏后转为 WebSocket 帧发送至浏览器（附录 A.1）
        ▼
 ⑦ 浏览器：运行期以跟随模式显示瀑布；任务结束后按视窗自产品文件回看
```

**图 1  一次任务的端到端处理流程**

该流程中有三点需要先行说明，它们是后续多项设计取舍的依据。

其一，**产品文件与事件流的复现承诺不同**。同种子两次运行，产品文件逐字节相同；事件流不作此承诺，
因为 `progress` 事件按墙钟节流。铁律 9 的复现对象是产品文件，详见 §11.1。

其二，**引擎全程不解析绝对路径**。第 ② 步将标识解析为路径，第 ④ 步注入，且路径统一为仓库相对、
纯 ASCII、以 `/` 分隔。引擎的窄字符 `main()` 因此不会接触到可能含中文或空格的根目录，详见 §1 与 D-042。

其三，**浏览器断开后第 ④ 至 ⑥ 步继续执行**。这是将引擎实现为子进程而非 Node 插件所直接带来的性质（D-031）。

### 0.5 设计摘要

以下十一条为本报告的全部结论。每条给出结论、依据与展开该结论的章节。

1. **样点序号是引擎内唯一的时间锚。** `BlockMeta.start_sample` 为权威量，逻辑时间
   `t_s = start_sample / sample_rate_Hz` 为派生量。仅在此前提下，块长与调度顺序的变化才不影响结果。
   按墙钟推进的量不得进入产品文件（§2.1）。
2. **引擎是子进程，而非插件。** `cuav_run` 以定长二进制产品文件与 stdout JSON 事件同应用服务交换数据。
   其依据为：Windows 平台上 Node ABI 与 MSVC 版本的匹配构成长期维护负担，
   而子进程边界可直接满足浏览器断开不终止任务的要求（§1、D-031）。
3. **端口分为两类，不得直接连接。** `IQStream` 为样点流，`SceneParamFrame` 与 `ChannelPathSet` 为
   慢变参数流。参数只能经施加类组件进入 IQ，因为参数作用于样点的方式必须由具体模型显式承担（§3、D-013）。
   该规则仅在 `Graph::connect` 与 `can_connect()` 一处实现，画布、服务与 `--validate` 均为其投影。
4. **组件目录仅由引擎生成。** 参数名、单位、范围、缺省值、互斥关系与内部参数标记均出自
   `IComponent::describe()`，由 `cuav_run --catalog` 导出。系统内不存在第二份组件清单（§4）。
5. **调度采用单线程拓扑轮转，每条边设一个深度 1 的缓冲。** 背压、多线程与共享内存首期均不实现，
   但接口已预留相应位置。不实现的内容及其理由见 §7（§6、§7）。
6. **多速率处理的口径为样点序号换算加群时延显式声明**，而非由各组件自行计算时间。抽取 D 倍后
   `start_sample_out = (start_sample_in − group_delay) / D`，群时延由组件声明、由封装层扣除。
   首期引擎内不含重采样组件，跨速率连接一律拒绝（§8）。
7. **参数帧按样点序号推进，帧内零阶保持。** 第 k 帧覆盖样点区间
   `[floor(k·fs/rate), floor((k+1)·fs/rate))`，为纯整数映射。不按墙钟推进，否则同种子不可复现（§9、D-033）。
8. **随机性显式注入，固定种子逐位复现。** 库内不设全局随机源，组件经 `IRandom&` 获取随机数。
   不使用 `std::random_device` 与 `std::normal_distribution`，因二者的平台实现存在差异（§11.1、铁律 9）。
9. **不作静默降级。** 无法处理时应将块标记为 `Degraded` 或 `Invalid` 并写明理由，不得以默认值替代。
   结果四态与运行状态相互正交：退出码表示运行是否完成，`task.state` 事件表示结果是否可信（§2.2、附录 A、铁律 15）。
10. **功率单位为 mW。** 任一观测点上 `|x|² = 功率 / mW`，取 `10·log10` 即得 dBm。合成源按 dBm 给定参数，
    回放源按清单常数换算；无标定常数时保持降级状态并仅除以满量程，不得产生无来源的 dBm 值（§9.6、D-047）。
11. **MATLAB Coder 产物是组件的内部实现，而非组件本身。** 生成的 C 代码置于 `models/<环节>/coder/`，
    引擎侧仅实现封装层：参数校验、铁律 4 检查、状态复位与溯源填充。运行时、交付包与界面均不出现
    MATLAB（§13、D-036）。

---

## 1. 引擎在系统架构中的定位

本节界定引擎进程的职责范围及其对外接口。该边界是后续各章所述机制的作用域前提。

04 §6.3 给出的架构基线为浏览器、应用服务、运行引擎三层，其中规定「应用服务和运行引擎采用粗粒度
任务接口，本机高吞吐组件仍使用同进程、共享内存或内存映射文件」。D-031 将该条款在首期落实为具体形态：
运行引擎为应用服务拉起的子进程 `cuav_run`，双方经文件与 stdout 交换数据，不采用函数调用方式。
进程边界与四条命令路径如图 2 所示。

```
 浏览器 ──REST / WebSocket── 应用服务 (Node/TS) ──spawn── cuav_run (C++ 子进程)
                                  │                            │
                                  │                            ├─ 组件与调度器（同一进程内，直接传递 Block）
                                  │                            │
                                  │   ┌── --catalog ───────────►│  导出组件目录 JSON，不建立任何目录
                                  │   ├── --validate <框图> ───►│  仅装载校验，不落盘
                                  │   ├── --scenario-track ────►│  仅执行运动学，输出 entity 事件流
                                  │   └── --run <框图> --out ──►│  完整运行
                                  │                            │
                        data/runs/<task_id>/ ◄──────────────────┤  产品文件与 events.jsonl
                                  │      spectrum.f32           │
                                  │      envelope.f32           │
                                  │      index.json             │
                                  │      track.jsonl / links.jsonl
                                  ▼                            │
                        按视窗抽取的展示数据 ◄──────────────────┘  stdout JSON 事件（逐行 flush）
                                  │
                                  ▼
                              浏览器仅接收展示数据，不接收原始 IQ（铁律 7）
```

**图 2  引擎与应用服务的进程边界及四条命令路径**

引擎的职责边界由五条规定构成。各条均有明确的技术后果，非风格性约定。

| 边界 | 规定 | 依据 |
|---|---|---|
| 不嵌入 Node | 不实现 N-API 插件。Windows 平台上 Node ABI 与 MSVC 版本的匹配构成长期维护负担，而子进程边界可直接满足 04 §8.3 末段「浏览器断开不得终止任务」的要求 | D-031 |
| 不内建 HTTP | 对外交换限于产品文件与 stdout 事件。HTTP、WebSocket、任务队列与鉴权均由应用服务承担 | D-031 |
| 不解析绝对路径 | 应用服务以仓库根为工作目录拉起子进程，命令行与解析旁挂中的路径统一为仓库相对、以 `/` 分隔、纯 ASCII。引擎的窄字符 `main()` 因此不会接触到可能含中文或空格的根目录，Windows 平台的路径编码风险在源头消除 | D-042；铁律 17 |
| 不解析数据与场景的存储位置 | 框图中仅含 `data_id` 与 `scenario_id`，实际文件位置由服务端解析后经旁挂注入。装载器对框图中出现的内部参数一律拒绝，不设置信任副本的例外 | D-037、D-040 |
| 平台差异集中于一处 | 目录创建、原子替换、字节序判断、路径拼接与时间戳均经 `platform.h`。`#ifdef _WIN32` 仅允许出现于 `platform.cpp` | 铁律 16 |

**当前规模**（2026-09-07 核实，macOS 开发机）。静态库 `cuav_engine` 由 16 个源文件构成，共 4796 行；
公开头文件 19 个，共 1684 行；运行器 `cuav_runner` 为单文件 621 行，可执行文件 `cuav_run` 的 `main`
为 14 行。`engine_version()` 返回 `0.1.0`，组件目录的 `schema_version` 为 `cuav-catalog/1`，
注册表内组件 12 个（附录 B）。测试方面，`engine/tests/` 下 13 个测试文件共 3952 行、114 个 doctest 用例；
`ctest` 注册 8 项，即引擎单测、`--catalog`、切片 ① 的 `--validate` 与 `--run`、`--scenario-track`、
切片 ② 的 `--validate` 与 `--run`，以及经 `add_subdirectory` 引入的 `cuav_geo_smoke`；黄金基准 3 份。

引擎自 2026-09-06 起经 CMake 的 `add_subdirectory` 引入 `geo/` 并链接 `cuav_geo`。`geo/` 是场景侧的
大地坐标、运动学与链路预算库，含 4 个头文件、4 个源文件与 1 个冒烟测试，共 1233 行，
并保持零第三方依赖，即仅包含 STL 与自身头文件。其代价是该库的 doctest 单测需寄放于 `engine/tests/`
（`test_geo.cpp` 19 例、`test_scenario.cpp` 13 例、`test_channel.cpp` 20 例），
另在库内保留一个不依赖 doctest 的裸 `main` 冒烟测试以保证可独立构建。该安排列为 §15 暂定项 ⑥，
待 D3 移植 emcore 的 148 例遮挡黄金基准时一并处理。

## 2. 块结构、块元数据与时间锚

本节定义引擎内流动的数据结构，并给出其必须满足的三条不变量。这三条不变量是同种子逐字节复现这一
承诺的技术基础，§6 的调度、§8 的多速率换算与 §9 的参数帧对齐均建立于其上。

04 §8.3 要求「每个 IQ 数据块同时携带样本和元数据」。元数据挂载于块而非图的全局配置，其原因在于：
多速率链路上各条边的采样率可以不同（§8），组件必须能够依据当前处理的块判断其上下文，
而不能查询一份可能与当前边无关的全局配置。该要求将 `Block` 确定为自描述对象：

```cpp
struct Block { std::vector<Complex> samples; BlockMeta meta; };   // Complex = std::complex<float>
```

内部统一采用复 float32（`docs/iq-format.md` §3.2）。复 int16 交织属文件层格式，引擎内部不出现：
回放源在读入时完成换算，观测点在写出时完成逆换算，不存在第三处转换点。

`BlockMeta` 的字段分为四组，各组的必要性如下表。

| 组 | 字段 | 必须置于块上的原因 |
|---|---|---|
| 采样上下文 | `sample_rate_Hz`、`center_frequency_Hz` | 多速率条件下各边速率可不同，组件须依据当前块判断 |
| 时间锚 | `start_sample`、`time_basis`、`continuous_with_previous` | 见 §2.1 |
| 功率标定 | `calibration{calibrated, offset_dB, source, note}`、`full_scale` | dBm 值仅在带来源时成立（D-047）。标定是块的属性，混合链路须能取来源较弱的一方 |
| 质量与溯源 | `state`、`state_reasons`、`trace` | 四态与溯源八件套随数据传递，不随节点固定（铁律 8、15） |

### 2.1 时间锚不变量

**不变量一：`start_sample` 为本块首样点在整条流中的序号，由生产者维护，消费者不得改写。**
源组件以 `produced_` 计数器累加已产出样点数并写入各块，下游组件在转发或加工时原样传递。
若消费者自行修改该字段，同一条流将出现两个相互矛盾的时间表述。

**不变量二：逻辑时间为派生量 `t_s = start_sample / sample_rate_Hz`，任何组件不得自行维护当前时间。**
该不变量是同种子逐字节复现的前提。设组件内部维护 `t += block_len / fs`，则块长自 65536 变更为 25000 后，
浮点累加的舍入路径随之改变，结果亦随之改变；而 `start_sample` 为整数，切块方式不影响其取值。
本报告在 §8 与 §9.2 两次应用同一原则：凡能以整数表达的时间关系，一律不得经由浮点秒。

**不变量三：分块与时间连续性相互正交。** `continuous_with_previous = false` 表示采集不连续，
即失锁、补零、重对齐等实际数据断点，不表示分块边界。铁律 3 关于「失锁、补零、重对齐不得当作连续数据」
的要求即由该字段承载。若将分块边界记为不连续，下游将把每个正常的块边界都识别为数据断点；
反之若吞掉真实断点，则一段经拼接的数据将被当作连续采集，后果更为严重。

图 3 给出样点序号在一条三节点链路上的推进方式。

```
  源节点                    施加类信道                  观测点
  ───────                   ──────────                  ──────
  块 A  start_sample=0      流长不变（§9.5）            行 0 ← 覆盖样点 [0, 4096)
        samples=25000       start_sample 原样传递       行 1 ← 覆盖样点 [4096, 8192)
        ↓                          ↓                          ⋮
  块 B  start_sample=25000  start_sample=25000          行 n ← …
        samples=25000
        ↓                          ↓
  块 C  start_sample=50000  start_sample=50000
        samples=25000（末块可短）

  逻辑时间 t_s = start_sample / sample_rate_Hz，由各消费者按需派生，不在块间累加传递
```

**图 3  样点序号在链路上的推进**

上述三条不变量的回归测试为 `engine/tests/test_types_graph.cpp` 中的
「块元数据降级会累积理由，且不覆盖更差的状态」与「四态取最差：not_applicable 不参与」。

### 2.2 状态降级入口

`BlockMeta::degrade(why)` 与 `BlockMeta::invalidate(why)` 是仅有的两个降级入口，二者均强制要求给出
理由字符串，调用方无法在不说明原因的情况下修改状态。`worst(a, b)` 返回两个状态中较差的一档，
`NotApplicable` 不参与比较。该语义与 Python 侧 `tools/iq_format/manifest.py` 中的 `worst()` 完全一致，
因为同一条数据在引擎内与在清单中必须给出相同的结论。

综上，在三条不变量成立的条件下，块长、调度顺序与机器速度均不进入计算结果。若其中任一条被破坏，
同种子逐字节复现即告失效，且其失效形式通常表现为一份表面正常的错误数据。

## 3. 端口类型系统与连线规则

本节规定组件之间的连接方式。所要解决的问题是：如何判定一张用户编辑的框图在语义上成立。
判定依据为端口类型及一组连线规则，且该组规则仅在引擎的一处实现，
画布的连线提示、服务端的提交校验与 `cuav_run --validate` 均为其投影。

```cpp
enum class PortType { IQStream, SceneParamFrame, ChannelPathSet, SpectrumFrame,
                      DetectionList, FeatureVector, RecognitionList };
bool can_connect(PortType from, PortType to);   // 现行规则：仅允许 from == to
```

七种类型是 03 §11.6 所列十一种标准数据端口在首期的子集，各自的角色如下表。
`RecognitionList` 于 2026-09-07 随 C-1 加入（D-051），生产者与消费者待 C-4、C-5。

| 端口类型 | 载荷 | 首期生产者 | 首期消费者 | 结构体 |
|---|---|---|---|---|
| `IQStream` | 一个 `Block`，即样点与元数据 | 全部源组件、施加类信道 | 信道、混合、频谱、检测、观测点 | 有 |
| `SceneParamFrame` | 一批慢变参数帧 | `ScenarioSource` | `SceneBoundChannel` | 有 |
| `ChannelPathSet` | 多径路径集合 | 无（首期不实现） | 无 | 无，仅占类型位 |
| `SpectrumFrame` | 一批功率谱帧 | `SpectrumAnalyzer` | 首期无下游消费者 | 有 |
| `DetectionList` | 检测结果列表 | `EnergyDetector` | `DetectionSink` | 有 |
| `FeatureVector` | 特征向量 | 待 C-4 `FeatureExtractor` | 待 C-4 `TemplateClassifier` | 无 |
| `RecognitionList` | 识别结果列表 | 待 C-4 `TemplateClassifier` | 待 C-5 `Evaluator` | 无 |

`can_connect` 现仅允许同类型相连，故 7×7 兼容矩阵中仅 7 个对角项为真。该规则并非为简化实现而设，
而是 D-013 的直接结果：`IQStream` 与参数流之间不存在合法的直接连接，二者之间必须存在一个施加类组件。
其理由为，参数作用于样点的方式（乘幅度、转相位或移时延）必须由具体模型显式承担，
不能由一条连线隐含表达。参数流之间同样不设自动转换。将来若出现合法的兼容对，
例如 `ChannelPathSet` 退化为单径 `SceneParamFrame`，需修改的仅有 `types.cpp` 一处，
目录导出与画布提示将自动跟随。

兼容矩阵在 `cuav_run --catalog` 的输出中以 49 条全枚举形式给出，每条不兼容项附带理由，
画布可直接向用户显示该理由。对应测试为 `engine/tests/test_registry.cpp` 中的
「目录导出：六类、端口兼容矩阵全枚举、D-013 规则、确定性」。

### 3.1 连线校验规则

`Graph::connect()` 拦截六种情形，各附带一个失败分类供装载器映射（`LinkFault`）：节点越界、自环、
输出口不存在、输入口不存在、类型不兼容，以及一个输入口只能连接一条边。
`Graph::validate()` 另行拦截两种情形（`GraphFault`）：图中存在环、**必填**输入口悬空。
自 2026-09-07（D-051，C-1）起 `PortSpec` 增加 `optional` 标志：声明为可选的输入口未连线时既不算悬空，
调度器也不等它的数据，组件用自身参数顶替。这是组件自己声明的能力，不是装载器替它做的假设；
一个只剩未连可选口的节点等同于源，每轮都会被调度。首批使用者为 C-2 的 `AntennaGain` 与 C-5 的 `Evaluator`
——定参链路（`FreeSpaceChannel`）没有场景，此时天线按固定来波角、评价器按数据清单真值工作。

规则仅在上述两个函数中实现。装载器依据失败分类映射为对外错误码，不解析报文内容
（错误码全表见 `docs/diagram-format.md` §4 与附录 A.3）。该安排的收益是，将来放宽或收紧任一规则时，
无需同步修改装载器、服务端与前端三处。对应测试为 `test_types_graph.cpp` 中的
「连线校验：类型不匹配、端口不存在、输入口重复占用都要挡住」「输入口没连线就不许跑」
「有环的框图必须报错」。

### 3.2 端口数据结构 PortData

`PortData` 为带标志的聚合体而非 `variant`。原因是引擎按 C++14 编写（与 emcore 保持同一标准），
而 `std::variant` 自 C++17 起提供。

```cpp
struct PortData {
    PortType type; bool has_data;
    Block iq;
    DetectionList detections;
    std::vector<SpectrumFrame> spectra;
    std::vector<SceneParamFrame> scenes;
    void clear();
};
```

`spectra` 与 `scenes` 均为向量，因为一个输入块可能对应多帧输出，单次 `process` 需将其全部交付。
`scenes` 必须为向量而非单帧的理由见 §9.3：块长可能大于一帧覆盖的样点数，此时一个块需配多帧；
也可能小于，此时多个块共用一帧。单帧字段仅能表达后者。

> **既往缺陷记录。** `scenes` 最初实现为单个 `scene` 字段，且 `clear()` 未清除该字段。
> 未清除的后果是上一轮的参数帧残留至下一轮。在参数帧尚无生产者时该缺陷不可见，
> 一旦场景线接入即表现为以陈旧参数处理新样点这一无声错误。两处已随 G-2 一并修正
> （`component.h:62` 与 `:69`），拍板记录见 §15 暂定项 ⑦。

`ChannelPathSet` 首期仅占端口类型位，不补充结构体。该类型已出现于 `PortType` 与连线禁令中，
但无对应数据结构。首期的单径与双径均使用 `SceneParamFrame`，多径进入 P3（05 的 P0 端口预留）。
现阶段即占用类型位的理由是，将来补充结构体时无需改动端口枚举与兼容矩阵的形状，
框图文件与目录 JSON 的字段亦无需变更。该项列为 §15 暂定项 ③。

## 4. 组件生命周期、注册表与参数描述

本节定义模型组件在引擎内的形态。组件作者据此确定需实现的接口函数与禁止事项；
画布与服务端的实现者据此确定参数表单与校验规则的来源。

组件是不持有全局状态、不假设块长、随机性由外部注入的纯状态机。它经 `describe()` 声明自身属性，
经 `configure()` 接收参数，经 `process()` 逐块处理。系统内描述组件的信息仅有一份，
即 `describe()` 的返回值。

### 4.1 生命周期

04 §8.4 建议「把组件运行接口控制在少量稳定函数，例如初始化、配置、处理、刷新、复位、状态查询和销毁」。
首期实现其中六项，`destroy` 由 C++ 析构函数承担。各阶段的约定如下表。

| 阶段 | 签名 | 约定 | 依据 |
|---|---|---|---|
| 自描述 | `ComponentInfo describe() const` | 进入目录的组件必须重写。默认实现仅给出类型与端口，将被目录自检拒绝 | 04 §8.4 |
| 配置 | `configure(num_params, text_params, err)` | 失败时必须写入 `err` 并返回 false，不得吞掉错误。跨参数约束在此处执行第二道校验 | 04 §8.4 |
| 挂接观察者 | `attach(IRunObserver*)` | `Graph::run` 在 `init` 之前对每个节点调用一次，多数组件忽略该调用 | §10.1 |
| 初始化 | `init(IRandom& rng, err)` | 随机性仅在此处注入。需落盘的组件在此处检查输出目录 | 铁律 9；D-040 ② |
| 处理 | `process(in, out, err)` → `Step` | 返回 `Produced`、`Idle`、`Finished`、`Error` 四者之一 | §6 |
| 收尾 | `flush(out, err)` | 输出内部缓存的不完整帧，或将其丢弃并记录 | §6、§10.3 |
| 复位与状态 | `reset()`、`status()` | 复位必须清除全部内部状态；`status()` 报告四态与进出计数 | 铁律 9 |

各阶段的调用顺序固定。`describe()` 可在任意时刻调用，包括在未 `configure` 的实例上调用；
其余按 `configure` → `attach` → `init` → `process`（多次）→ `flush` 进行，如图 4 所示。

```
 构造 ──► describe() ──► configure(params) ──► attach(observer) ──► init(rng)
                              │  参数校验失败即终止                      │
                              ▼                                         ▼
                          装载报错                          ┌──► process(in, out) ──┐
                                                            │      每轮调用一次      │
                                                            └───────◄───────────────┘
                                                                     │ 上游全部结束
                                                                     ▼
                                                                  flush(out) ──► 析构
```

**图 4  组件生命周期**

`describe()` 可在未 `configure` 的实例上调用这一性质决定了 `ScenarioSource` 的实现方式。
装载器导出目录时组件尚未配置，此时组件无法确定场景中辐射源的数量，因此端口列表为空，
目录中仅给出 `dynamic_ports` 声明，即 `{pattern: "link:<emitter_id>", source: "scene_binding"}`；
`configure()` 取得场景后，`outputs()` 才按场景中的辐射源给出具体的 `link:<emitter_id>` 端口。
`Graph::connect` 的执行时点在 `configure()` 之后，故可取得完整端口列表。

### 4.2 组件约束

以下三条约束写于 `component.h` 顶部。三者均为强制要求，违反任一条将破坏引擎的整体承诺。

1. **不得自带随机源。** 组件内不得出现 `rand()`、`std::random_device` 或任何静态随机引擎。
   随机数统一经 `init()` 注入的 `IRandom&` 获取。违反该约束将使同种子复现失效（铁律 9）。
2. **不得假设块长为定值。** 组件必须能够处理任意长度的输入块，包括末尾的短块。
   违反该约束将使计算结果与块长耦合，而块长是用户可在框图中修改的参数。
3. **不得静默降级。** 无法处理时应写明理由并标记四态，不得以默认值替代（铁律 15）。

### 4.3 参数描述

`ParamSpec` 是画布参数表单、装载器校验与目录导出三者共同的、也是唯一的信息来源，
其字段为 `name`、`type`、`unit`、`min`、`max`、`default`、`enum[]`、`required`、`description`、
`constraint`、`internal`、`excludes[]`。其中两个字段需要专门说明。

`internal` 标记由装载器解析注入、用户不应接触的参数，例如 `manifest_path`、`out_dir`、`scenario_path`。
画布不显示此类参数，且框图文件中出现该类参数即予拒绝，错误码为 `internal_param`。
该机制是「浏览器永不见服务器路径」（D-037）在引擎侧的执行点，不设例外。
全库现有内部参数 **11 处**：路径类两处（`FileReplaySource.manifest_path`、`ObservationTap.out_dir`），
以及 `ScenarioSource`、`SceneEmitterSource`、`SceneBoundChannel` 三者各自的三处
（`scenario_path`、`scenario_id`，加一个实体标识 `site_id` 或 `entity_id`）。

`excludes[]` 标记互斥参数。全库现有两对，均为 D-047 引入的功率单位互斥：`ToneSource` 的
`level_dBm` 与 `amplitude`、`NoiseSource` 的 `power_dBm` 与 `power`。互斥关系由注册表先行拒绝、
组件 `configure()` 二次校验，装载器映射为错误码 `param_conflict`。对应测试为 `test_components.cpp`
中的「合成源按 dBm 给功率：level_dBm / power_dBm 与线性参数互斥」。

### 4.4 注册表与目录自检

`Registry` 按类型名构造组件，`create_configured()` 等价于构造、`validate_params()` 与 `configure()`
三步的组合。目录导出时，`validate_catalog_entry()` 对每一条目逐项自检：类别必须属于六类之一、
M 层与 E 档必须合法、Coder 产物必须带有 `source_ref`、参数名不得重复、
既标记为必填又给出默认值的参数予以拒绝。最后一项的理由是该类参数的语义自相矛盾，
画布无法确定是否应予预填。

目录输出具备确定性：组件按类型名排序，不写入 `generated_at` 等随时间变化的字段。
仅在此前提下黄金基准方可逐字节比对。比对口径为「已有条目不变」，即允许新增组件，
不允许改动已有组件的端口或参数（铁律 10）。基准文件为 `tests/golden/component-catalog.json`，
对应测试为 `test_registry.cpp` 中的「内置注册表列出十二个组件，按名排序」与 `test_catalog_golden.cpp`。

综上，在组件描述仅有一份来源且目录导出具备确定性的条件下，画布、服务端与引擎三方不会对参数
产生不一致的理解。若将来为便利起见在前端另行维护一份组件清单，该项保证即告失效。

## 5. 框图 JSON 装载

本节说明框图 JSON 转换为可执行 `Graph` 的过程。装载是用户意图进入引擎的唯一入口，
也是多数错误的拦截点：装载阶段可报出确切的节点与端口，运行阶段则仅余数值问题。

装载器实现于 `engine/src/diagram_json.cpp`（915 行），分五步执行：

```
 ① 顶层结构      读取 schema_version / nodes / edges / observation_points / run / scenario_ref
                  未知键一律拒绝，避免拼写错误的字段被静默忽略
        ▼
 ② 逐节点        结构检查 → 参数分流（用户参数与内部参数）→ 数据与场景注入
                  → run 段派生量补齐 → 按注册表构造并 configure()
        ▼
 ③ 连线          逐条执行 Graph::connect()，失败分类映射为错误码
        ▼
 ④ 观测点并联    在目标 IQ 输出口后并联 ObservationTap，不改动用户绘制的边
        ▼
 ⑤ 图校验        graph.validate()：检查环与悬空输入口
```

### 5.1 关键设计取舍

**取舍一：解析结果写入旁挂文件，不改写框图副本。** `data_id` 与 `scenario_id` 的解析结果写入独立的
`cuav-resolved/1` 文件（`diagram.resolved.json`），框图副本始终仅含标识，不含任何路径。
若将解析结果写回副本，引擎必须开设「该副本可信、其中的内部参数允许存在」的例外，
D-037 中「引擎装载器同样拒绝」的规定随即出现例外，而例外一经设立即存在被扩大使用的风险。
装载器内置两个解析器：`MapDataResolver` 读取解析旁挂，用于服务端路径；
`IndexDataResolver` 直接按索引所在目录定位旁挂清单，用于 `cuav_run` 单机运行与回归测试。
对应测试为「解析器：IndexDataResolver 按索引所在目录定位旁挂清单并核对存在」。

**取舍二：`total_samples` 由时长补齐，显式取值不得超出时长。** 上限为
`cap = floor(duration_s × fs + 0.5)`。节点未给出 `total_samples` 时补为 `cap`；
给出且大于 `cap` 时报错误码 `duration`。舍入方向固定于此处，
以避免框图侧与引擎侧各取整一次而产生一个样点的偏差。

**取舍三：只校验模式不落盘。** `LoadOptions.out_dir` 为空时进入只校验模式，
观测点照常构造并校验参数，但不创建目录、不打开文件。为此 `ObservationTap` 对 `out_dir` 的检查
自 `configure()` 移至 `init()`（D-040 ②）。该设计使 `cuav_run --validate` 可在服务端同步执行
而不在磁盘上留下任何痕迹，实测耗时约 9 ms。对应测试为「装载器：只校验模式不给 out_dir，
观测点照常构造，运行时才在 init 被拒，盘上不留东西」。

### 5.2 场景注入

场景的引用方式与数据完全一致（D-049 ⑦）：框图中仅写 `scenario_id`，实际路径由解析旁挂中新增的
`scenarios` 段注入，或在单机运行时由 `cuav_run --scenario <场景文件>` 直接给入。

除路径解析外，装载器在此处执行四项检查。四项均遵循尽早拦截的原则。

1. **解析器缺失即报错**，不静默跳过场景绑定。
2. **哈希核对**：框图的 `scenario_ref.sha256` 必须与场景文件的原始字节哈希一致；
   场景自身的 `aoi.manifest_sha256` 必须与观测区域清单一致。任一项不符即拒绝。
3. **时长核对**：框图的仿真时长不得超过场景航线的总时长。若超出，后半段无人机将停留于末航点，
   在演示中表现为运行停滞。
4. **同一场景的绑定节点必须采用相同采样率**，否则参数帧到样点的映射将在不同节点上给出不同的边界。

上述检查置于装载期而非运行期，其理由是：运行期发现问题时任务已执行一段时间、产品目录已建立，
用户须等到运行结束才能看到错误。对应测试为「装载器：场景注入的四道闸——无解析器、哈希不符、
时长超出、绑定目标不存在」与「装载器：内部参数出现在框图里即拒，场景路径永远不进框图文件（D-037）」。

### 5.3 错误码与定位

04 §8.6 要求错误码能够映射到框图、模型与端口。装载器的错误报告为四元组
`{code, node_id, port, message}`，画布据此高亮出错的节点与端口。
错误码现共 19 个，全表见 `docs/diagram-format.md` §4，速查表见附录 A.3。
其中 `scenario` 与 `scene_binding` 两项与场景相关，前者为 D-049 随切片 ② 新增。
对应测试为「装载器：错误报文四键；文件装载对缺失文件与非法 JSON 报 json_parse」。

## 6. DAG 调度

本节规定图的执行方式。调度采用单线程按拓扑序轮转，每条边设一个深度 1 的缓冲。
该设计的简化是有意的，其依据为切片式推进的原则，即先确定语义再考虑吞吐；
但简化不等于随意，§6.1 的四条约定各自对应一类可能产生无声错误的情形。

拓扑序在装载完成后即确定，运行期不再变化。调度循环的骨架实现于
`engine/src/graph.cpp:146-287`，其结构如下：

```
for round in 0..max_rounds:
    for id in topological_order:
        if finished[id]: continue
        若该节点有输入口而缓冲未齐:
            若其全部上游已 finished  → flush(id)，标记 finished
            否则                     → 跳过，等待下一轮
        step = process(in, out)
        清空本节点的输入缓冲
        将 out 按边写入下游缓冲
    若本轮无任何节点前进且未全部结束 → 报告调度停滞
```

图 5 给出一条三节点链路前两轮的执行过程。

```
        轮 0                                  轮 1
  源     process → 产出块 A              源     process → 产出块 B
         写入 边(源→信道) 缓冲                   写入 边(源→信道) 缓冲  ← 覆盖，见约定四
  信道   缓冲已齐 → 处理块 A              信道   缓冲已齐 → 处理块 B
         清空输入缓冲                             清空输入缓冲
         写入 边(信道→观测点) 缓冲                写入 边(信道→观测点) 缓冲
  观测点 缓冲已齐 → 写出一行产品          观测点 缓冲已齐 → 写出一行产品
```

**图 5  三节点链路的两轮调度**

### 6.1 调度约定

**约定一：每条边设一个深度 1 的缓冲，键为 `<下游节点号>#<输入口名>`。**
深度 1 对单线程调度已足够，同时为 §4.2 的第二条组件约束提供强制力：
组件无法通过在端口上累积缓冲来规避块长无关的要求，因为端口不能容纳第二个块。

**约定二：`process` 执行后无条件清空本节点的输入缓冲。**
组件需跨块保留的数据必须存入自身成员变量，不得依赖端口保留。
§9.3 约束 B 中零阶保持须由消费端自行维护，其根源即在于此。

**约定三：结束语义依据上游集合判定。** 每个节点记录其上游节点集合；
仅当上游全部处于 `finished` 状态且本节点输入缓冲为空时，本节点才执行 `flush` 并结束。
缺少该约定时，上游停止产出后下游将持续等待不会到达的输入，调度进入假死状态。

**约定四：调度器每轮无条件将上游产出覆盖写入下游缓冲，不检查原有值是否已被消费。**
该行为实现于 `graph.cpp:241-249`，相同的覆盖语义在 flush 分支 `graph.cpp:207-215` 中再次出现：

```cpp
for (auto& kv : out) {
    if (!kv.second.has_data) continue;
    any_progress = true;
    for (const auto& e : edges_) {
        if (e.from == id && e.from_port == kv.first) {
            buffers[key(e.to, e.to_port)] = kv.second;   // 直接赋值，不检查旧值是否已被消费
        }
    }
}
```

> **约定四是 §9.3 全部约束的根源，须在此明确。** 对于具有多个输入口的节点，
> 只要任一输入口在本轮未取得数据，该节点整轮被跳过；而下一轮上游照常产出，
> 上一轮未被消费的块随即被静默覆盖。其结果是样点丢失且无任何报错，
> 同种子复现亦无法暴露该问题，因为两次运行丢失的是同一批样点。
> 当 `block_samples / fs < 1 / update_rate_Hz` 时，即高采样率、小块长、高更新率的组合下，
> 该情形几乎必然发生。对应的回归测试为 `engine/tests/test_channel.cpp` 中的
> 「场景参数源：每一轮都产出且至少一帧——这是调度上的正确性要求，不是优化」
> 与「场景参数源：与同参数的单音源在同一轮结束」。

停滞与轮数上限均报错，不静默退出。一轮内无任何节点前进表明图的结构存在问题；
超过 `max_rounds` 表明可能存在死循环。两种情形均给出明确错误，
而不返回一份不完整的结果。

运行结束后 `RunReport` 汇总 `{ok, error, rounds, state, notes, node_status[], node_names[]}`，
其中 `state` 取全部节点中最差的一档。

## 7. 背压与多线程机制

04 §8.3 将「执行分块、缓存、背压和流结束处理」列为运行引擎的职责之一，并指出首期可以采用
单计算节点多线程调度。本报告的结论是：首期不实现背压，亦不实现多线程调度。
本节给出该结论的依据、启用判据，以及为将来实现所预留的接口位置。

背压指下游处理速率低于上游产出速率时，由下游向上游反向传递处理能力信息、
从而约束上游产出速度的机制。缺少该机制时，中间缓冲将无界增长，最终耗尽内存。
本引擎当前不需要该机制：单线程拓扑轮转本身是同步的，调度器在同一轮内依次调用各节点，
上游不存在超前于下游运行的可能。

将来实现背压与多线程时，现有接口已预留三个位置，实现时均无需修改组件。

| 将来新增的机制 | 现有的预留位置 | 实现时需改动的部分 |
|---|---|---|
| 背压 | `Step::Idle` 已是合法返回值，其语义为本轮无产出且未结束。慢速消费者可据此使上游空转 | 调度器循环；组件不变 |
| 多线程 | 组件不假设块长、不持有全局状态、随机性经 `IRandom&` 注入。三条合并即构成组件为可迁移纯状态机的性质 | 调度器，以及将每条边替换为有界队列；组件不变 |
| 更深的缓冲 | 缓冲深度是 `run_impl` 的局部实现，未出现于任何组件接口 | 调度器；组件不变 |

启用判据为单线程吞吐成为演示或验收的瓶颈。当前实测数据如下（macOS 开发机，原型阶段验证值）：
切片 ① 的 2×10⁶ 样点链路墙钟耗时 0.14 s，约 14 MS/s；切片 ② 的 1.5×10⁷ 样点场景链路
（500 kS/s × 30 s，块长 25000，参数帧 20 Hz，600 轮）墙钟耗时 0.82 s，约 18 MS/s。
首期常用采样率为 5 至 50 MS/s 且为离线处理，按此速率，一分钟的 20 MS/s 数据约需一分半钟完成计算。

综上，在离线处理、单站单通道的首期边界内，现有吞吐可以接受，因此不实现背压与多线程。
若首期边界扩展至准实时或多通道，该结论需重新评估。本项列为 §15 暂定项 ①。

## 8. 多速率处理：时间基准与群时延

04 §8.3 要求运行引擎「在采样率变化时处理时间和群时延关系」。本节规定该处理的口径。

需先说明本节的性质：本节为先行规定，非实现记录。首期引擎内不含任何重采样组件，
`ADC`、`DDC`、`Channelizer` 均未实现，属 M-2 与 M-3 的范围，排期在切片 ④。
现有组件遇跨速率连接时一律由消费端拒绝：`AddMixer` 在两路采样率不同时返回 `Step::Error`，
`SceneBoundChannel` 与 `ObservationTap` 在输入采样率中途变化时报错。
本节规定上述组件实现时必须遵守的口径，先行确定以避免三个组件各自实现一套。

两个概念需先行界定。多速率指同一张图中不同的边可以具有不同的采样率，
典型情形为 100 MS/s 的宽带输入经 DDC 抽取至 5 MS/s 的窄带输出。
群时延指滤波器引入的整体延迟量；对线性相位、抽头偶对称的 FIR 滤波器，
群时延为与频率无关的常数，等于 `(N−1)/2` 个输入样点，N 为抽头数。
群时延是已知常数而非待估计量，该性质是口径二成立的前提。

**口径一：样点序号是唯一需要换算的量。** 抽取 D 倍的组件，其输出块必须满足

```
start_sample_out = (start_sample_in − group_delay_in_samples) / D
sample_rate_out  = sample_rate_in / D
```

除不尽时报错。组件不得先将 `start_sample` 换算为 `t_s`、计算完成后再乘回，
因为浮点往返将引入亚样点漂移，破坏与参考实现的逐位对拍。该口径与 §2.1 不变量二、
§9.2 的整数映射同属一条原则。

算例如下。输入采样率 100 MS/s，抽取 20 倍至 5 MS/s，FIR 抽头数 N = 161，
群时延为 `(161−1)/2 = 80` 个输入样点。设某输入块的 `start_sample_in = 2000000`，
则 `start_sample_out = (2000000 − 80) / 20 = 99996`，输出采样率为 5 MS/s。
换算全程为整数运算，在不同机器上必然得到相同结果。若 N 取 160，
群时延为 79.5 个输入样点而非整数，此时封装层应拒绝该抽头组合，理由见口径二。

**口径二：群时延显式声明，并在封装层扣除。** Coder 生成的 C 代码仅执行滤波运算，
群时延由封装层自 `start_sample` 中扣除，使输出块的 `start_sample` 指向对应的输入时刻，
而非滤波器输出该批样点的时刻。若抽头数使群时延不为整数样点，封装层应拒绝该 `fir_version`，
不作半样点插值，因为首期不引入分数延迟器。

**口径三：铁律 4 的检查置于封装层，不置于 Coder 产物内。** `|Δf| + B/2 + 保护带 < Fs/2`
与抽取前必须抗混叠两项属引擎侧的入参校验，以 `ParamSpec.constraint` 声明、由 `configure()` 执行。
生成的 C 代码仅执行运算，不校验合法性，使同一份 C 代码可在不同参数约束下复用。

**口径四：变更采样率的组件须记录溯源信息。** 该组件必须在输出块的 `state_reasons` 中写入一条说明，
该条目属记录性质而非降级；同时须将 `trace.parameter_version` 更新为含 `fir_version` 的取值，
使下游与产品清单能够区分同一 DDC 组件的不同抽头版本。

上述四条口径的首个使用者为 M-2 的 `DDC`。口径二列为 §15 暂定项 ②，待 M-2 实现后按实测确认。

## 9. 参数帧与 IQ 时钟对齐

本节是本报告技术密度最高的一节，亦是被外部引用最多的一节：`geo/`、`engine/`、
`docs/scenario-format.md` 与前端共有十余处注释按 §9.2、§9.3、§9.4 引用本节。
本节所解决的问题是：场景侧每秒仅变化数十次的量，如何施加至每秒数千万样点的 IQ 流上，
并保证同种子逐字节复现。

本节在 v1.0 时为先行设计，G-1 至 G-3（2026-09-06）按其实现后回填为实现记录。
实现落点为 `geo/src/{geodesy,kinematics,link_budget,scenario}.cpp`、
`engine/src/{scenario_json,scenario,channel}.cpp` 与 `web/src/scene/editor/preview.ts`。

### 9.1 问题

场景侧的量，即链路几何、路损、多普勒、时延与噪声底，属慢变量，更新率为 10 至 100 Hz；
IQ 属快变量，首期为 5 至 50 MS/s。两者相差五至六个数量级。
将慢变量施加至快变流上有三种可能做法，其中仅一种成立。

| 做法 | 评价 |
|---|---|
| 组件按墙钟取当前场景状态 | 不成立。墙钟与调度速度耦合，同种子两次运行结果不同，违反铁律 9 |
| 每个样点重新计算一次几何 | 不成立。慢变量按定义为分段常数，逐样点重算既增加开销，又在参数帧之外引入第二套真值 |
| 参数帧按样点序号推进，帧内零阶保持 | 成立。帧边界为样点序号的函数，与块长、调度顺序及机器速度均无关 |

第三种做法即 D-033 与 `docs/scenario-format.md` §7 已冻结的语义，本节将其落实至可实现的粒度。

### 9.2 帧到样点的映射

第 k 帧（k 自 0 起）覆盖的样点区间为半开区间

```
[ floor(k · fs / rate) , floor((k+1) · fs / rate) )
```

其中 `fs` 为该链路的 IQ 采样率，`rate` 为 `update_rate_Hz`，取值范围 `[10, 100]`，越界即拒绝。
块边界与帧边界的对齐关系如图 6 所示。

```
 样点序号 ──────────────────────────────────────────────────────────────►
           0        25000      50000      75000     100000     125000
           │          │          │          │          │          │
 参数帧    ├── 帧 0 ──┼── 帧 1 ──┼── 帧 2 ──┼── 帧 3 ──┼── 帧 4 ──┤
           │  增益 g0 │  增益 g1 │  增益 g2 │  增益 g3 │  增益 g4 │   ← 帧内零阶保持
           │          │          │          │          │          │
 IQ 块     ├─ 块 A ───┴──┬─ 块 B ─┴────┬─ 块 C ──┴───┬── 块 D ────┤
           0          30000        65000        95000       125000
              ↑            ↑            ↑             ↑
           跨 2 帧      跨 2 帧      跨 2 帧       跨 2 帧
                              （fs = 500 kS/s，rate = 20 Hz，一帧 25000 样点）
```

**图 6  参数帧与 IQ 块的边界对齐关系**

图 6 所示为块边界与帧边界互不对齐的一般情形，亦为实际运行时的常态。
一个块可能跨越若干帧，若干个块也可能落于同一帧内，两种情形均须处理。

映射采用整数除法而非先计算 `t = k / rate` 再乘以 `fs`，其原因如下。后者在 `fs / rate` 除不尽时
存在问题：设 `fs = 500000`、`rate = 30`，一帧为 16666.67 个样点，以浮点计算边界时，
C++ 侧与浏览器 TS 侧的舍入结果可能相差一个样点，而航迹对拍的容差为 1e-6 度（约 0.1 米），
一个样点的偏差即超出容差。整数形式在两侧均可逐位复现。
对应实现见 `engine/src/scenario.cpp:37` 的注释「用秒做中间量就会在边界上差一个样点」。

帧所上报的 `valid_from_s = k / update_rate_Hz` 与 `valid_to_s = (k+1) / update_rate_Hz`
为供显示与回放使用的派生量。施加参数时统一使用样点区间，不使用上述两个量。

### 9.3 参数帧在端口上的传递

`PortData` 中承载参数帧的字段为 `std::vector<SceneParamFrame> scenes`（§3.2）。
该字段必须为向量而非单帧，理由已由图 6 给出：块长可能大于一帧覆盖的样点数，此时一个块需配多帧；
也可能小于，此时多个块共用一帧。单帧字段仅能表达后者。

**生产端约定**（`ScenarioSource`）：每一轮产出恰好覆盖本轮样点窗口的全部帧，且至少一帧。
本轮窗口为 `[k·B, min((k+1)·B, total))`，其中 B 为块长，`total` 为总样点数；
末块长度不足时适用同一规则。

> **该约定是正确性要求，非性能优化。** 其根源为 §6.1 约定四：调度器每轮无条件将上游产出覆盖写入
> 下游缓冲（`engine/src/graph.cpp:241-249`）。只要 `ScenarioSource` 有一轮未产出，
> `SceneBoundChannel` 即因 `scene` 口无数据而整轮被跳过，
> 下一轮的 IQ 块随即将上一轮未被消费的块静默覆盖。其结果是样点丢失且无任何报错，
> 同种子复现亦无法暴露该问题。当 `block_samples / fs < 1 / update_rate_Hz` 时，
> 即高采样率、小块长、高更新率的组合下，该情形几乎必然发生。
> 对应的回归测试为 `engine/tests/test_channel.cpp` 中的
> 「场景参数源：每一轮都产出且至少一帧——这是调度上的正确性要求，不是优化」
> 与「场景参数源：与同参数的单音源在同一轮结束」。

**消费端约定**（`SceneBoundChannel` 等施加类组件）共六条，按字母编号。

**约束 A：收帧时仅追加晚于队尾的帧。** 重复发送的同一帧因此被自动去除，
生产端可在无新帧时重发上一帧以满足每轮至少一帧的要求。

**约束 B：消费端必须自行保留上一帧。** §6.1 约定二规定 `process` 执行后输入缓冲被无条件清空，
故跨块的零阶保持只能由消费端在自身成员中维护，不得依赖端口保留。

**约束 C：帧的定位采用只前进的游标逐样点推进，不采用 `k = floor(t·R)` 的除法取整。**
除法取整的问题在于：当 `t` 恰落于帧边界时，浮点误差可能将其归入上一帧，
而上一帧可能已被丢弃，从而取得空指针或更早的帧。只前进的游标不会回退，因而不存在该问题。
对应实现与注释见 `engine/src/channel.cpp:134`。

**约束 D：帧内不作插值。** 零阶保持是语义规定而非近似手段。若在帧之间作线性插值，
所得曲线虽更平滑，但将与 `link` 事件上报的读数、与场景视图显示的路损不一致，
系统内将出现两套相互矛盾的真值。

**约束 E：所收帧无法覆盖本块的样点范围时报错，不得以最近的帧替代**（铁律 15）。
该约束在源节点块长不一致、采样率不一致、场景时长短于框图时长三种情形下立即报错，
而不产生一段错误但表面正常的数据。对应测试为「场景绑定信道：scene 口一帧都没有即报错，
不拿默认值顶替」。

**约束 F：帧过期而无新帧到达时（正常结束除外），按上一帧继续，同时写入 `state_reasons`
并将块标记为 `Degraded`。** 对应测试为「场景绑定信道：帧过期而无新帧时按上一帧继续，
但把块标降级并写明理由」。

### 9.4 源节点间的同步

一条场景链路含两个源节点：`SceneEmitterSource` 产出 IQ，`ScenarioSource` 产出参数帧。
两者必须逐轮对齐，否则将触发 §9.3 约束 E 的报错。

约定为：两者均声明 `sample_rate_Hz`、`total_samples`、`block_samples` 三个参数，
由装载器的 `fill_run_derived()` 自同一个 `run` 段补齐，其中 `total_samples` 由 `duration_s × fs`
得出，`block_samples` 由 `run.block_size` 得出。自同一份 `run` 推导的三个量必然一致，
两个源因此自动同步。

尽管如此，信道仍须核对所收 IQ 块的 `meta.sample_rate_Hz` 与其依据场景计算的预期值是否一致，
不一致即报错。其理由是：用户可在框图中为单个节点显式指定不同的 `block_samples`，
自动一致不等于不会被人为破坏。`AddMixer` 对两路输入设有相同的检查。
对应实现见 `engine/src/diagram_json.cpp:349` 的装载期检查，
以及测试「场景绑定信道：三道守法——拒回放数据、拒采样率中途变、拒块不连续」。

### 9.5 施加范围与边界

首期施加三个量，均在 `SceneBoundChannel` 内完成。

| 量 | 施加方式 | 注意事项 |
|---|---|---|
| 增益 | `G_dB(t) = tx_power_dBm + G_t + G_r − path_loss_dB(t)`，幅度乘以 `10^(G/20)` | 输入必须是归一化至单位功率的波形（§9.6），否则电平将被重复计入 |
| 多普勒 | 相位斜坡 `φ += 2π·f_d/fs`，相位累加器跨块连续并按 2π 归约 | 每块重置相位将产生周期性相位跳变，谱上表现为假边带 |
| 时延 | 整数样点移位，历史缓冲长度取 `ceil(max_delay·fs)+1` | 时延变化时样点的丢弃与补充须显式记录；首期不实现分数延迟器 |

**不施加的量为噪声底。** `SceneParamFrame.noise_floor_dBm_per_Hz` 首期仅上报、不驱动噪声源，
由其驱动的噪声注入组件排期在切片 ④。该安排使切片 ② 的链路仅含一个可变量，即距离，
验证功率随距离变化时无需区分两个来源。

**帧的 `path_loss_dB` 仅包含纯传播损耗。** 发射功率与收发天线增益三项在首期为全程常量，
作为施加类信道的内部参数由装载器自场景注入，不并入帧内。若将其并入帧，
`link` 事件与场景视图所显示的路损读数将名实不符。拍板记录见 §15 暂定项 ⑧。

**信道不改变流长度。** 输出块的 `start_sample` 与样点数与输入块严格相同，
时延经历史缓冲实现，全流最前部的 D 个样点为精确零，`flush()` 不产出数据。
观测点的行索引、频谱的帧对齐，以及读端按 `row_index × row_len × 4` 定位的偏移读法，
均建立于该性质之上。对应测试为「场景绑定信道：整数样点时延用冲激响应测，前 D 个样点精确为零」
与「场景绑定信道：跨块的时延靠上一块的尾巴回看，块长换了结果不变」。

整数样点时延在 `delay_s` 变化处存在 ±1 个样点的跳变。该跳变属已声明的模型简化（E2 级），
不标记为降级：将其标记为 `Degraded` 将使整个任务的结果四态变为降级，从而掩盖真正的降级信号。
该跳变的次数记录于 `status().notes`。单站场景中绝对时延在频谱上不可观测，
其意义在于为将来的多站与到达时间差定位预留结构。

**回放数据不得绑定场景。** `SceneBoundChannel` 遇到 `time_basis = FileAcquisition` 的输入块即报错。
该检查是 06 备忘录防线二、三在引擎侧的执行点，其强度高于仅依赖组件目录中的
`scene_bindable = false`：目录属性可被绕过，运行期的类型检查不可绕过。

### 9.6 单位功率归一化口径

`tx_power_dBm` 在工程上指发射期间的功率。故场景绑定源的归一化按发射期间的瞬时平均功率为 1 mW 定义。

| 波形 | 归一化 |
|---|---|
| `tone` | `|x|² ≡ 1` |
| `noise` | `E|x|² = 1` |
| `burst` | 导通区间内 `|x|² = 1`，断开区间精确为零 |

由此，观测点上导通期间所读取的即为 `Prx = tx_power_dBm + G_t + G_r − L`，与频谱仪的读数口径一致。

若改按占空比平均归一化，瀑布上的突发峰值将比链路预算低 `10·log10(1/duty)`，验证时无法对应。
该差异不属精度问题而属口径错误：两种归一化各自自洽，但仅前者可使链路预算的计算值
与瀑布的读取值直接比较。对应测试为「场景辐射源：发射期间平均功率恰为 1 mW（0 dBm），
这是链路预算能直读的前提」与「场景辐射源：突发按 ON 态归一，图案与块长无关，断开区间精确为零」。

### 9.7 实测结果（macOS，原型阶段验证值）

切片 ② 的示例框图 `engine/tests/diagrams/slice2_scenario_link.json`（500 kS/s × 30 s = 1.5×10⁷ 样点，
块长 25000，参数帧 20 Hz）运行 600 轮，墙钟耗时 0.82 s，产出 3892 行产品、
300 条 `entity` 事件与 300 条 `link` 事件。验证结果如下表。

| 验证项 | 结果 |
|---|---|
| 绝对链路预算（包络 rms 对 `tx_power + G_t + G_r − 路损`） | t = 5 / 15 / 29 s 三点偏差 −0.021 / +0.006 / +0.001 dB |
| 两时刻带内功率差对几何路损差 | 实测 6.017 dB 对几何 5.943 dB，偏差 0.074 dB（切片 ② 容差 0.5） |
| 浏览器预览对 C++ 航迹 | 361 个样点逐点相同（容差 1e-6 度，实测差 0） |
| FSPL 解析锚点 | `fspl_dB(1000, 2.4e9) = 100.052008 dB`，时域与峰值 bin 两种口径均在 ±0.01 dB 内 |
| 多普勒 | ±1 kHz 峰值频移 8 个 bin（fs 1 MHz、nfft 8192、bin 宽 122.07 Hz），符号正确 |

上表中有一处读数口径需专门说明，否则复核时结果无法对应。hann 窗下将谱线邻域的 bin 在线性域求和，
所得为单音功率的 1.5 倍，即等效噪声带宽带来的 `10·log10(1.5) = 1.761 dB`。
因此读取绝对电平时，应读峰值 bin（且频偏须落于 bin 中心）或采用时域功率；
而带内功率适用于比较差值，因为该常数因子在相减时精确抵消，且不受多普勒引起的扇贝损耗影响。
切片 ② 的验收断言即按带内功率的差值计算，取峰值邻域 ±5 bin 在线性域求和。
其理由是多普勒使谱线在 ±1.2 个 bin 范围内移动，hann 窗的扇贝损耗最坏为 1.42 dB，
按单 bin 峰值断言无法稳定满足 0.5 dB 的容差。

### 9.8 场景观测量的上报通路

实体状态与链路读数经 `IRunObserver::on_entity` 与 `on_link` 两个回调上报，不设端口类型（D-033）。
判据为：端口承载参与下游计算的数据流，而实体位置与链路读数属供显示用的观测量，
无下游组件消费。若将其实现为端口，每张场景框图都须绘制两条无接收方的连线。

`cuav_run` 将上述两个回调转换为 `entity` 与 `link` 事件，同时写入 `track.jsonl` 与 `links.jsonl`
两份产品文件，供任务结束后回看。两条硬性契约如下，B-7 的读取层已按此实现：
每行必须包含 `t_s`，且必须以换行符结尾，否则将被读端作为不完整记录丢弃。

## 10. 观察者、观测点与产品文件

本节规定引擎输出结果的方式。输出有两条通路：观察者为运行期的旁路，将状态与显示产品实时推送至外部；
观测点为框图上的抽头，将流经的 IQ 归约为定长行产品并写盘。两者服务于不同需求，均不属于数据流。

### 10.1 观察者

04 §8.3 要求引擎「将运行状态、日志摘要和降采样显示产品发布给应用服务」。
上述内容均不属数据流，故不实现为端口，而实现为一组回调，
定义于 `engine/include/cuav/observer.h:46-58`：

```cpp
on_progress(const ProgressInfo&)                        // 每轮结束时；运行器按墙钟节流后再发事件
on_entity(const EntityState&)                           // 场景运行时
on_link(const LinkFrame&)                               // 场景运行时
on_log(const std::string& level, const std::string& message)
on_product_row(const std::string& op_id, const std::string& kind,
               std::uint64_t row_index, const float* row, std::size_t len, double t_s)
```

五个回调均提供空的默认实现，观察者本身亦为可选：`Graph::run` 在未传入观察者时行为完全不变，
使单元测试无需为运行一条链路而先行构造观察者。挂接点为 `IComponent::attach(IRunObserver*)`，
由 `Graph::run_impl` 在 `init` 之前对每个节点调用一次。

### 10.2 观测点

观测点对应 04 §5.4 定义的内部观测点 S0–S6，其中 S4，即 DDC 后、信道化前的宽带复基带 IQ，
为本系统的主数据产品。在框图中，观测点写于 `observation_points[]` 数组而非绘制为节点；
装载时引擎在目标 IQ 输出口后并联一个 `ObservationTap`（图内名为 `op:<id>`），
不改动用户绘制的边。采用并联而非串联的理由是：增加或移除观测点不应影响链路本身的拓扑与计算结果。

观测点写出两种定长行产品。

| 产品 | 行格式 | 口径 |
|---|---|---|
| `spectrum.f32` | 每行 `nfft` 个 float32 | `10·log10(max(power, 1e-30))`，已经 fftshift 使零频居中。与 `SpectrumAnalyzer` 共用 `WelchAccumulator`，两者逐位同源 |
| `envelope.f32` | 每行 3 个 float32 | 桶内 `|x|` 的 `min`、`max`、`rms`，其中 `rms = sqrt(Σ|x|²/n)` |

按 D-047，功率单位为 mW，故谱值直接即为 dBm，索引中记录 `scale = dBm` 与 `calibration` 块；
包络的单位为 `sqrt_mW`。观测点索引谱的 dBm 锚点由 `test_spectrum.cpp` 中的
「dBm 锚点：-70 dBm 单音峰值读 -70.00 dBm，-104 dBm 噪声在 1024 点 hann 下底噪均值 -132.34 dBm」保证。

### 10.3 产品写盘约定

以下四条约定均源于实测中发现的问题。

1. **行定长追加，逐行 `fflush`。** 读端按 `row_index × row_len × 4` 定位，偏移量为一次常数乘法。
   逐行刷新使运行中的任务亦可被读端取得已完成的行。
2. **索引先写 `.tmp` 再原子替换。** 读端或读到旧索引，或读到新索引，不会读到写入中途的文件。
   原子替换经 `platform.h` 的 `atomic_replace` 实现（§12）。
3. **写完第一行即刷新一次索引**，其后每 64 行刷新一次，收尾时再刷新一次。
   原实现为累积满 64 行后才写索引，导致任务开始后的一段时间内索引不存在，
   读端无法取得 nfft、带宽等几何参数，因而无法建立坐标轴（D-046 ③）。
   对应测试为 `test_tap.cpp` 中的「观测点：写完第一行就刷一次索引（B-7 读端靠它拿到几何参数，D-046）」。
4. **读端以文件长度确定行数**，索引中的 `rows` 仅用于判断是否已收尾。索引滞后于文件属正常状态。

**收尾语义**：末行段数不足、末桶样点数不足、尾部样点被丢弃三种情形均记录于 `notes` 但不降级，
因为三者属正常的边界行为而非数据问题。真正的降级情形，例如回放数据未标定，
由 `state_reasons` 承载。同种子复现由 `test_tap.cpp` 中的
「观测点：同种子两次运行，产品文件逐字节相同」保证。

### 10.4 场景运行时的 JSONL 产品

自切片 ② 起，场景链路另行输出两份按行写入的 JSON 产品：`track.jsonl` 记录实体位置与姿态，
`links.jsonl` 记录链路读数，包括距离、方位、俯仰、路损、时延与多普勒。
两者与二进制产品的区别在于每行为一条完整 JSON 而非定长记录，故读端按行解析而非按偏移定位。
两条硬性契约见 §9.8。

## 11. 种子与溯源

本节规定两项保证结果可信且可追溯的机制：随机性的管理方式，以及数据产物的身份标识。

### 11.1 随机性管理

铁律 9 要求随机性显式注入、固定种子逐位复现。不允许组件自行调用 `rand()` 的理由是：
该方式使计算结果取决于组件的构造顺序、调用次数与运行时库的实现，
三者均未记录于框图，用户无从控制，亦无从复现。

库内不设全局随机源。组件经 `init()` 注入的 `IRandom&` 获取随机数，该接口仅含四个方法：
`next_u64`、`uniform`、`normal`、`complex_normal`，实现为库内自带的 `Xoshiro256pp`
与 Box–Muller 变换。两项标准库设施不予采用：`std::random_device` 按定义不可复现；
`std::normal_distribution` 的标准仅规定分布而未规定算法，各平台实现不同，无法逐位对拍。
对应测试为 `test_golden.cpp` 中的「黄金基准：随机源本身逐位可复现」。

种子取自框图的 `run.seed`。`cuav_run --seed` 可覆盖该取值，覆盖时必须写入 `seed_source = cli`
并发出一条 `log` 事件，使产品的溯源信息中留有本次运行未采用框图种子这一事实（D-041 ⑤）。

复现的对象是产品文件而非事件流。`progress` 事件按墙钟节流，默认间隔 100 ms，
故两次运行的事件流不会逐字节相同，而产品文件作此承诺。该区别须在报告中明确说明，
以避免将两次运行的 `events.jsonl` 差异误判为缺陷。

随机流与节点书写顺序的耦合属已知技术债。调度的拓扑序等于框图中节点的书写顺序，
故多个噪声源共用一个 `IRandom` 时，调换节点在 JSON 中的先后即改变各自获得的样点序列。
`SceneEmitterSource` 的处理方式是在 `init()` 中取一个 `u64` 建立私有子流，
将耦合降低至 `init` 顺序这一级。`NoiseSource` 不作修改，因其已有黄金基准，
改动将破坏既有基准（铁律 10）。按节点 id 派生子流的彻底解法记为技术债，见 §16.2。
拍板记录见 §15 暂定项 ⑨。

### 11.2 溯源

溯源八件套，即 04 §9.2 的六件套加 01 §3.1 要求的 `model_layer` 与 `credibility`（D-012），
挂载于 `BlockMeta.trace`，随块传递。各字段的填充责任如下表。

| 字段 | 填充方 | 说明 |
|---|---|---|
| `model_id`、`model_version`、`model_level`、`model_layer`、`credibility` | 组件 | 取自 `describe()` 中的同名字段，两处不得不一致 |
| `parameter_version` | 组件 | 按其参数集版本；变更 `fir_version` 等情形时须更新（§8 口径四） |
| `confidence` | 组件 | 未给出时填负值，不得以 0 替代。0 是合法的置信度取值，以其表示未填将使下游无法区分 |
| `trace_id` | 组件 | 数据引用类组件填 `data_id`；场景绑定类组件填 `scenario_id` 与实体标识 |

对应测试为 `test_types_graph.cpp` 中的「溯源八件套不全就是不全」。

## 12. 共享内存抽象层

本节说明 04 §6.3 相关条款与首期实现之间的关系，并交代平台差异的收敛位置。

04 §6.3 规定「应用服务和运行引擎采用粗粒度任务接口，本机高吞吐组件仍使用同进程、共享内存或
内存映射文件」，§6.5 进一步指出「高吞吐 IQ 链应尽量在同一进程或共享内存域内完成，
以避免跨进程复制成为性能瓶颈」。

首期的结论是：组件与调度器同进程，进程内直接传递 `Block`，不实现共享内存。
D-031 已将该条款的释义写入决策：04 所述同进程指组件与调度器同进程，不指将引擎嵌入 Node。
其依据为，引擎本身即为子进程，其与应用服务之间的交换经文件与事件完成（§1），
而真正需要共享内存的情形是多个进程访问同一份 IQ，首期不存在该情形。
04 §6.5 所关注的跨进程复制瓶颈在首期不会出现，因为 IQ 自始至终仅在 `cuav_run` 单一进程内流动。

平台差异全部收敛于 `platform.h`，现含五个函数。

| 函数 | 用途 |
|---|---|
| `make_dirs(path, err)` | 递归创建目录，用于产品目录 |
| `atomic_replace(tmp, dst, err)` | 原子替换，用于索引写盘（§10.3 约定 2） |
| `is_little_endian()` | 字节序判断 |
| `join(a, b)` | 路径拼接 |
| `utc_now_iso8601()` | 时间戳，写入产品元数据 |

将来增加共享内存或内存映射时，其实现位置亦在此处。铁律 16 要求全部 `#ifdef _WIN32`
仅出现于 `platform.cpp`，使引擎移植至 Windows 或 Linux 时需审查的文件仅有该一个，共 106 行。

字节序方面，交换格式固定为小端且不作自动探测（D-020）。`is_little_endian()` 的作用是确认运行环境，
在大端机上使观测点直接拒绝运行，而非静默写出字节序错误的产品文件。

## 13. Coder 产物的组件封装规范

本节规定 MATLAB Coder 生成的 C 代码转换为引擎组件的方式。与 §8 相同，本节为先行规定：
首个使用者为 M-2 的 `DDC`，尚未开工。先行确定规范的目的是避免三个 DSP 组件各自实现一套封装。

背景如下。MATLAB Coder 是将 `.m` 代码生成为 C 或 C++ 的工具。D-036 确定的路线是：
滤波、DDC、信道化三类 DSP 组件采用 MATLAB Coder 生成 C，
手写 C++ 仅用于运算简单的组件（ADC 量化削顶、噪声注入、混合、观测量归约）与引擎骨架。
该划分的依据是：DSP 组件的正确性依赖大量已经过验证的工具箱算法，重新实现等于重新承担一次验证成本；
而运算简单的组件采用 Coder 反而增加构建复杂度。

Coder 产物是组件的内部实现，而非组件本身。三层分工如下表。

| 层 | 编写方 | 职责 |
|---|---|---|
| `matlab/ref/*.m` | 人工编写，MATLAB 执行 | 参考模型，标记 `%#codegen`；同时产出黄金向量 |
| `models/<环节>/coder/*.{c,h}` | Coder 生成 | 纯算法。不作参数校验、不写日志、不访问文件 |
| `engine/src/<组件>.cpp` | 人工编写 | 封装层，五项职责见下 |

封装层的五项职责如下。

1. **参数校验与铁律 4 检查**：`|Δf| + B/2 + 保护带 < Fs/2`，以及抽取前必须抗混叠。
   以 `ParamSpec.constraint` 声明，由 `configure()` 执行（§8 口径三）。
2. **样点序号换算与群时延扣除**，见 §8 口径一与口径二。
3. **状态管理**：Coder 产物按分块带状态方式生成，采用固定尺寸接口并禁止动态内存，
   状态结构体由封装层持有。`reset()` 必须将其完全清除，否则同种子两次运行结果不一致。
4. **溯源填充**：`implementation` 置为 `coder`，`source_ref` 为必填，
   且必须包含来源 `.m` 的路径、MATLAB 与 Coder 的版本、codegen 参数哈希。
   目录导出时校验该项，缺失即拒绝（§4.4）。
5. **四态传播**：Coder 产物不处理四态，由封装层负责将输入块的状态向下传递并按需降级。

codegen 配置为：`coder.config('lib')`、C 语言、固定尺寸接口、禁止动态内存分配、分块带状态。
目标编译器在 macOS 上为 clang，在 Windows 上为 MSVC（铁律 16：Windows x64 优先）。
生成的 C 代码进入仓库并入库，不在构建时调用 MATLAB，因为交付环境不含 MATLAB，
持续集成亦不应依赖 MATLAB。

等价性判据为：Coder 组件与 MATLAB 黄金向量的相对误差不大于 1e-9（铁律 10）。
三方互证时以 Python 参考实现作为独立的第二实现。
`source_ref` 的填写示例随 M-2 补入 `docs/component-catalog.md` §7。

## 14. 与上游方案的逐条对照

本节将 04、03、01、05 中与运行引擎相关的条款逐条列出，标明兑现该条款的章节及首期的实施状态。
未兑现的条款均给出去向，不作省略。

| 上游条款 | 要求 | 本报告与实现 | 首期状态 |
|---|---|---|---|
| 04 §5.2 | 主产品为 S4 观测点，位于 DDC 后信道化前 | §2、§10.2；`ObservationTap` 可挂接任一 IQ 输出口 | 实施 |
| 04 §5.4 | 内部观测点 S0–S6 按需暴露 | §10.2；首期仅实现 S4 口径的抽头，S0–S3 与 S5 待链路补齐后即可挂接 | 部分 |
| 04 §8.3 | 有向数据流与分块 IQ 处理，块携带元数据 | §0.1 定义、§2 结构 | 实施 |
| 04 §8.3 | 按拓扑顺序调度模型 | §6 单线程轮转 | 实施 |
| 04 §8.3 | 检查采样率、中心频率、通道数与数据格式 | §2 块自描述、§9.4 信道核对、`AddMixer` 两路一致性检查 | 实施（通道数：首期单通道） |
| 04 §8.3 | 执行分块、缓存、背压与流结束处理 | 分块与缓存见 §6；流结束见 §6.1 约定三；背压首期不实现，预留位置见 §7 | 部分 |
| 04 §8.3 | 模型初始化、运行、复位、销毁 | §4.1 六项生命周期，`destroy` 由析构承担 | 实施 |
| 04 §8.3 | 管理随机种子与确定性复现 | §11.1 | 实施 |
| 04 §8.3 | 记录模型版本、参数、耗时与异常 | §11.2 溯源八件套；耗时见 `RunReport` 与 `progress` 事件 | 实施 |
| 04 §8.3 | 支持文件流与内存流 | 文件流为 `FileReplaySource` 与观测点产品；内存流为进程内直接传递 `Block`（§12） | 实施 |
| 04 §8.3 | 将运行状态、日志摘要与降采样显示产品发布给应用服务 | §10.1 观察者五个回调 | 实施 |
| 04 §8.3 | 采样率变化时处理时间与群时延关系 | §8 四条口径；首期无重采样组件，跨速率连接一律拒绝 | 先行规定（M-2 实施） |
| 04 §8.3 | 避免无必要的 IQ 内存复制 | §6 边缓冲按值赋值一次；§12 进程内直接传递，不跨进程复制。尚未实施零拷贝优化，列入 §16.1 | 部分 |
| 04 §8.3 | 任务失败时保留可诊断信息 | 附录 A.2 退出码与四元组错误报文；`events.jsonl` 全量留档 | 实施 |
| 04 §8.4 | 组件至少声明的十二项 | §4.1 `ComponentInfo` 与 §4.3 `ParamSpec`；GPU 需求与许可说明首期留空 | 部分 |
| 04 §8.6 | 错误码映射至框图、模型与端口 | §5.3；四元组 `{code, node_id, port, message}`，19 个码 | 实施 |
| 04 §8.6 | 稳定标识符，不暴露服务器路径 | §1 边界、§4.3 `internal` 参数；D-037 | 实施 |
| 04 §9.2 | 最小元数据随产物传递 | §2 `BlockMeta`、§11.2 溯源八件套 | 实施 |
| 04 §12 | 常用 5–50 MS/s，100 MS/s 仅用于文件兼容与短窗 | §0.1、§7 吞吐判据与实测 | 实施 |
| 04 §13.3 | 宽带与信道化 IQ 可被外部算法读取 | §10 产品文件；`docs/display-products.md` | 部分（信道化待 M-3） |
| 04 §15.2 | 十二项标准算例 | 第 1、2、9、10 项已有组件与单测；其余待切片 ④ | 部分 |
| 03 §11.6 | 十一种标准数据端口 | §3；首期六种 | 部分 |
| 03 §11.7 | 组件生命周期与参数注入 | §4.1 | 实施 |
| 03 §11.3.1、§11.4 | MATLAB 为内部工具，Coder 生成 C | §13 | 先行规定（M-2 起） |
| 01 §3.1 | 同时声明 M / E / V | §4.1 `ComponentInfo`、§11.2 溯源 | 实施 |
| 05 P0 | 端口命名预留 `ArrayIQStream` 等八项 | 本期仅作命名预留，不实现；`ChannelPathSet` 已占端口类型位 | 预留 |

## 15. 暂定项与拍板记录

下表列九项，其中三项已在切片 ② 的实施过程中拍定，六项待评审或用户拍板。
拍板后应修改本表并在 §17 记录一次修订，不另行建立文档。

| # | 项 | 倾向方案 | 拍板 | 落点 |
|---|---|---|---|---|
| ① | 背压与多线程的实施时机 | 首期不实施。判据为单线程吞吐成为演示或验收的瓶颈，接口已预留三个位置 | 待定 | §7 |
| ② | 多速率群时延口径 | 线性相位 FIR 按 `(N−1)/2` 个输入样点计，由封装层自 `start_sample` 扣除；使群时延非整数样点的抽头组合予以拒绝，不实现分数延迟器 | 待 M-2 实测确认 | §8 |
| ③ | `ChannelPathSet` 是否补充结构体 | 首期仅占端口类型位。单径与双径均使用 `SceneParamFrame`，多径进入 P3 | 待定 | §3 |
| ④ | 共享内存 | 首期不实施。进程内直接传递 `Block`；`platform.h` 为将来的实现位置 | 待定 | §12 |
| ⑤ | `AddMixer` 的类别 | 现标记为 `source`（`engine/src/processing.cpp:324`），但其实质为两路 IQ 相加的处理组件，倾向改为 `channel`。目录黄金基准仅比对 `ports` 与 `params`，改动类别不会使测试失败，但将造成目录与基准不一致，须先记录决策再行修改 | 待定 | §4；`docs/component-catalog.md` §6 |
| ⑥ | 第三方件的位置 | doctest 与 nlohmann 现仅位于 `engine/third_party/`，`geo/` 无此依赖，导致 `geo` 的 doctest 单测须寄放于 `engine/tests/`。倾向于在 D3 移植 emcore 的 148 例遮挡黄金基准时，一并上提至仓库根 `third_party/` | 待 D3 | §1；`geo/CMakeLists.txt` |
| ⑦ | 慢变参数端口的数据形态 | `PortData::scene` 单帧改为 `scenes` 向量，并补充 `clear()` 对该字段的清除 | 已定（G-2 实施，D-049）：块长可能大于一帧亦可能小于，单帧字段仅能表达后者 | §3.2、§9.3 |
| ⑧ | 发射功率与天线增益的归属 | 三项在首期为全程常量，作为施加类信道的内部参数由装载器自场景注入；帧的 `path_loss_dB` 保持纯传播损耗语义 | 已定（G-3 实施，D-049）：并入帧将使 `link` 事件与场景视图的路损读数名实不符 | §9.5 |
| ⑨ | 随机流与框图节点书写顺序的耦合 | 拓扑序等于框图中节点的书写顺序，多个噪声源共用一个 `IRandom` 时调换顺序即改变样点。`SceneEmitterSource` 在 `init()` 中取一个 u64 建立私有子流，将耦合降至 init 顺序 | 部分已定（D-049）：`NoiseSource` 不作修改，因其已有黄金基准（铁律 10）。按节点 id 派生子流的彻底解法记为技术债 | §11.1、§16.2 |

## 16. 后续工作与已知技术债

### 16.1 待回填内容

- [ ] §8 的四条口径在 M-2 的 `DDC` 实现后按实测回填；为群时延扣除与抽取的样点序号换算补充一组单测
- [ ] `iq/` 原始 IQ 产品的落盘格式。`ObservationTap` 现拒绝 `products: ["iq"]`，
      错误码为 `product_unsupported`（D-040 ③），前端的 IQ 散点因此无数据源
- [ ] 04 §15.2 十二项标准算例与本报告各节的对应表（切片 ④，P1-7）
- [ ] 吞吐实测表：不同块长与不同 nfft 下的 MS/s，Windows 与 Linux 各一份（待阶段 0 冻结平台基线后）
- [ ] 04 §8.4 要求声明的 GPU 需求与许可说明两项，`ComponentInfo` 中尚未建立对应字段

### 16.2 技术债

以下三项均为已知的结构性问题，当前以其他手段控制风险。列出的目的是避免将其误判为新发现的缺陷。

**技术债一：时间推进分散于各源节点，未收归调度器。** 现由各源自行维护 `produced_` 计数并写入
`start_sample`，源节点之间的同步依赖装载器自同一个 `run` 段补齐三个参数（§9.4）。
彻底的解法是由调度器每轮向全部源广播 `(start_sample, n)`，可一次性消除源节点间同步的全部隐患。
其代价是须修改 `IComponent::process` 的签名，波及全部十二个组件与目录黄金基准。
本期以装载器校验与消费端覆盖检查报错两项手段控制风险，见 §5.2 与 §9.3 约束 E。

**技术债二：随机子流未按节点 id 派生。** 详见 §11.1。现仅 `SceneEmitterSource` 建立了私有子流，
`NoiseSource` 因已有黄金基准而未作修改。彻底的解法是按节点 id 派生子流，
使随机序列完全不依赖拓扑序，但该改动将变更 `NoiseSource` 的既有基准，
须按铁律 10 执行一次正式的基准变更流程。

**技术债三：产品文件的跨平台逐字节一致不成立。** `std::cos` 与 `std::sin` 在 MSVC 与 glibc 上
可能相差 1 ulp，`ToneSource` 已存在该情形。铁律 9 的复现对象为同一平台、同一种子，
跨平台一致从不在承诺范围内。该项属既有性质而非缺陷，在此说明以免将来作为缺陷排查。

## 17. 修订记录

| 版本 | 日期 | 修订 | 依据 |
|---|---|---|---|
| v2.1 | 2026-09-07 | 按技术设计文档的文体重写全文。章节标题改为名词短语（§7、§8、§9.3 至 §9.6、§9.8、§10.3、§11.1、§13、§16 等）；取消设问句、讲解式自指（「先解释」「值得单说」「举一个例子」）与口语化表述（「炸出来」「悄悄错掉」「内脏」「兜底」「照跑」）；统一规范性用语为应 / 必须 / 不得；作强调用的直角引号由 101 处减至必要的引用与界面文案；破折号清零；六张图统一编号为图 1 至图 6 并在正文中引用；§2.1 补图 3 样点序号推进。章节编号、事实与结论均未变更 | 用户 2026-09-07 指示「不能像写代码注释这么写，要按技术设计文档的方式写」；文体基准取 04 号方案 |
| v2.0 | 2026-09-07 | 以可读性为目标重排结构。新增 §0 前言（§0.1 问题定义并首次定义分块流、§0.2 适用读者与阅读指引、§0.3 术语与内部编号三表、§0.4 典型运行流程、§0.5 设计摘要）；为各节补写前置说明与论证段落，为每条硬约定标注对应的回归测试用例名；§5 补写切片 ② 的场景注入四项检查；§6 将每轮无条件覆盖下游缓冲的语义自 §9.3 提至约定四；§9.3 消费端约定由 1–4 编号改为 A–F 六条（`engine/src/channel.cpp:134` 引用的约束 C 由此解析至只前进的游标）；§10 补 `track.jsonl` 与 `links.jsonl`；§16 分为待回填与技术债两组；新增附录 C。订正过时事实：源文件 12 → 16、组件 8 → 12、doctest 62 → 114、ctest 4 → 8、错误码 17+1 → 19、`platform.h` 四件 → 五件、`PortData` 由待改状态改为已落地、附录 B 由 8 条补至 12 条、§15 表头六项改为九项并按 ①–⑨ 顺排 | 用户 2026-09-07 指示改写；事实经 `cuav_run --catalog`、`ctest`、`docs/diagram-format.md` 逐项核实 |
| v1.1 | 2026-09-06 | 按 G-1 至 G-3 的实现回填 §9（调度覆盖约束、游标取帧、三道一致性检查、流长不变、拒绝回放数据、实测数据与 hann 等效噪声带宽的读数口径）；§15 增补三项已定（⑦ 帧向量、⑧ 发射功率与天线增益归属、⑨ 随机子流）；§16 增补两条技术债 | G-1 / G-2 / G-3 实施；D-049 |
| v1.0 | 2026-09-06 | 初稿，§0 至 §16 及两个附录。§2 至 §6、§10、§11 为切片 ① 实现的设计记录；§7、§8、§12、§13 规定首期不实施的内容及将来的实现方式；§9 为切片 ② 开工前的先行设计 | 06 §13 第 3 条；P1-0 |

---

## 附录 A. 事件、退出码与错误码速查

### A.1 stdout 事件

事件信封与 WebSocket 文本帧同构（`docs/api-versions.md` §4），格式为
`{seq, task_id, type, t_s, payload}`，其中 `seq` 自 1 单调递增。
给出 `--out` 参数时，引擎将每行原样镜像至 `events.jsonl`，两处均逐行 flush，服务端无需重复落盘。

| type | 触发时机 |
|---|---|
| `validate` | `--validate` 通过 |
| `task.state` | `running` / `finished` / `failed`。结果四态位于 `payload` 中，与退出码正交 |
| `progress` | 按墙钟节流，默认 100 ms；`--progress-interval-ms 0` 表示每轮均发送 |
| `product_row` | 观测点每写出一行。不含数据，仅含 `{op_id, kind, row_index, row_len}` |
| `entity` / `link` | 场景运行时的实体状态与链路读数（§9.8） |
| `log` / `error` | 日志，以及带 `{code, node_id, port, message}` 定位信息的错误 |

### A.2 退出码

| 码 | 含义 |
|---|---|
| 0 | 成功：目录已输出、校验通过或运行完成 |
| 1 | 命令行错误，或未实现的子命令 |
| 2 | 框图装载失败 |
| 3 | 运行失败 |
| 4 | 产品目录或事件文件不可写 |

结果四态不影响退出码。运行完成但结果为 `invalid` 时退出码仍为 0，
四态由 `task.state` 事件承载，见 D-037 的两个正交徽标与 D-041 ④。

### A.3 错误码（21 项）

全表与详细定位规则见 `docs/diagram-format.md` §4，本表仅作速查。

| 分组 | 错误码 |
|---|---|
| 文件与结构 | `json_parse`、`schema`、`graph` |
| 组件与参数 | `unknown_type`、`param`、`param_conflict`、`internal_param` |
| 数据与场景引用 | `data_id`、`scenario`、`scene_binding`、`duration` |
| 连线与图 | `node_missing`、`port_missing`、`port_incompatible`、`input_occupied`、`input_unconnected`、`cycle` |
| 观测点 | `observation_port`、`product_unsupported` |
| 典型链路 | `template`、`port_optional`（后者预留，本版本不产生） |

其中 `scenario` 为 D-049 随切片 ② 新增，覆盖场景解析失败、场景文件不合法或不满足 schema、
跨引用校验不通过、`scenario_ref.sha256` 与文件字节哈希不符、
场景的 `aoi.manifest_sha256` 与观测区域清单不符五种情形。
`template` 与 `port_optional` 为 D-051 随 C-1 新增：前者是 `template_ref` 段的取值错误
（`template_id` 正则、`mode` 三选一、`version` 不小于 1 的整数；缺字段与未知键仍归 `schema`），
后者预留给「组件按参数要求某个可选输入口而它没有连线」的情形，本版本不产生。

## 附录 B. 组件端口与参数

本表以 `cuav_run --catalog` 于 2026-09-07 的输出为准，`schema_version` 为 `cuav-catalog/1`，
`engine_version` 为 `0.1.0`。标记含义：`*` 为必填，`†` 为内部参数（框图中出现即拒绝），`⊥` 为互斥。
单位、取值范围、缺省值与说明以目录输出为准，本表仅供导航。

| 组件 | 类别 | M / E | 输入 | 输出 | 参数 |
|---|---|---|---|---|---|
| `ToneSource` 单音源 | source | M3 / E2 | — | `out:IQStream` | `sample_rate_Hz*`、`total_samples*`、`center_frequency_Hz`、`offset_Hz`、`amplitude` ⊥ `level_dBm`、`phase_rad`、`start_sample`、`stop_sample`、`block_samples` |
| `NoiseSource` 复高斯噪声源 | source | M3 / E2 | — | `out:IQStream` | `sample_rate_Hz*`、`total_samples*`、`center_frequency_Hz`、`power` ⊥ `power_dBm`、`block_samples` |
| `AddMixer` 加法混合 | source（见 §15 ⑤） | M3 / E2 | `a:IQStream`、`b:IQStream` | `out:IQStream` | `gain_a`、`gain_b` |
| `FileReplaySource` 文件回放源 | data | M3 / E4 | — | `out:IQStream` | `data_id*`、`manifest_path†`、`block_samples`、`max_samples` |
| `EnergyDetector` 能量检测 | algorithm | M2 / E2 | `in:IQStream` | `out:DetectionList` | `nfft`、`band_lo_Hz*`、`band_hi_Hz*`、`pfa`、`noise_frames` |
| `DetectionSink` 检测汇聚 | algorithm | M2 / E1 | `in:DetectionList` | — | — |
| `SpectrumAnalyzer` 频谱分析 | algorithm | M3 / E2 | `in:IQStream` | `out:SpectrumFrame` | `nfft`、`window`、`overlap`、`segments_per_frame` |
| `ObservationTap` 观测点 | algorithm | M3 / E2 | `in:IQStream` | — | `op_id*`、`out_dir†`、`spectrum`、`envelope`、`nfft`、`window`、`overlap`、`segments_per_frame`、`bucket_samples` |
| `ScenarioSource` 场景参数源 | data | M2 / E2 | — | 动态 `link:<emitter_id>:SceneParamFrame` | `sample_rate_Hz*`、`total_samples*`、`update_rate_Hz`、`report_rate_Hz`、`report_entities`、`block_samples`、`scenario_path†`、`scenario_id†`、`site_id†` |
| `SceneEmitterSource` 场景辐射源 | source | M3 / E2 | — | `out:IQStream` | `sample_rate_Hz*`、`total_samples*`、`center_frequency_Hz*`、`block_samples`、`scenario_path†`、`scenario_id†`、`entity_id†` |
| `SceneBoundChannel` 场景绑定信道 | channel | M3 / E2 | `in:IQStream`、`scene:SceneParamFrame` | `out:IQStream` | `apply_gain`、`apply_doppler`、`delay_mode`、`max_delay_samples`、`scenario_path†`、`scenario_id†`、`entity_id†` |
| `FreeSpaceChannel` 自由空间信道 | channel | M3 / E2 | `in:IQStream` | `out:IQStream` | `distance_m*`、`frequency_Hz*`、`tx_power_dBm`、`tx_gain_dBi`、`rx_gain_dBi` |

上表需补充三点说明。

其一，`model_id` 与组件类型名不总是一致。四个场景相关组件采用概念模型库的编号：
`ScenarioSource`、`SceneBoundChannel`、`FreeSpaceChannel` 均为 `EM-P-01`，
`SceneEmitterSource` 为 `EM-B-09`。其余组件的 `model_id` 与类型名一致。

其二，`ScenarioSource` 是 `dynamic_ports` 的唯一使用者。目录中仅给出声明
`{pattern: "link:<emitter_id>", source: "scene_binding", type: "SceneParamFrame"}`，
具体端口须待 `configure()` 取得场景后方可确定，详见 §4.1。

其三，`scene_bindable` 属性为真的组件有 `ScenarioSource`、`SceneEmitterSource`、`SceneBoundChannel`
三个，其余九个为假。`FileReplaySource` 为假是 06 备忘录防线二、三在目录侧的表达，
运行期另有 §9.5 的检查作为保障。

## 附录 C. 依据条款索引

文首依据行所列上游条款与本报告各节的对应关系如下表。本表用于评审时的逐条核对，
亦用于上游文档修订时定位受影响的章节。

| 上游条款 | 要求摘要 | 本报告落点 |
|---|---|---|
| 04 §5.2 | 主产品为 DDC 后信道化前的宽带复基带 IQ | §0.1、§10.2 |
| 04 §5.4 | 内部观测点 S0–S6 | §0.3.2、§10.2 |
| 04 §6.3 | 三层架构；同进程、共享内存与内存映射 | §1、§12 |
| 04 §8.3 | 运行引擎十一项职责 | §14 全表 |
| 04 §8.4 | 组件至少声明的十二项；接口控制在少量稳定函数 | §4.1、§4.3 |
| 04 §8.6 | 错误码映射；稳定标识符不暴露路径 | §1、§5.3、附录 A.3 |
| 04 §9.2 | 最小元数据随产物传递 | §2、§11.2 |
| 04 §12 | 常用 5–50 MS/s；100 MS/s 仅用于文件兼容与短窗 | §0.1、§7 |
| 04 §13.3 | P1 退出条件 | §14 |
| 04 §15.2 | 十二项标准算例 | §14、§16.1 |
| 03 §11.6 | 十一种标准数据端口 | §3 |
| 03 §11.7 | 组件生命周期与参数注入 | §4.1 |
| 03 §11.3.1、§11.4 | MATLAB 为内部工具，Coder 生成 C | §13 |
| 01 §3.1 | 同时声明 M / E / V | §4.1、§11.2 |
| 05 P0 | 八个后置端口仅作命名预留 | §3、§14 |
| 铁律 3 | 四重时间不得混用 | §2.1 不变量三 |
| 铁律 4 | IQ 语义与频率约束 | §8 口径三、§13 职责一 |
| 铁律 5 | 单位与 dB 域不混算 | §9.6、§10.2 |
| 铁律 8 | 溯源八件套随产物传递 | §11.2 |
| 铁律 9 | 种子显式注入，固定种子逐位复现 | §2.1 不变量二、§9.1、§11.1 |
| 铁律 10 | 基准不得静默变更 | §4.4、§13、§15 ⑤⑨ |
| 铁律 15 | 不静默降级，结果四态 | §2.2、§9.3 约束 E、§10.3 |
| 铁律 16 | 目标平台与平台抽象 | §1、§12 |
| 铁律 17 | 路径可迁移 | §1、§5.1 |
| D-013 | IQ 流与参数流不得直连 | §3、§9 |
| D-031 | 引擎为子进程，非插件 | §1、§12 |
| D-033 | 参数帧按样点序号推进 | §9 |
| D-036 | DSP 组件采用 MATLAB Coder | §13 |
| D-037 | 浏览器不接触服务器路径 | §1、§4.3、§5.1 |
| D-040 | 装载器四项约定 | §5 |
| D-041 | 事件信封与退出码 | 附录 A |
| D-042 | 服务任务管理器的约定 | §1、§5 |
| D-046 | 视窗抽取；写完第一行即刷新索引 | §10.3 |
| D-047 | 功率单位为 mW，纵轴为 dBm | §9.6、§10.2 |
| D-049 | 切片 ② 场景运行时十一条约定 | §5.2、§9 全节、§15 ⑦⑧⑨ |
