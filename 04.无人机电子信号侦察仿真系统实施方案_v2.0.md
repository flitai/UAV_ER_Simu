# 无人机电子信号侦察仿真系统实施方案

> 文档版本：V2.0  
> 编制日期：2026年9月2日  
> 修订说明：正式采用可单机一体化或内网集中部署的B/S前端架构，保留C++高吞吐IQ运行引擎，明确控制面与数据面边界  
> 项目属性：科研条件建设项目  
> 编制基础：《基于MATLAB/Simulink的无人机信号侦察仿真系统可行性与技术路线_V3.0》及相关讨论  
> 建设取向：可用闭环优先、接收机观测域等效、实测数据校准、适度信号级仿真  

## 1. 编制目的

本方案用于指导一套面向反无人机电子信号侦察设备研发的仿真系统建设。系统服务对象主要是无人机信号接收机以及检测、识别、测向和定位设备的科研与研发人员。

本方案不是继续扩展一套追求极高物理精度的射频仿真工具，而是将已有技术讨论收敛为一条可实施、可交付、可验证和可持续扩展的工程路线。

系统的核心任务是：

> 在给定无人机工作状态、传播场景、接收天线和接收机配置条件下，对“无人机辐射源＋传播信道＋接收天线＋接收机前端＋ADC/DDC”进行端到端等效建模，生成与真实电侦设备信道化前宽带复基带IQ在时域、频域、统计特征和算法任务性能上具有相似性的仿真数据。

本方案重点回答以下问题：

- 系统首期到底模拟什么、输出什么；
- 哪些模型必须建设，哪些模型可以简化或后置；
- MATLAB/Simulink如何作为内部算法工具被自主软件使用；
- 自主框图平台需要实现哪些必要能力；
- 实测IQ数据如何用于研发、标定、验收和交付后科研；
- 项目如何分阶段实施、组织、测试和验收；
- 如何避免因指标过高、范围过大和物理细节过细导致项目失控。

## 2. 项目总体定位

### 2.1 产品定位

系统定位为：

> 面向无人机电子信号侦察接收机研究的、以宽带复基带IQ为核心数据产品的、实测数据驱动的自主框图仿真与算法试验平台。

系统不是完整复刻Simulink，也不是通用通信系统仿真软件。其专业特色在于：

- 面向无人机遥控、遥测、图传和数传等辐射活动；
- 面向侦察接收机实际能够观测到的综合信号结果；
- 支持无人机信号检测、识别、测向和定位算法接入；
- 支持实测数据回放、增强、校准和对照试验；
- 支持自主框图组织、模型替换、参数扫描和结果评价；
- 普通用户不需要操作或看到MATLAB/Simulink。

### 2.2 核心技术判断

本项目不要求唯一还原真实设备内部的每一个物理参数。只要内部模型在合理机理约束下，使接收机输出域的结果与实测结果相近，即可达到科研仿真的主要目的。

因此，模型可信度主要按照以下四个层次判断：

1. 数据格式和功率、频率、时间等基本量正确；
2. 接收IQ的时域、频域、时频域和统计特征相似；
3. 在信噪比、距离、干扰和工作状态变化时，结果变化趋势合理；
4. 同一检测、识别或定位算法处理实测和仿真数据时，性能接近。

不把仿真IQ与实测IQ逐采样点一致作为目标，也不以完整解调真实私有协议作为项目成功条件。

### 2.3 建设原则

项目遵循以下原则：

- 先完成单站、单通道、短时间窗的端到端IQ闭环，再扩展规模；
- 首期以离线和准实时运行为主，不承诺全带宽、多通道实时仿真；
- 采用复基带等效模型，不直接进行GHz载频射频波形逐周期仿真；
- 只模拟会显著影响侦察算法的因素；
- 辐射源、信道、天线和接收机内部保持模块化；
- 对外以综合接收IQ的等效性作为主要评价依据；
- MATLAB用于内部模型开发和组件生产，自主软件负责最终呈现和运行；
- 以实测数据校准模型，以合成数据扩展场景，以冻结数据保障验收；
- 所有模型、数据、参数、算法和试验结果均应可追溯、可复现；
- 首期不因预留未来能力而过度设计当前系统；
- 采用B/S架构承载框图编辑、参数配置、任务管理、数据检索和结果展示；高吞吐IQ计算仍在C++运行引擎和计算节点内完成；
- B/S不等同于公有云，系统应支持断网、内网和单机一体化部署，前端不得依赖公网CDN或在线服务；
- 浏览器只接收任务状态、日志、指标和按显示分辨率抽取后的可视化数据，原始宽带IQ不作为浏览器持续数据流。

### 2.4 可行性结论

| 维度 | 可行性判断 | 实施依据 | 主要约束 |
|---|---:|---|---|
| 复基带IQ生成与处理 | 高 | 调制、滤波、噪声、频移、重采样和DDC均有成熟算法 | 需要统一功率、时间和采样率语义 |
| 综合接收链等效 | 较高 | 可用参数化模型叠加主要前端非理想 | 设备参数不足时只能标定有效参数 |
| 无人机特定信号建模 | 中等至较高 | 可组合实测回放、半经验模板和类别级合成 | 私有协议和高质量实测数据决定上限 |
| 城市传播 | 中等 | 统计信道和简化LOS/NLOS模型容易实施 | 首期不应承诺精确复现每条传播路径 |
| 实测数据校准 | 较高 | 接收机输出域可直接进行特征和算法对比 | 依赖采集元数据、独立验证数据和真值 |
| 自主框图软件 | 高 | 节点编辑、数据流调度和组件插件均为成熟软件技术 | 需要控制首期功能，不复刻完整Simulink |
| MATLAB算法隐式集成 | 高 | 可采用生成库、运行时组件和粗粒度服务 | 需提前验证代码生成支持及许可条件 |
| 100 MS/s宽带IQ | 较高，适合离线 | 分块文件处理和短窗计算可行 | 多通道长时实时运行成本显著上升 |
| 阵列和多站定位 | 有条件可行 | 基础算法和数据结构成熟 | 同步、校准和实测真值不足时可信度受限 |

总体判断：在首期采用单站、单通道、离线或准实时、工程等效精度，并获得至少一批元数据较完整的实测IQ时，项目具备明确的技术可行性和交付可行性。若首期同时要求全协议、高精度城市传播、多通道实时和多站高精度定位，项目风险会显著增加。

### 2.5 V2.0软件架构决策

V2.0正式采用以下软件架构基线：

> 浏览器负责自主框图、项目与试验交互，应用服务负责权限、版本、任务和审计，C++运行引擎负责分块IQ计算，原始IQ留在计算与存储节点，浏览器仅获取控制信息和降采样结果。

该决策包含以下约束：

- 首期同时支持单机一体化B/S部署和内网集中B/S部署，不以分布式计算作为前置条件；
- 浏览器关闭或网络短时中断不得终止已经提交的后台任务；
- 应用服务和运行引擎采用粗粒度任务接口，本机高吞吐组件仍使用同进程、共享内存或内存映射文件；
- 正式交付环境可以完全离线运行，所有前端静态资源、字体、图标和依赖随交付包提供；
- 若后续接入SDR、FPGA或真实接收机硬实时闭环，可增加本地设备代理或专用控制端，不改变主B/S架构。

## 3. 建设目标与范围

### 3.1 总体建设目标

建设一套可供科研用户日常使用的自主软件，用户能够通过类似Simulink的框图方式完成：

1. 选择或配置无人机辐射源；
2. 配置无人机工作状态、位置、航迹和传播条件；
3. 配置接收天线、接收机前端、ADC和DDC；
4. 生成或回放宽带复基带IQ；
5. 从宽带IQ派生信道化后的窄带IQ；
6. 接入用户检测、识别、测向或定位算法；
7. 执行单次试验、参数扫描和批量对比；
8. 查看频谱、瀑布图、时域波形、检测结果和评价指标；
9. 保存场景、模型版本、数据、结果和报告；
10. 导入新的实测IQ和用户算法组件。

### 3.2 首期必须建设的范围

首期以形成可用闭环为准，建议至少包括：

- B/S自主框图编辑、参数配置、运行控制和结果展示；
- 合成信号、实测信号回放和混合增强三类信号源；
- 2至3类有实测依据的典型无人机辐射源模板；
- 自由空间、双径或简化多径、统计衰落三类传播能力；
- 简化接收天线方向图、极化和增益模型；
- 接收滤波、增益、频偏、相位噪声、IQ不平衡、噪声、ADC量化和DDC模型；
- DDC后、信道化前宽带复基带IQ输出；
- 数字信道化、滤波、频移和抽取后的窄带IQ输出；
- 频谱分析、时频分析、能量检测和基础特征提取；
- 至少一种基准识别算法或用户算法接入示例；
- 实测IQ导入、质量检查、回放、检索和版本管理；
- 仿真与实测数据对比、模型参数标定和固定基准测试；
- 框图、参数、随机种子、数据和结果的完整追溯。

### 3.3 可选扩展范围

以下能力保留接口，依据实际科研需求另行建设：

- 相干阵列多通道IQ和AOA测向；
- 多站同步IQ及TDOA、FDOA定位；
- 复杂城市几何射线追踪；
- SDR或真实接收机半实物接入；
- GPU、FPGA或专用硬件加速；
- 多计算节点弹性调度、跨节点任务排队和跨单位协同；
- 更大规模的辐射源模型库；
- 用户自定义插件市场和模型审签发布。

### 3.4 首期明确不追求的内容

- 不追求所有无人机型号和所有工作模式全覆盖；
- 不追求未知私有协议的比特级、加密级完整复现；
- 不追求全城市、全频段、全时段连续IQ生成；
- 不追求射频载波逐周期或器件晶体管级仿真；
- 不追求复杂建筑每一条反射、绕射和散射路径的精确还原；
- 不追求全部接收机非理想因素同时高精度建模；
- 不把多站、阵列和硬件在环作为首期必验能力；
- 不要求所有算法均脱离MATLAB重新开发；
- 不以实时运行为首期统一指标；
- 不以模型数量替代模型质量和闭环可用性。

## 4. 典型用户与应用场景

### 4.1 主要用户

| 用户类型 | 主要工作 | 平台提供的价值 |
|---|---|---|
| 接收机总体设计人员 | 选择带宽、增益、动态范围、ADC和信道化参数 | 快速评估接收链参数对算法的影响 |
| 检测算法人员 | 研究弱信号、短突发、跳频和多源检测 | 获得可控SNR、频偏、占空比和干扰的IQ |
| 识别算法人员 | 训练与评价分类、未知拒识和跨域适应算法 | 获得实测、合成及增强数据和统一基准 |
| 测向定位人员 | 研究阵列测向或多站定位 | 在扩展配置中获得同步多通道或多站IQ |
| 试验管理人员 | 组织批量试验、基线对比和回归测试 | 获得可复现流程、指标统计和报告 |
| 模型开发人员 | 研制辐射源、信道、接收机或算法组件 | 获得统一接口、测试工具和发布机制 |

### 4.2 五类优先应用

1. **接收机参数敏感性试验**  
   改变带宽、增益、滤波、量化位数、频偏和噪声，观察检测识别性能变化。

2. **低信噪比和短突发检测试验**  
   控制信号功率、占空比、突发位置和背景噪声，形成可重复的ROC或检出率曲线。

3. **多信号与强弱遮蔽试验**  
   将无人机信号与同频、邻频或宽带干扰叠加，验证接收机和算法的鲁棒性。

4. **实测信号复盘与增强试验**  
   回放实测IQ，并改变频移、功率、噪声、干扰、信道和接收机条件，扩展有限数据的使用价值。

5. **算法公平对比试验**  
   在相同数据、参数和评价规则下运行不同检测识别算法，形成可横向比较的结果。

### 4.3 三种核心运行模式

| 运行模式 | 输入来源 | 处理方式 | 主要用途 |
|---|---|---|---|
| 全合成模式 | 参数化辐射源 | 完整通过信道、天线、接收机和ADC/DDC | 参数扫描、边界条件和数据扩展 |
| 实测回放模式 | 实测宽带或信道化IQ | 原样回放或仅做格式适配 | 真实数据复盘和算法基准 |
| 混合增强模式 | 实测辐射源片段或实测接收IQ | 施加频移、功率、噪声、信道、干扰或接收机变换 | 提高逼真度并扩充场景 |

其中，混合增强模式通常是首期投入产出比最高的能力：它保留实测信号中难以显式建模的细节，同时提供一定的参数可控性。

## 5. 仿真对象、边界与IQ产品定义

### 5.1 端到端对象

系统的主要物理逻辑为：

    无人机任务和工作状态
              ↓
    辐射源复基带信号
              ↓
    发射特征与天线等效作用
              ↓
    传播损耗、时延、多径、多普勒
              ↓
    接收天线与馈线
              ↓
    接收机射频前端等效模型
              ↓
    ADC采样与量化
              ↓
    数字下变频DDC
              ↓
    信道化前宽带复基带IQ
              ↓
    信道化、检测、识别、测向或定位

系统内部可分别调整各环节，但外部主要评价从真实电磁辐射到接收机数字输出的综合结果。

### 5.2 主要IQ输出

系统的主数据产品定义为：

> DDC后、信道化前、未经检测识别和解调处理的宽带复基带IQ。

该数据应明确：

- 等效射频中心频率；
- 复数采样率；
- 有效分析带宽；
- I、Q数值格式和标度；
- 首样本时间或逻辑时间；
- 接收通道或站点；
- 增益和AGC状态；
- 是否存在削顶、丢样或不连续；
- 对应的场景、模型和参数版本；
- 可供评价使用但不向被测算法泄漏的真值索引。

### 5.3 派生IQ输出

由主宽带IQ可以进一步生成：

- 指定子频段的数字下变频IQ；
- 信道化滤波器组输出；
- 抽取后的窄带复基带IQ；
- 多个子信道同步IQ；
- 算法所需的定长IQ片段；
- 在扩展配置下的阵列多通道IQ或多站同步IQ。

因此，“先模拟宽带IQ，再模拟信道化IQ”是正确且推荐的处理顺序。前提是宽带IQ有效频谱及滤波过渡带处于复数采样率的奈奎斯特范围内，并在抽取前进行抗混叠滤波。

### 5.4 内部观测点

为便于调试和标定，系统可按需暴露以下内部观测点：

| 观测点 | 内容 | 默认是否长期保存 |
|---|---|---:|
| S0 | 辐射源输出复基带IQ | 否 |
| S1 | 传播到达接收天线端的IQ | 否 |
| S2 | 接收机前端等效输出 | 否 |
| S3 | ADC量化后IQ | 可选 |
| S4 | DDC后信道化前宽带IQ | 是，主产品 |
| S5 | 信道化后窄带IQ | 按任务保存 |
| S6 | 检测、特征和识别结果 | 是 |

默认不记录全部中间IQ，避免存储压力。只有在模型校准和问题定位时开启必要观测点。

### 5.5 不进行射频载波逐周期模拟

系统中的中心频率用于描述信号对应的射频位置、传播参数和接收机调谐状态。实际数值计算主要在复基带域完成。

例如，中心频率为2.44 GHz、复数采样率为100 MS/s时，系统计算的是相对于2.44 GHz中心的约负50 MHz至正50 MHz复基带频率范围，而不是以数GHz采样率直接生成2.44 GHz正弦载波。

这种方法能够保留侦察算法所需的幅度、相位、频偏、时延、多普勒和带宽信息，同时显著降低计算量。

## 6. 总体系统架构

### 6.1 逻辑架构

    ┌──────────────────浏览器自主软件前端──────────────────┐
    │ 项目管理 │ 框图编辑 │ 参数表单 │ 数据中心 │ 结果分析 │
    │ 场景配置 │ 运行控制 │ 批量试验 │ 模型管理 │ 报告生成 │
    └────────────────────────┬─────────────────────────────┘
                             │ REST＋WebSocket
    ┌────────────────────应用服务与控制层─────────────────┐
    │ 用户权限 │ 项目版本 │ 模型目录 │ 数据索引 │ 操作审计 │
    │ 任务提交 │ 状态恢复 │ 文件服务 │ 报告服务 │ 接口网关 │
    └────────────────────────┬─────────────────────────────┘
                             │ 粗粒度任务接口
    ┌────────────────────试验编排与运行层─────────────────┐
    │ 框图检查 │ 多速率调度 │ 分块流处理 │ 任务队列       │
    │ 随机种子 │ 缓存管理   │ 日志追溯   │ 异常处理       │
    └────────────────────────┬─────────────────────────────┘
                             │ 本机调用／共享内存／内存映射
    ┌────────────────────模型与算法组件层─────────────────┐
    │ 辐射源 │ 信道 │ 天线 │ 接收机 │ ADC/DDC │ 信道化    │
    │ 数据回放 │ 分析处理 │ 检测识别 │ 测向定位扩展       │
    └────────────────────────┬─────────────────────────────┘
                             │
    ┌────────────────────数据与证据资产层─────────────────┐
    │ 实测IQ │ 合成IQ │ 元数据 │ 真值 │ 参数库 │ 基准集   │
    │ 模型证据包 │ 试验结果 │ 报告 │ 版本与审计           │
    └────────────────────────┬─────────────────────────────┘
                             │
    ┌────────────────────基础运行环境──────────────────────┐
    │ CPU │ 可选GPU │ NVMe缓存 │ 集中存储 │ 数据库 │ 内网  │
    └──────────────────────────────────────────────────────┘

### 6.2 自主软件与MATLAB的分工

| 范围 | MATLAB/Simulink内部工具链 | B/S自主软件产品 |
|---|---|---|
| 模型研发 | 算法设计、参考模型、参数拟合和验证 | 不向普通用户开放 |
| 组件构建 | 生成C/C++库、运行时组件或粗粒度服务 | 加载组件并统一调用 |
| 框图组织 | 可用于内部参考和开发调试 | 浏览器中的用户正式建模界面 |
| 运行调度 | 不作为最终唯一调度器 | 自主运行引擎负责 |
| 数据管理 | 研发阶段分析脚本 | 统一数据中心和资产管理 |
| 结果展示 | 内部验证图表 | 自主频谱、瀑布图、指标和报告 |
| 产品品牌 | 不直接呈现 | 使用项目自有名称、图标和术语 |

该架构可以使用户不接触MATLAB界面、源文件和Simulink模型，同时合法复用成熟算法。第三方软件的许可、运行时再分发和声明义务仍应按采购合同和法务意见执行。

### 6.3 推荐软件实现

建议采用以下工程组合，具体框架可根据现有团队技术栈和原型验证结果调整：

- 软件形态：前后端分离的B/S架构，同时支持单机一体化和内网集中部署；
- 浏览器前端：TypeScript加Vue或React等成熟框架；
- 框图画布：基于成熟图编辑引擎二次开发，候选包括AntV X6或React Flow；必须实现节点、端口、连线、分组、撤销重做和错误定位；
- 图表显示：普通指标采用成熟图表组件，密集频谱和瀑布图采用Canvas或WebGL；
- 控制接口：REST用于项目、参数、数据和任务操作，WebSocket用于任务状态、日志、告警和降采样显示数据；
- 应用服务：负责身份、权限、项目、模型目录、任务、数据索引、审计和报告，技术栈按团队能力选择；
- 模型描述：JSON或等效可版本化文本格式，并在前后端执行一致的模式校验；
- 运行内核：C++分块流处理和有向无环图调度，不因采用B/S而改写为浏览器计算；
- 算法插件：动态库、独立工作进程或受控的粗粒度服务；
- 进程间高速数据：同一计算节点优先采用共享内存或内存映射文件；
- 数据索引：单机部署可采用SQLite，集中部署采用PostgreSQL；
- IQ文件：项目统一二进制格式并兼容SigMF元数据；
- 多通道数据：可选HDF5等容器，但不强制所有数据使用单一大文件；
- 结果报告：HTML为主，可导出PDF；
- 自动测试：前端组件测试、接口测试、模型单元测试、标准算例和端到端回归测试。

首期不建议将每一个小框图模型都做成远程服务。高吞吐IQ链应尽量在同一进程或共享内存域内完成，以避免跨进程复制成为性能瓶颈。应用服务与运行引擎之间采用“提交完整试验任务、查询状态、获取结果索引”的粗粒度接口。

### 6.4 B/S控制面与IQ数据面边界

B/S架构中必须区分控制面和数据面：

| 数据类别 | 典型内容 | 浏览器处理原则 |
|---|---|---|
| 控制数据 | 框图、参数、运行命令、权限和版本 | 可通过REST交换 |
| 状态数据 | 进度、日志、告警、资源占用 | 可通过WebSocket增量推送 |
| 展示数据 | 降采样时域、频谱、瀑布图瓦片、统计指标 | 按视窗、频段和分辨率请求，不传全量原始数据 |
| 原始IQ | 20至100 MS/s复数采样及中间节点IQ | 留在计算节点、NVMe或集中存储，不作为浏览器持续数据流 |
| 文件交换 | 实测IQ导入、结果导出、报告下载 | 采用分片、断点续传和服务端校验，避免JSON或Base64封装大文件 |

浏览器请求可视化结果时应携带时间窗、频率范围、像素宽度和所需统计量。服务端根据显示分辨率完成抽取、FFT、聚合或瓦片生成，避免浏览器因全量IQ造成内存、网络和渲染失控。

### 6.5 部署模式

首期支持两种部署模式，并保持同一套前端、接口和项目格式：

1. **单机一体化B/S部署**  
   浏览器、应用服务、C++运行引擎、SQLite和本地IQ存储运行在同一工作站。安装程序负责启动后台服务并打开本机页面，适用于原型、单用户、外场或完全断网环境。

2. **内网集中B/S部署**  
   浏览器通过受控内网访问应用服务器，计算工作进程靠近NVMe或集中存储部署，数据库采用PostgreSQL，适用于多用户、统一数据管理和批量任务。

两种模式均不得依赖公网CDN、在线字体、在线地图或外部身份服务。所有静态资源和运行依赖随交付包离线提供。集中模式应启用TLS、基于角色的权限控制、登录与操作审计、会话超时、备份和恢复机制。

## 7. 模型体系与简化策略

### 7.1 模型精度分级

系统采用三级精度，用户按试验目的选择，而不是默认使用最高精度。

| 等级 | 名称 | 主要内容 | 使用建议 |
|---|---|---|---|
| L1 | 基础等效 | 类别波形、路径损耗、AWGN、理想或简化接收机 | 接口验证、快速批量试验 |
| L2 | 工程等效 | 突发与频率活动、统计多径、接收机主要非理想、ADC/DDC | 首期默认模式 |
| L3 | 数据校准等效 | 使用实测参数分布、设备模板和残差补偿 | 重点型号和验收场景 |

阵列、多站和半实物不是更高精度的必然组成，而是独立扩展能力。

### 7.2 无人机辐射源模型

首期辐射源模型不以完整协议复刻为目标，采用三类实现：

1. **参数化合成模型**  
   生成单载波、多载波、突发、周期、跳频或扫频等类别级复基带信号，支持调制、带宽、符号率、占空比、功率和频率活动配置。

2. **实测片段回放模型**  
   将高信噪比、元数据完整的实测IQ作为辐射源模板，进行循环、拼接、重采样、频移和功率归一化。

3. **半经验特征等效模型**  
   根据实测数据拟合带宽、谱形、突发时长、重复周期、占空比、频偏和统计分布，生成不依赖真实协议内容的相似信号。

首期只选取有数据、有使用需求、能够验证的2至3类典型辐射源。每个模型应形成模型卡，说明数据依据、参数、适用范围和验证结果。

可暂缓：

- 加密内容和真实业务载荷；
- 未经授权的真实跳频序列；
- 细粒度协议状态机；
- 难以验证的个体射频指纹生成；
- 无实测依据的型号级精确声明。

### 7.3 传播信道模型

首期采用由简到繁的组合：

- 自由空间路径损耗和传播时延；
- 地面双径或简化反射；
- 对数正态阴影衰落；
- 瑞利、莱斯或抽头延迟线统计多径；
- 由距离、相对速度产生的多普勒；
- LOS、NLOS和轻度/中度/重度多径场景模板。

城市建筑可用于判断LOS/NLOS、附加损耗和场景类别，但首期不要求对每栋建筑进行高精度电磁材料建模，也不要求全场景射线追踪。

首期传播模型的重点是：

- 接收功率随距离和遮挡变化合理；
- 多径时延和频率选择性处于可解释范围；
- 相邻时间窗参数连续；
- 相同随机种子能够复现；
- 能够通过实测数据拟合有效参数。

### 7.4 接收天线模型

首期支持：

- 全向或简化方向图；
- 方位和俯仰增益表；
- 极化匹配损耗；
- 天线安装方向和接收站姿态；
- 馈线固定损耗；
- 可选的频率相关增益。

首期不要求精细平台电磁耦合、阵元互耦和机体散射。需要阵列测向时，再增加阵列流形、通道幅相误差和校准模型。

### 7.5 接收机前端等效模型

首期只保留对接收IQ和算法性能影响明显、参数容易获得或标定的因素：

- 接收带宽和滤波器幅频响应；
- 总增益或分段增益；
- 噪声系数或等效输入噪声；
- 本振频偏；
- 简化相位噪声；
- 直流偏置；
- IQ幅相不平衡；
- AGC固定、关闭或简化动态模式；
- 强信号削顶和简化增益压缩；
- 邻频阻塞的等效影响。

首期不进行晶体管级、混频器器件级和完整杂散树仿真。互调和阻塞可用输入输出曲线或等效项表达。

### 7.6 ADC模型

ADC模型至少包括：

- 复采样率；
- I、Q量化位数；
- 满量程和物理功率标度；
- 均匀量化；
- 削顶和过载标记；
- 可选采样时钟偏差；
- 可选丢样或不连续注入。

采样抖动、孔径效应和精细非线性仅在有明确研究需求和参数依据时增加。

### 7.7 DDC与信道化模型

DDC由以下基本环节组成：

1. 数控振荡器产生复数本振；
2. 复数混频将目标频段搬移到零频附近；
3. 低通滤波保留目标带宽并抑制带外信号；
4. 按目标采样率进行抽取；
5. 更新中心频率、采样率、时间和群时延元数据。

信道化可采用：

- 单通道数字下变频；
- 多路并行DDC；
- 多相滤波器组；
- FFT信道化。

首期至少实现单通道DDC和一种多通道信道化方式。信道化后的IQ必须能够追溯到原宽带IQ的样本区间和参数。

### 7.8 噪声、背景与干扰模型

首期支持：

- 复高斯白噪声；
- 可配置噪声底或目标SNR；
- 简化有色噪声；
- 实测背景IQ回放；
- 单音、窄带、宽带和脉冲干扰；
- 同频和邻频其他信号；
- 多源叠加与近远效应。

在有实测背景数据时，优先采用“实测背景＋合成目标”方式，通常比单纯构造复杂统计噪声更具真实性。

### 7.9 检测识别与评价模型

平台自带算法用于证明IQ可用和提供统一基线，不以建设完整侦察算法产品为目标。

首期建议内置：

- FFT、功率谱和瀑布图；
- 能量检测和自适应门限；
- 突发检测和占用统计；
- 中心频率、带宽、功率和持续时间估计；
- 基础时频特征提取；
- 一种传统或轻量学习识别基线；
- 混淆矩阵、ROC、检出率、虚警率和F1等评价；
- 用户算法插件示例。

测向定位算法可作为扩展包，不影响首期检测识别闭环验收。

## 8. B/S自主框图平台实施设计

### 8.1 前台主要功能

B/S自主软件的浏览器主界面建议包括：

- 顶部：项目、运行、数据、模型、试验和帮助；
- 左侧：按辐射源、信道、天线、接收机、数据和算法分类的组件库；
- 中部：框图画布及模型连接关系；
- 右侧：所选模型的参数、单位、范围、说明和验证状态；
- 下部：运行日志、告警、数据流状态和资源占用；
- 结果页：时域、频谱、瀑布图、星座图、统计特征和算法指标；
- 数据中心：实测数据、合成数据、标签、质量报告和数据集版本；
- 试验中心：参数扫描、任务队列、结果对比和报告。

[现有主界面交互原型](无人机信号侦察仿真系统_主界面交互原型_v1.0.html)可作为需求沟通和详细设计参考，不直接视为正式产品代码。

### 8.2 框图模型能力

首期框图编辑器必须支持：

- 模型拖放、删除、复制、分组和重命名；
- 端口连线、断开和兼容性检查；
- 参数编辑、单位显示、默认值和范围校验；
- 子系统封装和模板保存；
- 模型启用、旁路和替换；
- 框图保存、加载、另存和版本说明；
- 运行前完整性检查；
- 中间观测点设置；
- 单次运行、暂停、停止和批量运行；
- 任务提交后脱离页面继续运行，重新登录或重连后恢复状态；
- 错误定位到具体模型和端口。

首期不要求完全实现Simulink的连续系统求解器、代数环求解、自动代码生成界面和全部调试能力。

### 8.3 运行引擎

运行引擎采用有向数据流和分块IQ处理。每个IQ数据块同时携带样本和元数据。

运行引擎至少负责：

- 按拓扑顺序调度模型；
- 检查采样率、中心频率、通道数和数据格式；
- 执行分块、缓存、背压和流结束处理；
- 处理模型初始化、运行、复位和销毁；
- 管理随机种子和确定性复现；
- 记录模型版本、参数、耗时和异常；
- 支持文件流和内存流；
- 将运行状态、日志摘要和降采样显示产品发布给应用服务；
- 在采样率变化时处理时间和群时延关系；
- 避免无必要的IQ内存复制；
- 在任务失败时保留可诊断信息。

首期可以采用单计算节点多线程调度，不必一开始建设分布式计算系统。浏览器或网络连接中断只影响交互，不得直接终止计算任务；重新连接后，应用服务应根据持久化任务状态恢复进度、日志和结果索引。

### 8.4 模型组件规范

每个组件至少声明：

- 组件标识、名称、版本和类别；
- 输入输出端口；
- 数据类型、通道数和维度；
- 采样率和中心频率约束；
- 参数、单位、默认值和有效范围；
- 是否保持状态及状态复位方式；
- 固定或可变处理延迟；
- 分块长度要求；
- CPU、内存和可选GPU需求；
- 随机性及种子使用规则；
- 模型依据、验证状态和适用范围；
- 部署依赖和许可说明。

建议把组件运行接口控制在少量稳定函数，例如初始化、配置、处理、刷新、复位、状态查询和销毁。

### 8.5 MATLAB算法部署策略

推荐按优先级采用以下方式：

1. **C/C++代码或动态库**  
   用于高频调用、长时间运行和正式交付的核心算法。优点是性能高、与自主引擎结合紧密，并可减少运行时依赖。

2. **MATLAB Runtime组件**  
   用于代码生成支持不足、研发变化较快或首期快速落地的粗粒度算法。应放在独立工作进程中，避免故障影响主程序。

3. **FMU或粗粒度模型包**  
   用于整体模型交换和低频控制模型，不用于逐帧高吞吐IQ小组件。

4. **集中算法服务**  
   用于模型标定、批量数据分析和非实时任务，不作为单机高吞吐IQ主链的默认方式。

正式浏览器界面不启动或展示MATLAB桌面，不提供原始脚本和Simulink源模型。模型包由内部构建流水线生成、测试、签名和发布。

### 8.6 B/S接口与会话要求

应用服务接口至少满足：

- 项目、框图、模型、数据集、任务和结果均使用稳定标识符，不向浏览器暴露服务器真实文件路径；
- 修改类接口支持版本检查，避免多个页面或用户静默覆盖同一项目；
- 任务提交接口具备幂等标识，避免重试导致同一批量任务重复执行；
- WebSocket事件包含任务标识、事件序号和时间，断线重连后可补取缺失状态；
- 大文件导入采用分片、续传、哈希校验和配额检查，上传成功后才进入正式数据目录；
- 统一错误码能够映射到具体框图、模型、端口、数据文件或系统资源；
- API和项目文件格式独立版本化，升级后仍可加载约定范围内的旧项目；
- 浏览器不保存长期有效的明文口令、原始IQ缓存或超出授权范围的数据。

## 9. IQ数据与元数据规范

### 9.1 数据格式

建议：

- 交换和长期保存优先支持复数int16交织格式；
- 内部计算根据算法需要使用复数float32；
- 高精度标定可选复数float64，但不作为普通任务默认格式；
- 文件采用分段存储，避免单一超大文件；
- 每个数据文件配套机器可读元数据；
- 数据格式可与SigMF相互转换。

100 MS/s复数int16 IQ的数据率约为400 MB/s/通道；复数float32约为800 MB/s/通道。因此必须采用任务裁剪、分块处理和按需保存。

### 9.2 最小元数据

| 类别 | 必填字段 |
|---|---|
| 数据身份 | data_id、文件哈希、数据版本、创建时间 |
| 采样参数 | sample_format、sample_rate、sample_count、字节序、IQ排列 |
| 频率参数 | center_frequency、effective_bandwidth |
| 时间参数 | start_time或逻辑时间、时间基准、连续性标志 |
| 通道信息 | station_id、channel_id、天线或通道说明 |
| 功率标度 | scale、full_scale、增益、AGC状态 |
| 质量状态 | 削顶、丢样、空洞、直流、IQ交换和异常说明 |
| 来源 | 合成、实测、增强或派生；源数据引用 |
| 模型追溯 | 场景、模型、参数、算法和随机种子版本 |
| 真值 | 受控的目标、信号、位置和工作状态索引 |
| 权限 | 所有权、使用范围、保密和导出限制 |

### 9.3 宽带IQ基本约束

对复数采样率为Fs、相对于中心频率的信号带宽为B、频偏为Δf的信号，应满足：

    |Δf| + B/2 + 保护带 < Fs/2

抽取到新采样率前，必须使用抗混叠滤波器抑制新奈奎斯特范围之外的频谱。

系统在连线和运行前应自动检查：

- 有效带宽是否超出采样率；
- 目标信号是否跨越频带边缘；
- 抽取比例是否合法；
- 滤波器过渡带是否足够；
- 多源叠加后是否可能削顶；
- IQ标度和功率单位是否一致。

## 10. 实测数据建设与使用方案

### 10.1 对本项目最有价值的数据

优先级最高的数据是：

> 真实电侦设备DDC后、信道化前、未经检测识别和解调处理的宽带复基带IQ，并带有完整采集元数据。

理想情况下，同一段信号还能提供：

- 对应的信道化后IQ；
- 接收设备的检测、识别或定位输出；
- 设备配置和状态日志；
- 无人机类型、工作状态、距离、位置或航迹真值；
- 同期背景空采数据。

如果研究阵列测向，还需要共本振、共采样时钟的相干多通道IQ和通道校准数据。  
如果研究TDOA/FDOA，还需要多站同步IQ、绝对时间戳、站址、通道群时延和目标轨迹真值。

### 10.2 建议向用户索取的数据

首批数据不求数量特别大，优先保证可解释和可配对：

1. 同一型无人机的高信噪比近距离数据，用于提取辐射活动和信号结构；
2. 相同模式下多个距离或信噪比的数据，用于校准传播和接收链综合变化；
3. 无人机关闭或不在场时的背景数据，用于估计真实噪声和环境信号；
4. 不同日期、地点和飞行架次的数据，用于独立验证；
5. 固定增益或明确AGC状态的数据；
6. 包含中心频率、采样率、带宽、位宽、接收机、天线、增益、时间、地点、距离和工作状态的元数据。

可向用户简要表述为：

> 请优先提供侦测设备在信道化前保存的宽带复数IQ原始记录，并配套中心频率、采样率、数据格式、接收机及天线、增益或AGC状态、采集时间地点、无人机型号和工作状态、距离或航迹等信息；最好同时提供无目标背景数据和同一信号的信道化后IQ。

### 10.3 数据资产分层

| 数据层 | 内容 | 主要用途 |
|---|---|---|
| 原始证据母库 | 未修改原始IQ、原始元数据和采集记录 | 追溯、重新处理和高可信标定 |
| 研发标定库 | 清洗、切片、标注和派生数据 | 参数拟合、模型开发和算法训练 |
| 冻结基准库 | 未参与拟合调参的独立数据 | 验收、版本比较和回归测试 |
| 用户科研库 | 交付后用户导入和生成的数据 | 日常科研和本地扩展 |

相邻时间片不能随机分散到训练集和测试集。应按设备、日期、地点、架次、接收机或采集批次进行隔离。

### 10.4 交付前研发阶段的作用

实测数据在交付前主要用于：

- 确认系统主IQ输出边界和格式；
- 提取辐射源时频结构、突发和占空比特征；
- 估计综合频偏、相位噪声、噪声底和动态范围；
- 拟合有效传播、天线和接收链参数；
- 构建实测回放和混合增强模板；
- 训练或选择基准检测识别算法；
- 验证MATLAB参考算法与交付组件的一致性；
- 建立独立冻结验收集；
- 形成每个重点模型的证据包和适用边界。

### 10.5 交付后使用阶段的作用

实测数据在交付后主要用于：

- 真实事件和设备数据复盘；
- 用户自研算法公平比较；
- 新无人机或未知信号研究；
- 生成新的混合增强场景；
- 校准用户本地接收机和背景环境；
- 复现软件升级前后的能力变化；
- 发现模型与现实之间的系统偏差；
- 形成新模型版本，而不是直接覆盖旧版本。

### 10.6 数据质量检查

导入时自动检查：

- 文件长度、格式和元数据一致性；
- I/Q顺序、字节序和数值范围；
- 直流偏置、IQ交换和幅相异常；
- 削顶、过载、丢样、全零和时间空洞；
- 频谱占用、噪声底和带宽；
- 多通道样本数和时间对齐；
- 元数据必填项、单位和取值范围；
- 文件哈希和重复数据。

质量问题不一定导致数据无法使用，但必须被标记并影响其可信度等级。

## 11. 模型标定与逼真度验证

### 11.1 验证目标

验证目标不是证明内部每个参数都等于真实物理参数，而是证明：

> 在相同任务条件和相同后续处理链下，仿真系统生成的接收机输出IQ与实测IQ具有足够接近的观测特征、变化趋势和算法效果。

### 11.2 数据准备

对每一类重点无人机，建议准备四组数据：

- A组：高信噪比参考数据，用于提取辐射源和稳定特征；
- B组：多距离、多姿态或多场景数据，用于拟合综合变化；
- C组：背景、同频和邻频环境数据，用于建立真实环境；
- D组：不同日期、地点或设备采集的独立数据，只用于最终验证。

### 11.3 标定流程

1. 固定实测数据格式和参考处理链；
2. 对实测与仿真数据使用完全相同的分段、滤波和特征计算；
3. 先标定中心频率、采样率、功率标度和带宽；
4. 再标定突发时长、周期、占空比和频率活动；
5. 再调整噪声、衰落、多径和接收机非理想；
6. 对无法用简单机理解释的稳定差异，使用参数分布或残差模型补偿；
7. 在未参与标定的D组数据上进行独立验证；
8. 固化参数、版本、数据清单、结果和适用边界。

### 11.4 比较指标

| 层次 | 建议比较内容 |
|---|---|
| 数据层 | 采样率、中心频率、带宽、幅度标度、削顶率、连续性 |
| 频域层 | 平均PSD、带内功率、占用带宽、谱形、杂散和噪声底 |
| 时域层 | 包络分布、峰均比、突发长度、间隔、占空比和启动瞬态 |
| 时频层 | 频率活动轨迹、跳变、持续时间和时频纹理 |
| 复统计层 | I/Q均值方差、幅度分布、相位增量、循环或相关特征 |
| 趋势层 | 随距离、SNR、遮挡、干扰和增益变化的趋势 |
| 任务层 | 检出率、虚警率、识别混淆、置信度和算法排序 |

不建议用单一频谱相似度评价全部逼真度，也不建议把点对点样本误差作为主指标。

### 11.5 混合建模建议

重点型号推荐采用：

    机理模型
       ＋
    实测参数分布
       ＋
    必要的实测残差或背景片段
       ＝
    接收机观测域等效模型

该路线比完整逆向还原真实辐射源、传播环境和接收机内部结构更可行，也更符合科研条件建设项目的投入产出要求。

## 12. 性能、计算与存储设计

### 12.1 建议性能口径

首期性能不追求极限，建议采用以下可行口径：

| 项目 | 建议首期口径 |
|---|---|
| 主要运行方式 | 离线和准实时 |
| 单任务主链 | 单站、单接收通道 |
| 常用复采样率 | 5、10、20、40或50 MS/s |
| 宽带数据兼容 | 支持100 MS/s单通道文件导入、回放和分块处理 |
| 单次高采样率窗口 | 以秒级短片段为主 |
| 多通道扩展 | 1至4通道、较低采样率或短时间窗 |
| 数据格式 | int16交换/保存，float32内部计算 |
| 实时性 | 不作为所有模型的统一验收条件 |
| 批量试验 | 支持多参数、多随机种子顺序或并行运行 |
| 浏览器显示 | 只接收与视窗分辨率匹配的抽取波形、频谱、瀑布图瓦片和指标 |

最终数值应根据用户已有设备的数据率、典型记录长度和算法输入要求冻结。

### 12.2 计算策略

- IQ按固定帧长分块处理；
- 运动和传播参数使用低于采样率的更新周期；
- 只在目标时间窗和目标频段生成高采样率IQ；
- 尽早完成滤波和抽取；
- 中间节点默认只保留统计摘要；
- FFT、FIR、重采样和批量特征计算可选GPU加速；
- 参数扫描优先进行任务级并行；
- 大文件采用顺序流读写和局部缓存；
- 频谱、瀑布图和长时波形在计算侧预处理，按浏览器视窗和像素分辨率返回；
- 原始IQ、中间节点IQ和模型间帧数据不得以JSON或Base64作为常规传输格式；
- 发生内存不足时自动降低并发，不降低数据正确性；
- 每项性能指标同时声明硬件、模型组合和数据格式。

### 12.3 建议硬件

首期不需要大型集群，可采用一台研发计算服务器加若干客户端：

| 设备 | 建议配置方向 | 用途 |
|---|---|---|
| 计算服务器 | 32至64个CPU核心、256 GB内存、可选1张通用GPU、8至16 TB本地NVMe | IQ生成、回放、批量计算 |
| 数据存储 | 20至50 TB有效容量，可扩展和备份 | 实测IQ、基准集和结果 |
| 单机一体化工作站 | 16至32核CPU、64至128 GB内存、2至4 TB NVMe、独立显卡可选 | 断网单机部署、框图编辑、短任务和结果分析 |
| 浏览器终端 | 4至8核CPU、16至32 GB内存、支持WebGL的集成或独立显卡 | 框图编辑、任务控制和结果分析，不承担原始IQ主链计算 |
| 网络 | 浏览器终端接入1GbE起步；计算服务器和集中存储10GbE起步，按数据量选25GbE | 控制与展示、文件传输和集中计算 |

若用户现有数据规模较小，可先使用单机一体化工作站完成原型和首期使用，再依据实测吞吐和并发结果扩展应用服务器、计算节点和集中存储。首期不建议先行采购昂贵的射频信道模拟器、统一时频设备和大规模SDR阵列。

## 13. 分阶段实施计划

### 13.1 总周期与团队建议

建议项目周期为12至15个月，先在第6个月形成可演示的最小闭环，第9个月形成可供试用的版本，随后用实测数据校准并完成验收。

建议核心团队8至10人：

- 项目经理或系统总体：1人；
- 信号与辐射源建模：1至2人；
- 信道、接收机和DDC建模：1至2人；
- B/S前端和框图编辑：2人；
- 应用服务、任务接口和权限审计：1人；
- 运行引擎和组件集成：1至2人；
- 数据、测试和验证：1人；
- 用户侧领域专家按阶段参与评审和数据解释。

整体工作量可按约80至110人月估算。若已有成熟框图平台、算法组件、统一身份权限或数据管理底座，可相应减少；若首期加入阵列、多站或硬件实时闭环，应单独增加周期和资源。

### 13.2 阶段0：需求冻结与数据摸底

建议周期：1至1.5个月。

主要任务：

- 访谈3至5类典型用户；
- 明确首批无人机信号、频段和算法；
- 盘点现有实测IQ及元数据；
- 确定主IQ输出边界；
- 冻结首期功能、非功能和接口需求；
- 冻结单机一体化和内网集中两种B/S部署边界、浏览器基线及接口版本策略；
- 冻结控制面、展示数据和原始IQ数据面的边界；
- 制定数据、模型、版本和验证规范；
- 建立3至5个标准使用场景。

退出条件：

- 首期范围和不做事项经用户确认；
- 至少获得一批可解析实测IQ，或明确补采计划；
- 形成IQ格式、功率、时间和真值口径；
- 形成总体架构和验收框架。

### 13.3 阶段1：最小端到端IQ闭环

建议周期：2至2.5个月。

主要任务：

- 建立基础参数化波形和实测回放信号源；
- 完成路径损耗、噪声和简化接收机；
- 完成ADC、DDC和单通道信道化；
- 输出规范化宽带和窄带IQ；
- 完成频谱、瀑布图和能量检测；
- 完成最小B/S任务提交、状态查询和降采样结果展示；
- 建立标准单音、突发、频偏、噪声和滤波算例；
- 打通MATLAB参考模型与组件封装链路。

退出条件：

- 一个框图能够从浏览器提交并从信号源运行到检测结果；
- 宽带IQ和信道化IQ可被外部算法读取；
- 标准算例结果正确且可重复；
- 实测IQ能够导入并回放。

### 13.4 阶段2：B/S自主框图平台与模型组件化

建议周期：3个月，可与阶段1后半段并行。

主要任务：

- 完成B/S项目管理、组件库和浏览器框图画布；
- 完成参数表单、连线校验和运行控制；
- 完成应用服务、REST接口、WebSocket状态通道、权限和操作审计；
- 完成C++分块流运行引擎；
- 建立模型包、清单和版本机制；
- 接入MATLAB生成库和Runtime工作进程；
- 完成数据中心和试验记录；
- 完成基础结果展示和报告；
- 建立单机一体化与内网集中部署包、离线静态资源、升级和日志诊断机制；
- 完成浏览器断线重连、后台任务保持和大文件分片导入。

退出条件：

- 普通用户可完全通过浏览器自主软件搭建和运行试验；
- 用户界面不依赖MATLAB桌面；
- 浏览器关闭或短时断网不终止后台任务，重连后能够恢复任务状态；
- 原始宽带IQ不通过浏览器持续传输，显示数据量与视窗分辨率匹配；
- 模型可安装、启用、旁路、替换和升级；
- 试验能够完整复现。

### 13.5 阶段3：工程等效模型与实测校准

建议周期：3至4个月。

主要任务：

- 完成2至3类重点无人机辐射源模型；
- 增加统计多径、阴影、频偏和多普勒；
- 增加天线、滤波、噪声系数、IQ不平衡、量化和削顶；
- 建立实测背景叠加和混合增强；
- 完成实测与仿真特征对比工具；
- 建立模型参数拟合和版本冻结流程；
- 在独立实测数据上验证；
- 优化100 MS/s文件分块回放。

退出条件：

- 每个重点辐射源都有模型卡和证据包；
- 仿真与实测的主要特征及算法表现达到约定容差；
- 能够说明适用场景和不适用范围；
- 数据集划分无明显泄漏。

### 13.6 阶段4：系统试用、整改与交付

建议周期：2至3个月。

主要任务：

- 用户试用和典型科研任务演练；
- 完成稳定性、性能和异常恢复测试；
- 完成受支持浏览器兼容、网络中断恢复、会话安全和离线资源完整性测试；
- 固化模型、数据、参数和算法版本；
- 冻结验收数据集和场景；
- 完成安全、权限、备份和恢复；
- 完成单机与内网部署包、用户手册和培训；
- 完成第三方许可与部署合规检查；
- 整改问题并组织正式验收。

退出条件：

- 所有必验场景完成；
- 无阻断用户科研的严重缺陷；
- 冻结基线可以在交付环境复现；
- 文档、数据、软件和测试证据齐套。

### 13.7 后续扩展阶段

首期交付后，可依据用户真实使用频次和科研收益选择：

- 阵列多通道IQ与测向；
- 多站同步IQ与定位；
- SDR或真实接收机数字接口；
- 射频传导半实物；
- 复杂城市传播；
- 多计算节点弹性调度和跨节点数据就近计算；
- 新型无人机和未知信号模型。

扩展遵循“有数据、有用户、有验收方法才建设”的原则。

## 14. 工作包与责任分解

| 工作包 | 主要内容 | 主要输出 |
|---|---|---|
| WP1 总体与规范 | 需求、边界、架构、IQ和接口规范 | 总体设计、数据规范、接口规范 |
| WP2 B/S自主平台 | 浏览器前端、框图、应用服务、权限、接口、运行控制和报告 | B/S自主软件、应用服务和用户界面 |
| WP3 运行引擎 | 调度、分块、缓存、插件和追溯 | 运行内核和组件SDK |
| WP4 专业模型 | 辐射源、信道、天线、接收机、ADC/DDC | 模型组件库 |
| WP5 数据资产 | 导入、检查、索引、标注、版本和回放 | 数据中心和标准数据集 |
| WP6 算法与评价 | 频谱、检测、特征、识别基线和插件 | 算法基线和评价工具 |
| WP7 校准与V&V | 理论算例、实测对比、回归和验收 | 证据包、测试集和验证报告 |
| WP8 部署与保障 | 单机与内网部署、离线资源、权限、备份、升级、培训和运维 | 交付包和保障文档 |

系统总体负责接口和范围控制；专业模型组不能自行改变IQ语义；平台组不能以界面完成代替专业模型验证；测试组应独立管理冻结验收集。

## 15. 测试与验证体系

### 15.1 五级验证

1. **解析与理论验证**  
   对单音、噪声、滤波、频移、时延、量化等使用理论值检查。

2. **组件单元验证**  
   每个模型验证输入输出、边界条件、随机性、复位和异常。

3. **处理链集成验证**  
   验证辐射源到宽带IQ、宽带IQ到信道化IQ、IQ到算法结果的闭环。

4. **实测数据等效验证**  
   对比实测与仿真特征、趋势和下游算法表现。

5. **用户任务验收**  
   由用户用典型科研任务验证可用性、可复现性和扩展性。

### 15.2 标准算例

首期至少建立：

- 单音频移和功率标度；
- 带限噪声和目标SNR；
- 突发开关和占空比；
- 多信号同频及邻频叠加；
- 接收滤波和群时延；
- ADC量化和削顶；
- DDC频移、低通和抽取；
- 宽带IQ到信道化IQ；
- 实测IQ回放；
- 实测背景加合成目标；
- 固定随机种子重复运行；
- 用户算法插件处理。

### 15.3 回归测试

每次模型或软件版本发布前，应自动运行：

- 标准算例；
- 固定实测片段；
- 端到端框图；
- MATLAB参考算法与交付组件等价性；
- IQ文件兼容性；
- 典型性能任务；
- B/S接口模式、前后端版本兼容和权限边界；
- 支持浏览器的框图编辑、频谱瀑布图和报告显示；
- 浏览器关闭、网络中断和重连后的任务保持及状态恢复；
- 断网条件下前端静态资源和全部必验功能可用；
- 安装升级和旧项目加载测试。

任何模型、参数或数据集变化均不得静默改变既有基准结果。

## 16. 验收方案

### 16.1 验收原则

验收以“完整闭环、数据正确、用户可用、结果可复现、实测可校准”为主，不以模型数量、最高采样率或界面数量为主。

### 16.2 功能验收

建议至少验收：

- 能够用浏览器自主界面搭建、提交和运行完整框图；
- 能够生成、导入、回放和导出宽带复基带IQ；
- 能够从宽带IQ生成信道化后IQ；
- 能够配置主要辐射源、信道、天线和接收机参数；
- 能够叠加噪声、背景和干扰；
- 能够输出频谱、瀑布图和检测识别结果；
- 能够接入至少一种用户算法组件；
- 能够执行参数扫描和结果对比；
- 能够保存场景、数据、参数、随机种子和报告；
- 能够完成数据质量检查和版本管理。

### 16.3 正确性验收

| 验收对象 | 接受原则 |
|---|---|
| 频率和采样 | 中心频率、采样率、带宽和频移关系正确 |
| 功率和噪声 | 标准场景的功率、SNR和噪声统计处于约定容差 |
| DDC和抽取 | 目标信号搬移正确，带外抑制和抗混叠符合设计 |
| 接收机效应 | 单因素影响方向和幅度可解释 |
| 确定性 | 同版本、同参数、同数据、同种子结果一致 |
| 数据追溯 | 任一输出可追溯到输入、模型和参数 |
| 实测等效 | 主要特征、变化趋势和下游算法性能达到预先约定容差 |

实测等效容差应按信号类别和用途在详细设计阶段冻结。首期可采用以下参考口径：

- 主要带宽、占空比、突发时长和功率统计误差通常控制在10%至20%以内；
- 仿真与实测数据在相同算法上的检出率或识别率差异，按场景组统计，通常控制在5至10个百分点以内；
- 对难以稳定达到统一数值的特征，采用分布重叠、趋势一致和置信区间报告；
- 不要求IQ采样点逐点相等。

上述参考值应以实际数据质量和用户算法稳定性为前提，不应在缺少数据摸底时直接写成刚性合同指标。

### 16.4 性能与稳定性验收

- 100 MS/s单通道int16 IQ能够分块导入、回放和处理；
- 典型20至50 MS/s单通道任务能够在约定硬件上稳定运行；
- 多任务运行无内存持续增长、数据串扰和不可恢复错误；
- 浏览器关闭或网络短时中断不终止后台任务，重连后能够恢复任务、日志和结果索引；
- 浏览器频谱、瀑布图和长时波形使用抽取或瓦片数据，原始IQ不作为持续Web数据流；
- 受支持浏览器在约定最大框图规模和显示负载下交互流畅，无持续内存增长；
- 大文件处理中断后能够给出明确状态，必要时支持断点续算；
- 连续运行和批量任务满足约定时长；
- 软件能够识别格式错误、缺失元数据、丢样和削顶；
- 性能报告明确硬件、模型组合、数据格式和实时倍率。

### 16.5 数据与交付验收

- 原始、研发、冻结和用户数据分区明确；
- 数据文件、清单和哈希一致；
- 元数据必填项完整；
- 验收集未参与训练和参数拟合；
- 冻结基准在交付环境可复现；
- 用户可以导入新数据并生成质量报告；
- 数据权限、备份、恢复和导出流程有效；
- 单机和内网部署均不依赖公网资源，离线环境能够完成全部必验场景；
- 越权访问、未授权数据下载、非法文件类型、路径穿越和重复任务提交能够被拒绝并记录审计日志。

## 17. 交付成果

### 17.1 软件

- B/S自主框图前端及完整离线静态资源；
- 应用服务、REST接口、WebSocket状态通道、权限和审计模块；
- C++自主运行引擎和计算工作进程；
- 单机一体化部署包、启动器和卸载升级工具；
- 内网集中部署包、数据库初始化和服务配置；
- 模型组件管理与插件SDK；
- IQ数据中心；
- 试验管理与结果分析工具；
- 自动测试和诊断工具；
- 可选多计算节点调度服务。

### 17.2 模型与算法

- 首批2至3类无人机辐射源模型；
- 实测IQ回放和混合增强模型；
- 基础和工程等效传播模型；
- 天线与接收机等效模型；
- ADC、DDC和信道化模型；
- 噪声、背景和干扰模型；
- 基础检测、特征和识别基线；
- 用户算法接入示例。

### 17.3 数据

- 约定范围内的实测数据样例或受控数据集；
- 标准合成IQ数据集；
- 实测背景和混合增强样例；
- 训练、验证、内部测试和冻结验收清单；
- 元数据字典、质量报告和文件哈希；
- 模型标定参数和数据来源说明。

### 17.4 文档

- 需求规格说明；
- 总体设计和详细设计；
- B/S接口、部署拓扑、浏览器兼容和安全设计；
- IQ数据与接口规范；
- 模型开发与插件接入指南；
- 模型卡和模型证据包；
- 数据管理规范；
- 测试大纲、测试报告和验收报告；
- 安装部署、用户、管理员和运维手册；
- 第三方软件依赖和许可说明；
- 培训教材和典型案例。

## 18. 运行维护与交付后机制

### 18.1 用户权限

建议划分：

- 普通科研用户：创建和运行试验、查看授权数据；
- 模型开发人员：开发和调试用户组件；
- 模型管理员：审签、发布、停用和回滚模型；
- 数据管理员：导入、标注、分级和发布数据集；
- 系统管理员：部署、账户、存储、备份和升级；
- 审计人员：查看版本、操作和验收记录。

### 18.2 模型发布

新模型进入正式库前应完成：

1. 接口和参数检查；
2. 单元测试；
3. 标准算例；
4. 性能测试；
5. 实测或参考数据验证；
6. 适用范围说明；
7. 管理员审签；
8. 包签名和版本发布。

### 18.3 升级与回滚

- 软件、模型、参数、算法和数据分开版本；
- 浏览器前端、应用服务、接口和运行引擎声明兼容矩阵；
- 旧项目打开时提示依赖版本；
- 重要升级前自动备份；
- 新版本先在冻结基准上回归；
- 不允许静默替换数据、参数或算法；
- 出现明显退化时能够回滚到上一稳定版本；
- 前端升级后主动刷新版本化静态资源，避免浏览器缓存造成前后端版本错配。

### 18.4 B/S运行安全

- 正式环境采用受控内网或单机回环地址，按数据分级划分访问边界；
- 禁止公网CDN、外部遥测、第三方在线分析和未经审查的自动更新；
- 集中部署启用TLS，身份凭据和会话令牌采用安全存储、有效期和注销机制；
- 对跨站请求、脚本注入、越权访问、路径穿越、恶意文件和接口重放实施防护；
- 前端设置内容安全策略，第三方依赖建立清单、版本锁定、漏洞检查和许可审查；
- 计算工作进程按最小权限运行，不以应用服务账户直接访问全部原始数据；
- 数据导入、导出、模型发布、权限变更和验收基线变更写入不可静默修改的审计记录；
- 单机部署同样执行身份、目录权限、日志、备份和恢复要求，不因使用本机浏览器而降低安全等级。

### 18.5 培训建议

至少组织：

- 普通用户培训：框图搭建、数据回放、试验和报告；
- 算法用户培训：插件接口、数据格式和评价规则；
- 模型管理员培训：模型验证、发布和回滚；
- 数据管理员培训：数据导入、质量检查、标签和权限；
- 运维培训：部署、备份、日志和故障恢复。

## 19. 风险与控制措施

| 风险 | 可能后果 | 控制措施 |
|---|---|---|
| 首期范围不断扩大 | 工期和经费失控 | 冻结必做、可选和不做清单，扩展走变更流程 |
| 过度追求高保真 | 模型复杂但无法验证 | 以接收机输出域和算法效果为评价中心 |
| 实测数据不足 | 模型缺少可信锚点 | 项目启动即开展数据盘点和补采 |
| 实测数据元数据缺失 | 无法解释和标定 | 建立最低元数据门槛和数据可信度等级 |
| 把接收机特征误当发射机特征 | 模型迁移到其他设备时失真 | 保留模块化结构并用多接收机数据交叉验证 |
| 同一数据用于拟合和验收 | 验收结果虚高 | 独立冻结验收集并由测试人员管理 |
| MATLAB算法无法代码生成 | 产品化受阻 | 提前做兼容性试验，保留Runtime进程路线 |
| 每个小组件跨进程调用 | 吞吐和时延恶化 | 高频IQ组件同进程或共享内存，服务保持粗粒度 |
| 把原始IQ持续传入浏览器 | 网络、浏览器内存和显示性能失控 | 原始IQ留在计算节点，仅按视窗返回抽取波形、频谱和瓦片 |
| B/S架构过早微服务化 | 接口复杂、排障困难、首期成本增加 | 首期采用模块化单体应用服务和单计算节点，按实测瓶颈拆分 |
| 浏览器或网络断开导致任务丢失 | 长时间试验失败、结果不可追溯 | 任务状态持久化，计算与会话解耦，重连后按事件序号恢复 |
| 前端依赖公网资源 | 断网或涉密环境无法运行 | 静态资源和依赖全部离线交付，自动执行断网验收 |
| 浏览器版本差异 | 框图交互或WebGL显示不一致 | 冻结支持浏览器基线，建立兼容回归和版本提示 |
| 100 MS/s数据量过大 | 存储和内存失控 | 分块、短窗、int16保存、按需固化和自动清理 |
| 首期强行实现阵列多站 | 同步和标定拖累主线 | 保留接口，作为独立扩展包 |
| 用户只看到炫酷界面但不能科研 | 交付后闲置 | 用5类典型任务驱动功能设计和试用验收 |
| 第三方许可处理不清 | 部署和再分发风险 | 采购、法务和技术路线同步审查 |

## 20. 投资优先级建议

资金和资源优先级建议如下：

1. 实测数据整理、补采和元数据建设；
2. 端到端IQ链和DDC/信道化正确性；
3. B/S自主框图前端、应用服务与稳定C++运行引擎；
4. 2至3类重点辐射源的实测校准；
5. 数据管理、回归测试和验收证据；
6. 计算服务器、NVMe和必要存储；
7. 用户算法接入和典型案例；
8. 根据实际瓶颈再增加GPU和集中计算；
9. 根据明确任务再建设阵列、多站和SDR；
10. 最后考虑复杂射线追踪和大型半实物系统。

不建议首期优先采购：

- 大规模GPU集群；
- 数百TB高性能存储；
- 多台高端同步SDR；
- 商业射频信道模拟器；
- 大型屏蔽设施；
- 与首批验收场景无关的全部MATLAB工具箱。

MATLAB产品和许可应在最小原型确认依赖后按开发席位、构建方式和部署数量采购。

## 21. 首期推荐基线

综合可行性、科研价值和实施风险，首期建议冻结为：

> 一套可单机一体化或内网集中部署的B/S自主框图软件＋一套应用与任务服务＋一套单计算节点C++分块IQ运行引擎＋三类信号源模式＋2至3类重点无人机辐射源＋简化传播和天线＋工程等效接收机＋ADC/DDC及信道化＋实测数据中心＋基础检测识别与评价＋冻结验收基准。

首期主输出为信道化前宽带复基带IQ，并能派生信道化后IQ。  
首期以单站、单通道、离线和准实时为默认工作方式。  
100 MS/s作为文件兼容和短窗处理能力，不作为全部模型实时运行指标。  
首期B/S界面以控制和展示为主，原始IQ主链在计算节点内运行，浏览器关闭或短时断网不终止后台任务。  
阵列、多站、复杂城市射线追踪和射频半实物作为后续独立扩展。

建议选取以下三个演示与验收场景：

1. **实测回放场景**：导入真实宽带IQ，完成频谱分析、信道化和检测识别；
2. **全合成场景**：合成典型无人机信号，经过传播、接收机、ADC/DDC生成宽带IQ；
3. **混合增强场景**：在实测背景中注入可控目标或对实测目标施加可控变化，并与独立实测数据比较。

这三个场景能够覆盖系统最核心的模型、数据、平台和算法闭环。

## 22. 最终结论

本项目具有较高可行性，但可行的前提是把建设目标从“完整高精度复现全部无人机射频系统”收敛为“生成对侦察算法有用、与真实接收机观测结果等效的宽带复基带IQ”。

最推荐的技术路线是：

1. 以接收机DDC后、信道化前宽带复基带IQ为主产品；
2. 在复基带域对辐射源、传播、天线、接收机和ADC/DDC进行模块化等效建模；
3. 以工程等效精度作为默认等级，不追求无依据的过细物理模型；
4. 同时支持全合成、实测回放和混合增强；
5. 优先建设单站、单通道、离线和准实时闭环；
6. 用实测数据拟合有效参数，并在独立数据上验证；
7. 用特征分布、变化趋势和下游算法性能评价逼真度；
8. 将MATLAB/Simulink作为内部模型生产和验证工具；
9. 采用B/S自主前端和应用服务形成统一控制面，采用C++运行引擎形成高吞吐IQ数据面；
10. 支持单机一体化和内网集中部署，全部正式功能不依赖公网资源；
11. 浏览器只处理控制、状态和降采样显示数据，原始IQ和模型间数据留在计算与存储节点；
12. 将阵列、多站、复杂传播和半实物能力后置，按真实需求扩展。

采用该路线，可以在合理的团队、周期和硬件投入下形成一套真正可供反无人机接收机研发人员日常使用的科研仿真系统。项目核心资产最终不是某一个MATLAB模型，而是自主框图平台、统一IQ规范、重点辐射源模型、实测数据资产、标定方法、冻结基准和长期可扩展的组件体系。

## 附录A 首批模型清单

| 类别 | 首期必做 | 后续可选 |
|---|---|---|
| 信号源 | 参数化信号、实测回放、混合增强、2至3类无人机模板 | 更多型号、协议级模型、个体指纹 |
| 信道 | 自由空间、双径、统计衰落、多普勒 | 射线追踪、实测信道回放 |
| 天线 | 全向、简化方向图、极化和馈线损耗 | 阵列流形、互耦和平台遮挡 |
| 接收机 | 滤波、增益、噪声、频偏、相噪、IQ不平衡、削顶 | 详细互调、杂散和器件级模型 |
| 数字前端 | ADC、DDC、单通道和多通道信道化 | FPGA实时实现 |
| 环境 | AWGN、实测背景、同邻频和宽带干扰 | 复杂城市动态背景 |
| 算法 | 频谱、瀑布、检测、参数估计、轻量识别 | AOA、TDOA、FDOA和融合定位 |
| 数据 | 导入、检查、回放、检索、版本和基准集 | 分布式数据湖和跨单位共享 |

## 附录B 项目启动时需要用户确认的事项

- 首批重点无人机或信号类别；
- 重点频段和典型接收带宽；
- 现有设备在何处能够导出IQ；
- IQ是ADC后宽带、DDC后宽带还是信道化后窄带；
- 典型采样率、位宽、通道数和记录时长；
- 接收机增益、AGC、滤波和校准信息能否获取；
- 是否具备同期背景、距离、航迹和工作状态信息；
- 首期最重要的是检测、识别还是接收机参数研究；
- 用户算法的输入输出接口；
- 是否需要阵列或多站能力，若需要是否具备同步实测数据；
- 数据保密、导入导出和交付范围；
- 首期采用单机一体化还是内网集中部署，以及预计并发用户和任务数量；
- 交付环境允许使用的浏览器、操作系统、网络分区、身份认证和证书体系；
- 是否允许安装本地启动器或设备代理，以及是否需要接入SDR、FPGA等本地硬件；
- 验收更看重数据相似性、算法性能还是使用流程。

## 附录C 术语统一

| 术语 | 本方案含义 |
|---|---|
| 原始IQ | 相对于某一处理节点未经检测识别解调的数据，必须同时说明节点 |
| 宽带复基带IQ | DDC后、信道化前，覆盖接收机当前分析带宽的复数采样 |
| 信道化IQ | 从宽带IQ经频移、滤波和抽取后得到的一个或多个子信道IQ |
| 接收机观测域等效 | 综合输出与真实设备观测在特征、趋势和算法效果上接近 |
| 全合成 | 信号源至接收IQ均由模型生成 |
| 实测回放 | 直接使用已有实测IQ作为输入 |
| 混合增强 | 将实测IQ与可控信道、噪声、干扰或接收机变换结合 |
| 模型证据包 | 模型依据、参数来源、验证数据、结果、适用范围和版本的集合 |
| 冻结基准集 | 不参与训练和调参，仅用于验收和长期回归的数据集 |

## 附录D 编制参考

- [基于MATLAB/Simulink底层算法、自主框图平台与实测数据驱动的无人机信号侦察仿真系统可行性与技术路线V3.0](基于MATLAB_Simulink的无人机信号侦察仿真系统可行性与技术路线_v3.0.md)
- [面向反无人机应用的电子侦察IQ级信号模拟仿真概念模型需求分析](08.面向反无人机应用的电子侦察IQ级信号模拟仿真概念模型需求分析.md)
- [城市反无人机电子侦察IQ仿真两阶段建设方案](城市反无人机电子侦察IQ仿真两阶段建设方案.md)
- [信号侦察模型信号级与IQ级模拟能力说明](信号侦察模型_信号级与IQ级模拟能力说明.md)
- [信号特征数据库说明](signal%20signature%20database.md)
