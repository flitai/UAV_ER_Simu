# data/scene 目录

场景数据包，每个观测区域一个子目录。规范见 `docs/scene-package.md`，生产脚本见 `scene/`。

目录名与文件名必须是纯 ASCII，不含空格。出包前跑 `scripts/check-ascii.sh data/scene`。

## 现状

空。首个观测区域待定，临时计划用 em-demo 的西安范围
`108.8805,34.2956,109.0005,34.4056` 验证生产管线。
