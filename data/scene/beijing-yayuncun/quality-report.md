# 场景数据包质量报告：beijing-yayuncun

> 生成时间 2026-09-04T03:03:41Z，脚本 `scene/quality_report.py`。对应里程碑 D0-4（管线对拍）与 D0-7（质量报告）。
> 本报告的每个数字都由脚本从产物与原始快照现算，不是手抄。

## 1. 被检对象

| 产物 | 体积 | sha256 前 16 位 |
|---|---|---|
| `buildings.geojson` | 15909168 字节 | `269a8674706766e0` |
| `osm-buildings-raw.json`（对拍基准） | 34441515 字节 | `d5d405e27dd17375` |
| `basemap-slice.pmtiles` | 11147792 字节 | `60691abd6cb206dd` |

观测区域 `[116.288, 39.9, 116.522, 40.08]`，约 19.96 × 19.97 km；建筑实际覆盖 [116.279297, 39.89288, 116.531982, 40.086477]（瓦片对齐，略大于观测区域）。

## 2. 三条不可比因素（先读这一节，再看差异）

1. **数据时间不同**。瓦片来自 OSM 快照 `2026-08-17T04:00:00Z`，原始标签是 `2026-09-04T02:57:36Z` 拉取的实时数据，中间有编辑漂移。计数差异首先应归因于此，而不是管线缺陷。
2. **坐标精度不同**。矢量瓦片在 zoom 15 上按 4096 格量化，约 0.23 米一格；原始 OSM 是任意精度。
3. **关系型建筑不参与逐栋几何比较**。multipolygon 的内外环装配不在本报告范围，只比计数。

## 3. 要素计数

| 项目 | 数量 |
|---|---|
| 瓦片解码（本产物） | 47582 |
| 原始 OSM（同范围） | 47572 |
| 两边都有，按 (类型, 编号) 精确对上 | 47449（瓦片侧 99.72%，OSM 侧 99.74%） |
| 只在瓦片解码 | 133：way 131，relation 2 |
| 只在原始 OSM | 123：way 117，relation 6 |

对齐用的是编号而不是几何近邻：瓦片要素 id 的编码经实测为 `(类型 << 44) | OSM 编号`，类型 2 = way、3 = relation。两侧各有百余个对不上，量级与 18 天的编辑漂移相符。

## 4. 几何差异（逐栋，仅 way）

| 指标 | 值 |
|---|---|
| 参与比较的建筑 | 47100 |
| 面积相对差的中位数 | +0.008% |
| 面积相对差绝对值 q50 / q90 / q99 | 0.321% / 1.192% / 4.009% |
| 相对差绝对值超过 5% 的 | 297（0.63%） |
| 总占地：本产物 / 原始 OSM | 47979536 m² / 48207452 m²，相差 -0.473% |
| 顶点数：本产物 / 原始 OSM | 合计 338768 / 332902，即 101.8% |
| 顶点数相同 / 更多 / 更少的建筑 | 38456（81.65%） / 4666 / 3978 |

面积几乎不变而顶点数有增有减，两个方向的成因不同，已分别取证：

- **顶点变多**（4666 栋，中位多 2 个）来自跨瓦片合并的接缝。抽样核对：多出顶点的建筑里横跨两块瓦片的占 56.60%，而顶点数相同的对照组只占 1.73%。
- **顶点变少**（3978 栋）是瓦片简化去掉了近乎共线的点。面积中位差只有 +0.008%，说明简化没有削掉体量。

## 5. 高度来源

| 来源 | 数量 | 占比 | 含义 |
|---|---|---|---|
| `osm:height` | 828 | 1.74% | OSM `height` 标签，真实标注 |
| `osm:levels` | 9322 | 19.59% | OSM `building:levels` 折算，层数 × 3 + 2 米 |
| `tile:height` | 56 | 0.12% | 瓦片高度，对不上原始标签（数据漂移），来源不明 |
| `est:area` | 37376 | 78.55% | **估算值**，按占地面积分档（铁律 14 要求可识别） |

**近八成建筑的高度是估算的**，这是本数据包最大的欠项。原始 OSM 在本区域只有 833 个要素带 `height` 标签、9555 个带 `building:levels`，这是数据源本身的覆盖率，不是管线问题。估算值不得用于验收指标。

## 6. 高度与体量分布

| 指标 | 值 |
|---|---|
| 高度 q10 / q50 / q90 / p99.9 / 最高 | 5.0 / 20.0 / 38.0 / 100.0 / 528.0 m |
| 超过 100 m / 200 m 的建筑 | 47 / 16 栋 |
| 占地面积 q10 / q50 / q90 | 90.22 / 666.77 / 2005.99 m² |
| 总占地 | 49714066 m² |
| 带洞 / 多重多边形 | 254 / 36 栋 |
| `base_m` 非零 | 9 栋 |

`base_m` 几乎全为 0：原始 OSM 在本区域只有 7 个要素带 `min_height`、4 个带 `building:min_level`。架空层信息在数据源里就没有。

## 7. 结论与限制

- 几何可用：与原始 OSM 逐栋比较，面积中位差 +0.008%；另经像素级对拍，与底图自带建筑层的交并比为 0.993 至 0.999。
- 高度不可用于验收：近八成是估算值，一成九是层数折算，真实标注不足 2%。
- 架空层高度缺失：数据源没有。
- 升级路径：拿到更新的 OSM 标签或人工数据后，只改 `height_m`、`base_m` 与 `src`，**不改 `id` 与几何**，下游引用因此不会失效。

