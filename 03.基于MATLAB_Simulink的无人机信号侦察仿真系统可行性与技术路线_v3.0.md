# 基于 MATLAB/Simulink 底层算法、自主框图平台与实测数据驱动的无人机信号侦察仿真系统可行性与技术路线

> 文档日期：2026年8月22日  
> 文档版本：V3.0  
> 项目属性：科研条件建设项目  
> 主要用户：面向反无人机信号接收机及无人机信号检测、识别、测向和定位设备的科研与研发人员

### 版本修订说明

| 版本 | 日期 | 主要变化 |
|---|---|---|
| V1.0 | 2026年8月22日 | 完成可行性、重点难点、模型体系和技术路线分析 |
| V2.0 | 2026年8月22日 | 增加自主前端、自主框图运行引擎、MATLAB算法组件化部署、混合后端及产品化边界设计 |
| V3.0 | 2026年8月22日 | 增加实测信号数据资产体系，并区分交付前研发阶段和交付后科研使用阶段的数据作用、管理方式与验收要求 |

## 1. 项目背景

拟建设一套面向无人机信号侦察研究的信号级仿真模拟系统。系统借鉴 MATLAB/Simulink 的图形化建模理念，通过自主开发的软件前端和模块框图串接形式，对无人机辐射源、城市电磁传播环境、信号侦察接收机以及检测、识别、测向和定位算法进行灵活组合。

项目希望在内部充分利用 MATLAB/Simulink 生态中成熟的通信、信号处理、射频、阵列和仿真模型，减少通用底层算法的重复开发；对外则形成具有自主品牌、自主界面、自主模型组织方式和自主运行管理能力的软件产品。普通用户不需要接触 MATLAB 界面、`.m` 文件或 `.slx` 模型，而是通过项目自研的框图编辑器、场景工具和试验管理界面开展科研工作。

项目现有或后续采集的实测信号数据集，应从辅助测试资料提升为平台核心数据资产。实测数据既用于交付前的辐射源建模、接收机标定、算法训练、仿真可信度验证和验收基线建设，也用于交付后的真实信号回放、用户算法评价、模型迭代、数据扩充和能力复核。

## 2. 总体结论

该建设思路在技术上总体可行，并且适合作为科研条件建设项目实施。

项目不宜简单定位为“采购 MATLAB 工具箱并搭建若干框图”，而应定位为：

> **面向无人机信号侦察算法与接收机体制验证的分层、多保真、开放式数字试验平台。**

V3.0版明确平台的产品化技术定位：

> **自主框图软件前端 + 自主多速率运行引擎 + 标准算法插件接口 + MATLAB/Simulink内部模型生产工具链。**

并进一步确立实测数据的基础作用：

> **以实测数据建立模型可信度锚点，以合成数据扩展场景覆盖，以固定基准集保障交付验收和长期可比。**

MATLAB/Simulink 可以提供图形化组合、通用通信信号、数字信号处理、射频前端、阵列处理、传播信道、自动测试和半实物接入等基础能力。项目需要重点自主建设的部分包括：

- 无人机特定及私有辐射源模型；
- 无人机工作状态与辐射行为模型；
- 面向城市环境的传播参数和实测标定数据；
- 侦察接收机非理想特性及校准模型；
- 检测、识别、测向、定位和融合算法模型；
- 模型接口规范、可信度评价和版本管理体系；
- 面向科研用户的场景配置、批量试验和结果评估工具。
- 自主框图编辑器、模型描述格式和多速率调度执行引擎；
- MATLAB生成组件、自研C/C++组件、GPU/FPGA和SDR组件的统一插件接口。
- 实测I/Q数据管理、回放、标注、质量检查、数据集划分和版本冻结能力；
- 合成信号与实测信号的特征对比、参数拟合和模型可信度评估体系。

项目的主要风险不是“Simulink 能否连接这些模块”，而是模型真实性、宽带多通道计算效率、实测数据闭环、自主运行引擎复杂度以及模型资产的长期治理。

## 3. 可行性分析

| 建设内容 | 可行性 | 分析结论 |
|---|---:|---|
| 框图化模型串接 | 高 | Simulink 原生适合层次化、组件化和多速率建模 |
| 通用调制、编码和传播信道 | 高 | 可大量复用 Communications Toolbox 等现有能力 |
| 射频前端及接收机非理想特性 | 较高 | 可用 RF Blockset，并结合设备指标和实测数据参数化 |
| 阵列测向、波束形成和定位 | 较高 | 可复用阵列、DOA、TDOA及相关信号处理能力 |
| 无人机私有或非公开信号 | 中等 | 通常缺少可直接使用的完整模型，需要实测、分析和自研 |
| 复杂城市电磁传播 | 中等 | 可进行统计信道和射线追踪，但真实性依赖场景数据及标定 |
| 多站、多通道、长时间信号级仿真 | 中等 | 技术上可实现，但数据吞吐、内存和计算时间压力较大 |
| SDR和真实设备半实物接入 | 较高 | MATLAB/Simulink支持多类软件无线电硬件 |
| 多用户科研服务 | 较高 | 需要增加模型库、任务管理、权限、版本和试验数据管理能力 |
| 自主前端与MATLAB算法集成 | 高 | 可通过C/C++生成库、Compiler SDK组件、FMU或服务器API集成 |
| 完全脱离MATLAB Runtime运行 | 较高 | 采用MATLAB Coder或Simulink Coder，但需验证各算法的代码生成支持情况 |
| 实测I/Q回放与算法验证 | 高 | 原始I/Q及必要元数据可直接形成标准信号源和固定测试集 |
| 利用实测数据标定辐射源模型 | 较高 | 能够拟合时频、统计和硬件特征，但需区分信源、信道和接收机影响 |
| 多站定位数据集利用 | 取决于数据 | 只有具备可靠站间同步、站址和目标真值时，才能支持高可信TDOA/FDOA验证 |

Communications Toolbox能够生成标准或自定义波形，加入射频损伤和传播信道，并支持频谱监测、人工智能数据集生成及OTA试验。RF Blockset可用于建模噪声、非线性、互调、相位噪声、镜像抑制和直流偏置等射频前端特性。

Phased Array System Toolbox能够支持阵列建模、波束形成、DOA估计以及多基地信号处理，并可作为AOA、TDOA和FDOA定位算法研究的基础。

总体上，MATLAB生态可以覆盖大部分通用基础能力，但不能直接替代无人机专用辐射源建模、实测数据分析、设备参数标定和专业算法研发。

## 4. 建设目标与边界

### 4.1 建议建设目标

系统应支持以下科研活动：

- 无人机遥控、遥测、图传和数传等辐射源信号建模；
- 城市环境下无人机信号传播和接收特性研究；
- 侦察接收机体制、参数和处理链设计；
- 无人机信号检测、分类识别和未知类拒识研究；
- 单站测向及多站AOA、TDOA、FDOA定位研究；
- 多源、同频、弱信号、强干扰和非理想接收条件下性能分析；
- 算法训练数据、测试数据和标准化试验报告生成；
- 软件在环、处理器在环、硬件在环和SDR半实物验证。

### 4.2 建议明确的项目边界

本项目首先应建设科研仿真与试验验证能力，不宜在首期同时追求：

- 对所有无人机型号和所有私有协议的完整复现；
- 全城市、全频段、全时段的最高精度电磁仿真；
- 所有模型均达到实时运行；
- 仿真模型未经标定即可等同于真实设备；
- 一套模型同时满足科研探索、产品定型和鉴定测试的全部要求。

## 5. 总体技术架构

```text
场景与任务控制
  ├─ 城市地形、建筑、道路和电磁环境
  ├─ 无人机航迹、姿态和任务状态
  ├─ 辐射源启停、突发、频率和组网行为
  └─ 背景信号、干扰源及试验想定
                    │
                    ▼
无人机辐射源模型 → 天线与传播环境 → 侦察接收机 → 检测/识别/定位 → 评估分析
  波形与协议层        城市多径         阵列与射频      算法模型          指标统计
  发射行为模型        遮挡与衰落       ADC与信道化     融合与跟踪        数据集生成
  射频指纹模型        时延与多普勒     时钟与校准      未知类拒识        试验报告

实测数据资产层
  ├─ 原始单站/多站IQ数据及元数据
  ├─ 标注、质量检查、训练/验证/测试集
  ├─ 实测信号回放、参数拟合与模型标定
  └─ 固定验收基准、用户科研数据与增量数据
             │                  │
             ├────校准模型──────┤
             └────验证算法与平台─┘
```

平台应同时维护三类信息流：

1. **信号数据流**：复数I/Q采样、阵列多通道数据和中间处理结果；
2. **控制与元数据流**：采样率、中心频率、带宽、时间戳、坐标系、姿态、模型版本和随机种子；
3. **仿真真值流**：辐射源类别、真实位置、工作状态和信道参数，仅用于评价，不得泄漏给被测算法。
4. **实测数据与证据流**：原始I/Q、采集元数据、标签、质量信息、数据集版本和模型标定结果，用于回放、训练、验证和可信度评价。

## 6. 分层、多保真建模方案

系统不应强制所有模块都以最高精度运行。建议建立三级仿真保真度。

### 6.1 L0：参数/事件级

主要传递位置、功率、频率、带宽、工作状态、检测概率等参数，用于：

- 大规模场景推演；
- 侦察设备配置研究；
- 站点部署和能力覆盖分析；
- 快速蒙特卡洛试验。

### 6.2 L1：复基带I/Q级

以离散复数I/Q数据为主体，是本项目的核心层级，用于：

- 信号检测和参数估计；
- 调制及协议特征识别；
- 射频指纹研究；
- 阵列测向；
- TDOA/FDOA定位；
- 接收机数字处理链研究。

### 6.3 L2：射频等效/半实物级

在I/Q级基础上增加详细设备特性，或接入真实SDR和接收机，用于：

- 射频非线性及互调研究；
- 相位噪声、频偏和采样时钟误差分析；
- ADC量化、削顶及动态范围研究；
- 阵列通道幅相误差研究；
- 真实设备或处理板卡在环验证。

同一功能应支持多种保真度模型替换，例如：

```text
传播模型 = 自由空间 / 统计衰落 / 城市射线追踪 / 实测信道回放
辐射源   = 理想波形 / 含发射机缺陷的合成波形 / 实测IQ回放
接收机   = 理想接收机 / 参数化射频前端 / SDR或真实接收设备
```

## 7. 重点建设内容

### 7.1 无人机辐射源模型库

建议将单个辐射源拆分为以下四层：

1. **发射行为层**：启停、突发周期、频率选择、带宽变化、链路建立和工作状态切换；
2. **波形与协议层**：调制、编码、帧结构、导频、同步结构及占用带宽；
3. **发射机特征层**：频偏、IQ不平衡、功放非线性、相位噪声、杂散和启动瞬态；
4. **天线与运动层**：方向图、极化、安装位置、无人机姿态和旋翼运动影响。

对私有或不完全掌握的无人机信号，可采用三条技术路线：

- 实测I/Q信号直接回放；
- 根据观测特征建立半经验信号模型；
- 在掌握可靠技术依据后建立协议级可控模型。

实测I/Q回放应作为平台的标准辐射源类型，与合成信号模型具有相同的接口和调度能力。

### 7.2 侦察接收机模型库

建议覆盖以下处理环节：

- 单天线及阵列天线；
- 馈线、滤波器、低噪声放大器、混频器和AGC；
- 噪声系数、动态范围、互调、镜像、阻塞和杂散；
- ADC量化、削顶和采样抖动；
- 数字下变频、滤波、抽取和信道化；
- 频谱估计、能量检测和特征检测；
- 参数估计、特征提取和信号分类；
- 未知类检测和开集识别；
- AOA、TDOA、FDOA及多站融合定位；
- 航迹关联、状态估计和连续跟踪。

### 7.3 城市场景与传播模型库

建议建立：

- 自由空间传播模型；
- 地面双径及地形遮挡模型；
- 统计阴影、快衰落和多径模型；
- 城市经验传播模型；
- 基于建筑几何的射线追踪模型；
- 传播预计算、缓存和动态插值机制；
- 实测路径损耗及时延扩展数据集。

需要注意，三维场景在视觉上逼真并不意味着无线传播模型可信。应将建筑几何、建筑电磁材料参数和无线传播参数分开管理，并通过典型街区实测数据进行标定。

### 7.4 场景、试验与评价系统

建议提供：

- 无人机、辐射源、接收站和干扰源的图形化配置；
- 航迹、工作状态和事件序列编辑；
- 典型场景模板；
- 参数扫描和蒙特卡洛试验；
- 随机种子管理；
- 试验数据自动记录；
- 检测、识别和定位性能统计；
- 基线对比和回归测试；
- 自动生成试验报告。

## 8. 关键难点及解决建议

### 8.1 辐射源模型真实性

难点不只是生成相似的功率谱，而是保留侦察算法真正依赖的时频、统计和硬件特征。

建议为每个辐射源建立“模型证据包”，至少包括：

- 数据和技术依据来源；
- 适用频段及工作状态；
- 时域、频域和时频域特征；
- 突发长度、重复周期和占空比统计；
- 频偏、瞬态、循环平稳特征及射频指纹；
- 与实测数据的对比结果；
- 适用边界、误差范围和可信度等级。

### 8.2 合成数据与真实数据的域差异

完全依靠合成信号训练识别模型，容易出现仿真性能高、真实数据性能下降的问题。

建议建立“合成—实测—校准—再生成”的闭环：

1. 采集具有代表性的真实信号；
2. 提取可量化特征和统计分布；
3. 调整辐射源、传播和接收机模型参数；
4. 用未参与标定的实测数据进行独立验证；
5. 持续更新模型适用范围和可信度。

### 8.3 宽带、多通道计算压力

100 MS/s复数单精度I/Q约为0.8 GB/s/通道；8通道阵列约为6.4 GB/s，仿真10秒原始数据就可能达到64 GB。若使用双精度，数据量还会进一步增加。

建议采用：

- 帧式和分块流处理；
- 按需保留全采样率；
- 尽早进行数字下变频、信道化和抽取；
- 默认不记录全部中间I/Q数据；
- 将无人机运动、传播参数更新和I/Q采样设置为不同时间尺度；
- 对射线追踪结果进行预计算和插值；
- 使用并行计算开展参数扫描和蒙特卡洛试验；
- 对实时要求高的环节采用C/C++、GPU或FPGA加速。

### 8.4 多站定位的同步与校准

如果TDOA/FDOA模型只考虑理想传播时延，而不考虑各接收站的独立时钟和射频通道误差，仿真结果会明显过于理想。

每个接收站应独立建模：

- 采样时钟偏差和漂移；
- 本振频偏和相位噪声；
- 时间戳误差和网络传输抖动；
- 天线、馈线和滤波器群时延；
- 阵列通道幅相不一致；
- 站间同步与校准过程。

### 8.5 多人协作和模型复用

建议制定统一模型接口规范。每个模型必须声明：

- 输入输出数据类型、维度和单位；
- 采样率、中心频率和有效带宽；
- 时间戳、坐标系和姿态定义；
- 算法延迟、初始状态和状态复位条件；
- 随机种子；
- 保真度等级；
- 模型版本、数据来源和验证状态。

大型组件应采用引用模型、引用子系统和变体模型，避免在不同场景中复制模型。公共接口、枚举、总线和参数应放入统一数据字典。

## 9. MATLAB/Simulink产品组合建议

不建议项目初期一次性采购全部工具箱。应先完成最小原型，根据实际模型依赖、用户数量和部署方式确定许可组合。

### 9.1 基础核心

- MATLAB；
- Simulink；
- Communications Toolbox；
- Signal Processing Toolbox；
- DSP System Toolbox。

### 9.2 按需选配

- **RF Blockset**：射频前端及非理想特性；
- **Phased Array System Toolbox**：阵列、波束形成、测向和定位；
- **Antenna Toolbox**：天线方向图、阵列和安装影响；
- **UAV Toolbox**：无人机航迹、姿态和场景表达；
- **WLAN、5G、LTE、Bluetooth等工具箱**：对应制式的波形和参考模型；
- **Parallel Computing Toolbox/MATLAB Parallel Server**：批量仿真及数据生成；
- **Simulink Test/Requirements Toolbox**：回归测试、试验管理和需求追踪；
- **SDR支持包/Wireless Testbench**：真实信号采集和OTA试验；
- **HDL Coder/Simulink Real-Time**：FPGA实现和硬件在环验证；
- **MATLAB Coder/Simulink Coder**：生成可集成到自主软件中的C/C++代码、静态库或动态库；
- **MATLAB Compiler SDK**：将MATLAB算法封装为C/C++、.NET、Java、Python组件或微服务；
- **Simulink FMU Builder**：将较大粒度Simulink模型封装为标准FMU组件；
- **Simulink Compiler**：快速形成独立仿真应用或验证部署可行性；
- **MATLAB Production Server**：面向多用户的集中式算法服务和批量计算。

需要在采购前核实工具箱之间的依赖关系、并发用户数、并行计算节点许可、硬件支持范围以及部署模型的代码生成兼容性。

## 10. 用户使用模式

建议平台区分内部模型生产环境和对外科研试验环境，而不是将Simulink直接作为最终用户界面。

### 10.1 专家建模界面

面向项目内部或经过授权的辐射源、接收机和算法模型开发人员，允许进入MATLAB/Simulink进行：

- 模块开发与调试；
- 模型接口定义；
- 算法替换；
- 单元测试；
- 模型标定；
- 性能优化。

### 10.2 科研试验界面

面向一般科研用户，通过项目自研的软件前端完成：

- 选择无人机和辐射源；
- 选择城市环境和航迹；
- 配置接收机、阵列和站点；
- 选择检测、识别及定位算法；
- 设置参数扫描范围；
- 一键运行并生成评价报告。

科研用户看到的是项目自定义的组件名称、图标、参数、端口、框图和试验流程，不需要安装、启动或操作MATLAB/Simulink。这样既能保护底层算法实现，也能兼顾模型开放性与普通用户的易用性。

## 11. 自主框图平台与MATLAB算法隐式集成方案

### 11.1 总体设计原则

MATLAB/Simulink应作为内部算法开发、模型验证和组件生产工具，而不应成为最终产品的前台框架和唯一运行内核。

产品应形成三层隔离：

1. **模型生产层**：内部使用MATLAB、Simulink及相关工具箱开发和验证算法；
2. **组件构建层**：将算法生成或封装为标准动态库、FMU、运行时组件或服务；
3. **产品运行层**：由自主前端和自主运行引擎加载、连接、调度和监控算法组件。

用户侧原则上不交付`.m`源文件和`.slx`源模型，也不直接展示MATLAB或Simulink界面。产品品牌、术语、模型分类、图标、参数表单、运行控制、数据管理和报告全部由项目自主定义。

需要强调，“前台不呈现MATLAB”可以通过合理的软件架构实现；但不应将其理解为规避软件许可、删除依法或依约必须保留的声明，或者对底层第三方技术来源作不实表述。具体许可、再分发和第三方声明要求，应以MathWorks采购合同、许可条款和项目法务意见为准。

### 11.2 产品化总体架构

```text
┌────────────────────自主软件前端────────────────────┐
│ 模型组件库 │ 框图画布 │ 参数配置 │ 场景编辑 │ 结果展示 │
│ 连线检查   │ 运行控制 │ 试验管理 │ 数据管理 │ 报告生成 │
└────────────────────────┬───────────────────────────┘
                         │ 自定义模型描述文件
                         │ JSON / 数据库 / 专用DSL
                         ▼
┌────────────────────自主模型运行引擎────────────────┐
│ 图模型校验 │ 拓扑排序 │ 多速率调度 │ 缓冲与时间戳   │
│ 任务并行   │ 数据记录 │ 状态管理   │ 故障与异常隔离 │
└────────────────────────┬───────────────────────────┘
                         │ 统一组件接口
          ┌──────────────┼───────────────┬───────────┐
          ▼              ▼               ▼           ▼
  MATLAB生成库      Runtime封装组件     自研C/C++   GPU/FPGA/SDR
  DLL/SO/静态库     或远程算法服务       算法组件     硬件组件
```

### 11.3 推荐的四种MATLAB算法部署方式

#### 11.3.1 C/C++代码或库生成——最终产品主路线

使用MATLAB Coder或Simulink Coder，将算法生成C/C++源代码、静态库、Windows DLL或Linux SO，再由自主运行引擎调用。

主要优点：

- 目标机不需要安装MATLAB；
- 不依赖MATLAB Runtime；
- 启动速度、实时性和内存控制较好；
- 便于与共享内存、GPU、SDR和实时处理框架结合；
- 用户界面和运行过程不出现MATLAB；
- 适合FFT、滤波、信道化、检测、测向等高频调用组件。

主要限制：

- 不是所有MATLAB函数和工具箱能力都支持代码生成；
- 动态类型、动态脚本、部分图形和交互能力需要改写；
- 必须建立代码生成支持清单和等价性回归测试。

#### 11.3.2 MATLAB Compiler SDK封装——快速原型和兼容路线

将MATLAB算法封装为C/C++共享库、.NET程序集、Java包、Python包或容器化微服务，再由自主软件调用。

主要优点：

- 对原有MATLAB代码改造较少；
- 可较快打通自主前端到MATLAB算法的调用链；
- 打包代码可加密，便于保护算法知识产权；
- 适合复杂但调用频率相对较低的算法。

主要限制：

- 目标环境需要MATLAB Runtime；
- 安装包、启动时间和内存占用通常大于原生C/C++组件；
- 不适合将每个小框图节点分别启动成独立Runtime进程；
- 宽带I/Q数据跨语言传递时要减少内存复制。

#### 11.3.3 FMU标准组件——大粒度模型交换路线

使用Simulink FMU Builder将Simulink模型封装为符合FMI标准的FMU。适合传播模型、无人机运动模型、控制逻辑和较大粒度信号处理子系统。

FMU有利于建立标准化插件和第三方模型接入机制，但对于大带宽、多通道、连续I/Q数据处理，必须实测接口调用、数据复制和调度开销。

#### 11.3.4 集中式算法服务——多用户与离线任务路线

可使用MATLAB Production Server或自建服务，将完整算法或仿真任务发布为REST、gRPC或其他服务接口。

适用于：

- 多用户共享集中算力；
- 大规模离线数据生成；
- 参数扫描和蒙特卡洛试验；
- 统一控制算法版本；
- 涉密或高价值模型不下发客户端的场景。

不建议把每个滤波器、FFT或小粒度处理节点做成一次远程调用。对于宽带I/Q数据，应以完整处理链、完整任务或较大粒度组件为服务边界，减少网络传输和序列化开销。

### 11.4 推荐的混合后端策略

| 模型类型 | 推荐部署形态 |
|---|---|
| FFT、滤波、信道化、检测等高频调用模型 | C/C++动态库或静态库 |
| 阵列处理、DOA和实时定位模型 | C/C++库、GPU组件或硬件组件 |
| 复杂但低频调用的MATLAB算法 | Compiler SDK组件 |
| 无人机运动、状态机和传播大模型 | C/C++生成模型或FMU |
| 大规模离线仿真与数据集生成 | 集中式算法服务或计算集群 |
| AI训练 | MATLAB/Python离线环境 |
| AI推理 | ONNX、C++、GPU或代码生成组件 |
| SDR采集和实时流接口 | 原生C/C++及厂商硬件SDK |
| 项目自主算法 | C++、CUDA、FPGA或其他原生插件 |

最终运行引擎只识别统一组件接口，不应关心组件最初由MATLAB、Simulink、C++还是其他工具开发。

### 11.5 自主模型组件规范

每个算法组件建议提供统一生命周期接口：

```text
create       创建模型实例
configure    配置模型参数
initialize   分配资源并初始化状态
process      处理一帧或一个数据块
step         推进一步仿真时间
reset        复位内部状态
getStatus    获取状态、性能和诊断信息
destroy      释放模型资源
```

一个完整模型组件包建议包括：

```text
manifest.json        组件标识、版本和依赖
interfaces.json      输入输出端口及数据类型
parameters.json      参数定义、范围、单位和默认值
icon.svg             自主前端显示图标
model.dll/.so/.fmu   可执行模型组件
document.md          使用说明和适用边界
tests/               测试向量及基准结果
signature/           完整性校验与数字签名
```

模型清单应记录组件来源、构建工具版本、工具箱依赖、目标平台、保真度等级、验证状态和许可证类别，但这些内部治理信息不必全部呈现在普通用户界面中。

### 11.6 标准数据端口

建议优先定义以下通用端口类型：

- `IQStream`：单通道复数I/Q流；
- `ArrayIQStream`：阵列多通道I/Q流；
- `SpectrumFrame`：功率谱或时频分析结果；
- `DetectionList`：信号检测结果；
- `FeatureVector`：信号特征向量；
- `EmitterReport`：辐射源属性报告；
- `BearingReport`：测向结果；
- `PositionReport`：定位结果；
- `TrackReport`：目标航迹结果；
- `GroundTruth`：仿真真值；
- `ControlEvent`：状态、事件和控制命令。

端口数据必须携带或能够关联以下元数据：

- 采样率、中心频率和有效带宽；
- 数据帧长度和通道数；
- 帧起始时间及统一时间基准；
- 坐标系和姿态定义；
- 数据精度、量化方式和有效性标志；
- 处理延迟和质量标识。

### 11.7 自主运行引擎的关键能力

自主框图系统的技术核心不是画布，而是能够正确执行图模型的运行引擎。至少需要实现：

- 端口类型和维度检查；
- 图连接合法性检查和拓扑排序；
- 多采样率、多帧长和事件驱动调度；
- 数据缓冲、背压和流量控制；
- 时间戳传播和延迟补偿；
- 模型状态初始化、复位和保存；
- CPU、GPU和独立进程任务调度；
- 插件加载、版本检查和崩溃隔离；
- 指定节点数据观测和按需记录；
- 批量试验、随机种子和可重复运行；
- 运行性能监测和异常诊断。

典型模型可能具有完全不同的运行速率：

```text
无人机运动模型：       100 Hz
传播参数更新：         10～100 Hz
接收机I/Q处理：        20～200 MS/s
信号识别：             每50～500 ms运行一次
定位融合：             1～20 Hz
结果显示：             5～20 Hz
```

这些组件不能简单按照相同周期轮询，必须由调度器管理各自的时间尺度、缓存、时延和数据转换。

### 11.8 不建议采用MATLAB Engine作为最终运行方式

MATLAB Engine可以在开发阶段让C/C++或其他程序调用后台MATLAB进程，适合原型联调和算法对比。但运行计算机需要安装MATLAB，且整体仍依赖MATLAB进程，因此不宜作为正式交付产品的主要后端。

### 11.9 产品呈现与知识产权保护

自主软件前端建议包括：

- 左侧模型组件库；
- 中间拖放式框图画布；
- 右侧参数、接口和帮助面板；
- 下方日志、时域、频谱、时频和定位结果；
- 顶部场景配置、运行控制、参数扫描和报告功能；
- 后台模型版本、权限、签名和计算资源管理。

算法知识产权保护建议同时采用：

- 不交付MATLAB和Simulink源模型；
- 动态库、FMU或加密部署包交付；
- 组件数字签名和完整性校验；
- 重要算法采用独立进程或服务器部署；
- 模型参数分级授权；
- 运行日志中避免暴露内部工具链和源路径；
- 对外文档采用项目自主模型名称和接口规范。

## 12. 实测信号数据资产体系

### 12.1 核心价值与基本判断

实测信号数据能够把平台从理论模型组合工具提升为具有真实依据的模型标定、算法验证和科研试验平台。其主要价值体现在：

- 建立可重复使用的真实信号回放能力；
- 提取无人机辐射源的真实时频、统计和硬件特征；
- 标定合成辐射源、传播信道和侦察接收机模型；
- 训练和验证检测、识别、未知类拒识及定位算法；
- 衡量合成信号和真实信号之间的差异；
- 建立交付验收和后续回归测试的固定基准；
- 沉淀难以被通用工具箱替代的专业数据资产。

建议建立以下闭环：

```text
实测信号 → 数据质量检查 → 特征分析 → 模型参数标定 → 合成信号生成
    ↑                                                     ↓
真实数据复核 ← 固定基准测试 ← 算法与平台对比 ← 多场景仿真试验
```

需要注意，单站接收机采集到的I/Q数据通常同时包含发射源特征、传播信道影响和采集接收机影响，不能直接将全部特征解释为无人机发射机特征。必须结合采集条件、接收机标定和多环境对比进行分离分析。

### 12.2 实测数据的主要使用方式

#### 12.2.1 真实信号回放

将实测数据封装为自主框图平台中的标准“实测信号源”模型：

```text
实测IQ数据集 → 数据片段选择 → 功率/频偏/时间变换
              → 信道及接收机附加 → 被测算法 → 真值评价
```

用户可配置：

- 数据集、记录和信号片段；
- 起始时间、持续时间和回放次数；
- 中心频率、采样率和目标输出带宽；
- 功率缩放、频率偏移和时间偏移；
- 单次、循环、顺序或随机回放；
- 多路信号叠加；
- 噪声、干扰、衰落和接收机缺陷；
- 标签是否作为评价真值输出。

#### 12.2.2 辐射源模型标定

实测数据可以帮助确定或校准：

- 中心频率、瞬时带宽和功率谱形状；
- 突发长度、重复周期和占空比；
- 跳频、扫频或频率切换特征；
- 调制、帧结构、同步和导频特征；
- 载波频偏、采样时钟偏差和漂移；
- IQ不平衡、相位噪声和功放非线性；
- 启动、关断和功率切换瞬态；
- 杂散、带外泄漏和邻道特征。

由此形成三个层级的辐射源模型：

1. **实测回放模型**：真实性高，参数可调范围有限；
2. **实测特征驱动模型**：保留实测统计特征，并可调整功率、带宽、频偏和工作状态；
3. **协议或机理模型**：能够修改调制、编码、帧结构和发射行为。

三类模型应使用相同的标准端口，使科研用户可以在框图中直接替换。

#### 12.2.3 检测与识别算法研发

实测数据可用于：

- 弱信号检测和门限设计；
- 调制方式及信号类别识别；
- 无人机型号或链路类型识别；
- 未知信号发现和开集识别；
- 发射个体射频指纹研究；
- 深度学习模型训练、验证和独立测试；
- 虚警、漏警和误分类样本分析。

必须区分“信号类型识别”和“发射个体识别”。发射个体识别尤其容易受到接收机、距离、信道、增益设置和环境变化影响。若训练和测试数据均来自同一接收机、同一地点或同一次采集，算法可能学习到采集环境而不是真正的发射机指纹。

#### 12.2.4 接收机设计验证

同一段实测信号可以重复注入不同虚拟接收机链路，用于比较：

- ADC位数和采样率；
- 接收机带宽和信道化方案；
- AGC参数和动态范围；
- 数字下变频、滤波和抽取；
- 强信号阻塞、同频和邻频影响；
- 检测门限、帧长和处理延迟。

如果数据是在实际接收机输出端采集，原数据已包含该接收机的频响、噪声、增益和非线性。再次附加虚拟接收机模型时，应避免重复施加同类影响，或先完成接收机响应补偿。

#### 12.2.5 测向与定位算法验证

多通道或多站同步数据可以用于阵列校准、AOA、TDOA、FDOA和融合定位研究，但需要具备：

- 各通道或各站的精确坐标；
- 统一时间基准和可靠时间戳；
- 采样时钟、本振和同步方式说明；
- 天线、馈线和通道固定时延；
- 无人机位置、速度和航迹真值；
- 丢帧、失锁和时间戳异常记录。

不同站点独立录制但没有可靠时间同步的数据，仍可用于单站检测、识别和部分测向研究，但通常不能直接作为高精度TDOA验证数据。

#### 12.2.6 合成与实测联合数据生成

实测数据无法覆盖全部信噪比、距离、干扰和城市环境，建议采用“少量真实数据锚定真实性，大量合成数据扩展覆盖”的路线：

```text
真实信号片段
  ├─ 添加不同噪声和信噪比
  ├─ 添加频偏、时钟漂移和采样误差
  ├─ 叠加背景通信信号和干扰
  ├─ 通过统计信道或城市多径模型
  ├─ 模拟不同接收机非理想性
  └─ 生成具有准确标签的扩展数据集
```

还可以建立实测与机理模型之间的残差模型，将理论模型未覆盖的统计扰动附加到合成信号上，以缩小仿真域与真实域之间的差距。

### 12.3 数据集质量等级

| 数据形态 | 价值等级 | 主要用途 |
|---|---:|---|
| 频谱截图或时频图图片 | 低 | 展示、人工分析和资料说明 |
| 已提取特征数据 | 中低 | 特定算法研究，难以重新处理 |
| 单站原始I/Q，无完整元数据 | 中 | 回放、检测和有限识别研究 |
| 单站I/Q加信号类别和设备信息 | 较高 | 信号建模、分类识别和特征分析 |
| 多设备、多环境、多距离I/Q | 高 | 泛化性验证和射频指纹研究 |
| 阵列多通道I/Q加幅相校准 | 很高 | 阵列处理和AOA测向 |
| 多站同步I/Q加位置航迹真值 | 很高 | TDOA、FDOA和融合定位 |
| 发射端与接收端同步采集加完整校准参数 | 最高 | 分离辐射源、信道和接收机影响 |

### 12.4 元数据与数据质量规范

每段数据至少应记录：

- 数据集、采集任务、记录和文件编号；
- 无人机、遥控器或其他辐射源类型；
- 是否能够确认具体发射设备个体；
- 工作模式、链路状态和任务状态；
- 中心频率、采样率、有效带宽和采集时长；
- 数据类型、位宽、字节序和IQ排列方式；
- 接收机型号、序列号、增益、滤波和校准状态；
- 天线类型、极化、方向、安装位置和馈线信息；
- 采集时间、时间基准和同步方式；
- 接收站位置、姿态和坐标系；
- 无人机位置、航迹、距离、高度和速度；
- 视距/非视距状态、场景、建筑和天气信息；
- 同频背景、邻频信号和干扰情况；
- 标签来源、标注人员、审签状态和可信度；
- 削顶、过载、丢样、失锁和时间戳异常；
- 数据使用范围、保密等级和授权信息。

平台导入数据时应自动完成：

- 文件完整性和哈希校验；
- 样本数、文件长度和格式一致性检查；
- 削顶、直流偏置、IQ交换和空洞检测；
- 频谱占用、噪声底和异常脉冲检查；
- 时间戳连续性及多通道对齐检查；
- 元数据必填项和单位检查；
- 生成数据质量报告。

### 12.5 数据集划分与防止数据泄漏

不能将同一次长时间录制的数据随机切成大量相邻片段，再随机分配到训练集和测试集。相邻片段高度相关，会造成训练与测试数据泄漏，使识别结果虚高。

应根据研究目标，至少按照以下一种或多种边界进行隔离：

- 发射设备个体；
- 采集日期和飞行架次；
- 采集地点和传播环境；
- 接收机及天线系统；
- 工作状态和信号模式；
- 操作人员或遥控设备；
- 数据采集批次。

建议形成：

- **训练集**：允许用于算法参数学习；
- **验证集**：用于模型选择和参数调整；
- **内部测试集**：研发人员用于阶段性评价；
- **冻结验收集**：不得参与训练和调参，只用于交付验收；
- **未知类集**：包含训练阶段未出现的信号类型或设备；
- **跨域集**：包含不同地点、日期、接收机和环境。

数据集划分结果、随机规则和样本清单必须版本化保存，不能在算法性能不佳时随意调整验收集。

### 12.6 自主平台中的数据组件

建议在自主框图平台中增加：

- 实测I/Q数据集管理器；
- 数据查询、筛选和片段选择工具；
- 数据格式转换与重采样组件；
- 实测信号回放模型；
- 功率、频率、时间和信道变换模型；
- 多源信号叠加组件；
- 实测与合成信号特征对比工具；
- 模型参数拟合与标定工具；
- 标签管理和审签工具；
- 训练、验证、测试和验收集管理工具；
- 数据质量、覆盖度和分布分析工具；
- 算法基准测试和回归测试工具。

数据管理器应向框图运行引擎提供统一的`IQStream`、`ArrayIQStream`和`GroundTruth`端口，使实测数据源和合成辐射源能够无缝替换。

### 12.7 数据安全与资产治理

实测无人机信号可能涉及设备信息、采集位置、工作参数、第三方知识产权或项目敏感信息，建议：

- 明确数据来源、所有权、使用授权和再分发范围；
- 按项目、用户和保密等级实施访问控制；
- 原始数据、派生数据和公开演示数据分区存储；
- 使用哈希、数字签名和不可变清单保障基准数据完整性；
- 记录数据导入、导出、修改和标注历史；
- 对交付数据进行必要的脱敏、裁剪和加密；
- 不把全部原始母库默认随软件交付；
- 建立数据备份、恢复和长期可读性策略。

## 13. 实测数据在交付前后两个阶段的不同作用

### 13.1 阶段作用对比

| 维度 | 交付前研发阶段 | 交付后科研使用阶段 |
|---|---|---|
| 核心目标 | 建模、标定、训练、验证和形成验收证据 | 回放、评价、研究、扩展和持续复核 |
| 主要使用者 | 平台研发人员、算法人员、测试人员 | 设备研发用户、算法研究人员和试验管理人员 |
| 数据状态 | 原始母库、开发集、内部测试集、冻结验收集 | 随产品交付的标准集、用户自有集和增量集 |
| 对模型的作用 | 提取参数、校准信源/信道/接收机模型 | 选择模型、替换信号源、验证适用性并触发模型更新 |
| 对算法的作用 | 训练、调参、缺陷分析和版本选择 | 公平对比用户算法、开展未知场景和跨域研究 |
| 对平台的作用 | 功能、性能、稳定性和代码生成等价性验证 | 回归复测、升级验证、用户任务复现和能力扩展 |
| 数据变更原则 | 允许按流程清洗和修订，但必须保留版本 | 冻结基准集不得被静默修改，新增数据形成新版本 |
| 典型输出 | 标定参数、模型证据包、算法基线、验收报告 | 科研结果、用户模型、增量数据、复测报告和升级建议 |

### 13.2 交付前研发阶段的作用

交付前，实测数据主要作为“研发依据和验证证据”，重点开展以下工作。

#### 13.2.1 数据资产盘点和标准化

- 识别现有数据的格式、频段、设备、场景和标签情况；
- 评估哪些数据可用于回放、训练、标定或定位；
- 补充关键元数据，修复或隔离异常数据；
- 统一数据格式、命名、时间基准和质量等级；
- 确认数据权属、保密和交付范围。

#### 13.2.2 辐射源、信道和接收机模型标定

- 从实测数据提取模型参数和统计分布；
- 区分发射源、传播信道和采集接收机的影响；
- 对合成模型进行参数拟合和残差补偿；
- 生成实测与合成特征对比报告；
- 为每个模型形成适用边界和可信度等级。

#### 13.2.3 算法训练和基线建立

- 构建训练、验证、内部测试和未知类数据集；
- 形成检测、识别、测向和定位的基准算法；
- 建立统一评价指标和性能基线；
- 分析算法在不同设备、日期、地点和接收机之间的泛化能力；
- 识别虚警、漏警和误分类的典型困难样本。

#### 13.2.4 软件平台与部署组件验证

- 验证数据导入、查询、回放、变换和记录功能；
- 验证自主运行引擎对大带宽和多通道数据的吞吐能力；
- 验证MATLAB原始算法、生成C/C++组件和交付版本之间的数值等价性；
- 验证随机种子、模型版本和数据版本能否完整复现实验；
- 验证异常文件、丢样、削顶和时间戳错误的诊断能力。

#### 13.2.5 冻结交付和验收基线

交付前应形成一套研发团队不能再用于训练和调参的冻结验收集，并同时冻结：

- 数据文件及哈希；
- 元数据和标签版本；
- 测试场景和框图模型版本；
- 算法及组件版本；
- 参数、随机种子和运行环境；
- 评价指标、阈值和容差；
- 基准结果和验收报告模板。

冻结验收集是证明平台交付状态的证据，不应与日常训练集混用。

### 13.3 交付后科研使用阶段的作用

交付后，实测数据主要作为“科研试验资源和持续演进依据”，重点支持以下工作。

#### 13.3.1 真实信号场景复现

用户可在自主框图中将实测数据作为信号源，与传播、接收机、干扰和算法模型组合，重复研究同一真实信号在不同条件下的表现。

#### 13.3.2 用户算法公平评价

多个检测、识别和定位算法可以使用相同数据、相同接收机配置和相同评价规则运行，形成可横向比较的结果。固定基准集还可用于验证用户修改算法后是否造成能力退化。

#### 13.3.3 新设备和未知信号研究

用户可导入新的无人机或环境数据，建立用户扩展数据集，开展：

- 新信号发现；
- 未知类和开集识别；
- 新辐射源模型拟合；
- 用户自研算法训练；
- 新接收机数据适配；
- 跨区域和跨设备泛化研究。

#### 13.3.4 模型持续校准与版本升级

当用户数据表明现有合成模型与真实信号存在系统性差异时，应通过受控流程更新模型参数或增加新模型版本，而不是直接覆盖原模型。更新前后均应在冻结基准集和新增数据上运行回归测试。

#### 13.3.5 交付能力复核和问题定位

当用户发现检测率下降、识别错误或定位偏差时，可以利用保存的原始数据、框图、参数、随机种子和版本信息复现问题，并判断问题来自：

- 数据质量；
- 环境或信道变化；
- 接收机配置；
- 算法模型；
- 调度、缓存或时间同步；
- 软件升级和版本兼容。

### 13.4 建议的数据资产分层

建议将数据分为四类，明确不同生命周期和权限。

| 数据资产 | 主要内容 | 是否随产品交付 | 主要用途 |
|---|---|---:|---|
| 原始证据母库 | 未裁剪原始I/Q、完整元数据和采集记录 | 原则上不全部交付 | 追溯、再标定和高可信模型研究 |
| 研发训练库 | 清洗、切片、增强后的训练和验证数据 | 可按合同约定 | 算法开发和模型参数拟合 |
| 冻结基准库 | 独立测试集、未知类集和验收场景 | 应交付必要部分或提供受控访问 | 交付验收、回归测试和版本对比 |
| 用户科研库 | 用户采集、导入和派生的数据 | 由用户侧管理 | 交付后科研、扩展和本地适配 |

对于容量大或敏感程度高的数据，可以交付数据索引、特征摘要、测试结果和受控访问机制，而不是将全部原始母库复制到每个用户终端。

### 13.5 交付包与后续升级要求

交付包建议包含：

- 数据管理和实测回放组件；
- 约定范围内的标准实测数据集；
- 数据集清单、版本、哈希和质量报告；
- 元数据字典和数据格式说明；
- 固定基准场景和验收脚本；
- 基准结果及允许误差；
- 用户数据导入、检查和标注工具；
- 数据备份、迁移和恢复说明。

交付后新增数据或修订标签时，应生成新的数据集版本，并记录：

- 新增、删除和修改的样本；
- 标签变化及依据；
- 模型和算法受到的影响；
- 是否改变原有基准结果；
- 是否需要重新验收或仅进行一般升级验证。

冻结验收数据集和验收结果不得在普通软件升级中被静默替换。

## 14. 实施路线建议

### 14.1 阶段一：最小闭环原型

建议完成：

- 1～2类典型无人机辐射源；
- 单无人机动态航迹；
- 自由空间与简化城市多径；
- 单站阵列侦察接收机；
- 一套检测、识别和AOA算法；
- 实测I/Q回放；
- 实测数据资产盘点、元数据模板和质量检查；
- 形成训练、验证和独立测试的初步划分；
- 基本性能统计与报告。

阶段目标是验证模型接口、数据流、运行效率和技术路线，不追求信号类型齐全。

同时应完成自主框图前端的最小功能，包括组件拖放、连线、参数配置、模型保存和运行控制。算法后端可以先用Compiler SDK或粗粒度Simulink部署组件快速打通。

### 14.2 阶段二：形成平台底座

重点建设：

- 辐射源、信道、接收机和算法模型库；
- 统一数据字典和模型接口；
- 场景编辑、批量试验和结果管理；
- 单元测试、基准测试和回归测试；
- 实测数据管理及模型标定流程；
- 多站TDOA/FDOA和融合定位；
- 模型版本、依赖和发布管理；
- 自主多速率运行引擎和插件SDK；
- 高频核心算法的C/C++代码生成与集成；
- MATLAB组件与生成代码的等价性回归测试；
- 数据管理器、标签审签和实测/合成对比工具；
- 第一批辐射源模型证据包和可信度等级。

### 14.3 阶段三：半实物与工程化

逐步扩展：

- SDR实时采集与回放；
- 真实接收机或算法板卡在环；
- FPGA、GPU和集群加速；
- 多用户任务调度和资源管理；
- 模型权限、审签和受保护交付；
- 面向科研用户的桌面或Web应用；
- 多后端混合运行及模型市场式组件发布机制；
- 多通道或多站同步数据采集与标定；
- 固定基准集、未知类集和跨域测试集；
- 用户数据导入、质量检查和本地数据管理。

### 14.4 阶段四：产品化与去开发环境依赖

重点完成：

- 正式产品不依赖MATLAB Engine；
- 高频核心模型尽可能转为无Runtime依赖的C/C++组件；
- 必须使用MATLAB Runtime的组件实施集中管理和进程隔离；
- 完成安装部署、升级、回滚和版本兼容机制；
- 完成第三方软件许可和再分发合规审查；
- 形成自主品牌的用户手册、模型开发手册和插件接入规范；
- 冻结交付数据、场景、算法和评价基线；
- 完成数据权属、保密、脱敏、交付范围和升级机制审查。

## 15. 验收指标建议

不宜只以“模型数量”和“界面数量”作为验收依据。建议至少包含以下指标。

### 15.1 侦察算法指标

- 检测概率、虚警率和最低可检测信噪比；
- 识别准确率、精确率、召回率和F1；
- 未知类拒识率和误接纳率；
- 多信号重叠、邻道阻塞和强干扰条件下的性能；
- AOA误差；
- 定位RMSE、CEP50和CEP95；
- 首次定位时间、更新率和连续跟踪稳定性。

### 15.2 模型可信度指标

- 合成与实测信号的功率谱差异；
- 时频结构和突发统计差异；
- 频偏、相位噪声及射频指纹分布差异；
- 传播损耗、时延扩展和多径统计误差；
- 模型适用边界和可信度等级覆盖情况。

### 15.3 平台性能指标

- 支持的最大瞬时带宽；
- 支持的最大阵列通道数；
- 单次试验最大持续时间；
- 仿真实时比、任务吞吐量和资源占用；
- 并行试验规模；
- 可复现实验比例；
- 自动回归测试覆盖率；
- 新模型接入和完成验证所需时间。

### 15.4 数据资产与生命周期指标

- 可导入的数据格式、位宽和最大文件规模；
- 单站、多通道和多站数据的时间对齐能力；
- 元数据必填项完整率和标签审签覆盖率；
- 数据质量检查项目及异常检出能力；
- 数据集文件、清单和哈希的一致性；
- 训练集、验证集、测试集和冻结验收集的隔离情况；
- 按设备、日期、地点、接收机和工作状态的数据覆盖度；
- 实测回放结果的确定性和可重复性；
- 实测与合成信号的特征差异及允许范围；
- MATLAB算法、代码生成组件和交付组件的数值等价性；
- 交付后用户数据导入、版本化和回归复测能力；
- 冻结基准集在软件升级前后的完整性和结果一致性。

## 16. 应避免的建设误区

- 建设一个所有功能紧密耦合的超大型Simulink模型；
- 所有场景全部采用最高采样率和最高保真度；
- 只建立理想接收机，不建模动态范围、时钟和通道误差；
- 只使用合成信号，不建立实测数据闭环；
- 先投入大量资源开发界面，后补模型真实性和测试体系；
- 用单一“识别准确率”评价系统，而忽略未知类、虚警、强干扰和域差异；
- 以模型数量代替模型质量、适用范围和验证证据；
- 忽视MATLAB版本、工具箱依赖、许可和硬件支持变化；
- 将自主框图前端误认为完整仿真平台，而忽视调度器、数据总线和时间语义；
- 在高吞吐I/Q处理链中对每个小模型使用跨进程或远程调用；
- 把MATLAB Engine作为正式产品的长期运行依赖；
- 以隐藏第三方依赖为由忽视软件许可、再分发和声明义务；
- 把接收机采集到的全部特征都解释为发射机特征；
- 将同一次录制的相邻片段随机分到训练集和测试集，造成数据泄漏；
- 只保留提取后的特征或频谱图片，而不保存原始I/Q和采集元数据；
- 使用验收数据反复调参，使验收结果失去独立性；
- 软件升级时静默替换数据集、标签或基准结果；
- 将全部原始敏感数据默认随客户端软件交付。

## 17. 最终建议

建议将MATLAB/Simulink定位为内部模型开发、算法验证和组件生产工具，将项目自研软件定位为最终用户面对的模型组织、调度、运行和试验管理平台。自主建设的无人机辐射源库、侦察接收机库、城市传播场景库、实测信号数据资产、评价体系、组件规范和运行引擎共同构成项目核心资产。

平台应坚持以下总体路线：

1. 以复基带I/Q信号级仿真为主体；
2. 参数/事件级、I/Q级和半实物级并存；
3. 通过统一接口实现模型的可替换、可组合和可扩展；
4. 通过实测数据持续标定模型；
5. 通过自动测试和版本管理保障多人长期使用；
6. 采用统一插件接口兼容MATLAB生成库、自研C/C++、GPU、FPGA、FMU和SDR模型；
7. 高频实时算法优先生成或重构为无MATLAB Runtime依赖的C/C++组件；
8. 普通科研用户只使用自主软件，不直接接触MATLAB/Simulink；
9. 以实测数据标定模型、以合成数据扩展场景、以冻结数据保障验收；
10. 交付前将实测数据用于研发标定、算法训练、系统验证和验收证据建设；
11. 交付后将实测数据用于真实回放、用户算法评价、模型迭代和能力复核；
12. 训练集、测试集、冻结验收集和用户增量数据分开管理；
13. 先完成最小闭环，再逐步扩展信号类型、场景规模和实时能力。

采用该路线，可以在产品品牌、操作界面、模型组织、运行管理和数据体系上形成自主能力，同时充分发挥MATLAB专业工具箱和高质量通用模型的研发效率。实测数据为模型真实性提供依据，合成模型为科研场景提供可控扩展，固定基准集为交付验收和长期回归提供稳定尺度。长期来看，MATLAB只是可替换的模型生产工具之一，而实测数据、模型证据和自主运行框架才是平台持续演进的核心资产。

## 18. 参考资料

- [MathWorks：Communications Toolbox](https://www.mathworks.com/products/communications.html)
- [MathWorks：RF Blockset](https://www.mathworks.com/products/rf-blockset.html)
- [MathWorks：Phased Array System Toolbox](https://www.mathworks.com/products/phased-array.html)
- [MathWorks：TDOA Estimator](https://www.mathworks.com/help/phased/ref/tdoaestimator.html)
- [MathWorks：UAV Scenario Simulation](https://www.mathworks.com/help/uav/scenario-simulation.html)
- [MathWorks：Simulink Model References](https://www.mathworks.com/help/simulink/model-reference.html)
- [MathWorks：Simulink Sample Time](https://www.mathworks.com/help/simulink/sample-time.html)
- [MathWorks：Parallel Simulations](https://www.mathworks.com/help/simulink/ug/configure-and-run-simulations-with-multiple-simulations-ui.html)
- [MathWorks：Supported SDR Hardware](https://www.mathworks.com/help/comm/supported-hardware-software-defined-radio.html)
- [MathWorks：Simulink Test](https://www.mathworks.com/products/simulink-test.html)
- [MathWorks：Requirements Toolbox](https://www.mathworks.com/products/requirements-toolbox.html)
- [MathWorks：Simulink Compiler](https://www.mathworks.com/products/simulink-compiler.html)
- [MathWorks：MATLAB Coder](https://www.mathworks.com/products/matlab-coder.html)
- [MathWorks：Simulink Coder](https://www.mathworks.com/products/simulink-coder.html)
- [MathWorks：MATLAB Compiler SDK](https://www.mathworks.com/products/matlab-compiler-sdk.html)
- [MathWorks：Simulink FMU Builder](https://www.mathworks.com/products/simulink-fmu-builder.html)
- [MathWorks：MATLAB Production Server](https://www.mathworks.com/products/matlab-production-server.html)
- [MathWorks：MATLAB Engine API](https://www.mathworks.com/help/matlab/matlab_external/introducing-matlab-engine.html)





