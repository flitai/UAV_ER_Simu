# 06. 首期实施备忘录

> 版本 v1.0，2026-09-03。
> **用途**：执行层的路线图与步骤清单，供每次对话开工前对照"现在到哪、下一步做什么、做完怎么验"。
> **分工**：04 号方案定**做什么**；`CLAUDE.md` 定**约定、状态与决策**（真理源，每次会话自动加载）；`WORKLOG.md` 记**过程**；本备忘录定**顺序与步骤**。冲突时以 CLAUDE.md 为准，并在 §14 记一条修正。
> **维护**：每完成一步，更新 §0.3「当前位置」与该步骤的状态标记（`[ ]` 未做 / `[~]` 进行中 / `[x]` 完成，后附日期）；步骤变更写 §14，不改历史。

---

## 0. 一页总览

### 0.1 目标

在给定无人机工作状态、传播场景、接收天线与接收机配置下，端到端等效生成 **DDC 后、信道化前的宽带复基带 IQ**（S4 观测点），使其在时域、频域、统计特征与下游算法性能上与真实电侦设备观测相似；以 B/S 框图平台承载编辑、运行、数据与结果；以 2.5D 地理场景提供传播几何。首期单站、单通道、离线/准实时。

### 0.2 路线图

```
阶段 0 需求冻结（用户主导，§3）
   │
   ├── 显示子线（§4–§8）  D0 场景数据包 → D1 底图 → D2 态势图层 → D3 遮挡库 → D4 同源验收
   │                      不进入 P1 退出条件；D3 产出的慢变参数在 P1 后期 / P3 接入 IQ 引擎
   │
   ├── IQ 主线（§9–§10）  P1 最小闭环 → P2 B/S 平台与组件化 → P3 工程等效与实测校准 → P4 试用交付
   │
   └── 数据线（§11）      8 项摸底 → 入库并冻结 holdout → 阶段 1 回放源 → 阶段 3 四组校准
                          从阶段 0 起并行，不等阶段 3
```

### 0.3 当前位置（每次对话更新）

| 日期 | 位置 | 下一步 |
|---|---|---|
| 2026-09-03 | `CLAUDE.md` 完成（227 行，D-001 ~ D-017）；本备忘录 v1.0；无目录骨架、无代码、`WORKLOG.md` 未建；实测数据待用户提供 | §2 建骨架 → D0-1 定临时 AOI → P1-0 引擎设计报告 |
| 2026-09-03 | §2 工程骨架 S-1 至 S-7 全部完成：目录树、`.gitignore`、`WORKLOG.md`、`docs/` 八份规范骨架、`tests/` 五级与回归清单、`web`/`server`/`engine`/`geo`/`scene` 五个可空跑的工程、两个脚本。`scripts/build-all.sh` 全绿。git 已初始化但**尚无提交**，75 个文件待提交 | D0-1 定临时 AOI → D0-2 底图裁切；并行 P1-0 引擎设计报告 |
| 2026-09-03 | 本备忘录按公开数据集入手的事实修订 9 处（见 §14 第 2 条），新增 §11.4 数据集接入步骤 DS-1 至 DS-7 与两条防线。技术路线与两处退出条件均未变动 | 三条并行：D0-1 定临时 AOI；P1-0 引擎设计报告；DS-2 转换脚本（DS-1 许可待用户确认） |

### 0.4 不需要用户输入即可开工的三件事

1. §2 工程骨架（半天）。
2. D0 全流程，用 em-demo 的西安 bbox 作**临时 AOI** 验证管线，产出可与 em-demo `buildings.geojson` 对拍的建筑数据（真实 AOI 冻结后换参数重跑）。
3. P1-0 引擎设计报告与 P1-1 IQ 格式规范草案。

### 0.5 需要用户提供的输入

| 输入 | 阻塞哪一步 | 状态 |
|---|---|---|
| 正式 AOI（bbox 或多边形） | D0 正式出包；D1 验收截图 | 待定，临时用西安 |
| 首批频段、带宽、无人机型号 | P1-3 辐射源模板参数；P3 模型卡 | 待定 |
| 实测 IQ 数据 + 8 项摸底回答（§11.1） | 阶段 0 退出；P1-9b 回放；P3 全部 | 用户称稍后提供 |
| 交付平台基线：Windows 版本、浏览器、MSVC、Node LTS、Windows 验证机 | D3-4 MSVC 构建；P2 单机包 | 待定 |
| 04 附录 D / 05 附录 C 引用的 5 份缺失文档、主界面交互原型 html、01/02 三张架构图 | 阶段 0 退出（阻塞） | 待索取 |
| 框图画布、遮挡落点、坐标基座三项 OPEN 的拍板 | P2-1；D3-0 | 各自设计报告时 |
| DroneRFb-DIR 许可条款（能否随交付系统分发） | §11.4 DS-1，进而影响该数据集能否进入交付物 | 待确认，SciDB 页面为动态渲染，未取到 |

**防线一：不得因为手上有了公开数据集就放松索取甲方数据。** 2026-09-03 入手的公开数据集
DroneRFb-DIR（见 §11.4）覆盖不到甲方的目标机型、频段与接收机参数，也没有任何功率标定与
几何信息；P3 需要的四组标定数据（高信噪比参考、多距离场景、背景同邻频、独立验证）它一组
都提供不了。**阶段 0 的退出条件不因它放宽一个字。**

---

## 1. 技术路线摘要

### 1.1 架构分层

```
浏览器 web/         TS + React 19 + Vite；MapLibre GL 5.24 + pmtiles 4.4（vendored）
  三视图：场景 / 框图 / 结果 + 顶部试验上下文
        │ REST（控制）+ WebSocket（状态、日志、按视窗抽取的展示数据；事件带序号可补取）
应用服务 server/    Node/TS；权限、项目、模型目录、任务、数据索引、审计；HTTP Range 静态服务
        │ 粗粒度任务接口（提交完整试验 / 查状态 / 取结果索引）
运行引擎 engine/    C++14 分块流 + DAG 调度；同进程 / 共享内存；种子与溯源
  ├── models/       辐射源 / 信道 / 天线 / 接收机 / ADC-DDC / 信道化（含 MATLAB Coder 产物）
  ├── geo/          IMapQuery + LocalSceneAdapter + 刀口衍射（自 emcore 移植）→ SceneParamFrame 10–100 Hz
  └── algos/        PSD / 瀑布 / 能量检测 / 特征 / 评价基线 / 用户插件接口
数据 data/          SQLite 单机 / PostgreSQL 内网；IQ 二进制 + SigMF 兼容元数据；场景数据包；黄金基准
```

### 1.2 数据流与端口

| 流 | 端口类型 | 速率 | 说明 |
|---|---|---|---|
| IQ 主链 | `IQStream` | 采样率（5–50 MS/s 常用，100 MS/s 短窗） | S0 辐射源 → S1 天线端 → S2 前端 → S3 ADC → **S4 主产品** → S5 信道化 → S6 结果 |
| 慢变参数 | `SceneParamFrame`、`ChannelPathSet` | 10–100 Hz | 链路几何、LOS/NLOS、损耗分量、噪声底、天线复增益、扫描窗口、多径路径；只经"施加"类 M3 组件进入 IQ |
| 观测量归约 | `SpectrumFrame`、`DetectionList`、`FeatureVector`、`BearingReport` | 事件/帧 | IQ 观测量提取 → M2 裁决 → 蒙特卡洛 → M1 参数 |
| 控制与真值 | `ControlEvent`、`GroundTruth` | 事件 | 真值只供评价，不向被测算法泄漏 |
| 后置预留 | `ArrayIQStream`、`MultiSiteIQSet`、`CalibrationSet`、`PositionReport`、`TrackReport`、`DeviceStatus` | — | 只做命名预留 |

连线校验规则：`IQStream` 不得与参数流直连；M2 裁决组件不接受 `IQStream`。

### 1.3 三种运行模式与三个验收场景

全合成 / 实测回放 / 混合增强（04 §4.3）；对应验收场景：实测回放、全合成、混合增强（04 §21）。混合增强是首期投入产出比最高的能力，P3 主攻。

### 1.4 复用地图

| 来源 | 拷什么 | 到哪 | 改动 | 验证 |
|---|---|---|---|---|
| Airports `index.html` | `protomapsStyle()` 810–1092 行、`PM` 配色、hillshade 1218 行、`addBldg3d()` 1296 行、`newMap()` 1375 行、`localglyphs://` 协议 | `web/src/scene/` | 原生 JS 切成 TS 模块，**不改视觉参数** | 同 AOI 与 Airports 并排截图 |
| Airports `serve.py`、`fetch_tiles.py`、`fetch_levels.py`、`download_offline_maps.py` | Range 逻辑、AOI 裁切、DEM 下载 | `server/`（Range 用 Node 重写）、`scene/` | 参数化 AOI；DEM 加 bbox | `curl -r` 得 206 |
| Airports `tiles/planet.pmtiles`、`tiles/dem/` | 数据 | `data/scene/<aoi>/` | 只裁切、只拷贝 | `pmtiles show` |
| em-demo `src/components/{MapView,CoverageControl,SignalPanel}.tsx`、`src/models/coverage.ts`、`src/simulation/engine.ts` | 态势图形语义、覆盖场、iso-Pd、Canvas 叠加层、定频 tick | `web/src/scene/layers/` | 拆成图层模块；**色值在浅色底图重标定** | 逐图层截图 |
| em-demo `scripts/fetch-buildings.mjs` | 估高分档与 `src` 标记规则 | `scene/decode_buildings` | 数据源改为 z15 瓦片解码 | 与西安 Overpass GeoJSON 对拍 |
| emcore `include/emcore/{core,map,models/{occlusion,propagation}}`、`tests/golden/*.json`、`tests/golden_util.h` | 地图抽象、桶网格 DDA、刀口衍射、golden | `geo/` | 文件头注明来源；常数不动 | 148 例 rel ≤ 1e-9 + 解析锚点 |
| foundation `math/{UtEllipsoidalEarth,UtCoords,UtSphericalEarth,...}` | WGS-84 坐标基座 | `third_party/foundation/` | 补 `ModelApi.hpp` 与顶层 CMakeLists | 与 GeographicLib 或已知点对拍（D3-0 决定是否采用） |
| em-demo `桌面应用打包说明.md` | Windows 交付经验 | `deploy/README` | 摘录 | — |

---

## 2. 工程骨架

| # | 步骤 | 产物 | 状态 |
|---|---|---|---|
| S-1 | 按 CLAUDE.md 目录结构建目录（全部 ASCII 无空格） | `web/ server/ engine/ models/ geo/ algos/ scene/ data/ tests/ deploy/ docs/ third_party/ matlab/` | [x] 2026-09-03 |
| S-2 | `git init`；`.gitignore` 排除 `data/**`（保留 `*.manifest.json`、`*.md`）、`C-UAV Model Demo/**`、`node_modules`、`build`、`dist` | `.gitignore` | [x] 2026-09-03 |
| S-3 | `WORKLOG.md`：顶部状态快照 + 按日追加区 | `WORKLOG.md` | [x] 2026-09-03 |
| S-4 | `docs/` 空壳：`iq-format.md`、`api-versions.md`、`scene-package.md`、`display-route.md`、`decisions/`、`concept-model-layers.md`、`concept-model-extensions.md` | 各文件含标题与"待写"章节骨架 | [x] 2026-09-03 |
| S-5 | `tests/{unit,integration,e2e,golden,regression}/`；`tests/regression/README.md` 列 04 §15.2 十二项 + 跨层一致性四项 | 目录与清单 | [x] 2026-09-03 |
| S-6 | 工具链落位：`web/`、`server/` npm 工程（vendored 依赖，锁版本）；`engine/`、`geo/` CMake ≥ 3.16、C++14；`scene/` python + uv | 可空跑的 `npm run build`、`cmake --build` | [x] 2026-09-03 |
| S-7 | `scripts/check-ascii.sh`（铁律 15）、`scripts/build-all.sh`；CI 形态列 OPEN | 脚本 | [x] 2026-09-03 |

---

## 3. 阶段 0 需求冻结（04 §13.2）

用户主导，我准备材料。退出条件：范围与不做事项经确认；获得一批可解析实测 IQ 或补采计划；IQ 格式/功率/时间/真值口径形成；显示子线定位与 AOI 冻结；缺失引用文件已索取。

| 决策项 | 我准备的材料 | 状态 |
|---|---|---|
| AOI | 候选 bbox 的 `fetch_tiles.py --estimate` 体积表；建筑数量与高度覆盖率抽样 | [ ] |
| 首批频段、带宽、机型 | 从 04 附录 B 整理成勾选表；概念模型 EM-T-03 模板字段对照 | [ ] |
| 实测数据 | §11.1 八项问题；摸底工具 `tools/iq_survey`——**该工具不再被阻塞，可先在公开数据集 DroneRFb-DIR 上开发并验证**（§11.4 DS-5），甲方数据到货当天直接可用 | [~] |
| 交付平台基线 | WebGL2 检测页；MSVC / Node LTS 候选版本表 | [ ] |
| 缺失文档索取 | 清单（见 §0.5） | [ ] |
| M3 概念模型编号前缀 | `EM-Q-xx` 候选与理由 | [ ] |

---

## 4. D0 场景数据包

输入：AOI bbox；`/Users/zhiyu/CC/Airports/tiles/planet.pmtiles`；`tiles/dem/`。输出：`data/scene/<aoi>/{basemap.pmtiles, buildings.geojson, dem/, manifest.json, quality-report.md}`。

| # | 步骤 | 要点 | 状态 |
|---|---|---|---|
| D0-1 | 定临时 AOI | 用 em-demo 西安 bbox `108.8805,34.2956,109.0005,34.4056`，因为有 13,594 栋 Overpass GeoJSON 可对拍；正式 AOI 冻结后重跑 | [ ] |
| D0-2 | 底图裁切 | `pmtiles extract <planet> basemap.pmtiles --bbox=W,S,E,N --maxzoom=15`；`pmtiles show` 核对 maxzoom=15、`buildings` 层 z11–15；记录 planetiler 0.10.2 / OSM 2026-08-17 | [ ] |
| D0-3 | 建筑解码合并 | `scene/decode_buildings`：枚举 AOI 内 z15 瓦片 → MVT 解码（Node `@mapbox/vector-tile` + `pbf` 或 Python `mapbox-vector-tile`，建库阶段可联网安装）→ `buildings` 层多边形 → 瓦片坐标反投影 WGS-84 → **首先检查 feature 是否带 id**：有则按 id 跨瓦片合并，无则按共享边或缓冲相交合并 → 去掉 `kind=building_part` → 高度：`height` 有 → `src=osm:height`，`min_height` → `base_m`；缺 → 沿用 `fetch-buildings.mjs` 分档估高（瓦片无用途标签，只能按面积 → `src=est:area`）→ 输出 EPSG:4326，坐标 6 位小数，`id` 稳定 | [ ] |
| D0-4 | 管线对拍 | 与 em-demo `public/buildings.geojson` 比：要素数、总面积、高度分位数 q10/q50/q90、`src` 分布；差异写入质量报告 | [ ] |
| D0-5 | DEM 子集 | 按 AOI 枚举 z0–8 瓦片从 `tiles/dem/` 拷贝；小 AOI 直接整层拷 | [ ] |
| D0-6 | `manifest.json` | aoi、crs=EPSG:4326、osm_snapshot、planetiler 版本、height_src 分布、各文件 sha256、生成参数与时间、坐标版本 | [ ] |
| D0-7 | 质量报告 + ASCII 检查 | `quality-report.md`；`scripts/check-ascii.sh data/scene/<aoi>` | [ ] |

验收：CLAUDE.md D0 行；量化记录：要素数、`src` 分布百分比、包体积。首个技术未知点：Protomaps z15 建筑 feature 有无稳定 id（D0-3 首先验证）。

---

## 5. D1 2.5D 离线底图

| # | 步骤 | 要点 | 状态 |
|---|---|---|---|
| D1-1 | Range 静态服务 | Node 实现，参考 `serve.py` `RangeHandler`：206、`Content-Range`、`Accept-Ranges`、HEAD；`curl -r 0-99` 验证 | [ ] |
| D1-2 | web 骨架 | Vite + React + TS；vendored `maplibre-gl` 5.24.0、`pmtiles` 4.4.1；`addProtocol('pmtiles')` 走 Range 而非整文件 fetch | [ ] |
| D1-3 | 样式移植 | `web/src/scene/style/protomaps.ts`（照搬 810–1092 行与 `PM` 表）、`glyphs.ts`（`localglyphs://` + `vendor/glyphs.js`）、`hillshade.ts`（1218 行参数）、`buildings3d.ts`（源改为 GeoJSON，paint 与 `addBldg3d()` 一致：`#e6e1da`、minzoom 14、14→14.7 渐显）、`init.ts`（`newMap()` 参数、pitch 55、`NavigationControl({visualizePitch:true})`） | [ ] |
| D1-4 | 底图不需要 sprite | Airports 样式无 sprite 字段；态势图标后续用 `map.addImage` 运行时注册 | [ ] |
| D1-5 | 探针 | `window.__probe` 无副作用导出地图状态，供 e2e | [ ] |

验收：同 AOI 与 Airports 并排截图一致；断网可加载；WebGL2 检测通过；ASCII 检查通过。

---

## 6. D2 态势图层

| # | 步骤 | 要点 | 状态 |
|---|---|---|---|
| D2-1 | 状态流 schema | WS 消息：`EntityState{id, kind, lon, lat, alt_m, heading_deg, threat, effectTag}`、`TrackHistory`、`Bearing`、`Fix{ellipse}`、`CoverageField{grid, values}`、`Envelope`；带序号 | [ ] |
| D2-2 | 图层模块 | `layers/{targets,trails,bearings,fixes,coverage,envelope}.ts`，自 em-demo `MapView.tsx` 拆分 | [ ] |
| D2-3 | Canvas 叠加层 | PPI / 测向线 / 2σ 椭圆用 `map.project` 投影；DPR 处理 | [ ] |
| D2-4 | 色值重标定 | 在 Airports 浅色底图上重定威胁色、AOA/TDOA 双色、viridis 不透明度；结果进 `docs/display-route.md` | [ ] |
| D2-5 | 节流 | 20 Hz 定频 tick、每帧一次状态提交 | [ ] |

验收：无人机图标、航迹、覆盖热力图、包络随状态流刷新；Retina 截图无错位。

---

## 7. D3 遮挡库移植

| # | 步骤 | 要点 | 状态 |
|---|---|---|---|
| D3-0 | 设计报告 07 | 拍板两项 OPEN：遮挡计算落点（server 预计算场 + WS / 浏览器 TS 复算对拍）、坐标基座（foundation vs GeographicLib） | [ ] |
| D3-1 | 拷入 | emcore `core/{types,units,result,random,geo}.h`、`map/{imap_query,local_scene_adapter}`、`models/{occlusion,propagation}` → `geo/`；文件头注明来源与 golden 对应 | [ ] |
| D3-2 | 构建与 golden | CMake + doctest；`tests/golden/occlusion.json` 等 148 例，rel ≤ 1e-9 | [ ] |
| D3-3 | 解析锚点 | FSPL@2.4 GHz/1 km ≈ 100.05 dB；刀口 v=0 → ≈ 6.0 dB；热噪声@1 MHz/NF 6 = −108 dBm | [ ] |
| D3-4 | MSVC / GCC 构建 | 首次在 Windows 机或 CI 上验证 | [ ] |
| D3-5 | `SceneParamFrame` 生产者 | 站点 + 航迹 → 10–100 Hz LOS/NLOS、L_diff、FSPL、几何 → 帧；接 server WS 与 engine 施加组件 | [ ] |

验收：golden 全过 + 锚点全过 + 双编译器构建通过。

---

## 8. D4 渲染—物理同源验收

| # | 步骤 | 状态 |
|---|---|---|
| D4-1 | 同一 `buildings.geojson` 同时加载到 `geo/` `LocalSceneAdapter` 与 web `fill-extrusion` | [ ] |
| D4-2 | 探针点选任一建筑：id、`height_m`、`base_m` 两侧一致；任一链路 LOS 数值结果与画面遮挡一致（截图 + 数值） | [ ] |
| D4-3 | 探针无副作用；瓦片 `buildings` 层未进遮挡计算 | [ ] |

---

## 9. P1 最小端到端 IQ 闭环（04 §13.3）

| # | 步骤 | 要点 | 状态 |
|---|---|---|---|
| P1-0 | 设计报告 08：引擎分块流与多速率 | Block 结构、端口类型系统、DAG 调度、背压、采样率变化时的时间与群时延、共享内存抽象层、种子与 trace | [ ] |
| P1-1 | `docs/iq-format.md` | 主产品定义；复 int16 交织 + `scale/full_scale`；内部 float32；元数据 sidecar JSON 含 04 §9.2 十一类字段；SigMF 映射；分段存储；S0–S6 命名；`\|Δf\| + B/2 + 保护带 < Fs/2` 检查。**分两批定稿**：容器、样点编码与元数据结构现在即可定稿（公开数据集已提供真实样本，以及 `scale/full_scale` 的取值案例——满量程 32768、实际峰值码 1000 至 5500）；**功率与标定口径被甲方数据阻塞**，单列一节不与其余章节捆绑 | [~] |
| P1-2 | engine 核心 | `Block{samples, meta}`、`PortType`、组件接口 `init/configure/process/flush/reset/status/destroy`、拓扑排序、单线程调度先行、文件流与内存流、`IRandom&` 注入、trace | [ ] |
| P1-3 | models 首批 | `ToneSource`、`BurstSource`（占空比）、`FileReplaySource`（int16）、`FreeSpaceChannel`（路损 + 时延 + 常量多普勒）、`AWGN`（目标 SNR 或噪声底）、`RxFrontend`（带宽滤波 + 增益 + NF）、`ADC`（量化 + 削顶标记）、`DDC`（NCO + LPF + 抽取）、`Channelizer`（单通道） | [ ] |
| P1-4 | algos 首批 | Welch PSD、瀑布瓦片生成、能量检测 | [ ] |
| P1-5 | server 最小接口 | `POST /api/v1/tasks`（框图 JSON）、`GET /tasks/{id}`、WS 状态、`GET /results/{id}/psd?t0&t1&f0&f1&px`（按视窗抽取） | [ ] |
| P1-6 | web 最小页 | 任务 JSON 提交表单 + 结果页 PSD / 瀑布（Canvas）；框图画布留到 P2 | [ ] |
| P1-7 | 标准算例 | 单音频移与功率标度、带限噪声与目标 SNR、突发占空比、DDC 频移/低通/抽取、ADC 量化与削顶、固定种子复现；跨层一致性算例 ①（EM-S-02 Pd 公式 vs 蒙特卡洛） | [ ] |
| P1-8 | MATLAB Coder 链路 | 一个组件（如 FIR）`.m` → C → engine 组件；与 MATLAB 参考等价性测试 | [ ] |
| P1-9a | 公开数据集回放 | DroneRFb-DIR：转换（§11.4 DS-2）→ 04 §10.6 八项质检 → 回放 → PSD/瀑布/能量检测。**不阻塞，现在可做**；`FileReplaySource`（P1-3）一落地即有真实文件可喂 | [ ] |
| P1-9b | 甲方实测回放 | 数据到位后：导入 → 04 §10.6 八项质检 → 回放 → PSD/瀑布/能量检测；额外含真值对齐与功率标定 | [ ] |

退出：一个框图从浏览器提交并跑到检测结果；宽带与信道化 IQ 可被外部算法读取；标准算例正确可重复；实测 IQ 可导入回放；跨层算例 ① 在容差内（PROVISIONAL）。

---

## 10. P2 / P3 / P4 概要

**P2 平台与组件化（04 §13.4）**：设计报告 09 拍板框图画布（React Flow / AntV X6）→ 组件库与端口连线校验（含 `IQStream` 与参数流不直连）→ 参数表单与单位范围校验 → 模型包 `manifest/interfaces/parameters` 与版本 → WS 断线补取与后台任务保持 → SQLite 与权限审计 → 分片上传 → **Windows 单机一体化包在干净机上安装运行**（启动器形态 OPEN）→ 05 P0 端口命名预留检查。

**P3 工程等效与实测校准（04 §13.5）**：2–3 类辐射源模型卡与证据包 → 统计多径、阴影、频偏、多普勒 → 接收机非理想（相噪、IQ 不平衡、AGC、削顶）→ 混合增强 → 实测与仿真特征对比工具（04 §11.4 七层）→ 参数拟合与版本冻结 → 四组数据独立验证 → 跨层一致性算例 ②③④ → M1/M2 校准参数回写。D3 的 `SceneParamFrame` 在此正式接入 IQ 引擎。

**P4 试用与交付（04 §13.6）**：稳定性与异常恢复、断网离线完整性、浏览器兼容、许可合规、部署包、手册、验收集冻结。

---

## 11. 数据线（与各阶段并行）

### 11.1 八项摸底问题（用户回答）

采集点（ADC 后 / DDC 后 / 信道化后）；格式与元数据；采样率、带宽、中心频率、位宽、通道数；时长与连续性；真值（机型、距离、航迹、状态、时间对齐）；同期背景空采；增益、AGC、滤波、校准；密级与使用范围。

### 11.2 任何 IQ 数据集入库当天必做

本节是通用规则，对两条数据线同样适用：公开数据集线（§11.4）与甲方数据线。首个适用对象是
DroneRFb-DIR。

1. `tools/iq_survey`：解析 + 04 §10.6 八项自动质检 + PSD/瀑布 + 元数据报告，结论回填 `docs/iq-format.md`。质检结果必须落到四态语义（`valid` / `degraded` / `invalid` / `not_applicable`），**元数据缺失一律判 `degraded`，禁止用默认值顶替**（铁律 15）。
2. 按设备/日期/地点/架次划出**冻结验收子集**，记 sha256，清单入库；此后任何调参输入集须证明与其无交集。
3. 目录：`data/iq/measured/<batch>/`，大文件不入 git，只入索引与元数据。

### 11.3 参与时点

自 2026-09-03 起分成两条并行的数据线，用途不同，不得混为一谈。

| 线 | 数据 | 参与时点 | 能支撑什么 |
|---|---|---|---|
| 公开数据集线 | DroneRFb-DIR（§11.4） | **现在起** → P1-9a 回放 → 跨层一致性 ① 的真实背景版与 ④ | 仿真链条 S4 观测点**下游**的验证：回放、观测量提取、检测门限、识别 |
| 甲方数据线 | 待提供 | 阶段 0 摸底定格式 → P1-9b 回放源与辐射源模板 → P3 四组（A 高 SNR 参考 / B 多距离场景 / C 背景同邻频 / D 独立验证）标定与冻结 | 上述全部，外加功率标定、链路几何、模型校准 |

### 11.4 公开数据集 DroneRFb-DIR

2026-09-03 入手。浙江大学发布，论文 DOI `10.11999/JEIT240804`，SciDB 数据集编号
`84cf9101e739402784b1396783881202`。摸底的完整结论见 `WORKLOG.md` 同日条目，此处只列步骤。

**要点**：USRP-2955，采样率 80 兆样点每秒，中心频率 2.44 吉赫兹，每片 50 毫秒共 400 万点，
4690 片共 64 GB。数据本身正好落在 S4 观测点。底层是 16 位整数，转成本项目的复 int16 交织
格式**无损**（抽查 7 个文件，全部样点均为 2 的负 15 次方的整数倍）。收发距离固定 10 米，
无人机摆放不动，无坐标、无绝对功率标定、无绝对时间戳。

| # | 步骤 | 要点 | 状态 |
|---|---|---|---|
| DS-1 | 确认许可条款 | 特别是能否随交付系统分发。SciDB 页面动态渲染，需人工访问。**唯一需要用户出面的一步，也是唯一的阻塞项** | [ ] |
| DS-2 | 转换脚本 | HDF5 分离 I/Q 的 float32 → 复 int16 交织。脚本内断言每个样点为 2 的负 15 次方整数倍，转换后逐位回读对拍。**先转最小子集不全量转**（见下） | [ ] |
| DS-3 | 外挂元数据 | 采样率、中心频率、设备型号来自论文而非文件，一律标 `source=paper`；连续性标志写「片内连续、片间不连续」；时间基准只填「文件采集」一项 | [ ] |
| DS-4 | 冻结验收子集 | 直接用出版方划好的测试集 2513 片；另单列 A3/C3/D3/E3/F3/G3 六架训练集中从未出现的个体；记 sha256。出版方已划分的好处是不会在划分时把同一架次同时放进训练与验收 | [ ] |
| DS-5 | `tools/iq_survey` 跑通 | 在该数据集上落地 04 §10.6 八项质检。**它是比"完美数据集"更好的测试用例**：八项中六项可跑通，两项（元数据一致性、元数据必填项）必然失败，正好逼出「缺元数据判 degraded 而不崩溃」这条路径 | [ ] |
| DS-6 | 真实背景虚警率标定 | 用 62 片纯背景测能量检测器虚警率。预期按高斯白噪声假设推出的门限会明显超标，因为真实背景中的 Wi-Fi 与蓝牙是突发、非平稳、非高斯的。产出超标倍数与修正后的门限 | [ ] |
| DS-7 | 真实背景加合成目标 | 背景用真实数据，目标自行合成（信噪比等参数完全可控），产出检测概率曲线，与白噪声版本并排比较。即跨层一致性算例 ① 的真实背景版，同时覆盖 04 §15.2 标准算例第 10 项 | [ ] |

依赖：DS-2 → DS-3、DS-5；DS-5 → DS-6 → DS-7（DS-7 还依赖 P1-3、P1-4）。DS-1 与 DS-4 独立。
除 DS-1 外均不需要用户输入。

**转换体积与子集策略**。全部 4690 片转成复 int16 交织需 75 GB（每片 16 MB），加原始 64 GB
共 139 GB，没有必要。先转最小可用子集，管线跑通后再按需扩大；原始文件保留不动以便回溯。

| 子集 | 片数 | 体积 | 用途 |
|---|---|---|---|
| 全部纯背景 | 62 | 1.0 GB | DS-6 虚警率标定；DS-7 的背景源 |
| 训练集每个个体 5 片 | 60 | 1.0 GB | 回放与观测量提取验证 |
| 测试集抽样 | 80 | 1.3 GB | 验收子集的抽样验证 |
| 合计 | 约 200 | **约 3.2 GB** | |

**防线二：该数据集不得与任何地理场景绑定展示。** 它没有位置信息，把它"摆"到地图上任一
地点都是编造，违反铁律 14。这个诱惑一定会出现——"把真实信号放到场景里演示"看起来很有
说服力。另外该数据集采自杭州，而临时观测区域用的是西安，本来也对不上。

**命名陷阱**（极易搞反）：文件名中的 `IN` 代表**视距**、`OUT` 代表**非视距**，不是室内室外；
`S0`/`S1`/`S2` 是**采集时间序号**，不是场景编号。

---

## 12. 验证与验收入口

| 层级（04 §15.1） | 目录 | 内容 |
|---|---|---|
| 解析与理论 | `tests/unit` | 单音、噪声、滤波、频移、时延、量化理论值；D3-3 锚点 |
| 组件单元 | `tests/unit`、`tests/golden` | 每组件输入输出、边界、复位；emcore 148 例 golden |
| 处理链集成 | `tests/integration` | 辐射源 → S4 → S5 → 检测闭环 |
| 实测等效 | `tests/regression` | 固定实测片段、特征对比、跨层一致性 ①–④。公开数据集能支撑的范围：① 与 ④ 可做；② 只能做量级合理性检查（论文给出视距与非视距信噪比相差约 20 dB，可判断遮挡附加损耗是否落在十几到二十几分贝这个量级）；③ 完全不能（无高度、无距离变化，双径特征无从体现） |
| 用户任务 | `tests/e2e` | 浏览器提交到结果；断线重连；离线资源完整性 |

入口命令待代码建立后回填 CLAUDE.md「环境与命令」。性能数字一律带平台、模型组合、数据格式声明。

---

## 13. 每次对话的工作协议

1. **开工前**读三处：`CLAUDE.md`（自动加载）、本备忘录 §0.3、`WORKLOG.md` 顶部快照。
2. **动手前**核实引用的路径、版本、行号、常数真实存在；涉及既有成果的判断先抽样取证。
3. **复杂节点先出设计报告**（根目录 07+ 编号）：D3-0 遮挡落点与坐标基座、P1-0 引擎、P2 框图画布。报告定稿保留，偏差另记修正。
4. **收工时**：`WORKLOG.md` 追加节点与量化结果；更新本备忘录 §0.3 与步骤状态；里程碑有变则改 CLAUDE.md 状态表；新决策追加 `D-xxx`。
5. **不做**：不把合成场景截图当演示；不静默改基准；不在 `C-UAV Model Demo/` 内开发；不引入在线依赖到运行时。

---

## 14. 修正记录

| 日期 | 条目 | 原文 | 修正 | 原因 |
|---|---|---|---|---|
| 2026-09-03 | §2 S-2 | `.gitignore` 排除 `data/**`、`C-UAV Model Demo/**`、`node_modules`、`build`、`dist` | 增加排除 `Datasets1-DroneRFb-DIR/` | 该公开数据集 64 GB，2026-09-03 放入项目根目录，绝不可入库。步骤表编写时尚未纳入 |
| 2026-09-03 | §0.5、§3、§9 P1-1、§9 P1-9、§11.2、§11.3、§12 | 全篇隐含前提是「手上没有任何真实 IQ」，故实测相关步骤一律标「数据到位后」并排在最后 | §0.5 加许可行与防线一；§3 摸底工具解除阻塞；P1-1 拆定稿批次；P1-9 拆 a/b；§11.2 改为通用规则；§11.3 拆两条数据线并新增 §11.4；§12 标注支撑范围 | 2026-09-03 入手公开数据集 DroneRFb-DIR，该前提只对了一半：甲方数据仍未到，但已有一批可解析的真实 S4 观测点 IQ。**改的是顺序，不是技术路线**；阶段 0 退出条件与 P1 退出条件均未变动 |

---

## 附录 A 待用户确认清单（合并 CLAUDE.md OPEN 表）

AOI；首批频段/带宽/机型；实测数据与八项回答；交付平台基线；缺失引用文件；框图画布；遮挡计算落点；坐标基座；高分辨率 DEM 与垂直基准；跨层一致性容差；M3 概念模型编号前缀；参考快照去留；单机启动器形态；C++ 标准是否提升。

## 附录 B 首批标准算例与跨层一致性算例

04 §15.2 十二项：单音频移与功率标度；带限噪声与目标 SNR；突发开关与占空比；多信号同频及邻频叠加；接收滤波与群时延；ADC 量化与削顶；DDC 频移低通抽取；宽带 IQ 到信道化 IQ；实测 IQ 回放；实测背景加合成目标；固定随机种子重复运行；用户算法插件处理。

跨层一致性四项：① 自由空间 + AWGN 单音与突发：EM-S-02 Pd 公式 vs M3 蒙特卡洛；② 建筑遮挡：EM-P-04 附加损耗 vs M3 接收功率统计；③ 双径：EM-P-02 vs M3 复信道功率起伏；④ 混合增强：实测背景 + 合成目标 SNR 归约 vs M2 预测。
