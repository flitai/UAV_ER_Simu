# 06. 首期实施备忘录

> 版本 v1.1，2026-09-04（v1.0 于 2026-09-03；本次修订见 §14 首条）。
> **用途**：执行层的路线图与步骤清单，供每次对话开工前对照"现在到哪、下一步做什么、做完怎么验"。
> **分工**：04 号方案定**做什么**；`CLAUDE.md` 定**约定、状态与决策**（真理源，每次会话自动加载）；`WORKLOG.md` 记**过程**；本备忘录定**顺序与步骤**。冲突时以 CLAUDE.md 为准，并在 §14 记一条修正。
> **维护**：每完成一步，更新 §0.3「当前位置」与该步骤的状态标记（`[ ]` 未做 / `[~]` 进行中 / `[x]` 完成，后附日期）；步骤变更写 §14，不改历史。

---

## 0. 一页总览

### 0.1 目标

在给定无人机工作状态、传播场景、接收天线与接收机配置下，端到端等效生成 **DDC 后、信道化前的宽带复基带 IQ**（S4 观测点），使其在时域、频域、统计特征与下游算法性能上与真实电侦设备观测相似；以 B/S 框图平台承载编辑、运行、数据与结果；以 2.5D 地理场景提供传播几何。首期单站、单通道、离线/准实时。

**定位（D-039，用户 2026-09-04 与 2026-09-05 两次强调）**：技术验证的原型系统，用于技术路线验证与功能演示，目的是证明本方在反无人机电子侦察领域的行业理解与技术实力。本备忘录的步骤排序与厚薄取舍都以「演示链路可见、专业」为准，生产级加固只做演示所需最小集；可解释性（模型、参数、依据、溯源）是对内要求，写进设计文档与模型卡，系统界面不展示溯源、演示不主动讲标注。

### 0.2 路线图

```
阶段 0 需求冻结（用户主导，§3）
   │
   ├── 显示子线（§4–§8）  D0 ✔ → D1 ✔ → D3 遮挡库 → D4 同源验收        D2 已并入 G-5；D3-5 移交 G-2
   │                      不进入 P1 退出条件；D3 落地时只替换 G-1 的传播模型
   │
   ├── 桥接线 B（§9A）    注册表 ✔ → JSON 装载 ✔ → 观察者/产品 ✔ → cuav_run ✔ → 任务管理 ✔ → WS 补取 ✔ → 视窗抽取 ✔   B 线已全部完成
   ├── 界面线 U（§9B）    09 报告 → 三视图壳 → React Flow 画布 → 信号视图 → 结果页/数据中心
   ├── 场景线 G（§9C）    场景规范 → geo 运动学 → ScenarioSource → 施加信道 → 编辑器 → 态势图层 → 活动时间线
   ├── MATLAB 线 M（§9D） 工具链 → matlab/ 骨架 → 黄金向量三方互证 → Coder 生成 DDC → 信道化与接收滤波 → P3 参考
   ├── 模板线 T（§9F）    探针 → 规范 ✔ → 提取器 → TemplateSource → 闭环与模型卡        本期只记录不实现（D-045）
   ├── 链路线 C（§9G）    方案 10 ✔ → 格式与接口 → 天线/前端/ADC → 检测升级 → 特征识别 → 评价 → 框图落盘 → 典型链路视图 → golden-02 → 结果页 → DSP 接入 → 收口   D-051（2026-09-07）
   ├── 定位线 L（§9H）    方案 11 ✔ → 格式与接口 → 多源叠加 → 单站测向 → AOA 交叉 → TDOA → 服务与回看 → e2e → 文档收口   D-053（2026-09-09）；插在 ④b 之前
   ├── 传播线 R（§9I）    方案 12 ✔ → 组合框架与档位 → 大气降雨移植 → 地面双径 → 统计阴影 → 城市经验 → 界面 → 收口        D-058（2026-09-10）；E3 待 D3
   ├── 可视化线 V（§9J）  方案 13 ✔ → 卡片栈 → 叠加加重（含告警区）→ 全宽时间轴 → 方位图 → 精度与覆盖场 → 网格瀑布/余辉 → 全景一屏   D-061（2026-09-12）；切片 ⑧ 插在 ⑦ 之后
   │
   ├── IQ 主线（§9–§10）  P1 最小闭环（P1-5/P1-6 撤销，由 B/U 替代；P1-8 由 M-2 兑现）→ P2 → P3 → P4
   │
   └── 数据线（§11）      8 项摸底 → 入库并冻结 holdout → 阶段 1 回放源 → 阶段 3 四组校准
                          从阶段 0 起并行，不等阶段 3

纵向切片（§9E）：① 一条链到频谱 → ② 场景里飞起来 → ③ 拖出框图 → ④a 链路跑通 → **⑥ 多源多站测向定位（⑥a 叠加 → ⑥b 测向与 AOA → ⑥c TDOA，D-053 插在此处）** → **⑦ 选传播环境（D-058，2026-09-10 插在此处）** → **⑧ 一屏讲清楚（D-061，2026-09-12 插在此处）** → ④b 检测识别评价 → ④c DSP 厚化 → ④d 收口 → ⑤ 遮挡接入（D 线）
                M-0 / M-1 / M-4 与切片 ① 并行
```

### 0.3 当前位置（每次对话更新）

| 日期 | 位置 | 下一步 |
|---|---|---|
| 2026-09-03 | `CLAUDE.md` 完成（227 行，D-001 ~ D-017）；本备忘录 v1.0；无目录骨架、无代码、`WORKLOG.md` 未建；实测数据待用户提供 | §2 建骨架 → D0-1 定临时 AOI → P1-0 引擎设计报告 |
| 2026-09-03 | §2 工程骨架 S-1 至 S-7 全部完成：目录树、`.gitignore`、`WORKLOG.md`、`docs/` 八份规范骨架、`tests/` 五级与回归清单、`web`/`server`/`engine`/`geo`/`scene` 五个可空跑的工程、两个脚本。`scripts/build-all.sh` 全绿。git 已初始化但**尚无提交**，75 个文件待提交 | D0-1 定临时 AOI → D0-2 底图裁切；并行 P1-0 引擎设计报告 |
| 2026-09-03 | 本备忘录按公开数据集入手的事实修订 9 处（见 §14 第 2 条），新增 §11.4 数据集接入步骤 DS-1 至 DS-7 与两条防线。技术路线与两处退出条件均未变动 | 三条并行：D0-1 定临时 AOI；P1-0 引擎设计报告；DS-2 转换脚本（DS-1 许可待用户确认） |
| 2026-09-03 | `docs/iq-format.md` 第 3、4 节定稿（P1-1 第一批）；`tools/` 落地：转换脚本 DS-2/DA-2 与摸底工具 DS-5/DA-5，32 项单测全绿，两个数据集各三片真实文件跑通、逐段回读对拍通过。两个新实测事实：DroneRFa 片长非固定 1.3–1.7 亿点；正交不平衡在噪声路径上（D-020 据此改判据） | DS-6 真实背景虚警率标定；D0-1 定临时 AOI；批量转换与 DS-4/DA-4 冻结验收子集（等用户定跑批时间） |
| 2026-09-03 | 第二个公开数据集 DroneRFa 摸底完成（本地 22 片、21 GB，全集约 560 GB），得到首个来自真实数据的定量物理结果：路损指数 n = 1.90。本备忘录随之修订 §0.5、§11.1、§11.3、§12，新增 §11.5 与步骤 DA-1 至 DA-7。P3 的 B 组「多距离场景」由「完全不能」改为「粗档可以」 | 三条并行：重算路损指数误差范围（DA-6）；DS-2 与 DA-2 转换脚本、DS-5 摸底工具；D0-1 定临时 AOI。DS-1 与 DA-1 许可待用户确认 |
| 2026-09-03 | D0-1、D0-2、D0-5 完成：AOI 改为北京亚运村周围（用户指定，D-021），底图与 DEM 改为整份全球文件登记为共享资产 `data/basemap/`（用户"128 GB 没有问题"，D-022），另裁一份 AOI 切片作测试夹具。两个脚本 `scene/register_basemap.py`、`scene/fetch_tiles.py` 可复跑。整份底图经 Range 服务实测 206；无头浏览器渲染验证见 WORKLOG 同日条目 | D0-3 建筑解码（首先验证 z15 feature 有无 id）；D0-4 对拍基准改为对北京 bbox 重拉 Overpass；D1-1 Range 服务；AOI 范围大小待用户确认 |
| 2026-09-04 | 底图与 DEM 约 145 GB 拷入本项目自持（D-023），运行时不再依赖 Airports 路径；AOI 范围经实测比较后由用户定为 20 × 20 km（D-024），切片重裁为 749 块、11147792 字节；`scene/probe_buildings.py` 落库，销掉「z15 建筑有无稳定 id」这个未知点并测出北京高度来源分布（D-025） | D0-3 建筑解码（按 id 合并，`src` 需配合 D0-4 的 Overpass 拉取）；D1-1 Range 服务 |
| 2026-09-04 | **D0-3 完成**：`scene/decode_buildings.py` 产出 `buildings.geojson`，47582 栋、15919279 字节、sha256 `65ae4dce…`，`tile:height` 21.4% / `est:area` 78.6%。几何用像素级交并比与底图自带建筑层对拍，三个视角 0.9928 / 0.9964 / 0.9994，「只在瓦片层」的像素为个位到百位量级（抗锯齿残差） | D0-4 拉 Overpass（对拍 + 拆 `src`，需联网）；D0-6 区域总清单；D0-7 质量报告；D1-1 Range 服务 |
| 2026-09-04 | **D0 全部完成**：D0-4 拉原始 OSM 标签（唯一联网步骤，47572 要素）并对拍，D0-6 区域总清单，D0-7 质量报告；建筑高度来源按原始标签定案为 `osm:height` 1.74% / `osm:levels` 19.59% / `est:area` 78.55%（D-027b）。显示子线的场景数据包就位 | D1 底图上前端：D1-1 Node 版 Range 服务、D1-2 web 骨架、D1-3 样式移植 |
| 2026-09-04 | **D1 全部完成**：Range 文件服务（30 项测试）、web 骨架、样式移植（与 Airports 逐层等价，有黄金基准）、探针与端到端测试（14 项断言）。前端已能离线渲染北京亚运村的底图、山体阴影与 47582 栋建筑的三维拉伸 | D2 态势图层：状态流 schema、图层模块、Canvas 叠加层、浅色底图上的色值标定 |
| 2026-09-04 | 清掉入库文件里的机器相关绝对路径（新增铁律 17）：两个脚本改为从文件位置推导，三份产物清单改存仓库相对路径，三处注释去掉外部工程的绝对路径；新增 `scripts/check-paths.sh` 并接入全量构建。顺带修掉一个生成器缺陷：它会用脚本里内嵌的旧文字覆盖手工更新过的说明 | D2 态势图层 |
| 2026-09-04 | **路线图修订 v1.1**。用户定后续重点：界面、场景与目标活动生成、框图模型、波形频谱可视化、后端计算，并追问 MATLAB 优势是否用上。新增 B / U / G / M 四条步骤线（§9A–§9D）与五个纵向切片（§9E）；P1-5、P1-6 撤销，P1-8 由 M-2 兑现，D2 并入 G-5，D3-5 移交 G-2；四份规范骨架落地（`docs/{diagram-format,scenario-format,display-products,component-catalog}.md` 与 `docs/schemas/` 两份 schema）；用户拍板 React Flow、子进程 `cuav_run`、场景运行时在 `geo/`、切片顺序（D-030 至 D-036）。核实到的引擎缺口：无注册表与参数描述、`configure()` 只收两个 map、无可执行入口；开发机 MATLAB R2025a 与全套工具箱在盘 | 切片 ①：B-1 组件注册表与参数描述；并行 M-1 `matlab/` 骨架与 `pwelch` 黄金向量 |
| 2026-09-04 | **09 报告前半**（主界面框架与三视图，§0–§14，382 行）写出，U-0 转 `[~]`；**M-0 销项**（工具链已核实、用户确认 Coder 产物许可不受限）；**B-1 完成**：组件注册表、参数描述、目录导出与一致性测试，引擎 30 项单测全绿 | 切片 ①：P1-4a `SpectrumAnalyzer` 与 B-3 `ObservationTap`，并行 M-1；之后 B-2 框图 JSON 装载器 |
| 2026-09-04 | **09 报告前半评审与 v1.1**：按「必须改」十二条修订（面包屑顶栏、两正交徽标、链路对象、AOI 边界、频谱仪表约定、双游标、检测叠加、撤销重做、溯源面板、用户与主流程、快捷键避保留键、回放源改 `data_id`）；引擎 `FileReplaySource` 用户参数改 `data_id`、`manifest_path` 转内部参数（`ParamSpec.internal`），30 项单测全绿；四份规范同步（D-037） | 切片 ①：P1-4a `SpectrumAnalyzer` 与 B-3 `ObservationTap`，并行 M-1 |
| 2026-09-05 | **09 报告十三项暂定拍板**（v1.2，D-038）：数据中心独立页、2K 基线、瀑布最新行在顶且频谱紧贴其上、纵轴 dBm 优先带标定来源、验收集允许回放并提示，其余采纳原案；**DS-1 / DA-1 销项**（用户确认两个公开数据集无任何使用与署名约束）。数据线新增待办：按论文参数为两个公开数据集估算标定常数并写进索引 | 切片 ① 继续：B-2 框图 JSON 装载器 |
| 2026-09-05 | **切片 ① 引擎侧三件落地**：P1-4a `SpectrumAnalyzer`（Welch 功率谱 dBFS，double FFT，与 Python 与 MATLAB `pwelch` 三方互证）；B-3 运行观察者、平台抽象层、`ObservationTap` 显示产品（谱行与包络行定长二进制加索引，同种子逐字节复现）；M-1 `matlab/` 骨架与 M-4 首个黄金向量（`run_matlab.sh` 经 `MATLAB_ROOT`）。引擎 37 项单测全绿。踩到 MATLAB `pwelch` 的 `'centered'` 对偶数 nfft 与 `fftshift` 差一格的坑 | 切片 ①：B-2 框图 JSON 装载器 → B-4 `cuav_run` → B-5/B-6/B-7 服务侧 → U-1/U-3 |
| 2026-09-05 | **B-2 框图 JSON 装载器完成**：`engine/include/cuav/diagram_json.h` 与 `src/diagram_json.cpp`，按 `docs/diagram-format.md` 装成 `Graph`；观测点并联 `ObservationTap`；`IDataResolver` 两种实现（解析旁挂 / 数据索引）把 `data_id` 换成 `manifest_path`；内部参数出现即拒；错误带 `{code, node_id, port, message}`，`Graph::connect / validate` 增加失败分类供映射；示例框图装载运行、`total_samples` 由时长补齐。引擎 50 项单测全绿。四项口子拍板 D-040（解析旁挂、`out_dir` 到 `init` 才拒、`iq` 产品拒绝、错误码首版 17 个） | 切片 ①：B-4 `cuav_run`（`--catalog` 生成目录黄金基准、`--validate`、`--run … --resolved / --data-index`）→ B-5/B-6/B-7 服务侧 → U-1/U-3 |
| 2026-09-05 | **B-4 `cuav_run` 可执行完成**：`--catalog`（目录黄金基准 `tests/golden/component-catalog.json` 首次生成并有「已有条目不变」比对测试）、`--validate`（只校验不落盘，一条 `validate` 事件或一条 `error`）、`--run … --out … [--seed] [--resolved | --data-index]`（stdout 事件 `{seq, task_id, type, t_s, payload}`，`--out` 下原样镜像 `events.jsonl`；`product_row` 不带数据、观测点逐行刷盘；`progress` 按墙钟节流）；退出码 0–4。切片 ① 框图命令行跑通；引擎 57 项单测 + 3 项冒烟全绿。约定记 D-041 | 切片 ①：B-5 服务任务管理器（`spawn` 数组参数、`data/runs/<task_id>/`、写解析旁挂、`POST /api/v1/tasks`、`GET /api/v1/components` 缓存 `--catalog`）→ B-6 WS 补取 → B-7 视窗抽取 → U-1/U-3 |
| 2026-09-05 | **B-5 服务任务管理器完成**：`server/src/tasks/{engine,resolve,store,manager,routes}.ts`，端点 `GET /api/v1/components`、`POST /api/v1/tasks`、`GET /api/v1/tasks[/{id}]`、`POST .../cancel`；提交时同步 `--validate` 不过即 400；引擎 cwd = 仓库根、路径全相对 ASCII；整行脱敏；串行队列；取消、SIGTERM 收尾与重启对账；假引擎夹具使状态机测试不依赖 C++ 构建。服务 58 项测试全绿（含真引擎集成）；真服务冒烟：切片 ①（1953 + 489 行）、真实片段回放（3906 + 977 行）、取消、杀服务、重启恢复全过。约定记 D-042 | 切片 ①：B-6 WS 服务端与补取（`ws` vendored、`/ws` 订阅、`GET .../events?since`、`product_row` 按偏移读文件转二进制帧）→ B-7 视窗抽取 → U-1 / U-3 |
| 2026-09-05 | **B-6 WebSocket 服务端与补取完成**：`ws 8.21.3` 随包锁版并建仓库根 `THIRD-PARTY-NOTICES`；`server/src/ws/{events,hub}.ts`：`/ws` 订阅、`subscribed` 应答、按 `readEvents()` 回放到空批次再转实时、`product_row` 按偏移读 `.f32` 转二进制帧（布局 `[u32 header_len][JSON 帧头补齐 4 字节][Float32]`）、背压只丢行帧并以连续的 `dropped` 告知、心跳与漏 pong 断开；`GET /api/v1/tasks/{id}/events?since&limit` 缓冲内切片、缓冲外顺序读 `events.jsonl`、重启后合成服务端终态。服务 72 项测试全绿；真引擎切片 ① WS 回放 2447 条事件 82 ms；真服务冒烟：断开后以 `since` 重连首条 = since + 1、并集无缺号、SIGINT 时客户端收 1001。约定记 D-044 | 切片 ①：B-7 视窗抽取（`server/src/products/`，读端以文件长度定行数，与 Python 参考逐值一致）→ U-1 三视图壳（含 `web/src/api/ws.ts` 客户端）→ U-3 信号视图 |
| 2026-09-05 | **D-045 辐射源模板评估与 T 线**：用户问「现有数据集能否作为将来模拟特定无人机信号的基础」；核实为 04 §7.2 的既定路线、代码一件未做；定三模式（首选特征等效）、供给边界（未记录的量都是配置项）、回放模式的信噪比上限；规范草案 `docs/emitter-template.md`，新增 §9F，三处登记；**只记录不实现** | 切片 ①：B-7 视窗抽取 → U-1 三视图壳 → U-3 信号视图（不变）；T-0 探针待排期 |
| 2026-09-06 | **U-1 三视图壳完成，dBm 纵轴链打通（D-047）**：`web/src/{state,api,shell,results,signal,data}/`，手写 hash 路由、`useReducer` store、REST 与 WS 客户端（退避重连、`since` 补取、二进制行帧零拷贝、rAF 批量提交）、顶栏面包屑与两正交徽标、运行组、底部抽屉日志与状态条、框图 JSON 页、结果页信号页头、探针 `app`；引擎内部功率单位定为 mW（合成源 `level_dBm` / `power_dBm`，回放源按清单常数换算，修正了量化码未除满量程的 90.31 dB 偏差），两个公开数据集的标定常数估出并刷进 4714 份清单（DS-8 / DA-8 销项）；界面只标 `dBm`，来源只在 `?dev=1`。web 43 项单测、`scene-smoke` 14 项、`slice1-smoke` 23 项、引擎 62 项、服务 102 项、工具 37 项全绿 | 切片 ①：U-3 信号视图（频谱 + 瀑布，实时行走 WS 二进制帧、回看走 B-7，纵轴 dBm） |
| 2026-09-06 | **B-7 视窗抽取端点完成**：`server/src/products/{window,spectrum,envelope,source,meta,jsonl,routes}.ts` + 参考实现 `algos/reference/product_window.py` + 黄金基准 `tests/golden/product-window.json`；端点 `GET /api/v1/results/{task}/{op}/{spectrum\|envelope}[?t0&t1&f0&f1&px&py&stat]`、`.../{kind}/index`、`.../{track\|links\|detections}`；引擎 `ObservationTap` 改为写完第一行就刷索引。服务 102 项测试全绿、引擎 58 项全绿；真引擎产品与 Python 参考 2046363 个值逐位相同。约定记 D-046 | 切片 ①：U-1 三视图壳（含 `web/src/api/ws.ts` 客户端）→ U-3 信号视图（频谱 + 瀑布） |
| 2026-09-06 | **U-3 信号视图完成，切片 ① 收口**：`web/src/signal/` 十二个纯函数 / 引擎模块（视窗数学、客户端归约守黄金基准、色图、量程、迹线、包络映射、请求器、外部 store、瀑布簿记、渲染器、导出、命令）+ 四个组件，右栏仪表面板；跟随 / 回看两模式、结束即收口、marker 与游标键、导出；事件折叠每帧 ≤ 400 条。web 87 项单测、`slice1-smoke` 39 项、`scene-smoke` 14 项全绿；D-048 | 切片 ②「场景里飞起来」：G-0 示例场景 → G-1 `geo/` 运动学与链路预算 → G-2 `ScenarioSource` → G-3 施加信道 → G-5 态势图层 → G-4 编辑器；08 报告定稿 |
| 2026-09-06 | **切片 ②「场景里飞起来」收口**：08 报告定稿（v1.1，583 行）；G-0 示例场景 `demo-01`（亚运村，单站单机，过顶后远去）；G-1 `geo/` 大地坐标与运动学库 + `cuav_run --scenario-track` + 航迹黄金基准；G-2 `ScenarioSource` / `SceneEmitterSource` + 装载器场景注入 + `track.jsonl` / `links.jsonl`；G-3 `SceneBoundChannel` / `FreeSpaceChannel`；G-5 七个态势图层 + 20 Hz 定频驱动 + 色值标定（D2-4 销项）；G-4 场景编辑器（四工具、表单、撤销重做、`GET/PUT /api/v1/scenarios/{id}`）。组件目录 8 → 12。引擎 114 项单测 + 8 项 ctest、服务 110 项、web 95 项、`scene-smoke` 14 项、`slice1-smoke` 39 项、`slice2-smoke` 27 项全绿。两条硬断言：三时刻位置与黄金航迹差 **0**（容差 1e-6 度）、两时刻带内功率差与几何路损差偏差 **0.074 dB**（容差 0.5）。约定记 D-049 | 切片 ③「拖出框图」：U-0 后半（09 报告的框图画布交互）→ B-1 `describe()` 已有、U-2 React Flow 画布 → U-3 包络与散点余项 |
| 2026-09-07 | **08 报告改写为 v2.1**（1326 行，按技术设计文档文体重写并订正过时事实）；**U-0 后半写出，09 报告 v1.8**（854 行）：§6 扩为完整框图视图设计（节点外观与六类类别色实算对比度、拖放与吸附、连线三档拒绝提示、参数面板四控件与互斥成组、观测点右键打点与双向联动、场景绑定动态端口、报错四元组定位三档、撤销与序列化两条要求），新增 §7.4 对比模式与检测叠加，§11 补画布键鼠（`Ctrl+D` 与 Chromium 收藏冲突改 `Ctrl+Shift+D`），§16 改为「尚未规定的内容」。切片 ③ 的设计前置就位 | 切片 ③：**U-2 React Flow 画布**（按 09 §6 实现；React Flow 尚未安装，需 vendored 锁版本并进 `THIRD-PARTY-NOTICES`）→ U-3 余项（检测叠加、对比观测点；IQ 散点仍待引擎 `iq` 产品，D-040 ③） |
| 2026-09-07 | **U-2 框图画布完成，切片 ③ 主体交付**：React Flow 锁版随包并进 `THIRD-PARTY-NOTICES`（含 13 个传递依赖）；画布只查目录不复制规则——组件库、参数控件、连线判据与拒绝理由全部来自 `GET /api/v1/components`；节点按类别色与端口形状区分、无 `position` 的按拓扑深度分列、观测点画成端口下的标记、互斥参数并排成组、内部参数不出现；撤销栈存规范文本。web 131 项单测、`slice3-smoke` 22 项全绿，既有三套 e2e 无回归。过程中订正两处：全库内部参数是 **11 处**不是 8（08 §4.3、09 §6.6 已改），连线字段是嵌套的 `{node, port}` 不是扁平的 `from_port` | 切片 ③ 余项：U-3 的检测叠加与对比观测点（09 §7.4）；IQ 散点仍待引擎 `iq` 产品（D-040 ③）。之后进切片 ④ |
| 2026-09-07 | **U-2 收口后的两轮用户实测反馈已修**（拖动滞后：画布交互期间自持节点位置；框图节点穿透隐藏浮到地图上：React Flow 给节点写行内 `visibility: visible`，隐藏视图补 `opacity: 0`，坑记进 09 §4.2）；新增 `docs/diagram-canvas-guide.md`（操作说明与约束，186 行，把「已实现 / 画布当场拒绝 / 首期边界 / 设计已定但未实现」四类分开写）；补掉观测点标记不可点的功能空洞。`slice3-smoke` 29 项。**用户指出天线与接收机两类组件缺位**，核实后记 D-050：不是遗漏而是切片顺序，但天线增益折在信道里、`antenna.pattern` 声明未用、接收机完全没有、噪声是手兑而非由噪声系数推出，现观测点名为 `s4` 但严格说不是 S4 | **P1-0b 天线与接收机组件设计（另起对话先做设计，D-050）**；之后按设计实现前三件（不依赖 Coder），DDC 待 M-2 |
| 2026-09-07 | **10 号方案写出（v1.0，D-051）**：用户指示框图不再自由组合、先给固定的典型框架并给出检测识别与评价。方案定框图页默认为典型链路视图（04 §5.1 九环节固定、三种信号源模式、只改参数）、自由画布降为高级模式 `#/diagram/canvas`、框图格式只加 `template_ref`；顺带拍板 D-050 三问（解析式方向图 + 指向 + 极化；接收机噪声由噪声系数生成取代手兑噪声、既有基准不动；削顶为数据标记）；新增特征提取、模板匹配识别、评价器与真值口径；新增 §9G C 线十二步与切片 ④ 重排。P1-0b 由此兑现 | **切片 ④a：C-1 格式与接口 → C-2 天线 / 前端 / ADC 三件（手写 C++，不等 Coder）→ C-6 框图落盘 → C-7 典型链路视图 → C-8 `demo-02`**；之后 ④b C-3 至 C-5、C-9、G-6；M-2 / M-3 → C-10（④c）|
| 2026-09-07 | **C-1 格式与接口完成**：端口类型增至七种（新增 `RecognitionList`，兼容矩阵 36 → 49 行、既有 36 行逐字未变）；`PortSpec.optional`（未连的可选输入口不算悬空、调度器也不等它的数据）；框图可选顶层 `template_ref` 三处白名单同规则 + schema + 前端键序 + 错误码 `template`；`SceneParamFrame` 增离开角 / 到达角 / 航向 / `tx_on` / `tx_center_Hz` 七字段（离开角由 `geo/` 反向 `look_angles` 单算，不由到达角取反）；`BlockMeta.clip_count`（`AddMixer` 两路相加）；场景 `emission.polarization` 五档可缺省、站点 `antenna.pattern` 真正入库。引擎 121 项单测 + 8 项 ctest、服务 111 项、web 133 项、四套 e2e 全绿 | **C-2 天线、前端、ADC 三件 + 既有两件加参**（`AntennaGain` / `ReceiverFrontEnd` / `AdcQuantizer`，手写 C++，解析锚点见 10 报告 §3）；并行可做 C-3 检测升级与 C-6 服务端 |
| 2026-09-07 | **C-2 天线、前端、ADC 三件完成**：`AntennaGain`（EM-B-07 的 E1 解析式方向图 `12·(Δ/θ)²` 截于副瓣底、指向可随航向、五档极化失配只在接收端计一次、`scene` 是 C-1 那个可选输入口的首个真实使用者）、`ReceiverFrontEnd`（`N0 = −174 + 10·log10(T/290) + nf`，与 `geo/link_budget.cpp` 共用同一常数，故帧里的链路噪声底读数与实际注入逐项一致；增益、本振频偏、IQ 不平衡 `K1·y + K2·conj(y)`、直流）、`AdcQuantizer`（量化信噪比 `6.02·bits + 1.76 dB`；**削顶是数据标记不是降级**，逐块进 `clip_count`，全程比例超阈值才降级）。两个既有组件加参且缺省保旧行为，既有示例与产品基准一字未改。组件目录 12 → 15，04 §8.1 的六类首次全部有组件。引擎 142 项单测 + 8 项 ctest、服务 111 项、web 134 项、四套 e2e 全绿 | **C-6 框图落盘与结果端点** 或 **C-3 检测升级**（两者互不依赖）；之后 C-7 典型链路视图、C-8 `demo-02` |
| 2026-09-07 | **C-6 框图落盘与结果端点、C-7 典型链路视图完成，切片 ④a 收口**：框图页默认不再是自由画布，而是一条固定的九环节链——用户只选变体、改参数、勾观测点；自由画布降为 `#/diagram/canvas` 的高级模式，U-2 的全部能力保留。链路视图**不持有第二份状态**，它是同一份框图文档的投影（`parseChain` 解出、`compile` 编译回），撤销重做与脏标记因此沿用 U-2 那一套。框图开始落盘（`data/diagrams/`），`Ctrl+S` 与工具条「保存」接到真端点。**切片 ④a 的两条硬断言在浏览器里达成**：S1 实测 −51.60 dBm 对链路预算 −51.15 dBm（差 0.45 dB，线 1.5）；S2 底噪实测 −119.37 dBm 对解析 −119.35 dBm（差 **0.02 dB**，线 0.5）。引擎 142 项单测 + 8 项 ctest、服务 116 项、web 149 项、五套 e2e（14 + 39 + 27 + 30 + 25）全绿 | **切片 ④b**：C-3 检测升级（滑动噪声估计 / 恒虚警 + `detections.jsonl`）→ C-4 特征与识别 → C-5 真值与评价 → C-9 结果页两页签；G-6 活动时间线。C-8 `demo-02` 宽带场景与 C-10 的 DSP 接入随后 |
| 2026-09-08 | **用户实测发现一处导航缺口（待修）**：进了自由画布 `#/diagram/canvas` 之后，`Alt+2` 与顶栏「框图」按钮都只切视图不改子形态，回不到典型链路视图，只剩浏览器后退键与手改地址两条路。实测矩阵与根因见 WORKLOG 同日条目。10 报告 §5.6 的「单向」说的是**文档**的单向性（画布里改过的框图可能解不回槽位），不是导航；这是措辞被照字面执行 | **先修这处导航缺口**（用户 2026-09-08 指示另起对话）：画布加「回到典型链路」按钮、解不开时明说不静默丢改动、`ui/navigate` 不带 `canvas` 时按默认形态处理；之后进切片 ④b（C-3 检测升级）|
| 2026-09-08 | **导航缺口已修（D-052）**：画布工具条加「回到典型链路」，解得开直接回、解不开先说明再确认；`ui/navigate` 不带 `canvas` 按默认形态处理，于是 `Alt+2` 与顶栏「框图」都能从画布回到典型链路。实测纠正一条判据：**删**节点仍解得回链（缺席槽位按旁路或缺省），只有**加**了模板之外的节点才解不开。确认后也不丢改动——落在典型链路页既有的「不是本模板」提示上，由用户显式点「新建典型链路」才换文档。措辞同步改 10 §0 / §5.6 / §11、09 §17、`docs/diagram-canvas-guide.md` | **切片 ④b**：C-3 检测升级（滑动噪声估计 / 恒虚警 + `detections.jsonl`）→ C-4 特征与识别 → C-5 真值与评价 → C-9 结果页两页签；G-6 活动时间线 |
| 2026-09-08 | **观测点可见性三处已修**（用户「S0–S5 勾上和不勾看不出区别」）：取证确认链路是通的（S1 −52.61 dBm 对 S2 −32.61 dBm、`task.json` 记三个观测点），问题在界面。① 观测点一行由只写 `S0…S5` 改为写全名 `S0 辐射源输出`…`S5 子信道`（**链条结构不动**，用户拍板；名称原来只在悬停提示里等于没写），2K 下一行放得下；② 工具条给「观测点与当前任务不一致，重新运行后生效」；③ 结果页中栏加观测点页签（左栏在窗宽不足 1920 时默认收起，原来收起就换不了），默认选主产品 S4；④ 中栏版式：链条区改为占满工具条以下高度的面板并纵向居中，九个环节按 **3 × 3** 排（行距 56 px、卡片保持 176 × 128、连接件拉伸铺满列宽），连接箭头挪到卡片之后消除换行时行首的孤立箭头。10 §5.1 版式图仍画作一行，属示意，实排以 §5.2 为准。S2 / S3 / S4 读数相同不是缺陷，是 DDC 与信道化未实现的真实后果。中途走过一版「圆点上链」并按用户指示撤回。10 §5.2 / §5.7 补条款、§11 记一条偏离。`slice4-smoke` 34 → 50 项 | **切片 ④b**：C-3 检测升级 |
| 2026-09-09 | **L-0 完成：11 号方案写出（v1.0，807 行，D-053）**。用户问「场景中辐射源可能有多个；单站要做识别与测向；多站可做交叉定位或时差定位——框图里如何体现、如何设置」，并拍板交付范围为设计 + 纯软件第一切片。方案定：多源按源分支、接收天线后叠加（新组件 `Superposition`）；多站 = 典型链路加站点维度、尾部加「测向」与「多站定位」两槽位（9 → 11）；测向与到达时间为 M2 效应模型且正式声明取真值的合法边界（§1.3，每行 `truth_consumed`）；新增三种报告端口与固定可选口；`sites[].clock` 与时统质量；emcore 的 AOA / TDOA / 测向与四份 golden 移植到 `geo/` 并改平面坐标接口。新增 L 线（§9H）与切片 ⑥（§9E） | **切片 ⑥a**：L-1 格式与接口（三端口、帧身份字段、双绑定放宽、多站守卫替换、`check_wiring`、`sites[].clock`）→ L-2 `Superposition` 与多源模板 |
| 2026-09-09 | **切片 ⑥a 收口（L-1、L-2）**：三种报告端口与 100 行兼容矩阵、帧身份字段、多站解锁、`sites[].clock`、`check_wiring` 与 `port_optional`；`Superposition` 组件、三个实例维度、`demo-03` 三站三源场景与黄金航迹、「布目标」工具。硬断言达成：同频双源在 S1 的合成功率对两条链路预算的线性和偏差 **−0.001 dB**；3 源 × 3 站编译出 45 节点 72 连线并通过引擎校验。引擎 151 项 + 8 项 ctest、服务 117 项、web 163 项、五套 e2e 160 项全绿 | **切片 ⑥b**：L-3 `DirectionFinder` 与多站模板 → L-4 `MultiSiteLocator(aoa)` 与 emcore 移植 → L-6 服务端与回看 |
| 2026-09-09 | **L 线 L-0 至 L-8 全部完成，切片 ⑥ 三片收口**：11 号方案 807 行（D-053）；三种报告端口与多站解锁；`Superposition` 与多源模板、`demo-03` 三站三源场景；`DirectionFinder` / `ToaEstimator` / `MultiSiteLocator` 三件 M2 效应组件；emcore 的 `geo` / `locate_aoa` / `locate_tdoa` 移植（golden 43+6+42+36+128 组 rel ≤ 1e-9，求解器改平面坐标接口、legacy 常数只守 golden 且加了构建守卫）；地图测向线与定位椭圆。**硬断言**：同频双源合成功率对预算和差 −0.001 dB；逐站三源叠加 ≤ 0.013 dB；测向经验 σ 差 0.80%、1800 行归一化方差 1.0225；AOA 锚点 17.4532925 / 34.906585 / 20.5495 m 与 TDOA GDOP 0.942809 逐位命中；AOA 有效档 2σ 覆盖 87.0%；时差中位误差 7.95 m、扣掉钟差偏差后 85.3%。**三条实测发现**：emcore 的几何分级看不见近简并配置；未标定的固定钟差是协方差表达不了的系统偏差；两种时差加权的方向与计划假设相反（独立站对是保守而非乐观）。引擎 173 项 + 8 项 ctest、服务 119 项、web 163 项、六套 e2e 213 项全绿 | **回到切片 ④b**：C-3 检测升级（滑动噪声估计 / 恒虚警 + `detections.jsonl`）→ C-4 特征与识别 → C-5 真值与评价 → C-9 结果页两页签；L-9 定位评价随 C-5 / C-9 |
| 2026-09-10 | **R-0 完成：12 号方案写出（v1.0，D-058）**。用户指示传播信道要反映所处传播环境、地理空间与建筑物分布带来的变数，并在框图里从简单到细致地选择考虑哪些因素。核实到两个信道变体今天在物理上是同一个自由空间模型（`line_of_sight` 与 `extra_loss_dB` 都是字面常量），47582 栋建筑没进过任何链路计算。方案定档位 E1（= 今天，缺省）/ E2（双径或城市经验二选一 + 阴影 + 天气）/ E3（待 D3，枚举保留但两头都拒）、算在 `geo/` 配在 `ch` 卡片的代理机制、防重复计损的一条规则两道闸、`included_loss_terms`；顺带改正 `ChannelPathSet`「首期即启用」的错误表述 | R-1 组合框架与档位（收口条件是「什么都没变」） |
| 2026-09-10 | **R 线 R-0 至 R-7 全部完成，切片 ⑦ 收口**：12 号方案（D-058）；`geo/propagation.{h,cpp}` 五个效应与档位组合；天气两式移植 emcore（64 例 golden rel ≤ 1e-9）；双径锚点 A1–A5、阴影 C1–C5、城市经验 B1–B4 全过；槽位表 `proxy` 让传播参数配在「传播信道」卡片上、算在帧生产端；E3 界面置灰 + 引擎报错两头都做。**E1 缺省档的产品字节与上一版逐字节相同**（拿上一版提交建出的 `cuav_run` 直接对拍）。引擎 208 项 doctest + 8 项 ctest、服务 124 项、web 193 项、六套 e2e 14 + 39 + 27 + 30 + 86 + 63 = 259 项全绿 | C-3 检测升级（切片 ④b）|
| 2026-09-10 | **自由画布删除（D-060）**。用户判定它「不重要、难控制、华而不实」，先把一条完整的信号级仿真流程跑通。删掉画布本体与 React Flow 依赖（产物 −202 KiB，−12.3%，实测），保留全项目在用的框图文档模型与两件参数控件；路由去掉子形态但旧地址仍落在框图页；解不成典型链路的框图改为**只读展示原文**，不硬解也不改写（铁律 15）；端到端的示例框图改走公开端点 `PUT /api/v1/diagrams/{id}`，`slice3-smoke` 30 项整套删除 | C-3 检测升级（切片 ④b）|
| 2026-09-12 | **V-0 完成：13 号方案写出（v1.0，D-061）**。用户看过 Aaronia AARTOS 素材（`ref/Aaronia AG/`）与 em-demo 版式后要求借鉴态势与信号可视化；方案定三档十一项与四个切片，本期先排切片 ⑧「一屏讲清楚」（V-1 卡片栈、V-2 叠加加重含圆形告警区、V-3 全宽时间轴）+ 两处顺带发现（采用任务不载场景；链路线因 `link_id` 按最后一个 `-` 拆分从未画出）。新增 §9J 与切片 ⑧；代码、schema、黄金基准一件未动 | 切片 ⑧：V-1（先修两处缺口）→ V-2（基准变更单独收口）→ V-3（待用户排期，插在 ⑦ 之后、④b 之前，可与 C-3 并行） |
| 2026-09-12 | **V-1 完成：目标与站点卡片栈上线，两处缺口先修**（服务端 `task.json.scenario_id`、前端采用任务载入它的场景；`link_id` 按已知站与源精确匹配，链路线第一次画出来）。右栏常驻目标列表 + 分节目标卡（每站一行距离 / 路损 / 接收电平 / 信噪比 / 测向 ± 2σ）+ 站点卡；新 `slice8-smoke` 16 项，六套 e2e 245 项全绿 | 切片 ⑧：V-2 叠加加重（含 `zones[]`，基准变更单独收口）→ V-3 时间轴 |
| 2026-09-12 | **V-2 完成：告警区进场景格式（三处同一提交，demo-03 加区、航迹基准只哈希变）；地图叠加加重**（图层 7 → 13：告警圈、链路距离标注、高度立柱、选中环、目标标签；目标图标 40 px；测向渐隐色带；入圈目标红环 + 徽标）。`slice8` 28 项、六套 e2e 257 项全绿 | 切片 ⑧：V-3 全宽时间轴与回放缓冲 |
| 2026-09-12 | **V-3 完成、切片 ⑧「一屏讲清楚」收口**：全宽时间轴条（`.app` 网格第三行，场景页与结果页；计数、活动标记、可拖游标、播放与倍速、跟随实时）、`sceneStore` 历史与 `snapshotAt(t)`、地图 / 卡片 / 探针 / 计数四处共用 `currentSituation`、与信号游标双向同步、`Space / Home / End`。`slice8` 40 项、六套 e2e 269 项、web 204 项全绿 | ④b C-3 检测升级与落盘（切片 ⑨ 待排期） |
| 2026-09-12 | **V-3b 完成：场景页信息分层（D-062）**。用户看过收口后的场景页说「信息互相交错，缺乏层次，成为了信息的堆砌」；重新梳理为左栏 = 配置（对象树 7 行 + 选中对象表单 + 数据包一行）、右栏 = 观察（焦点卡一张 + 目标列表 + 站点行）、地图只给焦点目标画全套叠加、编辑工具收进「编辑场景」开关、有时间轴的页顶栏不重复读数。`slice8` 42 项、`slice2` 29 项、六套 e2e 272 项全绿 | ④b C-3 检测升级与落盘（切片 ⑨ 待排期） |
| 2026-09-12 | **排期拍板**：用户问「C-3 与切片 ⑨ 具体指什么、先做哪个」，采纳建议——主链到能量检测就断了（检出只计数、不落盘，结果页「检测识别」页签为空），C-4 / C-5 / V-8 都压在 `detections.jsonl` 上，切片 ⑨ 无下游依赖且 V-5 要动引擎子命令。**定：C-3 → C-4 → C-5（④b 收口）→ 切片 ⑨；V-4 半天量可随时插入** | **C-3 检测升级与落盘**（§9G；10 报告 §4.2；黄金基准 `energy_detector.json` 不得变） |
| 2026-09-12 | **C-3 完成（D-063）**：`EnergyDetector.noise_mode = sliding`（带删截的滑动中位数，典型链路模板固定、按站绑定）、突发分段、`Detection` 自描述、`on_detection / on_detection_summary`、`detections.jsonl` 逐帧 + `detections.index.json` 摘要、`detection` 事件按段首帧、端点抽稀键 `node_id` / 过滤 `hit` / 索引路由、结果页「检测识别」突发列表与信号页「叠加检测」。probe 基准逐字节不变、滑动黄金基准 4000 帧逐帧对拍；缺省链从 3.015 s 起持续检出。**DS-6 分半标定复跑是发现不是达标**：比值仍 7.3 / 8.3，失配在尾部的真实干扰突发而非噪声底。六个提交；引擎 217 + 9、服务 126、web 207、六套 e2e 286 项全绿 | **C-4 特征与识别**（§9G；10 报告 §4.3–§4.4；`rc_hopping` 一类等 G-6）|
| 2026-09-14 | **C-4 完成（D-066）**：`FeatureExtractor`（两套谱——矩形帧电量与检测器逐位同源、Hann 窗形状量；去噪过闸；按 `frame_index` 对齐检测器，块不连续即报错）、`TemplateClassifier`（EM-S-04 E2 区间加权匹配 + 开放集，库 v1 取值 `assumed`）、`features.jsonl` / `recognitions.jsonl`（每突发一行、自带 trace）、`feature` / `recognition` 事件、装载器跨节点核对（nfft / merge_gap / sliding）与 `library_version → library_path` 注入、`cuav_run --library-root`；检测器改为消费即产出（空列表也产出，堵住双输入下游静默丢块）；框图页「检测识别评价」一张卡三个环节（`SlotDef.group`）、特征参数由检测器派生；结果页突发表接标签 / 后验 / 结论；`features.py` / `classify.py` 与两份黄金基准；引擎级验收三类合成波形 56 段判对 55（0.982，原型阶段验证值）。两处与 10 报告不同（平坦度区间、接受门限 0.6 → 0.5）记 10 §11 与模型卡。顺带修掉「与服务的连接已断开」误报。引擎 234 项 doctest + 11 项 ctest、服务 126、web 214、六套 e2e 14 + 39 + 29 + 108 + 67 + 42 = 299 项全绿 | **C-5 真值与评价**（§9G；10 报告 §4.5；前置契约：消费即产出、`Evaluator` 绑站吃 det + rec；`metrics_summary` 进 `task.json`） |

| 2026-09-14 | **C-5 完成（D-067）**：`Evaluator`（绑站；真值 = 场景参数帧的 `tx_on` 连续段 ∩ 波形门控 ∩ 检测频段 / 回放清单类别 / 无；帧级 Pd / Pfa / F1、突发级检出与发现时延、ROC 门限扫描不重跑、识别混淆矩阵；分母为零一律 null）；调度器加 `accepts_partial_inputs()`（尾块到得了双输入节点，既有产品 20 个文件逐字节不变）；运行器落 `truth.jsonl` 并拼 `metrics.json`（`sites[]` 按站分节，无时间戳）；服务端 `task.json.metrics_summary`；框图页评价子环节（真值来源 / nfft / data_id 派生）、结果页右栏评价卡；`evaluate.py` 逐值对拍与黄金基准；**跨层一致性算例 ① 典型链路实例首跑即过**：突发源 −9 dB 带内信噪比，解析 Pd 0.7164 对蒙特卡洛 0.7026，差 0.0138 ≤ 0.05（`tests/regression/crosslayer_pd_chain.py`，接进 `build-all.sh`）。demo-01 缺省链：Pd 0.9952、Pfa 0.00068、发现时延 14.7 ms（源按块起点门控开关）、识别准确率 1。目录 21 → 22 组件，3×3 夹具 58 / 108 与 61 / 120。引擎 249 项 doctest + 13 项 ctest、服务 127、web 216、六套 e2e 14 + 39 + 29 + 117 + 71 + 42 = 312 项全绿。模型卡 `models/evaluation/README.md`；十项修正记 10 §11 | **C-9 结果页两页签**（检测识别时间线三行、评价页签的混淆矩阵与 ROC，随 V-8 标签条）→ 切片 ⑨（V-4 方位图、V-5 精度场）；C-8 `demo-02` 与 G-6 随后 |

| 2026-09-15 | **C-9 完成（D-068）**：结果页四个页签就位——「检测识别」= 时间线三行（真值 / 检测突发 / 识别标签，自带轴、只画焦点站，点条带即 seek）+ 检测列表（突发 / 帧两种粒度，帧粒度只画游标附近 400 行并写明共多少行）+ 识别列表（带 Top-3）；「评价」= 五张指标卡（带四态徽标，发现时延按量级选单位）+ 突发级 + ROC（**对数横轴**，工作点标出，`?dev=1` 叠**解析曲线族**）+ 混淆矩阵热表与 per_class 表。新 `truthStore` 与 `getTruth()`（端点早就有、前端一直没封装，所以真值那一行从未画出）。`analytic.ts` 把 D-026 两式与正则化上不完全伽马移植到 TS，对 Python 参考守新黄金基准 `tests/golden/analytic-pd.json`（135 点 rel ≤ 1e-9）。引擎只动一处：`DetectionSummary.m_bins` 落进 `detections.index.json`（解析族要 M，读端复算会差一个 bin）。V-8 前半随之做掉。**两处照实说而不是编数**：`pfa` 整列为 null 时（真值覆盖全程，demo-03 三站与回放）ROC 只画轴 + 一行「Pfa —（tn + fp = 0）」；帧表写明共多少行。**三处实测纠正了计划的假设**：解析族的档位必须随 M 走（固定档在 M = 921 上整族贴 Pd = 1）、且不能以检测边沿对称（中心那档 Pd 只有 0.017）；ROC 线性横轴读不出东西；瀑布上任何单色都不可能对整条 viridis 够 3:1，边界得靠白 + 墨两道。引擎 249 项 doctest + 13 项 ctest、服务 127、web **228**、六套 e2e 14 + 39 + 29 + **130** + **75** + 42 = **329** 项全绿 | **C-8 `demo-02` 与 G-6 活动时间线**（§9G C-8、§9C G-6）→ 切片 ⑨（V-4 站点方位图、V-5 精度场）；C-10 DSP 厚化等 M-2 / M-3 |
| 2026-09-15 | **C-8 与 G-6 完成（D-069）**：活动时间线从「按块起点取值」改成**按样点施加** —— 新增 `geo::ActivitySchedule`（样点域，与帧域的 `EmitterRuntime` **并存**，故两份航迹黄金基准逐点不变），`SceneEmitterSource` 按「同频同开关」的子段推进、`Evaluator` 的真值按同一份时间表切段，「源怎么发的」与「真值怎么记的」逐位同源；铁律 4 的闸覆盖跳频点（`cross_check` / `configure` / 前端 `plan.ts`）；`noise` 加 4 阶巴特沃斯带限与频率搬移（此前既不带限也不搬移，`center_Hz` 与 `offset_Hz` 对它完全不起作用）；新场景 `demo-02`（10 MS/s / 8 MHz，图传 2 MHz 带限噪声 + 五点跳频遥控，时序先错开末尾叠一段）；场景编辑器可建可改 hop。**`rc_hopping` 第一次由真实链路走通：180 行、识别准确率 1.0，C-4 欠的验收销项**；**跳频突发起点对活动时间线偏差 0.0 ns**（线一帧 102.4 µs）。基准变更单独一步收口：demo-01 的 `fn` 7 → 0、Pd 0.9952 → 1.0、发现时延 14.7 ms → 0（块门控的假象，不是物理）。顺带纠正一个自己定错的判据：带限降级原按「带边衰减 < 40 dB」，实测带边 39 dB 对应的绕折功率只有 2.4e-5，改成对绕折功率积分。十一个提交；引擎 249 → 264 项 doctest、web 216 → 230、server 127、**七套 e2e 14 + 39 + 29 + 130 + 75 + 42 + 18 = 347 项**全绿 | **切片 ④c「DSP 厚化」：M-2 首条 Coder 链路 = DDC**（用户 2026-09-15 定；这也是 §9E 正典切片顺序里 ④b 之后的下一片）→ M-3 信道化与接收滤波 → C-10 进槽位。理由：`DDC` / `Channelizer` 至今不在组件目录里、框图页两个槽位恒 `unavailable`，**S2 / S3 / S4 三个观测点读数因此完全相同**，而 CLAUDE.md 开篇把本系统定义为「以 DDC 后、信道化前宽带复基带 IQ 为主产品」；demo-02 的 10 MS/s 宽带正是它的用武之地；M-0 / M-1 已完成，且 D-036 定的「DSP 件走 MATLAB Coder」这条路线至今一件产物都没有。切片 ⑨（V-4 站点方位图、V-5 精度场）与 L-9 定位评价随后 |
| 2026-09-16 | **M-2 完成（D-070）**：`DDC` 落地——**手写 C++ 而不是 MATLAB Coder**。规划期实跑 codegen 发现开发机是学术许可、产物带「不得用于政府 / 商业 / 组织用途」，我据此先判放弃 Coder，用户当日澄清「本项目属学术用途，学术版许可没有问题」后改回：`DDC` 保持手写（工程取舍），**Coder 路线保留给 M-3 并由它兑现 P1-8**（D-070 ①②）。系数表走 scipy 等波纹设计冻结成 `models/adc-ddc/fir_lp_v1.json`（八档、抽头 ≈ 27·D、阻带 ≈ 62 dB），MATLAB 的 `firpm` 只作独立校验（吻合 1.5e-14）。**与 Python 参考四个算例最坏相对差 0（逐位相同）**；阻带实测 91.1 dB、冲激对齐偏差 0 样点。08 §8 四条口径全部回填：口径一改写成逐样点关系、口径二结案（§15 暂定项 ② 销项）。`ddc` 槽位缺省旁路，**产品 / 航迹 / 检测 / 评价基准与 `DEFAULT_CHAIN_TEXT` 零变化**，唯一基准变更是组件目录 22 → 23。三处实测发现：系数带边差一个 2（靠看 S4 的谱才发现，自检查不出来）、跳频真值边界的落差随跳数**累积**、`decim = 1` 不能套奈奎斯特那道闸。引擎 270 项 doctest + 14 项 ctest、web 232、七套 e2e 349 项全绿 | **M-3 信道化与接收滤波（走 MATLAB Coder，兑现 P1-8）**，封装层沿用 M-2 的 DSP 原语与三方互证做法 → C-10 DSP 厚化接入（④c 收口）；之后切片 ④d 或 ⑨（V-4 站点方位图、V-5 精度场）|
| 2026-09-16 | **M-3 完成（D-071）：首条 MATLAB Coder 链路走通，P1-8 销项**。`.m` → `codegen` → 入库 C（25 + 11 个文件）→ 静态库 `cuav_coder` → 组件 `Channelizer` / `RxFilter`。抽头数的两难（08 §8 口径二要奇数、多相要 M 的倍数）用 `N = M·T+1` 且 T 取偶数解掉，顺带让逐信道常数相位恒为 1、不需要任何修正。**判据分两个尺度**：算法核 double 进 double 出判 1e-9（实测对 MATLAB **1.4e-16**、对 Python **5.7e-13**），组件存 complex64 判 1e-6（实测**逐位相同**）——06 §9D 的「rel ≤ 1e-9」落在算法核上，拿组件输出去套是套不上的。标准算例 5（通带 0.148 dB、阻带 61.13 dB、冲激偏差 0 样点）与 8（中心增益 0.00 dB、邻道抑制 63.64 dB、冲激偏差 0 样点）通过。`chan` 槽位放开、缺省仍旁路，演示夹具 `chain-demo-02-chan.json`（10 MS/s 切 4 条、取零频那一路）**把图传与跳频两个辐射源分开**；通带内 S3 对 S5 逐带一致到 **0.1 dB**。**唯一基准变更是组件目录 23 → 25（纯插入）**，50 个产品文件、三份航迹基准、`DEFAULT_CHAIN_TEXT` 与五份夹具拿 M-3 前那一版另建工作树比对**逐字节相同**。四件 codegen 的坑记进 08 §13.1。引擎 **291** 项 doctest + **15** 项 ctest、web 232、server 127、七套 e2e **351** 项全绿 | **C-10 DSP 厚化接入**（`DDC` 与 `Channelizer` 进缺省链、`RxFilter` 串进前端子槽位、频率计划按 `fir_version` 对齐过渡带，④c 收口）→ 之后切片 ④d 或 ⑨（V-4 站点方位图、V-5 精度场）|
| 2026-09-17 | **C-10 完成（D-072）：DSP 厚化接入，切片 ④c 收口**。`RxFilter` 串进框图页（`rx_flt` 子槽位挂在前端卡片里、链上排在前端之前，缺省旁路，通带由场景带出）；频率计划新增 `firSpecs.ts`（冻结抽头表规格在浏览器一侧的副本，有对拍闸）与两条检查项，过渡带的 0.4 由写死改为按 `fir_version` 查表。**修掉两处同时开两级 DSP 才暴露的错**：S4 兜底是无条件覆盖、DDC 与信道化同开时主产品被挪到 S5 那一点上；编译只写 `channels` 不写 `select_channel`，改了子信道数就带着越界的路号出去。新夹具 `chain-demo-02-dsp.json` 三级全开（10 → 5 → 2.5 MS/s），标准算例 5 / 7 / 8 **从保存的典型链路**跑通。缺省链**不接**这三级（零基准变更）。引擎 291 doctest + **16** ctest、web **245**、七套 e2e **355** 项全绿 | **C-11 收口**（三种模式的回归夹具、标准算例 1/2/3/6/9/10/11 从典型链路生成、06 / CLAUDE 回填，④d）→ 之后切片 ⑨（V-4 站点方位图、V-5 精度场）或 L-9 定位评价 |
| 2026-09-17 | **C-11 完成（D-073）：04 §15.2 标准算例从保存的典型链路跑通，切片 ④d 的验收面收口**。三份模式夹具（全合成 / 实测回放 / 混合增强）+ `tests/regression/standard_cases.py`（接进 `build-all.sh`）。十二项标准算例完成十项，4（同频邻频叠加）与 12（用户插件）随后续。电平链从保存的链路上量全了：**S0 = 27.0000 dBm、S1 对链路预算 +0.081 dB、S2 − S1 = 20.000 dB**。混合增强要新场景（80 MS/s 短窗）才对得上实测背景的采样率，**三处测法是实测逼出来的**：缺省链本来就削顶所以期望是 degraded 不是 valid；量化抬升要从量化器自己的步长算（实测 +1.229 对解析 +1.194 dB）；混合的目标电平要逐行相减取**中位**不能取均值（真实背景的 WiFi / 蓝牙突发让均值是中位的 87 倍）。性能记录：demo-02 20 s @ 10 MS/s、DDC 启用 = 墙钟 30.86 s、实时因子 **0.648**（未触发多线程评估）。引擎 291 doctest + **17** ctest、web **246**、标准算例 **18** 项全过 | **切片 ④d 余项**（P1-7 首批标准算例入回归、P1-9a、U-4 任务列表与数据中心、规范定稿）→ 之后切片 ⑨（V-4 站点方位图、V-5 精度场）或 L-9 定位评价 |
| 2026-09-17 | **D3-0 完成（D-074）：07 号报告写出，两项从 2026-09-02 挂到今天的 OPEN 拍死**。用户拍板：**遮挡计算落点 = 浏览器 TS 复算并与 C++ golden 对拍**（实施方推荐的「引擎算、浏览器只显示」未采纳，选项与代价留档在 07 §2.1）；**坐标基座 = vendored GeographicLib**（D-035 回收，自写闭式保留作独立第二实现与对拍件）。报告另定了接线口径：遮挡直接写在 `frame(k)` 里（它是几何的纯函数，满足 `frame()` 的硬不变量，不像统计阴影要预算）、`line_of_sight = !blocked`、损耗只进 `extra_loss_dB` 且单程 ×1、`included_loss_terms` 增 `diffraction`、**补第三第四道闸**（E3 与统计阴影 / 城市经验互斥，都是同源双计）、legacy 投影与 `fresnelV` 的 3e8 照 D-009 留在 `geo::legacy`、建筑数据通路与八条声明过的简化。**四件事是这次调研跑真实数据量出来的**：① 性能不是约束——47546 栋真实建筑上单次射线 0.6–6.7 µs、整任务预算 ≈ 单核 1.7%；② 非视距这条显示链路早就通到界面了，红色 `#b91c1c` 在 D2-4 标定过却一次没画出来过；③ **换基座是一次真正的基准变更**——扰动相对 1e-13，而航迹基准按 double 往返精度打印，三份必重冻，产品 `.f32` 要字节比对去证；④ E3 一开，站点 30 m 时 76–100% 判非视距、demo-01 末端带内信噪比 33.8 → **1.4 dB**，正是切片 ⑤ 要的可见效果 | **D3-1 vendored GeographicLib**（需一次联网取源码，记版本号与 sha256）→ D3-2 换基座的基准变更（单独提交）→ D3-3 移植遮挡三件 → … → D4。与它并行可做的小步：④d 余项里 P1-7 / P1-9a 只差销项，L-9 定位评价是半步 |
| 2026-09-17 | **D3-1、D3-2 完成（D-074）：坐标基座换成 vendored GeographicLib 2.5.2，D-035 回收**。取 2.5.2 不是最新的 2.7——上游自 2.6 起要 C++17 而本项目是 C++14，2.5.2 是最后一个只要 C++14 的版本（提标准是另一项未决事项，不能为编过一个第三方件顺手提）。只取 `Geocentric` + `LocalCartesian` 及最小闭包七个文件 78105 字节（对照 foundation 那条路要 153 个文件 1886299 字节、其中只有 16 个与坐标有关）。两处多想了一步：`GEOGRAPHICLIB_HAVE_LONG_DOUBLE` 取 0 堵掉 MSVC 64 位 / clang 80 位的跨平台口子；站心旋转自己写不经 `LocalCartesian`，而「各家一样」这句话**在冒烟测试里对上游验实了**（9.3e-10 m），不留成注释里的声称。**D3-2 换基座推翻了 07 报告 §3.4 的两处预测**：产品 `.f32` 不是「大概率不变」（根因是纯单音谱的数值相消底，峰下 20 dB 以内其实只差 7.9e-4 dB），判据「逐点相对差 ≤ 1e-12」超差（顶到的是航向角，判据拿 foundation 替身定的、且对角度用相对差本就不对）——**改成按量的物理判据**，不是放宽原判据。**下游检测 / 识别 / 评价结论一个未变**，跨层算例 ① 仍 0.0138。引擎 293 doctest + 17 ctest、web 246、七套 e2e 355 项全绿 | **D3-3 移植遮挡三件**（`IMapQuery` / `LocalSceneAdapter` / `segment_occlusion` + `fresnelV` / `knifeEdgeLoss_dB` → `geo/`，接口改吃平面米、旧常数进 `geo::legacy`），收口条件是 148 例 golden `rel ≤ 1e-9` 与刀口 v=0 → 6.03 dB |
| 2026-09-17 | **D3-3 完成（D-074）：遮挡三件移植进 `geo/`，148 例黄金基准全过**（29 例判非视距），`fresnel_v` 108 例、`knife_edge_loss_dB` 14 例同样 `rel ≤ 1e-9`，掠射锚点 **6.0329 dB**。两处有意偏离 emcore：接口改吃平面米（投影挪到调用方）、命名改蛇形，数值一字未改。常数守卫为 `occlusion.cpp` 的两个符号开写明理由的窄例外，**并实测验证过别处仍拦得住**。**顺带逮到 07 报告写错的量级**（两个光速常数在刀口损耗上差 3e-3 dB 不是 1e-5 dB，根因是把相对差 6.9e-4 看成 6.9e-7，§14.3 修正）——因为把量级写成了**断言**而不是注释。**收尾**（用户问「两套旧常数要不要解决」后）：核实两套投影生产路径一把都不用，答不需要且不应该统一（统一 = 重新生成基准 = 抹掉基准独立于我方代码这个唯一价值）；但把两套并到 `cuav_geo/legacy_frames.h` 一个文件、按来源改名、合掉两个同形异名的结构体，并把「不许统一」从注释变成断言（钉住两套常数、必须不同、偏离方向相反 +0.257% / −0.445%）。**遮挡尚未接进链路预算**（`line_of_sight` 仍恒真）。引擎 **298** doctest + 17 ctest、标准算例 20 项全绿，黄金基准零改动 | **D3-4 建筑数据进引擎**：`engine/src/buildings_json.cpp` 解析 `buildings.geojson`、核对清单 sha256、`MultiPolygon` 36 件按外环拆、**懒加载**（只有 `prop_level == E3` 才读）。收口：47546 栋加载 < 200 ms，`--validate` 仍约 9 ms |
| 2026-09-18 | **D3-4 / D3-5 / D3-6 完成（D-074）：建筑几何进引擎、遮挡接进链路预算、浏览器侧复算落地**。`buildings_json.cpp` + `geo::SceneFrame` + 进程内共享缓存，懒加载只在 E3、`--validate` 中位 3.1 ms 不读一个字节；`line_of_sight = !blocked`（与损耗大小无关）、刀口损耗单程 ×1 只进 `extra_loss_dB`、`included_loss_terms` 增 `diffraction`、补闸三闸四（E3 与统计阴影 / 城市经验互斥）；浏览器侧 `web/src/scene/occlusion/` 五件自 em-demo 祖本回迁。**报告又被实测推翻三处**（栋数 47546 → 47662、加载过了 200 ms 的线且那条线漏算了读盘与核哈希、加载点按 §7.4 放在 `init()`）；**核实到 148 例黄金基准不够用**（12 栋合成楼加 legacy 投影，测不到解析规则、严格站心平面帧与四万多栋楼的桶网格），补 `occlusion-aoi.json` 真实建筑集五条射线两侧同守（最差 1.3e-12）。E1 / E2 零回归：134 + 24 个产品文件、三份航迹基准、组件目录基准全部逐字节不变 | D3-7 界面：E3 去置灰、点选探测、链路线第一次变红 |
| 2026-09-18 | **D3-7 完成（D-074 ⑰）：界面放开 E3，「点选建筑两侧一致」变成一次点击**。`LEVEL_UNAVAILABLE` 整个删掉、`propConflict()` 撤掉拦 E3 那条并补闸三闸四、`propView().terms` 增 `diffraction`、频率计划新增第 14 项「E3 有观测区域建筑几何」；场景页工具条加「视距」探测（站取焦点站、频率取焦点目标、**假设高度改过即固定住**——不固定的话楼两侧那两次不是同一把尺子）；`slice2` 跑一条 3 秒的 E3 链把链路线第一次画成红的。**顺带改掉 D3-5 欠的一句话**：`prop_level` 的参数描述还写着「待 D3，本版本收到即报错」，而它就是下拉的悬停提示——改代码不改面向用户的那句话，这条线上第二次。实测楼 `35185865451587`（62 m、143 m 外）楼前视距、楼后 37.1 dB | D3-8 双平台构建 |
| 2026-09-18 | **D3-8 完成（D-074 ⑱）：双平台构建走 GitHub Actions，本项目首次在开发机之外编译 C++ 侧**。铁律 16 欠了十六天。落点选 CI 不选真机：仓库公开、构建机免费，而 C++ 测试要的 29 份黄金基准、三个示例场景、清单与框图夹具全在 git 里（12 MB），只有 15.9 MB 建筑集不在、相关用例自己说跳过。**两平台全绿**，各 311 doctest + 17 ctest，组件目录逐字节相同。**九轮收敛，七个真问题**：git 在 Windows 上把 LF 换成 CRLF（57 个失败断言里 50 个，而本项目整套溯源建立在字节哈希上，**任何人在 Windows 上克隆都对不上**）、`.gitignore` 的 `ref/` 吞掉 `matlab/ref/`（8 个来源 `.m` 从未入库）、两个缺失标准头（**两个编译器各抓一个不同的**）、五份各抄一遍的 `temp_dir()`、`test_buildings` 判跳过判错文件、**坐标基座判据 `1e-9 m` 恰好是一个最低位**（第三次「换形状不是放宽」）、引擎自己在 stdout 上输出 CRLF。另立两条 CI 纪律：job 与慢步骤都要设超时（GitHub 在步骤结束前不发布日志）、测试要有逐条超时加带名字的等待。**D3 至此全线完成，切片 ⑤ 只剩 D4 同源验收** | D4 渲染—物理同源验收；④d 余项（U-4 任务列表与数据中心）|
| 2026-09-18 | **两处按用户实测反馈改的界面**：① 视距探测**放下工具就收走**（原先只有卡片上一个「清除」按钮，得手动点）——它是一次当场的推演不是场景对象，与「测量」同一条口径；② **传播档位下拉挪到卡片上**——右栏只有点中那张卡才出现，档位放那儿等于不点就看不出还有档位可调（用户原话「我怎么看不到遮挡的效应」），**这是对 D-058 ① 的一半修订**，记在决策条目、12 号报告开头与 07 §14quinquies，不悄悄改掉当初的拍板。卡片那行随之不再重复写档位前缀（D-062「一个事实屏上只出现一次」）。编译出来的框图逐字节不变；七套 e2e 369 项全绿 | D4 同源验收；或 ④d 的 U-4（结果页「任务」页签与数据中心两处占位是界面上仅剩的「未完成」痕迹）|
| 2026-09-19 | **U-4 完成（D-075）：界面上最后两处占位填上**。结果页「任务」页签 = 服务端分页的一页任务（盘上 941 个）+ 两正交徽标 + 评价摘要 + 采用 / 取消；数据中心 = 三栏，左栏分面**在全量上算**（在列出的那几条上算会说每个机型都只有 12 条）、中栏列表、右栏两档详情。服务端三处只读改动：`/tasks` 加 `offset` 与 `total`、`/datasets` 加四个过滤参数与 `facets`、新增 `/datasets/{data_id}`（api-versions §3.3 里最后一个未实现端点，本表就此清空）。**详情是一键一键挑的白名单**——它读的两份文件里合法地带着 `producer` / `origin.source_file` / `power.reason` / `calibration.table` / `truth.original_name` 这些仓库路径与外部来源，铁律 17 允许留档但不许转发；夹具故意把它们都造进去，不造的话那条「响应里没有路径」的断言是空过的（把原文塞回去验过它会红）。用户拍板三条：四态只显徽标、原因进 `?dev=1`；「用于回放」一键送到框图页；不做「用同一种子重跑」。顺带补掉**混合增强在界面上选不了背景片段**（`plan.ts` 早就在检查它，就是没处填，那条链只能靠夹具跑），并在提交前把 AddMixer 的采样率 / 中心频率相容性说出来。**另修一处 e2e 的隐含依赖**：slice4 的物理判据全按 demo-01 算，而应用启动会采用盘上最近的任务并跟着换场景——手工跑过一次 demo-02 之后它就在 S1 上差 9 dB。导航一律带 `?scenario=demo-01`（这个参数本来就是为此留的）。零黄金基准变更；server 127 → 134、web 263 → 278、`slice4-smoke` 138 → 153 | D4 渲染—物理同源验收；或 ④d 余项（P1-7、P1-9a、规范定稿）|
| 2026-09-19 | **D4 完成（D-076）：渲染—物理同源验收，切片 ⑤ 全线收口**。同源从「按结构成立」变成「在跑起来的画面上核过」：画面 2178 栋建筑在遮挡侧全部找得到、id / `height_m` / `base_m` 差异 0 处，两侧地址逐字相同且各取一次。**核出一处真问题**：渲染侧缺高度兜底 `?? 8`、物理侧同情形剔除——屏幕上立着一栋视线穿得过去的楼，兜底改 0。「瓦片层永不进遮挡」做成 `build-all.sh` 的结构守卫。两条与被测代码无关的：比了零个也算通过（改成 `rendered > 0` 才可能过）、无头 Chrome 约每十秒丢一次 WebGL 上下文（断言要说「第几次、从什么变成什么」，否则查三轮） | 下一步待定 |
| 2026-09-19 | **用户实测两处修（无新决策）**：① 场景页布目标时首个航点的 `speed_mps` 写的是 0，而 schema 与引擎都要求为正——**只要布一个目标这份场景就存不下去了**；后续航点「沿用上一个的速度」又用 `typeof x === 'number'` 判（对 0 为真），把这个 0 传遍整条航线。改成一处定义的 `DEFAULT_SPEED_MPS = 15`，且**非正的不算可沿用的值**。这条路径此前零覆盖——`slice2` 布的是站，没有一条用例走过「布目标 + 画航点 + 保存」。② 同一句提示叠四条（失败提示是 sticky 的，重试几次叠几条），改成同一句话在屏上就不再叠。web 286 → 289 | 下一步待定 |
| 2026-09-19 | **用户实测第二轮：画航点会和别的目标画串（无新决策）**。四个症状一个根子——**航点要素身上不带「我属于谁」**，于是画航点、点选航点、拖动航点、显示哪条航线四处各拿「当前选中的是谁」猜了一遍，而「没选中辐射源就落到第一个目标」是静默的。改法：航点工具**进来时锁定目标**（`editor.routeFor`）并让选择跟着它；航线显示改用**既有的**焦点规则 `focusTargetId`（D-062 一处定义）——根子是同一条规则被三处各算一遍，所以修法是减少一处定义而不是多加一处判断；航点要素带 `emitter_id`，点选与拖动从要素读，猜的那个函数删掉；工具条写出「正在给 uav-3 画航点」（**串不串是一回事，看不看得出来是另一回事**）。**顺带暴露一个未修的隐患**：在场景页随手编辑就会把黄金基准指向的入库场景永久改掉（自动保存无确认，demo-02 被五处基准钉着），要动 D-054 的自动保存口径，单独定。web 289 → 291 | 待定：示例场景只读 + 另存为？|
| 2026-09-19 | **示例场景改名 `golden-01/02/03` 并设为只读（D-077）**。用户拍板：基准场景不能动，基于它的修改一律另存为。**三份都是基准**（每份各有航迹黄金基准，demo-01 另有 `DEFAULT_CHAIN_TEXT`），所以三份一起改、一起保护。改名是真正的基准变更（`scenario_id` 在文件里，改名即改字节），**先证明物理没变**：航迹基准样点逐字段相同、产品 11 个文件逐字节相同。只读的真闸在服务端（PUT 409），界面只是提示；判据是**名字**不是名单（名单会漏登记）。框图页改设备参数改的也是场景，因此同样只读，且必须说出来不能默默不存。`slice2` 改成在另存的工作副本上编辑，基准文件全程不被碰。**一处覆盖回退**：可写场景的自动保存目前只手工验过。七套 e2e 393 项全过 | 下一步待定 |

### 0.4 不需要用户输入即可开工的三件事

1. §2 工程骨架（半天）。
2. D0 全流程，用 em-demo 的西安 bbox 作**临时 AOI** 验证管线，产出可与 em-demo `buildings.geojson` 对拍的建筑数据（真实 AOI 冻结后换参数重跑）。
3. P1-0 引擎设计报告与 P1-1 IQ 格式规范草案。
4. 切片 ①（§9E）全部步骤，以及 M-0、M-1、M-4（2026-09-04 增）。

### 0.5 需要用户提供的输入

| 输入 | 阻塞哪一步 | 状态 |
|---|---|---|
| 正式 AOI（bbox 或多边形） | D0 正式出包；D1 验收截图 | **已定**：北京亚运村周围 `116.288,39.900,116.522,40.080`，19.96 × 19.97 km。中心用户 2026-09-03 指定（D-021），范围用户 2026-09-04 拍板（D-024）。若阶段 0 另有正式区域，改 `scene/aoi/*.json` 后 `--force` 重跑 |
| 首批频段、带宽、无人机型号 | P1-3 辐射源模板参数；P3 模型卡 | 待定 |
| 实测 IQ 数据 + 8 项摸底回答（§11.1） | 阶段 0 退出；P1-9b 回放；P3 全部 | **公开数据集已提供两个，甲方自采数据仍未提供**：2026-09-03 收到 DroneRFb-DIR（§11.4）与 DroneRFa（§11.5），八项已在这两个数据集上逐项回答（§11.1 两张答案表）。DroneRFa 补上了 P3 的 B 组多距离场景（粗档，实测 n = 1.90）。**仍缺且只能由甲方补的**：绝对功率标定（缺馈线损耗与一次标定常数，dBm 类验证一律做不了）、逐米路损曲线（需精确 GPS 距离）、视距/非视距标签与建筑几何（跨层算例 ② ③ 因此不可做）。**是否另有甲方自采数据或补采计划，待用户确认** |
| 交付平台基线：Windows 版本、浏览器、MSVC、Node LTS、Windows 验证机 | D3-4 MSVC 构建；P2 单机包 | 待定 |
| 04 附录 D / 05 附录 C 引用的 5 份缺失文档、主界面交互原型 html、01/02 三张架构图 | 阶段 0 退出（阻塞） | 待索取 |
| 遮挡落点的拍板（坐标基座由 G-1 临时件先顶，D3-0 回收，D-035） | D3-0 | 设计报告 07 时。框图画布已于 2026-09-04 拍板 React Flow（D-032） |
| 两个公开数据集的许可条款（能否随交付系统分发） | §11.4 DS-1 与 §11.5 DA-1，进而影响它们能否进入交付物 | **已确认（用户 2026-09-05）：两个数据集没有任何使用约束与署名约束**，可随交付系统分发。两者均托管于 SciDB（DroneRFb-DIR 编号 `84cf9101e739402784b1396783881202`，DroneRFa 编号 `34f0a91e8a544904998b8fdc44477380`），页面动态渲染，实施方未能自动取到条款原文，以用户确认为准；数据中心页仍按惯例显示数据集来源 |

**防线一：不得因为手上有了公开数据集就放松索取甲方数据。** 2026-09-03 入手的两个公开数据集
（§11.4、§11.5）覆盖不到甲方的目标机型、频段与接收机参数。DroneRFa 补上了距离维度与部分
标定量（接收增益 50 分贝、天线 3 dBi，均引自论文），使 P3 的四组标定数据从「一组都不满足」
变成「A、B、C 三组粗档可用，D 组须自行划分」——但**绝对功率标定与几何信息两项仍然完全空缺**，
这两项正是模型校准从相对量走向绝对量的必要条件。**阶段 0 的退出条件不因它们放宽一个字。**

---

## 1. 技术路线摘要

### 1.1 架构分层

```
浏览器 web/         TS + React 19 + Vite；MapLibre GL 5.24 + pmtiles 4.4（vendored）
  三视图：场景 / 框图 / 结果 + 顶部试验上下文
        │ REST（控制）+ WebSocket（状态、日志、按视窗抽取的展示数据；事件带序号可补取）
应用服务 server/    Node/TS；权限、项目、模型目录、任务、数据索引、审计；HTTP Range 静态服务
        │ 粗粒度任务接口（提交完整试验 / 查状态 / 取结果索引）
运行引擎 engine/    C++14 分块流 + DAG 调度；同进程 / 共享内存；种子与溯源
  ├── models/       辐射源 / 信道 / 天线 / 接收机 / ADC-DDC / 信道化（含 MATLAB Coder 产物）
  ├── geo/          IMapQuery + LocalSceneAdapter + 刀口衍射（自 emcore 移植）→ SceneParamFrame 10–100 Hz
  └── algos/        PSD / 瀑布 / 能量检测 / 特征 / 评价基线 / 用户插件接口
数据 data/          SQLite 单机 / PostgreSQL 内网；IQ 二进制 + SigMF 兼容元数据；场景数据包；黄金基准
```

### 1.2 数据流与端口

| 流 | 端口类型 | 速率 | 说明 |
|---|---|---|---|
| IQ 主链 | `IQStream` | 采样率（5–50 MS/s 常用，100 MS/s 短窗） | S0 辐射源 → S1 天线端 → S2 前端 → S3 ADC → **S4 主产品** → S5 信道化 → S6 结果 |
| 慢变参数 | `SceneParamFrame`、`ChannelPathSet` | 10–100 Hz | 链路几何、LOS/NLOS、损耗分量、噪声底、天线复增益、扫描窗口、多径路径；只经"施加"类 M3 组件进入 IQ |
| 观测量归约 | `SpectrumFrame`、`DetectionList`、`FeatureVector`、`RecognitionList`、`BearingReport`、`ToaReport`、`PositionReport` | 事件/帧 | IQ 观测量提取 → M2 裁决 → 蒙特卡洛 → M1 参数。后三种自 D-053 起有生产者（M2 效应级：`DirectionFinder` / `ToaEstimator` / `MultiSiteLocator`）；`ToaReport` 是 05 名单外的补充 |
| 控制与真值 | `ControlEvent`、`GroundTruth` | 事件 | 真值只供评价，不向被测算法泄漏 |
| 后置预留 | `ArrayIQStream`、`MultiSiteIQSet`、`CalibrationSet`、`TrackReport`、`DeviceStatus` | — | 只做命名预留（`PositionReport` 自 D-053 起移入上一行）|

连线校验规则：`IQStream` 不得与参数流直连；M2 裁决组件不接受 `IQStream`。

### 1.3 三种运行模式与三个验收场景

全合成 / 实测回放 / 混合增强（04 §4.3）；对应验收场景：实测回放、全合成、混合增强（04 §21）。混合增强是首期投入产出比最高的能力，P3 主攻。

### 1.4 复用地图

| 来源 | 拷什么 | 到哪 | 改动 | 验证 |
|---|---|---|---|---|
| Airports `index.html` | `protomapsStyle()` 810–1092 行、`PM` 配色、hillshade 1218 行、`addBldg3d()` 1296 行、`newMap()` 1375 行、`localglyphs://` 协议 | `web/src/scene/` | 原生 JS 切成 TS 模块，**不改视觉参数** | 同 AOI 与 Airports 并排截图 |
| Airports `serve.py`、`fetch_tiles.py`、`fetch_levels.py`、`download_offline_maps.py` | Range 逻辑、AOI 裁切、DEM 下载 | `server/`（Range 用 Node 重写）、`scene/` | 参数化 AOI；DEM 加 bbox | `curl -r` 得 206 |
| Airports `tiles/planet.pmtiles`、`tiles/dem/` | 数据 | `data/scene/<aoi>/` | 只裁切、只拷贝 | `pmtiles show` |
| em-demo `src/components/{MapView,CoverageControl,SignalPanel}.tsx`、`src/models/coverage.ts`、`src/simulation/engine.ts` | 态势图形语义、覆盖场、iso-Pd、Canvas 叠加层、定频 tick | `web/src/scene/layers/` | 拆成图层模块；**色值在浅色底图重标定** | 逐图层截图 |
| em-demo `scripts/fetch-buildings.mjs` | 估高分档与 `src` 标记规则 | `scene/decode_buildings` | 数据源改为 z15 瓦片解码 | 与西安 Overpass GeoJSON 对拍 |
| emcore `include/emcore/{core,map,models/{occlusion,propagation}}`、`tests/golden/*.json`、`tests/golden_util.h` | 地图抽象、桶网格 DDA、刀口衍射、golden | `geo/` | 文件头注明来源；常数不动 | 148 例 rel ≤ 1e-9 + 解析锚点 |
| foundation `math/{UtEllipsoidalEarth,UtCoords,UtSphericalEarth,...}` | WGS-84 坐标基座 | `third_party/foundation/` | 补 `ModelApi.hpp` 与顶层 CMakeLists | 与 GeographicLib 或已知点对拍（D3-0 决定是否采用） |
| em-demo `桌面应用打包说明.md` | Windows 交付经验 | `deploy/README` | 摘录 | — |

---

## 2. 工程骨架

| # | 步骤 | 产物 | 状态 |
|---|---|---|---|
| S-1 | 按 CLAUDE.md 目录结构建目录（全部 ASCII 无空格） | `web/ server/ engine/ models/ geo/ algos/ scene/ data/ tests/ deploy/ docs/ third_party/ matlab/` | [x] 2026-09-03 |
| S-2 | `git init`；`.gitignore` 排除 `data/**`（保留 `*.manifest.json`、`*.md`）、`C-UAV Model Demo/**`、`node_modules`、`build`、`dist` | `.gitignore` | [x] 2026-09-03 |
| S-3 | `WORKLOG.md`：顶部状态快照 + 按日追加区 | `WORKLOG.md` | [x] 2026-09-03 |
| S-4 | `docs/` 空壳：`iq-format.md`、`api-versions.md`、`scene-package.md`、`display-route.md`、`decisions/`、`concept-model-layers.md`、`concept-model-extensions.md` | 各文件含标题与"待写"章节骨架 | [x] 2026-09-03 |
| S-5 | `tests/{unit,integration,e2e,golden,regression}/`；`tests/regression/README.md` 列 04 §15.2 十二项 + 跨层一致性四项 | 目录与清单 | [x] 2026-09-03 |
| S-6 | 工具链落位：`web/`、`server/` npm 工程（vendored 依赖，锁版本）；`engine/`、`geo/` CMake ≥ 3.16、C++14；`scene/` python + uv | 可空跑的 `npm run build`、`cmake --build` | [x] 2026-09-03 |
| S-7 | `scripts/check-ascii.sh`（铁律 15）、`scripts/build-all.sh`；CI 形态列 OPEN | 脚本 | [x] 2026-09-03 |

---

## 3. 阶段 0 需求冻结（04 §13.2）

用户主导，我准备材料。退出条件：范围与不做事项经确认；获得一批可解析实测 IQ 或补采计划；IQ 格式/功率/时间/真值口径形成；显示子线定位与 AOI 冻结；缺失引用文件已索取。

| 决策项 | 我准备的材料 | 状态 |
|---|---|---|
| AOI | **已定**（D-021 中心、D-024 范围）：北京亚运村周围 20 × 20 km。定范围时的实测材料：10 × 10 km 与 20 × 20 km 各解码一遍 z15 建筑层，面积 4.0 倍而建筑只 3.7 倍（13564 → 50201 栋去重后），底图体积不受影响 | [x] 2026-09-04 |
| 首批频段、带宽、机型 | 从 04 附录 B 整理成勾选表；概念模型 EM-T-03 模板字段对照 | [ ] |
| 实测数据 | §11.1 八项问题；摸底工具 `tools/iq_survey` **2026-09-03 已建成并在两个公开数据集上跑通**（§11.4 DS-5、§11.5 DA-5），甲方数据到货当天直接可用；八项问题已在两个公开数据集上逐项回答，甲方自采数据仍待提供 | [~] |
| 交付平台基线 | WebGL2 检测页；MSVC / Node LTS 候选版本表 | [ ] |
| 缺失文档索取 | 清单（见 §0.5） | [ ] |
| M3 概念模型编号前缀 | `EM-Q-xx` 候选与理由 | [ ] |

---

## 4. D0 场景数据包

输入：AOI 定义 `scene/aoi/<aoi>.json`；`data/basemap/planet.pmtiles`；`data/basemap/dem/`（2026-09-04 起由本项目自持，不再引用 Airports 工程路径，D-023）。输出（2026-09-03 按 D-022 修订）：共享资产 `data/basemap/{planet.pmtiles, planet.manifest.json, dem/, dem.manifest.json}`（整份全球文件，不按 AOI 拆分）+ 区域目录 `data/scene/<aoi>/{buildings.geojson, basemap-slice.pmtiles（可选，测试夹具）, basemap-slice.manifest.json, manifest.json, quality-report.md}`。

| # | 步骤 | 要点 | 状态 |
|---|---|---|---|
| D0-1 | 定 AOI | **完成**。中心：用户 2026-09-03 否决西安，指定北京亚运村周围（D-021），位置用从底图瓦片解出的地名（亚运村街道、安慧桥、奥林匹克公园、大屯、慧忠路）核实。范围：用户 2026-09-04 定为 20 × 20 km（D-024），`scene/aoi/beijing-yayuncun.json` 的 bbox 为 `116.288,39.900,116.522,40.080`，实测 19.96 × 19.97 km，中心 (116.405, 39.990)。原西安范围与 2026-09-03 暂定的 10 × 10 km 均作废 | [x] 2026-09-04 |
| D0-2 | 底图 | **2026-09-03 完成，形态有变（D-022）**：用户决定直接用整份 137 GB 全球 planet.pmtiles，不裁切。`scene/register_basemap.py` 登记为共享资产 `data/basemap/`（全文件 sha256、planetiler 0.10.2、OSM 2026-08-17、9 层）；`scene/fetch_tiles.py` 仍裁一份 AOI 切片 `basemap-slice.pmtiles`（六项自检通过，两次裁切 sha256 相同）只作测试夹具；2026-09-04 按 20 × 20 km 重裁为 749 块、11147792 字节、sha256 `60691abd…`。整份文件经 Range 服务实测返回 206 | [x] 2026-09-04 |
| D0-3 | 建筑解码合并 | **2026-09-04 完成**，脚本 `scene/decode_buildings.py`。按 D-025 的实测结论：跨瓦片按 id 合并（同 id 的碎片做并集还原，不是任选一片——任选会在瓦片边界留下假墙）；只保留 `kind=building`，剔除 `building_part`（子体量，重复计入）与 `address`（点要素）；瓦片 `height` 一律标 `src=tile:height` 不标 `osm:height`；无高度的按面积档估高标 `src=est:area`。产物 47582 栋、15919279 字节、sha256 `65ae4dce…`，`tile:height` 21.4% / `est:area` 78.6%，高度 q50 20 m、q90 38 m、最高 528 m。矢量瓦片的 protobuf 解析复用 `probe_buildings.py`，未引入 Node 或 Python 的瓦片解码依赖；shapely 只在建库阶段用 | [x] 2026-09-04 |
| D0-4 | 管线对拍 | **2026-09-04 完成**，脚本 `scene/fetch_osm_buildings.py`（唯一联网步骤）+ `scene/quality_report.py`。拉到 47572 个原始要素（34.4 MB）。按 `(类型, 编号)` 精确对齐，对上 99.72%；面积中位差 +0.008%，绝对值 q90 1.19%；顶点数为原始的 101.8%，多出的部分经取证来自跨瓦片合并接缝（多顶点组 56.6% 跨瓦片，对照组 1.7%）。差异写入 `quality-report.md` | [x] 2026-09-04 |
| D0-5 | DEM | **2026-09-03 完成，形态有变（D-022）**：不再按 AOI 拷子集，整层登记为共享资产 `data/basemap/dem`（z0–8，87381 个文件、7442617745 字节，九层均完整，索引哈希入清单） | [x] 2026-09-03 |
| D0-6 | `manifest.json` | **2026-09-04 完成**，脚本 `scene/make_manifest.py`。区域总清单把区域定义、共享资产引用（按 sha256）、7 项区域产物的哈希、溯源链（两个 OSM 时刻与 planetiler 版本）、复跑命令串成一条可核对的链 | [x] 2026-09-04 |
| D0-7 | 质量报告 + ASCII 检查 | **2026-09-04 完成**。`quality-report.md` 由 `scene/quality_report.py` 现算生成，含三条不可比因素、计数、几何差异、高度来源、结论与限制；`scripts/check-ascii.sh data/scene data/basemap scene` 全部合规 | [x] 2026-09-04 |

验收：CLAUDE.md D0 行；量化记录：要素数、`src` 分布百分比、包体积。原列的首个技术未知点「Protomaps z15 建筑 feature 有无稳定 id」已于 2026-09-04 实测销项：带稳定 id（D-025）。

---

## 5. D1 2.5D 离线底图

| # | 步骤 | 要点 | 状态 |
|---|---|---|---|
| D1-1 | Range 静态服务 | **2026-09-04 完成**：`server/src/{range,static,index}.ts`，零运行时依赖。单区间、开区间、后缀区间、416 越界、HEAD 全覆盖；多区间按无 Range 处理并写明；路径穿越防护的安全契约是「要么 null 要么落在根内」。30 项测试。实测对 137 GB 底图发 `bytes=0-15` 返回 206 与 `Content-Range: bytes 0-15/137370745450` | [x] 2026-09-04 |
| D1-2 | web 骨架 | **2026-09-04 完成**：Vite + React 19 + TS，`maplibre-gl` 5.24.0 与 `pmtiles` 4.4.1 随包。`addProtocol('pmtiles')` 走 Range 而非整文件取。前端不硬编码任何路径，从 `/api/v1/scenes` 与数据包入口清单读地址与统计 | [x] 2026-09-04 |
| D1-3 | 样式移植 | **2026-09-04 完成**：`web/src/scene/style/{colors,protomaps,glyphs}.ts`、`layers/{hillshade,buildings3d}.ts`、`init.ts`。**与 Airports 原函数求值结果逐层比对：60 层的 id、顺序、过滤条件、线宽档位、色值、标注参数完全一致**，固化为黄金基准 `tests/golden/airports-basemap-style.json`。建筑层数据源换成同源 GeoJSON（铁律 11），外观参数与 `addBldg3d()` 一致 | [x] 2026-09-04 |
| D1-4 | 底图不需要 sprite | **2026-09-04 确认**：Airports 样式无 `sprite` 字段，底图不用图标；态势图标在 D2 用 `map.addImage` 运行时注册 | [x] 2026-09-04 |
| D1-5 | 探针 | **2026-09-04 完成**：`web/src/scene/probe.ts` 的 `window.__probe()` 只读导出地图状态，无写操作。端到端测试 `tests/e2e/scene-smoke.mjs` 由它驱动，14 项断言全过；调试协议客户端 `tests/e2e/cdp.mjs` 零依赖 | [x] 2026-09-04 |

验收（**2026-09-04 全部通过**）：与 Airports 同相机、同视口、只留底图的并排比对，容差 8 内 99.83% 像素一致、平均绝对差 0.15/255（不逐位相同也不应该：本项目 MapLibre 5.24.0，Airports 5.6.1，两版文字排布与抗锯齿有亚像素差别）；数据全部来自本地服务，页面不发任何外网请求；探针报告 WebGL2 可用；`scripts/check-ascii.sh web/dist` 合规。

---

## 6. D2 态势图层（已并入 G-5，2026-09-04，D-030）

原 D2-1 状态流 schema 归入 `docs/api-versions.md` §4 的 WS 事件（`entity` 事件）与 `track.jsonl`；
D2-2 图层模块拆到 §9C G-5（目标与航迹；覆盖场、测向线、椭圆首期无生产者，延后）；D2-3 Canvas
叠加层随延后项延后；D2-4 色值重标定保留，随 G-5 一并做，结果进 `docs/display-route.md`；
D2-5 的 20 Hz 定频 tick 改为渲染节流（状态由引擎逻辑时间驱动，不再由浏览器定时器驱动）。

| # | 步骤 | 要点 | 状态 |
|---|---|---|---|
| D2-1 | 状态流 schema | → `docs/api-versions.md` §4 `entity` 事件与 `track.jsonl`（B-6、B-3） | 并入 |
| D2-2 | 图层模块 | → G-5 `layers/{targets,trails}.ts` | 并入 |
| D2-3 | Canvas 叠加层 | PPI / 测向线 / 椭圆随生产者（后置 P1/P2）一起延后 | 延后 |
| D2-4 | 色值重标定 | 保留，随 G-5 做；在 Airports 浅色底图上重定威胁色、AOA/TDOA 双色、viridis 不透明度；结果进 `docs/display-route.md` | [ ] |
| D2-5 | 节流 | 改为渲染节流：实体更新合并到 rAF，`setData` 小 GeoJSON | → G-5 |

验收随 G-5：无人机图标与航迹随任务状态流刷新，位置与黄金航迹同一时刻一致；Retina 截图无错位。

---

## 7. D3 遮挡库移植（2026-09-17 按 07 报告 / D-074 重排）

| # | 步骤 | 要点 | 状态 |
|---|---|---|---|
| D3-0 | 设计报告 07 | 拍板两项 OPEN 并定接线口径 | [x] 2026-09-17：`07.遮挡落点与坐标基座_v1.0.md`（693 行，D-074）。**遮挡落点 = 浏览器 TS 复算并与 C++ golden 对拍**；**坐标基座 = vendored GeographicLib**（D-035 回收，自写闭式保留作对拍件）。调研实测：单次射线 0.6–6.7 µs / 47546 栋真实建筑、整任务预算 ≈ 单核 1.7%；自写闭式对 foundation 差 1e-9 m；legacy 投影偏差折到刀口损耗 ≤ 0.1 dB；E3 下 golden-01 末端带内信噪比 33.8 → 1.4 dB |
| D3-1 | vendored GeographicLib | `Geocentric` + `LocalCartesian` 及最小闭包 → `third_party/geographiclib/`；记版本号与压缩包 sha256，署名进 `THIRD-PARTY-NOTICES`；`GeographicLibGeodesy` 与自写闭式对拍 ≤ 1e-6 m；`geo/` 的「零第三方依赖」三处表述一并改掉 | [x] 2026-09-17：**取 2.5.2 不是最新的 2.7**——上游自 2.6 起要 C++17，本项目是 C++14，2.5.2 是最后一个只要 C++14 的版本（实测子集在 `-std=c++14 -pedantic-errors -Wall -Wextra` 下零警告；2.7 的同一子集只能靠编译器扩展过，MSVC 严格模式不可靠）。七个文件 78105 字节，压缩包 sha256 `b3b4ce39…`。新增黄金基准 `tests/golden/geodesy.json`（210 例）与生成器 `cuav_geo_smoke_test --write-golden`。**实测**：两家一致性 ECEF 最差 **1.679e-9 m**、站心 **1.779e-9 m**（判据 1e-6）；自写的站心旋转对上游 `LocalCartesian` 最差 **9.313e-10 m**（把「各家一样」这句话验实了）。**缺省工厂仍返回自写闭式**，三份航迹基准与组件目录基准**逐字节不变**。引擎 293 项 doctest + 17 项 ctest 全绿 |
| D3-2 | **换基座的基准变更**（单独提交，铁律 10） | 三份航迹基准重冻；全部产品文件拿换基座前那一版 `cuav_run` **逐字节比对**并如实记录 | [x] 2026-09-17：只改 `default_geodesy()` 一处、调用方一行没动。**两处预测被实测推翻，写进 07 §14**：① 产品 `.f32` 不是「大概率不变」——42 个变了，根因是纯单音谱里那片**数值相消底**（−200 至 −300 dBm），分开量才对：**峰下 20 dB 以内最大差 7.9e-4 dB**、含相消底 58 dB；`chain-golden-02/s4` 干净对照 6000 万个值只变 1021 个；② 判据「逐点相对差 ≤ 1e-12」超差（2.312e-12），顶到的是航向角，根因是判据拿 foundation 替身定的且对角度用相对差本就不对，**改成按量的物理判据**（经纬度 1e-12 deg / 高度 1e-8 m / 航向 1e-8 deg / 速度严格 0），余量 35–127×。**下游结论一个未变**：五份夹具检测行 / 命中帧 / 突发段 / 识别标签 / 准确率逐项相同，跨层算例 ① 仍 0.0138。引擎 293 doctest + 17 ctest、web 246、server、七套 e2e **355 项**全绿 |
| D3-3 | 移植遮挡三件 | emcore `map/{imap_query,local_scene_adapter}`、`models/occlusion`、`propagation` 的 `fresnelV` / `knifeEdgeLoss_dB` → `geo/`（接口改吃平面米，旧常数进 `geo::legacy`）；文件头注明来源与 golden 对应 | [x] 2026-09-17：落点 `cuav_geo/map.h`、`occlusion.h/.cpp`、`propagation.cpp` 的 `legacy::{fresnel_v, knife_edge_loss_dB}`；新黄金基准 `tests/golden/occlusion.json`（148 例 + 12 栋楼，自 emcore 拷入，`_meta` 记来源去处与投影口径），`propagation.json` 补上 `fresnelV` 108 例与 `knifeEdgeLoss_dB` 14 例。**全部 `rel ≤ 1e-9`**，148 例中 29 例判非视距，掠射锚点 **6.0329 dB**。`legacy` 里由此有两套常数不同的投影并存（定位纬向 111320 / 遮挡纬向 110540），各守各的 golden；常数守卫为 `occlusion.cpp` 的两个符号开写明理由的窄例外，**实测验证过别处仍拦得住**。**顺带逮到 07 报告写错的量级**（两个光速常数差 3e-3 dB 不是 1e-5 dB，§14.3 修正）。遮挡**尚未接进链路预算**，接线是 D3-5。引擎 **297** doctest + 17 ctest 全绿 |
| D3-4 | 建筑进引擎 | `engine/src/buildings_json.cpp`（nlohmann，`geo/` 不碰 JSON）；核对 `manifest.json` 的 sha256；`MultiPolygon` 36 件按外环拆；**懒加载**，只有 E3 才读 | [x] 2026-09-18：`buildings_json.{h,cpp}` + `geo::SceneFrame`（平面工作帧，原点 = 清单里的 `aoi.center`，只给东 / 北、高度另给）+ 进程内共享缓存 `shared_scene_map()`（K 个站一份桶网格）。装载器只注入内部参数 `scene_root`，加载在 `ScenarioSource::init()`——**07 §9.2 原写「在装载器里加载」与 §7.4 冲突，按 §7.4 做**（装载器是 `--validate` 唯一走的路），实测 `--validate` 中位 **3.1 ms**、不读一个字节。**三个数与报告不符，已回填**：栋数 47546 → **47662**（`MultiPolygon` 36 件拆出 116 栋，§1.4 调研时明写跳过了它们）；加载 210–242 ms **过了 200 ms 的线**，逐段量出来是那条线漏算了读盘（≈45）与 sha256 核对（≈40），而 §7.1 自己要求核对——线改 250 ms、不优化（§14bis.1，另记 DOM 解析瞬态峰值内存 193 MB 与唯一有量级收益的改法 SAX）；投影本身只花 **29–32 ms**，不是瓶颈。忽略内环 345、剔除 0 件。**零回归**：五份夹具 54 个产品文件对上一版 `cuav_run` **逐字节相同**（`events.jsonl` 按 D-041 ③ 不承诺）；唯一基准变更是组件目录**纯插入 8 行**。引擎 **306** doctest + 17 ctest、web 246、server 127、七套 e2e **355** 项全绿 |
| D3-5 | 接进帧生产端 | `LinkFrameSource::build()` 增 `const IMapQuery*`；`line_of_sight = !blocked`；损耗进 `extra_loss_dB`、单程 ×1；`included_loss_terms` 增 `diffraction`；闸三（E3 × 阴影）与闸四（E3 × 城市经验） | [x] 2026-09-18：`link_geometry()` 增可选 `OcclusionQuery`（地图 + 平面帧 + **逐帧的瞬时频点**，跳频时 v ∝ 1/√λ 会变）；`LinkFrameSource` 的地图**与 build 分两步给**（`set_scene_map()` + `needs_scene_map()`）——建筑是懒加载的、要到 `init()` 才有，而链路在 `configure()` 里就搭好了（§14ter.2）。**07 §1.5 那张调研表逐行复现，而且是从保存的典型链路上跑出来的**：起飞 211.1 m / **36.98 dB**（调研 210 / 37.0）、视距窗口 t 7.2–26.4 s、第一航段末 801.2 m / **28.39 dB**（调研 799 / 28.4）、末端带内信噪比 **33.7 → 1.4 dB**（调研 33.8 → 1.4）；180 s 全程 **89.3% 判非视距**。**零回归**：E1 八份夹具 **134 个产品文件** + E2 全开另 24 个 + 三份航迹基准 + 组件目录基准**全部逐字节不变**（`diffraction_dB` 做成可选键，只在非零时写）。新夹具 `chain-golden-01-e3.json` 与回归脚本 `tests/regression/e3_occlusion_chain.py`（7 项，已接进 `build-all.sh`，缺建筑集时明说跳过）。**顺带**：①「`included_loss_terms` 按帧的挡没挡来声明」写错过一次——它回答的是「这条路损算没算过遮挡」不是「这一帧挡没挡住」，改成 E3 每帧都声明（§14ter.3）；② 前端 `propConflict()` 由「与引擎一一对应」变成「比引擎严一档」，报文照实改写并加断言钉住（§14ter.4）；③ 修掉折腾过两次的 e2e 假失败——七套用例共用写死的调试端口 9333，**上一次归因给旧服务进程是错的**（§14ter.5）。引擎 **310** doctest + 17 ctest、web 246、server 127、七套 e2e 355 项全绿 |
| D3-6 | 浏览器 TS 复算 | 自 em-demo 祖本 `occlusion.ts` 回迁（原点参数化、外环改不闭合）；读同一份 `tests/golden/occlusion.json` 148 例；建筑几何懒加载、不进主 store | [x] 2026-09-18：`web/src/scene/occlusion/{frame,adapter,occlusion,geojson,store}.ts`，**148 例 `rel ≤ 1e-9` 首跑即过**（29 例判非视距，与 C++ 同数）、`fresnelV` 108 例、`knifeEdgeLoss_dB` 14 例。**核实到 148 例不够用**（§14quater.1）：它用的是 12 栋合成楼加 legacy 投影，**测不到**解析规则、严格站心平面帧、四万多栋楼的桶网格——补了 `tests/golden/occlusion-aoi.json`（真实建筑集五条射线，C++ 是真理源、浏览器复算，**最差相对差 9.5e-13 / 1.3e-12**，判据 1e-9），顺带把九项解析计数也钉成两侧同守（47582 → **47662 栋**）。祖本两处按预告改（外环改不闭合、原点参数化），**第三处是有意的不同**：几何类型不认识时 C++ 报错、浏览器计数并跳过（同一份文件还要交给 MapLibre 渲染，一个坏要素不该让整幅图白掉）。**「懒加载」懒的是数据不是代码**：模块在产物里只占 **835 B**（gzip +0.57 kB），而 15.9 MB 的数据一字节不取；`slice2` 新增一条断言首屏 `occlusion.status === 'idle'`，那是「懒住了」唯一的自动化证据。浏览器侧实测 `JSON.parse` 88 ms / 投影 30 ms / 桶网格 24 ms。web 246 → **259**，七套 e2e 355 → **356** |
| D3-7 | 界面 | E3 去掉置灰并列出包含哪几项；点选地图任一点对焦点站算视距与损耗；链路线第一次变红 | [x] 2026-09-18：**三件都做了**。① **E3 放开**——`LEVEL_UNAVAILABLE` 整个删掉（只有 E3 一条，删完是空表，同 D-057 不留死代码），`propConflict()` 里拦 E3 那条撤掉、补闸三（E3 × 阴影）闸四（E3 × 城市经验）与引擎报文逐句对齐，`propView().terms` 增 `diffraction`（卡片上写「E3 · 自由空间 + 建筑遮挡」），新增第 14 项检查 `prop_scene`「E3 有观测区域建筑几何」（只在 E3 时出现，前端查「场景引用的观测区域与载入的数据包是不是同一个」，引擎查清单字节哈希、比前端严）。**顺带改掉 D3-5 欠的一句话**：`prop_level` 的参数描述还写着「待 D3，本版本收到即报错」，那是下拉的悬停提示——改它是一次组件目录基准变更，实测**只有这一行 description 变**（§14quinquies.1）。② **视距探测**——场景页工具条观察组加「视距」，点地图任一点对焦点站算视距与刀口损耗，右栏出一张只摆事实的卡（站 / 点 / 假设高度 / 距离 / 视距 / 刀口损耗 / 频率，**没有自由空间路损**：浏览器里没有第二份 `fspl_dB`），地图上画一条虚线加一个点、用链路线那一对色不新标定。**假设高度必须黏住**，否则楼两侧那两次用的不是同一把尺子而画面上看不出来（§14quinquies.4）。建筑几何仍懒加载，**第一次点击才取那 15.9 MB**（实测 130 ms）。③ **链路线第一次变红**——`slice2` 跑一条 3 秒的 E3 链（golden-01 头 7.2 秒全程非视距），断言 `links[0].los === false` 且渲染出的要素 `los` 全是 `false`。**服务端不用改**：`--scene-root` 的缺省值恰好就是 `data/scene`，而子进程 cwd 恒为仓库根（§14quinquies.3）。**验收不循环论证**：物理由两份 golden 两侧各守（D3-3 / D3-6），交互与同源由 `slice2` 验——楼从**渲染图层**上挑（铁律 11 同一份 GeoJSON）、真鼠标事件点击，断言分无条件（所有候选楼的楼后必非视距）与存在性（至少一栋楼前是视距）两条（§14quinquies.5）。**实测**：楼 `35185865451587`（62 m、143 m 外）楼前视距、楼后 **37.1 dB**、侵入 24.7 m。web 259 → **263**、`slice2` 30 → **42**、七套 e2e 356 → **368**（14 + 39 + 42 + 138 + 75 + 42 + 18）；引擎 311 doctest + 17 ctest、server 127、`build-all.sh` 全过 |
| D3-8 | MSVC / GCC 构建 | 首次在 Windows 机或 CI 上验证（铁律 16） | [x] 2026-09-18：**走 GitHub Actions，不找真机**（公开仓库、免费；C++ 测试要的 29 份黄金基准、三个示例场景、清单与框图夹具**全都在 git 里**，只有 15.9 MB 的建筑集不在，相关用例自己说跳过）。`.github/workflows/build.yml`：Linux / GCC 与 Windows / MSVC 两个平台各建 `geo` + `engine`、各跑 311 doctest + 17 ctest、再把组件目录与黄金基准逐字节比一次；另一个 job 建前端与应用服务。两边都用 bash，命令逐字相同；`fail-fast: false` 让一边红了另一边照跑完。**结果：两个平台全绿**（`geo` 第一次在 MSVC 上编过并通过 210 例坐标基准与 148 例遮挡基准）。**七轮里挖出七个真问题，没有一个能在开发机上出现**：① **git 在 Windows 上把 LF 换成 CRLF**——57 个失败断言里 50 个是它，而本项目整套溯源建立在字节哈希上（清单本地算 `4e1ed6a8…`、Windows 上算 `7e73ad02…`，把本地这份按 CRLF 重算得到的正是后者）；修法是 `.gitattributes` 的 `* -text`，**这从来不只是 CI 的事**——任何人在 Windows 上克隆，拿到的副本都对不上它自己记录的哈希；② `.gitignore` 里没加前导斜杠的 `ref/` 把 `matlab/ref/` 也吞了，**Coder 产物的 8 个来源 `.m` 从未入库**，溯源链只在开发机上闭合；③ 两个缺失的标准头，**两个编译器各抓到一个不同的**（GCC 卡 `std::rotate` 缺 `<algorithm>`、MSVC 卡 `std::function` 缺 `<functional>`），让脚本把项目头的传递关系算进去问一遍全仓库，真缺的正好就这两处；④ 五个测试各抄一份的 `temp_dir()` 读 `TMPDIR` 退回 `/tmp`，Windows 两样都没有——收成一个 `engine/tests/tmpdir.h`；⑤ `test_buildings` 判跳过判的是**入 git 的**清单而非**不入 git 的**建筑集，于是不跳过、直接硬失败；⑥ **坐标基座判据 `1e-9 m` 的形状不对**——double 在 6.4e6 m 处的最低位**正好是 9.3132e-10 m**，那条线等于要求比浮点本身能给的还准，在生成基准的同一台机上恒为 0、换个 libm 当场超差（GCC 实测站心 2.197e-9 m = 2.358 个最低位）；改成按最低位给上界并写出推导（正算 4 个、站心 8 个，余量 4.0× 与 3.4×），**是换形状不是放宽**；⑦ **引擎自己在 Windows 上 stdout 输出 CRLF**（落盘那一侧本来就都带 `ios::binary`，唯独 stdout 没有），D-041 ① 承诺的「stdout 与 `events.jsonl` 逐字节相同」在 Windows 上不成立，而应用服务正是逐行读那个管道的——`platform::set_stdout_binary()` 在 `main` 开头调一次。另有两条与平台无关但被 CI 逼出来的：CI 的 job 要设超时（挂住的 job 白占构建机六小时，且日志在步骤结束前根本看不到），以及测试要有逐条超时（挂死的测试不报名字） |
| ~~D3-5（旧）~~ | `SceneParamFrame` 生产者 | **移交 G-2**（2026-09-04，D-030），已于 2026-09-06 完成；编号腾给上表 | 移交 |

验收（切片 ⑤）：**148 例 golden `rel ≤ 1e-9`，C++ 与 web 两侧各守一遍** + 解析锚点（刀口 v=0 → 6.03 dB、FSPL@2.4 GHz/1 km = 100.05 dB、热噪声@1 MHz/NF 6 = −108 dBm）+ 点选建筑两侧一致 + 双编译器构建通过 + **E1 / E2 档下产品与航迹基准逐字节不变**（D3-2 那次基准变更除外）。

---

## 8. D4 渲染—物理同源验收

| # | 步骤 | 状态 |
|---|---|---|
| D4-1 | 同一 `buildings.geojson` 同时加载到 `geo/` `LocalSceneAdapter` 与 web `fill-extrusion` | [x] **2026-09-19（D-076）**：两侧地址逐字相同并且真的被取过——`sameUrl` 断言 + 网络记录。**同一份文件被取两次**（MapLibre 渲染一次、遮挡侧一次，有意不复用），写在断言里 |
| D4-2 | 探针点选任一建筑：id、`height_m`、`base_m` 两侧一致；任一链路 LOS 数值结果与画面遮挡一致（截图 + 数值） | [x] **2026-09-19**：`window.__cuav.scene.sameSource()` 逐栋核对，实测**画面 2178 栋、对上 2178 栋、差异 0 处**；LOS 与画面一致那半由既有的「点选建筑两侧一致」覆盖；截图 `cuav-slice2-smoke.png` |
| D4-3 | 探针无副作用；瓦片 `buildings` 层未进遮挡计算 | [x] **2026-09-19**：探针连调 30 次相机 / 实例 / 建筑加载状态不变、不增删图层、不触发那 15.9 MB；瓦片层没混进来由 `build-all.sh` 的结构守卫（遮挡侧一个文件都不认识 MapLibre）加桶网格 47662 栋的计数共同保证 |

**这一步改掉的一处真问题**：渲染侧 `fill-extrusion-height` 的兜底是 `['coalesce', ['get','height_m'], 8]`，
而物理侧对同一情形是**剔除并计数**——同一份文件两边两个意思，屏幕上立着一栋视线穿得过去的楼。
兜底改成 0（与「物理侧没有它」同义）。当前建筑集 47582 个要素没有一个缺高度，所以画面一个像素没变；
留着它是因为「碰巧没有反例」不是保证（铁律 11、15）。

**两条写下来免得被当成缺陷**：① MapLibre 对 GeoJSON 源按 `tolerance` 简化并在瓦片边界切开，
**顶点数两侧常不同**（实测 406 / 2178 栋），那是渲染的产物不是数据差异，只报不判；
② 无头 Chrome 约每十秒丢一次 WebGL 上下文并自动恢复（`tests/e2e/README.md` 第四个坑）。

---

## 9. P1 最小端到端 IQ 闭环（04 §13.3）

| # | 步骤 | 要点 | 状态 |
|---|---|---|---|
| P1-0 | 设计报告 08：引擎分块流与多速率 | Block 结构、端口类型系统、DAG 调度、背压、采样率变化时的时间与群时延、共享内存抽象层、种子与 trace。**2026-09-04 增写**：注册表与参数描述、JSON 装载、观察者、参数帧与 IQ 流时钟对齐、观测点与产品文件格式、Coder 产物的组件包装约定；时点：切片 ① 后、切片 ② 前定稿 | [ ] |
| P1-1 | `docs/iq-format.md` | 主产品定义；复 int16 交织 + `scale/full_scale`；内部 float32；元数据 sidecar JSON 含 04 §9.2 十一类字段；SigMF 映射；分段存储；S0–S6 命名；`\|Δf\| + B/2 + 保护带 < Fs/2` 检查。**分两批定稿**：容器、样点编码与元数据结构现在即可定稿（公开数据集已提供真实样本，以及 `scale/full_scale` 的取值案例——满量程 32768、实际峰值码 1000 至 5500）；**功率与标定口径被甲方数据阻塞**，单列一节不与其余章节捆绑 | [~] |
| P1-2 | engine 核心 | **2026-09-04 首版落地**：`Block{samples, meta}`、`PortType` 与连线校验（D-013 在 `connect()` 里执行）、组件接口、拓扑排序、单线程调度、文件流与内存流、`IRandom&` 注入、溯源八件套与四态传播，23 项单测全绿。**能量检测与 Python 参考实现逐帧对拍通过**（门限 2.3e-15、命中帧逐帧一致、检测量 3.3e-9）。尚无背压、多线程、共享内存抽象——薄切片刻意不做，接口已留位置 | [~] |
| P1-3 | models 首批 | **部分落地（2026-09-04）**：`ToneSource`（含起止样点，可作突发开关）、`NoiseSource`、`FileReplaySource`（读本项目 `.iq` 加旁挂清单）、`AddMixer` 已有并有单测。**尚缺** `FreeSpaceChannel`、`RxFrontend`、`ADC`、`DDC`、`Channelizer`。**2026-09-04 修订**：`FreeSpaceChannel` 移入 G-3；`DDC / Channelizer / RxFrontend` 滤波部分改走 M-2 / M-3 Coder 路线；`ADC` 与噪声注入保持 C++ 手写；全部以 B-1 注册表形式进目录，进切片 ④ | [~] |
| P1-4 | algos 首批 | 拆 a–d（2026-09-04）：a `SpectrumAnalyzer`（Welch → `SpectrumFrame`，首个生产者，以 M-4 的 `pwelch` 黄金向量验收，切片 ①）**[x] 2026-09-05**：`components/spectrum.h`，double FFT，四种周期窗，重叠与多段平均，块长无关，与 Python 及 MATLAB `pwelch` 三方互证 rel ≤ 1e-9；b 包络归约并入 B-3；c 能量检测改滑动噪声估计 / 恒虚警率（D-026）；d `detections.jsonl` | [ ] |
| P1-5 | server 最小接口 | **撤销（2026-09-04，D-030）**，由 §9A B-5 / B-6 / B-7 替代，端点改为 `/api/v1/results/{task}/{op}/...`。原文：`POST /api/v1/tasks`（框图 JSON）、`GET /tasks/{id}`、WS 状态、`GET /results/{id}/psd?t0&t1&f0&f1&px`（按视窗抽取） | 撤销 |
| P1-6 | web 最小页 | **撤销（2026-09-04，D-030）**，由 §9B U-1 / U-3 替代；「JSON 编辑框」只在切片 ① 作临时入口。原文：任务 JSON 提交表单 + 结果页 PSD / 瀑布（Canvas）；框图画布留到 P2 | 撤销 |
| P1-7 | 标准算例 | 单音频移与功率标度、带限噪声与目标 SNR、突发占空比、DDC 频移/低通/抽取、ADC 量化与削顶、固定种子复现；跨层一致性算例 ①（EM-S-02 Pd 公式 vs 蒙特卡洛）。输入改为画布保存的框图 JSON，算例框图入 `tests/regression/diagrams/`（2026-09-04） | [ ] |
| P1-8 | MATLAB Coder 链路 | ~~由 §9D M-2（DDC）兑现~~ **2026-09-16 改（D-070）**：`DDC` 以手写 C++ 完成（算法核约 60 行、封装层本来就得手写，工程取舍），M-2 交的是 DDC 组件本身与标准算例第 7 项。**本项改由 §9D M-3 兑现**（多相 FFT 信道化与接收滤波，工具箱的主场）。**同日由 M-3 兑现（D-071）**：`.m` → `codegen` → 入库 C → 静态库 `cuav_coder` → 组件 `Channelizer` / `RxFilter` 整条链路走通，产物重生成逐字节相同、`source_ref` 带齐 08 §13 第 4 条的四样 | [x] 2026-09-16 |
| P1-9a | 公开数据集回放 | DroneRFb-DIR：转换（§11.4 DS-2）→ 04 §10.6 八项质检 → 回放 → PSD/瀑布/能量检测。**不阻塞，现在可做**；`FileReplaySource`（P1-3）一落地即有真实文件可喂。经画布提交；目录里 `FileReplaySource.scene_bindable=false`，画布与 `--validate` 双重拒绝场景绑定（防线二、三） | [ ] |
| P1-9b | 甲方实测回放 | 数据到位后：导入 → 04 §10.6 八项质检 → 回放 → PSD/瀑布/能量检测；额外含真值对齐与功率标定 | [ ] |

退出：一个框图从**框图画布**提交并跑到检测结果（2026-09-04 修订，原文「从浏览器提交」）；宽带与信道化 IQ 可被外部算法读取；标准算例正确可重复；实测 IQ 可导入回放；跨层算例 ① 在容差内（PROVISIONAL）。

---

## 9A. B 线：引擎运行器与服务桥接（替代 P1-5；2026-09-04，D-030 / D-031）

引擎目前只有静态库与测试可执行，与应用服务零接口；`IComponent` 没有参数描述与按名构造，`configure()` 只收两个 map。本线补桥。

| # | 步骤 | 交付物 | 验收 | 依赖 | 状态 |
|---|---|---|---|---|---|
| B-1 | 组件注册表与参数描述 | `engine/include/cuav/registry.h`、`engine/src/registry.cpp`；`component.h` 增 `describe()`（`ParamSpec{name,type,unit,min,max,default,enum,required,description}`、`category` 六类、M/E 等级、`scene_bindable`、`implementation`） | 现有 6 组件经注册表按名构造，23 项单测不变；越界参数被拒且报错含参数名 | P1-2 | [x] 2026-09-04：`param_spec.h`、`registry.{h,cpp}`、`catalog.{h,cpp}`，六组件各带 `describe()`；新增 7 项测试，合计 30 项 523 断言全绿；目录导出 `catalog_json()` 已可用，黄金基准待 B-4 |
| B-2 | 框图 JSON 装载器 | `engine/include/cuav/diagram_json.h`、`engine/src/diagram_json.cpp`（nlohmann 已 vendored）；规范 `docs/diagram-format.md`；接受 `IDataResolver` 把 `data_id` 解析为内部参数注入 | 合法框图装成 `Graph` 且 `validate()` 过；`IQStream→SceneParamFrame` 直连被拒并报节点与端口；未知键拒绝；框图里出现 `internal` 参数被拒（D-037） | B-1 | [x] 2026-09-05：`diagram_json.{h,cpp}`，`DiagramError` 四字段、17 个错误码；`MapDataResolver`（解析旁挂 `cuav-resolved/1`）与 `IndexDataResolver`（数据索引）；示例框图逐字作夹具 `tests/diagrams/slice1_tone_noise_psd.json` 装载运行通过（`total_samples` 补齐 2000000、谱 1953 行、包络 489 行）；新增 13 项测试覆盖 16 个错误码，引擎 50 项 12564 断言全绿；D-040 |
| B-3 | 运行观察者与显示产品 | `engine/include/cuav/observer.h`（`on_progress / on_entity / on_link / on_log / on_product_row`）；组件 `ObservationTap`：任一 IQ 口 → `spectrum.f32 + index.json`、`envelope.f32`、可选 S4 `.iq` + 旁挂清单；规范 `docs/display-products.md` | 行数 = 帧数；索引与 `BlockMeta` 一致；同种子两次运行逐字节相同 | B-1、P1-4a | [x] 2026-09-05：`observer.h`（含 `on_link`）、`platform.{h,cpp}`（目录、原子替换、字节序）、`components/tap.h` + `tap.cpp`；`Graph::run(rng, observer)` 重载，`IComponent::attach()`；谱行与 `SpectrumAnalyzer` 逐位同源，同种子两次逐字节相同，末行 / 末桶 / 丢尾记备注不降级；`iq/` 产品未做 |
| B-4 | `cuav_run` 可执行 | `engine/tools/cuav_run/main.cpp`；子命令 `--catalog`、`--validate <diagram>`、`--run <diagram> --out <dir> [--seed N] [--resolved <旁挂> \| --data-index <索引>...]`（2026-09-05 增，D-040）、`--scenario-track <scenario>`；stdout 每行一条带 `seq` 的 JSON 事件，错误事件直接 `to_json(DiagramError)`；退出码约定 | 命令行跑通切片 ① 框图；`--catalog` 与 `tests/golden/component-catalog.json` 一致 | B-2、B-3 | [x] 2026-09-05：`tools/cuav_run/{runner.h,runner.cpp,main.cpp}`（逻辑在静态库 `cuav_runner`，测试直接驱动）；事件信封与 WS 文本帧同构并镜像 `events.jsonl`；`product_row` 不带数据、观测点逐行刷盘；退出码 0–4；目录黄金基准首次生成（8 组件、21763 字节）并有比对测试；切片 ① 框图命令行跑通，2442 行产品事件，同种子产品逐字节复现；引擎 57 项单测 + 3 项冒烟全绿；`--scenario-track` 留 G-2；D-041 |
| B-5 | 服务任务管理器 | `server/src/tasks/{engine,resolve,store,manager,routes}.ts`（2026-09-05 按职责由 `{manager,runner,store}` 收成五个；`spawn` 数组参数不走 shell，cwd = 仓库根，路径全相对 ASCII；任务目录 `data/runs/<task_id>/`；内存表 + `task.json` 落盘，SQLite 留 P2）；`index.ts` 增 `POST /api/v1/tasks`、`GET /api/v1/tasks[/{id}]`、`POST .../cancel`、`GET /api/v1/components`；提交时按数据索引把 `data_id` 解析成解析旁挂 `diagram.resolved.json`（`cuav-resolved/1`，D-040），框图副本不含内部参数，含 `internal` 参数的框图 400；提交时先同步 `cuav_run --validate`，不过即 400（D-042） | 提交→运行→完成状态机；关浏览器任务不停；错误带引擎定位的节点与端口；日志里的路径替换为 `data_id` | B-4 | [x] 2026-09-05：五个模块 + 假引擎夹具 `fake_engine.mjs`（与 `cuav_run` 同构，状态机测试不依赖 C++ 构建）；状态机、FIFO 队列、取消（SIGTERM → SIGKILL）、退出收尾、重启对账、整行脱敏、幂等；事件环形缓冲与 `subscribe()` 留给 B-6 加传输；新增 28 项测试，服务合计 58 项全绿；真服务冒烟：切片 ①、真实片段回放、取消、杀服务、重启恢复全过；D-042 |
| B-6 | WS 服务端与补取 | vendored `ws`（D-032）；`server/src/ws/{hub,events}.ts`；每任务序号环形缓冲；`GET /api/v1/tasks/{id}/events?since=N`；规范 `docs/api-versions.md` §4 | 断线后按 `since` 补取无缺号；丢弃只作用于二进制行帧且以 `dropped` 告知 | B-5 | [x] 2026-09-05：`ws 8.21.3` 随包 + 仓库根 `THIRD-PARTY-NOTICES`；`/ws` 订阅 `{subscribe, since}` → `subscribed` 应答 → 回放到空批次 → 实时；`product_row` 转二进制帧（布局定死于 §4.0）；`readEvents()` 缓冲内切片、缓冲外读 `events.jsonl`、重启后合成服务端终态；`dropped` 连续且先于下一帧；心跳与漏 pong 断开；服务退出先关连接（1001）。服务 72 项测试全绿；真服务冒烟：断开后以 `since` 重连首条 = since + 1，并集 1..2447 无缺号。约定记 D-044 |
| B-7 | 视窗抽取端点 | `server/src/products/{window,spectrum,envelope,source,meta,jsonl,routes}.ts`（2026-09-06 按职责收成七个：纯函数选择与分组 / 两种归约 / 文件行源 / 索引与行数 / JSONL 时间窗 / HTTP）；参考实现 `algos/reference/product_window.py`；端点见 `docs/display-products.md` §3 与 `docs/api-versions.md` §3.1b | 与 Python 参考对同一产品文件的归约逐值一致；`px` 超原始列数不插值 | B-3、B-5 | [x] 2026-09-06：谱与包络抽取、索引端点、三个 JSONL 端点（读取层先行，生产者等 G 线）；`scatter` 暂 404 `product_unsupported`；读端以文件长度定行数、409 `not_ready` 就绪语义、16 MB 上限与必定能过的 `suggest`；引擎 `ObservationTap` 改为写完第一行即刷索引（把 `index_missing` 窗口从 64 行缩到毫秒级）。新增服务 30 项测试（合计 102）、引擎 1 项（合计 58）；黄金基准 12 用例 + 真引擎三组视窗与 Python 参考 2046363 个值**逐位**相同（含 `mean`）；D-046 |

## 9B. U 线：人机交互界面（吸收 P1-6 与 P2 前端；2026-09-04，D-030 / D-032 / D-034）

| # | 步骤 | 交付物 | 验收 | 依赖 | 状态 |
|---|---|---|---|---|---|
| U-0 | 设计报告 09 | `09.主界面三视图与框图画布交互设计_v1.0.md`：04 §8.1 布局落成线框（顶部菜单 / 左组件库六类 / 中画布 / 右参数 / 下日志）、三视图与顶部试验上下文（项目、试验、场景、框图、任务、逻辑时间游标、种子）、观测点与错误定位交互、键鼠约定；PROVISIONAL，顶替缺失的原型 html。**分两批**：前半「主界面框架与三视图」2026-09-04 写出并经电磁仿真人机交互评审按十二条「必须改」修订为 v1.1（§0–§15，附录 A；新增用户与三条主流程、链路对象、频谱与瀑布仪表约定、撤销重做、溯源面板、两正交徽标；快捷键避开浏览器保留键；回放源改 `data_id`）；后半「框图画布与信号视图交互」切片 ③ 前补（其 §16 列了清单） | 用户认可 | — | [x] 后半 v1.8 2026-09-07（§6.3–§6.11 画布交互、§7.4 对比模式）；前半 v1.1 2026-09-04 |
| U-1 | 三视图壳 | 改 `web/src/App.tsx`；新建 `web/src/shell/{Router,TopBar,ExperimentContext,LogPanel}.tsx`（手写 hash 路由）、`web/src/state/{store,reducer}.ts`（`useReducer` + context）、`web/src/api/{client,ws}.ts`（重连 + `since` 补取）；`SceneView` 挂到场景页；顶栏为面包屑「项目 › 试验 › 任务」加运行组（09 §2、§3）；快捷键 `Alt+1/2/3`；状态条显示坐标与时间基准；`probe.ts` 扩展 `__probe().app` | 切视图不重建地图实例；WS 重连后序号连续；日志面板显示引擎 `log` 事件；`Alt+2` 切到框图 | B-5、B-6 | [x] 2026-09-06，`tests/e2e/slice1-smoke.mjs` 23 项 |
| U-2 | 框图画布 | `web/src/diagram/{DiagramView,Palette,ParamPanel,portRules,serialize,validate}.tsx`；React Flow vendored；组件目录来自 `GET /api/v1/components`，连线合法性查 `port_compat`；任一 IQ 口可打观测点；场景绑定节点选 `scenario_id + entity_id`，端口按绑定动态生成 | 拖拽建出切片 ① 框图，序列化与 golden 等价；非法连线当场拒且理由与 `--validate` 一致；引擎报错映射回节点高亮 | U-0、U-1、B-5 | [x] 2026-09-07：`@xyflow/react` 12.11.6 锁版随包；`web/src/api/catalog.ts`（目录类型与 `canConnect`，对真目录 12 项单测）、`web/src/diagram/{doc,layout,format}.ts`（文档模型与规范序列化、拓扑分层布局、工程计数法，26 项单测）、`{DiagramView,NodeCard,Palette,ParamPanel}.tsx`；探针加 `app.diagram`；`tests/e2e/slice3-smoke.mjs` 27 项（含拖动跟手与隐藏穿透的回归）；**2026-09-07 D-051：降为高级模式，入口 `#/diagram/canvas`，框图页主形态改为典型链路视图（§9G C-7）** |
| U-3 | 信号视图 | `web/src/signal/{SignalView,SpectrumPlot,WaterfallCanvas,EnvelopePlot,IQScatter,viewport,colormap}.ts(x)`；Canvas 2D 起步；实时行走 WS 二进制帧追加，缩放回看走 B-7；瀑布用离屏 Canvas 位移追加；「星座图」首期只做 S4 IQ 散点（D-034）；仪表约定按 09 §7.2：频谱在上瀑布在下共用频率轴、瀑布最新行在顶、纵轴 dBm（来源徽标只在开发者模式，D-047；无常数才退 dBFS）、参考电平与动态范围、RBW、迹线模式、峰值与差值 marker、频率与时间双游标联动、框选缩放、检测叠加、导出（D-038） | 单音峰值频率误差 ≤ 1 bin；纵轴标注以 `dBm` 开头（来源只在探针 `calibration.source` 与开发者模式，合成链为 `model`，D-047）；缩放后重取行数 = `py`；20 Hz 下主线程无 > 50 ms 长任务 | U-1、B-6、B-7 | [x] 2026-09-06：频谱 + 瀑布 + 包络竖条，跟随 / 回看、结束即收口（D-048）；实际文件 `web/src/signal/{viewport,reduce,colormap,scale,trace,envelopeMap,fetcher,viewStore,waterfall,renderer,export,commands}.ts` + `{SignalView,SignalHead,MarkerBar,InstrumentPanel}.tsx`；IQ 散点延后（观测点无 `iq` 产品，D-040 ③）；检测叠加、对比留 U-4 / 切片 ③ |
| U-4 | 结果页与数据中心页 | `web/src/results/{ResultsView,TaskList,TaskSummary,taskRows,taskListStore}.tsx/.ts`、`web/src/data/{DataCenter,DatasetList,DatasetDetail,datasetRows,datasetStore}.tsx/.ts`（独立页面 `#/data`，顶栏直达，`Alt+4`，D-038）；服务 `GET /api/v1/datasets` 加过滤与分面、新增 `/datasets/{data_id}`（两档，含标定常数；来源只在 `?dev=1`）、`/api/v1/tasks` 加 `offset` 与 `total` | 可回看历史任务的频谱 / 包络 / 检测；数据中心列两批公开数据集索引，`holdout` 标记可见，「用于回放」写入 `data_id` | U-3、B-5 | [x] **2026-09-19（D-075）**：两处占位填上。任务页签 = 一页 100 条 + 两正交徽标 + 评价摘要 + 采用 / 取消，页脚照实写「共 941 个 · 第 a–b 个」；数据中心三栏（全量分面 / 列表 / 两档详情），四态只显徽标、原因进 `?dev=1`（用户拍板），「用于回放」按模式送进框图页。顺带补掉**混合增强在界面上选不了背景片段**（此前那条链只能靠夹具跑）。**「用同一种子重跑」本步不做**（用户选定的范围里没有它，09 §7.1 那一行已记）。零黄金基准变更；`slice4-smoke` 138 → 153 项 |
| P1-0b | **设计报告：天线与接收机组件**（D-050，切片 ④ 的设计前置） | 新报告或 08 增节，定四件组件的语义与参数：`AntennaGain`（方向图形式：查表 / 解析式；指向与极化损耗是否本期做）、`ReceiverFrontEnd`（噪声系数生热噪声，与现有 `NoiseSource` 是取代还是并存；增益与 AGC 是否本期做）、`AdcQuantizer`（位数、满量程、削顶的降级标记口径）、`DDC`（群时延与样点序号换算按 08 §8 四条口径）；并给出 S0–S6 各观测点在新链路上的落点 | 三个待定问题各有拍板结论并记决策；「接收机噪声取代手兑噪声」若成立，须按铁律 10 走正式基准变更、列明受影响的示例框图与跨层算例 ① | U-2 已成、08 §8 口径、D-036 | [x] 2026-09-07：由 10 号方案 §3.2–§3.5 兑现（D-051）；三问结论见 10 §11；实现在 §9G C-2 |

## 9C. G 线：真实地理环境下的场景与目标活动生成（D-001 的兑现，04 之外，PROVISIONAL；D-030 / D-033 / D-035）

| # | 步骤 | 交付物 | 验收 | 依赖 | 状态 |
|---|---|---|---|---|---|
| G-0 | 场景文件规范 | `docs/scenario-format.md` + `docs/schemas/scenario.schema.json`（**2026-09-04 已落骨架，字段冻结**）；示例 `data/scene/beijing-yayuncun/scenarios/golden-01.scenario.json`（`synthetic: true`） | 示例过 schema | — | [x] 2026-09-06：示例场景写出（站点在观测区域中心，航线过顶后远去，斜距 211 → 70 → 3037 m，路损 86.7 → 77.1 → 109.9 dB）；`tests/unit/test_scenario_example.py` 八项（schema、清单哈希、跨引用、航线时长覆盖仿真时长、位置落在区域内）；文件以编辑器的规范序列化形式入库，空操作保存是逐字节的空操作 |
| G-1 | 大地坐标与运动学库 | `geo/include/cuav_geo/{geodesy,kinematics,link_budget,scenario}.h` + `geo/src/*.cpp`（删 `placeholder.cpp`）：`IGeodesy` 接口后先放自写 WGS-84 闭式（D-035）；航点插值 / 悬停 / 速度（语义参照 em-demo `engine.ts` 的 `moveAlongPlan`，C++ 重写并注明来源）；平地 LOS + FSPL + 多普勒 + 时延；`c = 299792458` | LLA↔ECEF 往返 < 1 mm；FSPL@2.4 GHz/1 km = 100.05 dB（D3-3 锚点前移）；固定场景航迹逐点确定 | G-0 | [x] 2026-09-06：`geo/` 四个模块（`ClosedFormWgs84` 用 Bowring 闭式不迭代；`Route` 预算段时轴后二分定位闭式插值；链路预算含 FSPL / 多普勒 / 时延 / 热噪声底）；`cuav_run --scenario-track [--track-rate] [--scene-root]` 实现，stdout 逐字节可复现，作黄金基准生成器；黄金基准 `tests/golden/scenario-track-golden-01.json`（361 样点）；引擎新增 `sha256.cpp` 与 `scenario_json.cpp`；单测 62 → 94 项。两处对 em-demo 的有意偏离（ECEF 弦长、闭式求值）写进 08 §9 与代码注释 |
| G-2 | 引擎组件 `ScenarioSource` | `engine/include/cuav/components/scenario.h`、`engine/src/scenario.cpp`；引擎 CMake 链接 `cuav_geo`；每条链路一个 `SceneParamFrame` 输出口 `link:<emitter_id>`；**按样点序号出帧**；实体状态经 `on_entity`、链路读数经 `on_link` 上报（场景视图链路对象，09 §5.4） | 帧连续覆盖仿真时长；同种子两次逐字节一致；`update_rate_Hz` 越界拒 | G-1、B-1、B-3 | [x] 2026-09-06：`ScenarioSource`（动态输出口 `link:<emitter_id>`，**每轮必产出至少一帧**，否则下游 IQ 块被调度器静默覆盖）与 `SceneEmitterSource`（按场景波形生成，归一化到发射期间单位功率）；装载器新增场景注入（`IScenarioResolver` 两种实现、`scenario` 错误码、场景与观测区域清单双哈希核对、跨节点采样率一致性）；`cuav_run` 落 `track.jsonl` / `links.jsonl`；服务端把 `scenario_id` 解析进旁挂的 `scenarios` 段 |
| G-3 | 施加类信道组件 | `engine/include/cuav/components/channel.h`：`SceneBoundChannel`（入 `iq` + `scene`，施加增益、整数样点时延、多普勒相位斜坡；帧间零阶保持并写 `state_reasons`）与 `FreeSpaceChannel`（定参，原 P1-3 欠项） | 单音经 FSPL 100.05 dB 后功率差 ± 0.01 dB；多普勒 1 kHz 峰值频移一个 bin 内 | G-2 | [x] 2026-09-06：`SceneBoundChannel`（增益、整数样点时延、跨块连续的多普勒相位累加器、帧内零阶保持、帧过期才降级、拒回放数据）与 `FreeSpaceChannel`；流长不变、`flush()` 不产出；实测 FSPL 时域与峰值 bin 两种口径都在 ±0.01 dB 内，±1 kHz 多普勒峰移 8 个 bin（bin 122.07 Hz）符号正确；单测 94 → 114 项 |
| G-4 | 场景编辑器（薄版） | `web/src/scene/editor/{ScenarioEditor,SiteTool,WaypointTool,ActivityTimeline,preview}.ts(x)`、图层 `web/src/scene/layers/{sites,plannedRoute}.ts`；服务 `server/src/scenarios.ts`（`GET/PUT /api/v1/scenarios/{id}`）；地图点选布站与航点、侧栏表单改高度 / 速度 / 悬停 / 活动事件；浏览器预览只做直线插值 | 保存后回读一致；预览航迹与 `cuav_run --scenario-track` 逐时刻差 ≤ 1e-6 度（golden `tests/golden/scenario-track-golden-01.json`） | G-0、U-1、G-1 | [x] 2026-09-06：`web/src/scene/{ObjectTree,ObjectForm}.tsx` + `editor/{preview,scenarioOps}.ts` + `server/src/scenarios.ts`；四个工具（选择 / 布站 / 航点 / 测量）、对象树（含派生链路组）、表单带 SI 前缀输入、撤销重做（整份文档入栈）、保存回传落盘哈希；预览与 C++ 航迹 361 个样点**逐点相同**（差 0，容差 1e-6 度）；场景语义校验交 `cuav_run --scenario-track`（D-042 同法） |
| G-5 | 态势图层由任务状态流驱动（吸收 D2） | `web/src/scene/layers/{targets,trails}.ts`（自 em-demo `MapView.tsx` 拆图标注册、航向、抽稀）消费 WS `entity` 事件；结束后由 `GET .../track` + 时间游标回放；D2-4 色值标定进 `docs/display-route.md` | 运行中图标位置与 golden 航迹同一 `t_s` 一致；界面不做「合成场景」标记，`synthetic` 只在场景文件里（D-043） | G-2、G-4、B-6、U-1 | [x] 2026-09-06：七个态势图层（站点圆点与图标、规划航线、航点、目标图标随航向旋转、航迹抽稀、链路线按视距着色）；高频量走 `web/src/scene/sceneStore.ts` 不进主 store，地图按 20 Hz 定频取值；结束后从 `track` / `links` 端点整批回放；色值在浅色底图重标定并写回 `docs/display-route.md` 第 4 节（**D2-4 销项**，最低对比度 3.56:1）；`slice2-smoke` 27 项断言含「界面不做合成场景标记」 |
| G-6 | 活动时间线施加 | `ScenarioSource` 解释 `activities`（起飞 / 巡航 / 悬停 / 降落 / 图传开关 / 跳频）→ 驱动辐射源节点起止与频率 | 瀑布上突发起止与时间线差 ≤ 一帧；跳频序列同种子复现 | G-2、G-3、U-3 | [x] 2026-09-15（D-069，随 C-8）：新增样点域的 `geo::ActivitySchedule`（与帧域的 `EmitterRuntime` 并存，两份航迹黄金基准逐点不变）；`SceneEmitterSource` 与 `Evaluator` 共用它，于是「源怎么发的」与「真值怎么记的」逐位同源；铁律 4 的闸覆盖跳频点（引擎两处 + 前端）；场景编辑器可建可改 hop。**实测跳频突发起点对活动时间线偏差 0.0 ns**（线是一帧 102.4 µs）。顺带修掉块粒度门控：golden-01 的 `fn` 7 → 0、发现时延 14.7 ms → 0 |

## 9D. M 线：MATLAB 内部模型生产与验证（03 §11.3.1 / 04 §8.5 第 1 档；兑现 P1-8；D-036）

MATLAB 的位置是**内部模型生产工具**：出参考模型、出黄金向量、用 Coder 出 C 代码进引擎。运行时、交付包、界面不出现 MATLAB（04 §8.5 末段、03 §11.8）。用户 2026-09-04 追问「MATLAB 的优势发挥出来了吗」，此前的答案是没有：参考实现全为 Python，P1-8 未排期，缺的 DDC / 信道化 / 接收滤波原计划手写 C++。

| # | 步骤 | 交付物 | 验收 | 依赖 | 状态 |
|---|---|---|---|---|---|
| M-0 | 工具链核实与调用约定 | 2026-09-04 已核实：开发机 MATLAB R2025a，工具箱 coder / dsp / comm / signal / phased / rf / fixedpoint / compiler；`matlab` 不在 PATH。约定：脚本经环境变量 `MATLAB_ROOT` 找 `bin/matlab -batch`（铁律 17，Windows 同法）。**许可结论 2026-09-16 修正（D-070）**：原记「Coder 产物随交付不受许可条款限制」与开发机实际不符——开发机是**学术许可**，codegen 产物每个 `.c` / `.h` 首部都盖着「不得用于政府 / 商业 / 组织用途」；**用户当日澄清本项目属学术用途，据此可用**。Coder 路线因此有效，首个使用者改为 M-3。留一条待核：产物若随交付物出去，需按实际用途另行核实 | 工具链清单 + 许可结论 | — | [x] 2026-09-04；许可结论 2026-09-16 修正 |
| M-1 | `matlab/` 骨架 | `matlab/ref/`（参考模型 `.m`，一函数一文件，可生成者标 `%#codegen`）、`matlab/coder/`（`codegen` 脚本与 `coder.config('lib')`：C 语言、固定尺寸接口、禁动态内存、分块带状态）、`matlab/golden/`（导出黄金向量为 `cuav-engine-golden/1` 同 schema 的 JSON 到 `engine/tests/golden/`）、`matlab/run_all.m`；产物 C 入 `models/<环节>/coder/`，文件头记来源 `.m`、MATLAB 与 Coder 版本、codegen 参数哈希 | `run_all.m` 经 `-batch` 跑通；`scripts/check-paths.sh` 过 | M-0 | [x] 2026-09-05：`matlab/{ref,golden,coder}/`、`run_all.m`、`run_matlab.sh`（经 `MATLAB_ROOT`）；首个参考 `ref/cuav_welch_power.m`；`coder/` 空待 M-2 |
| M-2 | **DDC（手写 C++；工程取舍，非许可所迫，D-070 ①）** | 系数表 `scripts/design_ddc_fir.py`（scipy 等波纹）→ `models/adc-ddc/fir_lp_v1.json` → `engine/src/ddc_taps.cpp`；算法核 `engine/src/dsp.cpp` 的 `ddc_*` 三件；组件 `engine/src/ddc.cpp`（参数 `f_shift_Hz / decim / fir_version`）；第二参考 `algos/reference/ddc.py`；MATLAB 只作系数的独立校验 `matlab/design/check_ddc_fir.m`。**P1-8「走通一条 Coder 链路」不由本步兑现，改由 M-3**（那里才是工具箱的主场） | 与 Python 参考四个算例最坏相对差 **0**（逐位相同）；标准算例第 7 项通过（阻带实测 91.1 dB、冲激对齐偏差 0 样点）；组件目录 22 → 23，产品 / 航迹 / 检测 / 评价基准与 `DEFAULT_CHAIN_TEXT` 零变化 | M-1、B-1 | [x] 2026-09-16 |
| M-3 | 信道化与接收滤波（**首条 Coder 链路，兑现 P1-8**） | `matlab/ref/cuav_pfb_cycle.m` + 六个钉死 M 的入口（多相 FFT）、`cuav_rx_fir.m`（群时延可测）→ `codegen` → `models/{channelizer,receiver}/coder/*.{c,h}`（25 + 11 个文件）→ engine `Channelizer` / `RxFilter`；封装层按 08 §13 五项职责手写，沿用 M-2 立下的冻结系数表与三方互证做法。**多级抽取（`decim > 20`）用户 2026-09-16 拍板推迟**（要动 `fir_lp_v1` 表与 `ddc.json` 基准，本期无场景需要，理由写进模型卡）。ADC 量化削顶与噪声注入保持手写 C++ | **已达成**：标准算例第 5 项（通带偏差 0.148 dB、阻带 61.13 dB、冲激对齐偏差 0 样点）与第 8 项（中心增益 0.00 dB、邻道抑制 63.64 dB、冲激对齐偏差 0 样点）；**算法核对 MATLAB 黄金向量 1.4e-16、对 Python 参考 5.7e-13（判据 1e-9）**，组件尺度对 Python 参考**逐位相同**；组件目录 23 → 25，其余基准零变化 | M-2 | [x] 2026-09-16 |
| M-4 | 三方参考互证 | Welch PSD（`pwelch`）、能量检测、ADC 量化：MATLAB 参考 + 现有 Python 参考 + C++ 引擎三方对拍；Python 参考保留为独立第二实现 | 切片 ① 的 `SpectrumAnalyzer` 以 `pwelch` 黄金向量验收；三方一致 rel ≤ 1e-9 | M-1 | [~] 2026-09-05：Welch 功率谱三方互证完成（`engine/tests/golden/spectrum_welch{,.matlab}.json`）；能量检测与 ADC 量化的 MATLAB 参考待做 |
| M-5 | 波形模板与非理想参考（P3） | 图传 OFDM / 遥控 FHSS 参考波形（comm 工具箱）、相噪 / IQ 不平衡 / AGC 参考、统计信道；实测与仿真特征对比工具（04 §11.4 七层）与模型卡证据包图表在 MATLAB 内部生成；参数拟合；输入含 §9F T-3 的模板，特征模式的参数拟合与 04 §11.4 七层比对在此做 | 随 P3 模型卡 | P3、T-3 | [ ] |
| — | MATLAB Runtime 独立工作进程（04 §8.5 第 2 档） | 首期不做；P3 若遇 Coder 不支持的复杂算法再评估，独立进程、不进主链 | — | — | OPEN |

## 9E. 纵向切片顺序（D-028「先打通薄的端到端切片再补厚设计」）

| 切片 | 步骤 | 切完在浏览器看到 | e2e 断言 |
|---|---|---|---|
| ① 一条链到频谱 | B-1…B-7（频谱）、P1-4a、U-1、U-3（频谱 + 瀑布）；并行 M-0、M-1、M-4 | 三视图壳（U-1 已交付：顶栏、三视图、抽屉与状态条、JSON 框图页、结果页信号页头显示 `dBm`）；提交 `Tone + Noise → AddMixer → SpectrumAnalyzer`（−70 dBm 单音、−104 dBm 噪声、2.44 GHz），日志滚进度，瀑布实时下滚；结束后缩放重取。**U-3 已交付（2026-09-06）**：频谱在上瀑布在下、最新行在顶、跟随实时追加、结束后自动转回看、框选 / 滚轮缩放重取、M1 / M2 marker 与读数、时间游标、包络竖条与色带、右栏仪表、导出 PNG / CSV | `__probe().task.state === 'finished'`；峰值 ≤ 1 bin；关 WS 再开序号连续；页面无外网请求；`SpectrumAnalyzer` 对 `pwelch` 黄金向量 rel ≤ 1e-9；缩放后重取行数 = `py`；结束收口窗尾 = 数据末尾；`M` / `Delete` / `←`；地图空闲后运行期间无 > 50 ms 长任务；刷新后已结束任务直接回看（`slice1-smoke` 共 39 项） |
| ② 场景里飞起来 | G-0…G-3、G-5、G-4、B-4 `--scenario-track`、08 报告定稿。**2026-09-06 全部交付** | 载入 demo 场景，点地图布站保存；运行绑定场景的框图，图标沿航线动、拖出航迹、链路线随之伸缩，瀑布功率随距离降并有多普勒；结束后从产品文件回放 | **已达成**：三时刻位置与 golden 差 **0**（容差 1e-6 度）；两时刻**带内**功率差与几何路损差偏差 **0.074 dB**（容差 0.5）。`slice2-smoke` 共 27 项 |
| ③ 拖出框图（**2026-09-10 由 D-060 撤销**：自由画布已删，本切片的画布部分不再有效；B-1 目录、B-3 包络、U-3 与目录驱动的参数控件照旧留用） | U-0（09）、B-1 `describe()` + `--catalog`、~~U-2~~、B-3 包络、U-3（包络 + 散点） | 六类组件库拖到画布、连线校验、参数表单带单位范围、S4 打观测点看频谱 / 包络 / 散点；报错高亮节点端口 | 序列化与 golden 等价；`IQStream → SceneParamFrame` 被拒且理由与 `--validate` 一致 |
| ④ 合流与厚化（**2026-09-07 D-051 重排为 ④a–④d，步骤见 §9G，细节见 10 报告 §8.2**） | ④a C-0、C-1、C-2、C-6、C-7、C-8；④b C-3、C-4、C-5、C-9、G-6；④c M-2、M-3、C-10；④d C-11、P1-7、P1-9a、U-4 余项、规范定稿、06 / CLAUDE 回填 | 框图页是一条九环节典型链路，选 `golden-02`、改噪声系数与天线增益即运行；S0 读发射功率、S1 读链路预算、S2 见噪声底、S3 见削顶计数；结果页出检测识别时间线与评价指标（图传识别为 `video_link`、遥控识别为 `rc_hopping`）；DDC / 信道化启用后 S4 采样率下降、S5 页签出现；数据中心「用于回放」直达典型链路 | `slice4-smoke`：三种模式编译过校验、保存后刷新仍是典型链路；S1 与链路预算 ≤ 0.05 dB；S2 底噪与 −174 + nf + 10·log10 fs ≤ 0.3 dB；`metrics.json` 与 Python 参考逐值一致；帧级 Pd 与解析预测差 ≤ 0.05；识别准确率 ≥ 0.9（原型阶段验证值）；标准算例 5 / 7 / 8；Coder 组件 rel ≤ 1e-9 |
| **⑥a 多源叠加**（D-053，插在 ④a 与 ④b 之间）| L-0 ✔、L-1、L-2 | 场景页用「布目标」放下第二个目标；框图页辐射源卡带 `×2`；S4 瀑布上两条同频谱线随两个源的几何各自起伏，第三条突发线闪烁 | 1×1 三模式往返逐字节且与 `DEFAULT_CHAIN_TEXT` 逐字节；3×1 往返；S4 同频双源合成线 = 两条链路预算的线性和（`stat=mean`、窗 ≥ 2 s，± 0.3 dB）；支路 `start_sample` 不一致即报错 |
| **⑥b 多站测向与交叉定位** | L-3、L-4、L-6 | 勾三个站；十一个环节蛇形排成 4 + 4 + 3、卡片带 `×3`；地图上三条测向线随目标转动，交汇处一个 AOA 椭圆；左栏读得到方位与 CEP | 3×3 往返逐字节；每站 S1 = 该站链路预算 ≤ 0.05 dB；移植 golden rel ≤ 1e-9；解析锚点 17.4532925 m / 34.906585 m / 20.5495 m；DF 蒙特卡洛经验 σ 差 ≤ 3%；真值落 2σ 椭圆内 ∈ [0.80, 0.93] |
| **⑥c 时差定位与融合** | L-5、L-7、L-8 | 「多站定位」切到 `tdoa` 出第二种颜色的椭圆；`aoa_tdoa` 两个解并存；`site-3` 的保持态让时统质量变化；`golden-01` 选 tdoa 时卡片上报「无站钟」 | GDOP 锚点 0.942809；`correlated_reference` 迹比 ∈ [0.95, 1.05]；`slice6-smoke` 20 项；既有五套 e2e 全绿 |
| **⑦ 选传播环境**（D-058，插在 ⑥ 与 ④b 之间）| R-0 ✔、R-1…R-7 | 传播信道卡片上写着这一档包含哪几项效应；右栏切到 E2 + 城市经验(urban)，瀑布上的电平整体下降十几 dB；切到地面双径，功率随距离起伏而不是单调下降；勾统计阴影，同一条航迹上的电平有缓慢的随机起伏；E3 选项置灰写「待 D3（切片 ⑤）」 | E1 档下 `DEFAULT_CHAIN_TEXT` 与两份 3×3 夹具往返逐字节、产品与航迹基准零变化；`open` 档城市经验对 `fspl_dB` rel ≤ 1e-9；天气 64 例 golden rel ≤ 1e-9；双径 A1–A5、阴影 C1–C5（12 §3）；E3 经 `--validate` 报错且落到 `ch` 卡片；`FreeSpaceChannel` + E2 被 `propagation` 检查拦住 |
| **⑧ 一屏讲清楚**（D-061，插在 ⑦ 与 ④b 之间，可与 C-3 并行）| V-0 ✔、V-1 ✔、V-2 ✔、V-3 ✔、V-3b ✔（信息分层，D-062）| 场景页右栏一开就列着三架机与三个站的卡，每站一行距离 / 电平 / 信噪比 / 测向 ± 2σ；地图上测向渐隐色带、2.5 px 椭圆、40 px 目标图标与选中环、链路线与距离标注、高度立柱、红色告警圈，目标入圈图标变红环；底部全宽时间轴拖到任一时刻，图标、测向线、信号游标同时到位，▶ 可回放；刷新后场景跟着最近任务 | **全部达成 2026-09-12**：`slice8-smoke` **40 项**（13 §11）；`slice2` 加「链路线渲染要素 ≥ 1」与新图层 id；卡片读数与端点行逐值相等；新色 ≥ 3:1；golden-03 航迹基准 723 样点逐点相同、只哈希变；拖到 30 s 后实体位置 = `track` 该时刻前最后一行、游标 = t − t0_s |
| ⑨ 精度场与方位图（D-061，待排期） | V-4、V-5 | 选中站点出测向直方与方位电平曲线；地图上开一层定位精度场或检测覆盖场，标「最佳精度 x m」 | GDOP 0.942809 网格复现；站址场值 = 链路预算 |
| ⑤ 遮挡接入（D 线，不进 P1 退出） | D3-0 ✔（07 报告，D-074）、D3-1…D3-8、D4 | 建筑后方 NLOS 时瀑布功率跌落、链路线第一次变红；点选地图任一点即出视距与刀口损耗 | **148 例 golden rel ≤ 1e-9，C++ 与 web 两侧各守一遍**；刀口 v=0 → 6.03 dB；点选建筑两侧一致；MSVC / GCC 构建过；**E1 / E2 档下产品与航迹基准逐字节不变**（换基座那一次除外，单独提交并按逐点相对差 ≤ 1e-12 重冻） |

纪律：每个切片以 e2e 探针断言 + WORKLOG 条目收口，切片之间不留「等下一条线」的半成品接口。

风险与坑（摘要，细节见 WORKLOG 2026-09-04「路线图修订」条目）：Windows 上 `spawn` 路径含空格与中文、子进程 stdout 编码、孤儿进程、读正在追加的产品文件；React Flow 与 `ws` 随包并进 THIRD-PARTY-NOTICES；三视图共享单个地图实例、信号页不挂载时瀑布不绘；参数帧按样点序号推进否则同种子不可复现；组件目录 golden 只比「已有条目不变」；09 报告标 PROVISIONAL 待与原型 html 对账。

---

## 9F. T 线：辐射源模板（实测数据集驱动的特定无人机信号再生；D-045，2026-09-05）

用户 2026-09-05 提出：以现有数据集为基础，按无人机的距离、方位与发射机性能，用数据集里的信号特征，叠加传播环境与侦察设备性能，给出针对该无人机的信号级侦测模拟。核实结论、三种模式与供给边界见 CLAUDE.md D-045，规范见 `docs/emitter-template.md`。要点：模板与位置无关、归一化到单位功率，数据集未记录的量都是场景配置项；首选特征等效再生；回放模式的信噪比上限 = 模板余量 − 裕度。**本线只记录，不拉前实现**（用户 2026-09-05 指示）：生成与施加组件仍按 G-3 / P1-3 / M-2 / M-5 的顺序，T-0 探针不依赖引擎、可随时排期，T-3 起随 P3。

| # | 步骤 | 交付物 | 验收 | 依赖 | 状态 |
|---|---|---|---|---|---|
| T-0 | 模板探针：用真实数据把口径与边界量化 | `algos/reference/t0_template_probe.py` + `data/iq/measured/t0-template-probe{.json,-report.md}` + 合成夹具单测 `tests/unit/test_template_probe.py`。输入：DroneRFb 训练集抽样（每机型 × 视距 / 非视距各 20 片，背景 37 片训练集）、DroneRFa `T0010` 三档（`band_state` 相同）与 `T0000` 背景；验收集拦截复用 `tools/freeze_holdout.py`。每片产出：无人机频段自动定位（相对同站背景中位谱的超出量、1 MHz 平滑、高出 6 dB 的最强连通段）、模板余量、同频背景污染、突发统计（约 50 µs 帧的带内功率序列）、归一化谱形、峰均比、视距 − 非视距带内功率差、DroneRFa 三档与 DA-6 交叉核对、按论文参数的一次量级核对 | 合成夹具提取误差 ≤ 5%；定位的频段与论文一致；视距 − 非视距差与论文约 20 dB 同量级；三档带内功率与 DA-6 的 27 / 18 / 16 dB 差 ≤ 1 dB；报告全部标「原型阶段验证值」 | 无（不依赖引擎） | [ ] 待排期 |
| T-1 | 模板规范 | `docs/emitter-template.md`：模板定位、三模式、供给表、`cuav-emitter-template/1` 字段、提取口径、回放模式的信噪比上限、进入仿真链的规则；三处登记（`docs/concept-model-extensions.md` §3 首条扩展、`docs/scenario-format.md` §4 / §9 的 `template` 波形类型、`docs/iq-format.md` §9） | 字段表齐；阈值待 T-0 回填后冻结 | — | [x] 2026-09-05 草案 |
| T-2 | 记录 | CLAUDE.md D-045、「已定」表与里程碑表 T 行；本节；§0.3；§9D M-5；§11.3 / §11.4 / §11.5 角色 ⑥；WORKLOG | — | — | [x] 2026-09-05 |
| T-3 | 模板提取器 | `tools/iq_template.py`：按 T-0 口径从索引与产物提取特征模式模板，写 `data/iq/templates/<template_id>.template.json`（小文件可入库）；回放模式模板只登记 `data_ids` 与哈希；MATLAB 侧参数拟合与七层比对走 M-5 | 模板过 schema；同输入逐字节复现；验收集拦截 | T-0 | [ ] |
| T-4 | 引擎 `TemplateSource` | 特征模式 = 带限成形噪声 + 突发门控（P3 换 OFDM / FHSS 参考，M-5）；回放模式 = 片段循环拼接 + 单位功率归一化 + 频移 / 重采样；`scene_bindable = true`，溯源带 `template_id → source data_ids`；模板文件经 `template_id` 引用、路径为内部参数（D-037 同法） | 再生波形与模板按 04 §11.4 七层比对在容差内；同种子逐字节复现；回放模式超出信噪比上限判 `degraded` | T-3、G-3 | [ ] |
| T-5 | 闭环与模型卡 | 场景合成辐射源引用模板 → G-3 施加信道 → 接收机链 → 检测；实测背景 + 合成目标；2–3 类重点机型模型卡与证据包（04 §7.2） | 跨层一致性算例 ④；P3 四组验证（D 组 = DroneRFb 验收集） | T-4、M-5 | [ ] |

## 9G. C 线：典型链路与检测识别评价（D-051，2026-09-07）

用户 2026-09-07 指示：框图不再自由组合，先给出固定的典型框架（04 §7 从辐射源发射机到接收机到 DDC 与信道化），用户在每个框图上编辑参数、运行、得到检测识别与评价；注重实用性，灵活性可适度降低。设计见 `10.典型链路框图与检测识别评价实施方案_v1.0.md`（下称 10 报告），决策 D-051。要点：一条固定的九环节链、三种信号源模式、只改参数；典型链路是标准框图的生成器（格式只加 `template_ref`）；U-2 自由画布降为高级模式；D-050 三问在此拍板；识别与评价首次进入路线图（P1-4e / P1-4f）。

| # | 步骤 | 交付物 | 验收 | 依赖 | 状态 |
|---|---|---|---|---|---|
| C-0 | 方案定稿与回填 | 10 报告 v1.0；CLAUDE.md D-051、状态表、文档表；本备忘录 §0.2 / §0.3 / §9B / §9E / §9G / §14；09 §17 修订行与 §6、§7.1 说明段；`docs/diagram-format.md` §10；`docs/component-catalog.md` §7；WORKLOG | 文档互引一致；D-050 三问各有结论 | D-050 | [x] 2026-09-07 |
| C-1 | 格式与接口 | 框图 `template_ref`（三处白名单 + schema + `web/src/diagram/doc.ts`）；端口类型 `RecognitionList`；`PortSpec.optional`；`SceneParamFrame` 七字段（`geo/` 算离开 / 到达角与航向，`ScenarioSource` 填 `tx_on / tx_center_Hz`）；`BlockMeta.clip_count`；场景 `emission.polarization`；错误码 `template` / `port_optional`；`docs/{diagram-format, scenario-format, component-catalog, display-products, api-versions}.md` 与 08 附录 | 目录黄金基准比对只有预期差异；`golden-01` 航迹与产品黄金基准零变化；`ctest` 与四套 e2e 无回归 | C-0 | [x] 2026-09-07：端口类型 7 种、`PortSpec.optional`、`template_ref`、`SceneParamFrame` 七字段、`BlockMeta.clip_count`、`emission.polarization`、错误码 `template`；引擎 121 项单测 + 8 项 ctest、服务 111 项、web 133 项、四套 e2e（14 + 39 + 27 + 29）全绿；目录黄金基准只多 `RecognitionList` 一种类型与 13 条兼容行 |
| C-2 | 天线、前端、ADC 三件 + 既有两件加参 | `engine/include/cuav/components/{antenna, receiver}.h`、`engine/src/{antenna, receiver, adc}.cpp`（`AntennaGain` 解析式方向图 + 指向 + 极化；`ReceiverFrontEnd` 噪声系数生噪 + 增益 + 频偏 + IQ 不平衡 + 直流；`AdcQuantizer` 位数 / 满量程 / 削顶计数）；`SceneEmitterSource.emit_at_tx_power`、`SceneBoundChannel.gain_mode`；`models/{antenna, receiver}/README.md` 模型卡 | 10 §3.2 / §3.4 / §3.5 解析锚点单测全过；噪声蒙特卡洛矩校验过；`AdcQuantizer` 与 M-4 的 MATLAB 量化参考 rel ≤ 1e-9；全合成链 S1 与链路预算三点偏差 ≤ 0.05 dB（带内功率法） | C-1 | [x] 2026-09-07：`AntennaGain`（解析式方向图 + 指向 + 五档极化 + 馈线，`scene` 为可选口）、`ReceiverFrontEnd`（噪声系数生噪 + 增益 + 频偏 + IQ 不平衡 + 直流，私有随机子流）、`AdcQuantizer`（位数 / 满量程 / 削顶计数，标记不降级）；`SceneEmitterSource.emit_at_tx_power` 与 `SceneBoundChannel.gain_mode` 加参且缺省保旧行为；观测点索引加 `clipped_samples`；两份模型卡 `models/{antenna,receiver}/README.md`。组件 12 → 15，六类首次全有组件。引擎 142 项单测 + 8 项 ctest、服务 111 项、web 134 项、四套 e2e 全绿；**口径等价实测：`golden-01` 上拆开四件与折在一处的接收电平差 < 0.001 dB（验收线 0.05）** |
| C-3 | 检测升级与落盘（P1-4c / P1-4d） | `EnergyDetector.noise_mode = sliding`（带删截滑动噪声估计）、`merge_gap_frames` 突发分段、`Detection` 增字段；观察者 `on_detection`；`cuav_run` 写 `detections.jsonl`；`algos/reference/energy_detector.py` 加滑动模式 | `probe` 模式黄金基准不变；`sliding` 与 Python 参考逐帧一致；DS-6 分半标定在 `sliding` 下不再失效（D-026） | C-1 | [x] 2026-09-12（D-063）：probe 基准逐字节不变；`energy_detector_sliding.json` 4000 帧逐帧对拍 rel 4.1e-7、命中 / 段号 / 环大小完全相同；缺省链（golden-01，6 s）从 3.015 s 起持续检出 1457 帧一段、开机前 1 帧虚警；**第三条验收未达成且是发现**——分半标定比值 7.27 / 8.30（静态 7.20 / 7.00），失配在 99.9% 尾部的真实 WiFi / 蓝牙突发，滑动估计归一的是主体（安静频段逐片中位数散布 1.32 → 0.52 dB），见模型卡 §5 |
| C-4 | 特征与识别（P1-4e） | `FeatureExtractor`（M3 观测量提取）、`TemplateClassifier`（EM-S-04 E2 模板加权匹配，开放集）；`models/recognition/library-v1.json` 与模型卡；`features.jsonl` / `recognitions.jsonl`；`algos/reference/{features, classify}.py` | 四类合成波形 `SNR ≥ 10 dB` 下识别到 `signal_role` 层准确率 ≥ 0.9（原型阶段验证值，样本 ≥ 40 突发）；特征与 Python 参考逐值一致 | C-3、G-6（`rc_hopping` 一类） | [x] 2026-09-14（D-066；准确率 0.982）；**`rc_hopping` 一类 2026-09-15 随 C-8 / G-6 销项**（golden-02 上 180 行、准确率 1.0，D-069） |
| C-5 | 真值与评价（P1-4f） | `Evaluator`（真值 = 场景参数帧 / 数据清单 / 无；帧级 Pd / Pfa / F1、突发级检出与发现时延、ROC 门限扫描、混淆矩阵）；`truth.jsonl` / `metrics.json`；`algos/reference/evaluate.py`；`task.json.metrics_summary` | `metrics.json` 与 Python 参考逐值一致；跨层算例 ① 典型链路实例差 ≤ 0.05 | C-3、C-4 | [x] 2026-09-14：逐值一致（golden-01 / golden-03 / 回放 / 跨层实例四份运行）；跨层 ① 差 **0.0138**（解析 0.7164 对蒙特卡洛 0.7026，突发源，首跑即过未调参）。`Evaluator` 不写文件、经观察者上报，运行器拼 `metrics.json`（`sites[]`）；调度器加 `accepts_partial_inputs()`；D-067 |
| C-6 | 服务端（B-8 框图落盘 + 结果端点） | `GET/PUT/DELETE /api/v1/diagrams[/{id}]`（`data/diagrams/` 入库，规范序列化同场景文件，`--validate` 不过 400 不落盘）；`GET /api/v1/results/{task}/{features \| recognitions \| truth \| metrics}` | 保存后 `GET` 逐字节相同、不改内容再保存是空操作；四个结果端点在假引擎夹具上可读 | C-1 | [x] 2026-09-07：`server/src/diagrams.ts`（`GET/PUT/DELETE /api/v1/diagrams[/{id}]`，与任务提交共用抽出来的 `prepareDiagram()`）；结果端点加 `features` / `recognitions` / `truth`（JSONL）与 `metrics`（整文件）；`data/diagrams/` 入库、`.tmp` 与旁挂不入库。服务 116 项测试全绿 |
| C-7 | 典型链路视图（U-5） | `web/src/chain/{ChainView, SlotCard, TapDot, ExperimentSetup, FrequencyPlan}.tsx`、`templates/chain-v1.ts`（黄金基准 `tests/golden/chain-template-v1.json`）、`{compile, parse, plan}.ts`（纯函数）；路由 `#/diagram` 指典型链路、`#/diagram/canvas` 指自由画布；`App.tsx` 初始框图改为最近保存或内置缺省；探针 `app.chain` | 三种模式各编译出框图并过 `--validate`；`compile(parse(doc)) === doc`；频率计划六项检查对拍算例；改站点参数带入且「已手改」不覆盖；引擎四元组错误落到卡片；`slice1 / slice3-smoke` 改入口后全过 | C-1、C-6 | [x] 2026-09-07：`web/src/chain/{model,plan,compile}.ts`（纯函数）+ `{ChainView,SlotCard}.tsx`；路由 `#/diagram` 指典型链路、`#/diagram/canvas` 指自由画布；内置缺省链路 `examples/default.ts` 由生成器产出并被单测守住；探针加 `app.chain`；`Ctrl+S` 接到 C-6 的端点。web 149 项单测、新增 `slice4-smoke` 25 项，四套既有 e2e 无回归 |
| C-8 | 场景 `golden-02` 与 G-6 | 宽带场景（`fs 10 MS/s`、`bw 8 MHz`、`nf 6`；图传 `noise 2 MHz` 长发 + 遥控 `burst` 跳频两个辐射源）；G-6 活动时间线施加到 `SceneEmitterSource` 并写帧 | 场景一致性测试过；突发起止与跳频在瀑布上与时间线差 ≤ 一帧；同种子逐字节复现 | G-2、G-3 | [x] 2026-09-15（D-069）：`golden-02` 单站两源、频率上不重叠、时序先错开末尾叠一段；`noise` 加 4 阶巴特沃斯带限与频率搬移（此前既不带限也不搬移，2 MHz 图传会铺满全带）；**识别准确率 1.0、`rc_hopping` 180 行——C-4 欠的那笔验收销项**；`slice9-smoke` 18 项 |
| C-9 | 结果页两页签 | `web/src/results/{DetectionView, DetectionTimeline, EvaluationView, ConfusionMatrix, RocPlot}.tsx` + 纯函数 `{timeline3, analytic, labels}.ts` + `truthStore.ts`；`ResultsTab` 增 `evaluation`（`detections` 页签 C-3 已建，故只增一个值）；信号页「叠加检测」的框按识别标签着色并写标签名（V-8）；引擎 `DetectionSummary.m_bins` → `detections.index.json` | 时间线三行与信号页游标联动；点检测行游标移动；指标卡四态；`?dev=1` 下叠解析预测 | C-5、C-6、U-3 | [x] 2026-09-15（D-068）：三行时间线（真值 / 检测 / 识别，自带轴、只画焦点站）、检测列表加「突发 / 帧」粒度、识别列表、评价页签（五张指标卡带四态徽标 + 突发级 + 对数横轴 ROC + 混淆矩阵与 per_class）；`analytic.ts` 对 Python 参考守 `tests/golden/analytic-pd.json`（135 点 rel ≤ 1e-9）；`pfa` 全 null 那一支只画轴与一行事实。产品与航迹黄金基准零变化（12 个 `.f32` 与 12 份观测点索引逐字节相同，`metrics.json` 只差 `task_id`）|
| C-10 | DSP 厚化接入 | `DDC`（M-2）与 `Channelizer`（M-3）进槽位（M-2 / M-3 各自放开，缺省旁路），`RxFilter` 串进前端（新子槽位 `rx_flt`，`group: 'rx_fe'`，链上排在前端之前）；`fir_version` 枚举与频率计划过渡带检查对齐（新增 `web/src/chain/firSpecs.ts` 与两条检查项）；新夹具 `chain-golden-02-dsp.json` 三级全开 | **已达成**：标准算例 5 / 7 / 8 从保存的典型链路跑通——三级链上图传带内功率 −47.163 → −47.227 → −47.161 dBm（每级 ≤ 0.07 dB，累计 +0.002 dB）；接收滤波对拍两次运行的 S3，静默窗口 3906 帧 × 1024 bin **逐位相同**、通带中位差 −0.048 dB；Coder 组件 rel ≤ 1e-9 由 M-3 兑现。**缺省链不接**（零基准变更） | M-2、M-3、C-7 | [x] 2026-09-17 |
| C-11 | 收口 | `tests/regression/diagrams/chain-{synthetic, replay, mixed}.json` 三份模式夹具 + 配套场景 `scenarios/mixed-wideband.scenario.json`（混合增强要 80 MS/s 短窗才对得上实测背景）；`tests/regression/standard_cases.py`（接进 `build-all.sh`）；标准算例 1 / 2 / 3 / 6 / 9 / 10 / 11 从保存的典型链路生成；~~`docs/diagram-canvas-guide.md` 改为两章~~（已由 D-060 于 2026-09-10 完成）；06 / CLAUDE 回填 | **已达成**：10 报告 §9 验收清单全过——十二项标准算例完成十项（4、12 随后续）、电平链 S0 = 27.0000 dBm / S1 +0.081 dB / S2 − S1 = 20.000 dB、三份模式夹具往返逐字节、同种子 24 个产品文件逐字节相同；性能记录 golden-02 20 s @ 10 MS/s DDC 启用 = 墙钟 30.86 s、实时因子 0.648（未触发多线程评估） | C-2 … C-10 | [x] 2026-09-17 |

C-2、C-3、C-6 互不依赖可并行；C-4 只有 `rc_hopping` 一类等 G-6。与既有步骤的对应：P1-0b 由 C-0 兑现；P1-3 余项由 C-2 兑现；P1-4c / d 由 C-3 兑现，新增 P1-4e = C-4、P1-4f = C-5；P1-7 输入改为保存的典型链路（C-11）；U-4 只剩任务列表与数据中心；T-4 `TemplateSource` 将来作 `tx` 槽位的第四个变体，不改链。

## 9H. L 线：多源、多站、测向与定位（D-053，2026-09-09；04 之外，PROVISIONAL）

用户 2026-09-09 问：「场景中的辐射源可能有多个；单个的侦测站要做到信号的识别与测向；多个测向站可以对同一个信号源做交叉定位或者时差定位。这些在框图里面如何体现、如何设置？」并拍板交付范围为**设计 + 纯软件第一切片**。设计见 `11.多源多站测向定位实施方案_v1.0.md`（下称 11 报告），决策 D-053。要点：多源按源分支、接收天线后叠加成一路；多站 = 典型链路加站点维度、尾部两个新槽位；测向与到达时间是 **M2 效应模型**（取真值的合法边界正式声明在 11 §1.3，每行带 `truth_consumed`）；三种新报告端口 + 固定可选口；emcore 的 AOA / TDOA / 测向与四份 golden 移植到 `geo/` 并改平面坐标接口。

| # | 步骤 | 交付物 | 验收 | 依赖 | 状态 |
|---|---|---|---|---|---|
| L-0 | 方案定稿与回填 | 11 报告 v1.0；CLAUDE.md D-053、文档表、已定表、里程碑表、上游修正段；本备忘录 §0.2 / §0.3 / §1.2 / §9E / §9H / §14；10 报告修正表；`docs/concept-model-layers.md`；WORKLOG | 文档互引一致；§1.3 真值边界与 §10 不做各有结论 | D-051、D-052 | [x] 2026-09-09 |
| L-1 | 格式与接口 | 端口 `BearingReport` / `ToaReport` / `PositionReport` + `port_compat` 100 行；`SceneParamFrame` 加 `link_id / site_id / emitter_id`；`scene_binding` 双绑定放宽（`oneOf → anyOf` + 目录闸）；`channel.cpp` 多站守卫替换为三分支；`IComponent::check_wiring()` + 启用 `port_optional`；`IRunObserver::on_bearing / on_position` + 运行器两个 JSONL；场景 `sites[].clock`；`docs/{diagram-format, scenario-format, component-catalog, display-products, api-versions}.md` | 目录基准只有 11 §7.3 列的预期差异；`golden-01` 航迹与全部产品基准零变化；ctest 与五套 e2e 无回归 | L-0 | [x] 2026-09-09：端口 7 → 10（既有 49 行兼容矩阵逐字未变）、帧三身份字段、双绑定放宽（闸门在 `inject_scene` 的早退**之前**才生效，`FakeBindable` 那个单测正好替我踩出这个洞）、多站守卫替换为三分支（旧行为原样保留）、`check_wiring` + 启用预留错误码 `port_optional`、观察者两回调与两个 JSONL、`sites[].clock`。目录基准差异经脚本逐键核对只有预期三类；产品与航迹基准零变化 |
| L-2 | `Superposition` + 多源模板 | 组件（`in1..8` 可选口、`min_inputs`、四项一致性检查）；`model.ts` 的 `SlotDef.per`、`compile.ts` 的实例 id / 顺序 / `sup` 隐含节点 / `parseChain` 反解、`plan.ts` 多源检查；试验设置栏目标复选 + `×N` 徽标；`scenarioOps.addEmitter` + 「布目标」工具；`golden-03` 场景 + 黄金航迹 | 1×1 三模式往返逐字节且与 `DEFAULT_CHAIN_TEXT` 逐字节；3×1 往返；S4 同频双源合成线 = 两条链路预算的线性和（`stat=mean`、窗 ≥ 2 s，± 0.3 dB）；支路 `start_sample` 不一致即 `Error` | L-1 | [x] 2026-09-09：`Superposition`（八个可选口、四项一致性报错不截断）；`SlotDef.per` 三维度、实例 id 与 `splitNodeId`、`sup` 隐含节点、`parseChain` 反解、复选站源与 `×N` 徽标、`addEmitter` 与「布目标」、`golden-03` 与黄金航迹。**同频双源在 S1 的合成功率对两条链路预算的线性和偏差 −0.001 dB**（线 ±0.3）；3×3 编译出 45 节点 72 连线并过 `--validate`。两处设计错误由真引擎暴露（判据该看场景站数不是选中数；只有信道要双绑定） |
| L-3 | `DirectionFinder` + 多站模板 | 组件 + `geo/df_error.h`（七分量预算、DF-Q 分档）；`df` 槽位；`siteIds[]`、`scn__<site>`、站后缀、观测点实例化、`bindOf` 三态、`report_entities` 只开第一个；频率计划第 9 项 `sites_consistent`；`×K` 徽标、实例查看器、3 × 4 版式；`bearings.jsonl` + 端点 + WS 事件；`sceneStore` + 测向线叠加层；辐射源表单测向读数；模型卡 `models/locate/README.md` §DF | 3×3 往返逐字节；每站 S1 = 该站链路预算 ≤ 0.05 dB；每个 (站, 源) 都有报告行；1e4 帧经验 σ 与预算差 ≤ 3%；同频双源在近站出现 `mixture`；色值对比度 ≥ 3:1 实测 | L-2 | [x] 2026-09-09：`geo/df_error.h` 七分量预算与 DF-Q 分档；`DirectionFinder`（八可选口、场景派生信噪比、同频混叠三条件判据）；`df` 槽位、`siteIds[]`、站后缀、观测点实例化、`bindOf` 三态、`report_entities` 单开；`bearings.jsonl` + 端点 + WS；Canvas 叠加层与读数；三色对比度实测最低 3.57:1。**1e4 帧经验 σ 差 0.80%；1800 行 |误差| ≤ 3σ 占 99.56%、归一化方差 1.0225；逐站 S1 对三源预算和差 ≤ 0.013 dB** |
| L-4 | `MultiSiteLocator(aoa)` + 移植 | `geo/{fix_geometry, locate_aoa}.h` + `legacy` 回放；`loc` 槽位、频率计划第 10 项 `stations`、`check_wiring`；`positions.jsonl` + 端点 + WS；AOA 定位点与 2σ 椭圆；`algos/reference/aoa_fix.py` | 两份 golden rel ≤ 1e-9；解析锚点 σ_pos 17.4532925 m / 2σ 半轴 34.906585 m / CEP 20.5495 m；协方差迹比 ∈ [0.95, 1.05]；与 Python 逐值一致；真值落 2σ 椭圆内 ≈ 86.5% ± 3%（n ≥ 1000） | L-3 | [x] 2026-09-09：`geo/{fix_geometry,locate_aoa}` 移植 + golden 43 / 6 / 42 组 rel ≤ 1e-9；`MultiSiteLocator(aoa)`（纯函数式，站址随报告走）；`loc` 槽位；`positions.jsonl` + 端点 + WS + 椭圆；`aoa_fix.py` 对拍 600 解 rel 1.68e-12。**锚点 17.4532925 / 34.906585 / 20.5495 m 逐位命中；有效档 2σ 覆盖 87.0%、50% 半径 / CEP = 0.920**。查清 emcore 几何分级看不见近简并（§11.3 第 2、3 条） |
| L-5 | `ToaEstimator` + `MultiSiteLocator(tdoa, aoa_tdoa)` | 组件（隐含节点 `toa__<site>`，站钟全部自场景、缺 `clock` 即失败）；`geo/locate_tdoa.h` 两种加权 + golden；TQ 分档、可行性门降级不裁、`unsynced → TQ-4` 剔除；TDOA 与融合色；`algos/reference/tdoa_fix.py`；模型卡 §TOA / §TDOA | golden rel ≤ 1e-9；GDOP 锚点 0.942809；`correlated_reference` 迹比 ∈ [0.95, 1.05] 且 `independent_pairs` 同样本 > 1（数值进模型卡，不调参）；`golden-03` 的 `site-3` 行 TQ ≥ 2；`golden-01` + tdoa → `--validate` 报 `param` 并落到「多站定位」卡片 | L-4 | [x] 2026-09-09：`ToaEstimator`（隐含节点、站钟自场景、缺 clock 即拒）；`geo/locate_tdoa` 两种加权 + golden 36 / 128 组；`MultiSiteLocator` 三档；`tdoa_fix.py`。**GDOP 锚点 0.942809 逐位命中；相关加权迹比 1.004–1.013、独立加权 0.755–0.915（保守，纠正计划的假设）；时差中位误差 7.95 m，几何良好且扣掉钟差偏差后 2σ 覆盖 85.3%** |
| L-6 | 服务端与回看 | `JSONL_KINDS` 加两项 + `site_id / emitter_id / method` 过滤；`useSituation` 回看并行拉两个端点；定位读数块；图层开关「测向线与定位椭圆」 | 服务测试假夹具两例可读；刷新后已结束任务的地图仍画线与椭圆 | L-3、L-4 | [x] 2026-09-09（随 L-3 / L-4 一并落地）：两个 JSONL 端点与 `JSONL_FILTERS` 等值过滤；结束后从文件整批补齐测向与定位；定位读数块与图层开关。刷新后已结束任务的地图仍画线与椭圆，由 `slice6-smoke` 验 |
| L-7 | e2e 与回归夹具 | `tests/e2e/slice6-smoke.mjs`（20 项）；`tests/regression/diagrams/chain-3x3-{aoa,tdoa}.json`；`slice4-smoke` 更新为 11 环节 / 4 + 4 + 3 断言 | 11 §9.2 全过；既有五套 e2e 全绿 | L-2…L-6 | [x] 2026-09-09：`tests/e2e/slice6-smoke.mjs` **52 项**；回归夹具 `tests/regression/diagrams/chain-3x3-{aoa,tdoa}.json`（49 / 52 节点）并在 README 记了分工；`slice4-smoke` 更新为 11 环节 / 4 + 4 + 3 版式 |
| L-8 | 文档收口 | `docs/{component-catalog, diagram-format, scenario-format, display-products, api-versions, display-route, concept-model-layers, diagram-canvas-guide}.md`；08 报告附录的组件 / 端口 / 错误码计数；WORKLOG；CLAUDE.md 里程碑 | 文档与 `cuav_run --catalog` 逐项核实 | L-7 | [x] 2026-09-09：`docs/{component-catalog, diagram-format, scenario-format, display-products, api-versions, display-route, concept-model-layers, diagram-canvas-guide}.md`；08 报告端口表 / 组件数 / 内部参数 18 处 / 错误码说明；模型卡 `models/locate/README.md`（234 行）；11 报告 §11.3 记六条偏离 |
| L-9 | （随 C-5 / C-9）定位评价 | `Evaluator` 加 `localization` 指标进 `metrics.json`；「评价」页签的定位表，标注「模型自洽性检查」而非「定位精度指标」 | 与 Python 参考逐值一致 | C-5、C-9、L-5 | [ ] C-5 已在 `metrics.json` 顶层预留 `localization: null`（2026-09-14）；定位是全图唯一的融合节点，故在顶层而不在按站的分节里 |

### 附：E 线（实体设备参数配置，D-054，2026-09-09）

用户在切片 ⑥ 收口后指示框图页要成为「场景内实体电子设备参数配置的主界面」。它不新起一条长线——
一次做完，作为 L 线的收尾补丁记在这里。

| 步 | 目标 | 交付物 | 验收 | 前置 | 状态 |
|---|---|---|---|---|---|
| E-1 | 场景设备型号字段 | `docs/schemas/scenario.schema.json` 与 `docs/scenario-format.md` 加可选 `equipment_model`（站点与辐射源各一）；`engine/src/scenario_json.cpp` 两处键表 + 可选读取器；`geo::Site` / `geo::Emitter` 各加一个字段 | 带字段与不带字段的场景都解得开、类型写错被拒；示例场景不改 | L-8 | [x] 2026-09-09 |
| E-2 | 共用的设备字段定义 | 新 `web/src/scene/editor/deviceFields.ts`；`ObjectForm` 改读它；`setPath` 支持按需建中间对象与「写 undefined 即删键」 | `slice2-smoke` 27 项不变；`scenarioOps.test.ts` 加三例 | E-1 | [x] 2026-09-09：顺带放出自 D-053 / D-051 起有格式却一直没有界面入口的 `sites[].clock` 与 `emission.polarization` |
| E-3 | 两层参数模型与往返 | `model.ts` 的 `SlotDef.owner` / `SlotConfig.byEntity` / `effectiveParams` / `paramScope` / `writeParam`；`compile.ts` 的 `slotParams(id, em, site)` 与 `parseChain` 归约 | 逐实体覆盖往返逐字节；归约确定且幂等；同实体多实例不一致仍返回 null；**3×3 回归夹具逐字节相同**且不产生 `byEntity` | E-2 | [x] 2026-09-09 |
| E-4 | 场景带出的设备参数（**基准变更**）| `model.ts` 的 `FROM_SCENE`；`missingParams` 加「场景给不给得出」的判据；夹具 `tx_ant__uav-3__*` 2 → 0；`slice6-smoke` 预算式改逐源取值 | `DEFAULT_CHAIN_TEXT` 逐字节不变；夹具只允许那一处差异；`slice6` 统计带实测在带内 | E-3 | [x] 2026-09-09：查清场景里逐源的天线增益一直被静默忽略，`uav-3` 链路预算高估 2 dB |
| E-5 | 实体选择器与三档范围 | `ChainView` 的 `.chain-entity-bar` 两个下拉 + 焦点回退；`SlotPanel` 的参数归属说明、场景字段组、共用 / 同型号 / 单独三档；探针加 `chain.byEntity` 与 `chain.effective`；删掉 `DERIVED_OF` 的重复定义 | 选 site-2 改只动 site-2；选同型号只动同型号；传播信道标共用且不给单独设置入口 | E-3 | [x] 2026-09-09：范围必须记在组件本地——它是派生量，只改下拉不改值会被归约当场打回 |
| E-6 | 场景自动保存 | `saveScenario` 抽到 `shell/actions.ts`，`SceneView` 改调它；`ChainView` 去抖 800 ms 自动触发、防重入、失败不清 dirty | 改完不点按钮即存下；`scenario_ref.sha256` 跟着回填 | E-5 | [x] 2026-09-09 |
| E-7 | e2e 与文档 | `slice4-smoke` +11 项、`slice6-smoke` +6 项；`tests/unit/test_scenario_example.py` 加规范形式守卫；CLAUDE.md D-054 与已定表、WORKLOG、10 / 11 报告修正 | 六套 e2e 234 项全绿；文档互引一致 | E-4、E-6 | [x] 2026-09-09 |

**E-4 与 E-7 之间查出一处陷阱**：`golden-03` 从未按 D-049 ⑧ 的规范序列化形式落盘（浮点写成 `120.0`，
7497 → 7473 字节），第一次自动保存即被规范化，航迹黄金基准与两份夹具记着的场景哈希同时失效。
处置是保留规范化后的文件、重生成航迹基准（723 个样点逐点相同，只 `scenario_sha256` 变）、
更新夹具哈希，并加守卫让它不能再悄悄发生。

与 C 线的关系：L 线插在 ④a 与 ④b 之间，L-1 至 L-8 完成后再进 C-3。首片测向门控 = 帧的 `tx_on` 且 SNR ≥ `min_snr_dB`；C-3 落地后 `det` 口可接实际检出作门控，这要求 `Detection` 带频率与源标识（C-3 字段增补的理由之一）。11 报告 §11.2 列出对 10 报告的四处修正（`detections.jsonl` 与 `truth.jsonl` 加 `node_id / site_id / emitter_id`、`Evaluator` 改绑站、`metrics.json` 按站分节并预留 `localization{}`），**只改 C-3 / C-5 的设计，不并入本次实现**。

---

## 9I. R 线：传播环境与效应选择（D-058，2026-09-10；04 之外，PROVISIONAL）

用户 2026-09-10 指示：「传播信道这个环节要反映侦测站与无人机目标所处的传播环境、地理空间、建筑物分布带来的变数——
自由空间、城市环境、是否考虑遮挡、绕射、多径，以及其他由传播环境带来的影响与效应；在这个框图里选择要考虑哪些因素，
可以从简单选到细致。」设计见 `12.传播环境与效应选择实施方案_v1.0.md`（下称 12 报告），决策 D-058。

核实到的现状（12 §1.1）：`SceneBoundChannel` 与 `FreeSpaceChannel` **今天在物理上是同一个模型**——都是自由空间路损，
区别只在距离是场景几何算出来的还是用户手填的；`geo/src/link_budget.cpp:32,60-61` 里 `line_of_sight` 是字面 `true`、
`extra_loss_dB` 是字面 `0.0`，场景数据包里 47582 栋建筑一栋也没进过链路计算。

要点：档位用 01 的 E1–E4 并直接写进溯源的 `model_level`；**E1 = 今天的行为、缺省**，E2 = 本期，E3 待 D3；
**算在 `geo/`（帧生产端）、配在 `ch` 卡片**（槽位表加 `proxy` 把 `ScenarioSource` 的参数代理过去）；
防重复计损 = 一条规则（至多一个替代型主模型）+ 两道闸（界面单选、运行时报错）；链路报告附 `included_loss_terms`；
帧结构一个字段不加。

| # | 步骤 | 交付物 | 验收 | 依赖 | 状态 |
|---|---|---|---|---|---|
| R-0 | 方案定稿与回填 | 12 报告 v1.0；CLAUDE.md D-058、文档表、已定表、里程碑表 R 行、上游修正段、**`ChannelPathSet` 表述改正三处**；本备忘录 §0.2 / §0.3 / §9E / §9I / §14；WORKLOG | 文档互引一致；三条拍板各有结论 | D-054、D-057 | [x] 2026-09-10 |
| R-1 | 组合框架与档位 | `geo/{include/cuav_geo,src}/propagation.{h,cpp}`：`PropagationLevel` / `PrimaryModel` / `EnvClass` / `GroundType` 四枚举、`PropagationConfig` / `PropagationTerms`、组合裁决与 `included_loss_terms`；`link_budget()` 接配置（默认实参，既有调用点不改）；`LinkGeometry` 加收发离地高度；`ScenarioSource` 加 15 个参数与两道闸；`LinkFrame` 加三项、`link` 事件与 `links.jsonl` 各加三键 | **E1 档逐数值等于今天**：`DEFAULT_CHAIN_TEXT`、两份 3×3 夹具、产品与航迹黄金基准、六套 e2e 硬断言一个数字不动；组件目录基准只多 15 个参数 | R-0 | [x] 2026-09-10：`geo/propagation.{h,cpp}`（四枚举 + `PropagationConfig` / `PropagationTerms` + `combine()` + `validate()` 两道闸）；`link_budget()` 与 `LinkFrameSource::build()` 各加一个默认实参，既有调用点与单测一字未改；`ScenarioSource` 加 15 个参数；`LinkFrame` 与 `link` 事件 / `links.jsonl` 各加三项。**收口靠字节比对**：拿上一版提交建出的 `cuav_run` 跑同样两份框图，`slice1` / `slice2` 的 `.f32` 与索引**逐字节相同**；slice2 的 0.074 dB、slice6 的 0.002 / −0.013 / 0.001 dB 一个数不动。比对当场揪出一处缺陷：`init()` 无条件从共享流抽 u64 会把下游所有组件的子种子挪位 |
| R-2 | 大气与降雨（移植 emcore） | `geo::atmospheric_loss_dB` / `rain_attenuation_dB` 逐字移植并**保留原常数与原分段**（D-009）；`tests/golden/propagation.json`（自 emcore 拷入，`_meta` 记来源与去处）；对拍单测 | 64 例 golden rel ≤ 1e-9；模型卡写明 2.4 GHz / 20 km 上是 0.1 dB 量级 | R-1 | [x] 2026-09-10：两式逐字移植并保留原分段原常数；`tests/golden/propagation.json` 自 emcore 拷入（`_meta` 记来源与去处、记明只取两组）。**64 例 rel ≤ 1e-9**。核实到实施计划的一处假设有误：这两式**都不含光速常数**，D-009 的常数冲突要到 D3 移植 `fresnelV` 才出现 |
| R-3 | 地面双径（EM-P-02） | 镜像法几何、复介电常数与 Fresnel `Γ_H` / `Γ_V`、Rayleigh 粗糙度、部分相干叠加、相消限幅、断点距离、fade 五档；材质表六行（取自 `天津机场_地表材质与建筑数据工作流.md` §3.5） | 解析锚点 A1–A5（12 §3.2）；`σ_h → ∞` 退化为 FSPL；`h ≤ 0` 降级不静默 | R-1 | [x] 2026-09-10：镜像法几何、复介电常数与 `Γ_H` / `Γ_V`、Rayleigh 粗糙度、部分相干叠加、限幅、断点距离、fade 五档、材质表六行。锚点 A1–A5 全过。**两处按物理改了断言而不是改模型**：A1 的掠射极限必须用水平极化（`Γ_V` 有伪 Brewster 凹陷，0.7° 掠射上 `|Γ_V| = 0.80` 而 `|Γ_H| = 0.997`）；A4 的 `d⁻⁴` 要把限幅放宽过零点深度，否则两端都被限住、斜率恰好退回 20 dB/十倍程 |
| R-4 | 统计阴影（EM-P-08） | `INormalSource` 抽象；σ 表（环境 × 视距）、`d_corr`、沿航迹一阶递推；序列在 `ScenarioSource::init()` 里按帧序一次算完 | 锚点 C1–C5（12 §3.4）；固定种子逐位复现；`frame(k)` 仍 `const` 且乱序调用结果相同 | R-1 | [x] 2026-09-10：`INormalSource` 抽象 + `ShadowSequence`；σ 表（环境 × 视距）、`d_corr`、沿航迹一阶递推；序列在 `init()` 里按帧序一次算完（`frame(k)` 的无副作用是硬不变量）。**40000 帧经验 σ 6.0015（设定 6，差 0.03%）、均值 −0.0196；Δs = 20 m / d_corr = 50 m 的相邻帧相关 0.6687（理论 0.6703，差 0.24%）** |
| R-5 | 城市经验（EM-P-05 的 E1 档） | log-distance + 环境类别表（`n` / `X_env`）、`ref_distance_m`、`urban_loss_mode` 与闸二 | 锚点 B1–B4（12 §3.3）；`open` 档与 `fspl_dB` rel ≤ 1e-9；闸二报错有测试钉住 | R-1 | [x] 2026-09-10：log-distance + 环境表 + `urban_loss_mode` 与闸二。锚点 B1–B4 全过，`open` 档与 `fspl_dB` 逐值相等（≤ 1e-9 dB）。近区（`d ≤ d0`）退回自由空间并标降级——`golden-01` 的航线会飞到距站 33 m，300 帧里有 105 帧走这一支，是设计行为 |
| R-6 | 界面 | `SlotDef.proxy` 与 `PROXY_PARAMS`；`compile` / `parseChain` 的代理划分与一致性核对；`ch` 卡片效应清单（`data-slot-effects`）；右栏「传播效应 · 全图共用」分组与显隐规则；E3 置灰；`planChecks` 第 11 项 `propagation`；`web/src/chain/effects.ts` | 往返逐字节（`DEFAULT_CHAIN_TEXT` 与两份 3×3 夹具）；`slice4` / `slice6` 加断言；E3 经 `--validate` 报错并落到 `ch` 卡片 | R-1…R-5 | [x] 2026-09-10：`SlotDef.proxy` + `PROPAGATION_PARAMS`；`compile` / `parseChain` 的代理划分（**显示什么就写什么**，不生效的收进 `inactive_slots`）；`web/src/chain/effects.ts`；卡片效应清单 `data-slot-effects`；右栏「传播效应」分组与显隐；E3 置灰；`planChecks` 第 11 项 `propagation`；探针加 `app.diagram.text`。**「显示什么就写什么」是实测逼出来的**：退回 E1 时若把 `prop_primary = two_ray` 一并写给 `scn`，引擎会（正确地）拒整次运行。web 193 项、`slice4-smoke` 74 → 86 项 |
| R-7 | 收口 | 模型卡 `models/channel/README.md`（五个效应的公式、常数、来源、适用范围、锚点、**与 04 §7.3 的落差对照**）；`docs/{scenario-format, component-catalog, display-products, api-versions}.md`；06 / CLAUDE 回填 | 全量回归；文档与 `cuav_run --catalog` 逐项核实 | R-6 | [x] 2026-09-10：模型卡 `models/channel/README.md`；`docs/{scenario-format, component-catalog（新增 §8）, display-products, api-versions, diagram-canvas-guide（新增 §2.9，顺带改正过时的 §5.2）}.md`；CLAUDE.md 与本备忘录回填；WORKLOG |
| R-8 | 撤掉手填距离的信道变体（D-059） | `model.ts` 的 `ch` 只剩一个变体 + `RETIRED_VARIANTS` / `retiredNote()`；`effects.ts` 删掉无人可达的那条判据、`plan.ts` 检查项改名「传播档位自洽」；`ChainView` 的「不是典型链路」兜底照实说缘由；12 报告 §4.5 改写、模型卡与两份 docs 同步 | 卡片上没有变体下拉、不再要求手填距离；用了已撤变体的框图**不硬解不改写**、界面说得出缘由；组件目录与黄金基准零变化 | R-7 | [x] 2026-09-10 |

R-2 / R-3 / R-4 / R-5 互不依赖，可任意顺序。**R-1 单独收口，不通过不进 R-2**（12 §7）。

与 D 线的关系：R 线做的是「不需要建筑几何的效应」，D3（切片 ⑤）补上建筑遮挡与刀口绕射后，
`prop_level = E3` 解禁、`line_of_sight` 由几何判定，阴影 σ 表的 NLOS 列与测向误差预算的
`sigma_mp_nlos_deg` 同时开始生效——三处今天都取不到（12 §1.2、§8 风险 5）。


## 9J. V 线：态势与信号可视化提升（D-061，2026-09-12；04 之外，PROVISIONAL）

用户 2026-09-12 指示：Aaronia AG 的反无解决方案「关于反无的态势、和系统的界面设计，特别是信号的可视化展示和方式对我们这个项目有很多借鉴之处」；
「之前实现的 demo 案例 C-UAV Model Demo，其中的界面排版还是可以有很多借鉴的」。设计见 `13.态势与信号可视化提升实施方案_v1.0.md`（下称 13 报告），决策 D-061。

核实到的现状（13 §1.4、§6）：数据一件不缺（链路帧、每站测向报告、三种定位解、dBm 谱都在产品与端点里），缺的是表达——
态势元素太轻、右栏空态、一次一个观测点、没有时间轴；另有两处缺口：刷新后采用了最近任务却载入缺省场景（`task.json` 没有 `scenario_id`），
链路线从未画出（`link_id` 按最后一个 `-` 拆分，`site-1-uav-1` 拆成 `site-1-uav` / `1`）。

要点：**数据不新造、只改表达**；右栏常驻目标列表 + 卡片栈，卡片只摆事实；全宽时间轴条作 `.app` 四行网格的第三行（抽屉之上）；
圆形告警区 `zones[]` 是场景对象、引擎收下不解释、是威胁色唯一生产者；时间轴时基 = 引擎逻辑时间，信号游标换算 `t − t0_s`；
基准变更只有 `golden-03` 加告警区一处（航迹基准只哈希变）。

| # | 步骤 | 交付物 | 验收 | 依赖 | 状态 |
|---|---|---|---|---|---|
| V-0 | 方案定稿与回填 | 13 报告 v1.0；CLAUDE.md D-061、文档表、已定表、里程碑表 V 行、切片枚举；本备忘录 §0.2 / §0.3 / §9E / §9J / §14；09 §3.2、§5.3 引用块与 v1.10 修订行；`docs/scenario-format.md` §9、`docs/display-route.md` §6.3 待写登记；WORKLOG | 文档互引一致；六条拍板各有落点 | D-060 | [x] 2026-09-12 |
| V-1 | 目标与站点卡片栈（含两处缺口） | ① 服务端 `TaskRecord.scenario_id`（`store.ts`、`manager.ts:264/301`），前端 `adoptTask` 载入任务的场景、`bootstrap` 先任务后场景；② `useSituation.ts:134-136` 与 `ObjectForm.tsx:377-379` 改按 `derivedLinks(doc)` 精确匹配；③ `web/src/scene/{TargetList,TargetCard,SiteCards}.tsx`：列表表 + 分节卡 + 站点卡，接收电平 / 信噪比按 13 §3.2 算；右栏 `ObjectPanel` 改为栈 + 叠表单；探针 `app.cards / siteCards` | 不点选即 `cards.length === emitters.length`；卡上距离 / 路损 / 方位 / 测向与端点行逐值相等；`rx_dBm = tx + Gt + Gr − L`；刷新后 `app.scene.scenarioId` 等于任务的场景；`cuav-link-line` 渲染要素 ≥ 1（`slice2` 加断言） | V-0 | [x] 2026-09-12：两处缺口先修（服务端 `task.json.scenario_id`、`adoptTask` 载场景与 `bootstrap` 先任务后场景、`splitLinkId` 精确匹配）；`scene/cards/{derive,Cards,icons}` 与 `RightColumn`；顺带修链路页「选场景」按链路自己上一份场景清空选择，加 `?scenario=`。`slice8-smoke` 16 项：九行几何与电平逐值相等、测向与定位相等、点行选中与返回、刷新跟任务、链路线渲染 19 要素；六套 e2e 245 项全绿 |
| V-2 | 地图叠加加重与告警区 | `fixOverlay.ts` 渐隐色带与 2.5 px 椭圆；`style/situation.ts` 40 px 目标图标带白环 + `cuav-drone-alert` 变体、32 px 站点；新图层 `cuav-target-ring / cuav-target-label / cuav-link-label / cuav-target-pole / cuav-zone-fill / cuav-zone-line`；图层菜单加两项；`zones[]`：`docs/scenario-format.md` §2 + 新节、`scenario.schema.json`、`engine/src/scenario_json.cpp` 键表 + `parse_zone` + `geo::Scenario::zones`（三处同一提交）；`scenarioOps.ts` 增删移、工具条「布告警区」、对象树组、`SceneSelection.zone`；`golden-03` 加一个告警区；新色值按 D2-4 判据实测回填 `display-route.md` §4 | 六个新图层在 `layers` 里且 `slice2` 七图层表已补；目标入圈 `inZone` 与图标变体切换；新色 ≥ 3:1；**基准变更单独收口**：`scenario-track-golden-03.json` 723 样点逐点相同、只哈希变，两份 3×3 夹具哈希更新；`DEFAULT_CHAIN_TEXT`、组件目录与产品基准零变化 | V-1 | [x] 2026-09-12：V-2a 三处同一提交 + golden-03 加 `z-east`，航迹基准 723 样点逐点相同、只哈希变（单独提交 263b9d8）；V-2b 图层 7 → 13、图标 40 / 32 px、渐隐色带、告警区编辑与入圈判定；两色实测 ≥ 4.60:1 回填 display-route §4；`slice8` 16 → 28 项、六套 e2e 257 项全绿 |
| V-3 | 全宽时间轴条与回放 | `web/src/shell/{Timeline.tsx, timeStore.ts, timelineOps.ts}`（命令文件不叫 `timeline.ts`：macOS 文件系统不分大小写、与组件撞名）；`.app` 网格三行改四行，时间轴在第三行；`sceneStore` 每键有序历史 + `snapshotAt(t)` + `hasHistory()`；新 `scene/situationView.ts` 的 `currentSituation / situationRev` 供地图 tick、卡片、探针、计数四处共用；与 `signal/cursor` 双向同步（拖动 100 ms 节流、数值相等即不回写防回环）；活动标记只列时长以内的、左端计数 4 Hz、播放 / 倍速 / 跟随实时；`Space` / `Home` / `End`；未运行时走 `RoutePreview`（`source = preview`）；探针 `app.timeline{t, mode, playing, speed, markers, source}`；`tests/e2e/slice8-smoke.mjs` 收口 | 拖到 30 s 后实体位置 = `track` 该时刻前最后一行（1e-9 度）、`cursor_t_s = t − t0_s`、卡片距离 = `links` 同时刻行；`Space` 后 `t` 单调增；`slice8-smoke` **40 项**全绿；六套 e2e **269 项**全绿 | V-1、V-2 | [x] 2026-09-12 |
| V-3b | 场景页信息分层（D-062） | 左栏 = 配置：`LeftColumn.tsx`（对象树三组 + 选中对象表单 + 数据包一行），航点与活动折进辐射源表单（`RouteSection` / `ActivitySection`），派生链路不进树；右栏 = 观察：`SituationPanel`（焦点卡 + 目标列表一行一目标 + 站点一行一站）；焦点规则 `scene/focus.ts` 三处共用；地图分焦点与背景（`useSituation` / `layers/situation.ts` / `fixOverlay.ts`），图层菜单「全部目标叠加」；工具条观察组常驻、编辑组收进「编辑场景」；顶栏在有时间轴的页不显示时间读数；字级三档；抽屉「资源」页签放数据包明细 | 焦点卡 = 第一个目标、树 7 行、左栏无表单、顶栏无时间读数、工具条只露「测量」；点行后表单在左栏、焦点卡换人、渲染出的距离标注全属于焦点目标；`slice8-smoke` **42 项**、`slice2-smoke` **29 项**、六套 e2e **272 项**全绿 | V-3 | [x] 2026-09-12 |
| V-4 | 站点方位图 | 选中站点时右栏极坐标测向直方 + 方位—电平曲线（`bearings?site_id=`）| 直方主瓣中心与真值方位差 ≤ 2σ（真值只在 `?dev=1`） | V-1 | [ ] 切片 ⑨，待排期 |
| V-5 | 精度场与覆盖场 | `cuav_run --field` 子命令（观测区域范围读 `manifest.json` 的 `aoi.bbox`；任务级网格产品与索引家族；`fake_engine.mjs` 分支）；viridis 热力层 + iso-Pd 包络 | golden 配置 GDOP 0.942809 网格复现；站址场值 = 链路预算；200 × 200 格 ≤ 1 s | V-2 | [ ] 切片 ⑨，待排期 |
| V-6 | 网格瀑布 | 信号页「并排」模式：K 站瀑布共时间轴与色标、量程取并集（09 §7.4） | 三幅同色标；三次抽取 ≤ 300 ms | U-3 | [ ] 切片 ⑩，待排期 |
| V-7 | 余辉频谱 | 迹线模式加「余辉」（最近 N 帧功率对频点二维直方按密度着色） | golden-03 跳频源呈稀疏点、连续单音呈实线 | U-3 | [ ] 切片 ⑩，待排期 |
| V-8 | 检测识别标签条 | 瀑布时频框加识别标签与颜色、面板顶部频段标签条 | 随 C-9 | C-3、C-4、C-9 | [~] 2026-09-15 随 C-9 做掉**前半**：瀑布时频框三道描边（外白 4 px / 中间类别色 2 px / 内墨 1 px）并在框内写标签名，色表 `web/src/results/labels.ts`，对比度实测记 `docs/display-route.md` §4.1（边界 max(白, 墨) 十个 viridis 锚点最低 **3.33:1**、标签文字对底衬最低 **5.37:1**）。**后半「面板顶部频段标签条」是宽带多段的事，随 ④c** |
| V-9 | 全带总览与分段放大 | 顶部全带频谱标频段框、下方每框放大面板；频段计划表来源标 `assumed` / `paper` | 随 ④c | M-2、M-3 | [ ] 随 ④c |
| V-10 | 全景一屏 | 信号视图实例化重构（`signalBuffer / viewStore / signalHooks` 改按实例持有）；场景页地图下方信号条（选中站 S4 瀑布 + 包络，紧凑模式） | 地图、卡片、瀑布同时动且 20 Hz 不掉帧 | V-3 | [ ] 切片 ⑪，待排期 |
| V-11 | 视觉密度 | 2K 下图标、线宽、字号整体上一档；数值统一等宽 | 目视验收 + 对比度复测 | V-2 | [ ] 切片 ⑪，待排期 |

纪律：V-1 的两处缺口**先修再做卡片**（卡片要对的场景与对的链路）；V-2 的基准变更**单独一步**收口（先加区、再重生成航迹基准并核对逐点相同、最后改夹具哈希），不通过不进 V-3；每步以 e2e 断言 + WORKLOG 条目收口，不留「等下一条线」的半成品。
## 10. P2 / P3 / P4 概要

**P2 平台与组件化（04 §13.4）**：框图画布、组件库、参数表单、连线校验、WS 断线补取已前移到 B / U 线（§9A、§9B，2026-09-04）。P2 剩余：模型包 `manifest/interfaces/parameters` 与版本 → SQLite 与权限审计 → 分片上传 → **Windows 单机一体化包在干净机上安装运行**（启动器形态 OPEN）→ 05 P0 端口命名预留检查。

**P3 工程等效与实测校准（04 §13.5）**：2–3 类辐射源模型卡与证据包 → 统计多径、阴影、频偏、多普勒 → 接收机非理想（相噪、IQ 不平衡、AGC、削顶）→ 混合增强 → 实测与仿真特征对比工具（04 §11.4 七层）→ 参数拟合与版本冻结 → 四组数据独立验证 → 跨层一致性算例 ②③④ → M1/M2 校准参数回写。D3 的 `SceneParamFrame` 在此正式接入 IQ 引擎。

**P4 试用与交付（04 §13.6）**：稳定性与异常恢复、断网离线完整性、浏览器兼容、许可合规、部署包、手册、验收集冻结。

---

## 11. 数据线（与各阶段并行）

> **本节全部结论属原型阶段验证值，不是交付指标（D-028）。** 公开数据集在本阶段的作用是
> 验证技术途径、软件架构与功能实现是否走得通，不是给出可写进合同或验收门槛的数字。
> 甲方实测数据到货后，用同样的脚本（`tools/iq_convert.py`、`tools/iq_survey.py`、
> `algos/reference/ds6_false_alarm.py`、`ds7_pd_curves.py`）重跑一遍并修订本节全部数值。
> 具体地说：虚警率超标倍数、环境代价 23.6–38.9 dB、路损指数 n≈2、5.8 GHz 放弃判定，
> **这些都不得被引用为设计指标**。它们在本阶段的正当用途只有两个：证明这条技术路线能产出
> 这类结论，以及充当引擎实现后的对拍基线。

### 11.1 八项摸底问题（用户回答）

采集点（ADC 后 / DDC 后 / 信道化后）；格式与元数据；采样率、带宽、中心频率、位宽、通道数；时长与连续性；真值（机型、距离、航迹、状态、时间对齐）；同期背景空采；增益、AGC、滤波、校准；密级与使用范围。

**对 DroneRFb-DIR 的回答**（2026-09-03，多数为实测核实，非照抄说明文件）。注意这些答案描述的是
**该公开数据集**的情况，**不等于甲方接收机与场景的情况**。

| # | 问题 | 对 DroneRFb-DIR 的回答 | 状态 |
|---|---|---|---|
| 1 | 采集点 | DDC 后、信道化前，即 S4 观测点。USRP-2955 硬件下变频到复基带，未做信道化 | 已答 |
| 2 | 格式与元数据 | MATLAB v7.3（实体是 HDF5）+ gzip；根下只有 `I`、`Q` 两个 float32 数据集，各 1×4000000。**文件内零元数据** | 已答（答案是「没有」） |
| 3 | 采样率、带宽、中心频率、位宽、通道数 | 80 兆样点每秒；80 兆赫兹（2.4–2.48 GHz）；2.44 吉赫兹；底层 16 位整数（实际只用约 12 位）；单通道。**这些来自论文而非文件，元数据须标 `source=paper`** | 已答（来源是论文） |
| 4 | 时长与连续性 | 每片 50 毫秒 / 400 万点；片内连续，**片间不连续**，且发布版已无绝对时间戳 | 已答 |
| 5 | 真值 | 机型与个体有（6 型 18 架 + 背景）；距离**固定 10 米**；**无航迹**（无人机摆放不动）；状态只有视距 / 非视距二值标签；**无时间对齐** | **部分** |
| 6 | 同期背景空采 | **有，62 片纯背景**（训练集 37 + 测试集 25）。这是该数据集对本项目最有价值的部分之一 | 已答 |
| 7 | 增益、AGC、滤波、校准 | **全部没有**。只能从数据反推：峰值在满量程以下 15 至 30 分贝，无削顶。**因此无法换算为分贝毫瓦，绝对功率类验证一律做不了** | **答不上** |
| 8 | 密级与使用范围 | 公开数据集。但许可条款未取到（SciDB 页面动态渲染），能否随交付系统分发待确认，见 DS-1 | **待确认** |

**对 DroneRFa 的回答**（2026-09-03，同为实测核实）。它与上表同栏对照看，差异见 §11.5。

| # | 问题 | 对 DroneRFa 的回答 | 状态 |
|---|---|---|---|
| 1 | 采集点 | 同为 DDC 后、信道化前的 S4 观测点。USRP-2955，未做信道化 | 已答 |
| 2 | 格式与元数据 | MATLAB v7.3（实体是 HDF5）+ gzip；根下有 `RF0_I`、`RF0_Q`、`RF1_I`、`RF1_Q` 四个 float64 数据集，各 1×150000000。**文件内零元数据** | 已答（答案是「没有」） |
| 3 | 采样率、带宽、中心频率、位宽、通道数 | 100 兆样点每秒；**双通道同时采集**，2440 + 5800 兆赫兹（`T10010`、`T10100` 例外，是 915 + 2440）；底层 16 位整数（实际只用约 9 至 10 位）。**均来自论文，须标 `source=paper`** | 已答（来源是论文） |
| 4 | 时长与连续性 | 每片 1.5 秒 / 每通道 1.5 亿点；**每 1000 万点（0.1 秒）内连续、块间有损伤**（采—存交替）；无绝对时间戳 | 已答 |
| 5 | 真值 | 机型有（24 型 + 背景、7 品牌，其中仅 9 类户外带距离标注）；距离是**三档区间**（20–40 / 40–80 / 80–150 米）；**无航迹、无 GPS 记录**（无人机飞行中，5–15 米每秒）；**无视距/非视距标签**；无时间对齐 | **部分** |
| 6 | 同期背景空采 | **有**。本地 3 片 `T0000`，实测含 WiFi 与蓝牙，突发帧占比 0.0% 至 2.5% | 已答 |
| 7 | 增益、AGC、滤波、校准 | **部分有**：接收增益 50 分贝、天线全向垂直 3 dBi（均引自论文）。**仍缺馈线损耗与一次功率标定常数，绝对功率类验证照样做不了** | **部分** |
| 8 | 密级与使用范围 | 公开数据集，许可条款未取到，见 DA-1 | **待确认** |

**对 P3 四组标定数据的覆盖情况**（两个数据集合并看）：

| 组 | 用途 | DroneRFb-DIR | DroneRFa |
|---|---|---|---|
| A 高信噪比参考 | 参考波形与特征模板 | 部分可以。视距（`IN`）片段在 10 米处信噪比较高 | 部分可以。`T10010` 的 RF1 峰值高出底噪 50.8 分贝，是两个数据集里最强最活跃的通道 |
| B **多距离场景** | 标定路损、遮挡与距离相关的模型 | **完全不能**。4690 片距离固定 10 米 | **粗档可以**。三档距离实测路损指数 n = 1.90（自由空间 2.00），片间重复性 0.3–1.3 分贝；只支持「路损指数约为 2」，**不支持逐米曲线**（距离标注是区间，相邻档比较被区间宽度淹没） |
| C 背景同邻频 | 真实干扰环境下的检测门限 | 可以。62 片纯背景 | 可以。本地 3 片背景，须按干净程度分别处理 |
| D 独立验证 | 未参与调参的验收集 | 可以。出版方划好的测试集 2513 片，含 6 架从未在训练集出现的个体 | 无出版方划分，**须自行按机型/距离档/采集批次分层并入库即冻结** |

B 组的硬伤由 DroneRFa 部分补上，但补的是粗档结论，不是曲线。**仍然只能由甲方自采数据或
补采计划补上的有三项**：绝对功率标定（两个数据集都缺馈线损耗与一次标定常数）、逐米路损
曲线（需精确 GPS 距离记录）、视距与非视距标签与建筑几何（两个数据集都没有，跨层一致性
算例 ② 建筑遮挡与 ③ 双径因此仍不可做）。

### 11.2 任何 IQ 数据集入库当天必做

> 2026-09-05 增（D-038）：为每批数据估一个**标定常数** `calibration{offset_dB, source}` 写进 `index.manifest.json`：有设备标定记录标 `measured`，只有论文参数（接收增益、天线增益、满量程假设）标 `paper`，都没有则标 `assumed` 并写明假设；界面据此显示 dBm，没有常数才退到 dBFS。
> 2026-09-06 更新（D-047）：常数定义为满量程对应的 dBm `full_scale_dBm`，表在 `data/iq/measured/calibration.json`，由 `scripts/ds8_calibration.py` 估算、`tools/apply_calibration.py` 刷进逐文件清单 `power.calibration`、`tools/build_batch_index.py` 汇总进索引 `calibration` 块；链路预算反推的常数标 `model`；来源徽标只在开发者模式。**DS-8 / DA-8 已做**：DroneRFb-DIR −1.6 dBm（model）、DroneRFa −50.0 dBm（paper），原型阶段验证值，`docs/iq-format.md` §5。

本节是通用规则，对两条数据线同样适用：公开数据集线（§11.4）与甲方数据线。首个适用对象是
DroneRFb-DIR。

1. `tools/iq_survey`：解析 + 04 §10.6 八项自动质检 + PSD/瀑布 + 元数据报告，结论回填 `docs/iq-format.md`。质检结果必须落到四态语义（`valid` / `degraded` / `invalid` / `not_applicable`），**元数据缺失一律判 `degraded`，禁止用默认值顶替**（铁律 15）。
2. 按设备/日期/地点/架次划出**冻结验收子集**，记 sha256，清单入库；此后任何调参输入集须证明与其无交集。
3. 目录：`data/iq/measured/<batch>/`，大文件不入 git，只入索引与元数据。

### 11.3 参与时点

自 2026-09-03 起分成两条并行的数据线，用途不同，不得混为一谈。

| 线 | 数据 | 参与时点 | 能支撑什么 |
|---|---|---|---|
| 公开数据集线 | DroneRFb-DIR（§11.4）、DroneRFa（§11.5） | **现在起** → P1-9a 回放 → 跨层一致性 ① 的真实背景版与 ④ → 路损粗档标定（仅 DroneRFa） | 仿真链条 S4 观测点**下游**的验证：回放、观测量提取、检测门限、识别；外加 DroneRFa 的距离维度可支撑路损指数粗档标定与正交不平衡模型的校准素材；**辐射源模板素材**（D-045，§9F：波形模板的谱形、突发、频段切换，以及视距 / 非视距附加损耗分布） |
| 甲方数据线 | 待提供 | 阶段 0 摸底定格式 → P1-9b 回放源与辐射源模板 → P3 四组（A 高 SNR 参考 / B 多距离场景 / C 背景同邻频 / D 独立验证）标定与冻结 | 上述全部，外加功率标定、链路几何、模型校准 |

### 11.4 公开数据集 DroneRFb-DIR（Datasets1）

2026-09-03 入手。浙江大学发布，论文 DOI `10.11999/JEIT240804`，SciDB 数据集编号
`84cf9101e739402784b1396783881202`。摸底的完整结论见 `WORKLOG.md` 同日条目，此处只列步骤。

**在两个公开数据集里承担的角色（D-018）**：① 跨层一致性算例 ② 建筑遮挡的**唯一**量级依据
（视距/非视距标签，论文给出两者信噪比差约 20 分贝；DroneRFa 无此标签）；② P3 的 D 组独立
验证集（出版方已划分，含 6 架从未在训练集出现的个体，天然无泄漏）；③ DS-6 真实背景虚警率
标定的主样本（62 片纯背景）；④ `tools/iq_survey` 八项质检中**正交项 `valid` 正例路径的唯一
数据**（标准差比 1.0000 至 1.0014），并作为 DA-7 判定 DroneRFa 正交不平衡根因的对照组；
⑤ 同型号分架次的个体识别素材（EM-S-03/04），且机型较新。转换与质检按这五个角色排序，
两个数据集都完整保留，不做删减。**2026-09-05 新增角色 ⑥（D-045）：辐射源模板的主素材**——6 型 18 架、固定 10 米的
高信噪比片段是特征模式的 A 组参考数据（04 §11.2），视距 / 非视距成对片段给出遮挡附加损耗分布；
模板与位置无关，防线二不变。

**要点**：USRP-2955，采样率 80 兆样点每秒，中心频率 2.44 吉赫兹，每片 50 毫秒共 400 万点，
4690 片共 64 GB。数据本身正好落在 S4 观测点。底层是 16 位整数，转成本项目的复 int16 交织
格式**无损**（抽查 7 个文件，全部样点均为 2 的负 15 次方的整数倍）。收发距离固定 10 米，
无人机摆放不动，无坐标、无绝对功率标定、无绝对时间戳。

| # | 步骤 | 要点 | 状态 |
|---|---|---|---|
| DS-1 | 确认许可条款 | 特别是能否随交付系统分发。SciDB 页面动态渲染，需人工访问。用户 2026-09-05 确认：没有任何使用约束与署名约束，可随交付分发 | [x] 2026-09-05 |
| DS-2 | 转换脚本 | **2026-09-03 完成**：`tools/iq_convert.py`，与 DA-2 共用。HDF5 分离 I/Q → 复 int16 交织；断言每样点为 2 的负 15 次方整数倍、失败即中止不留半成品；逐段回读对拍。已在三片真实文件上跑通（训练集、背景、测试集各一）。**批量转换尚未跑**，是一次批处理作业，等用户定时间 | [x] |
| DS-3 | 外挂元数据 | **2026-09-03 完成**：清单 schema 见 `docs/iq-format.md` 第 4 节，`field_sources` 逐字段标注来源。实测结果：十个采集类字段来源为 `paper` 或 `absent`，第 7 项质检因此一律 `degraded` | [x] |
| DS-4 | 冻结验收子集 | **2026-09-04 完成**，清单 `data/iq/measured/holdout.manifest.json`，工具 `tools/freeze_holdout.py`。验收集 **2487 片** = 出版方测试集 2513 片减去 26 片（25 片测试集背景已被 DS-6/DS-7 用作背景源与门限标定，1 片是转换脚本的验证样本，按保守原则排除）；其中从未在训练集出现的 6 架个体 1580 片单列。拦截按 `data_id` 与 `content_sha256` 双条件，换名也拦得住 | [x] |
| DS-5 | `tools/iq_survey` 跑通 | **2026-09-03 完成**：`tools/iq_survey.py`。实测结论与当初的预判有一处出入——**第 1 项（文件长度、格式与元数据一致性）实际是 `valid` 而不是失败**，因为一致性比的是转换后产物与其旁挂清单，转换脚本保证了这一点；真正失败的是第 7 项（元数据必填项）。第 6 项单通道判 `not_applicable`。整体 `degraded`，「缺元数据判 degraded 而不崩溃」这条路径已在真实数据上走通 | [x] |
| DS-6 | 真实背景虚警率标定 | **2026-09-04 完成**，62 片背景全部入库后实测。预期方向对，**量级比预期大得多**：目标 1e-3 时实测虚警率 2.32e-1，**超标 232 倍**，门限需抬高 32.6 dB；目标 1e-4 时超标 2223 倍。实测值在两成上下饱和（重尾所致）。合成高斯白噪声对照全部落在目标值两成以内，故超标是数据性质不是实现问题。逐频段扫描显示干扰有具体占用格局：信道 1 跨度内 Λ 均值 43.1、信道 6 跨度内只有 5.2。结论、限制与下游用法见 WORKLOG 2026-09-04 条目与 `data/iq/measured/ds6-false-alarm-report.md` | [x] |
| DS-7 | 真实背景加合成目标 | **2026-09-04 完成**（参考实现侧）。脚本 `algos/reference/ds7_pd_curves.py`，报告 `data/iq/measured/ds7-pd-curves-report.md`。三条结论：① **跨层一致性算例 ① 通过**，解析公式与蒙特卡洛的检出率最大偏差 0.0016–0.0050，比 04 §16.3 建议容差小两个数量级（引擎侧待 P1-2/P1-4 后重跑）；② **环境代价 23.6–38.9 dB**，即同样虚警率下真实背景里能检出的最弱目标要强这么多，频段选择本身值 15 dB；③ **门限在一半背景上标定、换到另一半就失效 7 倍**，随机打散后再分半则达标，说明失配来自背景随时间与地点漂移——静态门限不成立，P1-4 必须做滑动噪声估计或恒虚警率 | [x] |

依赖：DS-2 → DS-3、DS-5；DS-5 → DS-6 → DS-7。**DS-7 原写「还依赖 P1-3、P1-4」，实际不必**：
合成目标在参考实现里直接生成即可，引擎侧的组件是后来要对拍的对象，不是前置。DS-1 与 DS-4 独立。
除 DS-1 外均不需要用户输入。

**转换体积（2026-09-04 实测，原先的"先转子集"策略已作废）**。全部 4690 片已转完，实际
70 GB、单片约 0.15 秒、总耗时十几分钟，原始文件保留不动。当初提"先转 200 片子集"的理由是
省空间，而**存储不是本项目的约束**（项目记忆 `no-space-saving-tradeoffs`），该策略作废。
DroneRFa 同日转完 24 个产物、14 GB。

**防线二：该数据集不得与任何地理场景绑定展示。** 它没有位置信息，把它"摆"到地图上任一
地点都是编造，违反铁律 14。这个诱惑一定会出现——"把真实信号放到场景里演示"看起来很有
说服力。另外该数据集采自杭州，而临时观测区域用的是西安，本来也对不上。

**命名陷阱**（极易搞反）：文件名中的 `IN` 代表**视距**、`OUT` 代表**非视距**，不是室内室外；
`S0`/`S1`/`S2` 是**采集时间序号**，不是场景编号。

### 11.5 公开数据集 DroneRFa（Datasets2）

2026-09-03 入手。与 DroneRFb-DIR 同一课题组的前期数据集，论文 DOI `10.11999/JEIT230570`，
SciDB 数据集编号 `34f0a91e8a544904998b8fdc44477380`。摸底与路损验证的完整结论见 `WORKLOG.md`
同日第三条日志，此处只列步骤。

**要点**：USRP-2955，采样率 100 兆样点每秒，**双通道同时采集**（2440 + 5800 兆赫兹），每片
1.5 秒、每通道 1.5 亿点，本地 22 片共 21 GB（全集约 560 GB）。同样正好落在 S4 观测点，底层
同为 16 位整数、转复 int16 交织**无损**。与 DroneRFb-DIR 相比多了三样：**三档距离区间标注**
（20–40 / 40–80 / 80–150 米）、**飞行中**（5–15 米每秒）、**部分标定量**（接收增益 50 分贝、
天线 3 dBi，均引自论文）。少了两样：信号电平低约 15 分贝，且**没有出版方划好的训练测试划分**。

**在两个公开数据集里承担的角色（D-018）**：① 距离维度与路损粗档标定（P3 的 B 组，DA-6 已
完成）；② 运动与多普勒（无人机飞行中，DroneRFb-DIR 是摆放不动的）；③ 正交不平衡模型的
**唯一**实测校准素材（DA-7）；④ 1.5 秒长片可看突发时序（DroneRFb-DIR 每片只有 50 毫秒）；
⑤ 双通道 float64 解析分支，与 DroneRFb-DIR 的单通道 float32 互为对拍。**不能替代
DroneRFb-DIR**，两者互补，理由见 §11.4 角色段与 D-018。**2026-09-05 新增角色 ⑥（D-045）：模板的距离维度与运动
素材**——三档距离给回放模式的参考距离（区间中点是计算假设，D-019），飞行中片段给多普勒与突发时序；
防线三不变。

| # | 步骤 | 要点 | 状态 |
|---|---|---|---|
| DA-1 | 确认许可条款 | 与 DS-1 并列，同为 SciDB 托管、页面动态渲染。用户 2026-09-05 确认：没有任何使用约束与署名约束，可随交付分发 | [x] 2026-09-05 |
| DA-6b | 背景片参与 DS-6 | 3 片背景已入库并参与 2026-09-04 的虚警率标定，结果与 DroneRFb-DIR 同向但缓和（目标 1e-3 时超标 26.5 倍、门限需抬高 24.5 dB），说明两次采集的电磁环境拥挤程度不同 | [x] |
| DA-2 | 转换脚本（与 DS-2 共用一个） | **2026-09-03 完成**，四个分支均已实现并有单测：① 双通道 float64；② 默认只转 RF0（`--channel` 可指定）；③ 两种文件名形态；④ 中心频率按机型编号判断。已在三片真实文件上跑通，单片 1.4–1.6 亿点、约 5 s、3 段。**新实测事实：片长不是固定 1.5 亿点，22 片实为 1.3 至 1.7 亿点、均为 1000 万点的整数倍**，脚本按实际形状读，不写死长度 | [x] |
| DA-3 | 外挂元数据 | **2026-09-03 完成**，四条要求均已落地并有单测：距离写区间且 `distance_point_m` 为 `null`；连续性 flag 为 `damaged` 并附说明；正交不平衡判 `degraded`（判据见 D-020）；绝对功率标 `uncalibrated` 并写明原因 | [x] |
| DA-4 | 自行划分并冻结验收子集 | **2026-09-04 判定：本地 22 片一片都不能用作验收集**。它们全部已用于探索与拟合（DA-6 路损、DA-7 正交不平衡、DS-6/DS-7 背景源），按 `holdout-freeze-at-ingest` 不能追认。**验收集只能来自后续按 `download-list.md` 补下的片，下载当天立即冻结，冻结前不得用于任何分析。** 这件事必须在下一次动 DroneRFa 数据之前做 | [~] |
| DA-5 | `tools/iq_survey` 跑通 | **2026-09-03 完成**。互补作用兑现了，且比预期更有价值：正交不平衡这一项**按整片统计会漏掉三片中的两片**，因为不平衡只在噪声路径上、信号越强整片比值越接近 1；判据因此改用安静帧（D-020）。底噪估计用频率维第 10 百分位 | [x] |
| DA-6 | 路损指数重算并给误差范围 | **2026-09-03 完成**，脚本 `scripts/da6_pathloss.py`，结论见 WORKLOG 同日第四条日志：`T0010` 六片 n = 1.93（逐对极差 0.12）、`T0011` 三片 1.69，两机型宽基线一致；**主导不确定度是距离区间标注**（真实距离比只能定在 2.0–7.5，对应 n = 1.29–3.75），故只能冻结「n ≈ 2 粗档」（D-019）。相邻档比较一律不可用于标定 | [x] |
| DA-7 | 正交不平衡的根因判定 | 2026-09-03 缩小范围：实测确认**不平衡在噪声路径上、信号路径是平的**（安静帧比值 0.69–0.90，强帧 0.99–1.00），且 DroneRFb-DIR 同为 USRP-2955 却两种帧都是 1.000，**故非设备型号固有**。要查的因此是 DroneRFa 那次采集的配置或数据生产环节，可拿 DroneRFb-DIR 当对照组。结论若为接收链特性，它就是 P3 正交不平衡模型的唯一实测校准素材；若为数据生产问题，只能标记并弃用 | [~] |

依赖：DA-2 → DA-3、DA-5；DA-1、DA-4、DA-6、DA-7 各自独立。除 DA-1 外均不需要用户输入。

**两条已经得到结论的取舍**（理由都是数据内容，不是体积）：

1. **5.8 吉赫兹通道放弃。** 在 7 个文件（`T0010` 三档距离 6 片 + `T0011_D10_S1100`）、每片 6 个抽样位置上，RF1 通道突发帧占比全部为 0.0%，**含最近的 20–40 米档**；峰值高出底噪 32 至 36 分贝但完全不随时间变化，是本振泄漏或杂散。证据边界：实测覆盖 `T0010`，推断到其他 DJI 机型属推断，元数据须如实标注。复核最省的做法是取任一其他机型的 `D00` + `S1xxx` 单片。
2. **转换只转 RF0。** 直接后果是每片从约 1.2 GB 降到约 600 兆字节、22 片从 26 GB 降到 13 GB，但这只是副产品；**决定依据是 RF1 里没有信号**。原始文件一律保留不动。

**补数清单**：`data/iq/measured/dronerfa/download-list.md`，按论文命名规则枚举命名空间并按
优先级排序。该清单**是命名空间的完整枚举，不是「确认存在」的文件列表**——每个户外机型的
命名空间是 3 档距离 × 2 种频段状态 × 8 个片段序号 = 48 种组合，而论文只承诺每类不少于 12 片，
所以实际提供的序号是稀疏的，下载时与 SciDB 的实际文件列表取交集。

**防线三：DroneRFa 同样不得与任何地理场景绑定展示。** 它虽有三档距离标注，但**没有坐标、
没有航迹**，把它摆到地图上任一位置、或据此画一条航迹，都是编造，违反铁律 14。距离档只能
作为链路距离参数注入 M1/M2 传播模型，不得反过来在场景视图里画成位置。

**命名陷阱**（与 DroneRFb-DIR 的陷阱不同，不要混）：`S` 的**最高位是频段状态标志**（`0` 为
初始在 915 兆赫兹或 2.4 吉赫兹、`1` 为切换到 2.4 或 5.8 吉赫兹），低 3 位才是片段序号——
**做距离对照时最高位不能变**，否则频段状态跟着变、对照不成立；`D` 是**区间**不是点值；
24 类中只有 9 类是户外带距离标注，其余 15 类室内固定约 2 米、文件名没有 `D` 字段，判定方法
是看该机型是否同时出现 `D00`、`D01`、`D10`。

---

## 12. 验证与验收入口

| 层级（04 §15.1） | 目录 | 内容 |
|---|---|---|
| 解析与理论 | `tests/unit` | 单音、噪声、滤波、频移、时延、量化理论值；D3-3 锚点 |
| 组件单元 | `tests/unit`、`tests/golden` | 每组件输入输出、边界、复位；emcore 148 例 golden |
| 处理链集成 | `tests/integration` | 辐射源 → S4 → S5 → 检测闭环 |
| 实测等效 | `tests/regression` | 固定实测片段、特征对比、跨层一致性 ①–④。两个公开数据集合并能支撑的范围：① 与 ④ 可做；② 只能做量级合理性检查（DroneRFb-DIR 论文给出视距与非视距信噪比相差约 20 dB，可判断遮挡附加损耗是否落在十几到二十几分贝这个量级；两个数据集都无视距/非视距标签与建筑几何，做不到逐例判定）；③ 完全不能（无高度、无航迹，双径特征无从体现）。**另可做一项两个数据集之外原本做不了的**：DroneRFa 的三档距离支撑路损指数粗档标定（实测 n = 1.90），可作 EM-P-01 自由空间路损模型的实测对照 |
| 用户任务 | `tests/e2e` | 浏览器提交到结果；断线重连；离线资源完整性；切片 ① 至 ④ 的 e2e 断言见 §9E（探针 `window.__probe()` 扩展 `app` / `task` 字段） |

入口命令待代码建立后回填 CLAUDE.md「环境与命令」。性能数字一律带平台、模型组合、数据格式声明。

---

## 13. 每次对话的工作协议

1. **开工前**读三处：`CLAUDE.md`（自动加载）、本备忘录 §0.3、`WORKLOG.md` 顶部快照。
2. **动手前**核实引用的路径、版本、行号、常数真实存在；涉及既有成果的判断先抽样取证。
3. **复杂节点先出设计报告**（根目录 07+ 编号）：07 = D3-0 遮挡落点与坐标基座；08 = P1-0 引擎（切片 ① 后、切片 ② 前定稿）；09 = 主界面、三视图与框图画布交互设计（U-0，切片 ③ 前，顶替缺失的原型 html，D-034）。报告定稿保留，偏差另记修正。
4. **收工时**：`WORKLOG.md` 追加节点与量化结果；更新本备忘录 §0.3 与步骤状态；里程碑有变则改 CLAUDE.md 状态表；新决策追加 `D-xxx`。
5. **不做**：不把合成场景的结果当实测结论写进文档与模型卡（界面本身不作区分，D-043）；不静默改基准；不在 `C-UAV Model Demo/` 内开发；不引入在线依赖到运行时。

---

## 14. 修正记录

| 日期 | 条目 | 原文 | 修正 | 原因 |
|---|---|---|---|---|
| 2026-09-16 | §9D M-3 行、§10 P1-8；10 报告 §3.7 | M-3「多级抽取（`decim > 20`）也在这里做」；10 §3.7「`select_channel` 缺省 0」「`channels` 为 2 的幂」 | **多级抽取推迟**（写进 M-3 行与模型卡 §8.6，不是悄悄不做）；**`select_channel` 缺省改为 `channels/2`**（零频那一路）；**`channels` 改为「冻结原型表里那六档」** | 多级抽取要往 `models/adc-ddc/fir_lp_v1.json` 加档，而 `engine/tests/golden/ddc.json` 对着那张表的 sha256 —— 那是一次真正的基准变更，而本期最高只有 golden-02 的 10 MS/s、`decim` 到 20 绰绰有余（用户 2026-09-16 拍板）。`j = 0` 按低→高编号指的是**带边（±fs/2 绕回）那一路**，合法但作缺省最差；原型是冻结的等波纹设计（`N = M·T+1`、T 偶数），表外的 M 没有系数可用。见 D-071 |
| 2026-09-16 | §9D M-0 / M-2 / M-3、§10 P1-8 | M-0「Coder 产物随交付不受单位许可条款限制」（2026-09-04 销项）；M-2「首条 Coder 链路 = DDC」；M-3「→ Coder」；P1-8「由 M-2 兑现」 | M-0 的许可结论改记实情（开发机是**学术许可**，产物带「不得用于政府 / 商业 / 组织用途」）并附用户澄清（本项目属学术用途，据此可用）；M-2 改为「DDC，手写 C++」并已完成；**M-3 改为首条 Coder 链路并兑现 P1-8**；P1-8 由「M-2 兑现」改为「M-3 兑现」 | M-2 开工时实跑 codegen 发现学术许可条款，一度判定与 08 §13「产物入库并进交付包」冲突而放弃 Coder；用户当日澄清用途后路线恢复。`DDC` 仍保持手写是工程取舍——封装层本来就得手写、算法核仅约 60 行、且已与独立 Python 参考逐位相同。决策见 CLAUDE.md D-070（该条当天改写过一次，前后都记着）|
| 2026-09-17 | §0.3、§7 全表重排、§9E 切片 ⑤ 行、§12 OPEN 清单；CLAUDE.md 文档表 / 已定表 / OPEN 表 / D-035 / D3 里程碑行 | D3 步骤表还是 2026-09-04 定的五步（D3-0…D3-5），其中 D3-5 已移交 G-2；两项 OPEN 悬了十五天 | D3 重排为 D3-0…D3-8（新增 vendored GeographicLib、换基座的基准变更、建筑进引擎、浏览器 TS 复算、界面五步，旧 D3-5 标移交并腾出编号）；两项 OPEN 销项 | 用户 2026-09-17 在两个选项表上各拍一条（D-074）；07 号报告 v1.0 |
| 2026-09-12 | §0.2、§0.3、§9E、新增 §9J | 切片顺序 ⑦ 之后直接 ④b；无可视化步骤线 | 插入切片 ⑧「一屏讲清楚」与 V 线（V-0 至 V-11）；⑨–⑪ 待排期 | 用户 2026-09-12：借鉴 Aaronia 与 em-demo 的态势与信号可视化，先写方案、代码待排期（D-061） |
| 2026-09-10 | §9B U-2 行、§9E 切片 ③ 行、§13；`docs/diagram-canvas-guide.md`（改名 `chain-view-guide.md` 并全篇改写）；CLAUDE.md 已定表「框图画布」行、文档表、U 线里程碑 | 框图页有两种形态：缺省典型链路、高级模式自由画布（D-051 ④、D-052）；React Flow 随包（D-032） | **删掉自由画布**，框图页只留典型链路；`@xyflow/react` 与 13 个传递依赖不再随包；`slice3-smoke` 30 项删除；解不成典型链路的框图改为只读展示原文 | 用户 2026-09-10：「自由画布不重要，用户操作起来也很难控制，有点华而不实……先把一个完整的信号级仿真的流程跑通，免得分散精力。后续我想清楚了，再加也不迟」（D-060） |
| 2026-09-10 | §9I R-6 / R-7 行；12 报告 §4.5、§8 风险 6；`docs/{component-catalog, diagram-canvas-guide}.md`；模型卡 | 传播信道有两个变体，「自由空间（定参）」要用户手填距离；D-058 §4.5 给它加了一道频率计划检查拦住「定参 + 高档位」 | **撤掉该变体**，`ch` 只剩「场景绑定信道」，卡片上不再有变体下拉；那条只为它存在的判据随之删掉；组件本身保留供标准算例与自由画布用 | 用户 2026-09-10 实测：「手填一个固定值完全没有必要」。核实成立——框图页上能跑的配置一定有场景，该变体在这里永远是错的选择，撤掉比加闸拦住更彻底（D-059） |
| 2026-09-10 | §0.2、§0.3、§9E 切片表、新增 §9I；CLAUDE.md `:72` / §「工程约定」框图端口条 / D-013 三处 | 传播信道槽位是「场景绑定 / 自由空间」的二选一，与传播**环境**无关——两个变体在物理上是同一个自由空间模型；CLAUDE.md 三处称 `ChannelPathSet`「首期即启用」 | 槽位改成「选档位 + 勾效应」（E1 缺省 = 今天的行为，E2 本期，E3 待 D3），新增 R 线（§9I）与切片 ⑦；`ChannelPathSet` 改为「首期只做类型位与命名预留，实现留 P3」 | 用户 2026-09-10 指示「在这个框图里选择要考虑哪些因素，可以从简单选到细致」；`ChannelPathSet` 一处是逐行核实与 08 §15 对齐（D-058 ⑫） |
| 2026-09-09 | §9H 新增 E 线附表；本表 | 框图页的参数按槽位共享（D-053 ⑨），场景里逐实体的设备参数只能在场景视图改；11 §5.2 承诺的「场景带出的量转派生只读」从未实现 | 框图页加实体选择行、参数分出 `owner`（实例化维度与参数归属维度分离）、加 `byEntity` 逐实体覆盖与「同型号」编辑范围；场景加可选 `equipment_model`；`tx_ant.gain_dBi` / `rx_ant.gain_dBi` / `rx_fe.nf_dB` 改由场景逐实体带出以消掉双真理源；场景改完自动保存。新增 E-1 至 E-7 七步 | 用户 2026-09-09 指示「框图画布面板上方增加选择无人机和选择侦测站两个选择框……让该界面成为场景内实体电子设备参数配置的主界面。电磁传播信道不受两个选择框的影响，单独设置并共用」并拍板四条；D-054。实施中查出两处既有缺陷——场景里逐源的天线增益被静默忽略（`uav-3` 链路预算高估 2 dB）、`golden-03` 未按 D-049 ⑧ 的规范形式落盘（首次保存即打掉三处场景哈希），均按铁律 10 走正式基准变更 |
| 2026-09-09 | §0.2、§0.3、§1.2 端口表、§9E 切片表、新增 §9H；CLAUDE.md `:71` 与 06 §1.2 的口径出入 | §1.2 把 `BearingReport` 列在「观测量归约」而 CLAUDE.md `:71` 说 05 P0 八个端口**全部**只做命名预留，两处对不上；切片表只到 ⑤；`ToaReport` 不存在 | 统一为：`BearingReport` / `PositionReport` 自 D-053 起本期实现（M2 效应级），其余五件仍只预留，另补 05 名单外的 `ToaReport`；新增 L 线 §9H（L-0 至 L-9）与切片 ⑥（⑥a / ⑥b / ⑥c），插在 ④a 与 ④b 之间 | 用户 2026-09-09 提问多源、多站、测向与定位在框图里如何体现并拍板交付范围；D-053；11 报告。05 §3.2 明写「P2 可以先用纯软件多站数据开展」，是本线不必等 P1 / P2 的依据 |
| 2026-09-08 | 10 §5.1 版式图与 §5.2 | 版式图写「圆点：观测点 S0…S5」；实现只写编号、名称藏在悬停提示里；一行卡片吊在中栏顶上 | 观测点**保持链条下方独立一行（链条结构不动，用户拍板）但写全名**；中栏链条区改为占满高度的面板并纵向居中、九个环节按 3 × 3 排且行距拉开、箭头挪到卡片之后消除孤立箭头；另补「勾选需重新运行才生效」提示与结果页中栏的观测点页签。10 §5.2 / §5.7 补落实条款 | 用户实测「S0–S5 勾上和不勾看不出区别」，随后定「不动链条结构、只把编号换成全名」 |
| 2026-09-08 | 10 §0 表第 4 行与 §5.6；09 §17；`docs/diagram-canvas-guide.md` | 「典型链路可单向『展开为自由画布』（展开后不能折回）」 | 两个方向都通：画布工具条「回到典型链路」，解不开时先说明再确认、不静默丢改动；`Alt+2` 与顶栏「框图」回框图页默认形态 | 那句「单向」说的是**文档**的单向性（画布里改过的框图可能解不回九个槽位），不是导航的单向性；实现照字面执行，用户 2026-09-08 实测撞上：进了画布只剩浏览器后退键与手改地址。D-052 |
| 2026-09-07 | §0.2、§0.3、§9B U-2 / U-4 / P1-0b、§9E 切片 ④、新增 §9G | 框图页主形态 = U-2 自由画布（切片 ③「六类组件库拖到画布」）；切片 ④ 一行；U-4 含检测页签；P1-0b 另起对话设计 | 框图页主形态改为典型链路视图（九环节固定、三种信号源模式、只改参数），画布降为高级模式 `#/diagram/canvas`；切片 ④ 重排为 ④a–④d；检测识别与评价页签移入 C-9；P1-0b 由 10 号方案兑现；新增 C 线 C-0 至 C-11、P1-4e / P1-4f、U-5、B-8 | 用户 2026-09-07 指示「框图不这么自由组合了……先给出一个典型的框架……每个框图上进行参数的编辑……给出信号的检测识别与评价……注重实用性，灵活性可适度降低」；D-051；10 报告。技术路线不变：引擎、服务、产品与补取全部沿用 B 线，框图格式只加一个可选字段 |
| 2026-09-06 | §9E 切片 ② 的 e2e 断言；§9A B-4；§9C G-1 / G-2 / G-4 交付物 | 切片 ② 断言写「两时刻**峰值**功率差与 FSPL 差 ≤ 0.5 dB」；B-4 行写「`--scenario-track` 留 G-2」；G-1 交付物列 `geo/include/cuav_geo/scenario.h` 含场景读取；G-4 交付物列 `{ScenarioEditor,SiteTool,WaypointTool,ActivityTimeline,preview}` 五个文件 | 断言改为**带内功率差**（峰值邻域 ±5 bin 在线性域求和）；`--scenario-track` 随 G-1 实现（它只依赖运动学，不依赖 `ScenarioSource`），并加 `--track-rate` / `--scene-root`；场景 JSON 的读取放引擎 `engine/src/scenario_json.cpp`，`geo/` 保持零第三方依赖；编辑器按职责收成 `{ObjectTree,ObjectForm}.tsx` + `editor/{preview,scenarioOps}.ts` + `sceneStore.ts` + `useSituation.ts` + `layers/situation.ts` | G-1…G-5 实施；D-049。三处是实现时核实到的：① 多普勒把谱线在 ±1.2 个 bin 内推来推去，hann 的扇贝损耗最坏 1.42 dB，比单 bin 峰值守不住 0.5 dB，而带内功率与扇贝无关且正是频谱仪「通道功率」的口径；② `--scenario-track` 只跑运动学，把它压在 G-2 后面等于让黄金基准也一起等；③ nlohmann 只 vendored 在 `engine/third_party/`，`geo/` 要读 JSON 就得引第三方件，与它将来独立承载 emcore 移植的定位冲突 |
| 2026-09-06 | §0.3；§9B U-3；§9E 切片 ① | U-3 交付物列 `IQScatter`、文件名 `{SignalView,SpectrumPlot,WaterfallCanvas,EnvelopePlot,IQScatter,viewport,colormap}`；「实时行走 WS 二进制帧追加」隐含结束后仍停在环的画面 | 散点延后（观测点无 `iq` 产品，D-040 ③）；文件按职责收成十二个纯函数 / 引擎模块 + 四个组件（画布绘制集中在不依赖 React 的 `renderer.ts`，组件只转发事件）；任务结束且索引收尾且结束事件已折叠后自动转回看重取（D-048 ②）；事件折叠每帧 ≤ 400 条 | U-3 实施。实测突发下 WS 丢行帧 / 断线重连，环里尾部不完整，只有文件才是终态画面的可靠来源；一帧折叠 2447 条事件是 84 ms 长任务，与画布无关 |
| 2026-09-06 | §0.3；§9B U-1、U-3；§9E 切片 ①；§11.2 | U-3「纵轴 dBm 带标定来源徽标」；§11.2 常数为 `calibration{offset_dB, source}`、DS-8 / DA-8 待做；`scene-smoke.mjs` 不改 | 来源徽标只在开发者模式，用户只见 `dBm`；常数定义为 `full_scale_dBm`，表 `data/iq/measured/calibration.json`，DS-8 / DA-8 已做（−1.6 / −50.0 dBm）；`scene-smoke` 图层数 62 → 63（新增 `aoi-boundary`） | 用户 2026-09-06 指示「纵轴以 dBm 显示，标定来源徽标不给用户显示」；U-1 实现；D-047 |
| 2026-09-06 | §9A B-7；`docs/display-products.md` §2 / §3；`docs/api-versions.md` §3.1b | B-7 交付物列 `{spectrum,envelope,track,detections}.ts`；§2 写「读端只读 `rows` 以内的行」；§3 只列端点与参数，未定归约细节；`scatter`、`track`、`links`、`detections` 与谱包络并列为待实现端点 | 文件按职责收成七个（纯函数与 I/O 分离才能与 Python 逐行对译）；§2 改为「读端以文件长度定行数，`rows` 只判断索引是否收尾」；§3 新增 §3.1–§3.4 定死选择公式、分组边界、统计量的域、就绪语义与错误码；`scatter` 明确本版本 404，三个 JSONL 端点读取层先行 | B-7 实施；D-046。三处是实现时核实到的：① 索引前 64 行根本不存在（`init()` 只建 `.f32`），「读 `rows` 以内」在任务开头等于取不到数据，故引擎改为首行即刷、服务端用 409 而不是 404；② CPython 3.12 起 `sum()` 是补偿求和、numpy 是成对求和，参考实现必须写显式循环才能与 JavaScript 逐位一致；③ `iq` 产品被装载器拒绝（D-040 ③），`scatter` 没有数据源 |
| 2026-09-05 | 新增 §9F；§0.3 树与当前位置；§9D M-5；§11.3；§11.4 / §11.5 角色 | 无辐射源模板条目；公开数据集线只支撑 S4 观测点下游的验证 | 新增 T 线五步（T-0 探针、T-1 规范、T-2 记录、T-3 提取器、T-4 `TemplateSource`、T-5 闭环）与两个数据集的角色 ⑥；M-5 输入含模板 | 用户 2026-09-05 提问「现有数据集能否作为将来模拟特定无人机信号的基础」，确认数据集未记录的量都是配置项，并指示先记决策、更新文档与计划、留待指导实现；D-045 |
| 2026-09-05 | §9C G-5、§9E 切片 ②、§9E 风险、§13 第 5 条 | 「合成场景」小字标注常驻并进截图；demo 场景带合成徽标；「不把合成场景截图当演示」 | 界面不做「合成场景」标记，`synthetic` 只留在场景文件；「不做」改为不把合成结果当实测结论写进文档 | 用户 2026-09-05 指示（D-043）：演示系统不必向用户解释数据是否合成，D-042b 的延伸 |
| 2026-09-05 | §9A B-6；`docs/api-versions.md` §4 | 二进制帧只列字段不定布局；`dropped{from, to}`；补取「缓冲内走 `events()`、缓冲外读文件」未说两者如何衔接 | 帧布局定为 `[u32 LE header_len][JSON 帧头补齐到 4 字节][Float32]`；`dropped` 加 `count`、区间连续且先于下一帧；连接级报文 `seq = 0`；补取与回放共用 `readEvents()`：文件与缓冲紧接才拼、回放以空批次收口、重启后按 `task.json` 合成服务端终态并在对账时把 `last_seq` 推到文件末序号 + 1 | B-6 实施；D-044。`readline` 会吐出末尾半行、`send` 逐条 await 会让 `bufferedAmount` 失效、「不足 limit」收口会拼出缺号，三处都是实现时核实到的 |
| 2026-09-05 | §9A B-5 | 文件 `server/src/tasks/{manager,runner,store}.ts`；提交后直接入队运行 | 按职责收成 `{engine,resolve,store,manager,routes}.ts` + 假引擎夹具；提交时先同步 `cuav_run --validate`，不过即 400 不建任务 | 用户 2026-09-05 拍板；D-042。校验只需几毫秒，失败任务不该进列表；文件拆分让 B-6 只加传输层 |
| 2026-09-05 | §9A B-4、B-5 | B-5「解析成内部参数写入 `diagram.resolved.json`」；B-4 子命令没有数据解析入口 | B-5 改为写解析旁挂 `cuav-resolved/1`，框图副本不含内部参数；B-4 加 `--resolved <旁挂>` / `--data-index <索引>` | D-040：若副本是含内部参数的框图，引擎就得开「信任副本」的口子，D-037「引擎装载器同样拒绝」失效；旁挂让框图永远纯净 |
| 2026-09-04 | §9A B-2、B-3、B-5；§9B U-0、U-1、U-3；§9C G-2 | 回放源以 `manifest_path` 为用户参数；观察者无链路回调；U-1 未定顶栏形态与快捷键；U-3 未定仪表约定 | 回放源改 `data_id` 加内部参数；观察者加 `on_link`；U-1 定面包屑顶栏与 `Alt+1/2/3`；U-3 按 09 §7.2 仪表约定验收 | 09 报告 v1.1 评审「必须改」十二条；D-037 |
| 2026-09-04 | §0.2、§0.3、§0.4、§0.5、§6、§7 D3-0 / D3-3 / D3-5、§9、新增 §9A–§9E、§10、§12、§13、附录 A；版本号 v1.1 | 步骤表只细化到 P1；框图画布与前端留 P2 且无步骤表；「场景与目标活动生成」在 04 与本备忘录都无归属；P1-6 是 JSON 表单；P1-8 未排期；D2 只显示状态流 | 新增 B / U / G / M 四条步骤线与五个纵向切片；P1-5、P1-6 撤销由 B / U 替代；P1-8 由 M-2 兑现；D2 并入 G-5；D3-5 移交 G-2；四份规范骨架落地 | 用户 2026-09-04 定后续重点为界面、场景与目标活动生成、框图模型、波形频谱可视化、后端计算，并追问 MATLAB 优势是否用上；四项拍板见 CLAUDE.md D-030 至 D-036。**技术路线与阶段 0 退出条件不变**，P1 退出条件只把「从浏览器提交」改为「从框图画布提交」 |
| 2026-09-04 | 全篇引用铁律编号处 | 铁律共 16 条 | 新增第 17 条「路径可迁移」，原 16 条编号不变 | 用户提出「不能有绝对目录，都必须是项目内的相对目录，否则项目整体迁移或跨平台就会出问题」。新条只做补充，不改动既有 16 条的编号与内容 |
| 2026-09-04 | §4 D0-1、D0-2、D0-3、D0-4 与验收行；§0.5、§3 的 AOI 行 | AOI 范围 10 × 10 km 暂定；D0-3 需先验证 feature 有无 id，`height` 有则标 `src=osm:height`；D0-4 只作对拍 | 范围定为 20 × 20 km（D-024）；id 已验证为稳定，合并按 id 做，解析复用 `probe_buildings.py`；`src` 改标 `tile:height`，真实来源待 D0-4 的 Overpass 标签；D0-4 增加第二用途 | 用户 2026-09-04 拍板范围；实测发现瓦片 `height` 混合了真实标注与层数推导值，原定的 `src=osm:height` 会把 19.4% 的推导值当成实测标注，违反铁律 14 |
| 2026-09-04 | §4 输入行 | 输入为 `/Users/zhiyu/CC/Airports/tiles/planet.pmtiles` 与 `tiles/dem/` | 改为包内 `data/basemap/planet.pmtiles` 与 `data/basemap/dem/` | 用户指示把约 145 GB 底图与 DEM 拷入本项目自持，消除对外部工程路径的依赖（D-023）。资源内容不变，两项校验与来源逐字节一致 |
| 2026-09-03 | §4 D0-1、D0-2、D0-5 与输入输出行；§0.5 AOI 行；§3 AOI 行 | 临时 AOI 用 em-demo 西安范围；底图按 AOI 裁切为 `basemap.pmtiles`；DEM 按 AOI 拷子集 | AOI 改北京亚运村（用户指定，D-021）；底图与 DEM 改为整份全球文件登记为共享资产 `data/basemap/`（D-022），切片降为测试夹具 | 用户 2026-09-03 两次指示："不放在西安，放在北京亚运村周围"；"我可以用大的呀，128 GB 没有问题"。存储不是约束，不做省空间取舍。技术路线不变，D0-3 起步骤照旧 |
| 2026-09-03 | §2 S-2 | `.gitignore` 排除 `data/**`、`C-UAV Model Demo/**`、`node_modules`、`build`、`dist` | 增加排除 `Datasets1-DroneRFb-DIR/` | 该公开数据集 64 GB，2026-09-03 放入项目根目录，绝不可入库。步骤表编写时尚未纳入 |
| 2026-09-03 | §0.5、§3、§9 P1-1、§9 P1-9、§11.2、§11.3、§12 | 全篇隐含前提是「手上没有任何真实 IQ」，故实测相关步骤一律标「数据到位后」并排在最后 | §0.5 加许可行与防线一；§3 摸底工具解除阻塞；P1-1 拆定稿批次；P1-9 拆 a/b；§11.2 改为通用规则；§11.3 拆两条数据线并新增 §11.4；§12 标注支撑范围 | 2026-09-03 入手公开数据集 DroneRFb-DIR，该前提只对了一半：甲方数据仍未到，但已有一批可解析的真实 S4 观测点 IQ。**改的是顺序，不是技术路线**；阶段 0 退出条件与 P1 退出条件均未变动 |
| 2026-09-03 | §0.3、§0.5、§11.1、§11.3、§11.4 标题、§12 | 「B 组多距离场景完全不能覆盖，是硬伤，只能由甲方数据补上」；许可行只列一个数据集；防线一称四组标定数据「一组都提供不了」 | §0.3 加当前位置一行；§0.5 实测数据行与许可行改为覆盖两个数据集、防线一按新事实重写；§11.1 新增 DroneRFa 八项答案表并重写 P3 四组覆盖表（B 组改「粗档可以」）；§11.3 公开数据集线加 DroneRFa；§11.4 标题加「（Datasets1）」；§12 实测等效行补路损粗档标定；**新增 §11.5 与步骤 DA-1 至 DA-7、防线三** | 2026-09-03 入手第二个公开数据集 DroneRFa，它有三档距离标注、无人机在飞行、并记录了接收增益与天线增益，实测路损指数 n = 1.90。B 组缺口部分补上（粗档结论，非逐米曲线）。**绝对功率标定、逐米路损曲线、视距/非视距标签与建筑几何三项仍完全空缺**，阶段 0 与 P1 退出条件均未变动 |

---

## 附录 A 待用户确认清单（合并 CLAUDE.md OPEN 表）

AOI（已定）；首批频段/带宽/机型；实测数据与八项回答；交付平台基线；缺失引用文件；~~遮挡计算落点~~、~~坐标基座~~（2026-09-17 由 D-074 拍板：浏览器 TS 复算并对拍 / vendored GeographicLib）；高分辨率 DEM 与垂直基准；跨层一致性容差；M3 概念模型编号前缀；参考快照去留；单机启动器形态；C++ 标准是否提升；MATLAB Runtime 工作进程（首期不做）。已确认：Coder 产物许可不受限（2026-09-04）。**已拍板（2026-09-04）**：框图画布 React Flow、引擎桥接子进程 `cuav_run`、场景运行时归属 `geo/`、切片顺序。

## 附录 B 首批标准算例与跨层一致性算例

04 §15.2 十二项：单音频移与功率标度；带限噪声与目标 SNR；突发开关与占空比；多信号同频及邻频叠加；接收滤波与群时延；ADC 量化与削顶；DDC 频移低通抽取；宽带 IQ 到信道化 IQ；实测 IQ 回放；实测背景加合成目标；固定随机种子重复运行；用户算法插件处理。

跨层一致性四项：① 自由空间 + AWGN 单音与突发：EM-S-02 Pd 公式 vs M3 蒙特卡洛；② 建筑遮挡：EM-P-04 附加损耗 vs M3 接收功率统计；③ 双径：EM-P-02 vs M3 复信道功率起伏；④ 混合增强：实测背景 + 合成目标 SNR 归约 vs M2 预测。
