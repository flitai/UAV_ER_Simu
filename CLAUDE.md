# 无人机电子信号侦察仿真系统（804 C-UAV）

面向反无人机电子信号侦察接收机研发的、以 **DDC 后、信道化前宽带复基带 IQ** 为主产品的、实测数据驱动的 B/S 自主框图仿真与算法试验平台。

**系统定位（用户 2026-09-05 再次强调，D-039）：本系统是技术验证的原型系统，用于技术路线验证与功能演示，目的是证明本方在反无人机电子侦察领域的行业理解与技术实力。** 一切取舍据此：演示链路的可见、专业优先；每个演示环节的模型、参数与依据都要在设计文档与模型卡里讲得清楚（**对内要求**），系统界面不展示溯源、不解释数据来源与是否合成（D-042b、D-043）、演示时不主动讲标注与原型说明（**对外像交付系统**）；生产级加固只做演示所需的最小集；量化结论在文档里一律标「原型阶段验证值」（D-028）。**设计真理源 = `04.无人机电子信号侦察仿真系统实施方案_v2.0.md`**（下称 04）；05 为后置能力方案（本期只预留接口，不实施）；01–03 为依据。本文件只记约定、状态和决策，不复述设计内容。

## 文档体系

| 文档 | 作用 | 只读 |
|---|---|---|
| `01.反无人机系统电子信号侦察三级模型体系建设方案 v1.md` | M1 功能级 / M2 信号级 / M3 IQ 级 × E1–E4 × V1–V5 三维正交的顶层框架 | 是 |
| `02.面向反无人机应用的电子侦察IQ级信号模拟仿真概念模型需求分析.md` | M3 层 10 类新增概念模型、IQ-CM-001~012 需求 | 是 |
| `03.基于MATLAB_Simulink的无人机信号侦察仿真系统可行性与技术路线_v3.0.md` | MATLAB 定位为内部工具、11 种标准数据端口、组件生命周期、实测数据资产体系、18 条建设误区 | 是 |
| `04.无人机电子信号侦察仿真系统实施方案_v2.0.md` | **真理源**：B/S 架构、控制面/数据面边界、模型简化策略、IQ 规范、阶段 0–4、验收 | 是 |
| `05.无人机电子信号侦察仿真系统后置功能高阶建设方案_v1.0.md` | P0–P9 后置能力包；P0 的端口/时间/坐标/设备抽象与四态降级语义是本期须预留的接口 | 是 |
| `06.首期实施备忘录_v1.0.md` | 执行层路线图与分步清单（S / D0–D4 / P1 / B / U / G / M 步骤表与纵向切片、当前位置、待用户输入；内文版本 v1.1）；每次对话开工前对照 §0.3 | 否 |
| `C-UAV Model Demo/概念模型/*.md` | 60 份 EM-x 概念模型（公式、接口、参数），M1/M2 层已有模型的实体 | 是 |
| `/Users/zhiyu/CC/EM-C-UAV/{CLAUDE.md, 07.实现2.5D地图总体方案.md, 08.覆盖区域遮挡计算关键技术与创新性.md, 演示软件软件架构.md, 09.模型库C++工程化迁移方案.md, 10.模型库M2服务化深化方案.md}` | 场景数据包 EMScene、IMapQuery、遮挡算法、六边形架构、emcore 迁移史 | 是 |
| `WORKLOG.md` | 工作过程日志，每任务节点立即追加，状态快照置顶 | 否 |
| `07/08/09.*.md` | 设计报告：07 = D3-0 遮挡落点与坐标基座（待写，切片 ⑤）；**08 = 引擎分块流与多速率（2026-09-07 改写为 v2.1，1326 行）**：Block 与时间锚、端口与连线、注册表、JSON 装载、DAG 调度、背压与多线程的留位、多速率与群时延、**参数帧与 IQ 时钟对齐（§9，切片 ② 的设计核心）**、观察者与产品、种子与溯源、共享内存、Coder 产物包装；09 = 主界面、三视图与框图画布交互设计（**2026-09-07 全篇 v1.8，854 行**：前半主界面与三视图，后半 §6.3–§6.11 框图画布交互与 §7.4 对比模式，为 U-2 的实现依据；十三项暂定已拍板 D-038；顶替缺失的原型 html，D-034） | 否 |
| `10.典型链路框图与检测识别评价实施方案_v1.0.md` | 设计报告 10（**2026-09-07，v1.0，D-051**）：框图页默认改为固定九环节的**典型链路视图**（只选变体、改参数、勾观测点，不增删连线），一条链三种信号源模式（全合成 / 实测回放 / 混合增强），U-2 自由画布降为高级模式；定 `AntennaGain` / `ReceiverFrontEnd` / `AdcQuantizer` 三件与 DDC / 信道化的参数面并拍板 D-050 三问；新增特征提取、模板匹配识别、评价器与真值口径，产品 `detections / features / recognitions / truth / metrics`；新增 C 线步骤表（06 §9G）与切片 ④ 重排 | 否 |
| `11.多源多站测向定位实施方案_v1.0.md` | 设计报告 11（**2026-09-09，v1.0，D-053**）：多辐射源在链上**按源分支、接收天线后叠加**；典型链路加**站点维度**与尾部两槽位（测向、多站定位），一条任务跑 K 站；测向与到达时间是 **M2 效应模型**（取真值合法性的正式声明见 §1.3，每行带 `truth_consumed`）；新增端口 `BearingReport / ToaReport / PositionReport`；`sites[].clock` 与时统质量；emcore 的 AOA / TDOA / 测向与 `geo` 四份 golden 移植到 `geo/`（求解器改平面坐标接口，111320 常数只留 `geo::legacy`）；新增 L 线步骤表（06 §9H）与切片 ⑥ | 否 |
| `12.传播环境与效应选择实施方案_v1.0.md` | 设计报告 12（**2026-09-10，v1.0，D-058**）：传播信道槽位由「场景绑定 / 自由空间」二选一改成**选档位 + 勾效应**（E1 快速抽象 = 今天的行为、缺省；E2 工程 = 地面双径**或**城市经验二选一 + 统计阴影 + 大气降雨；E3 精细机理保留枚举但界面置灰、引擎拒，待 D3）；**算在 `geo/` 帧生产端、配在 `ch` 卡片**，参数声明在 `ScenarioSource` 上经槽位表的 `proxy` 代理到卡片；防重复计损 = 一条规则（至多一个替代型主模型）+ 两道闸（单选、运行时报错）；每条链路报告附 `included_loss_terms`；新增 R 线步骤表（06 §9I）与切片 ⑦ | 否 |
| `13.态势与信号可视化提升实施方案_v1.0.md` | 设计报告 13（**2026-09-12，v1.0，D-061**）：借鉴 Aaronia AARTOS 八种表达（`ref/Aaronia AG/`）、em-demo 版式与 T.META 概念图；**数据一件不新造、只改表达**：场景页右栏常驻**目标列表 + 目标卡片栈**（每站一行距离 / 电平 / 信噪比 / 测向 ± 2σ，只摆事实），地图叠加加重（测向渐隐色带、椭圆、40 px 目标图标与选中环、链路距离标注、高度立柱、**圆形告警区** `zones[]` 是威胁色唯一生产者），**全宽时间轴条**（`.app` 网格第四行）联动地图回放与信号游标；两处顺带发现（采用任务不载场景；链路线因标识拆分错从未画出）；新增 V 线步骤表（06 §9J）与切片 ⑧「一屏讲清楚」，⑨–⑪ 待排期 | 否 |
| `docs/` | 代码引用的规范：iq-format / api-versions / scene-package / display-route / **diagram-format / scenario-format / display-products / component-catalog**（2026-09-04 新增，字段已冻结，D-030）/ emitter-template（2026-09-05 草案，D-045）/ **chain-view-guide（2026-09-10 由 diagram-canvas-guide 改名并全篇改写，D-060：框图页即典型链路的操作说明与约束，面向使用者不是规范）** / schemas/ / decisions | 否 |

**分级符号口径**。三个正交维度来自 01，每个结果同时声明；IQ 模型另加 04 的等效档：

| 维度 | 取值 | 出处 |
|---|---|---|
| M 层（模型层次） | M1 功能级 / M2 信号级 / M3 IQ 级 | 01 |
| E 精度（同层内数学细致度） | E1 快速抽象 / E2 工程 / E3 精细机理 / E4 实测融合 | 01、概念模型库 |
| V 可信度（验证程度） | V1 – V5 | 01 |
| L 等效档（仅 IQ 模型） | L1 基础 / L2 工程（默认）/ L3 数据校准；对应 L1 ≈ E1–E2、L2 = E2、L3 = E4 | 04 |

代码、元数据与产物只允许这四种字段。其余符号只在引用原文时使用，且必须带文档前缀（如"03-L2"）：03 的 L0–L2 保真度层级、05 的 F1–F3 运行档位与 U1–U4 城市传播等级、02 对 E 的 IQ 专用释义。

## 铁律

1. **坐标**：交换与文件用 WGS-84 经纬度（度，EPSG:4326）；内部计算 ECEF + 站心 ENU（米）；方位真北顺时针、俯仰水平为 0；禁止 GCJ-02 混入；每个坐标产物记录坐标版本与转换参数（05 §6.2.3）。
2. **高程**：建筑 `base_m / height_m` 为离地高差，DEM 为海拔，禁止隐式相加；首期 LOS 采用显式平地假设（`terrainHeight_m` 常数），DEM 只作 hillshade 视觉；DEM 垂直基准 OPEN。
3. **时间**：逻辑仿真 / 文件采集 / 设备硬件 / 外部统一四重时间不得混用；元数据必填时间基准与连续性标志；失锁、补零、重对齐不得当作连续数据（04 §9.2；05 §6.2.2）。
4. **IQ 语义**：主产品 = S4 观测点（DDC 后、信道化前）；交换/保存复 int16 交织，内部 float32；`|Δf| + B/2 + 保护带 < Fs/2`，抽取前必抗混叠；专业模型组不得自行改变 IQ 语义（04 §5.2、§9、§14）。
5. **单位与 dB**：SI（m / Hz / dBm / deg / s）；dB 域与线性域不混算，功率聚合在线性域；损耗 dB 正值 = 变差（emcore README）。
6. **离线**：建库可联网、运行不联网。Overpass、planet extract 等联网动作只允许出现在 `scene/` 建库脚本；底图、字形、sprite、字体随包；交付环境不依赖 CDN / 在线字体 / 在线地图 / 外部身份（04 §6.5；EM-C-UAV 07）。
7. **原始 IQ 不进浏览器**：浏览器只收控制、状态与按视窗（时间窗 + 频段 + 像素宽 + 统计量）抽取的展示数据；大文件分片续传，禁 JSON/Base64 封装；浏览器不缓存原始 IQ（04 §6.4、§8.6）。
8. **溯源**：每个模型输出携带 `model_id / model_version / model_level(E) / model_layer(M) / credibility(V) / parameter_version / confidence / trace_id`，即同时声明 M/E/V 与 L 等级；每个数据产物挂同构元数据（来源、参数、输入哈希）（emcore README 六件套 + 01 §3.1 扩展；04 §9.2）。
9. **种子**：随机性显式注入（`IRandom&` / 请求 `seed`），库内无全局随机源；固定种子逐位复现；随机量用蒙特卡洛矩校验（04 §8.3、§15.2）。
10. **基准不得静默变**：任何模型/参数/数据集变化不得静默改变既有基准结果；超差 = 发现，查根因，禁止调参使其通过；黄金向量相对误差 ≤ 1e-9（04 §15.3；emcore golden）。
11. **渲染—物理同源**：建筑几何单一来源 `buildings.geojson`（`{id, base_m, height_m, src, footprint}`）同时驱动 `fill-extrusion` 渲染与遮挡桶网格；瓦片 `buildings` 层只作 AOI 之外的渲染回退，永不进遮挡计算（EM-C-UAV 08 §2.3；D-002 / D-010）。
12. **几何 / 材质 / 无线参数分管**：三维好看 ≠ 传播可信；建筑几何、电磁材质、无线参数分开管理、各自版本化；首期建筑只用于 LOS/NLOS、附加损耗与场景分类（03 §7.3；04 §7.3；05 §25）。
13. **第三方代码边界**：`foundation`（AFSIM 抽取，已获授权）可 vendored，放 `third_party/foundation/`，保留原文件头；emcore / em-demo 自研代码可拷，文件头注明来源与 golden 对应关系；Airports 为本方自有代码可复用，其 `vendor/` 第三方件与 OSM 瓦片的 ODbL 署名必须保留；MATLAB 仅内部模型生产工具，不作运行时依赖，界面与交付包不出现（03 §11；04 §6.2、§8.5）。
14. **合成与估算数据的标记留在数据层**：`src=est:*`（估算建筑高度）、合成底噪等标记随字段传到每个下游产物、清单与文档（04 §9.2），任何计算与统计都能据此区分；**界面不解释数据来源与估算**（演示系统只要求数据合理可算，D-042b），来源分色与占比只在开发者模式 `?dev=1` 作内部诊断。「不得用于验收指标」这类限定写在文档与模型卡里，不写在界面上。
15. **不静默降级 + ASCII 文件名**：结果状态四态 `valid / degraded / invalid / not_applicable`，禁止默认值顶替（如 `height ?? 8`）；Web 资源与数据包文件名纯 ASCII，出包执行 `find dist -type f | LC_ALL=C grep -nP '[^\x00-\x7f]'`（05 §16.3；em-demo 打包说明）。
16. **目标平台**：客户端与单机一体化包 **Windows x64 优先**；内网集中模式的服务端（应用服务 + C++ 引擎 + 数据库）**Windows 或 Linux x64 视情选择**，二者从第一天起都进 CI 构建。开发机是 macOS，其结果不作验收依据；打包与性能数字只认目标平台原生验证。引擎与服务不得直接调用平台私有 API，共享内存、内存映射、路径与编码一律经抽象层（D-015 / D-016；04 附录 B；方法论阶段 7）。
17. **路径可迁移**：入库文件里不得出现机器相关的绝对路径（`/Users/`、`/home/<用户名>`、`X:\Users\`）。路径一律从文件自身位置推导（Python `__file__`、Node `import.meta.url`、CMake `CMAKE_CURRENT_SOURCE_DIR`），或由命令行参数与环境变量给入；产物清单里仓库内的路径写成**相对仓库根目录**的形式，记录的命令只写工具名不写工具的绝对位置。外部参考工程的实际位置只在本文件的资源清单里记一处，代码注释引用它时只写工程名与文件名。数据清单里记录外部**历史来源**的 `origin` / `dev_link_target` 是唯一例外，它们是留档不是可解析的位置，任何代码不得解析。出包与提交前执行 `scripts/check-paths.sh`（已接入 `scripts/build-all.sh`）。

## 资源清单 · 只读边界 · 复用边界

| 路径 | 角色 | 复用方式 | 备注 |
|---|---|---|---|
| `C-UAV Model Demo/` | 只读参考快照，与 `/Users/zhiyu/CC/EM-C-UAV/` 逐字节相同 | 不在其中开发；拷入对应目录并注明来源 | 保留快照还是直接引用 EM-C-UAV：OPEN |
| `C-UAV Model Demo/em-demo/` | 态势展示参考（React 19.2 / TS 6.0 / Vite 8 / MapLibre GL 5.24 / pmtiles 4.4 / protomaps-themes-base 4.5 / Tauri 2.11） | 可拷：`src/map/basemapStyle.ts`、`src/components/{MapView,SignalPanel,CoverageControl}.tsx`、`src/models/{occlusion,coverage,geo-fix}.ts`、`src/simulation/engine.ts`、`scripts/fetch-buildings.mjs`、`桌面应用打包说明.md` | 前端 TS 自算物理，与 emcore 运行时零耦合；Tauri 无 sidecar；本项目不用 Tauri |
| `C-UAV Model Demo/emcore/` | C++14 模型核心（CMake ≥ 3.16，vendored doctest / nlohmann / cpp-httplib，零网络构建） | 可拷：`include/emcore/map/{imap_query,local_scene_adapter}.h`、`models/{occlusion,propagation}`、`core/{types,units,result,random,geo}.h`、`tests/golden/*.json`、`tests/golden_util.h` | 遮挡 148 例 golden；`emsvc` 5 端点 `/api/v1/{health, models/catalog, radar/detect, signal/detect, los/check}` |
| `C-UAV Model Demo/foundation/` | AFSIM 2.9 抽取：math 347 / io 151 / serialization 22 文件 | 可 vendored（D-004）：`UtEllipsoidalEarth`（WGS-84 LLA↔ECEF↔ENU/NED、UTM、Vincenty）、`UtSphericalEarth::MaskedByHorizon`、`UtCoords`、`UtCovariance*`、`UtAzElTable*`、`UtIntersectMesh`、`UtSpatialTree` | **无建筑遮挡库**；不能独立构建：无顶层 CMakeLists，134 个头引用缺失的 `ModelApi.hpp` / `MODEL_API`；target 实名 `model_math / model_io / model_serialization`；零单测 |
| `/Users/zhiyu/CC/Airports/` | 本方自有 2.5D 离线地图工程（MapLibre 5.6.1 + PMTiles + Python 标准库 Range 服务器） | 可拷：`serve.py`（Range）、`fetch_tiles.py`、`fetch_levels.py`、`download_offline_maps.py`、`index.html` 中的 `protomapsStyle()`（60 层含机场要素）/ `localglyphs://` / `probePmBldg()` / `window.__probe`；文档 `TILES.md`、`OFFLINE.md` | 无遮挡计算；未开 `setTerrain`；原生 JS 需手工切成 TS 模块 |
| `/Users/zhiyu/CC/Airports/tiles/planet.pmtiles` | 全球 Protomaps 底图 z0–15，137370745450 B，planetiler 0.10.2，OSM 2026-08-17；`buildings` 层 z11–15 含 `height / min_height / kind` | 只读留档。**2026-09-04 已整份拷入 `data/basemap/planet.pmtiles` 由本项目自持（D-023）**，代码与脚本一律引用包内路径；本行只记来源 | 缺瓦片渲染为空白不降级，AOI 档案 zoom 必须"满" |
| `/Users/zhiyu/CC/Airports/tiles/dem/{z}/{x}/{y}.png` | AWS terrarium DEM z0–8，87381 文件、7442617745 B | 只读留档；同样已拷入 `data/basemap/dem/`（D-023）。hillshade 用 | 百米级/像素，对街区 LOS 无用；高分辨率 DEM OPEN |
| `/Users/zhiyu/CC/C-UAV/天津机场_地表材质与建筑数据工作流.md` | 地表材质面层 + 建筑 GeoJSON 的 QGIS 工作流 | 只参考（材质分类与 εr/σ/roughness 默认表） | 双径地面参数将来用 |
| `data/` | IQ、共享底图与 DEM（`basemap/`）、场景数据包、黄金基准 | 大文件不入 git，只入索引与元数据；`data/basemap/{planet.pmtiles, dem}` 是包内自持的真实文件，约 145 GB（D-023） | 每个产物挂同构元数据；`data/basemap/*.manifest.json` 已生成（D-022） |

## 首期边界与架构决策

首期基线见 04 §21；不做事项见 04 §3.4；后置能力见 05（P0 端口八件中，`BearingReport` / `PositionReport` 自 D-053 起**改为本期实现**（M2 效应级，L 线），`ChannelPathSet` **只做类型位与命名预留、本期不实现**（D-058 ⑫ 对 D-013 原表述的改正），与其余五件 `ArrayIQStream / MultiSiteIQSet / CalibrationSet / TrackReport / DeviceStatus` 本期**只做命名预留**，不实现；另补 05 名单外的 `ToaReport`，见 D-053 ⑥）。首期单站、单通道、离线/准实时，常用 5–50 MS/s，100 MS/s 仅文件兼容与短窗（04 §12.1）。**「单站」自 D-053 起限定为「单站硬件形态」**：一条任务可以同时跑 K 个站点的接收链（纯软件多站，05 §3.2 允许），但每站仍是单通道、无阵列、无真实时钟同步。

**已定**

| 项 | 决定 | 出处 |
|---|---|---|
| 运行形态 | B/S；单机模式由本地启动器拉起应用服务并打开系统浏览器；**不用 Tauri** | 04 §2.5 / §6.5；D-003 |
| 前端 | TypeScript + React 19 + Vite；MapLibre GL **pin 5.24** + pmtiles 4.4，全部 vendored 离线；地图图层与显示风格照搬 Airports 手写样式，不引入 `protomaps-themes-base` | 04 §6.3；D-003 / D-008 / D-017 |
| 应用服务 | Node/TypeScript，REST + WebSocket（事件带序号可补取）；**必须支持 HTTP Range**（PMTiles 需要；em-demo 整文件 fetch 只是 Tauri 权宜） | 04 §6.3 / §8.6；D-003 |
| 运行引擎 | C++ 分块流 + DAG 调度，同进程 / 共享内存；C++14 起（与 emcore 兼容，提升标准 OPEN） | 04 §6.3 / §8.3 |
| 数据 | SQLite 单机 / PostgreSQL 内网；IQ 项目二进制 + SigMF 兼容元数据 | 04 §6.3 / §9 |
| 场景—框图集成 | 三视图 **场景 / 框图 / 结果** + 顶部试验上下文；场景中的站点、无人机航迹、建筑在框图中以"场景绑定"信道/天线组件出现；地理传播几何（LOS/NLOS、附加损耗、多径路径）由服务端 `geo/` C++ 库按 10–100 Hz 计算，经 `SceneParamFrame` / `ChannelPathSet` 端口交 IQ 引擎施加，浏览器只展示 | D-001 / D-013；05 §5.2 |
| 显示子线优先级 | 显示子线 D0–D4 不进入阶段 1 退出条件，不得挤占 IQ 主链 | D-001 |
| 界面不替用户下结论 | 界面只摆事实（机型、视距、距离、质量状态、验收集等**标签**照实写），**不写解释句**——「这一段在验收集里，结论不能再当作独立验证」「按高度来源分色」「合成场景」这类话一律不做。数据层的标记不受影响，照旧随产物走（铁律 14）；要诊断走 `?dev=1` | D-039、D-042b、D-043、D-056 |
| AOI | 北京亚运村周围 `116.288,39.900,116.522,40.080`，中心 (116.405, 39.990)，19.96 × 19.97 km；定义在 `scene/aoi/beijing-yayuncun.json` | D-021 / D-024 |
| 常数策略 | 从 emcore 移植的模块保留原常数守 golden（光速 `3e8`；遮挡投影 `110540 / 111320·cosφ`；定位投影 `111320` 双向；TDOA `299792458`）；新写代码统一 `c = 299792458` 与严格 ENU；禁止"顺手统一" | emcore README；D-009 |
| 交付平台 | 客户端与单机一体化包 **Windows x64 优先**；内网集中服务端 **Windows 或 Linux x64 视情**（`server/`、`engine/`、`geo/` 双平台构建）；浏览器基线须支持 WebGL2（MapLibre GL 5 不支持 WebGL1）；macOS 仅开发 | D-015 / D-016 |
| 引擎运行形态 | 子进程 `cuav_run`（`--catalog / --validate / --run / --scenario-track`）+ 定长二进制产品文件 + stdout JSON 事件；不做 N-API、不做引擎内嵌 HTTP；「同进程 / 共享内存」指组件与调度器同进程，不指嵌入 Node | D-031 |
| 框图画布 | **已删除（2026-09-10，D-060）**：框图页只留典型链路一种形态，`@xyflow/react` 连同 13 个传递依赖不再随包，前端产物 1679434 → 1472301 字节（−202 KiB，−12.3%，实测）。~~React Flow~~ 那半条作废；本项目自身状态管理仍是零依赖 `useReducer` + context；路由手写 hash | D-032、D-060 |
| 服务端 WebSocket | `ws` 8.21.3 随包锁版（已装，署名进仓库根 `THIRD-PARTY-NOTICES`）；服务端「零运行时依赖」改为「只加 `ws`」；帧布局与补取约定见 D-044 | D-032 / D-044 |
| 场景运行时 | 运动学与链路预算在 `geo/` C++ 库，引擎组件 `ScenarioSource` 薄封装；参数帧按样点序号推进；浏览器只做插值预览并与 C++ 输出 golden 对拍 | D-033 |
| DSP 组件生产路线 | 滤波、DDC、信道化 = MATLAB Coder 生成 C，入 `models/<环节>/coder/` 带来源头；手写 C++ 只用于算术简单件与引擎骨架；参考模型 MATLAB 为主、Python 为独立第二实现 | D-036 |
| 功率单位与 dBm 纵轴 | 引擎内部 `|x|² = 功率 / mW`，合成源按 dBm 给参数、回放源按清单常数换算，谱产品直接是 dBm；两个公开数据集的常数为原型阶段估算值（`data/iq/measured/calibration.json`）；界面只标 `dBm`，来源徽标只在 `?dev=1` | D-047 |
| D-048 | 2026-09-06 | **U-3 信号视图的约定**：① 两种模式——跟随（画环里最近 H 行，1 行 = 1 设备像素行，频率全带，离屏画布位移追加）与回看（B-7 抽取的矩阵整窗画；150 ms 去抖、同键忽略、换键中止在飞、代数校验丢陈旧、409 按 `Retry-After` 重试上限 10 次、413 用 `suggest` 重试一次、包络 404 只画谱）；任何缩放 / 平移 / 游标 / 取消勾选即回看，勾「跟随实时」或新任务回跟随；采用已结束的任务直接回看（`since = last_seq` 不回放，环里没有实时行）；② **任务结束即收口**：索引自身 `run_state` 终态且 `index_final`、且本地已折叠到流里的结束事件（`resultProvisional = false`）三条同时成立，才把当前画面转成回看窗口重取——突发下服务端按 D-044 丢行帧或断线重连，环里的尾部与缺口只有文件才完整（实测切片 ① 的 1953 行在 82 ms 内到达时一次跟随画面停在第 1877 行）；只看任务事件的终态会用陈旧索引算窗口，只看订阅应答会在环还空着时收口，两个竞态都在 e2e 里踩到过；③ 两模式一套公式：跟随模式的像素列归约（`groupBounds`、`max/min` 比较、`mean` 线性域顺序累加）与服务端逐行对译，像素 ← 源项 `floor(p·n/P)` 最近邻不插值，前端同样守 `tests/golden/product-window.json`；④ 色带 viridis 10 锚点分段线性 256 级随包内嵌，频谱与瀑布共用 `[ref − range, ref]`，自动量程 `ref = ceil10(最大值)`、`floor = floor10(10% 分位)`、只扩不缩并回写面板；⑤ 包络竖条在瀑布右侧与瀑布共时间轴（一像素行一组，回看用包络端点 `px = H`），时间游标是贯穿两者的同一条横线，`20·log10` 后 min–max 带 + rms 线，`sqrt_mW` → dBm、`linear_FS` → dBFS，自有量程；⑥ 鼠标：左键拖 = 框选（瀑布时间 × 频率、频谱只频率），双击 = 复位全窗，滚轮 = 时间缩放，Shift+滚轮 = 频率缩放，Alt+左键 / 中键 = 平移，单击 = 放 M2；键：`M` 放 M2（悬停处，否则 M1 处）、`Delete` / `Backspace` 删、`←→` 游标一帧、Shift 十帧，只在信号页且焦点不在输入框；⑦ 丢行只在跟随模式按 `row_index` 跳变画斜纹（回看时文件无洞），未到的行画纸色并在页脚写「已收 n 行」；⑧ 只缓存最近一次抽取（键 `task, op, t0, t1, f0, f1, px, py, stat, envPx`），量程变化只重查色表，瓦片化留 U-4；⑨ IQ 散点无数据源（观测点不产 `iq`，D-040 ③）不做占位面板；检测叠加与对比观测点留 U-4 / 切片 ③；⑩ 页头恒为固定行数（开发者标定行与索引摘要常驻、单行截断），否则索引到达时页头长高会把瀑布挤矮并触发整幅重画；状态条的序号靠 `WsClient` 在序号推进后节流上报（250 ms），此前只在状态迁移与 `dropped` 时上报，不断线跑完一个任务读数会停在订阅时的 `since`（内部 tracker 不受影响，只是读数陈旧）；观测点产品非 `valid` 时在瀑布左上角叠一行原因（09 §13.2，纯函数 `productStateNote`），已用回放任务的真实 `degraded` 产品验证；⑪ 事件折叠每帧最多 400 条——切片 ① 的 2447 条事件 82 ms 内到齐，一帧全折叠是 80 ms 长任务（实测 84 ms，与画布无关）；地图加载中的瓦片入库本身就是 88–106 ms 的长任务，验收在地图空闲后计。实测（macOS，2560 × 1440，原型阶段验证值）：整任务 22 次渲染最长 29.9 ms、地图空闲时运行期间无 > 50 ms 长任务；回看 893 × 1024 全窗；e2e 39 项 | 用户 2026-09-06「进行下一步 U-3」；09 §7.1–§7.2、§10、§11；06 §9B；D-046、D-047；实现 `web/src/signal/{viewport,reduce,colormap,scale,trace,envelopeMap,fetcher,viewStore,waterfall,renderer,export,commands}.ts` + `{SignalView,SignalHead,MarkerBar,InstrumentPanel}.tsx`、`web/src/api/window.ts`、`web/src/shell/longTasks.ts` |
| 辐射源模板 | 实测数据集作辐射源模板（回放 / 特征等效两模式）与背景的素材；模板归一化到单位功率、与位置无关，绝对电平由场景配置经链路预算得出；首选特征等效再生；回放模式的信噪比上限 = 模板余量 − 裕度；规范 `docs/emitter-template.md`，步骤 06 §9F | D-045 |
| 框图形态 | 框图页默认 = **典型链路视图**（04 §5.1 九环节固定为九个槽位，只选变体、改参数、勾观测点；一条链三种信号源模式；只有 DDC 与信道化可旁路）；U-2 自由画布降为高级模式 `#/diagram/canvas`；典型链路是标准框图的生成器，框图格式只加可选 `template_ref`；典型链路里接收机噪声由噪声系数生成（取代手兑噪声，既有示例与产品基准不动）；ADC 削顶为数据标记；识别基线 = 模板加权匹配到 `signal_role` 层；评价 = 帧级 Pd / Pfa / F1、突发级检出与发现时延、ROC（门限扫描不重跑）、混淆矩阵；框图开始落盘 `data/diagrams/` | D-051；10 报告 |
| 多源多站表达 | 多辐射源**按源分支、接收天线后叠加**成一路 IQ（新组件 `Superposition`）；典型链路加**站点维度**，接收天线之后每环节按站实例化，尾部加「测向」`DirectionFinder` 与「多站定位」`MultiSiteLocator` 两槽位（槽位 9 → 11）；测向与到达时间是 **M2 效应模型**（按误差预算给量测，取真值合法但每行须带 `truth_consumed`）；固定可选口 `in1..8 / scene1..8 / b1..8 / t1..8` 取代动态端口，N、K ≤ 8；实例后缀 `__`，N=K=1 时无后缀且既有框图逐字节不变 | D-053；11 报告 |
| 实体设备参数配置 | 框图页 = **场景内实体电子设备参数配置的主界面**：面板上方两个下拉选无人机与侦测站，**只切换配置焦点、不改运行范围**；`SlotDef` 分出 `owner`（`tx` / `tx_ant` 归源，`rx_ant` 及其后归站，`ch` 与 `loc` 共用，**传播信道不随下拉变化**）；参数两层持久（共用底值 + `byEntity` 逐实体覆盖），**型号是编辑范围不是存储层级**；场景加可选 `equipment_model`（辐射源缺席回退 `platform_type`）；`tx_ant.gain_dBi` / `rx_ant.gain_dBi` / `rx_fe.nf_dB` 改由场景逐实体带出以消掉双真理源；场景改完**自动保存**（去抖 800 ms） | D-054 |
| 传播环境与效应 | 传播信道槽位 = **选档位 + 勾效应**（用户 2026-09-10：「反映所处传播环境、地理空间、建筑物分布带来的变数……从简单选到细致」）。档位用 01 的 E1–E4 并直接写进 `model_level`：**E1 = 今天的行为、缺省**；E2 = 地面双径（EM-P-02）**或**城市经验（EM-P-05）二选一 + 统计阴影（EM-P-08）+ 大气降雨（EM-P-07，移植 emcore）各可单独开关；**E3（建筑遮挡与刀口绕射）枚举保留、界面置灰、引擎 `configure()` 收到即报错**，待 D3。**算在 `geo/`（帧生产端）、配在 `ch` 卡片**（槽位表 `proxy` 把 `ScenarioSource` 的参数代理过去）；该槽位**只剩「场景绑定信道」一个变体**，距离由航迹每 1/20 秒重算（D-059 撤掉手填距离的「自由空间（定参）」）；至多一个替代型主模型，附加项经 `extra_loss_dB` 汇入，帧结构不变；链路报告附 `included_loss_terms` | D-058 |
| 态势与信号可视化 | 借鉴 Aaronia 与 em-demo，**数据不新造、只改表达**（用户 2026-09-12）：场景页右栏常驻目标列表与卡片栈，卡片只摆事实（无威胁 %、概率 %）；地图叠加加重；**全宽时间轴条**是 `.app` 网格第四行，不是浮动面板或可停靠窗口；圆形告警区是场景对象 `zones[]`、引擎收下不解释、是「威胁红黄绿」唯一生产者；不做深色主题、彩虹色图、卫星底图；底部信号条留 V-10（需先把信号视图的三个模块级单例改按实例持有） | D-061；13 报告 |

**OPEN**

| 项 | 候选 | 决策时点 |
|---|---|---|
| 遮挡计算落点 | server 预计算场 + WS 推送 / 浏览器 TS 复算并与 C++ golden 对拍 | D3 设计报告 |
| 坐标基座实现 | foundation `UtEllipsoidalEarth` vendored / GeographicLib；G-1 先用自写 WGS-84 闭式临时件顶上（D-035） | D3 设计报告 07 回收 |
| MATLAB Runtime 独立工作进程（04 §8.5 第 2 档） | 首期不做 / P3 遇 Coder 不支持的算法再评估 | P3 |
| 高分辨率 DEM 与垂直基准 | Copernicus 30 m / 本地 DSM | D0 |
| 首批频段/带宽/机型 | 04 附录 B 清单 | 阶段 0 |
| 缺失引用文件 | 主界面交互原型 html、两阶段建设方案、signal signature database、实施方案 v1.0、01/02 三张架构图 | 阶段 0（阻塞退出） |
| 参考快照去留 | 保留 `C-UAV Model Demo/` / 直接引用 EM-C-UAV | 目录建立时 |
| 跨层一致性容差 δ | 按指标分别冻结，参考 04 §16.3（检出率差 5–10 个百分点、功率统计 10–20%） | 阶段 1 设计报告 |
| M3 新增概念模型编号前缀 | `EM-Q-xx` 候选 | 阶段 0 |
| 交付平台基线 | Windows 版本、浏览器（Edge / Chromium 内核国产浏览器）、MSVC 版本、Node LTS 版本、Windows 构建验证机或 CI；服务端若选 Linux：发行版（Ubuntu LTS / openEuler / 麒麟服务器版等）与 glibc、GCC 基线 | 阶段 0（04 附录 B） |
| 单机启动器形态 | `.bat` + 便携 Node / 单文件 exe / Windows 服务 | 阶段 2 设计报告 |

## 地理场景 / 态势显示子线

三条路线的决策如下，细节进 `docs/display-route.md`（待写）。

- **2.5D 地理环境（照搬 Airports）**：底图直接用整份全球 `planet.pmtiles`（137 GB，登记为共享资产 `data/basemap/`，D-022），不按 AOI 裁切；`scene/fetch_tiles.py` 裁出的 `data/scene/<aoi>/basemap-slice.pmtiles` 只作测试夹具（z0–15；建筑几何 z ≥ 11；档案必须"满"，AOI 外空白）。**图层顺序与显示风格逐项照搬 Airports `index.html`**：`protomapsStyle()`（810–1092 行，60 层浅色样式与 `PM` 配色表，含机场要素）、hillshade 参数（1218 行）、建筑拉伸外观（`addBldg3d()` 1296 行：`#e6e1da`、minzoom 14、14→14.7 渐显）、相机与控件（`newMap()` 1375 行、pitch 55、`NavigationControl({visualizePitch:true})`）、标签与字形（`localglyphs://` 拉丁 SDF + CJK 交给系统字体）、`probePmBldg()` 探测与 `window.__probe` 探针。只把原生 JS 切成 TS 模块，不改视觉参数；**不采用** em-demo 的 protomaps `black` 深色主题与 `protomaps-themes-base`。AOI 内建筑改用同源 GeoJSON 拉伸，但外观参数与 Airports 瓦片建筑层一致；AOI 外保留 Airports 瓦片建筑层（铁律 11）。任何视觉偏离 Airports 须记决策。
- **建筑数据（D-010）**：从**同一份** planet.pmtiles 的 z15 瓦片解码 `buildings` 层、跨瓦片合并去重 → `data/scene/<aoi>/buildings.geojson`，字段 `{id, base_m, height_m, src, footprint}`；`height` 缺失时按 em-demo `fetch-buildings.mjs` 的用途/面积分档确定性估高并记 `src=est:*`；缺高度地区后续逐步用 Overpass 全标签或人工数据补齐（只升级 `src` 与高度，不改几何 id）。国内城市 OSM 高度覆盖极低，这是首个已知数据欠项：西安 12×12 km 样本 13,593 栋中 height 1.0%、levels 1.3%、`building=yes` 96.1%；**北京亚运村 20×20 km 已按原始 OSM 标签定案（D-027b）：47,582 栋中真实 `height` 标注 1.74%、层数折算 19.59%、估算 78.55%**。瓦片 `height` 混合了真实标注与推导值，干净的 `src` 只能靠建库阶段拉 Overpass 原始标签（已于 2026-09-04 拉过一次）。要素带稳定 id，跨瓦片合并按 id 做。
- **无人机 / 电磁态势（参考 em-demo）**：图形语义沿用——威胁红/黄/绿、效应染色优先于威胁等级、AOA 与 TDOA 双色区分、覆盖 viridis 5 档 + 值域不透明度、iso-Pd 包络（marching squares，画在楼之上）；PPI 扫描 / 测向线 / 2σ 误差椭圆走 Canvas 叠加层并用 `map.project` 投影；定频 tick（20 Hz）+ 每帧一次状态提交；无人机图标 SVG 光栅化 `source-in` 染色、`icon-rotate` 航向；航迹抽稀。**具体色值须在 Airports 浅色底图上重新标定**（em-demo 的 AOA 橙 `#f97316`、TDOA 青 `#22d3ee` 等是为深色底图调的），标定结果进 `docs/display-route.md`。参考 `em-demo/src/components/{MapView,CoverageControl,SignalPanel}.tsx`、`src/models/coverage.ts`、`src/simulation/engine.ts`。
- **地理计算与遮挡（D-005）**：`IMapQuery`（`getTerrainHeight / queryBuildings / raycast / getMaterial`）+ `LocalSceneAdapter`（100 m 桶网格 + DDA，命中语义"侵入最深的等效单刀口"）+ `models/occlusion`（ITU-R P.526 刀口衍射）自 emcore 移植到 `geo/`；遮挡是软的：雷达 `SINR − 2·L_diff`、单程 `P_rx − L_diff`，实时与覆盖场同一注入点；移植后必须通过 148 例 golden 对拍（rel ≤ 1e-9）+ 独立解析锚点（如 FSPL@2.4 GHz/1 km ≈ 100.1 dB）才算可信。

## 功能级—信号级衔接（M1/M2 ↔ M3）

本系统按 01 口径 = **M2 信号级 + M3 IQ 级**（口语"信号级"包含二者）。`概念模型/` 60 份是 M1/M2 层专业模型单元（01 §9.2），在本系统中只有两种角色：**向下的参数供给层**与**向上的归约目标层**；IQ 级不是另一套模型，而是同一条电侦模型链的高粒度展开（02 §6.1）。概念模型库表头没有 M 层字段，按 01 §3.5 判据的归属如下（逐份确认后写入 `docs/concept-model-layers.md`）：

| 族 | M 层 | 在本系统中的角色 |
|---|---|---|
| EM-B-07 天线 / B-11 接收机基础 / B-12 噪声 | M1 参数供给 | 复增益与极化损耗 / 接收采样链能力边界（NF、带宽、增益、AGC、阻塞）/ 样本级噪声的目标 PSD |
| EM-B-09 信号抽象 / EM-T-03 辐射源特性 | M1（E1/E2），E3 时频包络进 M2 | 波形模板与业务活动的输入（频率计划、带宽、功率、谱形、突发/跳频摘要、调制家族）；保持"不生成 IQ" |
| EM-B-10 频谱栅格 | M2 | M3 归约后时频观测产品的容器；也为 IQ 任务选频段、窗口与背景源 |
| EM-P-01/02/04/08/13（P-03/05/07 备用） | M1/M2 | 路损、双径、建筑遮挡、阴影、模型选择与降级 → 慢变信道参数；P-11 射线追踪与 P-12 缓存 → 后置 P3 的 `ChannelPathSet` |
| EM-S-01 扫描策略 | M2 调度 | IQ 捕获窗口：时间窗、中心频率、驻留、采样上下文 |
| EM-S-02/03/04 检测、特征、识别 | M2 裁决 | 只消费 M3 归约出的观测量，不碰原始 IQ（02 §8.5）；其 Pd 曲线与特征模板是 M3 蒙特卡洛的校准对象 |
| EM-S-05/06/07 测向定位 | M2 | **自 D-053 起以 M2 效应级实现**（纯软件多站，05 §3.2）：`DirectionFinder` / `ToaEstimator` / `MultiSiteLocator`，从链路帧取真值按误差预算给量测，是算法的统计模型不是被测算法（11 §1.3），每行 `truth_consumed=true`、`credibility ≤ V2`；阵列与 IQ 相关到达时间仍后置 P1/P2，届时接同一端口替换。EM-S-08 频差定位不做 |
| EM-S-10/11/12 运动估计、关联、退化 | M1/M2 | 退化模型提供 IQ 质量标记的映射（削顶、镜像、缺样 → 检测/识别质量修正） |
| EM-T-04 建筑材质 | M2 参数 | 首期不用（铁律 12），P3 启用 |
| EM-R / J / H / N / C / F 六族 + EM-T-01/02（雷达目标 RCS、微多普勒） | M1 效应裁决 / 雷达目标特性 | **04 电侦范围外，不使用**；保留引用，如需纳入须修订 04 |

**三条衔接机制**

- **向下参数注入（M1/M2 → M3）**：功能级模型按 10–100 Hz 输出慢变参数（链路几何、LOS/NLOS、各损耗分量、噪声底、天线复增益、扫描窗口、多径路径），M3 组件（波形生成、时变复信道、噪声注入、接收采样链、信道化）在采样率上施加（03 §11.7；04 §12.2；05 §5.2）。参数只在 M3 组件的"施加"环节进入 IQ，M3 不得绕过 M1/M2 自行硬编码传播或噪声参数。
- **向上归约（M3 → M2 → M1）**：IQ → 内置"IQ 观测量提取"（FFT/PSD/瀑布、能量检测、突发统计、参数估计、特征、TOA/方位）→ `SpectrumFrame / DetectionList / FeatureVector / BearingReport` → 批量蒙特卡洛 → Pd/Pfa 曲线、混淆矩阵、σ_θ、CEP 分布 → 回写为 M1/M2 模型的校准参数（01 §8.2、§12.4；02 §9.2）。归约必须在统一场景、参数版本、随机上下文与统计口径下进行。
- **跨层一致性约束（01 §8.3）**：同场景、同种子、同参数版本下，M3 归约到 M2/M1 的统计量与 M1/M2 直接计算值的相对偏差 δ 超容差 = 发现，追查抽象误差 / 参数不一致 / 样本不足 / 适用范围，禁止选有利结果。首批跨层一致性算例（进 `tests/regression`，与 04 §15.2 十二项并列）：① 自由空间 + AWGN 单音与突发：EM-S-02 Pd 公式 vs M3 蒙特卡洛 Pd；② 建筑遮挡：EM-P-04 附加损耗 vs M3 接收功率统计；③ 双径：EM-P-02 vs M3 复信道功率起伏；④ 混合增强：实测背景 + 合成目标的 SNR 归约 vs M2 预测。容差 OPEN，参考 04 §16.3。

**工程约定**

- **溯源字段**：六件套之外新增 `model_layer ∈ {M1, M2, M3}` 与 `credibility ∈ {V1..V5}`（01 §3.1）。同一概念模型编号可同时有 M1/M2 函数式实现（自 emcore `compute*` 移植）与 M3 分块流组件，共用 `parameter_set_id`，不共用代码路径。
- **框图端口**：在 03 §11.6 端口之外新增慢变参数端口 `SceneParamFrame`（链路几何、LOS/NLOS、损耗分量、噪声底、天线复增益、扫描窗口；携带 `valid_from / valid_to / update_rate_Hz` 与溯源）；多径路径用 05 P0 的 `ChannelPathSet`：**接口从第一天就留好（类型位与命名），实现留 P3**——本期它零生产者、零消费者，`PortData` 里也没有放它的容器，08 报告 §15 写的「仅占类型位」才是实情（D-058 ⑫ 改正 D-013 的「首期即启用」）。连线校验：`IQStream` 与 `SceneParamFrame / ChannelPathSet` 不得直接相连，必须经"施加"类 M3 组件；M2 裁决组件只接受观测量端口，不接受 `IQStream`。
- **职责边界**：原始 IQ 处理只由"IQ 观测量提取"组件承担；概念模型库保持"不生成 IQ"的边界（EM-B-09 假设 A-001）；对既有概念模型的 IQ 相关字段扩展按 02 §8 七条写入 `docs/concept-model-extensions.md`（待建），不修改只读快照。
- **新增 M3 概念模型**：02 §6.2 十类，编号前缀 OPEN（候选 `EM-Q-xx`）。首期只写与阶段 1 闭环相关的：波形模板与业务活动、复基带波形生成、时变复信道、样本级噪声与干扰、接收机采样链、IQ 观测量提取、IQ 数据产品与回放、IQ 验证与校准；多站同步与阵列采样后置。
- **运行模式**：默认"算法验证模式 M2+M3"；"工程分析模式 M1+M2，争议窗口调 M3"为第二模式（01 §10.2）；M3 触发单元 = 站点集合 + 空间区域 + 时间窗口 + 频率窗口 + 信号集合 + 验证指标（01 §10.3），显式声明并进入试验记录。

## 目录结构（2026-09-03 已按此建立，见 WORKLOG 同日「工程骨架」条目）

```
804 C-UAV/
├── CLAUDE.md  WORKLOG.md  01–05.*.md      方案文档留根目录（只读）；新设计报告按 06+ 编号同放根目录
├── docs/            代码引用的规范：iq-format / api-versions / scene-package / display-route / decisions/
├── web/             WP2 前端 TS+React：src/{diagram,signal,scene,api}
├── server/          WP2 应用服务 Node/TS：REST+WS、权限/项目/任务/审计、文件 Range 服务
├── engine/          WP3 C++ 分块流 IQ 引擎 + 组件 SDK
├── models/          WP4 模型组件：radiator/channel/antenna/receiver/adc-ddc/channelizer（含 Coder 产物）
├── geo/             显示子线物理侧：IMapQuery + LocalSceneAdapter + 刀口衍射（自 emcore 移植，带 golden）
├── third_party/foundation/   AFSIM 抽取库 vendored（补 ModelApi.hpp + 顶层 CMakeLists，保留文件头）
├── algos/           WP6 算法与评价基线、用户插件接口
├── tools/           内部数据工具：iq_convert（DS-2/DA-2）、iq_survey（DS-5/DA-5，04 §10.6 八项质检）、iq_format 共享库；不进交付包
├── matlab/          内部工具链（参考模型、Coder 工程），不进交付包
├── scene/           场景数据包生产脚本：pmtiles extract / buildings 解码合并 / dem；AOI 定义
├── data/{iq/{measured,synthetic,mixed}, basemap/, scene/<aoi>/{…, scenarios/}, runs/<task_id>/, golden/}   大文件不入 git，只入索引与元数据；basemap/ 为共享底图与 DEM（D-022）；runs/ 为任务产品目录（docs/display-products.md），不入 git
├── tests/{unit,integration,e2e,golden,regression}/               04 §15.1 五级验证
├── deploy/          WP8 单机/内网部署包、离线静态资源、启动器
└── C-UAV Model Demo/   只读参考快照，不在其中开发
```

新建目录与文件名一律 ASCII 无空格。根目录与快照目录名含空格，脚本中路径必须加引号。

## 环境与命令

- 开发机（2026-09-02 核实）：macOS 26.5 arm64；node 25.8 / npm 11.12；cmake 4.2 / Apple clang 21；GDAL 3.12；`pmtiles` CLI；python 3.13 + uv 0.12；cargo 1.96；java 11。不需要 tilemaker / planetiler / osmium / qgis。
- 目标机：客户端与单机 Windows x64；集中部署服务端 Windows 或 Linux x64（D-015 / D-016）。MSVC / GCC 构建、Node LTS 版本与验证机待阶段 0 冻结；emcore / foundation 的 CMake 无平台私有 flag 且已链 `ws2_32`，但**只在 macOS 实测过**，MSVC 与 GCC 构建需在 D3 首次验证。
- 前端与服务（已可用）：`cd server && npm run build && node dist/index.js` 起服务（8080），`cd web && npm run build` 出产物，浏览器开 `http://127.0.0.1:8080/`；`npm test` 在 server（117 项，含真引擎集成，二进制缺失则跳过）与 web（150 项，`node:test` + `tsx`，含前端归约对 `tests/golden/product-window.json` 的 12 用例对拍、浏览器航迹预览对 `tests/golden/scenario-track-demo-01.json` 的逐点对拍）各自可跑；端到端 `node tests/e2e/scene-smoke.mjs`（14 项）、`node tests/e2e/slice1-smoke.mjs`（39 项，U-1 + U-3）、`node tests/e2e/slice3-smoke.mjs`（30 项，U-2 自由画布）、`node tests/e2e/slice4-smoke.mjs`（50 项，切片 ④a：典型链路视图、九环节、观测点全名与版式、S1 对链路预算、S2 底噪对解析、框图落盘、画布 ↔ 典型链路往返、结果页观测点页签）与 `node tests/e2e/slice2-smoke.mjs`（27 项，切片 ②：场景对象树、四工具、布站与撤销重做、提交场景绑定框图、三时刻位置对黄金航迹、两时刻带内功率差对几何路损差、保存场景且不留副作用；需真引擎）。前端开发者模式 `?dev=1`（显示溯源 / 标定来源 / 分色，挂 `window.__cuav.{ws.dropForTest(), signal.zoomTo() / reset() / csv(), perf.reset()}`，探针 `app.perf.longTasks`，`performance.measure('signal.render' | 'stream.flush')`）；`web/src/api/ws.ts` 客户端约定见 `docs/api-versions.md` §4.0。
- 服务任务端点（已可用，B-5，`docs/api-versions.md` §3.1a）：`GET /api/v1/components`（缓存 `--catalog`）、`POST /api/v1/tasks`（框图 JSON，`Idempotency-Key` 可选，先同步 `--validate` 不过即 400）、`GET /api/v1/tasks[/{id}]`、`POST /api/v1/tasks/{id}/cancel`；任务目录 `data/runs/<task_id>/{diagram.json, diagram.resolved.json, task.json, events.jsonl, <op_id>/}`；环境变量 `CUAV_RUN`（引擎二进制，缺省 `engine/build/cuav_run`）、`CUAV_MAX_CONCURRENT_TASKS`（缺省 1）。引擎子进程 cwd = 仓库根、参数路径全部仓库相对（D-042）。假引擎夹具 `server/src/tasks/fake_engine.mjs` 让状态机测试不依赖 C++ 构建。
- WebSocket 与补取（已可用，B-6，`docs/api-versions.md` §4、§4.0）：`ws://<host>:8080/ws` 发 `{subscribe: task_id, since}` 订阅，`subscribed` 应答后回放再实时；`product_row` 在 WS 上是二进制帧 `[u32 LE header_len][JSON 帧头补齐 4 字节][Float32]`，在 `GET /api/v1/tasks/{id}/events?since&limit` 上是文本；连接级报文（`subscribed / heartbeat / dropped / error`）`seq = 0`；只丢行帧且以连续的 `dropped{from, to, count}` 告知；关闭码 4400 / 4404 / 4409 / 4013 / 1001。假引擎每行先写 `.f32` 再发事件，`many` / `norows` 两模式供 WS 测试。
- 视窗抽取端点（已可用，B-7，`docs/display-products.md` §3、`docs/api-versions.md` §3.1b）：`GET /api/v1/results/{task}/{op}/spectrum?t0&t1&f0&f1&px&py&stat`、`.../envelope?t0&t1&px`（Float32 小端行主序，维度与实际范围在 `X-CUAV-*` 头）、`.../{kind}/index`（索引原文 + `rows_available` + `index_final`）、`.../{track|links|detections}?t0&t1&stride`（JSON 数组；`track` 与 `links` 的生产者 2026-09-06 上线，`detections` 待 P1-4d）；`scatter` 暂 404。读端以文件长度定行数，产品或索引未就绪回 409 `not_ready` + `Retry-After`，超 16 MB 回 413 带必定能过的 `suggest`。参考实现与黄金基准：`uv run --quiet python algos/reference/product_window.py <产品目录> --kind spectrum --px 800 --py 400 --stat mean --out <文件>`、`... --golden tests/golden/product-window.json`。
- 信号视图（已可用，U-3，09 §7.1–§7.2；D-048）：结果页信号页签 = 频谱（上）+ 瀑布（下，最新行在顶）+ 包络竖条 + 色带，纵轴 `dBm`；跟随模式画环里最近 H 行（WS 二进制帧，离屏画布位移追加），任何缩放 / 平移 / 游标转回看（B-7 抽取，150 ms 去抖，409 / 413 自动重试）；任务结束且索引收尾后自动把当前画面转回看重取（突发下被丢的行帧从文件补齐）；前端归约 `web/src/signal/{viewport,reduce}.ts` 与服务端同式并守同一黄金基准；快捷键 `M` / `Delete` / `←→`（Shift 十帧）；导出 PNG / CSV；探针 `app.signal.{mode, follow, lastFetch, bounds, drawnRows, liveFrames, markers, geom}`。IQ 散点无数据源（观测点不产 `iq`，D-040 ③），未做。
- 场景脚本（已可用，复跑顺序见 `data/scene/<aoi>/manifest.json` 的 `reproduce` 字段）：`register_basemap.py` 登记共享底图与 DEM；`fetch_tiles.py` 裁切片；`fetch_osm_buildings.py` 拉原始 OSM 标签（**唯一联网步骤**，只允许建库阶段）；`decode_buildings.py --osm-tags ...` 解码建筑并按 id 合并；`quality_report.py` 对拍并出质量报告；`make_manifest.py` 出区域总清单；`probe_buildings.py` 只读探测。shapely 只在建库阶段用，运行时不依赖。
- 构建与测试命令待代码建立后回填。占位：`web`、`server`：`npm run dev | build | test`；`engine`、`geo`：`cmake -S . -B build && cmake --build build && ctest --test-dir build --output-on-failure`；`scene`：`uv run python scene/fetch_tiles.py --aoi <id> --estimate`（已可用）、`uv run python scene/register_basemap.py --planet <planet.pmtiles> --dem <dem> --sha256 <hex> --link`。
- 引擎（已可用）：`cmake -S engine -B engine/build -DCMAKE_BUILD_TYPE=Release && cmake --build engine/build -j && ctest --test-dir engine/build --output-on-failure`（114 项 doctest + 8 项 ctest）。引擎 CMake 自 2026-09-06 起 `add_subdirectory` 挂上 `geo/` 并链接 `cuav_geo`；`geo/` 仍可独立构建（`scripts/build-all.sh` 两个工程各建一次），它零第三方依赖，doctest 单测寄放在 `engine/tests/{test_geo,test_scenario,test_channel}.cpp`。
- 引擎运行器 `cuav_run`（已可用，B-4；构建产物 `engine/build/cuav_run`）：`--catalog`（目录 JSON，黄金基准 `tests/golden/component-catalog.json`）、`--validate <框图.json>`（只校验不落盘）、`--run <框图.json> --out data/runs/<task_id> [--task-id <id>] [--seed N] [--resolved <旁挂.json> | --data-index <index.manifest.json>...] [--scenario <场景.json>...] [--scene-root <目录>] [--progress-interval-ms N]`；stdout 每行一条 JSON 事件并镜像到 `<out>/events.jsonl`，退出码 0–4，约定见 `docs/api-versions.md` §4.1；产品目录格式见 `docs/display-products.md`。
- 航迹预览 `cuav_run --scenario-track <场景.json> [--track-rate Hz] [--scene-root <目录>]`（已可用，G-1）：只跑运动学、输出 `entity` 事件流、不建产品目录；不发 progress、不按墙钟节流，stdout 逐字节可复现，既是黄金基准的生成器也是服务端 `PUT /api/v1/scenarios/{id}` 的语义校验器。再生成基准：`uv run --quiet python scripts/gen_scenario_track_golden.py --scenario data/scene/beijing-yayuncun/scenarios/demo-01.scenario.json --rate 2 --out tests/golden/scenario-track-demo-01.json`。
- 场景端点（已可用，G-4，`docs/api-versions.md` §3.1c）：`GET /api/v1/scenarios`、`GET /api/v1/scenarios/{id}`（响应头带落盘字节的 `X-CUAV-Sha256`）、`PUT /api/v1/scenarios/{id}`（服务端只做最小结构检查，语义交 `cuav_run --scenario-track` 看退出码；先落临时文件、过了才原子改名）。**场景文件的规范序列化形式是 `JSON.stringify(doc, null, 2)` 加末尾换行**，不改内容的保存是逐字节的空操作。示例场景一致性测试：`uv run --quiet --with jsonschema python -m unittest discover -s tests/unit -p 'test_scenario*'`（8 项）。
- MATLAB（内部工具，不进交付包，D-036）：开发机 `/Applications/MATLAB_R2025a.app`，工具箱 coder / dsp / comm / signal / phased / rf / fixedpoint / compiler（2026-09-04 核实）；`matlab` 不在 PATH，脚本经环境变量 `MATLAB_ROOT` 找 `bin/matlab -batch`（铁律 17，Windows 同法）；Coder 目标编译器 macOS clang / Windows MSVC。跑黄金向量：`MATLAB_ROOT=<MATLAB 安装目录> sh matlab/run_matlab.sh`（约一分钟，写 `engine/tests/golden/*.matlab.json`）。
  第三方件 vendored 在 `engine/third_party/`（doctest、nlohmann/json，自 emcore 拷入），构建零网络。
  黄金基准 `engine/tests/golden/energy_detector.json` 由 `algos/reference/gen_engine_golden.py` 生成，
  两侧从同一种子各自生成输入，因此不入二进制夹具。
- 数据工具（已可用，依赖 h5py + numpy，经 uv 拉起，不进交付包）：
  转换 `uv run --quiet --with h5py --with numpy python tools/iq_convert.py <源.mat 或目录> -o data/iq/measured/<batch>/`；
  质检 `uv run --quiet --with h5py --with numpy python tools/iq_survey.py data/iq/measured/<batch>/ --report <报告.md>`；
  单测 `uv run --quiet --with h5py --with numpy python tests/unit/test_iq_tools.py`（37 项，用合成夹具，不依赖数据集）。
  功率标定常数（D-047）：估算 `uv run --quiet --with numpy python scripts/ds8_calibration.py --out data/iq/measured/calibration.json`（约一分钟，读数据集）；刷进清单 `uv run --quiet --with numpy python tools/apply_calibration.py data/iq/measured/<batch>`（幂等，不读 .iq）；再 `tools/build_batch_index.py` 重建索引；新转换直接加 `--calibration data/iq/measured/calibration.json`。
- 两项出包前必查：`scripts/check-paths.sh`（路径可迁移，铁律 17）、`scripts/check-ascii.sh`（文件名纯 ASCII，铁律 15），均已接入 `scripts/build-all.sh`。
- 传播效应（已可用，R 线，D-058）：档位与逐项开关是 `ScenarioSource` 的十五个参数（`docs/component-catalog.md` §8），界面上显示在框图页「传播信道」卡片右栏（槽位表的 `SlotDef.proxy`）；公式、参数来源、解析锚点与适用范围见模型卡 `models/channel/README.md`；**缺省 E1 = 自由空间，与 2026-09-10 之前逐数值相同**，E3 待 D3。
- 验收入口：`tests/golden` 与 `tests/regression` 全绿 + 12 项标准算例（04 §15.2）。

## 里程碑状态

单元格 ≤ 5 行，量化结果一行数字，其余进 WORKLOG。

| # | 里程碑 | 对应 | 状态 | 退出条件 | 量化验收 | 锚点 |
|---|---|---|---|---|---|---|
| P0 | 需求冻结与数据摸底 | 04 §13.2 / WP1 | 进行中 | 04 §13.2 四条 + 显示子线定位与 AOI 已冻结 + 缺失引用文件已索取 | — | — |
| P1 | 最小端到端 IQ 闭环 | 04 §13.3 / WP3-4-6 | 进行中：P1-2 引擎核心首版、P1-3 部分；P1-5 / P1-6 撤销由 B / U 线替代，P1-8 由 M-2 兑现（D-030） | 04 §13.3 四条，其中「从浏览器提交」改为「从框图画布提交」+ 1 个跨层一致性算例（PROVISIONAL） | 能量检测与参考实现逐帧对拍：门限偏差 2.3e-15、命中帧 39 对 39 | WORKLOG 2026-09-04「P1-2 引擎核心」 |
| P2 | B/S 框图平台与组件化 | 04 §13.4 / WP2-3-8 | 未开始；画布、组件库、参数表单、WS 补取已前移到 B / U 线 | 04 §13.4 六条 + 05 P0 端口命名预留检查 + Windows 单机一体化包在干净 Windows 机上安装运行；剩余：模型包版本、SQLite 与审计、分片上传、启动器 | — | — |
| P3 | 工程等效模型与实测校准 | 04 §13.5 / WP4-7 | 未开始 | 04 §13.5 四条 + 首批跨层一致性算例全部在容差内，M1/M2 校准参数已回写并版本冻结 | — | — |
| P4 | 试用、整改与交付 | 04 §13.6 / WP8 | 未开始 | 04 §13.6 四条 + 第三方许可合规 | — | — |
| D0 | 场景数据包 | scene/ | **完成 2026-09-04**（D0-1 至 D0-7 七步全过） | 共享底图与 DEM 已登记 + AOI buildings.geojson（解码合并，含 src）+ 区域总清单，全离线可加载 | 建筑 47582 栋 15909168 B、sha256 269a8674…；来源 `osm:height` 1.74% / `osm:levels` 19.59% / `est:area` 78.55%；与原始 OSM 对齐 99.72%、面积中位差 +0.008%；与瓦片层交并比 0.993–0.999；切片 749 瓦片；共享资产约 145 GB | WORKLOG 2026-09-04「D0-4/D0-6/D0-7」；2026-09-03「D0-1 / D0-2」 |
| D1 | 2.5D 离线底图在 web/ 可显 | web/scene | **完成 2026-09-04** | Range 服务、字形随包（底图无 sprite）、ASCII 验收通过；同一 AOI 与 Airports 并排截图，图层与风格一致 | 样式 60 层与 Airports 逐层等价（黄金基准）；并排比对容差 8 内 99.83%、平均绝对差 0.15/255；端到端 14 项、server 30 项、web 6 项测试全过 | WORKLOG 2026-09-04「D1」 |
| D2 | 态势图层 | web/scene | **随 G-5 完成 2026-09-06** | 由任务状态流驱动的无人机图标与航迹；覆盖场 / 测向 / 椭圆图层首期无生产者，延后；**D2-4 色值标定已销项**（`docs/display-route.md` 第 4 节，八项色值在浅色底图上最低对比度 3.56:1） | 七个态势图层；高频量走独立小 store、地图 20 Hz 定频取值 | 06 §9C G-5 |
| D3 | 遮挡库移植与验证 | geo/ | 未开始（切片 ⑤） | golden 148 例 rel ≤ 1e-9 + 解析锚点（FSPL 锚点已前移到 G-1）；接入 04 §7.3 LOS/NLOS；D3-5 移交 G-2，D3 只替换 G-1 的传播模型 | — | — |
| D4 | 渲染—物理同源验收 | web + geo | 未开始 | 同一 GeoJSON 驱动渲染与遮挡；探针无副作用 | — | — |
| B | 引擎运行器与服务桥接 | engine/ + server/ | **完成 2026-09-06**（B-1 至 B-7 七步全过）：注册表与目录；框图 JSON 装载器与数据解析器；观察者、平台层、观测点产品；`cuav_run` 可执行与事件流；服务任务管理器；WS 订阅与补取、`product_row` 二进制帧；视窗抽取端点与 Python 参考对拍。P1-4a 频谱分析完成；引擎 58 项单测 + 3 项冒烟、服务 102 项测试全绿 | B-1 注册表与参数描述 → B-2 JSON 装载 → B-3 观察者与产品 → B-4 `cuav_run` → B-5 任务管理 → B-6 WS 补取 → B-7 视窗抽取；关浏览器任务不停；断线按 `since` 补取无缺号；抽取结果与 Python 参考逐值一致 | 示例框图装载运行：`total_samples` 补齐 2000000、谱 1953 行、包络 489 行；`cuav_run --run` 同一框图 2442 行产品事件、`events.jsonl` 与 stdout 逐字节相同、同种子产品逐字节复现；目录黄金基准 8 组件；经 `POST /api/v1/tasks` 提交同一框图 1953 + 489 行、`--validate` 约 9 ms、墙钟 0.14 s；真实片段 `dronerfb_0_CH0_S4` 回放 3906 + 977 行、事件与 `task.json` 无服务器路径；WS 回放切片 ① 2447 条事件（2442 帧二进制）82 ms，断线以 `since` 重连首条 = since + 1、并集无缺号；视窗抽取与 Python 参考在真引擎产品上 2046363 个值逐位相同（含线性域 `mean`），1953 × 1024 全窗 26 ms / `mean` 71 ms、5632 行取 4802 × 873 为 70 ms | 06 §9A；WORKLOG 2026-09-05「B-2」「B-4」「B-5」「B-6」、2026-09-06「B-7」 |
| U | 人机交互界面 | web/ | U-0 全篇已写（前半 2026-09-04，后半 2026-09-07，v1.8）；**U-1 三视图壳完成 2026-09-06**；**U-3 信号视图完成 2026-09-06**（频谱 + 瀑布 + 包络条，跟随 / 回看，marker、游标、导出；web 87 项单测、`slice1-smoke` 39 项；D-048）；**U-2 React Flow 画布完成 2026-09-07 → 2026-09-10 整体删除（D-060）**：用户判定「不重要、难控制、华而不实」，框图页只留典型链路一种形态；`@xyflow/react` 连同 13 个传递依赖不再随包（产物 −202 KiB，−12.3%，实测），`slice3-smoke` 30 项随之删除。目录驱动、连线判据只查 `port_compat`、参数面板由 `ParamSpec` 生成这几条经验留在 09 §6 与 git 历史里；U-4 缩为任务列表与数据中心，未开始；**2026-09-07 D-051：框图页主形态改为典型链路视图（C 线 C-7 / U-5）**；~~2026-09-08 D-052 两种形态双向可达~~ **随 D-060 作废：只剩一种形态，谈不上往返**；IQ 散点待引擎 `iq` 产品（D-040 ③） | U-0 报告 09 → U-1 三视图壳 → U-2 React Flow 画布（目录驱动、连线校验与 `--validate` 同源、观测点）→ U-3 信号视图（频谱、瀑布、包络、IQ 散点，按视窗抽取）→ U-4 结果页与数据中心 | — | 06 §9B |
| C | 典型链路与检测识别评价 | web/ + engine/ + server/ + algos/ + models/ | **C-0 设计、C-1 格式与接口、C-2 天线 / 前端 / ADC 三件均完成 2026-09-07**：端口类型 7 种、`PortSpec.optional`、框图 `template_ref`、`SceneParamFrame` 七字段、`BlockMeta.clip_count`、场景 `emission.polarization`；`AntennaGain` / `ReceiverFrontEnd` / `AdcQuantizer` 落地，两个既有组件加参且缺省保旧行为，**组件目录 12 → 15，04 §8.1 的六类首次全部有组件**（D-050 销项）。引擎 142 项单测 + 8 项 ctest、服务 111 项、web 134 项、四套 e2e 全绿；**C-6 框图落盘与结果端点、C-7 典型链路视图完成**：框图页默认是九环节固定链，自由画布降为 `#/diagram/canvas`；框图落盘 `data/diagrams/`；**切片 ④a 收口，S1 对链路预算差 0.45 dB、S2 底噪对解析差 0.02 dB**。引擎 142 项单测 + 8 项 ctest、服务 117 项、web 150 项、五套 e2e（14 + 39 + 27 + 30 + 34）全绿。**2026-09-08 按用户实测补掉两处可见性缺口**：① 导航——画布工具条加「回到典型链路」、解不开时先问不静默丢改动、`Alt+2` 与顶栏「框图」回默认形态（D-052）；② 观测点——一行改写全名（`S0 辐射源输出`…`S5 子信道`，链条结构不动）、勾选与当前任务不一致时提示要重新运行、结果页中栏加观测点页签且默认选主产品 S4（S2 / S3 / S4 读数相同是 DDC 与信道化未实现的真实后果，不是缺陷）；③ 中栏版式——链条区占满高度的面板 + 纵向居中，九个环节按 **3 × 3** 排（行距 56 px、卡片 176 × 128、连接件拉伸铺满列宽），连接箭头挪到卡片之后消除换行时的孤立箭头。五套 e2e 14 + 39 + 27 + 30 + 50 = 160 项；C-3 起未开始 | C-0 方案 → C-1 格式与接口 → C-2 天线 / 前端 / ADC 三件 → C-3 检测升级与落盘 → C-4 特征与识别 → C-5 真值与评价 → C-6 框图落盘与结果端点 → C-7 典型链路视图 → C-8 `demo-02` 与 G-6 → C-9 结果页两页签 → C-10 DSP 厚化接入 → C-11 收口；切片 ④ 重排为 ④a–④d | **已达成（C-2）**：拆开天线后与折在一处的接收电平差 **< 0.001 dB**（线 0.05）；S2 底噪与 −174 + nf + 10·log10 fs 差 **< 0.05 dB**（线 0.3）；ADC 量化信噪比与 6.02·bits + 1.76 dB 差 < 0.5 dB。**待 C-5**：`metrics.json` 与 Python 参考逐值一致、跨层算例 ① 典型链路实例差 ≤ 0.05、识别准确率 ≥ 0.9（原型阶段验证值） | 06 §9G；10 报告 §8 |
| G | 场景与目标活动生成 | docs + geo/ + engine/ + web/ | **切片 ② 完成 2026-09-06**（G-0 至 G-5 六步全过）；G-6 活动时间线随切片 ④；PROVISIONAL | G-0 示例场景 → G-1 `geo/` 运动学与链路预算 + `--scenario-track` → G-2 `ScenarioSource` / `SceneEmitterSource` + 装载器场景注入 → G-3 `SceneBoundChannel` / `FreeSpaceChannel` → G-5 态势图层 → G-4 编辑器；G-6 活动时间线（跳频与图传开关驱动波形）留切片 ④ | 三时刻位置与黄金航迹差 **0**（容差 1e-6 度）；两时刻带内功率差与几何路损差偏差 **0.074 dB**（容差 0.5）；绝对链路预算三点偏差 ≤ 0.021 dB；FSPL 锚点 100.052008 dB；预览与 C++ 航迹 361 点逐点相同；`slice2-smoke` 27 项 | 06 §9C；08 报告 §9；WORKLOG 2026-09-06「切片 ②」 |
| L | 多源多站测向定位 | docs + geo/ + engine/ + server/ + web/ | **L-0 至 L-8 完成 2026-09-09**（切片 ⑥ 三片全收口）；L-9 定位评价随 C-5 / C-9；PROVISIONAL | L-0 方案 → L-1 格式与接口 → L-2 `Superposition` 与多源模板 → L-3 `DirectionFinder` 与多站模板 → L-4 `MultiSiteLocator(aoa)` 与移植 → L-5 `ToaEstimator` 与 TDOA → L-6 服务端与回看 → L-7 e2e → L-8 文档收口 → L-9（随 C-5 / C-9）定位评价 | **已达成**：移植 golden 五组 255 例 rel ≤ 1e-9；解析锚点 AOA **17.4532925 / 34.906585 / 20.5495 m**、TDOA GDOP **0.942809** 逐位命中；DF 蒙特卡洛经验 σ 差 **0.80%**（线 3%）、1800 行归一化误差方差 **1.0225**；AOA 有效档 2σ 覆盖 **87.0%**（理论 86.5%）、50% 半径 / CEP **0.920**；时差中位误差 **7.95 m**、几何良好且扣掉钟差偏差后 2σ 覆盖 **85.3%**；同频双源合成功率对预算和差 **−0.001 dB**、逐站三源叠加 ≤ **0.013 dB**；`slice6-smoke` **52 项** | 06 §9H；11 报告 §8 |
| R | 传播环境与效应选择 | docs + geo/ + engine/ + web/ | **R-0 至 R-7 完成 2026-09-10**（切片 ⑦ 收口）；E3 建筑遮挡与绕射随 D3（切片 ⑤）；E4 实测融合随 P3；PROVISIONAL | R-0 方案 → R-1 组合框架与档位 → R-2 大气降雨移植 → R-3 地面双径 → R-4 统计阴影 → R-5 城市经验 → R-6 界面 → R-7 收口 | **已达成**：E1 缺省档下 `slice1` / `slice2` 的产品文件与上一版提交**逐字节相同**（拿上一版建出的 `cuav_run` 直接对拍），slice2 的 **0.074 dB**、slice6 的 **0.002 / −0.013 / 0.001 dB** 一个数不动；天气两式对 emcore golden **64 例 rel ≤ 1e-9**；城市经验 `open` 档与 `fspl_dB` 逐值相等（≤ 1e-9 dB）；双径掠射极限 `C_2ray ∈ [−20（限幅）, +6.01] dB`、远场 20 → 200 km 路损增 **39.99 dB**（`d⁻⁴`）；阴影 40000 帧经验 σ **6.0015**（设定 6）、相邻帧相关 **0.6687**（理论 0.6703）；`slice4-smoke` 74 → **86 项** | 06 §9I；12 报告 §9 |
| V | 态势与信号可视化提升 | docs + web/ + engine/（场景键表）+ server/（`task.json` 一字段） | **V-0 方案完成 2026-09-12**（13 报告 v1.0）；切片 ⑧（V-1 至 V-3 + 两处缺口）待排期；⑨–⑪ 待排期；PROVISIONAL | V-0 方案 → V-1 卡片栈（先修采用任务不载场景、链路线未画出）→ V-2 叠加加重（含 `zones[]`，基准变更单独收口）→ V-3 全宽时间轴 → V-4 站点方位图 → V-5 精度场与覆盖场 → V-6 网格瀑布 → V-7 余辉 → V-8 标签条（随 C-9）→ V-9 全带总览（随 ④c）→ V-10 全景一屏 → V-11 视觉密度 | 待 ⑧：卡片读数与 `links` / `bearings` 端点行逐值相等；`slice8-smoke` 约 30 项；新色值三种底色对比度 ≥ 3:1；`demo-03` 航迹基准 723 样点逐点相同、只哈希变 | 06 §9J；13 报告 §11 |
| T | 辐射源模板（实测数据集驱动的信号再生） | docs + tools/ + engine/ | T-1 规范草案与 T-2 记录完成 2026-09-05；T-0 探针待排期；T-3 起随 P3，本期只记录不实现（D-045） | T-0 探针 → T-1 规范 → T-3 提取器 → T-4 `TemplateSource` → T-5 闭环与模型卡；阈值由 T-0 回填后冻结 | — | 06 §9F；`docs/emitter-template.md` |
| M | MATLAB 内部模型生产与验证 | matlab/ + models/ | M-0、M-1 完成；M-4 部分（Welch 功率谱三方互证过）；M-2 起未开始 | M-0 工具链 → M-1 `matlab/` 骨架 → M-4 黄金向量三方互证 → M-2 Coder 生成 DDC（兑现 P1-8）→ M-3 信道化与接收滤波 → M-5 P3 波形与非理想参考；Coder 组件与 MATLAB 黄金向量 rel ≤ 1e-9 | 开发机 R2025a，工具箱 coder / dsp / comm / signal / phased / rf / fixedpoint / compiler | 06 §9D；D-036 |

D0–D4 挂靠 P0 冻结后启动，不进入 P1 退出条件。B / U / G / M 按纵向切片交错推进（06 §9E：① 一条链到频谱 → ② 场景里飞起来 → ③ 拖出框图 → ④a 链路跑通 → ⑥ 多源多站测向定位 → ⑦ 选传播环境 → ⑧ 一屏讲清楚 → ④b 检测识别评价 → ④c DSP 厚化 → ④d 收口 → ⑤ 遮挡接入）；G 线与显示子线同属 04 之外的 PROVISIONAL 扩展。

## 决策日志

| # | 日期 | 决策 | 依据 / 影响 |
|---|---|---|---|
| D-001 | 2026-09-02 | 地理场景视图 = 信号级仿真的场景设置与环境背景（传播/绕射/反射计算依据）；框图 = 计算链路、参数与 I/O 监控；三视图切换 | 用户确认；04 无此需求，挂 04 §3.1-2 与 §7.3，须进阶段 0 冻结清单 |
| D-002 | 2026-09-02 | 底图沿用 Airports；建筑走独立 GeoJSON 同源驱动渲染与遮挡 | 瓦片建筑层经切割简化且 `height ?? 8`，不适合计算 |
| D-003 | 2026-09-02 | B/S，不用 Tauri；应用服务 Node/TS | 04 §2.5；与前端同语言共享 schema |
| D-004 | 2026-09-02 | foundation 已获授权，可 vendored 使用 | 用户确认；需补 `ModelApi.hpp` 与顶层 CMakeLists |
| D-005 | 2026-09-02 | 建筑遮挡复用 emcore 实现，移植后须 golden + 解析锚点验证 | 用户确认 |
| D-006 | 2026-09-02 | WORKLOG 纪律：每任务节点立即追加，状态快照置顶；里程碑表单元格 ≤ 5 行 | 方法论阶段 5 |
| D-007 | 2026-09-02 | 单仓多目录布局如上；方案文档与设计报告留根目录按序号编号 | 用户既有项目惯例 |
| D-008 | 2026-09-02 | MapLibre GL pin 5.24 + pmtiles 4.4（取 em-demo 版本，非 Airports 5.6.1） | 避免版本分叉 |
| D-009 | 2026-09-02 | 常数策略：移植模块守原常数，新代码统一 `c = 299792458` 与严格 ENU | emcore README W5 警告 |
| D-010 | 2026-09-02 | 建筑 GeoJSON 从本地 planet.pmtiles z15 瓦片解码合并生成，与底图同一 OSM 快照；缺高度地区后续逐步补数据 | 用户确认；建库不联网 |
| D-011 | 2026-09-02 | 概念模型库定位为 M1/M2 参数供给层与归约目标层；本系统建设 M2+M3；EM-R/J/H/N/C/F 六族不在本系统范围 | 用户提出衔接问题；01 §9.2、02 §6.1 |
| D-012 | 2026-09-02 | 溯源字段扩展 `model_layer(M)` 与 `credibility(V)` | 01 §3.1 要求同时声明 M/E/V |
| D-013 | 2026-09-02 | 新增 `SceneParamFrame` 慢变参数端口；`ChannelPathSet` 首期即启用；IQ 流与参数流不得直连（**「首期即启用」自 2026-09-10 由 D-058 ⑫ 改正为「首期只做类型位与命名预留，实现留 P3」**，原意是接口先留好、P3 接多径时不改端口表） | 03 §11.6、§11.7；05 §5.2 |
| D-014 | 2026-09-02 | P1 退出条件含 1 个跨层一致性算例（PROVISIONAL，待 04 修订确认） | 01 §8.3；04 §13.3 原文无此条 |
| D-015 | 2026-09-02 | 首期交付平台 Windows x64 优先；Linux / macOS 可构建不阻塞；国产化平台（麒麟 / 统信、ARM64 / 龙芯）不在首期范围，MATLAB Runtime 路线因此不受 ARM 限制影响 | 用户确认"先保证 Windows 上可用" |
| D-016 | 2026-09-02 | 细化 D-015：内网集中模式的服务端可视情选 Linux x64，`server/`、`engine/`、`geo/` 双平台构建；客户端与单机一体化包仍 Windows 优先。D-015 中"国产化不在首期"相应放宽：服务端 Linux 发行版含国产化服务器版作为候选，阶段 0 定；ARM64 / 龙芯仍不在首期 | 用户确认"后台服务适情可以是 Linux" |
| D-017 | 2026-09-02 | 地图渲染的图层顺序与显示风格照搬 Airports（浅色手写 Protomaps 样式、hillshade、建筑拉伸外观、相机与控件、字形策略）；em-demo 只贡献态势图层的图形语义与离线加载机制，其深色主题与 `protomaps-themes-base` 不采用；态势色值在浅色底图上重标定 | 用户确认"沿用或照搬 Airports" |
| D-018 | 2026-09-03 | 两个公开数据集**互补不替代**，都完整保留。DroneRFb-DIR 承担：跨层算例 ② 的唯一量级依据（视距/非视距标签）、P3 的 D 组独立验证集（出版方已划分）、DS-6 虚警率标定主样本（62 片背景）、`iq_survey` 正交项 `valid` 正例与 DA-7 对照组、个体识别素材。DroneRFa 承担：距离与路损粗档标定、运动与多普勒、正交不平衡唯一校准素材、长突发时序、双通道解析分支。转换与质检按角色排序，不做删减 | 用户问"DroneRFb 是否可以不用了"；06 §11.4、§11.5 角色段 |
| D-019 | 2026-09-03 | 实测路损指数只冻结**粗档结论「2.4 GHz 城市户外链路 n ≈ 2」**：`T0010` 六片 n = 1.93（逐对极差 0.12、标准差 0.05），`T0011` 三片 1.69，两机型宽基线一致。**1.93 的前提是"距离取区间中点"这一计算假设，不代表数据精度**——真实距离比只能定在 2.0–7.5，同一实测降幅对应 n = 1.29–3.75。P3 模型卡必须三件事一并写；相邻距离档比较一律不得用于标定；带真实置信区间的路损曲线仍只能靠带 GPS 距离的甲方自采数据 | DA-6 实测（`scripts/da6_pathloss.py`）；WORKLOG 2026-09-03 第四条 |
| D-020 | 2026-09-03 | IQ 数据格式定稿（`docs/iq-format.md` 第 3、4 节）：容器用**裸样点文件加旁挂 JSON 清单**（Range 取区间时字节偏移是常数乘法，且与 SigMF 两文件结构同构）；字节序**固定小端且不做自动探测**；单段上限 67108864 复样点；元数据十四个顶层键，另加 `field_sources` 逐字段标注 `measured/paper/derived/assumed/absent`；`not_applicable` 与 `valid` 严格分开，整体状态取八项最差。**正交不平衡判据取安静帧的 I/Q 标准差比，不取整片**——实测 DroneRFa 的不平衡在噪声路径上，整片值随信噪比趋近 1，按整片判会漏掉三片中的两片。直流、标准差比、互相关三项判据取「固定阈值」与「5 倍统计标准差」中较宽者，避免短窗误报 | P1-1 第一批定稿；WORKLOG 2026-09-03 第五条；实现见 `tools/` |
| D-021 | 2026-09-03 | **AOI = 北京亚运村周围**（用户否决原计划的 em-demo 西安范围）。中心 (116.405, 39.990) 由用户定；范围 `116.345,39.945,116.465,40.035`（约 10 × 10 km）由实施方暂定、标 PROVISIONAL；定义文件 `scene/aoi/beijing-yayuncun.json`，含从底图瓦片解出的地名核实证据。后果：em-demo 的 13,594 栋西安 Overpass 建筑集不再能作 D0-4 对拍基准，需在建库阶段对北京 bbox 重拉 | 用户指示"不放在西安，放在北京亚运村周围"；范围大小待用户确认 |
| D-022 | 2026-09-03 | **底图与 DEM 直接用整份全球文件，登记为共享资产 `data/basemap/`，不按 AOI 裁切**：`planet.pmtiles` 137370745450 B（sha256 `b4c46742…`，planetiler 0.10.2，OSM 2026-08-17）、terrarium DEM z0–8 共 87381 文件。开发机上软链到 Airports，目标机放真实文件；必须经 Range 服务提供。`scene/fetch_tiles.py` 裁出的 AOI 切片 `basemap-slice.pmtiles` 降为测试夹具与便携底图（AOI 外空白）。缩小到 AOI 之外仍有完整世界图，"AOI 外空白"这一继承的坑随之消失 | 用户指示"我可以用大的呀，128 GB 没有问题"；项目记忆：存储不是约束，不做省空间取舍 |
| D-023 | 2026-09-04 | **底图与 DEM 整份拷入本项目自持**，`data/basemap/{planet.pmtiles, dem/}` 由软链改为真实文件，合计约 145 GB（底图 137370745450 B、DEM 87381 文件 7442617745 B）；来源 `/Users/zhiyu/CC/Airports/tiles/` 保持只读留档。校验：底图全文件 sha256 与来源一致，DEM 索引哈希 `13b213a4…` 与来源一致，逐层完整。`scene/fetch_tiles.py` 默认源改为包内路径，清单新增 `storage`（`in_place`/`symlink`/`external`）与 `origin` 字段。**此后代码、脚本、配置一律不得引用 `/Users/zhiyu/CC/Airports/` 路径** | 用户指示"也拷贝到本项目目录中，避免后续出现资源路径的问题"；消除对外部工程路径的运行时依赖 |
| D-024 | 2026-09-04 | **AOI 范围定为 20 × 20 km**：`116.288,39.900,116.522,40.080`，实测 19.96 × 19.97 km，中心不变。理由是单站置于中心时轴向到边界 10 km、角向 14 km，覆盖包络由传播条件截断而非由框子截断；10 × 10 km 的 5 km 轴向半径小于 2.4 GHz 图传的可探测距离，会用 AOI 边界冒充物理边界。实测代价次线性：面积 4.0 倍，z15 瓦片 132 → 529，建筑（按 id 去重）13564 → 50201 即 3.7 倍；底图体积不受影响（D-022）；切片 206 → 749 块、4342046 → 11147792 字节。同时定：`buildings.geojson` 估约 25 MB、遮挡桶网格 200×200、覆盖场同分辨率下格点数 4 倍，均在小量级 | 用户 2026-09-04 拍板；实测见 `scene/probe_buildings.py`、WORKLOG 同日条目 |
| D-025 | 2026-09-04 | **z15 建筑要素带稳定 id**（5.9 万要素无一缺 id，去重后 5.0 万），D0-3 的跨瓦片合并直接按 id 做，不再需要共享边或缓冲相交的启发式判定。**高度来源实测**：北京 20 × 20 km 内 76.9% 无任何高度、19.4% 是 planetiler 按 `building:levels x 3 + 2` 推导、真实标注上界仅 3.7%。**瓦片 `height` 字段把两种来源混在一起**，「不小于 5 的整数且模 3 余 2」只是启发式判据，会把真高 20 m 的楼误判为推导值；干净的 `src` 只能靠建库阶段拉 Overpass 原始标签，与 D0-4 重建对拍基准是同一次动作 | 实测 `scene/probe_buildings.py`；06 §4「首个技术未知点」由此销项 |
| D-026 | 2026-09-04 | **跨层一致性算例 ① 的公式侧由本项目显式选定**：EM-S-02 只要求输出检测概率并允许「解析近似或查表」，未钉死闭式。选定随机型目标 `Pd = Q(M, M·η/(1+s))`、确定型目标 `Pd = Σ_k Poisson(k; M·s)·Q(M+k, M·η)`（非中心卡方的泊松混合，不引入第二套特殊函数），两式与 `algos/reference/energy_detector.py` 的检测量定义严格配套、s→0 时精确退化为虚警率。**算例 ① 在参考实现侧首次通过**：检出率最大偏差 0.0016–0.0050，04 §16.3 建议容差 0.05–0.10；引擎侧待 P1-2/P1-4 后重跑对拍。**能量检测的交付形态不得是静态门限**：实测门限在一半背景上标定、换到另一半失效 7 倍，随机打散分半则达标，说明背景随时间与地点漂移，P1-4 须做滑动噪声估计或恒虚警率 | WORKLOG 2026-09-04「DS-7」条目；D-014 |
| D-027 | 2026-09-04 | **IQ 产物只入合并索引，不入逐产物清单**。两个公开数据集共 4714 个产物，逐个入库等于给仓库塞近五千个小文件、约 19 MB。改为每批入一份 `index.manifest.json`（每产物一行：标识、来源、真值摘要、样点数、`content_sha256`、质量状态）与一份 `holdout.manifest.json`。逐产物清单与 `.iq` 留盘上不入库，由 `tools/iq_convert.py` 确定性重生成，索引里的哈希用于核对一致性。工具 `tools/build_batch_index.py` | CLAUDE.md「大文件不入 git，只入索引与元数据」；WORKLOG 2026-09-04「批量转换与冻结」条目 |
| D-027b | 2026-09-04 | （原编号与上一条重号，2026-09-04 改为 D-027b；WORKLOG 与质量报告里的旧引用「D-027」凡指建筑高度分档者均指本条）**D0-4 拉原始 OSM 标签后，建筑高度来源的分档定案**：`osm:height` 1.74%（真实标注）、`osm:levels` 19.59%（层数折算）、`tile:height` 0.12%（对不上原始标签的漂移件）、`est:area` 78.55%（估算）。层数折算固定用 **层数 × 3 + 2 米**（+2 计入女儿墙与屋面构筑物，与 planetiler 一致），**与 em-demo 的 × 3.0 有意差 2 米**；层数标注为 0 或负视为无效，退回面积估算。D-025 的启发式判据（模 3 余 2）经原始标签验证：推导值预估 19.4% 对实际 19.59%，真实标注上界 3.7% 对实际 1.74%，判据方向正确、上界成立。**瓦片与原始标签取自不同时刻的 OSM**（快照 2026-08-17 对实时 2026-09-04），两侧各百余件对不上，计数差异首先归因于漂移 | D0-4 实测；`data/scene/beijing-yayuncun/quality-report.md` |
| D-028 | 2026-09-04 | **当前处于原型系统阶段，公开数据集只用于验证技术途径、软件架构与功能实现，不作交付指标。** 由此三条：① 已得出的公开数据集量化结论（虚警率超标倍数、环境代价 23.6–38.9 dB、路损指数 n≈2 等）一律标注「原型阶段验证值」，**甲方数据到货后按同样脚本重跑并修订**，任何人不得把它们当作设计指标或验收门槛；② 数据侧不再深挖（DA-7 根因、补数下载、更多频段标定停止投入），已建成的四件工程件（转换器、八项质检、检测器参考实现与解析式、验收集冻结与拦截）保持可用，等真实数据；③ 精力转向 P1 主链，**先打通薄的端到端切片再补厚设计**，顺序为引擎核心 → 服务最小接口 → 前端最小页 → 与场景视图合流 | 用户 2026-09-04 明确定位：「现在主要是验证技术途径和系统的软件架构和功能实现，属于前期的原型系统阶段」 |
| D-029 | 2026-09-04 | **端到端验收自建调试协议探针，不引入浏览器自动化框架**：`tests/e2e/cdp.mjs` 一百余行，零依赖（只用 Node 内置 `fetch` 与 `WebSocket`），覆盖开页面、轮询只读探针 `window.__probe()`、取截图三件事。理由是交付环境不联网、依赖须随包（铁律 6），为三件事引入一整套框架不划算。同时固化无头截图的三个坑：`load` 事件早于瓦片加载、`--virtual-time-budget` 下瓦片不加载、合成器截图取不到 WebGL 内容 | D1-5 实测；`tests/e2e/README.md` |
| D-030 | 2026-09-04 | **路线图修订 v1.1**：用户定后续重点为人机交互界面、真实地理环境下的场景与目标活动生成、B/S 框图模型、波形与频谱可视化、后端计算。06 备忘录新增 **B 桥接 / U 界面 / G 场景 / M MATLAB** 四条步骤线与五个纵向切片（§9A–§9E）；P1-5、P1-6 撤销由 B / U 替代；P1-8 由 M-2 兑现；D2 并入 G-5；D3-5 移交 G-2。**G 线是 D-001「场景视图 = 仿真的场景设置」的兑现，04 无条款，标 PROVISIONAL，进阶段 0 冻结清单。** 四份规范骨架落地：`docs/{diagram-format,scenario-format,display-products,component-catalog}.md` 与 `docs/schemas/` 两份 schema，字段已冻结 | 用户 2026-09-04 指示；核实到的引擎缺口：无组件注册表与参数描述、`configure()` 只收两个 map、无可执行入口 |
| D-031 | 2026-09-04 | **引擎运行形态 = 子进程 `cuav_run`** + 定长二进制产品文件 + stdout JSON 事件（带 `seq`）；不做 N-API 插件、不做引擎内嵌 HTTP。释义：CLAUDE.md「同进程 / 共享内存」指组件与调度器同进程，不指嵌入 Node；D-013「服务端 geo/ 计算」的「服务端」= 非浏览器，实际在 `cuav_run` 进程内 | 用户拍板；Windows 上 Node ABI 与 MSVC 匹配风险；「关浏览器任务不停」天然成立 |
| D-032 | 2026-09-04 | **随包依赖**：前端 **React Flow**、服务端 **`ws`** 允许并锁版本、进 THIRD-PARTY-NOTICES；状态管理零依赖 `useReducer` + context；路由手写 hash；服务端「零运行时依赖」改为「只加 `ws`」。框图画布 OPEN 项销项 | 用户拍板；离线只要求随包不要求零依赖（铁律 6） |
| D-033 | 2026-09-04 | **场景运行时归属**：运动学（航点插值、悬停、航向）与链路预算（距离、LOS、FSPL、多普勒、时延）在 **`geo/` C++ 库**，引擎组件 `ScenarioSource` 薄封装并链接 `cuav_geo`；**参数帧按样点序号推进，不按墙钟**；`EntityState` 不做端口类型，走观察者回调；浏览器预览只做直线插值并与 C++ 航迹 golden 对拍（≤ 1e-6 度） | 用户拍板；铁律 9；D-013；D3 落地时只替换传播模型 |
| D-034 | 2026-09-04 | **09 号报告扩为「主界面、三视图与框图画布交互设计」**，顶替缺失的原型 html（PROVISIONAL，原件到手后对账）；04 §8.1 的「星座图」首期只做 S4 观测点 IQ 散点，真正的星座图随 P3 调制波形 | 04 §8.1；原型 html 不在盘上 |
| D-035 | 2026-09-04 | **坐标基座临时件**：G-1 先用自写 WGS-84 闭式 LLA↔ECEF↔ENU 放在 `IGeodesy` 接口后，D3-0（07 报告）再换 foundation / GeographicLib 并对拍回收 | 不让 G 线等 D3；接口隔离使替换零扩散 |
| D-036 | 2026-09-04 | **DSP 类组件（滤波、DDC、信道化）的生产路线 = MATLAB Coder 生成 C**，进 `models/<环节>/coder/` 带来源头（`.m` 路径、MATLAB 与 Coder 版本、codegen 参数哈希）；手写 C++ 只用于算术简单件（ADC 量化削顶、噪声注入、混合、观测量归约）与引擎骨架。参考模型双轨：MATLAB 工具箱为主参考，Python numpy 参考保留为独立第二实现，黄金向量三方互证。MATLAB Engine / Runtime 不进运行时，`matlab/` 不进交付包，界面不出现 MATLAB。开发机 R2025a 与工具箱已核实。**许可**：用户 2026-09-04 确认 Coder 生成的 C 代码随交付不受单位许可条款限制，M-0 待确认项销项 | 用户追问「MATLAB 的优势发挥出来了吗」，答案是此前没有：参考实现全为 Python，P1-8 未排期；03 §11.3.1、§11.4、§11.8；04 §6.2、§8.5、§13.3 |
| D-037 | 2026-09-04 | **浏览器永不见服务器路径：数据引用只用标识，路径是内部参数**。框图里对实测数据的引用一律写数据索引的 `data_id`；服务端在任务装载前按 `index.manifest.json` 解析成 `manifest_path` 注入服务端副本；组件目录用 `internal: true` 标记这类参数，画布不显示，提交的框图里出现即 400，引擎装载器同样拒绝。`FileReplaySource` 的用户参数改为 `data_id`，`manifest_path` 转内部参数，`data_id` 进溯源 `trace_id`。同一次评审定下的另两条硬约束：快捷键避开 Chromium 保留键（`Ctrl+1…9`、`Ctrl+Tab`、`Ctrl+W/T/N`、`F5/F11/F12`），任务的**运行态与结果四态是两个正交徽标** | 09 报告 v1.1 评审（§6、§7.3、§11、§13.1）；04 §8.6；此前 `manifest_path` 是用户参数，路径会进框图 JSON 与参数面板 |
| D-038 | 2026-09-05 | **09 报告十三项暂定的拍板**（报告升 v1.2）：五项改定——数据中心为独立页面顶栏直达（`#/data`）；视口基线按 2K 取 2560 × 1440、最小 1920 × 1080；瀑布最新行在顶部向下滚，频谱紧贴其上共用频率轴并列对齐；**纵轴以 dBm 为目标**，先按可得依据（公开数据集取论文的接收增益与天线增益）估一个标定常数并带来源徽标（实测 / 论文 / 假定 / 模型），没有任何常数才退到 `dBFS（未标定）`，绝不显示不带来源的 dBm；验收集片段允许回放但提示。其余八项采纳原案。同日用户确认两个公开数据集**无任何使用与署名约束**，DS-1 / DA-1 销项 | 用户 2026-09-05 拍板；D-020 未标定判据不变，只是显示层多了「带来源的估算常数」这一档 |
| D-039 | 2026-09-05 | **系统定位再确认：技术验证原型，用于技术路线验证与功能演示，目的是证明本方的行业理解与技术实力。** 由此五条取舍：① 优先级为「可见的端到端链路（切片 ①–④）」>「专业的仪表与态势表达（09 §5.4 链路读数、§7.2 频谱仪约定、§9 溯源）」>「生产级加固（权限审计、分片上传、并发、多用户）」，后者只做演示所需最小集；② 每个演示环节都要能讲出背后的模型、参数与上游依据（01–05 条款、概念模型编号、模型卡）——**这是对内要求**，写进设计文档、模型卡与证据包，溯源数据按铁律 8 随产物走；**系统界面不展示溯源**，只留隐藏的开发者模式供内部诊断，否则像实验系统而不像交付系统（用户同日修正）；③ 演示用真实地理（亚运村）、真实数据（公开数据集回放）与合成数据；合成标注、估算高度、「原型阶段验证值」按铁律 14、15 保留在数据与文档里，界面上以低调形式存在（小字、可折叠），**演示时不主动讲**；④ 性能数字与量化结论不承诺为交付指标（D-028 不变）；⑤ Windows 单机包仍要做，因为演示在目标机上跑，服务端双平台与国产化等留后 | 用户 2026-09-05 强调，同日补充「可解释性对内不对外、标注不主动讲」；D-028 是它的前身，本条把「证明行业理解与技术实力」这一目的写明并给出取舍规则。铁律 8、14、15 不变，变的只是界面上的呈现强度 |
| D-040 | 2026-09-05 | **B-2 框图装载器的四项口子**：① `data_id` 的解析结果经**解析旁挂** `diagram.resolved.json`（`cuav-resolved/1`，`data: {data_id → manifest_path}`）注入，框图文件与其服务端副本永远不含内部参数，引擎装载器对内部参数无条件拒绝，D-037「引擎装载器同样拒绝」因此没有例外；装载器自带 `MapDataResolver`（读旁挂，B-5 用）与 `IndexDataResolver`（直接读 `index.manifest.json`，`<索引目录>/<data_id>.manifest.json`，`cuav_run` 单机与回归用）；② `ObservationTap` 缺 `out_dir` 的检查从 `configure()` 挪到 `init()`，装载器不给 `out_dir` 即「只校验」模式，`cuav_run --validate` 不落盘；③ 观测点 `products` 含 `iq` 本版本明确拒绝（`product_unsupported`），不静默忽略（铁律 15）；④ 错误码首版 17 个（`docs/diagram-format.md` §4），`Graph::connect / validate` 增加失败分类 `LinkFault / GraphFault` 供装载器映射，规则仍只在 Graph 一处解释。另按规范直接做：`total_samples` 补齐取四舍五入、显式值超时长即拒，`run.block_size` 给未写块长的节点；回放节点的时长截断留 B-4 / P1-3 | 用户 2026-09-05 批准 B-2 计划；`docs/api-versions.md` B-5 行与 06 §9A B-4、B-5 行据此修正（06 §14） |
| D-041 | 2026-09-05 | **`cuav_run` 事件与退出码约定**（B-4）：① stdout 事件信封与 WS 文本帧同构 `{seq, task_id, type, t_s, payload}`，`--out` 给出时引擎自己把每行原样落 `events.jsonl`（两处逐行 flush），服务端不必再落盘；② `product_row` 事件不带数据，`ObservationTap` 改为逐行 `fflush`，服务端按 `row_index × row_len × 4` 偏移读文件转二进制帧，索引 `rows` 仍每 64 行更新、只服务回看端点；③ `progress` 按墙钟节流（默认 100 ms，`--progress-interval-ms 0` 每轮），事件流因此不承诺逐字节复现，产品文件才承诺（铁律 9 的复现对象是产品）；④ 退出码 0 成功 / 1 命令行或未实现子命令 / 2 装载失败 / 3 运行失败 / 4 目录或事件文件不可写，结果四态只在 `task.state` 里，与退出码正交（D-037 两徽标）；⑤ `--seed` 覆盖框图种子须写 `seed_source = cli` 并发 `log`；`--catalog` 不加 `generated_at`，黄金基准逐字节生成、按「已有条目不变」比对；`--scenario-track` 留 G-2；`task_id` 缺省取 `--out` 末级目录名 | B-4 实施；`docs/api-versions.md` §4.1、`docs/display-products.md` §1 §4、`docs/component-catalog.md` §2 |
| D-042 | 2026-09-05 | **B-5 服务任务管理器的约定**：① 提交时先同步 `cuav_run --validate`，不过即 400 并带引擎四字段 `{code, node_id, port, message}`，不建任务目录（用户拍板，不建 `failed` 任务污染列表）；② 引擎子进程 cwd = 仓库根，参数与旁挂里的路径全部仓库相对、纯 ASCII、字面 `/`，恒传 `--task-id`——引擎的窄字符 `main()` 永远见不到可能含中文或空格的绝对根目录，Windows 路径风险在源头消除；③ 脱敏唯一入口：引擎每行先整行替换（清单路径 → `data_id`、剥仓库根前缀）再解析，缓冲、`task.json`、将来的 WS 与补取都只见脱敏后的事件；`events.jsonl` 引擎所有、原样不动、不对外暴露；④ 串行 FIFO 队列，`CUAV_MAX_CONCURRENT_TASKS` 缺省 1（用户拍板）；⑤ 终态：引擎给了 `task.state` 终态就信它，没给则已请求取消 → `cancelled / not_applicable`、其余 → `failed / invalid`，由服务端补一条 `task.state`（`payload.source = server`）接在引擎序号之后；取消 = SIGTERM → 3 s → SIGKILL；⑥ 服务收 SIGINT / SIGTERM 同步杀引擎并把运行中任务标 `failed`「服务停止时任务被中止」，重启按 `events.jsonl` 尾部最后一条 `task.state` 对账，没有终态标 `failed`「服务重启时任务未结束」；⑦ 服务端不复刻框图 schema，只做最小结构检查 + 内部参数 400 + `data_id` 解析，其余交引擎；`task.json` 的 `rows_seen` 是下界，B-7 读端以文件长度为准 | B-5 实施；`docs/api-versions.md` §3.1a、`docs/diagram-format.md` §9、`docs/display-products.md` §1.1、`docs/schemas/resolved.schema.json`；实测（macOS，原型阶段验证值）：`--validate` 约 9 ms，切片 ① 经 HTTP 提交 1953 + 489 行、墙钟 0.14 s，真实片段回放 3906 + 977 行 |
| D-042b | 2026-09-05 | （原编号与上一条重号，2026-09-05 改为 D-042b，先例 D-027b；09 报告、`SceneView.tsx`、e2e 与 WORKLOG 里的引用已同步改）**界面不解释建筑高度来源与估算**：本系统是演示系统，本来就不保证建筑高度是实际值，用户与本方都不关心，只要数据合理、能用于后续计算。由此：① `SceneView` 侧栏删去估算警示框与来源占比行，「按高度来源分色」与占比只在 `?dev=1` 开发者模式出现（带 `data-dev`）；② 端到端断言由「估算高度有显式标注」改为「界面不解释估算高度且无 `[data-dev]`」；③ 铁律 14 改写为「标记留在数据层，界面不解释」，`src` 字段、清单与 `docs/scene-package.md` 的记录不变；④ 09 报告 §0 第 9 条、§5.1、§14 相应改写（v1.4），数据包组不再列估算比例；D-039 ③ 的「低调呈现」对估算高度收紧为「不呈现」，合成场景的小字署名暂按 D-039 保留 | 用户 2026-09-05 指示；D-039 ③ 的进一步收紧 |
| D-043 | 2026-09-05 | **去掉「合成场景」小字署名**：D-042b 的延伸——演示系统不必向用户解释站点与航迹是编的还是录的。场景文件的 `synthetic` 字段保留并随产物溯源走（铁律 14 的数据层要求），界面不据此显示任何标记；09 报告升 v1.5（§0、§3、§5、§12、§14 探针去掉 `badges.synthetic`），06 §9C G-5 验收项、§9E 切片 ② 与风险、§13「不做」第 5 条相应改写；`docs/scenario-format.md` 的 `synthetic` 字段说明改为只作数据层标记。至此界面上不再有任何「向用户解释数据有多不完美」的元素，这类元素默认不做，除非用户点名要 | 用户 2026-09-05 指示「去掉」 |
| D-044 | 2026-09-05 | **B-6 WebSocket 与补取的约定**：① 二进制帧 `[u32 LE header_len][UTF-8 JSON 帧头，空格补齐到 4 字节倍数][Float32 LE × row_len]`，帧头 `{seq, task_id, op_id, kind, row_index, row_len, t_s}`，载荷就是 `.f32` 该行原字节，客户端可零拷贝取 `Float32Array`；② 连接级报文 `subscribed{since, last_seq, run_state}` / `heartbeat{last_seq}` / `dropped{from, to, count}` / `error{code, node_id, port, message}` 沿用信封但 `seq = 0`，不推进客户端 `lastSeq`，唯 `dropped` 令 `lastSeq = to`；③ `product_row` 在 WS 上恒为二进制帧（回放与实时都是），在 `GET /api/v1/tasks/{id}/events` 上恒为文本（无数据）；行读不到退回文本事件、序号不断；④ 只丢二进制行帧（`bufferedAmount` > 1 MiB），丢弃区间在发送下一帧之前先以 `dropped` 告知、故区间连续有序，文本事件永不丢，超 16 MiB 以 4013 断开；⑤ 补取与 WS 回放共用 `readEvents()`：缓冲内切片、缓冲外顺序读 `events.jsonl`（经同一脱敏器、丢末尾半行、文件与缓冲不紧接则不拼）、回放以空批次收口；服务端补发的终态重启后按 `task.json` 合成，对账时 `last_seq` 推到文件末序号 + 1；⑥ 关闭码 4400 订阅报文不合法 / 4404 任务不存在 / 4409 重复订阅 / 4013 跟不上 / 1001 服务停止（退出前先关连接），非 `/ws` 的 upgrade 直接 404 断开；⑦ 一连接一订阅，已结束任务可订阅但应以 `since = last_seq` 订阅、历史走 B-7；⑧ `ws 8.21.3` 随包锁版，`THIRD-PARTY-NOTICES` 建于仓库根 | B-6 实施；`docs/api-versions.md` §3.1a、§4.0；实测（macOS，原型阶段验证值）：切片 ① WS 回放 2447 条事件 82 ms，断线以 `since` 重连首条 = since + 1、并集无缺号，SIGINT 时客户端收 1001 |
| D-045 | 2026-09-05 | **实测数据集作为辐射源模板：评估与路线**（用户问「现有数据集能否作为将来模拟特定无人机信号的基础：按距离、方位与发射机性能，用数据集里的信号特征，叠加传播环境与侦察设备性能，给出信号级侦测模拟」）。① 结论：可以，且正是 04 §7.2 三类辐射源模型（参数化合成 / 实测片段回放 / 半经验特征等效）、§4.3 混合增强与 §11.5「机理模型 + 实测参数分布 + 实测残差或背景片段」的既定路线；设计早已预留，代码一件未做（目录 8 个组件全在 source / data / algorithm，channel / antenna / receiver 为零；`SceneParamFrame` 有字段无生产者与消费者；`geo/` 占位；`models/` 全空；场景波形只有 tone / noise / burst）。② 三种模式都要，**首选特征等效为主线**（参数从实测拟合、波形按参数再生、传播与接收机按模型施加），回放模板作近距离高保真对照，实测背景 + 合成目标作背景。③ 供给边界：数据集能给波形模板（中心频率、占用带宽、谱形、突发统计、频段切换、峰均比）、真实背景、接收机非理想样本、路损粗档，以及可实测的视距 / 非视距附加损耗分布（DroneRFb 同距 10 m 3432 对 1196 片）；数据集未记录的发射功率、位置与方位、天线增益、噪声系数、接收增益**都是场景与设备的配置项，按实际情况设置即可，不是障碍**（用户确认）。模板归一化到单位功率，绝对电平由配置经链路预算得出；配置值带来源字段随产物走，界面不显示（铁律 8、14；D-042）。④ 唯一的技术约束在回放模式：片段自带的底噪与同频背景随信号一起缩放，**回放模板能呈现的最大信噪比 = 模板余量 − 裕度**，不能向比录制更高的信噪比推，往远处推反而越干净；场景要求更高信噪比的情形只能用特征等效。⑤ 防线二、三的释义：公开数据集片段仍不得放到地图上；模板与位置无关，绑定场景的是合成辐射源，`FileReplaySource.scene_bindable = false` 不变，将来的 `TemplateSource` 才可绑定且溯源带 `template_id → source data_ids`。⑥ 落地：规范草案 `docs/emitter-template.md`（T-1）；06 新增 §9F T 线（T-0 探针 → T-3 提取器 → T-4 `TemplateSource` → T-5 闭环与模型卡）；**本期只记录不实现**（用户指示），引擎组件仍按 G-3 / P1-3 / M-2 / M-5 顺序，T-0 探针待排期 | 用户 2026-09-05 提问、确认「配置项不是障碍」并指示先记决策更新文档；04 §7.2、§4.3、§11.2–§11.5；02 §7.1–§7.2；03 §12.2.2；D-018、D-019、D-028、D-037、D-038、D-039 |
| D-046 | 2026-09-06 | **B-7 视窗抽取的约定**：① 区间半开 `[lo, hi)`，行按起始时刻 `r0 = clamp(floor(t0/dt))`、`r1 = clamp(ceil(t1/dt))`，列按 bin 中心 ± 半格 `u(f) = f/bw + floor(nfft/2) + 0.5`；分组边界 `B[g] = floor(g·n/m)` 纯整数运算，`m = min(目标, n)` 即「超过原始数不插值」；② 统计量 `max/min` 直接对 dB 值取（逐位可复现），`mean` 在**线性功率域**聚合 `10·log10(Σ10^(v/10)/count)`、float64 按行主序顺序累加（铁律 5），包络合桶 rms 按样点数加权、末桶只有索引收尾后才按 `last_bucket_samples` 计权；③ **读端一律以文件长度定行数**，索引的 `rows` 只判断是否收尾——引擎因此改为**写完第一行就刷一次索引**（原来要等 64 行，任务开头整段取不到几何参数）；④ 产品或索引未就绪一律 409 `not_ready` + `Retry-After: 1`（不是 404，运行中这是正常早期状态），终态确实没有才 404；⑤ 缺省 `px = min(列数, 4096)`、`py = min(行数, 2048, floor(16 MiB/(列数×4)))`，**不带参数的请求永远不会 413**；超限 413 带按 `sqrt` 缩的 `suggest`，重取必定能过；⑥ 新增索引端点 `.../{kind}/index`（索引原文 + `rows_available` + `index_final` + `run_state`）供客户端建轴；⑦ `scatter` 本版本 404 `product_unsupported`（观测点不产出 `iq` 产品，D-040 ③）；`track/links/detections` 读取层先行、生产者等 G 线，闭区间取窗、按键抽稀；⑧ **Python 参考必须写显式循环累加**——CPython 3.12 起内置 `sum()` 是 Neumaier 补偿求和、numpy 是成对求和，都不是「acc = acc + x」，与 JavaScript 逐位不同 | B-7 实施；`docs/display-products.md` §2、§3.1–§3.4，`docs/api-versions.md` §3.1b，`algos/reference/product_window.py`，`tests/golden/product-window.json`；实测（macOS，原型阶段验证值）：真引擎切片 ① 与回放产品的三组视窗与 Python 参考 2046363 个值**逐位**相同（含 `mean`），黄金基准 12 用例全过；1953 × 1024 全窗 26 ms、同规模 `mean` 71 ms、5632 行取 4802 × 873 为 70 ms、索引端点 0.5 ms |
| D-047 | 2026-09-06 | **功率单位约定与 dBm 纵轴**（U-1 与 DS-8 / DA-8 一并落地）：① 引擎内部功率单位 = mW，任一观测点 `|x|² = 功率 / mW`，`10·log10` 直接是 dBm；合成源新增 `level_dBm` / `power_dBm`（与线性参数互斥，目录 `excludes[]`，装载错误码 `param_conflict`），块元数据 `calibration{calibrated, offset_dB, source, note}`，合成链标 `model`；② 回放源在源端换算 `x = code / full_scale × 10^(full_scale_dBm/20)`，常数来自清单 `power.calibration`；无常数只除满量程并保持降级——**顺带修正了量化码未除满量程的缺陷**（回放谱值曾比 dBFS 高 20·log10(32768) = 90.31 dB，无 golden 依赖，记发现不记基准变更）；`AddMixer` 两路都标定才标定、来源取弱者（`measured > paper > model > assumed`）；观测点索引谱 `scale = dBm` + `calibration`，包络 `sqrt_mW`；③ 两个公开数据集的常数按可得依据估算（`scripts/ds8_calibration.py`，表 `data/iq/measured/calibration.json`）：**DroneRFa −50.0 dBm `paper`**（论文增益 50 dB + 满量程 0 dBm 假定；链路预算法给 −9.8 dBm、相差 40 dB，但按底噪推得的等效噪声系数论文法 1.1 dB、链路预算法 41 dB，故取论文法并把「隐含噪声系数须在 0–15 dB」定为取舍判据）、**DroneRFb-DIR −1.6 dBm `model`**（论文无增益，10 m 视距链路预算反推，隐含噪声系数 42 dB 即近零增益录制）；全部原型阶段验证值（D-028），甲方数据到货后按同一脚本重估；4714 份清单 `absolute_power = estimated` + `power.calibration`，索引加 `calibration` 块；④ **界面只标 `dBm`**，来源徽标、常数与出处只在 `?dev=1` 的 `[data-dev]` 里（用户「标定来源徽标不给用户显示，体现真实性」，修正 D-038 的显示层；数据层「绝不产生不带来源的 dBm」不变，索引写 dBm 却无常数时前端按 `dBFS（未标定）` 显示并记日志）；⑤ 示例框图改真实量级：−70 dBm 单音、−104 dBm 噪声、2.44 GHz。实测（macOS，原型阶段验证值）：单音峰值 −69.9997 dBm、噪声底均值 −132.349 dBm（解析值 −132.342）；示例框图经 `cuav_run` 底噪 −132.34 dBm、峰 −70.9 dBm（100 kHz 偏 0.4 bin 的 hann 扇贝损耗）；DroneRFb 回放标定后每 bin 底噪 −83 dBm（RBW 78 kHz）、突发峰 −49 ~ −36 dBm | 用户 2026-09-06 指示「纵轴以 dBm 显示，标定来源徽标不给用户显示，体现真实性」；D-038、D-020、D-042b、D-043；`docs/display-products.md` §2、`docs/iq-format.md` §4.2 / §5、`docs/component-catalog.md` §4、`docs/diagram-format.md` §4；09 报告 v1.6 |
| D-049 | 2026-09-06 | **切片 ② 场景运行时的约定**：① **距离一律 ECEF 弦长**，段长与链路斜距共用一个函数——有意偏离 em-demo 的球面半正矢（差 0.5%，2 km 段上就是 10 m，远超航迹对拍 1e-6 度的容差）；② **运动学按绝对时间闭式求值**，预算段时轴后二分定位，不做 em-demo 的增量积分（增量积分与块长耦合，换块长就换结果，破坏铁律 9）；`loop` 缺省为假，与 em-demo 的恒循环不同；③ **参数帧到样点用纯整数映射** `[floor(k·fs/rate), floor((k+1)·fs/rate))`，`PortData::scene` 改为 `scenes` 向量并补上 `clear()` 漏清；**`ScenarioSource` 每轮必产出至少一帧**——调度器每轮无条件覆盖下游缓冲（`graph.cpp:241-249`），少产出一轮就会让没被消费的 IQ 块被静默覆盖，样点丢了还不报错；④ **单位功率按「发射期间」定义**：`tone` 恒为 1、`noise` 取 `E|x|²=1`、`burst` 导通区间内为 1 断开区间精确为零，于是观测点上导通期间读到的就是 `Prx = tx_power + G_t + G_r − L`；按占空比平均归一会让瀑布峰值比链路预算低 `10·log10(1/duty)`；⑤ **帧的 `path_loss_dB` 只装纯传播损耗**，发射功率与收发天线增益作施加类信道的内部参数由装载器自场景注入（折进帧会让链路读数名不副实）；⑥ **信道不改变流长度**，时延靠历史缓冲、全流前 D 个样点精确零、`flush()` 不产出；整数样点时延的 ±1 跳变是声明过的模型简化，记 `notes` 但**不标降级**；`time_basis = FileAcquisition` 的输入直接拒（防线二、三的引擎侧执行点）；⑦ **场景标识与数据标识同法**（D-037）：框图只写 `scenario_id`，路径经解析旁挂新增的 `scenarios` 段或 `cuav_run --scenario` 注入；装载器核对场景文件字节哈希与观测区域清单哈希、框图时长不超场景时长、同一场景的绑定节点必须同采样率；新增错误码 `scenario`（18 → 19 个）；⑧ **场景文件的规范序列化形式 = `JSON.stringify(doc, null, 2)` 加末尾换行**，服务端 `PUT` 回传落盘字节的哈希由前端写回框图——两端各自序列化再各自算哈希必然对不上；⑨ 场景 JSON 的读取放引擎（`engine/src/scenario_json.cpp`），`geo/` 保持零第三方依赖，其单测寄放在 `engine/tests/`；⑩ 前端**高频量不进主 store**：实体与链路走 `web/src/scene/sceneStore.ts`，地图按 20 Hz 定频取值；地图拆除是异步事件驱动的，图层入口一律先查地图是否还活着——抛在 React 效应里会让整棵界面树被卸载；⑪ 切片 ② 的功率断言按**带内功率**（峰值邻域 ±5 bin 线性域求和）而非单 bin 峰值：多普勒把谱线在 ±1.2 个 bin 内推来推去，hann 的扇贝损耗最坏 1.42 dB。顺带记两条读数口径：hann 窗下邻域求和得到的是 1.5 倍单音功率（等效噪声带宽，1.761 dB），比差值时精确抵消；`std::cos/sin` 跨平台可能差 1 ulp，产品文件的跨平台逐字节一致本来就不成立 | G-0 至 G-5 实施；08 报告 §9、§15；`docs/{scenario-format, diagram-format, component-catalog, display-products, display-route, api-versions}.md`；实测（macOS，原型阶段验证值）：示例框图 1.5×10⁷ 样点墙钟 0.82 s，绝对链路预算三点偏差 ≤ 0.021 dB，两时刻功率差偏差 0.074 dB，浏览器预览与 C++ 航迹 361 点逐点相同 |
| D-050 | 2026-09-07 | **天线与接收机两类组件缺位的定性与补齐路线（先设计后实现，设计另起对话）**。用户指出「辐射源发射机和天线、接收机和天线都需要在框图中，为何现在没有」。核实结论：① **不是遗漏，是切片顺序**——切片 ①②③ 先打通可见链路，厚化在切片 ④（06 §9E 已写明「完整 S0→S6 链可搭」）；`models/` 六个环节目录早已按 `radiator/channel/antenna/receiver/adc-ddc/channelizer` 建好。② **但代价大于「两个空分组」**：天线增益被折进信道（`engine/src/channel.cpp:122-123` 自场景取 `antenna_gain_dBi` 与站点 `antenna.gain_dBi`，`:237` 施加 `tx_power + G_t + G_r − 路损`），场景里 `antenna.pattern` 字段**声明了但全库无一处使用**，故方向图、指向、副瓣、极化损耗全部缺席，一律按各向同性标量增益；接收机**完全没有**——无噪声系数、无增益与 AGC、无 ADC 量化与削顶、无模拟滤波，噪声靠独立 `NoiseSource` + `AddMixer` 按用户给的绝对电平手兑，而非由噪声系数推出（场景帧算了 `noise_floor_dBm_per_Hz`，`channel.cpp:55` 注明只供链路读数不驱动注入）。③ **现观测点名为 `s4` 但严格说不是 S4**：无接收机前端与 ADC/DDC，三个节点同为 500 kS/s，等于把接收机建模成 500 kS/s 窄带观测窗、发射带宽取 400 kHz；该简化已显式记在场景 `trace.notes` 与 08 §15，但 S1/S2/S3 当前无从生成。④ **补齐的最小集四件**：`AntennaGain`（antenna）、`ReceiverFrontEnd`（receiver，噪声系数生热噪声）、`AdcQuantizer`（receiver，位数/满量程/削顶，产出 S3）、`DDC`（receiver，产出真正的 S4）；**前三件是算术简单件按 D-036 手写 C++ 即可，不必等 MATLAB，只有 DDC 卡在 M-2 的 Coder 产物**。⑤ **先出设计再实现**（06 §13 第 3 条），三个待定问题必须先拍板：天线方向图的形式（查表还是解析式）、接收机噪声与现有 `NoiseSource` 的关系（取代还是并存）、ADC 削顶的降级标记口径。**「接收机噪声取代手兑噪声」会改变现有示例框图与跨层算例 ① 的基准，按铁律 10 不得顺手改**，需走正式的基准变更 | 用户 2026-09-07 提问并指示「在新的对话中先做设计，保存决策记忆」；04 §5.1（端到端对象八环节）、§5.4（S0–S6）；06 §9E 切片 ④、§13 第 3 条；D-036、D-049 ⑤；铁律 10；概念模型 EM-B-07 天线 / EM-B-11 接收机基础 / EM-B-12 噪声是其 M1 参数供给层 |
| D-051 | 2026-09-07 | **框图页改为固定的典型链路、只改参数；检测识别评价成链**（10 报告 v1.0）。用户指示「框图不这么自由组合了，用户使用起来还挺麻烦……先给出一个典型的框架……用户可以在每个框图上进行参数的编辑，然后进行相应的计算，给出信号的检测识别与评价……注重实用性，灵活性可适度降低」。十条：① 框图页默认 = **典型链路视图**，04 §5.1 九环节固定为九个槽位（辐射源 → 发射天线 → 传播信道 → 接收天线 → 接收机前端 → ADC → DDC → 信道化 → 检测识别评价），用户只选变体、改参数、勾观测点，不增删节点、不连线；② **一条链、三种信号源模式**（全合成 / 实测回放 / 混合增强，04 §4.3）：回放模式下六个前端环节标「回放数据已含」并锁定；混合模式合成目标走全链、回放背景在 S4 相加且前端 `noise_mode = none`；只有 DDC 与信道化可旁路（Coder 产物到位前）；采样率只填一处（站点 `fs_Hz`）、频率计划面板做 04 §9.3 六项检查；③ **典型链路是标准框图的生成器**：框图格式只加可选顶层 `template_ref{template_id, mode, version}`，引擎与服务端不解释语义只按白名单接受；编译与反解是纯函数，`compile(parse(doc)) === doc` 逐字节；④ **U-2 自由画布不废，降为高级模式** `#/diagram/canvas`，典型链路可「展开为自由画布」（原文写「单向」，指的是文档解不回槽位，不是导航；**导航双向可达见 D-052**），09 §6 与 `slice3-smoke` 继续有效只改入口；⑤ 新增组件 8 件：`AntennaGain`、`ReceiverFrontEnd`、`AdcQuantizer`（手写 C++，不等 MATLAB）、`DDC`、`Channelizer`（M-2 / M-3 Coder）、`FeatureExtractor`、`TemplateClassifier`、`Evaluator`；既有 3 件加参数且缺省保旧行为（`SceneEmitterSource.emit_at_tx_power`、`SceneBoundChannel.gain_mode`、`EnergyDetector.noise_mode / noise_window_frames / merge_gap_frames / band_power_dBm`）；新端口类型 `RecognitionList`（`port_compat` 36 → 49 行，既有行不变）、`PortSpec.optional`；`SceneParamFrame` 增离开角 / 到达角 / 航向 / `tx_on` / `tx_center_Hz`；`BlockMeta.clip_count`；场景增 `emission.polarization`；错误码 19 → 21；⑥ **D-050 三问拍板**：天线用解析式方向图（全向 / 高斯主瓣 `12·(Δ/θ)²` 截于副瓣底）+ 指向（固定或随航向）+ 五档极化损耗表，查表方向图留 P3；典型链路里**接收机噪声由噪声系数生成、取代手兑噪声**（`N0 = k·T·F`，290 K 时 −174 dBm/Hz + nf；切片 ② 手兑的 −111 dBm 正是 500 kS/s、nf 6 的解析值），既有示例与全部产品黄金基准一件不动；**ADC 削顶是数据标记不是降级**（逐块 `clip_count` 进元数据与索引），全程比例超 `degrade_clip_ratio` 才 `degraded`；⑦ **检测识别评价口径**：能量检测加带删截的滑动噪声估计（`noise_mode = sliding`，兑现 D-026）与突发分段，落 `detections.jsonl`（P1-4c / d）；`FeatureExtractor`（M3 观测量提取，接 IQ + 检测列表，按突发出中心频率 / 带宽 / 功率 / 平坦度 / 占空比 / 跳频差）落 `features.jsonl`；`TemplateClassifier`（EM-S-04 E2：区间距离 + 后验 + 开放集未知，只到 `signal_role` 层；库 `models/recognition/library-v1.json` 取值标 `assumed`）落 `recognitions.jsonl`；`Evaluator` 的真值来自场景参数帧（`tx_on / tx_center_Hz`）或数据清单 `truth`，**只进评价器不进被测算法**（04 §5.2），出帧级 Pd / Pfa / F1、突发级检出与发现时延、ROC（门限扫描不重跑）、识别混淆矩阵，落 `truth.jsonl` + `metrics.json`，`algos/reference/evaluate.py` 逐值对拍；⑧ **基准变更**只有组件目录黄金基准（新增与加参）一次更新，产品基准零变化；`slice1 / slice2 / replay` 原样保留为回归夹具；界面缺省框图改为典型链路（全合成，`demo-02`）；跨层算例 ① 加典型链路实例、原实例不动；⑨ **框图开始落盘**：`GET/PUT/DELETE /api/v1/diagrams[/{id}]`（B-8），`data/diagrams/` 入库，规范序列化同场景文件；结果端点增 `features / recognitions / truth / metrics`；结果页页签改为 信号 / 检测识别 / 评价 / 任务；`task.json` 增 `metrics_summary`；⑩ **新增 C 线**（06 §9G，C-0 至 C-11），切片 ④ 重排为 ④a 链路跑通 → ④b 检测识别评价 → ④c DSP 厚化 → ④d 收口；P1-0b 由 10 报告兑现，P1-4 增 e（特征与识别）、f（评价），U-4 只剩任务列表与数据中心，新增 U-5（典型链路视图）与 B-8（框图落盘）。不做：用户自定义模板与子系统封装（P2）、阵列多站、相噪与压缩（M-5）、学习型与型号级识别（P3 / T 线）、批量参数扫描（P2）、自由画布新功能 | 用户 2026-09-07 指示；04 §4.3、§5.1、§5.2、§5.4、§7、§7.9、§8.2、§9.3、§13.3、§15.2；09 §1.2、§6.11；08 §8、§13；概念模型 EM-B-07 / B-11 / B-12 / S-02 / S-03 / S-04；D-013、D-026、D-036、D-037、D-045、D-047、D-049、D-050；铁律 4、8、9、10、14 |
| D-052 | 2026-09-08 | **框图页两种形态之间的导航是双向的**，修正 D-051 ④ 的「单向」。用户实测：进了自由画布 `#/diagram/canvas` 之后，`Alt+2` 与顶栏「框图」按钮都只切视图不改子形态，回不到典型链路，只剩浏览器后退键与手改地址两条路。根因是 `ui/navigate` 不带 `canvas` 时 reducer 保持原值、Router 又因一致而不改 hash，两处各自都对，合起来是「切视图不改子形态」。三条：① 画布工具条加「回到典型链路」（`data-action="open-chain"`），与「展开为自由画布」对称；② 按下时先 `parseChain(parse(text))`，解得开直接回，解不开（画布里**加**过模板之外的节点；**删**节点仍解得开，缺席槽位按旁路或缺省处理）就当场说明并要确认，确认后落在典型链路页的「不是本模板」提示上，由用户显式点「新建典型链路」才真的换掉文档——**全程不静默丢弃编辑**（铁律 15）；③ `ui/navigate` 不带 `canvas` 时按 `false` 处理，于是顶栏「框图」与 `Alt+2` 回的是框图页的**默认形态**（典型链路），要进画布走按钮或直接用地址。D-051 ④ 那句「单向」说的是**文档**的单向性（画布里改过的框图可能解不回九个槽位），不是导航的单向性，实现时被照字面执行；10 §5.6、09 §17、`docs/diagram-canvas-guide.md` 的措辞已同步改，WORKLOG 的历史条目不改 | 用户 2026-09-08 实测发现并指示另起对话修；D-051 ④；铁律 15；实现 `web/src/diagram/DiagramView.tsx`、`web/src/state/{reducer,selectors}.ts`、`web/src/chain/ChainView.tsx`；`slice4-smoke` 25 → 34 项，新增九条覆盖三条修法与「留在画布 / 仍要回去」两支 |
| D-053 | 2026-09-09 | **多源、多站、测向与定位在框图里的表达（11 号报告 v1.0）**。用户问「场景中的辐射源可能有多个；单个侦测站要做信号识别与测向；多个测向站可以对同一信号源做交叉定位或时差定位——这些在框图里如何体现、如何设置」，并拍板交付范围为**设计 + 纯软件第一切片**。十二条：① **多源按源分支、接收天线后叠加**——辐射源 / 发射天线 / 传播信道 / 接收天线四环节按源（或链路）实例化，新组件 `Superposition` 在接收天线端把 N 路相加，此后仍是一路 IQ（与真实接收机一致：天线口面上是场的线性叠加，接收机只有一条通道）；② **多站 = 典型链路加站点维度**，接收天线之后每环节按站实例化，一次任务跑 K 条接收链，尾部加「测向」与「多站定位」两槽位（9 → 11）；③ **测向与到达时间是 M2 效应模型**（EM-S-05 / S-07 的 E2 档）：从链路帧取真值方位与电平、按噪声系数算 SNR、按七分量误差预算出 σ_θ 与 DF-Q 档，抽一个高斯样本作量测。**取真值的合法性正式声明在报告 §1.3**：01 §6.2 允许模型输入是真值，04 §5.2「真值只进评价器」约束的是**被测的 M3 算法**（检测器、识别器），本线是新加的观测量生产者、不参加检测识别评价；三件必须声明——`model_layer=M2` / `credibility≤V2`、每行 `truth_consumed=true`、模型卡写明「散布来自建模的误差预算，不代表任何真实设备的实测性能」；P1 的阵列估计器将来接**同一端口**替换，框图形状不变；④ **`MultiSiteLocator` 一个组件一个下拉** `method ∈ {aoa, tdoa, aoa_tdoa}`，「关」= 槽位旁路；⑤ 新增端口 `BearingReport / ToaReport / PositionReport`，`can_connect` 仍纯对角、`port_compat` 49 → 100 行且既有 49 行逐字不变；**`ToaReport` 是 05 名单外的补充**——05 假定 TDOA 直接吃 `MultiSiteIQSet`，没给站级到达时间量测留口；⑥ **固定可选口取代动态端口**（`in1..8 / scene1..8 / b1..8 / t1..8`，全 `optional`），零格式改动、N 与 K ≤ 8；身份靠帧与报告自带的 `site_id / emitter_id / link_id`（帧新增这三个显式字段，**不解析 `trace_id`**：那是溯源约定不是数据契约）；配套启用**已预留未使用的错误码 `port_optional`** 与新虚函数 `IComponent::check_wiring()`，错误码总数仍 21；⑦ **`scene_binding` 由「必须且只能二选一」放宽为「至少一个」**（schema `oneOf → anyOf`），两个同时出现须组件目录同时声明两个内部参数；`channel.cpp:125-133` 的单站守卫替换为三分支（空且单站 → 取唯一站，**旧行为原样保留**；空且多站 → 报错；非空 → `find_site`）；⑧ **`sites[].clock`**（`sync_sigma_ns / bias_ns / rx_delay_ns / rx_delay_sigma_ns / sync_state ∈ locked|holdover|unsynced`）是 `LogicalSim` 下**建模的**同步误差，逐行给 `sync_state / time_quality`，把 05 §6.2.2「失锁、补零、重对齐不得当作连续数据」落到每一行；缺 `clock` 而选 tdoa 即 `configure` 失败，**不默认完美时钟**；⑨ **参数按槽位共享，实例差异只来自场景**（九个站不是九组接收机参数）；工具条「查看实例：源 ▾ 站 ▾」只影响派生量显示；⑩ **`template_ref.version` 不升、实例后缀 `__`、N=K=1 逐字节不变**——v1 文档是 v2 槽位表的真子集，升版号反而让既有框图解不开；后缀选 `__` 是因为 id 正则 `[a-z0-9_-]` 里 `@` 与 `:` 不合法；⑪ **移植 emcore** 的 `locate_aoa / locate_tdoa / direction_finding / core/geo` 与四份 golden 到 `geo/`，**求解器改平面坐标接口**（吃米不吃经纬度），111320 常数投影只留 `geo::legacy` 守 golden、引擎喂严格 ENU（D-009 的落地方式），`build-all.sh` 加 `grep legacy::` 守卫；TDOA 缺省加权改为 **`correlated_reference`**（`R = c²(diag σ_i² + σ_0²·11ᵀ)`）——emcore 的站对独立加权低估协方差、结果偏乐观，两种口径并列进模型卡不许调参使其一致；**2σ 椭圆的二维置信是 86.5% 不是 95%**，纠正 em-demo 注释并把 `confidence: 0.865` 写进每一行；⑫ **基准变更**：组件目录黄金基准重生成一次（新增 4 件 + `port_compat` 49→100 + 既有 `SceneBoundChannel` 加内部参数 `site_id`），**产品黄金基准与 `demo-01` 航迹基准零变化**，`DEFAULT_CHAIN_TEXT` 零变化；新增 `demo-03`（三站三角形 × 三源含同频与突发）与五份 golden。**显示**：Canvas 叠加层画测向线（±2σ 楔形）与 AOA / TDOA 双色定位椭圆，`display-route.md` §3 冻结的语义第一次有生产者，三个新色值须按 D2-4 同一脚本实测对比度 ≥ 3:1；**新增 L 线**（06 §9H，L-0 至 L-9）与**切片 ⑥**（⑥a 多源叠加 → ⑥b 多站测向与 AOA → ⑥c TDOA 与融合），插在切片 ④b（C-3）之前。**不做**：阵列测向与 `ArrayIQStream`（P1）、IQ 相关的真到达时间与 `MultiSiteIQSet`（P2，500 kS/s 下 2 µs ≈ 600 m 做不出有意义的 TDOA）、真实站钟与 `DeviceStatus`（P2）、频差定位 EM-S-08、跨站关联 EM-S-11 与多假设、`TrackReport`、NLOS 几何驱动的测向多径误差（等 D3）、用户自定义站点组合模板（P2） | **实施后（2026-09-09，L-0 至 L-8 全部完成）追加三条实测修正**（11 报告 §11.3 六条，模型卡 `models/locate/README.md`）：① **emcore 的 AOA 几何分级看不见近简并配置**——它按**最大**张角分档，「两条近乎平行的线 + 一条好线」最大张角 51–90° 被判 good，而这批解的 2σ 椭圆覆盖率只有 45%；处置是分级一字不动（守 golden），**另加**最小两两交会角 `min_crossing_angle_deg` 并改用它作降级判据（参数名本来就叫最小交会角，原实现比的却是最大张角）。有效档（≥ 15° 且无低权重站）实测覆盖 **87.0%**；② **未标定的固定钟差是协方差表达不了的系统偏差**——`site-3` 的 `bias_ns = 20`（≈ 6 m 距离差）使时差解整体偏 4.4 m，扣掉后 `uav-2` / `uav-3` 覆盖率回到 **87.5% / 86.5%**；这是 `bias_ns` 存在的意义，作特性保留；③ **两种时差加权的方向与本决策原文相反**——原写「独立站对低估协方差、偏乐观」，五种配置实测迹比 0.755–0.915，即它**保守偏大**；`correlated_reference` 1.004–1.013 自洽。两个口径都保留、数值进模型卡，**不调参使其一致**（铁律 10）。另：`MultiSiteLocator` 改为**站址随报告走**（`BearingReport` 加 `site_lon/lat/alt_m`），因此它不绑场景、是纯函数式的——「绑一个站」对全图唯一的融合节点没有语义；`ToaEstimator` 的相关带宽取**站的实际采样率**而非 emcore 写死的 10 MHz（写死会给出与配置无关的假精度）。 | 用户 2026-09-09 提问并拍板四个岔路；01 §6.2、§8；04 §5.1、§5.2、§12.1、§21；05 §3.1–§3.2、§6.2.1–§6.2.3；EM-S-05 §10.14、EM-S-06 §10.4–§10.7、EM-S-07 §10.3–§10.5；D-009、D-013、D-037、D-046 ⑧、D-049 ③⑤、D-051、D-052；铁律 1、3、4、5、9、10、13、15 |
| D-054 | 2026-09-09 | **框图页成为场景内实体电子设备参数配置的主界面**。用户指示「框图画布面板上方增加选择无人机和选择侦测站两个选择框，根据选择具体的实体，同时切换相应对应的框图的参数……电磁传播信道不受两个选择框的影响，单独设置并共用」，并拍板四条：下拉**只切换配置焦点**不改运行范围；共用粒度**引入设备型号字段**（同型号共用一套、可逐实体单独设置）；场景视图的对象表单**保留**并与框图页共用同一套字段定义；改完**自动保存场景**。八条落地：① **实例化维度与参数归属维度分离**——`SlotDef` 新增 `owner`：`tx` / `tx_ant` 归**源**，`rx_ant` 及其后归**站**，`ch` 与 `loc` 归**共用**。`tx_ant` / `rx_ant` 是关键：节点仍按链路展开 N×K 份，但发射天线长在无人机上、接收天线长在站上，按 `per` 取参数会把「改一架机的天线」变成「改一条链路的天线」；② **两层持久 + 型号作派生视图**——`SlotConfig` 只加 `byEntity`（实体 → 覆盖）。**型号层不进状态**：`parseChain()` 只拿得到框图（`DiagramView.tsx:69` 那条路径上根本没有场景），把型号写进状态会让反解随「场景载没载入」变化，`compile(parseChain(doc)) === doc` 当场失效；型号因此是参数行上的**编辑范围**三档（共用 / 同型号 / 单独），**用户选的范围记在组件本地**——范围是从「各实体值一不一致」派生的，只改下拉不改值会被归约立刻打回「共用」，用户永远选不动它（实测踩到）；③ **反解的归约算法**：按 `owner` 把实例归到实体，**同一实体**的多个实例参数仍必须一致（不一致即被手改过，返回 null），**不同实体**允许不同；逐参数取众数作共用底值、并列时取实体 id 字典序最小者，其余进覆盖——划分唯一、逐实体有效值不变，故往返仍逐字节相同；④ **场景加可选 `equipment_model`**（站点与辐射源各一个，辐射源缺席时回退 `platform_type`）。不叫 `model` 是因为本项目里该词专指仿真模型；引擎对场景未知键硬拒绝，故 schema / `scenario_json.cpp` 键表 / `geo::Site`、`geo::Emitter` / 文档同步，引擎只收下不解释；⑤ **消掉双真理源**：`tx_ant.gain_dBi` / `rx_ant.gain_dBi` / `rx_fe.nf_dB` 改由场景**逐实体**带出（新表 `FROM_SCENE`），在框图页可编辑但写的是场景文件；兑现 11 报告 §5.2 第 449 行的承诺。**场景给不出值时仍按「待填」看待**，不无条件放行（铁律 15）；⑥ **自动保存**：`saveScenario` 抽到 `shell/actions.ts` 两处共用，去抖 800 ms、保存中防重入（服务端每次 PUT 都要起一次 `cuav_run --scenario-track`），失败不清 dirty；`useSceneSync` 的既有分支负责把新哈希回填进 `scenario_ref`；⑦ **共用字段定义** `web/src/scene/editor/deviceFields.ts`，两处界面都读它；顺带把自 D-053 / D-051 起有格式却一直没有界面入口的 `sites[].clock` 与 `emission.polarization` 放出来，`setPath` 相应支持按需建中间对象（只建对象不建数组元素）与「写 undefined 即删键」；⑧ **基准变更两处**（铁律 10，各自单独一步收口）：**其一**，`tx_ant.gain_dBi` 改由场景带出后，`chain-3x3-{aoa,tdoa}` 夹具里 `tx_ant__uav-3__*` 由 2 变 0 —— demo-03 的竞速机场景里写的就是 0 dBi，而此前参数按槽位共享逼着三架机共用 2 dBi、`antenna.cpp:169` 又只认组件参数，**场景里逐源的天线增益一直被静默忽略**，`uav-3` 链路预算高估 2 dB、场景页链路读数与 IQ 链对同一条链路差 2 dB；`slice6-smoke` 的预算式同步改逐源取值。**其二**，`demo-03` 从未按 D-049 ⑧ 的规范序列化形式落盘（浮点写成 `120.0`，7497 → 7473 字节），第一次自动保存即被规范化，三处记录的场景哈希同时失效——处置是保留规范化后的文件、重生成航迹黄金基准（723 个样点逐点相同，只 `scenario_sha256` 变）、更新两份夹具的 `scenario_ref.sha256`，并在 `tests/unit/test_scenario_example.py` 加一道守卫：入库的场景文件必须**已经**是规范形式。组件目录黄金基准与产品黄金基准**零变化**，`DEFAULT_CHAIN_TEXT` 逐字节不变 | **实测（macOS，原型阶段验证值）**：`uav-3` 降 2 dB 后 slice6 的统计带全部仍在带内且更准——每站 S1 总功率对三条链路预算和差 **0.002 / −0.013 / 0.001 dB**（此前 ≤ 0.013，两边曾一起错），AOA 有效档 2σ 覆盖 **87.0% → 82.5%**（带 [0.80, 0.93]，是测向信噪比下降的真实后果）、CEP 比 1.029、测向归一化误差方差 1.0225、时差中位误差 7.95 m。全量：引擎 174 项 doctest + 8 项 ctest、服务 119 项、web 175 项、场景 schema 9 项、六套 e2e 14 + 39 + 27 + 30 + 61 + 63 = **234 项**全绿；`build-all.sh` 全过 | 用户 2026-09-09 指示并拍板四条；D-049 ⑧、D-051、D-052、D-053 ⑨（本条修正它的后半：「工具条只影响派生量显示」升级为实体配置焦点）；铁律 8、10、15 |
| D-055 | 2026-09-09 | **框图格式加一段可选的「非活动槽位参数」`template_ref.inactive_slots`，换模式不再丢参数**。起因是 D-054 的实体选择器上线后，用户在辐射源卡片上选「实测片段回放」点运行报 `[param] 未知参数 center_frequency_Hz`，顺藤查出三个缺陷，前两个当场修、第三个由本条兑现：① **换变体时上一个变体的参数照写不误**——槽位参数按**槽位**存，换零件时旧参数被硬塞给新零件；修在 `push()` 这一个收口处（组件目录里没有的参数不写，**不写 ≠ 丢弃**，换回原变体即恢复；目录未到手时不过滤，宁可交给引擎判）。② **辐射源变体与信号源模式各管各的**，造出「模式说全合成、辐射源是回放源」这种设计里没有的状态——回放源不绑场景，`emitterIds` 反解成空，整条链的场景绑定连同 `scn` 节点**静默消失**；新增 `switchTxVariant()` 让两个入口联动（10 报告 §2.2「一条链、三种信号源模式」本来就是同一件事），并给 `useSceneSync` 补 `needAdopt`：非回放模式却没有场景引用时认下当前载入的那一份。③ **换模式会丢前端参数**（本条的主体）：典型链路视图「不持有第二份状态」，画面每次从框图重解，而回放模式下前端六个环节不适用、旁路的环节与组件未实现的环节也一样，都不变成节点，于是**不在文档里的参数就等于不存在**——「全合成 → 实测回放 → 全合成」走一圈，接收机增益与 ADC 满量程悄没声地没了，再点运行报「缺必填参数」（违反铁律 15）。用户从三条路（切模式前先问 / 格式加一段 / 视图层留第二份账）中选了**加格式**，因为只有它真的不丢东西。落地：`template_ref` 加可选 `inactive_slots`（`{槽位: {variant?, params?, by_entity?}}`），挂在 template_ref 下是因为这一段本来就是「还原视图用的线索，不是运行语义」；**引擎与服务端只校验取值、不解释语义**（新增 `check_inactive_slots`，取值错误报 `template`、未知键仍报 `schema`，与该节其余字段同一分工）；写入规则保证既有框图逐字节不变——只存用户改过的（`variant` 非零、`params` 非空、`by_entity` 非空），三样都空不写条目、一条都没有整段不写，槽位按链路顺序、参数名按字母序。逐实体的单独设置（D-054 的 `byEntity`）一并暂存 | **实测**：`DEFAULT_CHAIN_TEXT` 与两份 3×3 回归夹具往返**逐字节相同**（缺省链里 ddc / chan / df / loc 虽不活但参数全空，整段不写）；带 `inactive_slots` 的框图经 `cuav_run --validate` 通过；「全合成 → 实测回放 → 全合成」转一圈后 ADC 满量程 −20、前端增益 20 原样回来，旁路的信道化 `channels: 16` 同样保住。引擎 175 项 doctest + 8 项 ctest、服务 119 项、web 182 项、场景 schema 9 项、六套 e2e 14 + 39 + 27 + 30 + 66 + 63 = **239 项**全绿；组件目录与产品黄金基准零变化 | 用户 2026-09-09 实测报错并在三条路中选定「加格式」；D-051（`template_ref` 的定位）、D-054、D-049 ⑧；铁律 15（不静默丢弃用户输入）、铁律 10 |
| D-056 | 2026-09-09 | **实测录音的挑单**：新增只读端点 `GET /api/v1/datasets?q=&data_id=`，参数面板把 `data_id` 由文本框换成挑单。起因是 D-055 之后用户试回放模式，报「缺必填参数 data_id」而**界面上无处可选**——盘上 4714 段录音、服务端启动时也已全部加载进内存（提交时用来解析 `data_id` → 清单路径），却没有任何接口把清单给出去；「数据中心」整页是 U-4 的活。本条只做挑片段所需的最小集，整页仍留 U-4。五条：① **只出摘要不出路径**（04 §8.6、D-037），`manifestRel` 一律不进响应，服务端有一条测试专门盯着它；② **缺省按机型分组抽样**（每组 ≤ 12、总计 ≤ 400）——挑片段挑的是机型不是某一片，4714 条塞进一个下拉既慢又没用；`total / matched / truncated` 如实给出，界面写明「共多少、列了多少」，不假装列全（铁律 15）；③ `q` 在标识 / 批次 / 机型 / 视距 / 距离 / 划分上子串匹配，用来找具体某一片；④ `data_id` 可精确查一条——框图里已经填着的那个未必落在抽样里，界面仍要显示得出来；⑤ **两批数据的真值字段不一样**（DroneRFb 有视距与精确距离，DroneRFa 只有频段状态与距离区间），故 `distance_text` 是文字不是数值：有精确值给 `10 m`、只有区间给 `20–40 m`、都没有就不给这个键，缺的就是缺的不编。挑单挂在 `ParamPanel` 的 `Field` 里按参数名 `data_id` 分派，典型链路与自由画布因此拿到同一个控件；命中验收集的片段在挑单条目上**只标事实**（写「验收集」二字），不写解释句——**本条同时修订 D-038 的「验收集片段允许回放但提示」**：提示那句话去掉，与 D-042b「界面不解释数据来源与估算」、D-043「去掉合成场景署名」同一条线，用户 2026-09-09 的原话是「这种话都不要，专业人士自己可以判断」。数据层的 `holdout` 标记不受影响，照旧随索引与产物走（铁律 14）。**同批修掉一处假失败**：回放模式下 ADC 与接收机前端都是「回放数据已含」、不参与计算，噪声系数又自 D-054 起要靠场景带出而回放没有场景，于是频率计划里的 `adc_floor` 永远落到「算不出」、挂一个用户消不掉的红叉；改为回放模式不做这条检查 | **实测**：端点对真数据返回 `total 4714 / 12 种机型 / 列出 108 条`；浏览器里筛「Mavic」收窄到 12 条，选中即写进框图、直接运行并跑完（`finished / degraded`，`degraded` 是数据集固有状态，采集参数来自论文而非设备记录，D-020）。服务 124 项、web 183 项、六套 e2e 14 + 39 + 27 + 30 + 72 + 63 = **245 项**全绿；黄金基准与示例场景零改动 | 用户 2026-09-09 实测报错并指示「把选录音的下拉做了」；D-037（浏览器永不见路径）、D-038（验收集可回放但要提示）、D-020、D-054、D-055；铁律 15 |
| D-057 | 2026-09-09 | **同一件事只留一个操作入口：辐射源卡片不再有变体下拉，信号源模式只由试验设置栏那个开关决定**。用户原话：「在实验设置中已经有模式的选择了，在辐射源框图中就不需要再有一个重复的选择了，增加了操作入口，让逻辑变复杂了」。**这是对 D-055 ② 的修正**：那次已经看出「同一件事有两个开关」是「模式说全合成、辐射源是回放源」的根因，却选了让两个开关联动（`switchTxVariant`），而不是去掉多余的那一个——留着的代价是多一个入口、多一条要维护的联动规则，而收益为零（「场景辐射源」= 全合成 / 混合增强，「实测片段回放」= 实测回放，本来就是同一件事）。落地：`SlotDef` 加 `variantFrom?: 'mode'`，`tx` 声明它；`SlotCard` 对这类槽位只把当前变体的**名字**写出来（`data-slot-variant-fixed`），不给下拉；`switchTxVariant` 随之无人调用，删掉不留死代码；`ChainView` 的 `onVariant` 回到最朴素的一版。**传播信道的变体下拉保留**——它的两个变体（场景绑定 / 自由空间定参）不对应任何模式，不是重复入口。D-055 的另两项修正（`push()` 过滤组件不认识的参数、`useSceneSync` 的 `needAdopt`）照旧有效且仍然必要：换模式一样会换 `tx` 变体 | 单测改为对着模式下拉验（并加一条「除辐射源外没有别的槽位跟着模式走」的守卫）；`slice4-smoke` 相应改走 `[data-field=mode]`，新增两项断言（卡片上没有第二个开关、卡片写的名字跟着模式走）。web 183 项、服务 124 项、六套 e2e 14 + 39 + 27 + 30 + 74 + 63 = **247 项**全绿；黄金基准与示例场景零改动 | 用户 2026-09-09 指示；D-055 ②、D-051（10 报告 §2.2「一条链、三种信号源模式」）|
| D-058 | 2026-09-10 | **传播信道从「二选一」改成「选择考虑哪些传播效应」，档位从简单到细致（12 号报告 v1.0）**。用户指示：「传播信道这个环节要反映侦测站与无人机目标所处的传播环境、地理空间、建筑物分布带来的变数——自由空间、城市环境、是否考虑遮挡、绕射、多径……在这个框图里选择要考虑哪些因素，可以从简单选到细致。」并拍板三条：档位 + 可逐项覆盖、选项放右侧参数栏、卡片只列这一档包含哪几项；本期只做框架 + 不需要建筑几何的效应；环境类别放信道参数。**核实到的现状**：`SceneBoundChannel` 与 `FreeSpaceChannel` 今天**在物理上是同一个模型**——都是自由空间路损，区别只在距离是场景几何算出来的还是用户手填的；`geo/src/link_budget.cpp:32,60-61` 里 `line_of_sight` 是字面 `true`、`extra_loss_dB` 是字面 `0.0`，47582 栋建筑一栋也没进过链路计算。十二条：① **档位用 01 的 E1–E4 并直接写进溯源的 `model_level`**：E1 快速抽象（自由空间 + 多普勒 + 时延）= **今天的行为、缺省**；E2 工程（地面双径 EM-P-02 **或** 城市经验 EM-P-05 二选一、统计阴影 EM-P-08、大气与降雨 EM-P-07，各可单独开关）= 本期；E3 精细机理（建筑遮挡与刀口绕射）= 待 D3（切片 ⑤）。② **E3 两头都做**：枚举里保留、界面置灰写「待 D3」、引擎 `configure()` 收到即报错——不隐藏是因为隐藏会让人以为只有两档，引擎也拒是因为界面置灰拦不住手写的框图（铁律 15）。③ **算在 `geo/`（帧生产端），配在 `ch` 卡片，参数声明在 `ScenarioSource` 上**。分层依据：EM-P-01/02/04/08/13 属 M1/M2 参数供给层，「M3 不得绕过 M1/M2 自行硬编码传播参数」，而 `SceneBoundChannel` 是 M3 施加器；实际后果：场景页链路面板的「路损」读数取自 `link` 事件、事件由帧生产端产出，算在信道里读数就名不副实（`link_budget.h` 头注专门告诫过）；顺带的好处是测向误差预算吃的是帧里的信噪比，算在生产端它自动跟着变。④ **槽位表加代理表** `SlotDef.proxy = { type: 'ScenarioSource', node: 'scn', params }`：`SlotPanel` 渲染 `ch` 时把这些参数的 `ParamSpec` 从 `ScenarioSource` 的目录条目取出来一并生成表单；`compile()` 把代理键写到 `scn` 节点、其余写 `ch` 节点；`parseChain()` 反向且要求 K 个 `scn` 的代理参数完全一致。代理集合固定、划分唯一，`compile(parseChain(doc)) === doc` 逐字节仍成立。⑤ **防重复计损 = 一条规则 + 两道闸**（EM-P-13 §10.9 的最小落地）：至多一个「替代型主模型」，选中它就由它给出基础损耗、不再单独叠加自由空间；闸一在界面上——双径与城市经验做成**单选** `prop_primary`，「城市经验 + 自由空间双计」构造不出来；闸二在运行时——城市经验取 `mean_with_shadow_margin` 时自带 90% 分位裕度，此时再开统计阴影即同源双计，`configure()` **报错并说明**，不静默禁用。⑥ **每条链路报告附 `included_loss_terms`**（`free_space / ground_reflection / urban_mean / shadow / weather` 的子集），进 `link` 事件与 `links.jsonl`。⑦ **帧结构不改**：替代型主模型也走 `extra_loss_dB`（`extra = L_primary − L_fs`），于是 `path_loss_dB = free_space_dB + extra_loss_dB` 恒等式在任何档下都成立，`link_budget.h` 承诺的「只改 `extra_loss_dB` 与 `line_of_sight` 两处」逐字兑现，`SceneParamFrame` 一个字段不加。⑧ **随机性**：`geo/` 保持零第三方依赖，定义极小抽象 `INormalSource`，由引擎侧 `Xoshiro256pp` 适配器喂进去，不复制第二份发生器；阴影序列在 `init()` 里按帧序**一次算完存成向量**——`frame(k)` 对同一个 `k` 在 `process()` 里确实会被调用两次（出帧一次、上报一次），且类注释把「可任意乱序、任意次数调用，结果逐位相同」定为硬不变量，递推写在 `frame()` 里会当场违反它。⑨ **`FreeSpaceChannel`（定参）只能是 E1**：它不吃场景帧，选它再选高档位会算出一个不会被施加的路损。前端 `planChecks` 新增第 11 项 `propagation` 拦住；**引擎侧拦不住**（`configure()` 看不见别的节点的类型），如实记，不假装两侧都拦得住。⑩ **已知取舍**：环境类别放在信道参数里意味着同一个场景换一份框图就能变成另一种环境，且全图只有一份（`ch` 的 `owner` 是 `shared`）。物理上环境是地理属性，要收紧就把 `env_class` 挪进场景的 `sites[]` 或 `aoi`，框图只留「算不算它」——与 D-054 挪天线增益同一套路，本期不做。⑪ **参数来源多为 `assumed`**：`env_class` 的 `n` / `X_env`、阴影 σ 表、`d_corr` 是工程取值；材质表（`ε_r / σ / roughness` 六行）直接采用本项目自有的`天津机场_地表材质与建筑数据工作流.md` §3.5；天气两式移植 emcore（其上游是 em-demo 的简化 ITU-R）。**D-019 的「2.4 GHz 城市户外 n ≈ 2」不能拿来给 `urban` 行背书**——那批样本是空地准视距链路，且 D-019 自己写明真实距离比只能定在 2.0–7.5、同一降幅对应 n = 1.29–3.75。全部标「原型阶段验证值」（D-028）。⑫ **顺带改正一处文档错误**：CLAUDE.md 与 D-013 称 `ChannelPathSet`「首期即启用」，与代码不符——它只是 `PortType` 枚举的一个值，零生产者零消费者，`PortData` 里连放它的容器都没有，也没有对应结构体；08 报告 §15 写的「无（首期不实现），仅占类型位」才是实情。改为「首期只做类型位与命名预留，实现留 P3」。**基准变更**：只有组件目录黄金基准重生成一次（`ScenarioSource` 新增 15 个参数）；产品与航迹黄金基准、`DEFAULT_CHAIN_TEXT`、两份 3×3 夹具**零变化**；新增 `tests/golden/propagation.json`（自 emcore 拷入，本期只用大气与降雨两组 64 例，刀口衍射与菲涅尔 v 随 D3）。**不做**：建筑遮挡与绕射（D3）、地形遮挡（DEM 与垂直基准两项都 OPEN）、射线追踪与预计算缓存（P3）、抽头延迟线式统计多径与 `ChannelPathSet` 的实际启用（P3）、LOS/NLOS 场景模板（要 D3）、建筑材质 EM-T-04（铁律 12，P3）、环境类别按地理位置自动分区 | **实测（macOS，原型阶段验证值）**：E1 缺省档下 `slice1` / `slice2` 的 `.f32` 与索引文件与上一版提交**逐字节相同**；slice2 的两时刻功率差偏差 **0.074 dB**、slice6 的逐站 S1 对预算和差 **0.002 / −0.013 / 0.001 dB** 一个数不动。天气两式对 emcore golden **64 例 rel ≤ 1e-9**（2.4405 GHz / 20 km：大气 0.1488 dB、25 mm/h 降雨 0.2231 dB，合计不到 0.4 dB）；城市经验 `open` 档与 `fspl_dB` 逐值相等；双径 `demo-01` 的 `d_bp = 48.84 km`（20 km 观测区域内恒在干涉区）、掠射水面 `C_2ray ∈ [−20, +6.01] dB`、远场 20 → 200 km 增 **39.99 dB**；阴影 40000 帧经验 σ **6.0015**、相邻帧相关 **0.6687**（理论 0.6703）。全量：引擎 **208** 项 doctest + 8 项 ctest、服务 124 项、web 193 项、六套 e2e 14 + 39 + 27 + 30 + 86 + 63 = **259 项**全绿；`build-all.sh` 全过。**实施中三处按物理改断言而不是改模型**：① 双径掠射极限必须用**水平**极化（`Γ_V` 有伪 Brewster 凹陷，0.7° 掠射上 `|Γ_V| = 0.80` 而 `|Γ_H| = 0.997`，垂直极化的相长上界只到 +5.1 dB）；② `d⁻⁴` 远场渐近要把相消限幅放宽过零点深度，缺省 20 dB 限幅下两端都被限住、斜率恰好退回 20 dB/十倍程；③ 25 mm/h 中雨 20 km 是 **0.223 dB**，方案原估的 0.021 dB 差一个数量级。**另外两处实测发现**：④ `ScenarioSource::init()` 无条件从共享流抽 u64 会把下游所有组件的子种子整体挪位，「E1 逐数值等于今天」当场不成立——不开阴影就一个数都不取；⑤ 代理参数必须**「显示什么就写什么」**，退回 E1 时把 `prop_primary = two_ray` 一并写给 `scn`，引擎会（正确地）拒整次运行，不生效的那半收进 `template_ref.inactive_slots`（与 D-055 同一做法） | 用户 2026-09-10 指示并拍板三条；01 §3、§3.5、§8.3、§9.2；04 §5.1、§7.3、§7.4、§9.2、§12.2；05 §5.2、§6.2.3、§25；EM-P-01 / P-02 / P-04 / P-05 / P-07 / P-08 / P-13；08 §9、§15；10 §2、§5；11 §2、§5；D-005、D-009、D-013、D-019、D-028、D-033、D-036、D-049、D-050、D-051、D-053、D-054、D-055；铁律 1、2、5、8、9、10、11、14、15 |
| D-059 | 2026-09-10 | **传播信道撤掉「自由空间（定参）」变体：框图页上它永远是错的选择**。用户实测后指出：「手填一个固定值完全没有必要，我也没这个需求去计算一个特定距离的自由空间传播衰减」。核实成立，而且比 D-058 §4.5 原定的「加一道检查拦住」更彻底——**框图页上能跑起来的配置一定有场景**：回放模式下本环节整个不适用，全合成与混合增强都必须选中场景、站点与目标才通得过频率计划的 `stations` 检查。也就是说这个变体在这个页面上没有一次是对的，摆着就是个陷阱。四条：① **从槽位表撤掉**，`ch` 只剩「场景绑定信道」；`SlotCard` 的下拉渲染条件本来就是 `variants.length > 1`，于是卡片上连变体下拉都不再出现，距离、发射功率、收发天线增益四项「待填」随之消失（它们在场景绑定模式下由场景带出）。② **组件本身保留**：`FreeSpaceChannel` 是标准算例与解析锚点的对拍件（`test_channel.cpp` / `test_receiver_chain.cpp` 在用），自由画布与手写框图里照常可用；组件目录、端口表、黄金基准**零变化**。③ **`propConflict()` 里那条只为它存在的判据删掉不留死代码**（同 D-057 的处置）——风险从源头消除，比留一道闸干净；检查项 `propagation` 保留但改名「传播档位自洽」，只剩与引擎 `PropagationConfig::validate()` 同源的三条。④ **解不开的框图照实说缘由**：新增 `RETIRED_VARIANTS` / `retiredNote()`，`parseChain()` 解到已撤变体时仍返回 null（**不硬解、不改写用户的文档**，铁律 15），但界面从「当前框图不是典型链路」改为说明「那个零件撤了、在自由画布里照常打开」。**残留风险如实记**：自由画布里仍可搭出 `ScenarioSource(prop_level = E2)` + `FreeSpaceChannel`，此时链路读数与实际施加的 IQ 不一致；画布不跑频率计划检查，引擎也看不见别的节点的类型（`check_wiring()` 只拿得到自己被连上的输入口名，而 `ScenarioSource` 没有输入口）。属高级用法自负其责，写进模型卡 | web 195 项（新增 2 项：只剩一个变体、已撤变体的框图不硬解且说得出缘由）；`slice4-smoke` 86 → **88 项**（卡片上没有变体下拉、不再要求手填距离）；引擎 208 + 8、服务 124、六套 e2e **261 项**全绿；黄金基准与示例场景零改动 | 用户 2026-09-10 实测提问「目标和侦测站的距离是随时变化，但是可以得到的值，不是应该用这个值吗」并拍板；**修订 D-058 §4.5**；D-057（同一件事不留多余入口、删死代码）；铁律 15 |
| D-060 | 2026-09-10 | **删掉自由画布，框图页只留典型链路一种形态**。用户原话：「自由画布功能先删除吧，目前先按这个固定模式来，先把一个完整的信号级仿真的流程跑通。免得分散精力，自由画布不重要，用户操作起来也很难控制，有点华而不实。后续我想清楚了，再加也不迟。」七条：① **删的是画布，不是文档模型**。`web/src/diagram/` 里 `doc.ts`（框图文档的解析与规范序列化）与 `format.ts`（工程计数法）是全项目在用的，留；`ParamPanel.tsx` 只有 `Field` / `range` 两件被典型链路用，拆出来改名 `Field.tsx`，其余（整块节点参数面板、互斥参数分组、观测点面板）随画布去掉。删掉的是 `DiagramView / NodeCard / Palette / layout` 与它的单测，外加**本来就已无人引用的** `DiagramJsonView`（切片 ① 的过渡形态，被画布顶替后忘了删）与只导出这两者的 `index.ts`；CSS 去掉画布壳、组件库、节点与端口把手四段。② **`@xyflow/react` 连同 13 个传递依赖不再随包**，`THIRD-PARTY-NOTICES` 记一条说明；**D-032 允许 React Flow 随包的那半条作废**，`ws` 那半仍然有效。前端运行时依赖回到 `maplibre-gl / pmtiles / react / react-dom` 四件。③ **路由去掉子形态**：`Route.canvas`、`ui.diagramCanvas`、`ui/diagramCanvas` 动作、探针里两处 `canvas` 全部删掉。旧地址 `#/diagram/canvas` **不做特殊处理但也不丢**——`#/diagram` 之后多出来的段一律忽略，于是收藏夹里的旧链接仍落在框图页；地址规范化放在 Router 的 **hashchange 处理**里而不是「状态 → hash」那个 effect 里：`#/diagram` → `#/diagram/canvas` 是同文档跳转，解析出的视图没变、状态不动、组件不重渲染，那个 effect 一次也不会触发（第一版就写错在这儿，端到端当场红）。用 `replaceState` 不进历史栈。④ **D-052 连同它守的缺口一起消失**：没有子形态，就不可能被困在某个子形态里。`switchTxVariant` 式的死代码不留（同 D-057）。⑤ **解不成典型链路的框图：不硬解、不改写，原文只读摆出来**（铁律 15）。`parseChain()` 照旧返回 null，但页面从「在自由画布打开」改为把 `diagram.text` 只读放进 `<pre>` 供拷走，加一个「新建典型链路」按钮并注明它会**换掉**这份文档。只读是有意的：可编辑的原始 JSON 框跟画布一样难以控制，这里要的只是一条不丢数据的退路。⑥ **端到端改走公开端点**：`slice1` / `slice2` 原先靠画布「源码」页签的示例下拉载入手写框图，改为`PUT /api/v1/diagrams/{id}` 存进去再刷新页面，由启动时的「载入最近保存的框图」捡起来（`loadDiagramViaApi()`，两处各一份），`finally` 里走 HTTP 删掉不留副作用。**`slice3-smoke`（30 项，画布专属）整套删除**；`slice4` 里 D-052 的画布往返一段换成三条新断言（旧地址落在框图页、地址被规范回、页面上没有组件库与画布入口）加两条只读退路的断言。⑦ **文档**：`docs/diagram-canvas-guide.md` 改名 `docs/chain-view-guide.md` 并全篇改写为框图页（典型链路）的操作说明；10 报告 §5.6 与 09 §6 的画布章节标注作废但不删（它们记的是当时的设计过程）。**恢复成本**：全部改动在一个提交里，git 历史完整，将来想加回来是 revert 加适配，不是重写 | 前端产物 **1679434 → 1472301 字节**（−202 KiB，−12.3%；实测：把上一版提交检出到工作树另建一次，两侧同法打包比对）；`web/src` 与 `tests/e2e` 合计 **−1353 / +270 行**；web 189 项、六套 e2e 减为五套（`slice3` 30 项整套删除，`slice4` 88 → 85——换掉的画布往返一段比新加的断言多）。**顺带修了两处端到端框架本身的坑**：① 只改 hash 不算导航、页面不重载，先改 hash 再 `Page.reload` 又可能在 hash 生效前重载旧地址，改用一次性查询参数一步到位；② 页面有未保存改动时 `beforeunload` 让 Chrome 弹「离开此页？」，无头下没人点，`Page.navigate` 永远不返回、用例既不失败也不结束（slice2 布过站后正是脏的），在 `cdp.mjs` 里根治：`Page.enable` + 自动放行对话框；五套 e2e 14 + 39 + 27 + 85 + 63 = **228 项**、引擎 208 + 8、服务 124 项全绿；`build-all.sh` 全过；黄金基准与示例场景零改动 | 用户 2026-09-10 指示；D-032（React Flow 随包的那半条据此作废）、D-051 ④（自由画布降为高级模式）、D-052（画布 ↔ 典型链路双向可达，随之作废）、D-057、D-059；铁律 6、15 |
| D-061 | 2026-09-12 | **态势与信号可视化提升：借鉴 Aaronia AARTOS 与 em-demo，数据不新造、只改表达（13 号报告 v1.0）**。用户看过 `ref/Aaronia AG/` 的 25 张图与 2 段视频后指出其态势与信号可视化「对我们这个项目有很多借鉴之处」，又指出 em-demo「界面排版还是可以有很多借鉴的」；拍板六条：先写方案、代码待排期；第一个切片「一屏讲清楚」= V-1 目标卡片栈 + V-2 地图叠加加重 + V-3 时间轴 + 采用任务不载场景的缺口；时间轴做全宽条；圆形告警区进本切片；卡片栈常驻右栏；底部信号条留 V-10。十二条：① **学什么**——Aaronia 八式（一屏、目标卡片、重叠加、方位玫瑰、多频段总览、网格瀑布、余辉、时差面板）、em-demo 版式（左工具栏 / 地图 / 底部信号条 / 右栏目标列表表格 + 分节详情 / 悬浮播放条）、T.META 概念图（顶栏计数卡、地图上目标卡、目标列表）；② **缺的是表达不是数据**：链路帧、每站测向报告、三种定位解、dBm 谱都在，界面上态势元素太轻（站点 24 px、目标 25.6 px、测向线 1.5 px、楔形 α 0.1）、右栏空态、一次一个观测点、无时间轴；③ **版式**守 09 §4.4 三栏，右栏常驻目标列表 + 卡片栈（选中对象时表单叠在栈顶），**全宽时间轴条作 `.app` 网格第四行**（`var(--topbar-h) 1fr auto auto`，场景页与结果页可见），不做浮动面板与可停靠窗口；④ **卡片只摆事实**：无「威胁 %」「概率 %」（无生产者，铁律 15），接收电平与信噪比按链路帧算 `P_rx = P_tx + G_t + G_r − L`、`N = −174 + nf + 10·log10 fs`（与 `locate.cpp:255-257` 同式），AOA 行的 `gdop` 实为 `rms_trace_m`、按行标单位；⑤ **告警区** = 场景对象 `zones[]`，本期只圆形 `{id, name, kind ∈ alert \| warning, shape: circle, center, radius_m, alt_max_m?}`，引擎收下不解释（`equipment_model` 先例），服务端 `PUT` 不用改，schema / 格式文档 / 引擎键表三处同一提交；目标入圈即图标红环变体与「告警区内」徽标，判定纯前端几何、不进引擎与产品——`display-route.md` §3 冻结的「威胁红黄绿」第一次有生产者；⑥ **时基**：时间轴 `t` = 引擎逻辑时间；信号游标相对 `index.t0_s`，换算 `cursor = t − t0_s`；状态放外部小 store `timeStore`（同 `cursorStore` 范式）不进主 reducer，拖动中 100 ms 一次同步信号游标；⑦ **回放缓冲**：`sceneStore` 今天只留每键最后一帧（`replaceFromTrack`），加按键排序的历史数组与 `snapshotAt(t)` 二分取值；未运行时用 `RoutePreview` / `ActivityPreview` 预览；⑧ **叠加加重**：测向楔形改渐隐色带（α 0.35 → 0.05）、椭圆 2.5 px、目标图标 40 px 加白环与选中环、站点 32 px、链路线 3 px 加距离标注（09 §5.2 早已规定）、高度立柱用 `fill-extrusion` 小方块（AGL，平地假设）；新色值按 D2-4 同一判据 ≥ 3:1 实测后回填 `display-route.md` §4；⑨ **顺带发现一**：刷新后采用了最近任务却载入缺省场景——`bootstrap` 场景作业恒取清单第一项且与任务作业并发（`actions.ts:31-39, 62-68`），`task.json` 只有 `scenario_sha256` 没有 `scenario_id`（`manager.ts:611/692` 算出、`:264` 丢掉）；修法：服务端加 `scenario_id`，`adoptTask` 据此载入，`bootstrap` 先任务后场景；⑩ **顺带发现二：链路线从未画出**——`link_id = "site-1-uav-1"` 被 `useSituation.ts:134-136` 与 `ObjectForm.tsx:377-379` 按最后一个 `-` 拆成 `site-1-uav` / `1`，站查不到就 `return`，链路 GeoJSON 恒空；`slice2` 只断言图层存在与数据、不断言渲染；修法：按 `derivedLinks(doc)` 精确匹配，`slice2` 加渲染断言；⑪ **基准变更只有一处**：`demo-03` 加告警区 → 重生成 `tests/golden/scenario-track-demo-03.json`（723 样点必须逐点相同、只 `scenario_sha256` 变）+ 两份 3×3 夹具的 `scenario_ref.sha256`；`demo-01` 不动故 `DEFAULT_CHAIN_TEXT` 逐字节不变；组件目录与产品黄金基准零变化；⑫ **不学**：深色主题与彩虹色图（D-017）、可停靠块窗口（D-060 同理）、无生产者的数字、卫星底图、DroneID / 干扰 / 光电。**切片**：⑧ 一屏讲清楚（V-0 至 V-3，插在 ⑦ 之后、④b 之前，可与 C-3 并行）；⑨ V-4 方位图 + V-5 精度场（`cuav_run --field`，已核实 `geo::Scenario` 无观测区域范围、`--out` 只配 `--run` 的守卫、网格产品不进 `<op_id>/` 时间行契约）；⑩ V-6 网格瀑布、V-7 余辉、V-8、V-9；⑪ V-10 全景一屏（信号视图三个模块级单例改按实例持有）、V-11。新增 06 §9J 与切片 ⑧；09 报告 §3.2 的「顶栏原位可拖游标」改为全宽条（v1.10）。**本轮只写文档，代码待用户排期** | 用户 2026-09-12 指示并拍板六条；04 §3.1、§8.1、§8.2；05 §5.2；09 §2、§3.2、§4.4、§5、§7、§11–§13；10 §5.7；11 §5；12 §5；D-001、D-017、D-030、D-039、D-042b、D-043、D-047、D-048、D-049、D-051、D-053、D-054、D-058、D-060；铁律 1、2、6、8、10、14、15 |

## 对上游文档的修正与补充

- 显示子线（D0–D4）是 04 之外的新增需求，04 修订前标 **PROVISIONAL**。
- 场景与目标活动生成（G 线）同为 04 之外的新增需求：04 只在 §3.1 提到「配置无人机工作状态、位置、航迹和传播条件」，无场景编辑条款；按 D-001 兑现，04 修订前标 **PROVISIONAL**（D-030）。
- 多源多站测向定位（L 线）是 04 之外的扩展：04 §5.1 的端到端对象末环节写有「测向或定位」，但 §12.1 与 §21 把首期定为单站单通道，05 把测向与多站定位分别归 P1、P2。按 05 §3.2「P2 可以先用纯软件多站数据开展」执行，04 修订前标 **PROVISIONAL**（D-053）。
- 传播环境与效应选择（R 线）是 04 之外的细化：04 §7.3 列了传播模型简化策略六项，但没有「让用户在框图里选算哪几项」这一条，也没有档位机制；EM-P-13「传播模型选择与降级」在概念模型库里是现成的，本系统此前一件未实现。按 D-058 兑现，04 修订前标 **PROVISIONAL**。
- 04 §8.1 结果页的「星座图」首期只做 S4 观测点 IQ 散点，真正的星座图随 P3 调制波形（D-034）。
- 05 编制依据是 04 V1.0（其首期基线假设无 B/S 条目）；05 与 04 V2.0 冲突处以 V2.0 为准。
- 04 附录 D 引用编号有误："08.面向反无人机应用…需求分析" 实为 02；"基于MATLAB_Simulink…v3.0" 应为 03。
- 04 §8.1 引用的 `无人机信号侦察仿真系统_主界面交互原型_v1.0.html`、04 附录 D / 05 附录 C 引用的《城市反无人机电子侦察IQ仿真两阶段建设方案》《信号侦察模型_信号级与IQ级模拟能力说明》《signal signature database》《实施方案_v1.0》、01/02 引用的三张架构图 png，均不在本目录，已列 OPEN。
- foundation README 所述顶层 CMakeLists 与 target 名 `model_foundation_*` 与实际不符，以实际为准（无顶层；`model_math / model_io / model_serialization`）。
- 概念模型库 60 份无 M 层字段；本项目以「功能级—信号级衔接」一节的族归属表为口径，建 `docs/concept-model-layers.md` 时逐份确认。
- 04 §15.2 十二项标准算例建议增补跨层一致性算例（D-014）；04 修订前以本文件为准。

## 继承的坑（尚未在本项目复现，先记）

- PMTiles 需要 HTTP Range：`python3 -m http.server` 对 Range 返回整文件；用 Airports `serve.py` 或自实现。
- 矢量档案 zoom 范围内缺瓦片渲染为空白，不回退父级；混合粒度 merge 后区域外放大一片空白。
- Tauri 自定义协议不支持 Range，WKWebView 明显慢于 Chrome（本项目不用 Tauri 的直接原因之一）。
- Web 资源文件名含非 ASCII，macOS 打包后在 Windows 解压乱码导致 404。
- foundation 缺 `ModelApi.hpp`；emcore 三套常数刻意不统一，"统一化"会破坏 golden。
- 定位 CEP 公式曾经蒙特卡洛散布校验发现有误（√trace → 0.5887·(σmax+σmin)），TS/C++ 两侧同步修正（EM-C-UAV 09 号方案 W4）——解析锚点与统计校验缺一不可。
- 本机仓库目录名 `804 C-UAV` **含空格是有意保留的探针**：远端仓库名 `UAV_ER_Simu` 不含空格，克隆、持续集成检出与交付包都不会有，所以这个空格只存在于开发机上，正好让"路径没加引号、`file://` 没做编码"这类问题在开发阶段就暴露，而不是等到 Windows 上（那里的真实路径常含空格与中文）才炸。代价是必须守两条：脚本里路径一律加引号；拼 `file://` 一律用 `pathToFileURL` 而非字符串拼接——2026-09-04 服务静默不启动就是后者造成的，`import.meta.url` 把空格编码成 `%20`，与 `process.argv[1]` 比较永远不相等。
- MATLAB `pwelch` 的 `'centered'` 频率范围对偶数 nfft 是 `(−fs/2, fs/2]`，把奈奎斯特频点放在正端，与 `fftshift`（numpy 与 MATLAB 自身）的 `[−fs/2, fs/2)` **差一格**；三方互证时一律用 `'twosided'` 再 `fftshift`。2026-09-05 首次对拍相对误差 7e4 就是这一格造成的。
- Windows 交付经验（em-demo 打包说明）：启动脚本 `.bat` 必须 GBK 编码 + CRLF 换行，否则中文乱码或无法执行；macOS 上 zip 打包中文文件名到 Windows 解压乱码；Windows 原生构建在 Mac 上无法交叉完成，需 Windows 机或 CI。
