# 12. 传播环境与效应选择实施方案

> **版本** v1.0，2026-09-10。**定位**：把「传播信道」这个环节从「场景绑定 / 自由空间」的二选一，改造成「**选择要考虑哪些传播效应，从简单选到细致**」；档位与逐项开关放在框图右栏，计算落在帧生产端 `geo/`。作为独立的 **R 线**，插在切片 ④b（C-3 检测升级）之前。**依据**：01 §3（M/E/V 三维正交分级）、§3.5（层次判据）、§8.3（跨层一致性）、§9.2（概念模型单元的角色）；04 §5.1（端到端对象）、§7.3（传播模型简化策略）、§7.4、§9.2（溯源）、§12.2（慢变参数在采样率上施加）、§15.2、§16.3；05 §5.2（`SceneParamFrame` / `ChannelPathSet`）、§6.2.3、§25；概念模型 EM-P-01（自由空间）、EM-P-02（地面双径）、EM-P-04（城市建筑遮挡）、EM-P-05（城市经验）、EM-P-07（大气与降雨）、EM-P-08（统计阴影）、EM-P-13（传播模型选择与降级）；08 报告 §9、§15；10 报告 §2、§5；11 报告 §2、§5；决策 D-005、D-009、D-013、D-033、D-036、D-049、D-050、D-051、D-053、D-054、D-055；铁律 1、2、5、8、9、10、11、14、15。**本方案的结论记为决策 D-058。**
>
> **用户指示（2026-09-10）**：「传播信道这个环节要反映侦测站与无人机目标所处的传播环境、地理空间、建筑物分布带来的变数——自由空间、城市环境、是否考虑遮挡、绕射、多径，以及其他由传播环境带来的影响与效应；在这个框图里选择要考虑哪些因素，可以从简单选到细致。」
>
> **用户拍板（2026-09-10，三条）**：① **档位 + 可逐项覆盖**，选项放在右侧参数栏里选，卡片上只列出这一档包含哪几项效应；② **本期只做框架 + 不需要建筑几何的效应**，建筑遮挡与绕射留 D3（切片 ⑤）单独一轮；③ **环境类别放信道参数**（已知取舍见 §1.4）。
>
> 本方案只定语义、参数、公式、界面与步骤，不含实现记录；实现中核实到的偏差按 06 §14 体例另记修正。量化数字一律「原型阶段验证值」（D-028）。

---

## 0. 一页结论

| # | 问题 | 结论 |
|---|---|---|
| 1 | 今天的两个变体差在哪 | **差在距离从哪来，不差在物理**。`SceneBoundChannel` 与 `FreeSpaceChannel` 的内核都是 `geo::fspl_dB`；前者的距离由场景几何算出、后者由用户手填。「场景绑定」这个名字容易让人以为它考虑了环境，其实 `line_of_sight` 恒为字面 `true`、`extra_loss_dB` 恒为字面 `0`（`geo/src/link_budget.cpp:32,60-61`），47582 栋建筑一栋也没进过链路计算 |
| 2 | 改成什么 | 传播信道槽位改成「**选档位 + 勾效应**」。档位用 01 的精度等级 E1 / E2 / E3，直接写进溯源六件套的 `model_level`；每一档声明它包含哪些效应、依据哪个概念模型、可信度如何 |
| 3 | 本期做到哪一档 | **E1 与 E2**。E1 = 今天的行为（自由空间 + 多普勒 + 时延），**缺省**；E2 = 地面双径**或**城市经验（二选一）+ 统计阴影 + 大气与降雨（各可单独开关）。E3（建筑遮挡与刀口绕射）**枚举里保留、界面置灰、引擎 `configure()` 收到即报错**——不隐藏是因为隐藏会让人以为这条链只有两档，引擎也拒是因为界面置灰拦不住手写的框图（铁律 15） |
| 4 | 算在哪、配在哪 | **算在 `geo/`（帧生产端），配在 `ch` 卡片（用户要求的位置），参数声明在 `ScenarioSource` 上**。CLAUDE.md「功能级—信号级衔接」明写 EM-P-01/02/04/08/13 属 M1/M2 参数供给层、「M3 不得绕过 M1/M2 自行硬编码传播参数」，而 `SceneBoundChannel` 是 M3 的施加器（只做逐样点乘、相位斜坡、样点搬移）。还有一条实际后果：场景页链路面板的「路损」读数取自 `link` 事件，事件由帧生产端产出，把附加损耗放到信道里算，读数就与实际施加的不一致 |
| 5 | 卡片上的参数怎么跨节点 | 槽位表加一张**代理表** `SlotDef.proxy = { type: 'ScenarioSource', node: 'scn', params: [...] }`。`SlotPanel` 渲染 `ch` 时把这些参数的 `ParamSpec` 从 `ScenarioSource` 的目录条目里取出来一并生成表单；`compile()` 把 `slots.ch.params` 里属于代理集合的键写到 `scn` 节点、其余照旧写 `ch` 节点；`parseChain()` 反向。代理集合固定，往返划分唯一，`compile(parseChain(doc)) === doc` 逐字节仍成立 |
| 6 | 防重复计损怎么做 | 一条规则 + 两道闸。规则：**至多一个「替代型主模型」；选中它就由它给出基础损耗，不再单独叠加自由空间**。闸一在界面上——地面双径与城市经验做成**单选** `prop_primary ∈ {free_space, two_ray, urban_empirical}` 而不是两个勾选框，「城市经验 + 自由空间双计」在 UI 层面构造不出来。闸二在运行时——城市经验取 `mean_with_shadow_margin` 档时自带阴影裕度，此时再开统计阴影就是同源双计，`configure()` **报错并说明**，不静默禁用 |
| 7 | 下游怎么知道已经含了什么 | 每条链路报告附 `included_loss_terms`，取值是 `free_space / ground_reflection / urban_mean / shadow / weather` 的子集（EM-P-13 §10.9 的最小落地）。它进 `link` 事件与 `links.jsonl`，随溯源走 |
| 8 | 帧结构改不改 | **不改**。`LinkBudget` 早就把 `free_space_dB` 与 `extra_loss_dB` 分开存了，只是没往帧里带；替代型主模型也走 `extra_loss_dB`（`extra = L_primary − L_fs`），于是 `path_loss_dB = free_space_dB + extra_loss_dB` 这个恒等式照旧成立，`link_budget.h` 头注承诺的「只改 `extra_loss_dB` 与 `line_of_sight` 两处，帧结构与本接口不变」逐字兑现。`SceneParamFrame` 一个字段不加 |
| 9 | 随机性怎么注入 | 沿用既有范式：组件在 `init(IRandom&)` 里派生私有子流，**每条 (站, 源) 链路一条独立子流**。`geo/` 保持零第三方依赖：定义一个极小的抽象 `INormalSource { virtual double normal() = 0; }`，由引擎侧的 `Xoshiro256pp` 适配器喂进去，**不在 `geo/` 里复制第二份发生器**。阴影序列在 `init()` 里按帧序**一次算完存成向量**，`frame(k)` 因此仍是 `const`、无副作用、可乱序调用——这是既有的硬不变量 |
| 10 | 缺省档必须是什么 | **逐数值等于今天**。E1 档下代码路径与今天逐字相同：`extra_loss_dB = 0.0`、`path_loss_dB = free_space_dB`。`DEFAULT_CHAIN_TEXT`、两份 3×3 回归夹具、产品黄金基准、`demo-01` 与 `demo-03` 航迹基准、六套 e2e 的硬断言一个数字不动。R-1 单独收口，不通过不进 R-2 |
| 11 | 基准怎么变 | **只有组件目录黄金基准重生成一次**（`ScenarioSource` 新增 15 个参数）。产品黄金基准、航迹黄金基准、`DEFAULT_CHAIN_TEXT`、两份回归夹具**零变化**。新增 `tests/golden/propagation.json`（自 emcore 拷入，只用其中大气与降雨两组共 64 例） |
| 12 | 顺带改正一处文档错误 | CLAUDE.md `:72` 与 D-013 称 `ChannelPathSet`「首期即启用」，**与代码不符**：它只是端口类型枚举的一个值，零生产者、零消费者，`PortData` 里连放它的容器都没有，也没有对应结构体。08 报告 §15 自己写的是「无（首期不实现），仅占类型位」。本方案改正这处表述（§6.4） |
| — | **不做** | 建筑遮挡与刀口绕射（EM-P-04，D3 / 切片 ⑤）；地形遮挡（EM-P-03，需 DEM 与垂直基准，两项都 OPEN）；射线追踪与预计算缓存（EM-P-11 / P-12，P3）；抽头延迟线式统计多径与 `ChannelPathSet` 的实际启用（P3）；LOS / NLOS 场景模板（需 D3 的视距判定）；建筑材质 EM-T-04（铁律 12，P3）；环境类别按地理位置自动分区（§1.4 的已知取舍） |

---

## 1. 现状、依据与已知取舍

### 1.1 现有传播只有三项（逐行核实）

全仓库的传播物理集中在 `geo/src/link_budget.cpp`（68 行），只有三项：

| 项 | 算式 | 位置 |
|---|---|---|
| 自由空间路损 | `L = 20·log10(4πd/λ)`，`λ = c/f`，`c = 299792458`（D-009） | `link_budget.cpp:11-15` |
| 多普勒 | `−f·(dr/dt)/c`，径向速率取 ECEF 速度在视线单位矢量上的真投影 | `:17-19, 34-40` |
| 传播时延 | `d/c` | `:21` |

没有第四项。全量搜 `occlusion / diffract / multipath / shadow / knife / P.526 / two-ray / 遮挡 / 绕射 / 多径 / 阴影` 的命中**全部是注释与「待做」说明，零实现**。

距离、方位、俯仰、多普勒是**真算**的（WGS-84 椭球 ECEF→ENU 闭式换算，`geo/src/geodesy.cpp:104-116`）；只有 `line_of_sight` 是字面常量 `true`。

### 1.2 两处连带后果

1. **测向误差预算里的非视距多径项永不生效**。`geo/src/df_error.cpp:22` 按 `line_of_sight` 二选一取 `sigma_mp_los_deg` 或 `sigma_mp_nlos_deg`（缺省 5.0°），而视距恒真，所以非视距那一支从未参与过任何一次计算。`engine/src/locate.cpp:101-103` 的参数说明自己也写明了这一点。**本期不改变这件事**——视距判定要建筑几何，留 D3；模型卡与界面读数须说清「视距判定待 D3」，不能让人以为城市经验模型顺带做了遮挡判定（§8 风险 5）。
2. **场景数据包里 47582 栋建筑目前只用于三维显示**（D0 已完成，`buildings.geojson` 15.9 MB），没有参与任何物理计算。铁律 11「渲染—物理同源」在渲染这一侧已经兑现（同一份 GeoJSON 驱动 `fill-extrusion`），在传播这一侧要等 D3。

### 1.3 架构上这是预留好的扩展点

`geo/include/cuav_geo/link_budget.h` 的头注原文：

> 首期只有自由空间：`line_of_sight` 在显式平地假设下恒真，`extra_loss_dB` 恒 0。
> D3（切片 ⑤）接入建筑遮挡后**只改 `extra_loss_dB` 与 `line_of_sight` 两处**，帧结构与本接口不变。

本方案照此扩展：新增的效应全部经 `extra_loss_dB` 汇入，**包括替代型主模型**——`extra = L_primary − L_fs(d)`，于是 `path_loss_dB = free_space_dB + extra_loss_dB` 这个恒等式在任何档位下都成立，帧结构一个字段不加。

### 1.4 已知取舍：环境类别放在信道参数里

用户拍板第 ③ 条。这意味着**同一个场景换一份框图就能变成另一种环境**：物理上环境是地理属性、不该由框图决定。

记在这里，等哪天要收紧时知道从哪儿动手：把 `env_class` 挪进场景的 `sites[]` 或 `aoi`，框图只留「算不算它」，与 D-054 把天线增益与噪声系数挪进场景是同一个套路（那次是为了消掉双真理源）。收紧的代价是场景 schema 变更 + 一次基准迁移，本期不做。

同一取舍还有一个副作用：`env_class` 与 `ground_type` 是**全图一份**（`ch` 槽位的 `owner` 是 `shared`，用户 2026-09-09 明确要求传播信道不随实体选择变化），不是逐链路的。三个站分处闹市与郊野时只能取一个类别，这在 20 × 20 km 的演示 AOI 内可接受，写进模型卡的适用范围。

### 1.5 可用资产

| 来源 | 内容 | 本期取用 |
|---|---|---|
| `C-UAV Model Demo/emcore/{include,src}/models/propagation.*`（147 行） | 自由空间、刀口衍射、菲涅尔 v、建筑穿透、大气、降雨、综合 | **只取大气与降雨两个函数**（R-2）。刀口衍射与菲涅尔 v 随 D3 一并移植 |
| `C-UAV Model Demo/emcore/tests/golden/propagation.json`（548 例） | 上述七组的黄金基准 | 只用 `atmosphericLoss_dB` 40 例 + `rainAttenuation_dB` 24 例 = **64 例**；其余留 D3 |
| `C-UAV Model Demo/emcore/{include,src}/models/occlusion.*` + `map/local_scene_adapter.cpp` | 线段-建筑遮挡、`IMapQuery`、100 m 桶网格 + DDA | **D3（切片 ⑤）**，148 例 golden |
| `/Users/zhiyu/CC/C-UAV/天津机场_地表材质与建筑数据工作流.md` §3.5 | 地表材质 → `εr / σ / roughness` 默认表（六行） | **本期直接采用**作双径反射系数的材质表（§3.2） |

**移植常数**：本期只移植的两个函数（大气、降雨）**都不含光速常数**，因此 D-009 警告的「emcore 用 `3e8`、`geo/` 用精确值，FSPL 两处不逐位相等」这一条**在本期不构成冲突**——冲突要到 D3 移植刀口衍射与菲涅尔 v 时才出现（`fresnelV` 用 `kSpeedOfLight`）。这是对实施计划原假设的一处更正，记在这里免得日后当缺陷查。

---

## 2. 档位与效应清单

档位用 01 的精度等级 E1–E4（CLAUDE.md 的分级口径规定代码与元数据只允许 M / E / V / L 四种字段）。选中的档直接写进溯源六件套的 `model_level`。

| 档 | 名称 | 包含的效应 | 本期 |
|---|---|---|---|
| **E1** | 快速抽象 | 自由空间路损 + 多普勒 + 时延 | ✅ **= 今天的行为，缺省** |
| **E2** | 工程 | E1 之上：地面双径 **或** 城市经验模型（二选一）、统计阴影、大气与降雨（各可单独开关） | ✅ 本期 |
| **E3** | 精细机理 | E2 之上：建筑遮挡与刀口绕射（逐建筑几何）、由几何判定的 LOS / NLOS | ❌ **待 D3（切片 ⑤）**：枚举保留、界面置灰、引擎 `configure()` 收到即报错 |
| E4 | 实测融合 | 区域实测校准残差回写（EM-P-05 §10.11 的 `C_cal`、EM-P-08 §10.15 的 σ 校准） | ❌ 待 P3（甲方数据到货后） |

### 2.1 效应与它们的组合角色（EM-P-13 的 `model_role`）

| 效应 | 概念模型 | 角色 | 与自由空间的关系 | 本期 |
|---|---|---|---|---|
| 自由空间 | EM-P-01 | `baseline` | — | ✅ 已有 |
| 地面双径 | EM-P-02 | **替代型主模型** | `substitute`：`L_2ray = L_fs(d_d) − C_2ray`，自带基础损耗 | ✅ 新写（R-3） |
| 城市经验 | EM-P-05 | **替代型主模型** | `substitute`：`L_emp` 已含基础损耗 | ✅ 新写（R-5） |
| 统计阴影 | EM-P-08 | `residual_statistical` | `additive`，加在均值上 | ✅ 新写（R-4） |
| 大气与降雨 | EM-P-07 | `auxiliary_additive` | `additive` | ✅ 移植（R-2） |
| 建筑遮挡绕射 | EM-P-04 | 附加型主模型 | `additive`（进 `extra_loss_dB`） | ❌ D3 |

### 2.2 防重复计损：一条规则 + 两道闸

EM-P-13 有八种 `model_role`、八种 `path_loss_semantics`、六种组合关系。本期**不全套实现**，压缩成：

> **至多一个「替代型主模型」；选中它就由它给出基础损耗，不再单独叠加自由空间。其余是加项。**

- **闸一（界面上就构造不出来）**：`prop_primary` 是**单选**（`free_space` / `two_ray` / `urban_empirical`），不是两个勾选框。EM-P-13 §10.9 高风险组合表里的「EM-P-04 + EM-P-05 逐建筑遮挡与城市经验遮挡重复」在本期天然不存在（P-04 不实现），「城市经验 + 自由空间双计」由单选消除。
- **闸二（运行时裁决，报错不静默）**：EM-P-13 §10.9 的「EM-P-05 + EM-P-08 经验残差与阴影衰落重复抽样」是本期唯一真实存在的同源双计。裁决按原文——「若 EM-P-05 只给均值，可继承 sigma；**若已抽样则禁用 EM-P-08**」：城市经验模型的 `urban_loss_mode = mean` 时只给均值，可与统计阴影并用；取 `mean_with_shadow_margin` 时自带 90% 分位裕度（`1.2816·σ`），此时再开统计阴影，`ScenarioSource::configure()` 报错并说明，不静默禁用（铁律 15）。

### 2.3 `included_loss_terms`

每条链路报告附一个有序子集，取值域固定五项：

```
free_space | ground_reflection | urban_mean | shadow | weather
```

`free_space` 恒在（替代型主模型的表达式里也含它）。下游据此判断能不能再叠加——这是 EM-P-13 §10.9 的最小落地。它进 `link` 事件与 `links.jsonl`，不进 `SceneParamFrame`（帧是 IQ 施加路径，加字段没有消费者）。

---

## 3. 公式与解析锚点

以下 `d` 一律是 ECEF 弦长（D-049 ①「距离一律 ECEF 弦长」），`λ = c/f`，`c = 299792458`（D-009）。

### 3.1 自由空间（EM-P-01，E1，已有）

```
L_fs(d, f) = 20·log10(4π·d/λ)
```

解析锚点（已在 `slice2-smoke` 中）：2.4 GHz、1 km → **100.052008 dB**。

### 3.2 地面双径（EM-P-02，E2，R-3）

**几何**（EM-P-02 §10.2，局部平面地表假设，铁律 2 的显式平地假设 `terrainHeight_m`）：

```
h_t = tx.alt_m − terrainHeight_m       离地高度
h_r = rx.alt_m − terrainHeight_m
d_d = d                                 直达路径 = 已有的 ECEF 弦长
ρ   = sqrt(max(d_d² − (h_t − h_r)², 0)) 水平距离
d_r = sqrt(ρ² + (h_t + h_r)²)           镜像法反射路径
Δd  = d_r − d_d
sin α = (h_t + h_r)/d_r                 掠射角；入射角 θ_i = 90° − α，故 cos θ_i = sin α
```

**反射系数**（§10.4）：

```
ε_c = ε_r − j·σ/(ω·ε_0),  ω = 2πf,  ε_0 = 8.8541878128e-12 F/m
Γ_H = (cosθ_i − sqrt(ε_c − sin²θ_i)) / (cosθ_i + sqrt(ε_c − sin²θ_i))
Γ_V = (ε_c·cosθ_i − sqrt(ε_c − sin²θ_i)) / (ε_c·cosθ_i + sqrt(ε_c − sin²θ_i))
A_rough = exp[−(4π·σ_h·cosθ_i/λ)²]      Rayleigh 型粗糙度削弱（§10.4）
Γ_eff = Γ_pol · A_rough                  A_div = A_shadow = A_material = 1（E2 档，§10.4 允许）
```

极化取场景 `emission.polarization`（D-051 已有字段）：`vertical` → `Γ_V`，`horizontal` → `Γ_H`，其余（`rhcp` / `lhcp` / `slant45`）按 §10.4「极化未知时不得默认高可信相干深衰落」处理——取 `Γ_H` 与 `Γ_V` 的**幅度较小者**并标 `degraded`、`reason = "极化未按线极化给出，双径反射系数取保守值"`。

**材质表**（直接采用 `天津机场_地表材质与建筑数据工作流.md` §3.5 的默认表；`ground_type` 是信道参数）：

| ground_type | ε_r | σ (S/m) | σ_h (m) |
|---|---:|---:|---:|
| `paved` | 5.0 | 0.008 | 0.003 |
| `grass` | 15.0 | 0.020 | 0.010 |
| `water` | 80.0 | 5.0 | 0.002 |
| `dirt` | 8.0 | 0.010 | 0.010 |
| `unknown`（缺省） | 8.0 | 0.010 | 0.005 |

**叠加**（§10.6 的部分相干式，`ρ_c` 是相干因子）：

```
Δφ = arg(Γ_eff) − 2π·Δd/λ
r   = |Γ_eff| · d_d/d_r
C_2ray = 10·log10[ 1 + r² + 2·ρ_c·r·cos(Δφ) ]      dB，> 0 为增强
L_2ray = L_fs(d_d) − C_2ray
```

`ρ_c = 1` 即退化为 §10.7 的相干式 `10·log10|1 + Γ_eff·(d_d/d_r)·e^{−j2πΔd/λ}|²`。缺省取 **1.0**（窄带演示场景），并在模型卡里写明 §10.6 的建议默认是 `partially_coherent`——取 1.0 是为了让「双径」这件事在演示里看得见，属于**有意的演示取向**，不是对概念模型的照搬。

**限幅**（§10.8 第 3 条）：`C_2ray` 下限为 `−max_fade_depth_dB`（缺省 20 dB），触发即记 `clipped` 标记。数值上完全相消会给出不现实的无限损耗。

**衰落状态**（§10.8）：`constructive ≥ +3 dB`、`neutral (−3, +3)`、`destructive (−10, −3]`、`deep_fade ≤ −10 dB`；`ρ_c < 1` 时一律降为 `averaged`。

**断点距离**（§10.7）：`d_bp = 4·h_t·h_r/λ`，作解释字段输出。

**解析锚点**（R-3 验收）：

| # | 配置 | 期望 |
|---|---|---|
| A1 | `Γ_eff ≈ −1`（掠射极限：`ground_type = water`、`σ_h = 0`、`h_t = h_r = 5 m`、**水平极化**，扫 `d ∈ [300, 2000] m`），`ρ_c = 1` | 相长上界 `+6.01 dB`（`Δd = λ/2`，`d ≈ 814 m`），相消触发限幅 `−20 dB`（`Δd = λ`，`d ≈ 407 m`）。**必须用水平极化**：`Γ_V` 有伪 Brewster 凹陷，同一 0.7° 掠射角上 `|Γ_V| = 0.80` 而 `|Γ_H| = 0.997`，垂直极化的相长上界只到 `+5.1 dB` |
| A2 | `h_r = 0`（接收天线贴地） | `d_r = d_d`、`Δd = 0`、`α = arcsin(h_t/d_d)`；`C_2ray = 20·log10|1 + Γ_eff|`，与手算一致 |
| A3 | `σ_h → ∞`（粗糙面） | `A_rough → 0`、`C_2ray → 0`、`L_2ray → L_fs`，即**双径退化为自由空间** |
| A4 | `d ≫ d_bp`（`h_t = h_r = 2 m` ⇒ `d_bp = 130 m`），水平极化、`ρ_c = 1`、**`max_fade_depth_dB` 放宽到 60** | `L_2ray ≈ 40·log10 d − 20·log10(h_t h_r)`，20 km → 200 km 路损增 **39.99 dB**（自由空间只增 20 dB）。**限幅比零点浅时看不到远场渐近**：缺省 20 dB 限幅下两端都被限住，斜率恰好退回 20 dB/十倍程 |
| A5 | `d_bp` 数值 | `demo-01`（`h_t = 50`、`h_r = 30`、2.4405 GHz）→ `d_bp = 4·50·30/λ = 48.83 km`，远大于 AOI 尺度 ⇒ 演示范围内**恒在干涉区**，看到的是起伏而不是 `d⁻⁴` |

**降级**（铁律 15，不静默）：`h_t ≤ 0` 或 `h_r ≤ 0`（收发端在参考平面之下）时没有地面反射路径，退回自由空间并置 `degraded = true`、`reason = "收发高度不在参考平面之上，无地面反射路径"`；`included_loss_terms` 相应**不含** `ground_reflection`。

### 3.3 城市经验（EM-P-05 的 E1 档 log-distance，E2 档位内，R-5）

EM-P-05 §10.6 的 log-distance 模板：

```
L_emp = L_fs(d_0, f) + 10·n·log10(d/d_0) + X_env  [+ 1.2816·σ_sh  当 urban_loss_mode = mean_with_shadow_margin]
```

| env_class | n | X_env (dB) | 说明 |
|---|---:|---:|---|
| `open` | 2.0 | 0.0 | **恒等于自由空间**——这是替代关系的钉子（§9 验收） |
| `suburban` | 2.6 | 3.0 | |
| `urban`（缺省） | 3.0 | 6.0 | |
| `dense_urban` | 3.5 | 9.0 | |

参数来源标 `assumed`（原型阶段验证值，D-028）。**与本项目自有实测的关系**：D-019 从 DroneRFa / DroneRFb 得出「2.4 GHz 城市户外链路 `n ≈ 2`」的粗档结论，且明确记了「1.93 的前提是距离取区间中点这一计算假设，真实距离比只能定在 2.0–7.5，同一实测降幅对应 `n = 1.29–3.75`」。因此上表**不能说是被 D-019 证实的**：D-019 的样本是空地链路（无人机在空中、多为准视距），落在 `open`–`suburban` 之间；表里 `urban` / `dense_urban` 两行是地面站到街谷内目标的工程取值，无本项目实测支撑。模型卡按 D-019 的三件事一并写：粗档结论、计算假设、不得用于标定。

`n` 与 `d_0` 都可由用户覆盖（`path_loss_exponent`、`ref_distance_m`；`-1` 表示按表取）。

**近区**（§10.2 归一化失败处理 / §10.18 out_of_range）：`d ≤ d_0` 时 log-distance 外推没有意义，退回 `L_fs(d)` 并置 `degraded = true`、`reason = "距离小于参考距离 d0，近区退回自由空间"`。缺省 `d_0 = 100 m`。

**解析锚点**（R-5 验收）：

| # | 配置 | 期望 |
|---|---|---|
| B1 | `env_class = open`（`n = 2`、`X_env = 0`），任意 `d > d_0`、任意 `f` | `L_emp` **逐位等于** `L_fs(d, f)`（因为 `L_fs(d_0) + 20·log10(d/d_0) ≡ L_fs(d)`，只差一次浮点重排，容差 1e-9） |
| B2 | `d = d_0` | `L_emp − L_fs(d_0) = X_env` 精确 |
| B3 | `d` 十倍 | `ΔL_emp = 10·n` dB/十倍程 |
| B4 | `urban_loss_mode = mean_with_shadow_margin`，`env_class = urban` | 比 `mean` 档高 `1.2816 × 5.0 = 6.408 dB` |

### 3.4 统计阴影（EM-P-08，E2，R-4）

dB 域零均值正态、沿航迹一阶相关（§10.3、§10.6、§10.9）：

```
σ_sh = shadow_sigma_dB（≥ 0 时用它）否则按下表
ρ_k  = exp(−Δs_k / d_corr)                Δs_k = 第 k 帧与第 k−1 帧目标位置的 ECEF 弦长
X_0  = σ_sh · Z_0
X_k  = ρ_k·X_{k−1} + sqrt(1 − ρ_k²)·σ_sh·Z_k,   Z_k ~ N(0,1)
extra += X_k                              正值 = 额外衰减
```

σ 表（EM-P-08 §10.4 的两个维度：`urban_class` × `los_state`）：

| env_class | σ_LOS (dB) | σ_NLOS (dB) |
|---|---:|---:|
| `open` | 3.0 | 5.0 |
| `suburban` | 4.0 | 6.0 |
| `urban` | 5.0 | 8.0 |
| `dense_urban` | 6.0 | 10.0 |

**本期 `line_of_sight` 恒真，所以只有 LOS 一列会被取到**（§1.2 第 1 条）。NLOS 列先写进表里，D3 接上视距判定即自动生效——这与 `df_error.cpp` 里 `sigma_mp_nlos_deg` 的处境一样，模型卡须写明。

`d_corr` 缺省 **50 m**（EM-P-08 §10.6 的 `d_corr` 语义：相关系数降到 `1/e` 的距离）。

**复现性**：阴影序列在 `ScenarioSource::init()` 里按帧序**一次算完存成 `std::vector<double>`**，`LinkFrameSource::frame(k)` 只查表。这是必须的——`frame(k)` 在现有代码里对同一个 `k` 会被调用多次（一次给端口出帧、一次给观察者上报），且必须 `const`、无副作用、可乱序（`geo/include/cuav_geo/scenario.h` 的类注释是硬不变量）。递推式实现会当场违反它。

**解析 / 统计锚点**（R-4 验收）：

| # | 判据 | 期望 |
|---|---|---|
| C1 | 固定种子跑两次 | 序列逐位相同 |
| C2 | `d_corr → 0`（或 `Δs ≫ d_corr`） | `ρ_k → 0`，退化为独立抽样；样本标准差 → σ_sh |
| C3 | 蒙特卡洛矩校验，`n ≥ 20000` | 经验 σ 与 σ_sh 相对差 ≤ 3%（照 L 线测向 σ 的做法）；经验均值 ‖μ‖ ≤ 0.05·σ |
| C4 | 相关系数 | 相邻帧样本相关系数与 `exp(−Δs/d_corr)` 相对差 ≤ 5% |
| C5 | 平稳性 | 递推式的稳态方差恒为 σ²（`ρ²σ² + (1−ρ²)σ² = σ²`），逐帧方差不随 k 漂移 |

### 3.5 大气与降雨（EM-P-07，E2，R-2，移植 emcore）

逐字移植 `emcore/src/models/propagation.cpp` 的两个函数，**保留原常数与原分段**（D-009：移植模块守原常数，禁止顺手统一）：

```
γ(f_GHz) = 0.005 + 0.001·f_GHz        f < 10 GHz
         = 0.01·f_GHz − 0.05          10 ≤ f < 50
         = 0.5 + 0.02·(f_GHz − 50)    f ≥ 50
L_atm = γ · d_km

k = 1e-4 · f_GHz^1.5
a = 1 + 0.02·f_GHz
L_rain = k · R^a · d_km               R = rain_rate_mmh，R ≤ 0 时为 0
```

黄金基准：`tests/golden/propagation.json`（自 `emcore/tests/golden/propagation.json` 拷入，`_meta` 记来源与去处，与 L 线四份 golden 同法），只用 `atmosphericLoss_dB` 40 例 + `rainAttenuation_dB` 24 例，rel ≤ 1e-9。

**量级说明**（写进模型卡，实测值）：2.4405 GHz、20 km → `L_atm = 0.1488 dB`；同距离 25 mm/h 中雨 → `L_rain = 0.2231 dB`。**在本项目的频段与距离上，天气项合起来不到 0.4 dB**。留它是为了框架完整与将来的高频段，不是因为它在 2.4 GHz 演示里重要——模型卡要写清楚，免得有人以为「开了天气就算上了雨衰」是个有分量的效应。

---

## 4. 参数、配置与落点

### 4.1 参数表（声明在 `ScenarioSource` 上，显示在 `ch` 卡片右栏）

| 参数 | 类型 | 缺省 | 含义 |
|---|---|---|---|
| `prop_level` | 枚举 `E1 / E2 / E3` | `E1` | 传播精度档。`E3` 收到即报错（§4.4） |
| `prop_primary` | 枚举 `free_space / two_ray / urban_empirical` | `free_space` | 替代型主模型，**至多一个**（闸一） |
| `prop_shadow` | 布尔 | `false` | 统计阴影（EM-P-08） |
| `prop_weather` | 布尔 | `false` | 大气与降雨（EM-P-07） |
| `env_class` | 枚举 `open / suburban / urban / dense_urban` | `urban` | 环境类别（§1.4 的已知取舍） |
| `ground_type` | 枚举 `paved / grass / water / dirt / unknown` | `unknown` | 反射面材质，查 §3.2 的 `ε_r / σ / σ_h` 表 |
| `ground_roughness_m` | 数值 m | `-1` | RMS 粗糙度；`-1` = 按 `ground_type` 取表值 |
| `coherence_rho` | 数值 [0, 1] | `1.0` | 相干因子 `ρ_c` |
| `max_fade_depth_dB` | 数值 dB | `20` | 相消限幅（§10.8 第 3 条） |
| `path_loss_exponent` | 数值 | `-1` | 城市经验的 `n`；`-1` = 按 `env_class` 取表值 |
| `ref_distance_m` | 数值 m | `100` | 城市经验的 `d_0` |
| `urban_loss_mode` | 枚举 `mean / mean_with_shadow_margin` | `mean` | 是否自带 90% 分位阴影裕度（闸二） |
| `shadow_sigma_dB` | 数值 dB | `-1` | `σ_sh`；`-1` = 按 `env_class` × 视距取表值 |
| `shadow_corr_distance_m` | 数值 m | `50` | `d_corr` |
| `rain_rate_mmh` | 数值 mm/h | `0` | 降雨率 |

`-1` 一律读作「按表取」，`0` 一律是**字面 0**（`rain_rate_mmh = 0` 就是无雨、`ground_roughness_m = 0` 就是理想光滑面）。三处用 `-1` 而不是 `0` 作哨兵，正是因为 `0` 在这三处是有物理意义的取值。

### 4.2 计算落点

**算在 `geo/`（帧生产端），配在 `ch` 卡片（用户要求的位置），参数由典型链路编译进隐含节点 `scn`。**

- 新增 `geo/{include/cuav_geo,src}/propagation.{h,cpp}`：纯函数、零第三方依赖，只用标准库；
- `geo::link_budget()` 增一个 `const PropagationConfig&` 参数（**带默认实参**，既有调用点与单测一字不改）；
- 结果仍只经 `extra_loss_dB` 与被替代的基础损耗汇入 `path_loss_dB`。

不放在 `SceneBoundChannel` 里的三条理由（任一条单独成立）：

1. **分层**。CLAUDE.md「功能级—信号级衔接」写明 EM-P-01/02/04/08/13 属 M1/M2 参数供给层，「M3 组件在采样率上施加」且「M3 不得绕过 M1/M2 自行硬编码传播参数」。`SceneBoundChannel` 是 M3 施加器（`engine/src/channel.cpp:223-283` 只做逐样点乘、相位斜坡、整数样点搬移）。
2. **读数自洽**。场景页链路面板的「路损」取自 `link` 事件，事件由帧生产端产出。附加损耗算在信道里，读数就与实际施加的不一致——`link_budget.h` 头注专门告诫过这件事。
3. **复用**。测向误差预算（`geo/df_error.cpp`）吃的是链路帧里的信噪比，它依赖 `path_loss_dB`。算在帧生产端，测向自动跟着变；算在信道里，测向仍按自由空间算。

### 4.3 三处让改动比预想小的事实

1. **计算不需要给参数帧加字段**。`geo::link_budget()` 收到的 `LinkGeometry` 本来就带 `distance_m / azimuth_deg / elevation_deg`；帧是**输出**不是输入。双径要的收发离地高度由 `link_geometry()` 顺手算出（`alt_m − terrainHeight_m`），只加两个成员。
2. **链路报告已经有距离**。`LinkFrame` 已带 `distance_m / azimuth_deg / elevation_deg`，读数侧不缺东西，只新增 `free_space_dB / extra_loss_dB / included_loss_terms` 三项。
3. **阴影要的沿航迹状态在生产端拿得到**。`LinkFrameSource` 是按（站, 源）一条一个实例，站址与目标航线都在手，序列可以在 `init()` 里一次算完。

### 4.4 E3 的两头处理

E3 是**参数枚举值**不是组件，因此与 DDC「组件不在目录里」的处理方式不同，两头都要做：

- **界面**：`prop_level` 的 `E3` 选项**置灰并写明「待 D3（切片 ⑤）」**。不隐藏——隐藏会让人以为这条链只有两档。
- **引擎**：`ScenarioSource::configure()` 收到 `E3` 即报错，报文写明「E3（建筑遮挡与刀口绕射）需要逐建筑几何，待 D3（切片 ⑤）；本版本支持 E1 / E2」。界面置灰拦不住手写的框图（铁律 15）。

### 4.5 `FreeSpaceChannel`（定参）不再出现在框图页（**2026-09-10 由 D-059 修订**）

原方案的处置是：`FreeSpaceChannel` 不吃场景帧（`engine/src/channel.cpp:379-410` 是无状态常数增益），拿不到几何与航迹，选它再选高档位会算出一个**不会被施加**的路损（`link` 事件里带着附加损耗，IQ 上一点没有），因此在频率计划里加一项检查拦住。

**用户 2026-09-10 实测后指出这个变体本身就不该在这里**：「手填一个固定值完全没有必要，我也没这个需求去计算一个特定距离的自由空间传播衰减」。核实下来他是对的，而且比「拦住」更彻底——**框图页上能跑起来的配置一定有场景**：回放模式下本环节整个不适用，全合成与混合增强都必须选中场景、站点与目标才通得过频率计划的 `stations` 检查。也就是说这个变体在这个页面上**永远是错的选择**，摆着就是个陷阱。

改为（D-059）：**从槽位表里撤掉该变体**，`ch` 只剩「场景绑定信道」一个，卡片上连变体下拉都不再出现（`SlotCard` 的渲染条件就是 `variants.length > 1`）。组件本身保留——它是标准算例与解析锚点的对拍件（`test_channel.cpp` / `test_receiver_chain.cpp` 在用），在自由画布与手写框图里照常可用。

随之：`propConflict()` 里那条只为它存在的判据无人可达，**删掉不留死代码**（同 D-057 的处置）；检查项 `propagation` 保留，改名为「传播档位自洽」，只剩与引擎 `validate()` 同源的三条。

**自由画布上的残留风险如实记**：在画布里仍然可以搭出 `ScenarioSource(prop_level = E2)` + `FreeSpaceChannel`，此时链路读数与实际施加的 IQ 不一致。画布本来就不跑频率计划检查，引擎侧也看不见别的节点的类型（`IComponent::check_wiring()` 只拿得到自己被连上的输入口名，而 `ScenarioSource` 没有输入口）。这一条属于自由画布的高级用法自负其责，写进模型卡。

---

## 5. 界面

### 5.1 `ch` 卡片：只列这一档包含哪几项

卡片正文（`SlotVariant.summary` 之外）加一行**效应清单**，由当前配置直接派生（不读引擎输出）：

```
E1 · 自由空间
E2 · 自由空间 + 地面双径 + 统计阴影
E2 · 城市经验(urban) + 大气降雨
```

数据属性 `data-slot-effects="free_space,ground_reflection,shadow"` 供端到端断言。

### 5.2 右栏：档位与逐项开关

`SlotPanel` 渲染 `ch` 时，除本槽位组件（`SceneBoundChannel` / `FreeSpaceChannel`）自己的参数外，再把代理参数的 `ParamSpec` 从 `ScenarioSource` 的目录条目取出来生成表单，分成一个独立的分组「传播效应 · 全图共用」。

分组内按相关性显隐（不禁用、直接不渲染无关项，避免十五行参数一次全摆出来）：

| 显示条件 | 参数 |
|---|---|
| 恒显 | `prop_level`、`prop_primary`、`env_class` |
| `prop_level = E2` | `prop_shadow`、`prop_weather` |
| `prop_primary = two_ray` | `ground_type`、`ground_roughness_m`、`coherence_rho`、`max_fade_depth_dB` |
| `prop_primary = urban_empirical` | `path_loss_exponent`、`ref_distance_m`、`urban_loss_mode` |
| `prop_shadow = true` | `shadow_sigma_dB`、`shadow_corr_distance_m` |
| `prop_weather = true` | `rain_rate_mmh` |

`prop_level = E1` 时，`prop_primary` 之外的效应项一律不渲染，并在分组标题下写一行「E1 只算自由空间路损、多普勒与时延」。

面板上的「参数归属」行照旧写「全图共用」（`ch` 的 `owner` 是 `shared`，D-054）。

### 5.3 代理参数机制

槽位表 `SlotDef` 新增：

```ts
proxy?: { type: 'ScenarioSource'; node: 'scn'; params: readonly string[] }
```

- `compile()`：`ch` 槽位的参数按代理集合切成两份——代理键写进**每个** `scn` 节点（`ch` 是 `shared` 槽位，K 个站拿到同一份），其余照旧写 `ch` 节点。
- `parseChain()`：从 `scn` 节点读回代理键并填进 `slots.ch.params`；**K 个 `scn` 节点的代理参数必须完全一致**，不一致说明这份框图被手改过，返回 `null`（与「同一实体的多个实例参数必须一致」同一判据）。
- 回放模式与 `ch` 旁路时没有 `scn` 节点，代理参数随 `ch` 整个槽位进 `template_ref.inactive_slots`（D-055），换模式不丢。

代理集合固定、划分唯一，`compile(parseChain(doc)) === doc` 逐字节仍成立。

### 5.4 `missingParams` 与错误反查

- `missingParams(chain, 'ch', ...)` 除本槽位组件的必填项外，还要看代理参数——它们全部有缺省值，因此实际不会产生「待填」，但判据要写对，免得将来加一个必填代理参数时漏掉。
- 引擎报错落在 `scn` 节点上时，`nodeSlot[scn] = 'scn'`，报文进「试验设置」那一格（`__setup`）。传播参数报错**应该落到 `ch` 卡片**：`compile()` 的 `nodeSlot` 无法区分同一节点上的两类参数，故改由 `ChainView` 按错误报文里的 `param` 名反查代理集合——命中即改挂 `ch`。命不中照旧落 `__setup`。

---

## 6. 格式与接口变更清单

### 6.1 `geo/`

| 文件 | 变更 |
|---|---|
| `include/cuav_geo/propagation.h`（新） | `PropagationLevel` / `PrimaryModel` / `EnvClass` / `GroundType` 四个枚举；`PropagationConfig`；`PropagationTerms`；`INormalSource`；`ground_params()`、`two_ray_correction_dB()`、`urban_log_distance_dB()`、`atmospheric_loss_dB()`、`rain_attenuation_dB()`、`shadow_sigma_dB()`、`ShadowSequence` |
| `src/propagation.cpp`（新） | 上述实现，零第三方依赖 |
| `include/cuav_geo/link_budget.h` | `LinkGeometry` 加 `tx_height_m` / `rx_height_m`；`link_geometry()` 加 `terrain_height_m` 默认实参；`LinkBudget` 加 `terms`（`PropagationTerms`）、`degraded`；`link_budget()` 加 `const PropagationConfig&` 默认实参 |
| `include/cuav_geo/scenario.h` | `LinkFrameSource::build()` 加 `const PropagationConfig&` 默认实参；新增 `init_shadow(INormalSource&, double duration_s, std::string&)`；`Frame` 加 `free_space_dB / extra_loss_dB / included_loss_terms / degraded` |
| `CMakeLists.txt` | 加 `src/propagation.cpp` |

### 6.2 `engine/`

| 文件 | 变更 |
|---|---|
| `include/cuav/observer.h` | `LinkFrame` 加 `free_space_dB / extra_loss_dB / included_loss_terms`（`std::vector<std::string>`） |
| `include/cuav/components/scenario.h` | `ScenarioSource` 加 `geo::PropagationConfig prop_;` |
| `src/scenario.cpp` | `describe()` 加 15 个 `ParamSpec`；`configure()` 解析并做两道闸（E3 拒、阴影双计拒）；`init()` 为每条链路派生子流并 `init_shadow()`；`process()` 把三个新字段填进 `LinkFrame` |
| `tools/cuav_run/runner.cpp` | `link` 事件与 `links.jsonl` 各加三个键 |

`SceneParamFrame` **零变更**；端口类型、错误码、组件数量**零变更**。

### 6.3 `web/`

| 文件 | 变更 |
|---|---|
| `src/chain/model.ts` | `SlotDef.proxy`；`ch` 槽位声明代理集合；`PROXY_PARAMS` 常量与 `proxyOf()` |
| `src/chain/compile.ts` | `scn` 节点参数合并代理键；`ch` 节点参数剔除代理键；`parseChain` 从 `scn` 读回并做一致性核对 |
| `src/chain/plan.ts` | 第 11 项检查 `propagation` |
| `src/chain/SlotCard.tsx` | 效应清单一行 + `data-slot-effects` |
| `src/chain/ChainView.tsx` | `SlotPanel` 的「传播效应」分组与显隐规则；代理参数错误反查 |
| `src/chain/effects.ts`（新） | 由配置派生 `included_loss_terms` 与中文清单的纯函数（与 C++ 侧同一张表，单测钉住） |

### 6.4 文档

| 文件 | 变更 |
|---|---|
| `docs/scenario-format.md` §7 | 「首期 `path_loss_dB` = 自由空间路损」改写为分档说明；`included_loss_terms` 语义 |
| `docs/component-catalog.md` | `ScenarioSource` 参数表 15 行 |
| `docs/display-products.md` | `links.jsonl` 三个新键 |
| `docs/api-versions.md` §4 | `link` 事件三个新键 |
| `models/channel/README.md`（新） | 模型卡：五个效应各自的公式、常数、来源、适用范围、锚点、**与 04 §7.3 的落差对照** |
| CLAUDE.md | D-058；「已定」表加一行；里程碑表加 R 行；文档表加 12 号报告；**改正 `ChannelPathSet`「首期即启用」的表述** |
| `06.首期实施备忘录_v1.0.md` | §9I（R 线步骤表）；§9E 切片表加「切片 ⑦」 |

**`ChannelPathSet` 的改正**（§0 第 12 条）。CLAUDE.md 三处称它「首期即启用」（`:72` 的工程约定段、D-013、首期边界段），核实结果是：它只是 `PortType` 枚举的一个值，**零生产者、零消费者**；`PortData`（`engine/include/cuav/component.h:164-189`）里有 `iq / detections / spectra / scenes / bearings / toas / positions` 七个容器，**没有 `paths`**；也没有对应的结构体定义。08 报告 §15 自己写的是「无（首期不实现），仅占类型位」。改成：**`ChannelPathSet` 首期只做命名与类型位预留，不实现；D-013「首期即启用」的原意是「接口从第一天就留好，P3 接多径时不改端口表」，实现留 P3。**

---

## 7. 实施步骤（06 §9I，R 线）

| 步 | 目标 | 交付物 | 验收 |
|---|---|---|---|
| R-0 | 方案与决策 | 本报告；CLAUDE.md D-058 与四处回填；06 §9I 与 §9E；WORKLOG。顺带改正 `ChannelPathSet` 的表述 | 文档互引一致；三条拍板各有结论 |
| R-1 | 组合框架与档位 | `geo/propagation.{h,cpp}` 的 `PropagationConfig` / `PropagationTerms` / 组合裁决；`link_budget()` 接配置；`ScenarioSource` 加 15 个参数与两道闸；`included_loss_terms` 进链路报告 | **E1 档逐数值等于今天**；六套 e2e、产品与航迹基准零变化；组件目录基准只多 15 个参数 |
| R-2 | 大气与降雨（移植） | 从 emcore 移植 `atmospheric_loss_dB` / `rain_attenuation_dB`；`tests/golden/propagation.json` | 64 例 golden rel ≤ 1e-9；**保留 emcore 原常数与原分段**（D-009） |
| R-3 | 地面双径 | EM-P-02：镜像法几何、Fresnel 反射系数、粗糙度、部分相干叠加、限幅、断点距离、fade 五档 | 解析锚点 A1–A5 全过；`σ_h → ∞` 时退化为 FSPL；`h ≤ 0` 时降级不静默 |
| R-4 | 统计阴影 | EM-P-08：σ 由环境 × 视距决定、空间相关、沿航迹递推、`init()` 里一次算完；`INormalSource` 抽象 | 锚点 C1–C5 全过；固定种子逐位复现；`frame(k)` 仍是 `const` 且乱序调用结果相同 |
| R-5 | 城市经验 | EM-P-05 的 E1 档 log-distance；环境类别表；`urban_loss_mode` 与闸二 | 锚点 B1–B4 全过；`open` 档与自由空间逐值相等（rel ≤ 1e-9）；闸二报错有测试钉住 |
| R-6 | 界面 | 槽位表 `proxy` 机制、`ch` 卡片效应清单、右栏档位与逐项开关、E3 置灰、`propagation` 检查项 | 往返逐字节（含 `DEFAULT_CHAIN_TEXT` 与两份 3×3 夹具）；`slice4` / `slice6` 加断言 |
| R-7 | 收口 | 模型卡 `models/channel/README.md`；四份 docs 与 04 §7.3 落差对照；06 / CLAUDE 回填 | 全量回归；文档与 `--catalog` 逐项核实 |

**R-1 的收口条件是「什么都没变」**：缺省 E1 档下，`DEFAULT_CHAIN_TEXT`、两份 3×3 夹具、产品黄金基准、`demo-01` / `demo-03` 航迹基准、六套 e2e 的硬断言（slice2 的 0.074 dB、slice6 的 ≤ 0.013 dB）必须一个数字不动。这一步不通过就不进 R-2。

R-2 / R-3 / R-4 / R-5 互不依赖，可任意顺序；R-6 依赖 R-1；R-7 依赖全部。

### 7.1 切片 ⑦（06 §9E）

| 切片 | 步骤 | 切完在浏览器看到 | e2e 断言 |
|---|---|---|---|
| ⑦ 选传播环境 | R-0…R-7 | 传播信道卡片上写着这一档包含哪几项效应；右栏切到 E2 + 城市经验(urban)，瀑布上的电平整体下降十几 dB；切到地面双径，功率随距离起伏而不是单调下降；勾统计阴影，同一条航迹上的电平有缓慢的随机起伏；E3 选项置灰写「待 D3」 | E1 档下 `DEFAULT_CHAIN_TEXT` 与两份夹具往返逐字节；`open` 档城市经验与 FSPL rel ≤ 1e-9；64 例天气 golden rel ≤ 1e-9；双径 A1–A5、阴影 C1–C5；E3 经 `--validate` 报错且落到 `ch` 卡片；`FreeSpaceChannel` + E2 被 `propagation` 检查拦住 |

---

## 8. 风险与坑

1. **缺省档不得改变任何既有数字**。这是整条线的主要防线，R-1 单独收口（§7）。
2. **往返逐字节**。代理参数机制会动 `compile` / `parseChain` 的划分，照 D-054 的做法——先立「夹具逐字节相同」再动别的。
3. **`geo/` 零第三方依赖**。新模块只用标准库；随机源走 `INormalSource` 抽象接口，不复制第二份发生器（引擎已有 xoshiro256++，两份实现迟早分叉）。
4. **`frame(k)` 的无副作用不变量**。阴影是有状态的递推，实现成「递推在 `frame()` 里」会当场破坏「可任意乱序、任意次数调用，结果逐位相同」——而 `process()` 里对同一个 `k` 确实会调用两次（出帧一次、上报一次）。必须在 `init()` 里一次算完。
5. **本期不做遮挡 ⇒ `line_of_sight` 仍恒真**。测向误差预算里的非视距多径项（缺省 5.0°）与阴影 σ 表的 NLOS 列**继续不生效**。这不是本期引入的，但选了「城市」环境却仍报视距会显得自相矛盾——模型卡与界面读数须说清「视距判定待 D3」，不能让人以为城市经验模型顺带做了遮挡判定。
6. **物理自洽的边界**：`FreeSpaceChannel`（定参）不吃场景帧。**2026-09-10 由 D-059 修订**——该变体已从框图页撤掉（那里能跑的配置一定有场景，它永远是错的选择），风险从源头消除；自由画布上仍可搭出来，属高级用法自负其责（§4.5）。
7. **04 §7.3 的六项本期仍差两项**：统计多径（要抽头延迟线与 `ChannelPathSet`，那是 P3）与 LOS/NLOS 场景模板（要 D3）。模型卡要如实列出落差，不能让档位名字暗示「精细档 = 全都算了」。
8. **参数来源多为 `assumed`**。`env_class` 的 `n` / `X_env`、σ 表、`d_corr` 都是工程取值，只有材质表来自本项目自有文档、天气两式来自 emcore（其上游是 em-demo 的简化 ITU-R）。全部标「原型阶段验证值」（D-028），甲方数据到货后按同一脚本重估。D-019 的粗档结论**不能拿来给 `urban` 行背书**（§3.3）。

---

## 9. 验证与验收

```bash
cd "/Users/zhiyu/CC/804 C-UAV"
cmake --build engine/build -j && ctest --test-dir engine/build --output-on-failure
(cd server && npm test) && (cd web && npm test)
for t in scene slice1 slice2 slice3 slice4 slice6; do node tests/e2e/$t-smoke.mjs; done
bash scripts/build-all.sh
git diff --stat data/scene tests/golden/scenario-track-*.json tests/regression   # 期望：无改动
```

| # | 判据 | 线 |
|---|---|---|
| 1 | E1 档下 `DEFAULT_CHAIN_TEXT`、`chain-3x3-{aoa,tdoa}.json` 编译往返**逐字节**相同 | 逐字节 |
| 2 | 产品黄金基准、`demo-01` / `demo-03` 航迹黄金基准零变化 | 逐字节 |
| 3 | `slice2-smoke` 两时刻带内功率差与几何路损差偏差（今天 0.074 dB） | ≤ 0.5 dB 且**数值不变** |
| 4 | `slice6-smoke` 每站 S1 总功率对三条链路预算和的偏差（今天 0.002 / −0.013 / 0.001 dB） | ≤ 0.05 dB 且**数值不变** |
| 5 | 天气两式对 emcore golden | rel ≤ 1e-9，64 例 |
| 6 | 城市经验 `open` 档对 `fspl_dB` | rel ≤ 1e-9 |
| 7 | 双径 A1–A5 | 见 §3.2 |
| 8 | 阴影 C1–C5 | 见 §3.4 |
| 9 | 闸二（城市经验带裕度 + 统计阴影）与 E3 | `configure()` 报错，报文含处置建议 |
| 10 | 组件目录黄金基准 | 差异只有 `ScenarioSource` 新增的 15 个参数 |

---

## 附录 A. 与 04 §7.3 的落差对照（模型卡须原样收录）

04 §7.3 列的传播模型简化策略六项，本期到位情况：

| 04 §7.3 的项 | 本期 | 落点 |
|---|---|---|
| 自由空间路损 | ✅ | EM-P-01，E1 |
| 大气与降雨衰减 | ✅ | EM-P-07，E2，移植 emcore（2.4405 GHz / 20 km：大气 0.15 dB + 25 mm/h 降雨 0.22 dB） |
| 地面双径 / 多径起伏 | ✅ 单次地面反射 | EM-P-02，E2；**抽头延迟线式统计多径不做**（要 `ChannelPathSet`，P3） |
| 建筑遮挡与绕射附加损耗 | ❌ | EM-P-04，D3（切片 ⑤） |
| 阴影衰落 | ✅ | EM-P-08，E2；**σ 的 NLOS 列本期取不到**（视距恒真） |
| LOS / NLOS 场景模板 | ❌ | 要 D3 的几何视距判定 |

**档位名字不暗示「精细档 = 全都算了」**：本期最高只到 E2，且 E2 内部也有上表的两处空缺。

## 附录 B. 依据条款索引

| 出处 | 条款 | 用在 |
|---|---|---|
| 01 | §3、§3.5、§8.3、§9.2 | 档位用 E1–E4；概念模型的参数供给角色；跨层一致性 |
| 04 | §5.1、§7.3、§7.4、§9.2、§12.2、§15.2、§16.3 | 端到端对象；传播简化策略（附录 A）；溯源；慢变参数在采样率上施加 |
| 05 | §5.2、§6.2.3、§25 | `SceneParamFrame` / `ChannelPathSet`；四态降级；几何 / 材质 / 无线参数分管 |
| EM-P-01 | 全 | §3.1 |
| EM-P-02 | §10.2、§10.4、§10.6、§10.7、§10.8 | §3.2 |
| EM-P-05 | §10.2、§10.6、§10.18 | §3.3 |
| EM-P-07 | 全 | §3.5 |
| EM-P-08 | §10.3、§10.4、§10.6、§10.9 | §3.4 |
| EM-P-13 | §10.8、§10.9、§10.10 | §2.2、§2.3 |
| 决策 | D-005、D-009、D-013、D-019、D-028、D-033、D-037、D-049、D-051、D-053、D-054、D-055 | 全篇 |
| 铁律 | 1、2、5、8、9、10、11、14、15 | 全篇 |
