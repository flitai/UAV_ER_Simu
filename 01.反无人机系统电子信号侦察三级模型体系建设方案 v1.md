# 反无人机系统电子信号侦察三级模型体系建设方案

## 1. 方案概述

### 1.1 建设背景

城市低空反无人机系统需要在复杂电磁环境中完成无人机遥控、图传、数传、中继以及其他相关辐射源的发现、识别、测向、定位、跟踪和态势支撑。实际任务同时具有大范围、长时间、多站点、多目标和强背景干扰等特点，也存在重点信号算法验证、多站高精度定位、装备性能复现和外场试验复盘等高保真需求。

单一精度的模型难以同时满足上述任务：仅采用效果统计模型，无法解释信号为什么被发现或漏检；全部采用复基带采样模型，又会造成计算量、数据量和参数需求过高，难以支撑城市级大规模推演。因此，电子信号侦察仿真应建设为功能级、信号级和复基带采样级三个相互衔接的模型层次。复基带采样级即同相/正交双通道采样级，后文按照行业习惯简称为 IQ 级。

### 1.2 总体结论

本方案提出将三个层次定义为反无人机系统电子信号侦察仿真的统一模型体系：

| 模型层次 | 核心问题 | 主要任务 | 建设定位 |
|---|---|---|---|
| M1 功能级模型 | 信号源在做什么，系统能否发现、识别、定位和告警 | 参数化信号描述、体系推演、效能评估、布站分析、能力边界分析 | 全域常态运行的基础层 |
| M2 信号级模型 | 接收端看到了怎样的时频功率与信号特征 | 频谱扫描、检测、特征、识别、测向定位、退化分析 | 侦察机理仿真的核心层 |
| M3 IQ 级模型 | 接收机产生了怎样的复基带采样数据 | 重点算法验证、多站同步定位、接收链路闭环、实测复盘 | 按需触发的高保真层 |

三个层次不是彼此独立的三套模型，也不是简单的低、中、高三个版本，而是建立在统一场景、统一信号语义、统一传播环境、统一接收机参数和统一验证证据之上的分层模型体系。常规任务以 M1 和 M2 为主，重点频段、重点时间窗和重点站点按需触发 M3。

## 2. 需求分析与建设必要性

### 2.1 反无人机系统的实际仿真需求

电子信号侦察模型需要支撑以下任务：

1. 评估侦察系统对不同类型、不同工作状态和不同飞行阶段无人机信号的发现能力。
2. 评估城市建筑遮挡、街谷多径、噪声抬升、同频干扰和接收机阻塞对侦察效果的影响。
3. 复现频谱扫描、候选发现、特征提取、信号识别、未知聚类、复扫和告警过程。
4. 评估单站测向以及 AOA、TDOA、FDOA 等多站定位能力和布站几何条件。
5. 支撑重点信号处理算法测试、参数敏感性分析、边界条件验证和算法对比。
6. 支撑外场频谱、测向定位结果和实测 IQ 数据的回放、校准与复盘。
7. 在参数不完整、协议敏感或设备信息受限时，仍能给出可解释、可降级的工程评估结果。

### 2.2 三层模型建设的必要性

| 实际矛盾 | 仅采用单一层次的不足 | 三级体系的解决方式 |
|---|---|---|
| 城市级大场景与高保真计算之间的矛盾 | 全量 IQ 无法经济地支撑长时间、多站点推演 | M1/M2 全域运行，M3 局部触发 |
| 效能结论与机理解释之间的矛盾 | 仅输出发现率或定位误差，难以解释原因 | M2 提供时频、功率、特征和退化证据 |
| 模型评估与算法验证之间的矛盾 | 抽象特征无法检验底层信号处理算法 | M3 提供可重复的接收端 IQ 输入 |
| 模板参数与真实装备差异之间的矛盾 | 理论模板无法覆盖硬件非理想和现场背景 | M3 接入实测 IQ，反向校准 M2/M1 |
| 多任务精度需求不同 | 固定精度造成资源浪费或能力不足 | 依据任务目的选择模型层次和精度等级 |

### 2.3 建设可行性判断

项目已有模型已覆盖信号抽象、传播、接收机、噪声背景、频谱扫描、能量检测、特征检测、信号识别、单站测向、AOA/TDOA/FDOA 定位、运动估计、目标关联和侦测退化等环节。三级体系是在已有模型链上增加统一层次定义、跨层数据映射、局部 IQ 生成与验证闭环，技术路线连续，模型职责清晰，具备分阶段实施条件。

可行性边界如下：

- M1 功能级和 M2 信号级具备全域建设和常态运行的必要性与可行性。
- M3 IQ 级具备重点对象、重点频段、重点站点和重点时间窗建设的必要性与可行性。
- 在缺少授权协议、真实跳频序列、设备指纹或接收机标定参数时，M3 采用类别级模板、统计波形、脱敏特征或实测片段回放，不对未知细节作无依据复现。
- 不将“生成 IQ 文件”本身视为能力完成，而以生成数据能否支撑检测、识别、测向定位和实测一致性验证作为判断依据。

## 3. 模型体系定义

### 3.1 三个独立维度

为避免把模型层次、精度和可信度混为一谈，体系采用三个相互独立的标识维度：

| 维度 | 分级 | 含义 |
|---|---|---|
| 模型层次 M | M1 功能级、M2 信号级、M3 IQ 级 | 模型对物理对象的表达粒度 |
| 精度等级 E | E1 快速抽象、E2 工程模型、E3 精细机理、E4 实测/半实物融合 | 同一模型层次内部的计算精细程度 |
| 可信度等级 V | V1-V5 | 参数来源、验证证据、校准程度和适用范围的可信程度 |

模型层次高不等于结果必然更准确。例如，未经过设备标定的 M3-E3 理论 IQ 模型，不应被认为天然优于使用大量外场统计校准的 M2-E4 模型。每次仿真结果应同时声明 M、E、V 三个维度以及适用范围。

### 3.2 三层总体架构

![反无人机系统电子信号侦察三层模型总体架构](三层总体架构图.png)

图中体现了任务驱动、按需升阶和统一底座支撑，虚线表示统计归约与校准回灌。M1、M2、M3 在统一任务想定、环境、参数和证据底座上运行，并通过理论算例、跨层一致性、半实物验证和外场数据形成可信度闭环。

### 3.3 统一因果链

三级模型共享以下物理和任务因果链：

$$
\text{任务状态}
\rightarrow
\text{辐射源信号}
\rightarrow
\text{传播与环境作用}
\rightarrow
\text{接收机观测}
\rightarrow
\text{检测与特征}
\rightarrow
\text{识别与定位}
\rightarrow
\text{告警与任务效能}
$$

不同层次的区别仅在于因果链中间过程的展开程度。M1 以载频、带宽、功率、占空比、信号角色等参数化描述，以及概率、状态和误差分布表达中间过程；M2 显式表达信号的时频功率结构、动态过程与可观测特征；M3 显式生成或接入接收端复基带采样。

### 3.4 与既有能力粒度分级的关系

既有说明中的描述级、时频级、特征级、IQ 级和半实物级，描述的是信号数据及处理能力的细化粒度；本方案中的 M1、M2、M3 描述的是面向反无人机任务的模型体系层次。两者关注维度不同，其映射关系如下：

| 既有能力粒度 | 归属模型层次 | 关系说明 |
|---|---|---|
| 描述级 | M1-E1/E2 | 以载频、带宽、功率、占空比、信号角色和工作状态描述辐射活动，不展开时频形成过程 |
| 时频级 | M2-E2/E3 | 形成时间—频率—功率占用、突发、跳频和扫频轨迹 |
| 特征级 | M2-E2/E3 | 形成检测、识别、未知聚类和测向定位所需的结构化特征 |
| IQ 级 | M3-E2/E3 | 生成或接入接收端复基带采样，验证信号处理与测量过程 |
| 半实物级 | M3-E4 | 接入实测 IQ、实际设备或标定数据，并反向校准 M2 和 M1 |

描述级所使用的信号参数仍由统一信号描述模型管理，但其基础模型归属不决定仿真层次归属：当这些参数仅用于表示辐射源身份、活动状态和效能裁决时，属于 M1；当参数被展开为时频包络、谱形、跳频轨迹和特征序列时，进入 M2。

从广义通信与信号处理术语看，IQ 模拟也可被视为信号级模拟的一种高保真形式；本项目将其单列为 M3，是为了明确逐采样数据与结构化时频特征在计算代价、参数要求、验证用途和适用范围上的显著差异。这是一种面向工程建设与资源管理的层次划分，不否定 IQ 数据的信号属性。

### 3.5 时频参数与时频结构的层次边界

“包含时间和频率信息”不能直接作为信号级模型的判据。M1 同样需要时间窗口和频率范围，以判断信号是否活动、扫描是否覆盖以及任务功能能否实现；M2 的本质是进一步展开信号在时间—频率—功率空间中的内部结构及动态变化。

| 表达内容 | 归属层次 | 判定依据 |
|---|---|---|
| 中心频率、起止频率、带宽 | M1 功能级 | 只定义信号活动的频率边界 |
| 发射起止时间、活动窗口、占空比 | M1 功能级 | 只定义信号何时存在及活动比例 |
| 频段占用摘要、发射/静默状态 | M1 功能级 | 用于覆盖、扫描命中和效能裁决，不展开内部结构 |
| 时间—频率—功率栅格 $S(f,t)$ | M2 信号级 | 显式表达各时频单元的功率分布 |
| 突发包络、谱形、跳频轨迹、扫频轨迹 | M2 信号级 | 显式表达信号内部时频动态过程 |
| 时频纹理、谱峰、瞬时带宽和带外特征 | M2 信号级 | 能够直接支撑检测、识别、聚类和信号分析 |
| 由复采样计算的高分辨率时频图 | M3 生成、M2 归约使用 | 原始复基带采样属于 M3，时频观测产品属于 M2 |

本模型体系采用以下分层原则：

M1 可以描述时间范围和频率范围，但不生成信号内部的时频结构；M2 显式模拟时间—频率—功率分布及其动态变化；M3 生成或接入形成这些时频观测的复基带 IQ 采样。

因此，既有能力中的“时频级”归入 M2 信号级；M1 中出现的频率和时间信息属于参数化活动描述，不称为时频级模拟。

## 4. 总体建设目标

### 4.1 能力目标

建设一个能够覆盖“全域推演—机理分析—重点验证—实测校准”的电子信号侦察模型体系，实现：

- 全域、多站、多目标、长时间任务的快速推演；
- 城市复杂电磁环境下的检测、识别、测向定位机理表达；
- 重点信号与重点算法的 IQ 级闭环验证；
- 三层模型结果一致、参数同源、误差可解释；
- 模型可按任务目的、资源预算和数据条件自动或人工选择；
- 仿真结论可追溯到输入参数、模型版本、精度等级和验证证据。

### 4.2 建设原则

1. **任务牵引。** 模型精度服从评估问题，不以数据量或波形细节多少代替任务有效性。
2. **统一语义。** 三层共用信号对象、场景时间、空间坐标、频率基准、功率口径和设备状态。
3. **分层解耦。** 每层具有明确输入输出和适用范围，上层不重复下层机理，下层不越权给出任务结论。
4. **按需升阶。** 只有当 M1/M2 无法回答关键问题，或任务明确需要底层采样时，才触发 M3。
5. **双向校准。** 高层为低层提供任务边界，低层为高层提供参数和统计校准。
6. **可信度显式。** 缺少数据、发生外推或模型降级时必须降低可信度并说明原因。
7. **实测闭环。** 理论算例保证正确性，外场与半实物数据保证工程代表性。

## 5. M1 功能级模型建设

### 5.1 模型定位

M1 面向体系任务和装备效能，回答“辐射源在什么频段、以什么功率和时间方式活动，以及在给定部署、环境和装备能力条件下，能否完成发现、识别、定位、跟踪和告警”。该层采用载频、带宽、功率、占空比、信号角色和工作状态等参数描述辐射活动，可以包含时间窗口和频率范围，但不形成 $S(f,t)$、时频栅格、谱形或动态轨迹，也不生成原始采样；侦察链路主要采用概率模型、状态机、统计误差模型和事件关系表达。

### 5.2 建设内容

| 模型组 | 建设内容 | 主要输出 |
|---|---|---|
| 参数化信号描述模型 | 载频、频率范围、带宽、发射功率、占空比、信号角色、工作状态和活动窗口 | 信号描述符、辐射事件、频段占用摘要 |
| 任务活动模型 | 无人机、操控站及相关辐射源的开机、待机、发射、突发、静默和模式切换 | 辐射事件、活动窗口、任务状态 |
| 覆盖与可观测模型 | 频段覆盖、扫描命中、传播可达、接收机可用 | 可观测状态、覆盖概率、盲区原因 |
| 发现模型 | 单次与多次扫描发现概率、虚警、检测延迟 | 检出状态、发现概率、首次发现时间 |
| 识别模型 | 类别识别、型号候选、未知拒识和混淆概率 | 类别概率、识别置信度、未知状态 |
| 测向定位效能模型 | 测向误差、定位误差、站点几何和可定位性 | 方位、位置、误差椭圆、定位状态 |
| 告警与效能模型 | 告警形成、确认、保持、消失和任务链路完成度 | 告警事件、任务成功率、能力短板 |

### 5.3 主要原理

若一个目标在任务窗口内经历 $K$ 次有效观测，各次条件检出概率为 $p_k$，在条件独立近似下，窗口累计发现概率为：

$$
P_{\mathrm{det,win}}
=
1-\prod_{k=1}^{K}(1-p_k)
$$

当各次观测相关时，不直接使用独立乘积，而应引入时间相关状态或基于实测统计形成累计发现曲线。首次发现时间的期望可由离散观测窗口表示为：

$$
E[T_{\mathrm{det}}]
=
\sum_{k=1}^{K} t_k\,p_k\prod_{m=1}^{k-1}(1-p_m)
$$

功能级定位误差不只给出单一数值，而以位置协方差 $\mathbf{P}_{x}$ 或误差椭圆表达。对于二维位置估计，可由协方差特征值 $\lambda_1,\lambda_2$ 给出置信椭圆半轴：

$$
a=\sqrt{\chi^2_{2,\alpha}\lambda_1},
\qquad
b=\sqrt{\chi^2_{2,\alpha}\lambda_2}
$$

### 5.4 适用范围与边界

M1 适用于城市级大样本推演、设备配置对比、布站优化、任务链路评估和统计验收。其参数化信号描述用于驱动频段覆盖、传播可达、扫描命中和效能裁决；即使其中包含发射时间和工作频段，也不等同于时频结构模拟。其准确性依赖 M2/M3 或实测数据提供的检出曲线、识别混淆关系、误差分布和退化参数。M1 不用于评价具体频谱检测、调制识别、同步估计或相关算法的底层性能。

## 6. M2 信号级模型建设

### 6.1 模型定位

M2 是三级体系的核心层，面向信号在时间、频率、功率、调制特征和空间传播上的可观测机理，回答“侦察设备为什么能够发现、识别或定位该信号，以及环境和设备条件如何改变结果”。M2 的最低进入条件是显式形成时频功率结构、谱形、突发/跳频/扫频轨迹或可用于信号分析的结构化特征，而不是仅具有时间和频率参数。

本方案中的“信号级”是面向工程体系仿真的结构化信号与观测级模型。它可表达高分辨率时频细节和统计特征，但常规运行不要求逐采样点生成复基带序列；逐采样点的复基带表达归入 M3。

### 6.2 建设内容

| 模型组 | 关联专业模型 | 建设重点 |
|---|---|---|
| 信号时频展开 | 统一信号描述模型、频谱栅格模型 | 由 M1 参数描述展开谱形、时频功率包络、突发轨迹、跳频/扫频过程和频谱栅格 |
| 传播与环境 | 分级传播模型、噪声与背景电磁环境模型 | 路径损耗、遮挡、阴影衰落、多径上下文、噪声底和背景辐射 |
| 接收机 | 接收机基础与约束模型 | 带宽、噪声系数、选择性、动态范围、阻塞、饱和和门限 |
| 扫描与检测 | 频谱扫描策略模型、能量检测模型 | 扫描窗口、驻留时间、分辨率带宽、门限、检出概率和虚警 |
| 特征与识别 | 信号特征检测模型、信号识别模型 | 时频纹理、带宽、占空比、突发节律、跳频/扫频特征、识别概率和未知拒识 |
| 测向与定位 | 单站测向及 AOA、TDOA、FDOA 多站定位模型 | 单站方位、AOA/TDOA/FDOA 约束、协方差、几何质量和多假设 |
| 退化与关联 | 信号源运动估计、目标关联与侦测退化模型 | 运动估计、同源关联、强弱遮蔽、NLOS、多源混叠和侦测退化 |

### 6.3 信号与接收功率表达

信号在给定时间窗内可由功率谱密度和时间活动函数共同描述：

$$
S_q(f,t)
=
P_{q,\mathrm{tx}}\,G_q(f,t)\,A_q(t)\,\Psi_q(f,t)
$$

其中，$P_{q,\mathrm{tx}}$ 为发射功率，$G_q$ 为发射天线和指向增益，$A_q(t)$ 为发射活动函数，$\Psi_q(f,t)$ 为归一化谱形。

多个信号经过传播后，在第 $r$ 个侦察站形成的接收功率谱可表示为：

$$
S_r(f,t)
=
\sum_{q=1}^{Q}
S_q(f,t)\,|H_{qr}(f,t)|^2
+S_{\mathrm{bg},r}(f,t)
$$

其中，$H_{qr}(f,t)$ 为发射源到接收站的传播与天线综合响应，$S_{\mathrm{bg},r}$ 为背景噪声和环境辐射功率谱。

检测带宽 $\mathcal{B}$ 内的信干噪比为：

$$
\mathrm{SINR}
=
\frac{\int_{\mathcal{B}}S_{\mathrm{sig}}(f,t)\,df}
{\int_{\mathcal{B}}\left[S_{\mathrm{int}}(f,t)+S_{\mathrm{noise}}(f,t)\right]df}
$$

### 6.4 扫描与能量检测

信号活动窗口 $\mathcal{T}_{s}$ 与扫描驻留窗口 $\mathcal{T}_{d}$ 的重叠决定有效观测时间：

$$
T_{\mathrm{obs}}
=
\mu\left(\mathcal{T}_{s}\cap\mathcal{T}_{d}\right)
$$

其中，$\mu(\cdot)$ 表示时间区间长度。对短突发、跳频和低占空比信号，即使平均接收功率足够，若 $T_{\mathrm{obs}}$ 太短，也可能发生扫描漏失。

能量检测统计量可抽象为：

$$
Y
=
\frac{1}{N}\sum_{n=0}^{N-1}|x[n]|^2
$$

当 $Y>\eta$ 时形成候选信号，其中 $\eta$ 应由目标虚警概率、噪声估计误差、检测带宽和观测样本数共同确定。M2 可直接使用统计分布或校准后的 ROC 曲线，无须在每次运行中生成 $x[n]$。

### 6.5 特征与识别

候选信号的结构化特征向量可表示为：

$$
\mathbf{z}
=
[f_c,\,B,\,P,\,D,\,T_b,\,T_r,\,K_{\mathrm{tf}},\,K_{\mathrm{hop}},\ldots]^T
$$

其中，$f_c$ 为中心频率，$B$ 为带宽，$P$ 为功率或功率谱特征，$D$ 为占空比，$T_b$ 和 $T_r$ 为突发宽度及重复节律，$K_{\mathrm{tf}}$ 和 $K_{\mathrm{hop}}$ 分别表示时频纹理及跳频特征。

识别结果以类别后验概率而不是绝对标签表达：

$$
P(c_i\mid\mathbf{z})
=
\frac{p(\mathbf{z}\mid c_i)P(c_i)}
{\sum_j p(\mathbf{z}\mid c_j)P(c_j)}
$$

当最大后验概率、模板相似度或特征质量低于门限时，应输出未知或低置信度，不将所有观测强制归入已知类型。

### 6.6 测向定位

AOA 方位约束可表示为信号源位置 $\mathbf{x}$ 相对于站点位置 $\mathbf{p}_i$ 的方向观测：

$$
\hat{\theta}_i
=
\operatorname{atan2}(y-y_i,x-x_i)+v_{\theta,i}
$$

TDOA 观测满足：

$$
\Delta t_{ij}
=
\frac{\|\mathbf{x}-\mathbf{p}_i\|-\|\mathbf{x}-\mathbf{p}_j\|}{c}
+b_{ij}+v_{ij}
$$

其中，$b_{ij}$ 表示站间时钟、接收链群时延或 NLOS 偏置，$v_{ij}$ 表示随机测量误差。定位解应同时输出残差、协方差、误差椭圆、站点权重和 NLOS/多径风险，而不是只给出一个坐标点。

### 6.7 适用范围与边界

M2 适用于频谱态势、检测识别机理、城市环境退化、扫描策略、测向定位和算法上层逻辑评估。M2 不完整复现真实协议比特流、加密内容、硬件指纹和逐采样相位连续性；这些内容只有在任务确有必要且参数来源可靠时才进入 M3。

## 7. M3 IQ 级模型建设

### 7.1 模型定位

M3 面向侦察接收机的复基带采样输入，回答“在给定发射波形、传播信道、噪声干扰、接收机状态和采样条件下，算法实际接收到怎样的 I/Q 数据”。该层用于验证无法由功率、特征或概率摘要充分回答的问题。

### 7.2 建设内容

| 模型组 | 建设内容 | 主要用途 |
|---|---|---|
| 基带信号源 | 调制类别、符号/码片节律、成形、突发门控、跳频/扫频和多载波摘要 | 生成类别级或授权体制 IQ |
| 信道作用 | 复增益、时延、多径、多普勒、阴影衰落和 NLOS 偏置 | 复现城市传播对波形的影响 |
| 噪声与干扰 | 热噪声、外部噪声、同邻频信号、压制干扰和强弱遮蔽 | 检验检测与抗扰性能 |
| 接收机非理想 | 频偏、相位噪声、采样时钟偏差、滤波、AGC、动态范围、阻塞和饱和 | 接收链路闭环 |
| 采样与同步 | 采样率、量化位数、时间戳、多通道和多站同步关系 | TDOA/FDOA 与阵列处理验证 |
| 实测回放 | 实测 IQ、标定曲线、场景元数据和真值标注 | 校准、复盘与半实物验证 |

### 7.3 IQ 信号原理

发射端复基带信号可表示为：

$$
s_q(t)
=
A_q(t)\sum_k a_{q,k}g_q(t-kT_{s,q})e^{j\phi_q(t)}
$$

第 $r$ 个接收站的离散复基带序列可表示为：

$$
r_r[n]
=
\sum_{q=1}^{Q}\sum_{\ell=1}^{L_q}
\alpha_{qr\ell}
s_q[n-\tau_{qr\ell}]
e^{j2\pi\nu_{qr\ell}nT_s}
+i_r[n]+w_r[n]
$$

其中，$\alpha_{qr\ell}$、$\tau_{qr\ell}$ 和 $\nu_{qr\ell}$ 分别为路径复增益、时延和多普勒/频偏，$i_r[n]$ 为干扰及其他辐射源，$w_r[n]\sim\mathcal{CN}(0,\sigma_r^2)$ 为复高斯噪声。

考虑接收机滤波、增益与量化后：

$$
r_{Q,r}[n]
=
Q_b\left\{G_r[n]\left(h_r[n]*r_r[n]\right)e^{j\phi_{\mathrm{pn},r}[n]}\right\}
$$

其中，$Q_b\{\cdot\}$ 为 $b$ 位量化，$G_r[n]$ 为接收增益或 AGC 状态，$h_r[n]$ 为接收通道响应，$\phi_{\mathrm{pn},r}[n]$ 为相位噪声。

### 7.4 IQ 级模拟的必要触发条件

出现以下任一情况时，可进入 M3：

- 需要验证能量检测、时频检测、调制识别或未知拒识算法的底层性能；
- 需要分析低信噪比、同频多源、短突发、跳频、强弱遮蔽或接收机饱和条件；
- 需要由多站同步采样形成相关峰，验证 TOA/TDOA/FDOA 观测质量；
- 需要评估频偏、相位噪声、采样时钟偏差、量化和接收滤波等硬件非理想；
- 需要回放外场 IQ 数据并与仿真结果进行一致性比对；
- M1/M2 给出低置信度、争议结论或临界边界，需要更细粒度证据。

### 7.5 IQ 级模拟的数据与性能边界

IQ 数据量可估算为：

$$
D
=
2bF_sTN_{\mathrm{ch}}N_{\mathrm{rx}}
$$

其中，因子 2 表示 I、Q 两路，$b$ 为每路单样本字节数，$F_s$ 为采样率，$T$ 为持续时间，$N_{\mathrm{ch}}$ 为通道数，$N_{\mathrm{rx}}$ 为接收站数。采样率需满足有效带宽与滤波裕量：

$$
F_s\geq\kappa B_{\mathrm{eff}}
$$

因此，M3 按“任务窗口”管理，而不作为全城市、全频段、全站点、全时长的连续默认模型。每个 IQ 任务必须明确频率范围、采样率、持续时间、站点组合、信号数量、信道精度、接收机效应和验证目的。

## 8. 三层模型之间的衔接关系

### 8.1 向下展开

M1 发现概率降低时，可将对应时间窗、频段、站点、目标活动和主要退化原因交给 M2，分析是扫描未命中、传播损耗、背景噪声、同频遮蔽还是接收机受限。若 M2 仍无法解释算法差异或相关峰质量，则进一步选取局部窗口进入 M3。

### 8.2 向上归约

复基带采样级模型输出 IQ 后，通过检测、特征和测量过程归约为信号级模型的时频片段、特征向量、SNR/SINR、TOA、频差和方位质量；信号级模型再通过统计试验和误差建模归约为功能级模型的检出概率、识别混淆矩阵、测向误差分布和定位成功率。

![三层模型向上归约关系](三层模型向上归约图.png)

向上归约不是简单删除数据，而是在统一场景、参数版本和统计口径下，将高粒度采样结果转换为可解释的信号观测证据，再形成具有统计代表性的任务效能参数。关键边界、低置信度或争议结论可反向触发局部复基带采样级复核。

### 8.3 跨层一致性约束

同一场景、同一随机条件和同一参数版本下，M3 归约到 M2、M2 归约到 M1 的统计量应在预定容差内一致。对指标 $J$，可定义跨层相对偏差：

$$
\delta_{ab}(J)
=
\frac{|J_{M_a}-J_{M_b}|}
{\max(|J_{M_b}|,\varepsilon)}
$$

当 $\delta_{ab}(J)$ 超过容差时，应追查抽象误差、参数不一致、随机样本不足或模型适用范围差异，而不是直接选择更有利的结果。

## 9. 模型组成与职责划分

### 9.1 三级模型目录

| 业务环节 | M1 功能级 | M2 信号级 | M3 IQ 级 |
|---|---|---|---|
| 信号源 | 载频、带宽、功率、占空比、信号角色、辐射状态与活动概率 | 谱形、时频功率、突发/跳频轨迹和特征 | 复基带波形或实测片段 |
| 传播 | 覆盖概率、损耗等级 | 路径损耗、遮挡、衰落、多径上下文 | 复信道增益、时延、多普勒 |
| 接收机 | 可用/退化/失效状态 | 灵敏度、带宽、门限、阻塞、饱和 | 滤波、增益、频偏、相噪、量化 |
| 扫描 | 命中概率与扫描周期 | 时间频率窗口、驻留和分辨率 | 通道化或采样窗口 |
| 检测 | $P_d$、$P_{fa}$、检测延迟 | 能量/时频统计与候选片段 | 由 IQ 执行检测形成统计量 |
| 识别 | 识别率、混淆率、未知率 | 特征向量、类别概率、未知聚类 | 从 IQ 提取特征并验证算法 |
| 测向 | 方位误差分布 | 方法相关方位、误差和偏置 | 多通道相位/幅度观测 |
| 定位 | 定位成功率与误差分布 | AOA/TDOA/FDOA 约束及协方差 | 多站同步 IQ 和相关/频差观测 |
| 告警 | 告警状态与任务效能 | 证据链与置信度 | 为争议事件提供底层证据 |

### 9.2 与已有模型的继承关系

已有统一信号描述、频谱栅格、接收机约束、背景电磁环境、分级传播以及信号侦察处理链等模型继续作为专业模型单元。三级体系不改变这些模型的专业职责，而是为每个模型增加适用层次、输入粒度、输出归约关系和跨层验证要求。

其中：

- 统一信号描述模型为三个层次提供信号身份、参数来源、模板版本和工作状态；
- 分级传播模型为三个层次提供一致的传播环境，并按层次输出覆盖等级、功率损耗或复信道参数；
- 接收机约束模型和背景电磁环境模型分别提供设备能力边界、接收状态、噪声底和背景辐射；
- 频谱扫描、能量检测、特征提取和信号识别模型构成侦察处理主链；
- 单站测向以及 AOA、TDOA、FDOA 多站定位模型构成测向定位主链；
- 信号源运动估计、目标关联和侦测退化模型提供时序状态、同源关联和环境退化解释。

## 10. 保真度选择与运行策略

### 10.1 选择准则

模型层次由任务目的、结果指标、场景规模、参数条件、计算预算和可信度要求共同决定。可定义保真度选择评分：

$$
S(M_k)
=
w_1R_k+w_2A_k+w_3D_k+w_4C_k-w_5K_k
$$

其中，$R_k$ 为任务相关性，$A_k$ 为预期准确性收益，$D_k$ 为数据可得性，$C_k$ 为验证证据充分度，$K_k$ 为计算与数据代价。该评分用于辅助选择，不代替任务专家最终判断。

### 10.2 推荐运行模式

| 运行模式 | 默认层次 | 升阶条件 | 典型用途 |
|---|---|---|---|
| 全域推演模式 | M1 | 对关键区域调用 M2 | 城市级任务效能、部署对比 |
| 工程分析模式 | M1+M2 | 临界或争议窗口调用 M3 | 检测识别、频谱态势、退化分析 |
| 算法验证模式 | M2+M3 | 接入实测或半实物数据 | 检测、识别、同步和定位算法验证 |
| 外场复盘模式 | M1+M2+M3 | 以实测证据决定精度 | 事件复盘、偏差解释、参数校准 |
| 参数敏感性模式 | M1/M2 | 少量代表点使用 M3 校核 | 能力边界和关键参数识别 |

### 10.3 空间、时间和频率裁剪

M3 的触发单元由“站点集合 + 空间区域 + 时间窗口 + 频率窗口 + 信号集合 + 验证指标”共同定义。裁剪策略包括：

- 空间上只选择关键站点、遮挡边界、疑似 NLOS 站点和定位几何敏感区域；
- 时间上只选择首次发现前后、突发活动、识别冲突、定位跳变或受扰窗口；
- 频率上只选择目标占用带宽及必要保护带；
- 对象上只选择影响结论的目标信号、主要干扰源和背景分量；
- 精度上只启用与验证指标存在因果关系的接收机或信道非理想因素。

## 11. 参数与数据体系建设

### 11.1 参数分类

| 参数域 | 主要内容 | 主要来源 |
|---|---|---|
| 场景参数 | 地形、建筑、材质、站点、目标轨迹和任务时间 | 项目场景库、地理数据、想定 |
| 信号参数 | 频率、带宽、功率、谱形、占空比、调制和活动模式 | 公开资料、授权数据、实测归纳、工程模板 |
| 传播参数 | 路径损耗、遮挡、阴影衰落、多径和大气参数 | 理论模型、城市数据、外场校准 |
| 接收机参数 | 灵敏度、噪声系数、带宽、动态范围、阻塞和同步能力 | 设备资料、标定试验、工程估计 |
| 侦察算法参数 | 扫描、检测门限、特征模板、识别曲线和定位误差 | 算法试验、半实物测试、外场统计 |
| IQ 任务参数 | 采样率、量化位数、通道数、时钟、滤波和非理想项 | 设备能力、验证任务、实测元数据 |

### 11.2 参数治理要求

每项关键参数应具有单位、参考点、适用频段、适用设备、有效时间、来源、版本、置信区间、权限和校准状态。功率参数需明确是发射机端口功率、EIRP、接收机输入功率、检测带宽内功率还是功率谱密度；时间参数需明确仿真时钟、采样时钟和多站同步基准。

### 11.3 数据安全与脱敏

专有协议、真实跳频序列、设备指纹、站点精确位置和外场 IQ 数据按权限管理。低权限模型使用类别模板、统计区间和脱敏标识；权限不足时应显式降低 V 等级，不以虚构参数填补敏感数据缺口。

## 12. 准确性与可信度建设

### 12.1 准确性来源

模型准确性由物理机理、参数准确性、数值正确性、统计代表性和适用范围共同决定。总体不确定性可分解为：

$$
\sigma_{\mathrm{total}}^2
=
\sigma_{\mathrm{model}}^2
+\sigma_{\mathrm{param}}^2
+\sigma_{\mathrm{env}}^2
+\sigma_{\mathrm{measurement}}^2
+\sigma_{\mathrm{sampling}}^2
$$

其中分别对应模型抽象、参数、环境、测量及有限样本引入的不确定性。对存在相关性的误差项，应使用完整协方差而不是简单方差相加。

### 12.2 可信度证据链

| 证据层 | 主要方法 | 证明内容 |
|---|---|---|
| 理论验证 | 公式推导、量纲检查、边界分析 | 机理与数学关系正确 |
| 标准算例 | 单源、自由空间、已知 SNR、已知方位和标准几何 | 模型实现与理论一致 |
| 交叉对比 | 不同精度模型、独立算法或工具对比 | 结果趋势和数量级合理 |
| 半实物验证 | 接收机、信号源或算法链路接入 | 设备与算法接口有效 |
| 外场校准 | 频谱、检测、识别、测向定位和 IQ 实测 | 工程代表性和误差分布可信 |

### 12.3 统计验证

对功能级检出率，$N$ 次独立或等效独立试验中检出 $n_d$ 次，估计值为：

$$
\hat{P}_d=\frac{n_d}{N}
$$

同时应给出置信区间而不是只报告点估计。对识别模型使用混淆矩阵、类别准确率、未知拒识率和概率校准误差；对定位模型使用 RMSE、CEP、误差椭圆覆盖率、残差分布和异常点比例；对 IQ 模型使用频谱、时域、统计、相关和算法输出的一致性指标。

### 12.4 三层校准闭环

M3 通过多次蒙特卡洛或实测回放形成 M2 的特征分布、检测曲线和测量误差；M2 在不同场景参数下批量运行，形成 M1 的任务概率和状态转移参数；外场数据同时校准三个层次。校准结果必须绑定场景类别和适用条件，不能将某一设备、某一环境的参数无条件推广到全部场景。

## 13. 验证与验收方案

### 13.1 分层验收重点

| 层次 | 必验内容 | 主要指标 |
|---|---|---|
| M1 功能级 | 参数化信号描述、任务链路、概率统计、状态转移、降级和批量运行 | 时间/频率边界一致性、$P_d$、$P_{fa}$、识别率、定位成功率、检测延迟、运行规模 |
| M2 信号级 | $S(f,t)$、时频功率、谱形与动态轨迹、扫描检测、特征、识别、测向定位和环境退化 | 时频结构一致性、频率/带宽/功率误差、ROC、混淆矩阵、方位误差、定位误差、趋势一致性 |
| M3 IQ 级 | 波形参数、采样口径、信道、接收机非理想、多站同步和算法可用性 | EVM 或波形误差、PSD 偏差、SNR、相关峰、TOA/FDOA 误差、数据完整性 |
| 跨层 | 参数同源、统计归约、触发和降级 | 跨层偏差、重复性、追溯完整率、升降阶正确率 |

### 13.2 代表性验收场景

1. **无遮挡单源基准场景：** 验证功率预算、检测概率、特征和三层归约一致性。
2. **建筑遮挡与街谷多径场景：** 验证 NLOS、反射、多径对检出、测向和定位的影响。
3. **短突发与低占空比场景：** 验证扫描窗口命中、检测延迟和漏扫概率。
4. **同频多源与强弱遮蔽场景：** 验证候选分离、识别混淆、接收机阻塞和 IQ 级算法差异。
5. **跳频/扫频场景：** 验证扫描策略、时频轨迹、复扫反馈和重点 IQ 窗口。
6. **多站 AOA/TDOA/FDOA 场景：** 验证站点几何、同步误差、相关峰质量和误差椭圆覆盖率。
7. **外场数据复盘场景：** 验证实测频谱、检测记录、定位结果和 IQ 片段的一致性。

### 13.3 接受准则制定原则

具体数值门限应依据装备指标、可获得数据和任务场景共同确定，不在缺少试验依据时预设过度精确的统一指标。验收门限至少分为理论一致性门限、工程误差门限、统计置信门限和运行性能门限，并声明适用频段、场景类别、设备状态和模型 M/E/V 等级。

## 14. 分阶段建设计划

### 14.1 第一阶段：统一体系与功能级基线

建设内容：

- 建立 M/E/V 三维分级规则和统一信号、场景、设备、结果语义；
- 将已有信号侦察概念模型映射到三级体系；
- 建立载频、带宽、功率、占空比、信号角色和工作状态等 M1 参数化信号描述；
- 完成功能级发现、识别、测向定位、告警和效能模型；
- 建立标准算例、任务场景集和基础参数库；
- 形成 M1 与现有 M2 统计参数的映射关系。

阶段成果：三级模型体系规范、功能级模型集、基础参数集、标准算例集和功能级验证报告。

### 14.2 第二阶段：信号级主链完善

建设内容：

- 完善信号模板、时频功率、传播、噪声背景和接收机模型；
- 打通扫描、检测、特征、识别、测向定位和退化分析链路；
- 建立城市遮挡、多径、同频冲突、短突发和跳频等典型场景；
- 形成 M2 到 M1 的 ROC、混淆矩阵、误差分布和状态参数归约；
- 完成工程级算例和跨模型一致性验证。

阶段成果：信号级模型集、信号模板库、典型场景库、跨层参数映射集和信号级验证报告。

### 14.3 第三阶段： IQ 能力建设

建设内容：

- 建设重点信号类别的复基带模板和局部 IQ 生成能力；
- 引入多径、频偏、相位噪声、采样偏差、滤波、AGC、阻塞、饱和和量化等必要因素；
- 建设多站同步 IQ 与 TOA/TDOA/FDOA 观测生成能力；
- 支撑重点检测、识别、同步和定位算法验证；
- 建立 M3 到 M2 的特征与统计归约。

阶段成果：重点 IQ 模型集、IQ 场景集、算法验证数据集、多站同步验证集和 IQ 级验证报告。

### 14.4 第四阶段：实测融合与可信度提升

建设内容：

- 接入外场频谱、接收机标定、检测识别、测向定位和实测 IQ 数据；
- 形成理论、仿真、半实物和外场数据的对比流程；
- 校准模型参数、误差分布和适用范围；
- 建立模型版本、证据、可信度和退化原因的持续管理机制；
- 形成重点场景的 V4/V5 级验证证据。

阶段成果：实测校准库、半实物回放集、模型可信度评估报告、适用范围声明和更新后的参数模型库。

## 15. 建设优先级建议

### 15.1 必须建设

- M1 参数化信号描述和全功能链，包括辐射活动、发现、识别、测向定位、告警和任务效能；
- M2 信号时频展开、扫描检测、特征识别、传播环境、接收机和测向定位主链；
- 三层统一参数、统一场景、统一时间频率与功率语义；
- M2 到 M1 的统计归约以及全链路追溯；
- 标准算例、典型城市场景和验证证据管理。

### 15.2 重点建设

- 窄带遥控、宽带图传/数传、短突发和跳频类信号的局部 IQ 模型；
- 同频多源、强弱遮蔽、城市多径和接收机阻塞条件下的 IQ 验证；
- 多站同步 IQ 与 TDOA/FDOA 相关观测；
- 实测 IQ 回放和 M3 到 M2/M1 的校准闭环。

### 15.3 条件建设

- 真实专有协议、完整加密链路和设备指纹级复现；
- 全频段、全时长、多站点连续 IQ 生成；
- 与任务无直接关系的高复杂度射频硬件细节；
- 缺少授权数据和验证依据的特定型号精确波形。

上述内容只有在任务明确、数据合法可得、验证条件充分且收益大于成本时建设。

## 16. 风险与控制措施

| 风险 | 影响 | 控制措施 |
|---|---|---|
| 将 IQ 级等同于绝对准确 | 忽略参数和校准质量，造成错误结论 | 强制声明 M/E/V 与适用范围，以实测证据确定可信度 |
| IQ 建设范围过大 | 计算、存储和数据准备成本失控 | 按重点频段、窗口、站点和指标触发 |
| 三层参数不一致 | 跨层结果无法对比 | 共用参数主数据和版本，建立跨层一致性试验 |
| 城市传播参数不足 | 测向定位偏差和检测结果失真 | 使用分级传播模型并通过外场数据校准 |
| 专有信号参数不可得 | 无法精确复现真实波形 | 使用类别模板、统计波形、脱敏特征和实测回放 |
| 只验证理想场景 | 结果缺少工程代表性 | 增加强弱遮蔽、NLOS、多径、短突发和阻塞场景 |
| 只给结果不报不确定性 | 结论难以审查和使用 | 输出误差、置信区间、降级原因和证据来源 |

## 17. 总体建设路线图

![电子信号侦察模型体系总体建设路线](总体建设路线图.png)

总体建设按照“统一基础—常规能力—跨层耦合—专项提升—可信闭环”的路线推进。首先统一模型语义、职责边界、参数底座和验证口径；随后同步形成功能级任务效能评估能力与信号级侦察机理分析能力；在此基础上建立向上归约和跨层一致性验证；再针对重点信号、重点站点和重点时间窗建设复基带采样级专项能力；最终通过半实物与外场数据校准，形成模型参数、误差分布、可信度和版本持续更新的闭环。

该路线坚持基础能力先行、常规模型优先、复基带采样能力按需建设和实测证据闭环，能够在控制建设成本和运行资源的同时，兼顾城市级反无人机任务推演、专业信号侦察分析和关键算法验证。

## 18. 结论

功能级、信号级和 IQ 级可以作为反无人机系统电子信号侦察仿真的三个模型层次，并且从实际需求、技术可行性和建设经济性看，采用三级一体化体系优于采用单一精度模型。

其中，M1 功能级承担全域任务效能评估，M2 信号级承担侦察机理表达和主要工程分析，M3 IQ 级承担重点算法、同步定位、接收机非理想和实测复盘验证。三层通过统一参数、统一环境、向下展开、向上归约和实测校准形成一个完整闭环。

本体系的专业性不体现在所有任务都使用最高数据粒度，而体现在每个任务选择与问题相匹配的模型层次，并保证结果有明确机理、参数来源、误差范围、验证证据和适用边界。按照本方案分阶段建设，可以形成兼顾规模、精度、效率和可信度的反无人机电子信号侦察仿真能力。

