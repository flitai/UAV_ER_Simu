# 10. 典型链路框图与检测识别评价实施方案

> **版本** v1.0，2026-09-07。**定位**：切片 ④「合流与厚化」的设计前置（06 §13 第 3 条），同时兑现 P1-0b（D-050「天线与接收机组件先设计后实现」）。**依据**：04 §5.1（端到端对象）、§5.4（S0–S6 观测点）、§7（模型体系与简化策略）、§7.9（检测识别与评价）、§8.1 / §8.2（组件库六类、框图能力）、§9.3（宽带 IQ 约束）、§13.3（阶段 1 退出条件）、§15.2（十二项标准算例）、附录 A（首批模型清单）；09 报告 §1.2、§6、§7；08 报告 §8（多速率）、§9（参数帧）、§13（Coder 封装）；概念模型 EM-B-07 / B-11 / B-12 / S-02 / S-03 / S-04；决策 D-013、D-036、D-037、D-045、D-047、D-049、D-050。**本方案的结论记为决策 D-051。**
>
> **用户指令（2026-09-07）**：「框图我想不这么自由组合了，用户使用起来还挺麻烦。我想先给出一个典型的框架，就象 04 实施方案中 7 模型体系与简化策略里的从辐射源发射机到接收机到 DDC 与信道化，给规定起来，用户可以在每个框图上进行参数的编辑，然后进行相应的计算，给出信号的检测识别与评价。我们需要注重实用性，灵活性可适度降低，仅仅围绕主要功能即可。」
>
> 本方案只定语义、参数、格式、界面与步骤，不含实现记录；实现中核实到的偏差按 06 §14 体例另记修正。量化数字一律「原型阶段验证值」（D-028）。

---

## 0. 一页结论

| # | 问题 | 结论 |
|---|---|---|
| 1 | 框图页默认长什么样 | **典型链路视图**：一条固定的横向链，九个环节（辐射源 → 发射天线 → 传播信道 → 接收天线 → 接收机前端 → ADC → DDC → 信道化 → 检测识别评价），每个环节一张卡片；用户只做三件事——选变体（若有）、改参数、勾观测点；不能增删节点，不能连线。04 §5.1 的链就是画面本身 |
| 2 | 灵活性留多少 | **一条链、三种信号源模式**（全合成 / 实测回放 / 混合增强，04 §4.3）。模式切换只改变「哪些环节参与计算」：回放模式下天线、信道、前端、ADC、DDC 六个环节标「回放数据已含」并锁定（录制的 S4 已经过它们）；混合模式下合成目标走完整链、回放背景在 S4 相加。环节可「旁路」的只有 DDC 与信道化（在 Coder 产物到位前允许旁路运行） |
| 3 | 框图文件格式改不改 | **只加一个可选顶层字段 `template_ref`**，其余不动。典型链路是标准框图（`cuav-diagram/1`）的**生成器**：视图状态编译成节点固定、连线固定的框图文件；引擎与服务端不需要知道「模板」，校验、运行、产品、补取全部走既有通路（B-1 至 B-7） |
| 4 | U-2 自由画布怎么办 | ~~不废，降为高级模式~~ **2026-09-10 由 D-060 改判：整体删除。** 用户判定它「不重要、用户操作起来也很难控制，有点华而不实」，先把一条完整的信号级仿真流程跑通。框图页只留典型链路一种形态；`@xyflow/react` 不再随包，`slice3-smoke` 30 项删除。本行原案与 §5.6 记的是当时的设计过程，保留不删 |
| 5 | 缺位的组件怎么补 | 新增 8 件：`AntennaGain`、`ReceiverFrontEnd`、`AdcQuantizer`（手写 C++）、`DDC`、`Channelizer`（M-2 / M-3 Coder）、`FeatureExtractor`、`TemplateClassifier`、`Evaluator`；既有 3 件加参数（缺省保持旧行为）；新增端口类型 `RecognitionList`；`SceneParamFrame` 增 7 个字段；`BlockMeta` 增 `clip_count` |
| 6 | D-050 三问 | ① 天线方向图用**解析式**（全向 / 高斯主瓣 + 副瓣底），指向做（固定或随航向），极化损耗做（五档表）；查表方向图留 P3。② 典型链路里**接收机噪声由噪声系数生成、取代手兑噪声**；既有示例与黄金基准一件不动，新参数缺省保旧行为。③ ADC 削顶是**数据标记不是降级**：逐块计数进元数据与产品，整段削顶比例超阈值才降级 |
| 7 | 检测识别评价怎么给 | 检测 = 能量检测升级为带删截的滑动噪声估计（恒虚警）+ 突发分段；识别 = 特征提取（M3 观测量提取）+ 模板加权匹配（EM-S-04 E2 档，开放集含「未知」）；评价 = 评价器按真值算帧级 Pd / Pfa / F1、突发级检出与发现时延、ROC（门限扫描，不重跑）、识别混淆矩阵；真值来自场景参数帧（全合成 / 混合）或数据清单（回放），**只进评价器不进被测算法**（04 §5.2） |
| 8 | 基准怎么变 | 组件目录黄金基准按「新增允许、已有条目加参数须记决策」更新一次（本决策）；产品黄金基准零变化；`slice1 / slice2 / replay` 三份示例原样保留为回归夹具；界面缺省框图改为典型链路（全合成，`demo-02` 场景）；跨层算例 ① 在典型链路上加一个实例，原实例不动 |
| 9 | 实施顺序 | 新增 **C 线**（§8.1，C-0 至 C-11）；切片 ④ 重排为 ④a 链路跑通 → ④b 检测识别评价 → ④c DSP 厚化（M-2 / M-3）→ ④d 收口。C-2 三件手写组件不等 MATLAB；DDC / 信道化到位前槽位旁路 |
| 10 | 不做 | 用户自定义模板与子系统封装（P2）；阵列、多站；相位噪声、增益压缩（P3，M-5）；学习型识别与型号级识别（P3，T 线）；参数扫描批量运行（试验中心，P2）；自由画布的任何新功能 |

---

## 1. 依据与现状

### 1.1 上游依据

| 条款 | 原文要点 | 在本方案里的落点 |
|---|---|---|
| 04 §5.1 | 「无人机任务和工作状态 → 辐射源复基带信号 → 发射特征与天线等效作用 → 传播损耗、时延、多径、多普勒 → 接收天线与馈线 → 接收机射频前端等效模型 → ADC 采样与量化 → 数字下变频 DDC → 信道化前宽带复基带 IQ → 信道化、检测、识别、测向或定位」；「系统内部可分别调整各环节，但外部主要评价从真实电磁辐射到接收机数字输出的综合结果」 | §2.1 的九个槽位一一对应；「外部主要评价综合结果」正是典型链路视图的取舍依据 |
| 04 §5.4 | S0 辐射源输出 / S1 到达接收天线端 / S2 前端输出 / S3 ADC 后 / S4 DDC 后信道化前（主产品）/ S5 信道化后 / S6 检测特征识别结果 | §2.3 每个观测点钉在链上的一条边；S6 不是 IQ，是 §4 的 JSONL 产品 |
| 04 §7.1 | L2 工程等效 = 首期默认；不逐环节给 L 档 | 各组件的 M/E 档按目录字段声明，整链默认 L2；不新增「等效档」界面元素 |
| 04 §7.2–§7.8 | 辐射源三类；传播由简到繁；接收天线全向 / 简化方向图 / 极化 / 馈线；前端十项非理想；ADC 七项；DDC 五步 + 信道化；噪声背景七项 | §3 逐环节取「首期必做」子集，其余列为后续变体 |
| 04 §7.9 | 「平台自带算法用于证明 IQ 可用和提供统一基线」；内置能量检测与自适应门限、突发检测与占用统计、参数估计、基础特征、一种轻量识别基线、混淆矩阵 / ROC / 检出率 / 虚警率 / F1 | §4 全部覆盖，只做「一种」基线 |
| 04 §8.1 | 左侧「按辐射源、信道、天线、接收机、数据和算法分类的组件库」；结果页「时域、频谱、瀑布图、星座图、统计特征和算法指标」 | 六类组件库仍存在（高级模式）；典型链路视图按环节而不是按类别组织；结果页新增两个页签 |
| 04 §8.2 | 「子系统封装和模板保存」「模型启用、旁路和替换」 | 系统预置的典型链路是「模板」的一种；旁路只对 DDC / 信道化开放；替换 = 槽位变体；用户自存模板留 P2 |
| 04 §9.3 | `\|Δf\| + B/2 + 保护带 < Fs/2`；连线和运行前自动检查六项 | §2.4 频率计划面板逐项检查，引擎侧在 DDC 封装层复查（08 §8 口径三） |
| 04 §13.3 | 阶段 1 退出条件之一「一个框图能够从浏览器提交并从信号源运行到检测结果」 | 典型链路直接兑现，且比「检测结果」多出识别与评价 |
| 04 §15.2 | 算例 5 接收滤波与群时延、6 ADC 量化与削顶、7 DDC 频移低通抽取、8 宽带到信道化 | 分别落在 M-3、C-2、M-2、M-3；算例框图从保存的典型链路生成 |
| 04 附录 A | 天线首期必做「全向、简化方向图、极化和馈线损耗」；接收机「滤波、增益、噪声、频偏、相噪、IQ 不平衡、削顶」；数字前端「ADC、DDC、单通道和多通道信道化」 | §3.2、§3.4、§3.5、§3.6、§3.7；相噪与滤波推到 M-3 / M-5 |
| 09 §1.2 | 「新建 › 全合成模板」得到 S0 → S4 链；实测回放模板；混合增强模板 | 就是 §2.2 的三种模式，本方案把它从「新建菜单里的三项」提升为框图页的主形态 |
| 09 §6.11、§16 | 「子系统封装与模板」需要框图文件格式增加字段，排到 P2 | 那是**用户把自建链存为模板**；本方案是**系统预置的典型链路**，只加 `template_ref`，两者不冲突（§1.3） |
| 08 §8、§13 | 多速率四条口径；Coder 封装五项职责 | DDC / 信道化组件按此实现，本方案不重复 |
| D-050 | 四件最小集、三个待拍板问题、铁律 10 | §3.2、§3.4、§3.5 逐条拍板；§7 基准变更 |

### 1.2 现状与差距

按目标链九个环节核对当前引擎目录（12 个组件，`tests/golden/component-catalog.json`）、装载器与前端：

| 环节 | 现状 | 缺口 |
|---|---|---|
| 辐射源 | `ToneSource` / `NoiseSource` / `SceneEmitterSource` / `FileReplaySource` | 场景辐射源归一化到单位功率，发射功率折在信道里（08 §9.6、§15 ⑧），S0 读不到发射电平 |
| 发射天线 | **无组件**；增益取自场景 `emission.antenna_gain_dBi`，在 `engine/src/channel.cpp` 里与路损合并施加 | 方向图、指向、极化全部缺席 |
| 传播信道 | `SceneBoundChannel`（路损 + 多普勒 + 时延，FSPL）/ `FreeSpaceChannel` | 有；增益公式把 `tx_power + G_t + G_r − L` 合在一处，拆天线要改公式 |
| 接收天线 | **无组件**；增益取自场景 `sites[0].antenna.gain_dBi`；`antenna.pattern` 声明但全库无一处使用 | 同发射天线 |
| 接收机前端 | **无组件**；噪声靠 `NoiseSource + AddMixer` 手兑绝对电平；帧里的 `noise_floor_dBm_per_Hz` 只供读数 | 噪声系数、增益、频偏、IQ 不平衡、直流全部缺席 |
| ADC | 无 | 量化、削顶、满量程标度缺席；S3 无从生成 |
| DDC | 无（M-2 未开工） | 真正的 S4 无从生成；现观测点名为 `s4` 只是同采样率的窄带观测窗（D-050 ③） |
| 信道化 | 无（M-3 未开工） | S5 无从生成 |
| 检测识别评价 | `EnergyDetector`（固定探针帧噪声估计）→ `DetectionSink`（只计数）；`detections.jsonl` 端点读取层就绪、生产者未做；`FeatureVector` 端口有类型无组件 | 滑动噪声估计 / 恒虚警（P1-4c）、检测落盘（P1-4d）未做；**识别与评价在路线图里没有任何步骤行、没有任何代码** |
| 框图落盘 | 无端点，刷新即丢（当时的 `docs/diagram-canvas-guide.md` §5.1） | 典型链路的参数集必须能保存与再开 |
| 结果页 | 信号页签已交付；检测、任务页签是占位 | 检测识别、评价两个页签待建 |

### 1.3 两个「模板」

09 报告里「模板」一词有两个含义，本方案把它们分开：

| 含义 | 出处 | 谁来做 | 需要改框图格式吗 |
|---|---|---|---|
| 系统预置的典型链路（本方案） | 09 §1.2「新建 › 全合成 / 实测回放 / 混合增强模板」、§1.3 空状态三张模板卡片 | 前端内置，编译成标准框图 | 只加可选字段 `template_ref` |
| 用户把自建链存为模板、子系统封装、旁路与替换 | 04 §8.2；09 §6.11、§16 | P2 | 要，届时另记决策 |

---

## 2. 典型链路的定义

### 2.1 环节与槽位

九个环节固定为九个**槽位**。槽位是视图概念，不进框图文件；每个槽位在编译时展开为 0 到 n 个节点，节点 `id` 固定，`type` 由该槽位当前选定的**变体**决定。

> **2026-09-09 修正（D-053，11 报告 §2.1）**：槽位增至**十一**个——尾部加「测向」`DirectionFinder`（按站实例化，可旁路）与「多站定位」`MultiSiteLocator`（全图唯一，缺省旁路）。同时引入三个**实例维度**（源 N / 站 K / 链路 N×K）：`tx` 按源、`tx_ant` / `ch` / `rx_ant` 按链路、`rx_fe` 及其后按站；隐含节点增 `sup`（`Superposition`，N > 1 时在接收天线后叠加）与 `toa`（`ToaEstimator`，TDOA 时按站）。本表第 9 行「测向（后置 P1 / P2）」由此销项。N = K = 1 时新槽位缺席、节点无后缀，既有框图逐字节不变。

| 序 | 槽位 `slot` | 名称 | 04 环节 | 首期变体（组件 `type`） | 后续变体 | 可旁路 |
|---|---|---|---|---|---|---|
| 1 | `tx` | 辐射源 | 辐射源复基带信号 | 全合成：`SceneEmitterSource`（场景绑定）；回放：`FileReplaySource`；混合：两者并存 | `TemplateSource`（T-4）；`ofdm` / `fhss` 波形（P3） | 否 |
| 2 | `tx_ant` | 发射天线 | 发射特征与天线等效 | `AntennaGain`（角色 `tx`） | 查表方向图（P3） | 否（回放模式不适用） |
| 3 | `ch` | 传播信道 | 传播损耗、时延、多径、多普勒 | `SceneBoundChannel`（场景绑定）/ `FreeSpaceChannel`（定参距离） | 双径、阴影、统计多径（P3）；刀口衍射随 D3 进 `SceneBoundChannel`，不换组件 | 否（回放模式不适用） |
| 4 | `rx_ant` | 接收天线 | 接收天线与馈线 | `AntennaGain`（角色 `rx`） | 同上 | 否（回放模式不适用） |
| 5 | `rx_fe` | 接收机前端 | 接收机射频前端等效 | `ReceiverFrontEnd`；M-3 后在其前串 `RxFilter` | 相噪、增益压缩、AGC 动态（M-5） | 否（回放模式不适用） |
| 6 | `adc` | ADC | ADC 采样与量化 | `AdcQuantizer` | 采样时钟偏差、丢样注入（04 §7.6 可选项） | 否（回放模式不适用） |
| 7 | `ddc` | DDC | 数字下变频 | `DDC`（M-2） | 多路并行 DDC | **是**（M-2 到位前） |
| 8 | `chan` | 信道化 | 信道化 | `Channelizer`（M-3，多相 FFT，选一路输出） | 全部子信道并行输出 | **是**（缺省旁路） |
| 9 | `det` | 检测识别评价 | 检测、识别 | 固定三节点：`EnergyDetector` → `FeatureExtractor` → `TemplateClassifier`，加 `Evaluator` 汇聚 | 用户算法插件（P1-9）、测向（后置 P1 / P2） | 否 |

另有两个**隐含节点**，不占槽位、不显示为卡片，由模式与绑定决定：`scn`（`ScenarioSource`，绑站点，全合成与混合模式必有）与 `mix`（`AddMixer`，混合模式在 S4 处把回放背景与合成目标相加）。

### 2.2 三种信号源模式下的槽位状态

| 槽位 | 全合成 | 实测回放 | 混合增强 |
|---|---|---|---|
| `tx` | `SceneEmitterSource` 绑辐射源 | `FileReplaySource` 选 `data_id` | 合成目标 `SceneEmitterSource` + 背景 `FileReplaySource`（卡片分上下两行） |
| `tx_ant` `ch` `rx_ant` `rx_fe` `adc` `ddc` | 启用 | **不适用**（卡片灰显「回放数据已含」，参数不可编辑） | 只对合成目标启用；背景不经过 |
| `chan` | 缺省旁路 | 缺省旁路 | 缺省旁路 |
| `det` | 启用 | 启用 | 启用 |
| 真值来源（§4.5） | 场景参数帧 | 数据清单 `truth` | 场景参数帧（背景视为「无目标」） |
| 采样率约束 | 一处填（§2.4） | 由数据清单决定，不可改 | **DDC 输出采样率与中心频率必须等于回放数据的**，频率计划面板把它作为硬约束反推 `decim` 与 `f_shift_Hz`，反推不出整数抽取比即报错 |
| 接收机噪声 | 由 `nf_dB` 生成 | 不适用 | `ReceiverFrontEnd.noise_mode = none`（背景片段自带接收机噪声，再注入即重复计入，04 §7.8「实测背景 + 合成目标」） |

三种模式对应 04 §4.3 的三种运行模式与 06 §1.3 的三个验收场景。模式切换保留各槽位已填的参数，只改变启用状态。

### 2.3 观测点 S0–S6 的落点

观测点挂在槽位之间的边上，视图里画成链上的圆点；勾选即在编译时生成 `observation_points[]` 一项（`products` 缺省 `["spectrum", "envelope"]`）。

| 观测点 | 挂在哪条边 | 采样率 | 中心频率 | 缺省勾选 | 说明 |
|---|---|---|---|---|---|
| S0 | `tx.out`（混合模式为合成目标的输出） | `fs_rf` | 辐射源 `center_Hz` | 否 | 发射电平（`emit_at_tx_power = true` 后读数即 `tx_power_dBm`，§3.1） |
| S1 | `rx_ant.out` | `fs_rf` | 站点 `center_Hz` | 否 | 到达接收天线端（含收发天线增益与路损），读数 = 链路预算 `Prx` |
| S2 | `rx_fe.out` | `fs_rf` | 同上 | 否 | 加了噪声、增益与非理想 |
| S3 | `adc.out` | `fs_rf` | 同上 | 否 | 量化后；索引带 `clip_count` |
| **S4** | `ddc.out`（DDC 旁路时 = `adc.out`；回放模式 = `tx.out`；混合模式 = `mix.out`） | `fs_rf / decim` | 站点 `center_Hz + f_shift_Hz` | **是** | 主产品（04 §5.2） |
| S5 | `chan.out`（信道化启用时才有） | `fs_s4 / channels` | 所选子信道中心 | 否 | 窄带 IQ |
| S6 | 不是 IQ | — | — | 恒有 | `detections.jsonl` / `features.jsonl` / `recognitions.jsonl` / `truth.jsonl` / `metrics.json`（§4.6） |

`iq` 产品仍按 D-040 ③ 拒绝（散点待引擎 `iq` 产品）。观测点 `id` 固定为 `s0` … `s5`，结果页按此命名页签与页头。

### 2.4 采样率计划与频率计划

典型链路里采样率只填一处、频率只填两处，其余派生；视图左栏「频率计划」面板列出全部派生量与 04 §9.3 的六项检查。

| 量 | 谁填 | 派生到 |
|---|---|---|
| `fs_rf`（宽带采样率） | 站点 `receiver.fs_Hz`，绑定站点后带出，可改 | `SceneEmitterSource.sample_rate_Hz`、`ScenarioSource.sample_rate_Hz`、`AntennaGain` / 信道 / 前端 / ADC 的块采样率（由流携带，不写参数） |
| `f_rx`（站点中心频率） | 站点 `receiver.center_Hz` | `ReceiverFrontEnd` 输出块的 `center_frequency_Hz`；`ScenarioSource` 的链路预算 |
| `f_tx`（辐射源中心频率） | 场景 `emission.center_Hz` | `SceneEmitterSource.center_frequency_Hz`；辐射源在宽带里的偏置 `f_tx − f_rx` 由 `SceneEmitterSource` 按块元数据施加（现有行为） |
| `B_tx`（辐射源带宽） | 场景 `emission.bw_Hz` | 频率计划检查；`FeatureExtractor` 不用它（真值不泄漏） |
| `decim`、`f_shift_Hz` | `DDC` 槽位 | `fs_s4 = fs_rf / decim`；S4 中心 = `f_rx + f_shift_Hz`；缺省 `f_shift_Hz = f_tx − f_rx`（把目标搬到 S4 零频附近），用户可改成任意值以演示「目标不在观测中心」 |
| `channels`、`select_channel` | `chan` 槽位 | `fs_s5 = fs_s4 / channels` |
| `nfft`、`pfa`、`band_lo_Hz / band_hi_Hz` | `det` 槽位 | 检测频段缺省 `[−0.45, +0.45] · fs_s4`（相对 S4 中心）；识别与评价按 S4 |

频率计划面板的六项检查（04 §9.3），每项通过打勾、不通过给出数值与建议：

| 检查 | 表达式 | 不通过时 |
|---|---|---|
| 有效带宽不超采样率 | `B_tx + 保护带 ≤ fs_rf`，保护带缺省 `0.1 · fs_rf` | 提示提高 `fs_rf` |
| 目标不跨频带边缘 | `\|f_tx − f_rx\| + B_tx / 2 + 保护带 < fs_rf / 2` | 提示改 `f_rx` 或 `f_tx` |
| 抽取比例合法 | `decim` 为正整数且 `fs_rf mod decim = 0`；混合模式另要求 `fs_s4 = 回放 fs` | 提示可用的 `decim` 列表 |
| 滤波器过渡带足够 | `\|f_tx − f_rx − f_shift_Hz\| + B_tx / 2 ≤ 0.4 · fs_s4`（与 `fir_version` 的通带边缘一致，08 §8） | 提示提高 `decim` 或改 `f_shift_Hz` |
| 叠加后不削顶 | 链路预算估计的 S2 峰值电平 `+ gain_dB + 峰均比余量（缺省 10 dB）< full_scale_dBm` | 提示改 `full_scale_dBm` 或 `gain_dB`（只警告不阻止提交，削顶本身是要演示的效应） |
| 标度与单位一致 | 回放数据有标定常数（混合模式）；否则背景为 `dBFS` 不能与 dBm 目标相加 | 混合模式阻止提交；回放模式只提示纵轴退到 `dBFS（未标定）` |

前端的检查是**预检**，引擎侧在各组件 `configure()` 里复查自己能知道的那一部分（08 §8 口径三）；两侧口径写在同一份表里，避免出现前端放行、引擎拒绝的情况。

### 2.5 槽位的三种状态

| 状态 | 含义 | 编译结果 | 界面 |
|---|---|---|---|
| 启用 | 参与计算 | 生成节点与两侧连线 | 正常卡片 |
| 旁路 | 用户关掉（只允许 `ddc`、`chan`） | 不生成节点，上游直接连下游；观测点随之落到上游输出 | 卡片压扁、显示「旁路」，参数保留 |
| 不适用 | 模式决定该环节不存在（回放模式的六个前端环节） | 不生成节点 | 灰显「回放数据已含」，不可点开 |

旁路是 04 §8.2「模型启用、旁路」的最小实现，且只在模板层做，不需要框图格式支持旁路字段。

### 2.6 场景绑定

全合成与混合模式必须选一个场景、一个站点、一个辐射源（首期单站单机，04 §12.1）。绑定只在视图左栏做一次，编译时写到需要它的节点：`scn`（站点）、`tx`（辐射源）、`ch`（辐射源）、`tx_ant`（辐射源，取航向与离开角）、`rx_ant`（站点）、`Evaluator`（辐射源，取真值）。`scenario_ref.sha256` 由前端从 `scene.scenario.sha256`（服务端 `X-CUAV-Sha256`）写入，不再手写（09 §6.8）。回放模式没有场景绑定，顶栏按 09 §1.2 标「无场景绑定」。

---

## 3. 各环节的组件语义

约定：所有新组件按 B-1 注册表形式进目录（`describe()` 给 `ParamSpec`），参数值只用四种标量类型（`docs/diagram-format.md` §3），功率单位按 D-047（引擎内部 `|x|² = 功率 / mW`）。角度按铁律 1（方位真北顺时针、俯仰水平为 0）。M/E 档按 01 口径写在目录里，不在界面显示。

### 3.1 辐射源

首期不新增辐射源组件，只给 `SceneEmitterSource` 加一个参数：

| 参数 | 类型 | 缺省 | 说明 |
|---|---|---|---|
| `emit_at_tx_power` | bool | `false` | `true` 时输出按 `tx_power_dBm`（场景注入的内部参数）标定电平，即发射期间 `\|x\|² = 10^(tx_power_dBm/10)`；`false` 保持现行单位功率归一化（08 §9.6），供既有示例与黄金基准使用 |

典型链路恒设 `true`，于是 S0 读数就是发射功率，S1 读数 = `tx_power + G_t(θ) + G_r(θ) − L_path − L_pol`，与频谱仪口径一致。回放源 `FileReplaySource` 不变（D-037、D-045 ⑤：不可绑定场景）。活动时间线（`tx_on` / `tx_off` / `hop`）由 G-6 施加到 `SceneEmitterSource`，本方案只要求它**同时把施加结果写进参数帧**（§3.9），供评价器取真值。

### 3.2 天线 `AntennaGain`（D-050 问题 ①）

**结论：解析式方向图，指向做，极化做，查表留 P3。** 理由：框图参数只允许标量，查表方向图必须走「版本号引用外部文件」（同 `fir_version`），首期没有任何实测方向图可填；EM-B-07 §10.3 把「主瓣内固定 / 渐变增益、主瓣外衰减」列为 E1 抽象，§15 把查表列为 E2，首期按 E1 声明并在模型卡里写明。

| 项 | 取值 |
|---|---|
| 类别 / M / E | `antenna` / M3 施加 / E1；模型依据 EM-B-07 §10.1 链路预算式与 §10.3 |
| 端口 | `in: IQStream`、`scene: SceneParamFrame`（可选输入，§6.3；不接时用固定角参数）；`out: IQStream` |
| `scene_bindable` | **`false`**（2026-09-09 修正：实现里是 `false`，见 `engine/src/antenna.cpp:68`——场景派生的缺省由典型链路视图填入，组件自己不读场景文件；`scene` 口的连线由编译器接到 `scn` 的对应 `link:` 端口）|

参数：

| 参数 | 类型 | 缺省 | 范围 / 枚举 | 说明 |
|---|---|---|---|---|
| `role` | enum | 必填 | `tx` / `rx` | 决定从帧里取离开角还是到达角 |
| `pattern` | enum | `omni` | `omni` / `directional` | 全向：常量增益；定向：高斯主瓣 + 副瓣底 |
| `gain_dBi` | number | 必填 | [−10, 40] | 峰值增益；绑定场景时缺省带出 `antenna.gain_dBi` / `emission.antenna_gain_dBi` |
| `beamwidth_az_deg` | number | 60 | (0, 360] | 方位半功率波束宽度，仅 `directional` |
| `beamwidth_el_deg` | number | 60 | (0, 180] | 俯仰半功率波束宽度，仅 `directional` |
| `sidelobe_dB` | number | 20 | [0, 60] | 副瓣底相对峰值的衰减，仅 `directional` |
| `pointing` | enum | `fixed` | `fixed` / `heading` | `heading`：视轴方位跟随平台航向（帧里的 `tx_heading_deg`，只对 `role = tx` 有意义） |
| `boresight_az_deg` | number | 0 | [0, 360) | 视轴方位（`fixed`） |
| `boresight_el_deg` | number | 0 | [−90, 90] | 视轴俯仰 |
| `polarization` | enum | `vertical` | `vertical` / `horizontal` / `slant45` / `rhcp` / `lhcp` | 极化 |
| `feeder_loss_dB` | number | 0 | [0, 20] | 馈线固定损耗（04 §7.4） |
| `aspect_az_deg`、`aspect_el_deg` | number | 0 | — | **不接参数帧时**的固定来波 / 离开方向（`FreeSpaceChannel` 定参链路用） |

模型（增益按 dB，幅度乘 `10^(G/20)`，每帧零阶保持，与信道增益同一口径）：

```
Δaz = wrap180(aspect_az − boresight_az)，Δel = aspect_el − boresight_el
omni:        G = gain_dBi − feeder_loss_dB
directional: G = gain_dBi − min( 12·[(Δaz/beamwidth_az)² + (Δel/beamwidth_el)²], sidelobe_dB ) − feeder_loss_dB
```

`12·(Δ/θ)²` 在 `Δ = θ/2` 处恰为 3 dB，是半功率波束宽度的定义；这是 ITU-R 类文件里常用的抛物线主瓣近似，属 E1。极化损耗 `L_pol` 只在 `role = rx` 端计一次（发射端极化经参数帧带到接收端，§3.9），按五档表：同极化 0 dB；线极化与圆极化之间 3 dB；`vertical` 与 `horizontal` 之间、`rhcp` 与 `lhcp` 之间 20 dB（工程标称，非物理无穷大）；`slant45` 与另一线极化之间 3 dB。表值写进模型卡，来源标 `assumed`（铁律 8）。

解析锚点（实现时以单测核对）：`directional`、`beamwidth 60°`、`Δaz = 30°`、`Δel = 0` 时 `G = gain − 3.0 dB`；`Δaz = 90°` 时 `12·(1.5)² = 27 dB` 被 `sidelobe_dB = 20` 截住，`G = gain − 20 dB`。

### 3.3 传播信道

`SceneBoundChannel` 加一个参数，其余不动：

| 参数 | 类型 | 缺省 | 说明 |
|---|---|---|---|
| `gain_mode` | enum | `link_budget` | `link_budget`：现行 `tx_power + G_t + G_r − path_loss`（保持黄金基准）；`path_loss_only`：只施加 `−path_loss_dB`，发射功率与天线增益由 `tx` / `tx_ant` / `rx_ant` 各自负责 |

典型链路恒设 `path_loss_only`。`FreeSpaceChannel` 不改：其 `tx_power_dBm / tx_gain_dBi / rx_gain_dBi` 缺省 0，典型链路不填即为纯路损。D3 落地时 `path_loss_dB` 换成自由空间 + 刀口衍射附加损耗，帧结构与组件都不变（`docs/scenario-format.md` §7）。双径、阴影、统计多径按 04 §7.3 作后续变体，接口走 05 P0 的 `ChannelPathSet`（D-013 首期即启用端口类型，08 §15 ③）。

### 3.4 接收机前端 `ReceiverFrontEnd`（D-050 问题 ②）

**结论：典型链路里接收机噪声由噪声系数生成，取代 `NoiseSource + AddMixer` 手兑；既有示例与黄金基准一件不动。** 理由：04 §7.5 把「噪声系数或等效输入噪声」列为首期必做；手兑噪声让「噪声系数」这个演示时必讲的参数没有落点（D-039 ②）；而既有示例仍合法，跨层算例 ① 的原实例（`Tone + Noise → EnergyDetector`）恰好独立于接收机模型，保留不动，新加一个典型链路实例（§9）。

| 项 | 取值 |
|---|---|
| 类别 / M / E | `receiver` / M3 施加 / E2；模型依据 EM-B-11 §10.1（`N = −174 + NF + 10·log10 B`）、EM-B-12 §10.1（参考温度）、04 §7.5 |
| 端口 | `in: IQStream`；`out: IQStream` |
| 随机性 | `init()` 从 `IRandom&` 取一个 u64 建私有子流（同 `SceneEmitterSource`，08 §11.1），噪声样点与节点书写顺序解耦 |

参数（04 §7.5 十项里取算术简单的六项，其余列后续）：

| 参数 | 类型 | 缺省 | 范围 / 枚举 | 说明 |
|---|---|---|---|---|
| `noise_mode` | enum | `thermal` | `thermal` / `none` | `none` 用于混合模式（背景已含噪声） |
| `nf_dB` | number | 必填 | [0, 30] | 噪声系数；绑定站点时带出 `receiver.nf_dB` |
| `reference_temperature_K` | number | 290 | [100, 1000] | 热噪声参考温度（EM-B-12 `reference_temperature`） |
| `gain_dB` | number | 0 | [−20, 80] | 总增益，噪声按输入端计入后再放大（噪声折合到输入，EM-B-11） |
| `lo_offset_Hz` | number | 0 | — | 本振频偏，相位累加器跨块连续 |
| `iq_gain_imbalance_dB` | number | 0 | [−3, 3] | IQ 幅度不平衡 |
| `iq_phase_imbalance_deg` | number | 0 | [−20, 20] | IQ 相位不平衡 |
| `dc_offset_mW` | number | 0 | [0, ∞) | 直流分量功率，0 为关；I、Q 各占一半 |

处理顺序与式子（每样点）：

```
n  ~ CN(0, N0·fs)，N0 = k·T·10^(nf_dB/10)·1000 mW/Hz         （T = 290 K 时 N0 ≈ −174 dBm/Hz + nf_dB）
y1 = 10^(gain_dB/20) · (x + n)
y2 = y1 · exp(j·2π·lo_offset_Hz·n_sample/fs)                 （相位跨块连续，按 2π 归约）
y3 = K1·y2 + K2·conj(y2)，K1 = (1 + g·e^{jφ})/2，K2 = (1 − g·e^{jφ})/2，g = 10^(imb_dB/20)，φ = imb_deg
y  = y3 + sqrt(dc_offset_mW/2)·(1 + j)
```

噪声覆盖整个复奈奎斯特带 `fs`，即假定模拟前端带宽不小于 `fs`；接收滤波（幅频响应、群时延）是 M-3 的 `RxFilter`，串在本组件之前，本组件不做滤波。IQ 不平衡取 `K1 / K2` 形式，是为了与 MATLAB `comm.IQImbalance` 的定义一致，M-5 的参考模型可以直接对拍。

解析锚点（实现时以单测核对）：`fs = 10 MS/s`、`nf = 6 dB`、`gain = 0` 时噪声总功率 `−174 + 6 + 70 = −98 dBm`，`nfft = 1024` 的每格底噪 `−98 − 30.1 = −128.1 dBm`；`fs = 500 kS/s`、`nf = 6` 时 `−111 dBm`，与切片 ② 示例里手兑的 `−111 dBm` 一致，说明现有示例可以逐值迁移到本组件；`imb = 0.5 dB`、`φ = 5°` 时镜像抑制比 `|K1|²/|K2|² ≈ 25.6 dB`。噪声统计按蒙特卡洛矩校验（铁律 9）：功率相对误差 ≤ 1%、I / Q 方差比 1 ± 0.02、I / Q 互相关 ≤ 0.01（样本 ≥ 10⁶）。

### 3.5 ADC `AdcQuantizer`（D-050 问题 ③）

**结论：削顶是数据标记，不是降级；整段削顶比例超阈值才降级。** 理由：04 §5.2 要求 IQ 元数据说明「是否存在削顶、丢样或不连续」，这是标记；08 §9.5 已确立「已声明的模型效应不标降级，否则会掩盖真正的降级」；但持续过载的数据对检测识别确实不可信，EM-S-02 §11.2 有 `overload_flag`，所以过载要作为质量标记随检测结果走，并在比例失控时把任务标 `degraded`。

| 项 | 取值 |
|---|---|
| 类别 / M / E | `receiver` / M3 施加 / E2；依据 04 §7.6，EM-S-12 退化模型（削顶 → 质量标记） |
| 端口 | `in: IQStream`；`out: IQStream` |

参数：

| 参数 | 类型 | 缺省 | 范围 / 枚举 | 说明 |
|---|---|---|---|---|
| `bits` | enum | `14` | `8` / `10` / `12` / `14` / `16` | I、Q 各自的量化位数 |
| `full_scale_dBm` | number | 必填 | [−60, 30] | 满量程复单音（`\|x\| = A`）对应的功率，`A = 10^(full_scale_dBm/20)`；I、Q 各在 `±A` 削顶 |
| `rounding` | enum | `nearest` | `nearest` | 均匀量化，四舍五入到最近码；留枚举位以便将来加抖动 |
| `degrade_clip_ratio` | number | 0.01 | (0, 1] | 全程削顶样点比例超过此值时组件状态置 `degraded`，原因 `adc_overload` |

模型：`q = 2A / 2^bits`；`I_q = clamp(round(I/q), −2^(bits−1), 2^(bits−1) − 1) · q`，Q 同；任一分量触碰边界计一次削顶。输出仍是 float32（引擎内部格式），量化只体现在取值离散化上；`BlockMeta` 新增 `clip_count`（本块削顶样点数），`state_reasons` 追加 `adc_clip:<n>`（记录性质），`calibration` 不变（满量程标度是 D-047 的同一口径）。观测点索引把每行的 `clip_count` 累计写进 `index.json`（`clipped_samples`），前端在 S3 / S4 页头显示「削顶 n 样点」（不解释来源，只给数）。

解析锚点（实现时以单测核对）：满量程复单音的量化信噪比 `6.02·bits + 1.76 dB`（14 位为 86.0 dB）；`+3 dB` 过驱的单音每周期四个象限各有削顶段，削顶比例可解析算出用于对拍；无输入时输出精确为零（不造出假直流）。

### 3.6 DDC `DDC`（M-2）

按 06 §9D M-2 与 08 §8、§13 实现，本方案只补典型链路要用的参数面：

| 参数 | 类型 | 缺省 | 说明 |
|---|---|---|---|
| `f_shift_Hz` | number | 必填 | 复混频频移，缺省由频率计划填 `f_tx − f_rx` |
| `decim` | number | 必填 | 整数抽取比，`fs_in mod decim = 0` |
| `fir_version` | enum | `lp_v1` | 抗混叠低通的版本；`lp_v1` 通带 `0.4·fs_out`、阻带 `0.5·fs_out`、抽头数取奇数使群时延为整数样点（08 §8 口径二） |

输出块 `start_sample`、`sample_rate_Hz`、`center_frequency_Hz` 按口径一换算；`trace.parameter_version` 含 `fir_version`。M-2 到位前，槽位允许旁路：S4 = S3，`fs_s4 = fs_rf`，结果页页头与 `task.json` 记「DDC 旁路」，这是演示可见的状态，不隐藏。

### 3.7 信道化 `Channelizer`（M-3）

| 参数 | 类型 | 缺省 | 说明 |
|---|---|---|---|
| `channels` | number | 8 | 子信道数，2 的幂 |
| `select_channel` | number | 0 | 输出哪一路（相对 S4 中心从低到高编号，`channels/2` 为零频那一路） |
| `fir_version` | enum | `pfb_v1` | 多相原型滤波器版本 |

首期只输出一路 S5；「全部子信道并行」留后续。缺省旁路：检测识别评价按 S4 做；启用后 `det` 槽位的三个节点接到 S5，并在结果页页头标「S5 子信道 k」。

### 3.8 既有组件的加参与基准

| 组件 | 改动 | 缺省是否保旧行为 | 影响 |
|---|---|---|---|
| `SceneEmitterSource` | 加 `emit_at_tx_power`（§3.1） | 是 | 目录黄金基准该条目 `params` 变化 |
| `SceneBoundChannel` | 加 `gain_mode`（§3.3） | 是 | 同上 |
| `EnergyDetector` | 加 `noise_mode` / `noise_window_frames` / `merge_gap_frames` / `band_power_dBm`（§4.2） | 是（`noise_mode = probe` 即现行算法） | 同上；`engine/tests/golden/energy_detector.json` 不变 |
| `DetectionSink` | 不改；典型链路不用它，自由画布仍可用 | — | — |
| `ObservationTap` | 索引新增 `clipped_samples`（只在上游有 `clip_count` 时非零） | 是 | 索引文件加一个键，B-7 读端忽略未知键 |

三处加参数都触发 `docs/component-catalog.md` §5「已有条目不变」规则，统一以 D-051 记录，黄金基准更新一次并在 WORKLOG 记一条（§7）。

### 3.9 参数帧与块元数据的字段增补

`SceneParamFrame`（`engine/include/cuav/types.h`，`docs/scenario-format.md` §7）增补，由 `geo/` 链路预算与 `ScenarioSource` 填：

| 字段 | 单位 | 说明 | 消费者 |
|---|---|---|---|
| `aod_az_deg`、`aod_el_deg` | deg | 辐射源处的离开方向（ENU，真北顺时针、水平为 0） | `AntennaGain(role = tx)` |
| `aoa_az_deg`、`aoa_el_deg` | deg | 站点处的到达方向 | `AntennaGain(role = rx)` |
| `tx_heading_deg` | deg | 辐射源航向（与 `EntityState.heading_deg` 同值） | `AntennaGain(pointing = heading)` |
| `tx_on` | bool | 该帧发射是否开启（G-6 施加结果） | `Evaluator` |
| `tx_center_Hz` | Hz | 该帧辐射源中心频率（跳频后的值） | `Evaluator` |

`tx_polarization` 不进帧：接收端 `AntennaGain` 的极化损耗按其自身参数与装载器注入的辐射源极化（新增场景字段 `emission.polarization`，缺省 `vertical`，进 `docs/scenario-format.md` §4）计算。`BlockMeta` 增 `clip_count`（u64，缺省 0，跨组件按「取上游值」传递，`AdcQuantizer` 覆写为本块值）。两处都是结构体加字段，序列化只在观察者与产品索引里出现，不影响既有产品文件的字节。

---

## 4. 检测、识别与评价

### 4.1 职责边界

按 D-011 与 `docs/concept-model-layers.md`：原始 IQ 只由「IQ 观测量提取」组件处理；M2 裁决组件只消费观测量。典型链路 `det` 槽位的三个节点因此分两层：

| 节点 | 层 | 输入 | 输出 | 概念模型 |
|---|---|---|---|---|
| `EnergyDetector` | M3 观测量提取（帧级判决属 M2，但它直接读 IQ，按现状归 M2/E2 声明不变） | `IQStream` | `DetectionList` | EM-S-02 |
| `FeatureExtractor` | M3 观测量提取 | `IQStream` + `DetectionList` | `FeatureVector` | EM-S-03 的 M3 侧（特征的计算） |
| `TemplateClassifier` | M2 裁决 | `FeatureVector` | `RecognitionList`（新端口类型） | EM-S-04 |
| `Evaluator` | 评价，不是模型 | `DetectionList` + `RecognitionList` + `SceneParamFrame`（可选） | 无端口输出，写 `truth.jsonl` / `metrics.json` | 04 §7.9、§15.2 |

真值只进 `Evaluator`（04 §5.2「可供评价使用但不向被测算法泄漏的真值索引」）。`FeatureExtractor` 与 `TemplateClassifier` 不接参数帧、不读场景，任何「用真值提高识别率」的捷径在端口上就走不通。

### 4.2 检测 `EnergyDetector`（P1-4c）与 `detections.jsonl`（P1-4d）

新参数（旧参数不变，缺省即现行算法，`engine/tests/golden/energy_detector.json` 不动）：

| 参数 | 类型 | 缺省 | 说明 |
|---|---|---|---|
| `noise_mode` | enum | `probe` | `probe`：现行，前 `noise_frames` 帧估噪声后固定；`sliding`：带删截的滑动噪声估计（下） |
| `noise_window_frames` | number | 256 | 滑动窗长（帧） |
| `merge_gap_frames` | number | 2 | 命中帧之间的空隙不大于此值即并入同一突发（EM-S-02 `merge_time_gap`） |
| `band_power_dBm` | bool | `true` | 在 `Detection` 里附带频段内功率与噪声估计的 dBm 读数（输入已标定时） |

滑动噪声估计（`sliding`）：维护最近 `W` 帧的逐频点功率环，**只纳入未命中的帧**（删截），噪声取环内逐频点中位数除以 `ln 2`（与现行 `probe` 同一估计量，保证 `H0` 下检测量均值为 1 的口径不变），门限仍取 `dsp::threshold_for_pfa(M, pfa)`。环未满时用已有帧并把 `Detection.noise_frames_used` 写小，不标降级（这是运行初期的正常状态）；环内从未出现未命中帧（信号持续占满）时保持上一次估计并计数 `noise_stale`，进 `status().notes`。这是 D-026「不得静态门限」的兑现：背景漂移时门限跟着走，而删截避免信号自身抬高噪声估计。

`Detection` 结构增补 `segment_id`（突发编号，按 `merge_gap_frames` 合并）、`band_power_dBm`、`noise_dBm`、`overload`（该帧所在块 `clip_count > 0`）。运行观察者新增 `on_detection(const std::string& node_id, const Detection&, const BlockMeta&)`，`cuav_run` 惰性开 `detections.jsonl` 逐行落盘（与 `track.jsonl` 同法），每行：

> **2026-09-09 修正（D-053）**：`on_detection` 加 `node_id`，行里加 `node_id` 与 `site_id`。多站下每个站各有一个检测器（`det__<site>`），不带节点与站标识就分不清哪一行是哪个站检出的。

```
{"t_s", "node_id", "site_id", "start_sample", "frame_index", "segment_id", "f_lo_Hz", "f_hi_Hz",
 "statistic", "threshold", "hit", "band_power_dBm", "noise_dBm", "snr_dB", "overload", "noise_frames_used"}
```

`f_lo_Hz / f_hi_Hz` 是绝对频率（`center + band`），`snr_dB = band_power − noise`。`GET /api/v1/results/{task}/detections` 的读取层已就绪（B-7），不改。

### 4.3 突发分段与特征 `FeatureExtractor`（P1-4e）

| 项 | 取值 |
|---|---|
| 类别 / M / E | `algorithm` / M3 / E2；依据 EM-S-03 §10.5–§10.10、04 §7.9「中心频率、带宽、功率和持续时间估计」「基础时频特征提取」 |
| 端口 | `iq: IQStream`、`det: DetectionList`；`out: FeatureVector` |
| 参数 | `nfft`（缺省 1024，必须等于检测器的 `nfft`，装载器核对）、`bandwidth_method`（`occupied_99` / `edge_minus_20dB`，缺省 `occupied_99`）、`min_frames`（缺省 2，短于此的突发只记不出特征）、`window_frames`（缺省 64，占空比与重复周期的统计窗） |

按 `start_sample` 与检测器同帧对齐（两者都从样点 0 切帧、不加窗、不重叠），对每个 `segment_id` 在突发结束时输出一条特征：

| 特征 | 单位 | 算法 |
|---|---|---|
| `t_start_s`、`t_end_s`、`duration_s`、`frames` | s | 由帧序号换算 |
| `center_Hz` | Hz | 突发内平均功率谱的功率加权质心，加上块中心频率 |
| `bandwidth_Hz` | Hz | 平均功率谱的 99% 功率占用带宽（或 −20 dB 边缘） |
| `band_power_dBm`、`peak_dBm` | dBm | 突发内平均 / 峰值频段功率（输入已标定时；否则 `dBFS` 并置 `calibrated = false`） |
| `snr_dB` | dB | 相对检测器噪声估计 |
| `spectral_flatness` | 0–1 | 占用带内几何均值 / 算术均值（噪声样宽带信号趋近 1，单音趋近 0） |
| `crest_factor_dB` | dB | 突发内样点峰值功率 / 平均功率 |
| `duty` | 0–1 | 最近 `window_frames` 帧里命中帧比例 |
| `interval_from_prev_s`、`hop_from_prev_Hz` | s、Hz | 与上一突发的起始间隔与中心频率差（跳频与周期性的证据） |
| `overload` | bool | 突发内任一帧过载 |
| `quality` | enum | `full` / `short` / `low_snr` / `overload`（EM-S-03 §10.13） |

写 `features.jsonl`（观察者 `on_feature`），每行一条上表。特征名与 EM-S-03 §11.2 对齐（`estimated_bandwidth`、`center_frequency_trace`、`duty_cycle`、`burst_pattern`、`hop_sweep_feature` 的首期子集）。

### 4.4 识别 `TemplateClassifier` 与模板库 v1

**基线取 EM-S-04 E2「工程模板加权匹配」**，不做学习型（04 §7.9「一种传统或轻量学习识别基线」取传统）。

| 项 | 取值 |
|---|---|
| 类别 / M / E | `algorithm` / M2 / E2；依据 EM-S-04 §10.2（假设空间与三层标签）、§10.5（区间距离）、§10.6（似然与后验）、§10.9（开放集） |
| 端口 | `in: FeatureVector`；`out: RecognitionList` |
| 参数 | `library_version`（string，缺省 `v1`；库文件路径是内部参数 `library_path`，装载器自 `models/recognition/library-<version>.json` 注入，D-037 同法）、`accept_threshold`（缺省 0.6）、`ambiguity_margin`（缺省 0.2）、`unknown_distance`（缺省 4.0）、`min_quality`（enum，缺省 `short`：`quality` 低于此的突发直接给 `unknown_low_quality`） |

算法（每条特征）：对库中每个模板 `k`，逐特征取区间外距离 `d_ik`（EM-S-04 §10.5 区间形式，尺度 `s_i` 由库给），加权平均 `D_k = Σ w_ik·m_i·d_ik / Σ w_ik·m_i`（`m_i` 为该特征是否可用）；似然 `L_k = exp(−D_k/2)`，未知假设似然 `L_u = exp(−unknown_distance/2)`；先验均匀；后验 `p_k = L_k / (Σ L_j + L_u)`。判决：最大后验 ≥ `accept_threshold` 且与次大之差 ≥ `ambiguity_margin` → `known`；差不足 → `ambiguous`（仍给 Top-1）；最大后验低于门限 → `unknown`。输出（`RecognitionList` 一项 = 一条突发）：

```
{"t_s", "t_end_s", "segment_id", "label", "posterior", "top_n": [{"label", "posterior"}...3],
 "distance", "result": "known" | "ambiguous" | "unknown", "unknown_kind", "evidence_quality"}
```

写 `recognitions.jsonl`（观察者 `on_recognition`）。标签按 EM-S-04 §10.2 三层，**首期只到 `signal_role` 层**；型号级（`signal_template`）随 T 线的辐射源模板与 P3 数据校准。模板库 v1（附录 D）先给五类：`video_link`（宽带、噪声样、长时占用）、`telemetry_burst`（窄带、短突发、周期）、`rc_hopping`（窄带、短突发、跳频）、`cw_beacon`（带宽约等于分辨率、连续）、`background`（不匹配任何一类时由开放集给 `unknown`，`background` 不作显式模板）。这五类恰好能由现有波形（`tone` / `noise` / `burst`）加 G-6 的 `hop` 生成，跨层算例可以闭环；库文件的区间与权重来源标 `assumed`，甲方数据到货后按 04 §11.3 标定并升版本。

### 4.5 真值与评价 `Evaluator`（P1-4f）

| 项 | 取值 |
|---|---|
| 类别 / M / E | `algorithm` / 评价（M 层写 `M2`，`model_id = eval-baseline`）；依据 04 §7.9、§11.4 任务层指标、EM-S-02 §10.18 |
| 端口 | `det: DetectionList`、`rec: RecognitionList`、`scene: SceneParamFrame`（可选输入，§6.3） |
| `scene_bindable` | **绑站点**（2026-09-09 修正，D-053）：原写「绑辐射源」，多站多源下评价器应当**绑一个站、吃该站的全部链路帧**——一个站的检测器看到的是 N 个源叠加后的信号，真值区间必须是这 N 条链路的并集，绑单个源会把其它源的发射算成虚警 |
| 参数 | `truth_source`（enum `scenario` / `manifest` / `none`）、`data_id`（string，`manifest` 模式必填，解析同回放源）、`match_overlap`（缺省 0.5，突发匹配的时间重叠比）、`roc_points`（缺省 32）；内部参数 `out_dir`（同 `ObservationTap`）、`manifest_path`、`scenario_path` |

真值的三种来源：

| 来源 | 帧真值 `on(t)` | 突发真值 | 类别真值 |
|---|---|---|---|
| `scenario` | 参数帧 `tx_on` 且 `tx_center_Hz ± bw/2` 与检测频段相交 | `tx_on` 的连续区间（`hop` 切分区间，因为中心频率变了） | 由场景 `emission.waveform.type` 与活动映射：`noise` 且 `bw ≥ 1 MHz` → `video_link`；`burst` 无 `hop` → `telemetry_burst`；`burst` 有 `hop` → `rc_hopping`；`tone` → `cw_beacon`。映射表写在评价器里并进模型卡，它只是「场景怎么造的」的反查，不是识别算法的一部分 |
| `manifest` | 清单 `truth.class_code` 非背景类 → 全片为真；背景类 → 全片为假 | 全片一段 | 清单类别到 `signal_role` 的映射表（DroneRFb 的机型一律 `video_link` 或按论文标注的链路类型；DroneRFa 的 `T0000` 背景为假），映射表进 `data/iq/measured/README.md` |
| `none` | 无真值 | — | 只出检测与识别的计数摘要，指标项全部 `not_applicable` |

混合模式下真值取 `scenario`，回放背景视为无目标；若背景片段本身含无人机信号（清单类别非背景），评价器把结果标 `degraded`，原因「背景片段含目标，真值不完整」。

指标（`metrics.json`，`cuav-metrics/1`，附录 C）：

| 组 | 指标 | 定义 |
|---|---|---|
| 帧级 | `pd`、`pfa`、`precision`、`recall`、`f1` | 以检测帧为单位与 `on(t)` 对照；`pd = tp / (tp + fn)`、`pfa = fp / (fp + tn)` |
| 突发级 | `pd_segment`、`false_segments`、`detect_delay_s{mean, max}` | 检测突发与真值区间时间重叠 ≥ `match_overlap` 记匹配；发现时延 = 匹配突发首帧时刻 − 真值区间起点 |
| ROC | `[{threshold, pd, pfa}] × roc_points` | 用 `detections.jsonl` 里每帧的 `statistic` 对真值扫门限，**不重跑**；当前工作点单独给出 |
| 识别 | `confusion[真值类][识别类]`（含 `unknown` 列）、`accuracy`、每类 `precision / recall / f1 / support`、`unknown_rate`、`ambiguous_rate` | 只对匹配上真值区间的突发计；未匹配的识别结果计入 `false_segments` 不进混淆矩阵 |
| 状态 | `state`、`reasons[]` | 四态：真值不完整 / 过载比例高 / 噪声估计陈旧 → `degraded`；`truth_source = none` → `not_applicable` |

评价器在 `flush()` 写 `truth.jsonl`（它实际使用的真值区间，每行 `{t_s, t_end_s, site_id, emitter_id, center_Hz, bw_Hz, label}`）与 `metrics.json`。

> **2026-09-09 修正（D-053）**：`truth.jsonl` 每行加 `site_id` 与 `emitter_id`（多源下真值区间要说清是哪个源造成的）；`metrics.json` 顶层按 `site_id` 分节（单站时仍只有一节，附录 C 的结构作为该节的内容），并预留 `localization{}` 段供 L-9 填定位指标。Python 参考 `algos/reference/evaluate.py` 从 `detections.jsonl + recognitions.jsonl + truth.jsonl` 独立重算，与 `metrics.json` 逐值对拍（浮点按 D-046 ⑧ 显式循环累加）。

### 4.6 产品文件与端点

| 文件（`data/runs/<task_id>/`） | 生产者 | 端点 | 状态 |
|---|---|---|---|
| `detections.jsonl` | `cuav_run` 经 `on_detection` | `GET /api/v1/results/{task}/detections?t0&t1&stride` | 读取层已有（B-7），生产者 P1-4d |
| `features.jsonl` | `on_feature` | `.../features` | 新增，复用 JSONL 时间窗读取器 |
| `recognitions.jsonl` | `on_recognition` | `.../recognitions` | 新增，同上 |
| `truth.jsonl` | `Evaluator` | `.../truth` | 新增，同上；只在有真值时存在 |
| `metrics.json` | `Evaluator` | `.../metrics` | 新增，整文件；未就绪 409 `not_ready`，终态无文件 404 |
| `bearings.jsonl` | `cuav_run` 经 `on_bearing`（D-053）| `.../bearings?t0&t1&stride&site_id&emitter_id` | 新增，L-3；抽稀键 `link_id` |
| `positions.jsonl` | `cuav_run` 经 `on_position`（D-053）| `.../positions?t0&t1&stride&emitter_id&method` | 新增，L-4 / L-5；抽稀键 `emitter_id:method` |

`task.json` 增 `metrics_summary`（`pd / pfa / f1 / accuracy` 四个数，供任务列表显示），由服务端在收到 `task.state` 终态后读 `metrics.json` 填。

---

## 5. 界面：典型链路视图与结果页

### 5.1 框图页 = 典型链路视图

路由 `#/diagram` 改指典型链路视图（`web/src/chain/ChainView.tsx`），自由画布移到 `#/diagram/canvas`。三栏沿用 09 §6.1 的宽度（左 280 / 中自适应 / 右 320），只换内容：

```
┌ 试验设置 ───┐ ┌ 典型链路 ───────────────────────────────────────────────────────┐ ┌ 参数 ────┐
│ 模式 ○全合成  │ │ [辐射源]─S0─[发射天线]─[信道]─[接收天线]─S1─[前端]─S2─[ADC]─S3─    │ │ 所选槽位  │
│     ○回放    │ │  [DDC]─S4─[信道化]─S5─[检测]─[特征]─[识别]─[评价]                  │ │ 的参数   │
│     ○混合    │ │                                                                  │ │ 面板     │
│ 场景 demo-02 │ │ 卡片：名称 / 变体下拉 / 三行参数摘要 / 状态角标（待填 ✓ 旁路 不适用）│ │ （09 §6.6）│
│ 站点 site-1  │ │ 圆点：观测点 S0…S5，点击勾选产品                                  │ │          │
│ 目标 uav-1   │ │                                                                  │ │          │
│ 时长 / 种子  │ │ 工具条：模式说明 · 校验 · 运行 ⏵ · 保存 · 另存 · 展开为自由画布     │ │          │
│ 频率计划 ✓✓✗ │ └──────────────────────────────────────────────────────────────────┘ └──────────┘
└─────────────┘
```

卡片数固定，窗口窄时链条折成两行（2K 基线一行放下，09 §11 视口基线）。没有组件库面板，没有拖放，没有连线操作。

### 5.2 槽位卡片与参数面板

- 卡片正文三行参数摘要由该槽位当前变体的「关键参数」列表决定（模板描述符里声明，附录 A），用 `format.ts` 的工程计数法。
- 变体下拉只在有多个变体的槽位出现（`tx` 的回放数据选择、`ch` 的场景绑定 / 定参、`chan` 的启用 / 旁路、`ddc` 的启用 / 旁路）。
- 右栏复用 `ParamPanel.tsx`（`ParamSpec` 驱动，互斥成组、内部参数不出现、清空回缺省，09 §6.6），上方加「变体」与「旁路」两行；派生参数（§2.4 一处填多处派生的目标）在面板里显示为只读并注明来源（「由站点接收机带出」）。
- `det` 槽位点开是一个三页签面板（检测 / 特征 / 识别），评价器参数在「试验设置」里（真值来源随模式自动定，用户只见 `match_overlap`）。
- 卡片状态角标：`待填`（必填缺）/ `✓`（本地预检通过）/ `✕`（预检或 `--validate` 失败，悬停显示错误）/ `旁路` / `不适用`。校验态与结果态仍是两个正交徽标（D-037）。
- **观测点一行写全名**：`S0 辐射源输出`、`S1 接收天线端`、`S2 前端输出`、`S3 量化后`、`S4 主产品`、`S5 子信道`，2K 基线下一行放得下。只写 `S0…S5`、名称藏在悬停提示里等于没写（2026-09-08 实测反馈）。所在环节旁路或未实现时置灰禁用（如信道化未实现时的 S5），不隐藏。**链条结构不动**：观测点仍是链条下方独立的一行，不插进卡片之间。
- **勾选只改框图，产品要下次运行才有**：当前勾选与页面上那个任务的观测点不一致时，工具条给一条黄色说明「观测点与当前任务不一致（现在是 …），重新运行后生效」。没有在先的任务时不提示。
- **中栏版式**：九个环节按 **3 × 3** 排（`.chain-strip` 是三列网格），行距 56 px；**2026-09-09 改为每行四张的蛇形**（D-053 把槽位加到十一个之后，3 列会排成四行、末行只剩两张；用户改定四列并要求双数行从右往左，十一个排成 4 + 4 + 3，见 11 报告 §5.2）；**卡片保持 176 × 128 px**（放大到 190 px 高试过，太大，退回）。连接件横向拉伸铺满列宽（`.chain-arrow` 是 `flex: 1` 的细线加箭头），卡片不跟着变大而链条仍横贯中栏——不拉伸的话三张小卡片会挤在中间、两侧空一大片。链条与观测点行放在一块占满工具条以下高度的面板里（`.chain-body`）并纵向居中。原来是一行九张吊在中栏顶上，2K 屏下方留约 1100 px 裸空白。连接箭头跟在卡片**之后**而不是之前——写在之前的话，换行时下一行会以一个孤立箭头开头；行末那个 `→` 表示接下一行。**§5.1 的版式图仍画作一行，那是示意，实际排布以本条为准。**

### 5.3 试验设置栏与频率计划

左栏自上而下：模式（三选一）、场景 / 站点 / 目标三个下拉（回放模式换成「回放数据」按钮，直达数据中心「用于回放」）、运行时长与种子（写 `run`）、频率计划面板（§2.4 六项检查，实时重算）。混合模式多一行「背景数据」。改站点会把接收机参数（`fs_Hz / center_Hz / nf_dB / antenna.gain_dBi`）带入对应槽位，已被用户手改过的参数不覆盖并标「已手改」。

### 5.4 校验与报错映射

- 本地预检：必填、范围、枚举（来自目录）+ 频率计划六项。任一失败运行按钮禁用，卡片标 `✕`。
- 服务端 `--validate` 的四元组 `{code, node_id, port, message}` 按节点 `id` 反查槽位（描述符里 `node id → slot` 固定），高亮卡片；`node_id` 为隐含节点（`scn` / `mix`）时高亮「试验设置」栏。19 个错误码全部能落到某张卡片或左栏，没有「无处可去」的错误。
- 预检说「待修正」、服务端说「校验失败」，沿用 09 §6.9 的措辞区分。

### 5.5 保存、载入、另存

框图开始落盘（06 §9A 新增 B-8）：

| 端点 | 作用 |
|---|---|
| `GET /api/v1/diagrams` | 列出 `data/diagrams/*.diagram.json`（`diagram_id / name / template_ref / scenario_ref / 修改时间`） |
| `GET /api/v1/diagrams/{id}` | 原文，响应头 `X-CUAV-Sha256` |
| `PUT /api/v1/diagrams/{id}` | 最小结构检查 + 内部参数 400 + `--validate`；先落临时文件再原子改名；规范序列化 `JSON.stringify(doc, null, 2)` 加末尾换行（与场景文件同约定） |
| `DELETE /api/v1/diagrams/{id}` | 删除；已被任务引用的不阻止（任务目录自有副本） |

`data/diagrams/` 是小文件目录，**入库**（同场景文件，`.gitignore` 放行）。「保存」写当前 `diagram_id`；「另存」弹出新 `id` 与 `name`；版本说明写 `trace.notes`，正式的版本检查留 P2（04 §8.2）。初始化时框图页载入最近保存的典型链路，没有则载入内置缺省（全合成、`demo-02`）；`web/src/App.tsx` 不再把 `EXAMPLES[0]` 当初始框图。

载入一份框图时：有 `template_ref` 且节点 `id` / `type` 全部匹配描述符 → 进典型链路视图；否则（自由画布保存的、或手改过的）→ 提示并在自由画布打开。三份既有示例保留在自由画布的「示例框图」下拉里。

### 5.6 自由画布的去处

> **2026-09-10（D-060）作废**：自由画布已整体删除，本节记的是 2026-09-07 当时的设计。
> 框图页现在只有典型链路一种形态；解不成十一个槽位的框图改为**只读展示原文**（铁律 15），
> 编辑要回到手写 JSON 经 `PUT /api/v1/diagrams/{id}` 那条路。当前的操作说明见
> `docs/chain-view-guide.md`。


- 路由 `#/diagram/canvas`；典型链路工具条「展开为自由画布」把当前编译结果送过去；顶栏视图切换仍是三视图，画布不占 `Alt+` 数字键。
- **两个方向都通**：画布工具条有对称的「回到典型链路」（`data-action="open-chain"`）。按下时先 `parseChain(parse(text))`：解得开就直接回；解不开（画布里加过模板之外的节点）就当场说明「当前框图已不是典型链路的形状，回去只能新建一条链，新建那一下当前改动才会丢」并要用户确认，**不静默丢弃编辑**（铁律 15）；确认后落在典型链路页的「不是本模板」提示上，由用户显式点「新建典型链路」才真的换掉文档。
- 顶栏「框图」按钮与 `Alt+2` 回的是框图页的**默认形态**（典型链路）：`ui/navigate` 不带 `canvas` 时按 `false` 处理。要进画布走工具条按钮或直接用地址。
- **本节初稿的「单向，展开后不能折回」说的是文档的单向性**——画布里改过的框图可能解不回九个槽位——不是导航的单向性；实现时被照字面执行，2026-09-08 用户实测撞上（见 §11 修正表与 WORKLOG 同日条目）。
- `?dev=1` 下顶栏多一个「画布」直达入口；非开发者模式经工具条进入。
- 09 §6.3–§6.10 的全部约定与 `slice3-smoke` 继续有效，e2e 的入口改为直接开 `#/diagram/canvas`。
- 画布的「示例框图」下拉保留 `slice1 / slice2 / replay`，新增「典型链路（全合成）」等三项，便于对照。

### 5.7 结果页：检测识别与评价页签

页签改为 **信号 / 检测识别 / 评价 / 任务** 四个（`ResultsTab` 增两个值，hash 路由同步）。

| 页签 | 内容 | 数据 |
|---|---|---|
| 检测识别 | 上：时间线条带（三行：真值区间 / 检测突发 / 识别标签，同一时间轴，与信号页时间游标联动）；中：检测列表（帧或突发两种粒度切换，列 `t / 频段 / 统计量 / 门限 / 功率 / 信噪比 / 过载`）；下：识别列表（`t / 标签 / 后验 / Top-3 / 结论`）。点一行把信号页游标移到该时刻（09 §7.1 已承诺）；信号页「叠加检测」开关（09 §7.4）画命中帧的时频框，颜色按识别标签 | `detections / recognitions / truth` 三个 JSONL 端点，按视窗 `t0 / t1 / stride` 取 |
| 评价 | 指标卡一排（`Pd / Pfa / F1 / 发现时延 / 识别准确率`，四态徽标）；混淆矩阵热表；ROC 曲线（Canvas，工作点标出）；`?dev=1` 下叠一条解析预测（跨层算例 ①，§9）与「与参考实现逐值一致」的自检结果 | `metrics` 端点；未就绪显示「运行结束后生成」 |
| 任务 | 任务列表与摘要（09 §7.1）加 `metrics_summary` 四个数；「用同一种子重跑」 | `GET /api/v1/tasks` |

指标全部按 D-028 语义：界面只给数，不标「原型阶段验证值」；该限定写文档与模型卡（D-039 ③）。

**观测点的切换放在中栏顶上**（信号页签下的一排圆角页签，写全名如 `S4 主产品`），左栏的观测点列表保留作详情（节点、端口、产品）。原来只在左栏，而左栏可以收起——收着栏的人换不了观测点，会以为勾选没生效（2026-09-08 实测反馈）。**默认选中 S4**：它是 04 §5.2 的主产品，勾了 S1 / S2 之类的中间点不该把默认视图挪走。

### 5.8 探针与 e2e 断言

`window.__probe().app` 增 `chain{mode, scenarioId, siteId, emitterId, slots: {id: {variant, state, missing[]}}, taps[], plan: {fs_rf, fs_s4, checks: {name: ok}}, diagramId, dirty}` 与 `results{metrics: {pd, pfa, f1, accuracy, state}}`。新 e2e `tests/e2e/slice4-smoke.mjs`（§9）。`app.diagram` 探针保留给自由画布。

---

## 6. 数据格式与接口变更清单

### 6.1 框图文件：`template_ref`

`docs/diagram-format.md` §2 新增可选顶层字段：

| 字段 | 类型 | 说明 |
|---|---|---|
| `template_ref` | object | `{template_id: "chain-v1", mode: "synthetic" \| "replay" \| "mixed", version: 1}`；只作视图还原的线索，引擎与服务端不解释语义，但按白名单接受 |

改动点：`docs/schemas/diagram.schema.json`；`engine/src/diagram_json.cpp` 顶层键白名单与 `template_ref` 键白名单（三个键、值域检查、未知键拒）；`server/src/tasks/manager.ts` `checkShape` 放行；`web/src/diagram/doc.ts` `TOP_ORDER` 加一项（排在 `scenario_ref` 之后、`trace` 之前）。三处同规则（`docs/diagram-format.md` §2「未知键一律拒绝」）。

### 6.2 模板描述符

前端内置 `web/src/chain/templates/chain-v1.ts`（类型化，单测），并导出 JSON 到 `tests/golden/chain-template-v1.json` 作黄金基准（「已有条目不变」规则同组件目录）。描述符结构见附录 A：槽位序、每个槽位的变体（组件 `type`、固定节点 `id`、关键参数、与场景绑定的字段映射、派生参数映射）、三种模式下的槽位状态、观测点落点、隐含节点与连线规则。编译器 `compile(chainState, catalog, scenario) → DiagramDoc` 与反解 `parse(doc) → chainState | null` 是两个纯函数，对三份回归框图满足 `compile(parse(doc)) === doc`（逐字节）。

### 6.3 组件目录

| 变化 | 内容 |
|---|---|
| 新增组件 | `AntennaGain`、`ReceiverFrontEnd`、`AdcQuantizer`、`DDC`、`Channelizer`、`FeatureExtractor`、`TemplateClassifier`、`Evaluator` |
| 加参数 | `SceneEmitterSource.emit_at_tx_power`、`SceneBoundChannel.gain_mode`、`EnergyDetector.{noise_mode, noise_window_frames, merge_gap_frames, band_power_dBm}` |
| 新端口类型 | `RecognitionList`；`port_types` 6 → 7，`port_compat` 36 → 49 行（既有 36 行不变，新增行仍只允许同类型相连） |
| 可选输入 | `PortSpec` 增 `optional`（缺省 `false`，目录里为 `false` 时不输出该键，既有条目字节不变）；`Graph::validate` 对可选输入不报 `input_unconnected`；首批使用者 `AntennaGain.scene`、`Evaluator.scene` |
| 内部参数 | 新增 `library_path`（`TemplateClassifier`）、`out_dir` / `manifest_path` / `scenario_path`（`Evaluator`）、`polarization`（`AntennaGain`，自场景注入）；全库内部参数 11 → 16 处，08 §4.3 与 09 §6.6 的计数同步改 |
| 索引 | 观测点 `index.json` 增 `clipped_samples`；`task.json` 增 `metrics_summary` |

### 6.4 服务端点

新增：`GET/PUT/DELETE /api/v1/diagrams[/{id}]`（§5.5）；`GET /api/v1/results/{task}/{features | recognitions | truth}`（JSONL 时间窗读取器复用）；`GET /api/v1/results/{task}/metrics`（整文件）。写进 `docs/api-versions.md` §3.1d，版本号不变（只增不改）。

### 6.5 错误码

`docs/diagram-format.md` §4 增两个：`template`（`template_ref` 取值非法）、`port_optional`（可选输入未接时下游要求它——目前无此情形，预留）。19 → 21 个，08 附录 A.3 同步。

---

## 7. 基准变更（铁律 10）

| 基准 | 变化 | 处理 |
|---|---|---|
| `tests/golden/component-catalog.json` | 新增 8 条目、3 条目加参数、`port_types` / `port_compat` 增行 | 以 D-051 为决策依据更新一次，WORKLOG 记条目；更新前先跑「已有条目不变」比对确认只有上述差异 |
| `engine/tests/golden/{energy_detector, spectrum_welch, *.matlab}.json` | 无 | 新参数缺省即旧算法 |
| `tests/golden/{product-window, scenario-track-demo-01}.json` | 无 | 不动 `demo-01` |
| 示例框图 `slice1 / slice2 / replay` 与引擎夹具 | 无 | 原样保留，作回归夹具；`slice2` 继续用 `gain_mode = link_budget` 与手兑噪声 |
| 界面缺省框图 | 由 `slice1` 改为典型链路（全合成，`demo-02`） | `web/src/App.tsx`、`slice1-smoke` 的载入步骤改为在自由画布选示例 |
| 跨层算例 ① | 原实例不动；新增典型链路实例（§9） | `tests/regression/` |
| `slice3-smoke` | 入口改 `#/diagram/canvas` | 断言不变 |

「接收机噪声取代手兑噪声」的影响范围到此为止：没有任何既有产品文件的字节改变，D-050 担心的「顺手改基准」被「新参数缺省保旧行为」化解。

---

## 8. 实施步骤与切片重排

### 8.1 C 线：典型链路与检测识别评价（D-051）

| # | 步骤 | 交付物 | 验收 | 依赖 | 状态 |
|---|---|---|---|---|---|
| C-0 | 本方案定稿与回填 | 本报告；CLAUDE.md D-051 与状态表；06 §0.2 / §0.3 / §9B / §9E / §9G / §14；09 §17 修订记录；`docs/diagram-format.md` §10 | 文档互引一致；D-050 三问各有结论 | D-050 | [x] 2026-09-07 |
| C-1 | 格式与接口 | `template_ref`（三处白名单 + schema + `doc.ts`）；`RecognitionList` 端口类型；`PortSpec.optional`；`SceneParamFrame` 七字段（`geo/` 链路预算算离开 / 到达角与航向、`ScenarioSource` 填 `tx_on / tx_center_Hz`）；`BlockMeta.clip_count`；场景字段 `emission.polarization`；错误码两项；`docs/{diagram-format, scenario-format, component-catalog, display-products, api-versions}.md` 与 08 附录 | 目录黄金基准比对只有预期差异；`demo-01` 航迹与产品黄金基准零变化；`ctest` 与三套 e2e 无回归 | C-0 | [ ] |
| C-2 | 天线、前端、ADC 三件 + 既有两件加参 | `engine/include/cuav/components/{antenna, receiver}.h` + `engine/src/{antenna, receiver, adc}.cpp`；`SceneEmitterSource.emit_at_tx_power`、`SceneBoundChannel.gain_mode`；`models/{antenna, receiver}/README.md` 模型卡（依据、参数、适用范围、锚点） | §3.2 / §3.4 / §3.5 的解析锚点单测全过；噪声蒙特卡洛矩校验过；`AdcQuantizer` 与 M-4 的 MATLAB 量化参考 rel ≤ 1e-9；全合成链 S1 读数与链路预算三点偏差 ≤ 0.05 dB（沿用切片 ② 的带内功率法） | C-1 | [ ] |
| C-3 | 检测升级与落盘（P1-4c / P1-4d） | `EnergyDetector.noise_mode = sliding` 等四参；`Detection` 增字段；观察者 `on_detection`；`cuav_run` 写 `detections.jsonl`；Python 参考 `algos/reference/energy_detector.py` 加滑动模式 | `probe` 模式黄金基准不变；`sliding` 模式与 Python 参考逐帧一致；DS-6 背景片段的分半标定用 `sliding` 后虚警率不再随分半方式失效（D-026 的验证） | C-1 | [ ] |
| C-4 | 特征与识别（P1-4e） | `FeatureExtractor`、`TemplateClassifier`；`models/recognition/library-v1.json` 与模型卡；`features.jsonl` / `recognitions.jsonl`；Python 参考 `algos/reference/{features, classify}.py` | 四类合成波形（`tone / noise / burst / burst + hop`）在 `SNR ≥ 10 dB` 下识别到 `signal_role` 层准确率 ≥ 0.9（原型阶段验证值，样本 ≥ 40 突发）；特征与 Python 参考逐值一致 | C-3、G-6 | [ ] |
| C-5 | 真值与评价（P1-4f） | `Evaluator`；`truth.jsonl` / `metrics.json`；`algos/reference/evaluate.py`；`task.json.metrics_summary` | `metrics.json` 与 Python 参考逐值一致；跨层算例 ① 典型链路实例在容差内（§9） | C-3、C-4 | [ ] |
| C-6 | 服务端 | B-8 框图端点；`features / recognitions / truth / metrics` 端点；`data/diagrams/` 目录与 `.gitignore` 放行 | 服务测试：保存后 `GET` 逐字节相同、不改内容再保存是空操作、`--validate` 不过 400 不落盘；四个结果端点在假引擎夹具上可读 | C-1 | [ ] |
| C-7 | 典型链路视图（U-5） | `web/src/chain/{ChainView, SlotCard, TapDot, ExperimentSetup, FrequencyPlan}.tsx`、`templates/chain-v1.ts`、`compile.ts` / `parse.ts` / `plan.ts`（纯函数，单测）；路由与 `App.tsx` 初始框图；自由画布移到 `#/diagram/canvas` | 三种模式各编译出一份框图并通过 `--validate`；`compile(parse(doc)) === doc`；频率计划六项检查对拍表格里的算例；改站点参数带入且「已手改」不覆盖；引擎四元组错误落到卡片；`slice3-smoke` 改入口后全过 | C-1、C-6 | [ ] |
| C-8 | 场景 `demo-02` 与 G-6 | 宽带场景（`fs 10 MS/s`、`bw 8 MHz`、`nf 6`、两个辐射源：图传 `noise 2 MHz` 长发 + 遥控 `burst` 跳频）；G-6 活动时间线施加到 `SceneEmitterSource` 并写帧 | `demo-02` 通过场景一致性测试；跳频与突发起止在瀑布上与时间线差 ≤ 一帧；同种子逐字节复现 | G-2、G-3 | [ ] |
| C-9 | 结果页两个页签 | `web/src/results/{DetectionView, Timeline, EvaluationView, ConfusionMatrix, RocPlot}.tsx`；`ResultsTab` 增两值；信号页「叠加检测」开关 | 时间线三行与信号页游标联动；点检测行游标移动；指标卡四态；`?dev=1` 下解析预测叠加 | C-5、C-6、U-3 | [ ] |
| C-10 | DSP 厚化（M-2、M-3 的典型链路接入） | `DDC` 与 `Channelizer` 进槽位；`RxFilter` 串进 `rx_fe`；`fir_version` 枚举与频率计划的过渡带检查对齐 | 标准算例 5 / 7 / 8 从保存的典型链路运行通过；Coder 组件与 MATLAB 黄金向量 rel ≤ 1e-9 | M-2、M-3、C-7 | [ ] |
| C-11 | 收口 | `tests/regression/diagrams/chain-{synthetic, replay, mixed}.json`；`tests/e2e/slice4-smoke.mjs`；标准算例 1 / 2 / 3 / 6 / 9 / 10 / 11 从典型链路生成；~~`docs/diagram-canvas-guide.md` 改为两章~~（已由 D-060 于 2026-09-10 完成：改名 `chain-view-guide.md` 并全篇改写为框图页的操作说明，自由画布那一章随画布删除）；06 / CLAUDE 回填 | §9 的验收清单全过 | C-2 … C-10 | [ ] |

C-2、C-3、C-6 互不依赖，可并行；C-4 需要 G-6 的跳频才能验 `rc_hopping`，其余三类不等 G-6。

### 8.2 切片 ④ 重排

| 切片 | 步骤 | 切完在浏览器看到 | e2e 断言 |
|---|---|---|---|
| ④a 链路跑通 | C-0、C-1、C-2、C-6、C-7、C-8（`demo-02` 部分，G-6 可后） | 框图页是一条九环节链；选 `demo-02`、改噪声系数与天线增益、运行；S0 读发射功率、S1 读链路预算、S2 见噪声底、S3 见削顶计数；DDC 与信道化旁路 | `slice4-smoke`：三种模式编译过校验；S1 与链路预算偏差 ≤ 0.05 dB；S2 底噪与 `−174 + nf + 10·log10 fs` 偏差 ≤ 0.3 dB；保存后刷新再开仍是典型链路 |
| ④b 检测识别评价 | C-3、C-4、C-5、C-9、G-6 | 结果页出检测识别时间线与评价指标；图传与遥控两个目标分别识别为 `video_link` 与 `rc_hopping`；ROC 曲线 | `metrics.json` 与 Python 参考逐值一致；帧级 Pd 与解析预测差 ≤ 0.05；识别准确率 ≥ 0.9（原型阶段验证值） |
| ④c DSP 厚化 | M-2、M-3、C-10 | DDC 启用后 S4 采样率降为 `fs_rf / decim`、目标搬到零频附近；信道化启用后 S5 页签 | 标准算例 5 / 7 / 8；Coder 组件 rel ≤ 1e-9 |
| ④d 收口 | C-11、P1-7、P1-9a、U-4 余项（任务列表、数据中心）、规范定稿 | 数据中心「用于回放」直达典型链路；任务列表带指标摘要 | 首批标准算例从保存的典型链路运行通过；跨层算例 ① 两个实例在容差内 |

切片 ③ 的余项（U-3 检测叠加、对比观测点）并入 ④b（叠加检测随 C-9 做，对比观测点用 S1 / S4 两点）。

### 8.3 与既有步骤的对应

| 既有步骤 | 处置 |
|---|---|
| P1-0b 天线与接收机设计 | 由本方案 §3.2–§3.5 兑现，标 `[x]` |
| P1-3 models 余项（ADC、噪声注入） | 由 C-2 兑现 |
| P1-4c / P1-4d | 由 C-3 兑现；新增 P1-4e（特征与识别）= C-4、P1-4f（评价）= C-5 |
| P1-7 标准算例 | 输入改为「保存的典型链路」，C-11 |
| U-2 | 保留，降为高级模式（09 §6 加一段说明） |
| U-4 结果页与数据中心 | 检测识别与评价页签移入 C-9；U-4 只剩任务列表与数据中心 |
| U-5（新） | 典型链路视图 = C-7 |
| B-8（新） | 框图落盘端点 = C-6 |
| M-2 / M-3 | 不变；典型链路接入在 C-10 |
| G-6 | 不变；C-4 的 `rc_hopping` 与 C-5 的真值依赖它 |
| T-4 `TemplateSource` | 将来作 `tx` 槽位的第四个变体，不改链 |

---

## 9. 验证与验收

| 层 | 项 | 判据 |
|---|---|---|
| 单元 | §3.2 / §3.4 / §3.5 解析锚点；噪声矩校验；量化信噪比；IQ 不平衡镜像抑制比 | 锚点 rel ≤ 1e-9（确定量）；统计量在 §3.4 给的容差内 |
| 单元 | 编译器与反解 | 三份回归框图 `compile(parse(doc)) === doc` 逐字节；频率计划六项检查对拍表格算例 |
| 集成 | 三种模式的典型链路经 `cuav_run --validate` 与 `--run` | 全过；同种子产品逐字节复现 |
| 集成 | 全合成链的电平链 | S0 = `tx_power_dBm`（±0.05 dB）；S1 = 链路预算（带内功率法，±0.05 dB）；S2 底噪 = `−174 + nf + 10·log10 fs`（±0.3 dB，噪声统计量）；S3 削顶计数与解析值一致 |
| 跨层算例 ① 典型链路实例 | `cw_beacon` 目标、`FreeSpaceChannel` 定距、`nf` 生成噪声、`sliding` 检测；EM-S-02 随机型 / 确定型 Pd 公式（D-026）以 `N0 = −174 + nf` 与链路预算的 SNR 为输入 vs `metrics.json` 的帧级 Pd | 差 ≤ 0.05（04 §16.3 建议容差 0.05–0.10 取严者） |
| 参考对拍 | `detections / features / recognitions / metrics` 与 `algos/reference/` 四个脚本 | 逐值一致（浮点按显式循环累加） |
| 端到端 | `slice4-smoke`（§8.2 四个切片的断言合并） | 全过；既有 `scene / slice1 / slice2 / slice3` 无回归 |
| 标准算例 | 04 §15.2 的 1、2、3、5、6、7、8、9、10、11 项从保存的典型链路生成 | 全过（4、12 项随后续） |
| 性能（原型阶段验证值） | `demo-02` 全合成 20 s、`fs_rf 10 MS/s`、DDC 启用 | 记录墙钟与实时因子；若实时因子 < 0.2 即触发 08 §15 ① 的多线程评估，不在本线解决 |

---

## 10. 不做与风险

**不做**（本线范围外，已有归属或明确后置）：

- 用户自定义模板、子系统封装、自由画布的旁路 / 替换（04 §8.2，P2）；
- 阵列、多站、测向定位（05 P0–P2）；
- 相位噪声、增益压缩、AGC 动态、接收滤波以外的模拟非理想（M-5，P3）；查表方向图（P3）；
- 学习型识别、型号级识别、时序证据融合（EM-S-04 §10.14，P3；T 线）；
- 参数扫描与批量运行（试验中心，P2）：ROC 用门限扫描得到，不靠批量重跑；
- 自由画布的任何新功能；`iq` 产品与散点（D-040 ③）。

**风险**：

| 风险 | 表现 | 对策 |
|---|---|---|
| 宽带采样率下的吞吐 | `10 MS/s` 以上单线程实时因子低，演示等待 | 缺省 `fs_rf 10 MS/s`、时长 20 s；测得实时因子后决定是否启动 08 §15 ① |
| Coder 产物晚于界面 | DDC / 信道化槽位长期旁路，S4 名不副实 | 旁路状态在页头与 `task.json` 明示；M-2 排在 ④c 而不是最后 |
| 回放真值粗 | 公开数据集只有片级标签，突发级指标对回放模式意义有限 | 回放模式的评价只报帧级与识别；文档标明；甲方数据到货后补 |
| 模板库 v1 是假定值 | 识别准确率只对合成波形成立 | 库文件来源标 `assumed`，模型卡写适用范围；P3 标定升版 |
| 既有 e2e 与文档大面积改入口 | 遗漏处让测试失败 | C-7 一次改完 `slice1 / slice3` 入口，C-11 全量跑 |
| 参数联动的「已手改」状态 | 用户改了站点又改了参数，来源不清 | 面板显示来源与「已手改」角标，重置按钮回带出值 |

---

## 11. 决策与修正记录

**D-051（2026-09-07）** 全文见 CLAUDE.md 决策日志，要点即 §0 十条。**D-050 三问**的结论：① 解析式方向图 + 指向 + 五档极化损耗，查表留 P3（§3.2）；② 典型链路里噪声由噪声系数生成，取代手兑噪声，既有示例与基准不动（§3.4、§7）；③ 削顶是数据标记，比例超阈值才降级（§3.5）。

对既有文档的修正（按 06 §14 体例）：

| 文档 | 原文 | 修正 |
|---|---|---|
| 06 §9B U-2、§9E 切片 ③ | 「六类组件库拖到画布」是框图页的主形态 | 框图页主形态改为典型链路视图；画布降为高级模式，入口 `#/diagram/canvas` |
| 06 §9B U-4 | 结果页含检测页签 | 检测识别与评价页签移入 C-9；U-4 余任务列表与数据中心 |
| 06 §9E 切片 ④ | 一行 | 重排为 ④a–④d（§8.2） |
| 09 §1.2、§1.3、§6.1 | 「新建 ▾」三个模板、空状态三张模板卡片 | 三个模板 = 典型链路的三种模式，成为框图页本身；§6 开头加说明段，§6.11 的「模板」限定为用户自存模板 |
| 09 §7.1 | 页签 信号 / 检测 / 任务 | 信号 / 检测识别 / 评价 / 任务 |
| `docs/diagram-format.md` §2、§4、§10 | 无 `template_ref`；19 个错误码 | 加 `template_ref`；21 个错误码；§10 待写项改为 C-1 |
| `docs/diagram-canvas-guide.md` | 全篇按自由画布写 | ~~C-11 改为两章~~ → 2026-09-10 由 D-060 改名 `chain-view-guide.md` 并全篇改写为框图页的操作说明 |
| 本报告 §5.1 版式图与 §5.2（2026-09-08 补） | 版式图写「圆点：观测点 S0…S5」；实现做成链条下方一行复选框，只写编号、名称藏在悬停提示里；一行卡片吊在中栏顶上 | **观测点保持链条下方独立一行（链条结构不动），但写全名**；中栏链条区改为占满高度的面板并纵向居中、九个环节按 3 × 3 排（行距 56 px、卡片保持 176 × 128、连接件拉伸铺满列宽）；连接箭头挪到卡片之后以消除换行时的孤立箭头；结果页观测点切换加到中栏顶上、默认选 S4；勾选与当前任务不一致时明说要重新运行。见 §5.2、§5.7 |
| 本报告 §0 表第 4 行、§5.6（2026-09-08 补） | 「展开为自由画布（单向，展开后不能折回）」 | 那句话说的是**文档**的单向性，不是导航；实现照字面执行，导致进了画布只剩浏览器后退键。改为两个方向都通：画布工具条加「回到典型链路」，解不开时先问不静默丢改动，`Alt+2` 与顶栏「框图」回默认形态 |
| 08 §4.3、附录 A.3、附录 B | 内部参数 11 处；19 个错误码；12 个组件 | 16 处；21 个；20 个组件（随 C-1 / C-2 / C-4 / C-5 / C-10 分批回填） |

### 补：D-053 对本方案的修正（2026-09-09）

11 号报告在本方案之上加了源与站两个实例维度，因此对本方案作以下修正（细节见 11 §11.2）：

| 处 | 修正 |
|---|---|
| §2.1 | 槽位 9 → 11（加「测向」与「多站定位」）；引入源 / 站 / 链路三个实例维度；隐含节点加 `sup` 与 `toa`；第 9 行「测向（后置 P1 / P2）」销项 |
| §3.2 | `AntennaGain.scene_bindable` 由 `true` 改为 `false`（与 `engine/src/antenna.cpp:68` 的实际值一致，本方案原文写错） |
| §4.2 | `on_detection` 加 `node_id`；`detections.jsonl` 每行加 `node_id` 与 `site_id` |
| §4.5 | `Evaluator` 由「绑一个源」改为「绑一个站、吃该站全部链路帧」；`truth.jsonl` 加 `site_id` / `emitter_id`；`metrics.json` 按 `site_id` 分节并预留 `localization{}` |
| §4.6 | 产品表加 `bearings.jsonl` 与 `positions.jsonl` |
| §8.2 | 切片 ④ 与 ④b 之间插入切片 ⑥（⑥a 多源叠加 → ⑥b 多站测向与 AOA → ⑥c TDOA 与融合），见 06 §9E / §9H |
| §5.2 | 中栏版式由九个环节的 3 × 3 改为**每行四张的蛇形**（2026-09-09 用户改定）：槽位到十一个之后 3 列会排成四行、末行只剩两张；四列排成 4 + 4 + 3，且双数行从右往左，一行读到头下一张就在正下方。卡片尺寸与行距 56 px 不变；连接件改为绝对定位到两张卡片之间（原来是 flex 撑开、跟在卡片之后），行末换成向下的转折。排布由纯函数 `snakePos` 给出。实测 1680 / 1920 / 2048 / 2560 四种视口宽度都不横向溢出，连接件贴边与转折对中均为 0 px |

**这些修正只改 C-3 / C-5 的设计，不并入 D-053 的实现**：C-3 与 C-5 尚未开工，按修正后的字段设计即可，无既有代码需改。

### 补：D-054 对本方案的修正（2026-09-09）

用户指示框图页要成为「场景内实体电子设备参数配置的主界面」，据此对本方案再作三处修正（细节见 CLAUDE.md D-054）：

| 处 | 修正 |
|---|---|
| §2.1 | 槽位表新增 `owner` 列（参数归属维度），与 `per`（实例化维度）**分开**：`tx` / `tx_ant` 归源，`rx_ant` 及其后归站，`ch` 与 `loc` 全图共用。`tx_ant` / `rx_ant` 两者不一致——节点按链路展开 N×K 份，参数按源 / 按站取 |
| §5.1 | 中栏工具条之下新增**实体选择行**（`.chain-entity-bar`）：「无人机 ▾」「侦测站 ▾」两个下拉，候选只列参与运行的实体，条目写「id · 名称 · 型号」。它**只切换配置焦点**，不改运行范围。右栏面板标明本槽位的参数归属；传播信道与多站定位写「全图共用，不随上方的实体选择变化」，且不给「单独设置」的入口 |
| §3.2 / §3.3 | 本方案把 `AntennaGain.gain_dBi` 与 `ReceiverFrontEnd.nf_dB` 设计成用户参数，与场景里的同名字段并存。实施后发现这是**两处真理源**：`demo-03` 的 `uav-3` 场景写 0 dBi 而链路强制 2 dBi，场景值被静默忽略、链路预算高估 2 dB。改为由场景**逐实体**带出（`FROM_SCENE` 表），在框图页仍可编辑但写的是场景文件。这同时兑现了 11 §5.2 第 449 行的承诺 |

---


## 附录 A. 模板描述符 `chain-v1`（骨架）

```json
{
  "schema_version": "cuav-chain-template/1",
  "template_id": "chain-v1",
  "version": 1,
  "slots": [
    { "id": "tx", "name": "辐射源", "variants": {
        "scene": { "type": "SceneEmitterSource", "node": "tx", "bind": "emitter",
                   "fixed": { "emit_at_tx_power": true }, "derive": { "sample_rate_Hz": "fs_rf", "center_frequency_Hz": "f_tx" },
                   "summary": ["center_frequency_Hz", "sample_rate_Hz"] },
        "replay": { "type": "FileReplaySource", "node": "tx", "summary": ["data_id"] } } },
    { "id": "tx_ant", "name": "发射天线", "variants": { "default": { "type": "AntennaGain", "node": "tx_ant", "bind": "emitter",
        "fixed": { "role": "tx" }, "from_scene": { "gain_dBi": "emission.antenna_gain_dBi" },
        "summary": ["pattern", "gain_dBi", "polarization"] } } },
    { "id": "ch", "name": "传播信道", "variants": {
        "scene": { "type": "SceneBoundChannel", "node": "ch", "bind": "emitter", "fixed": { "gain_mode": "path_loss_only" },
                   "summary": ["delay_mode", "apply_doppler"] },
        "free":  { "type": "FreeSpaceChannel", "node": "ch", "summary": ["distance_m", "frequency_Hz"] } } },
    { "id": "rx_ant", "name": "接收天线", "variants": { "default": { "type": "AntennaGain", "node": "rx_ant", "bind": "site",
        "fixed": { "role": "rx" }, "from_scene": { "gain_dBi": "antenna.gain_dBi" }, "summary": ["pattern", "gain_dBi", "polarization"] } } },
    { "id": "rx_fe", "name": "接收机前端", "variants": { "default": { "type": "ReceiverFrontEnd", "node": "rx_fe",
        "from_scene": { "nf_dB": "receiver.nf_dB" }, "summary": ["nf_dB", "gain_dB", "lo_offset_Hz"] } } },
    { "id": "adc", "name": "ADC", "variants": { "default": { "type": "AdcQuantizer", "node": "adc", "summary": ["bits", "full_scale_dBm"] } } },
    { "id": "ddc", "name": "DDC", "bypassable": true, "variants": { "default": { "type": "DDC", "node": "ddc",
        "derive": { "f_shift_Hz": "f_tx - f_rx" }, "summary": ["f_shift_Hz", "decim", "fir_version"] } } },
    { "id": "chan", "name": "信道化", "bypassable": true, "default_bypass": true, "variants": { "default": { "type": "Channelizer", "node": "chan",
        "summary": ["channels", "select_channel"] } } },
    { "id": "det", "name": "检测识别评价", "nodes": [
        { "type": "EnergyDetector", "node": "det", "derive": { "band_lo_Hz": "-0.45 * fs_s4", "band_hi_Hz": "0.45 * fs_s4" }, "fixed": { "noise_mode": "sliding" } },
        { "type": "FeatureExtractor", "node": "feat" },
        { "type": "TemplateClassifier", "node": "rec" },
        { "type": "Evaluator", "node": "eval", "bind": "emitter", "derive": { "truth_source": "mode" } } ] }
  ],
  "implicit": {
    "scn": { "type": "ScenarioSource", "bind": "site", "modes": ["synthetic", "mixed"], "derive": { "sample_rate_Hz": "fs_rf" } },
    "mix": { "type": "AddMixer", "modes": ["mixed"] }
  },
  "modes": {
    "synthetic": { "tx": "scene", "ch": "scene", "not_applicable": [] },
    "replay":    { "tx": "replay", "not_applicable": ["tx_ant", "ch", "rx_ant", "rx_fe", "adc", "ddc"] },
    "mixed":     { "tx": ["scene", "replay"], "ch": "scene", "not_applicable": [], "fixed": { "rx_fe.noise_mode": "none" } }
  },
  "taps": { "s0": "tx.out", "s1": "rx_ant.out", "s2": "rx_fe.out", "s3": "adc.out", "s4": "ddc.out|adc.out|mix.out|tx.out", "s5": "chan.out" },
  "edges": "按 slots 顺序串 IQStream；scn 的 link:<emitter> 接 ch.scene / tx_ant.scene / rx_ant.scene / eval.scene；det.out → feat.det，feat.out → rec.in，det.out → eval.det，rec.out → eval.rec"
}
```

## 附录 B. 全合成模式编译后的框图（`demo-02`，DDC 启用、信道化旁路）

```json
{
  "schema_version": "cuav-diagram/1",
  "diagram_id": "chain-synthetic-demo-02",
  "name": "典型链路 · 全合成 · demo-02",
  "nodes": [
    { "id": "scn",    "type": "ScenarioSource",     "params": { "sample_rate_Hz": 10000000, "update_rate_Hz": 20 }, "scene_binding": { "scenario_id": "demo-02", "site_id": "site-1" } },
    { "id": "tx",     "type": "SceneEmitterSource", "params": { "sample_rate_Hz": 10000000, "center_frequency_Hz": 2442000000, "emit_at_tx_power": true }, "scene_binding": { "scenario_id": "demo-02", "entity_id": "uav-1" } },
    { "id": "tx_ant", "type": "AntennaGain",        "params": { "role": "tx", "gain_dBi": 2 }, "scene_binding": { "scenario_id": "demo-02", "entity_id": "uav-1" } },
    { "id": "ch",     "type": "SceneBoundChannel",  "params": { "gain_mode": "path_loss_only" }, "scene_binding": { "scenario_id": "demo-02", "entity_id": "uav-1" } },
    { "id": "rx_ant", "type": "AntennaGain",        "params": { "role": "rx", "gain_dBi": 3 }, "scene_binding": { "scenario_id": "demo-02", "site_id": "site-1" } },
    { "id": "rx_fe",  "type": "ReceiverFrontEnd",   "params": { "nf_dB": 6 } },
    { "id": "adc",    "type": "AdcQuantizer",       "params": { "full_scale_dBm": -20 } },
    { "id": "ddc",    "type": "DDC",                "params": { "f_shift_Hz": 2000000, "decim": 4 } },
    { "id": "det",    "type": "EnergyDetector",     "params": { "band_lo_Hz": -1125000, "band_hi_Hz": 1125000, "noise_mode": "sliding" } },
    { "id": "feat",   "type": "FeatureExtractor",   "params": {} },
    { "id": "rec",    "type": "TemplateClassifier", "params": {} },
    { "id": "eval",   "type": "Evaluator",          "params": { "truth_source": "scenario" }, "scene_binding": { "scenario_id": "demo-02", "entity_id": "uav-1" } }
  ],
  "edges": [
    { "id": "e1", "from": { "node": "tx",     "port": "out" },        "to": { "node": "tx_ant", "port": "in" } },
    { "id": "e2", "from": { "node": "scn",    "port": "link:uav-1" }, "to": { "node": "tx_ant", "port": "scene" } },
    { "id": "e3", "from": { "node": "tx_ant", "port": "out" },        "to": { "node": "ch",     "port": "in" } },
    { "id": "e4", "from": { "node": "scn",    "port": "link:uav-1" }, "to": { "node": "ch",     "port": "scene" } },
    { "id": "e5", "from": { "node": "ch",     "port": "out" },        "to": { "node": "rx_ant", "port": "in" } },
    { "id": "e6", "from": { "node": "scn",    "port": "link:uav-1" }, "to": { "node": "rx_ant", "port": "scene" } },
    { "id": "e7", "from": { "node": "rx_ant", "port": "out" },        "to": { "node": "rx_fe",  "port": "in" } },
    { "id": "e8", "from": { "node": "rx_fe",  "port": "out" },        "to": { "node": "adc",    "port": "in" } },
    { "id": "e9", "from": { "node": "adc",    "port": "out" },        "to": { "node": "ddc",    "port": "in" } },
    { "id": "e10", "from": { "node": "ddc",   "port": "out" },        "to": { "node": "det",    "port": "in" } },
    { "id": "e11", "from": { "node": "ddc",   "port": "out" },        "to": { "node": "feat",   "port": "iq" } },
    { "id": "e12", "from": { "node": "det",   "port": "out" },        "to": { "node": "feat",   "port": "det" } },
    { "id": "e13", "from": { "node": "feat",  "port": "out" },        "to": { "node": "rec",    "port": "in" } },
    { "id": "e14", "from": { "node": "det",   "port": "out" },        "to": { "node": "eval",   "port": "det" } },
    { "id": "e15", "from": { "node": "rec",   "port": "out" },        "to": { "node": "eval",   "port": "rec" } },
    { "id": "e16", "from": { "node": "scn",   "port": "link:uav-1" }, "to": { "node": "eval",   "port": "scene" } }
  ],
  "observation_points": [
    { "id": "s1", "node": "rx_ant", "port": "out", "products": ["spectrum", "envelope"], "label": "S1 接收天线端" },
    { "id": "s4", "node": "ddc",    "port": "out", "products": ["spectrum", "envelope"], "label": "S4 主产品" }
  ],
  "run": { "seed": 20260907, "duration_s": 20, "block_size": 100000, "time_basis": "LogicalSim" },
  "scenario_ref": { "scenario_id": "demo-02", "sha256": "<服务端回传>" },
  "template_ref": { "template_id": "chain-v1", "mode": "synthetic", "version": 1 }
}
```

注：一个输出口接多条边（`ddc.out` 同时进 `det` 与 `feat`，`det.out` 同时进 `feat` 与 `eval`）是现行连线规则允许的（只限制一个输入口一条边）。`s0 / s2 / s3 / s5` 观测点缺省不勾。

## 附录 C. `metrics.json`（`cuav-metrics/1`）

```json
{
  "schema_version": "cuav-metrics/1",
  "task_id": "…",
  "truth_source": "scenario",
  "frames": { "total": 0, "truth_on": 0, "tp": 0, "fp": 0, "fn": 0, "tn": 0, "pd": 0.0, "pfa": 0.0, "precision": 0.0, "recall": 0.0, "f1": 0.0 },
  "segments": { "truth": 0, "detected": 0, "matched": 0, "pd_segment": 0.0, "false_segments": 0, "detect_delay_s": { "mean": 0.0, "max": 0.0 } },
  "roc": { "working_point": { "threshold": 0.0, "pd": 0.0, "pfa": 0.0 }, "points": [ { "threshold": 0.0, "pd": 0.0, "pfa": 0.0 } ] },
  "recognition": { "labels": ["video_link", "telemetry_burst", "rc_hopping", "cw_beacon", "unknown"], "confusion": [[0]], "accuracy": 0.0,
                   "per_class": [ { "label": "video_link", "precision": 0.0, "recall": 0.0, "f1": 0.0, "support": 0 } ], "unknown_rate": 0.0, "ambiguous_rate": 0.0 },
  "quality": { "overload_frames": 0, "noise_stale_frames": 0 },
  "state": "valid", "reasons": [],
  "trace": { "model_id": "eval-baseline", "model_version": "1", "model_layer": "M2", "model_level": "E2", "credibility": "V2", "parameter_version": "library-v1", "trace_id": "…" }
}
```

`detections.jsonl`、`features.jsonl`、`recognitions.jsonl`、`truth.jsonl` 的行格式见 §4.2–§4.5；四个文件都遵守 `docs/display-products.md` §3.4 的两条硬契约（每行自带 `t_s`、以换行结尾）。

## 附录 D. 模板库 v1（`models/recognition/library-v1.json`，取值为假定值）

| 标签 `signal_role` | `bandwidth_Hz` | `duration_s` | `duty` | `spectral_flatness` | `hop_from_prev_Hz` | `interval_from_prev_s` | 权重要点 |
|---|---|---|---|---|---|---|---|
| `video_link` | [1e6, 40e6] | [0.5, ∞) | [0.8, 1] | [0.6, 1] | 不计 | 不计 | 带宽与占用为主 |
| `telemetry_burst` | [20e3, 500e3] | [0.5e-3, 50e-3] | [0.02, 0.6] | [0.3, 1] | [0, 20e3] | [5e-3, 1] | 周期性与窄带 |
| `rc_hopping` | [20e3, 2e6] | [0.5e-3, 20e-3] | [0.05, 0.8] | [0.3, 1] | [200e3, ∞) | [1e-3, 0.1] | 跳频差为主 |
| `cw_beacon` | [0, 20e3] | [0.5, ∞) | [0.8, 1] | [0, 0.2] | 不计 | 不计 | 平坦度低、带宽约等于分辨率 |

区间外距离的尺度 `s_i` 取区间宽度的一半（对数域取 0.3），`unknown_distance = 4`。文件头写来源 `assumed`、`library_version = v1`、对应的场景波形映射（§4.5）。

## 附录 E. 依据条款索引

04 §3.2、§4.3、§5.1、§5.2、§5.4、§7.1–§7.9、§8.1、§8.2、§8.4、§9.3、§11.4、§12.1、§13.3、§15.2、§16.3、附录 A；01 §3.1、§8.3、§9.2；02 §6.1、§8.5；03 §11.6、§11.7；05 §5.2、P0 端口；06 §1.3、§9A–§9F、§13、§14；08 §4.3、§8、§9.5、§9.6、§11.1、§13、§15；09 §1.2、§1.3、§6.1–§6.11、§7.1、§7.4、§10、§11、§13；概念模型 EM-B-07 §10.1 / §10.3 / §11.3 / §15、EM-B-11 §10.1 / §15、EM-B-12 §10.1 / §11.3、EM-S-02 §10.6 / §10.7 / §10.18 / §11.2 / §11.3、EM-S-03 §10.10 / §11.2 / §11.3、EM-S-04 §10.2 / §10.5 / §10.6 / §10.9 / §11.2 / §11.3；决策 D-011、D-013、D-026、D-028、D-036、D-037、D-039、D-040、D-042、D-045、D-046、D-047、D-049、D-050；铁律 1、4、5、8、9、10、14、15。
