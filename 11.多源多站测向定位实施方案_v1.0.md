# 11. 多源多站测向定位实施方案

> **版本** v1.0，2026-09-09。**定位**：把「场景里有多个辐射源、单站要出测向、多站要出交叉定位与时差定位」这件事在框图里表达出来并落到第一切片；插在切片 ④b（C-3 检测升级）之前，作为独立的 **L 线**。**依据**：01 §3（三维正交分级）、§5.2、§6.2（模型的输入可以是真值）、§6.6、§8（跨层一致性）；04 §5.1（端到端对象）、§5.2（真值只进评价器）、§5.4（S0–S6）、§7.4（传播）、§7.9（检测识别与评价）、§12.1（首期单站单通道）、§21（首期基线）、附录 A；05 §3.1–§3.2（P1 测向、P2 多站定位，且「P2 可以先用纯软件多站数据开展」）、§6.2.1–§6.2.3（端口 / 时间 / 坐标抽象与四态降级）、§7、§8、§16.3、§17.3、§19.1、§21.4；02 §6.2、§7.6、§7.7、§15.3；概念模型 EM-S-05（单站测向）、EM-S-06（多站交叉定位）、EM-S-07（多站时差定位）、EM-S-08（频差定位，本期不做）；08 报告 §8、§9、§15；09 报告 §6、§7；10 报告 §2、§4、§5；决策 D-009、D-013、D-030、D-033、D-036、D-037、D-042、D-045、D-046、D-047、D-049、D-050、D-051、D-052；铁律 1、3、4、5、8、9、10、13、15。**本方案的结论记为决策 D-053。**
>
> **用户提问（2026-09-09）**：「场景中的辐射源可能有多个；单个的侦测站要做到信号的识别与测向；多个测向站可以对同一个信号源做交叉定位或者时差定位。这些在框图里面如何体现、如何设置？」
>
> **用户拍板（2026-09-09，四个岔路）**：① 交付范围 = **设计 + 纯软件第一切片**（本报告 + D-053，同时落地多源叠加、单站测向、多站交叉与时差定位、测向线与误差椭圆上图），阵列 IQ 与真实时钟同步仍留 05 的 P1 / P2；② 测向层次 = **M2 效应模型**（按误差预算给方位量测，不做阵列相位估计）；③ 多源表达 = **前四环节按源分支、接收天线后叠加**成一路 IQ；④ 多站表达 = **典型链路加站点维度 + 尾部两个槽位**，一条任务跑 K 站，融合节点消费 K 路报告。
>
> 本方案只定语义、参数、格式、界面与步骤，不含实现记录；实现中核实到的偏差按 06 §14 体例另记修正。量化数字一律「原型阶段验证值」（D-028）。

---

## 0. 一页结论

| # | 问题 | 结论 |
|---|---|---|
| 1 | 多个辐射源在框图里怎么体现 | **前四个环节按源分支，接收天线之后叠加成一路**。辐射源、发射天线、传播信道、接收天线各按源实例化 N 份，新组件 `Superposition` 把 N 路复基带在接收天线端相加，此后（前端 → ADC → DDC → 信道化 → 检测）仍是**一路** IQ。这与真实接收机一致：天线口面上是电磁场的线性叠加，接收机只有一条通道。视图上这四张卡片带 `×N` 角标，不是四叠卡片 |
| 2 | 多个站在框图里怎么体现 | **典型链路加一个站点维度**。选中 K 个站，接收天线之后的每个环节各实例化 K 份，一次任务把 K 条接收链一起跑完；末尾加两个新槽位——每站一个「测向」，全局一个「多站定位」。不选站就是 K=1，画面与今天一模一样 |
| 3 | 测向做到什么层次 | **M2 效应级**（EM-S-05 的 E2 档）：从链路参数帧取真值方位与电平，按噪声系数算信噪比，按七分量误差预算得 σ_θ，抽一个高斯样本作为量测方位，给出测向质量 DF-Q 分档。它是**测向算法的统计模型**，不是被测的测向算法；05 P1 的阵列估计器将来用**同一个端口**替换它，框图形状不变 |
| 4 | 取真值合法吗 | 合法，但必须声明。01 §6.2 允许模型的输入是真值；04 §5.2「真值只进评价器」约束的是**被测的 M3 算法**（检测器、识别器不得看真值）。本线的测向与到达时间估计是 M2 效应模型，输出一律 `model_layer = M2`、`credibility ≤ V2`，每行带 `truth_consumed = true`，评价时不得把它们的指标当作「算法性能」（§1.3） |
| 5 | 交叉定位与时差定位怎么设 | **一个组件、一个下拉**：`MultiSiteLocator` 的 `method ∈ {aoa, tdoa, aoa_tdoa}`。AOA 吃各站的 `BearingReport`，TDOA 吃各站的 `ToaReport`（由隐含节点 `ToaEstimator` 产出），`aoa_tdoa` 两者都吃并用 AOA 解作 TDOA 的迭代初值。「关」= 槽位旁路 |
| 6 | 新增哪些端口 | 三种：`BearingReport`、`ToaReport`、`PositionReport`。`BearingReport` / `PositionReport` 是 05 P0 名单里的；**`ToaReport` 是名单外的补充**——05 假定 TDOA 直接消费 `MultiSiteIQSet`（阵列 / 多站 IQ），没有给「站级到达时间量测」留口，而纯软件切片必须有这一层。端口兼容仍是纯对角（同类型才连），`port_compat` 由 49 行变 100 行 |
| 7 | 多路输入怎么接 | **固定的可选输入口**，不做动态端口：`Superposition` 有 `in1..in8`，`DirectionFinder` 有 `scene1..scene8`，`MultiSiteLocator` 有 `b1..b8` 与 `t1..t8`，全部 `optional = true`。零格式改动，N、K ≤ 8 对演示足够；报告与帧自带 `site_id / emitter_id / link_id`，身份不靠端口序号 |
| 8 | 框图格式改不改 | **不改**。`template_ref.version` 不升——v1 文档是 v2 槽位表的真子集（N=K=1 时新槽位缺席、新后缀不出现），既有框图逐字节反解、逐字节编译回去。实例后缀用双下划线 `__`（节点 id 正则 `[a-z0-9_-]{1,64}` 里 `@` / `:` 都不合法） |
| 9 | 参数怎么设 | **按槽位共享，实例差异只来自场景**。九个站的接收机不是九组参数，而是同一组参数在九个位置上跑；站址、站钟、天线朝向、发射功率这些差异全部在场景文件里。工具条给「查看实例：源 ▾ 站 ▾」，只影响派生量的显示 |
| 10 | 界面上看到什么 | 场景地图上：每站一条测向线（±2σ 楔形），AOA 与 TDOA 两色的定位点与 2σ 误差椭圆；左栏辐射源表单里每站一行「方位 / σ / DF-Q」加一个「定位」块。叠加层是 Canvas（`display-route.md` §3 冻结的语义在此第一次有生产者）。**2σ 椭圆的二维置信是 86.5%，不是 95%**——纠正 em-demo 注释里的说法 |
| 11 | 移植哪些既有代码 | emcore 的 `locate_aoa` / `locate_tdoa` / `direction_finding` / `core/geo` 与四份黄金基准移到 `geo/`。求解器改成**平面坐标接口**（吃米，不吃经纬度），111320 常数投影只保留在 `geo::legacy` 命名空间里守 golden（D-009「移植模块守原常数，新代码用严格 ENU」的落地方式）；引擎侧用 `default_geodesy().to_enu()` 喂平面点 |
| 12 | 基准怎么变 | 组件目录黄金基准重生成一次（新增 4 件组件、`port_compat` 49 → 100 行、既有条目加内部参数 `site_id`）；**产品黄金基准零变化**；`demo-01` 场景与其航迹基准一字不动；新增 `demo-03` 场景与四份移植 golden。目录基准的差异逐项列在 §7.3，按铁律 10 走正式变更 |
| — | **不做** | 阵列测向（MUSIC / 相位干涉，`ArrayIQStream`，P1）；IQ 相关的真到达时间估计（`MultiSiteIQSet`，P2）；真实站钟与 GNSS 驯服、`DeviceStatus`（P2）；频差定位 EM-S-08；跨站关联 EM-S-11 与多假设；`TrackReport` 航迹滤波；NLOS 几何驱动的多径测向误差（等 D3 遮挡接入）；用户自定义站点组合模板（P2） |

---

## 1. 依据、现状与真值边界

### 1.1 上游依据

| 条款 | 原文要点 | 在本方案里的落点 |
|---|---|---|
| 04 §5.1 | 端到端对象的末环节是「信道化、检测、识别、**测向或定位**」 | 测向与定位本来就在 04 的链上，只是排在最后；本方案把它从「一句话」落成两个槽位 |
| 04 §5.2 | 「真值只进评价器，不进被测算法」 | §1.3 逐字辨析：约束对象是被测的 M3 算法；M2 效应模型取真值合法且必须声明 |
| 04 §12.1、§21 | 首期单站、单通道 | 本方案**只解锁多站的软件表达**（一条任务跑 K 条接收链），不涉及多通道硬件与阵列采样；单站单通道的 IQ 语义（铁律 4）一字未动 |
| 04 §3.4 | 不做事项 | 阵列与真实同步仍不做，见 §10 |
| 05 §3.1–§3.2 | 测向归 P1、多站定位归 P2；**「P2 可以先用纯软件多站数据开展」** | 本方案就是这句话的兑现：纯软件多站数据 = 一条任务里 K 条链的参数帧 |
| 05 §6.2.1 | P0 端口抽象 `BearingReport / PositionReport / TrackReport` | `BearingReport` 与 `PositionReport` 从「命名预留」变成有生产者；`TrackReport` 仍只预留 |
| 05 §6.2.2 | 四重时间不得混用；失锁、补零、重对齐不得当作连续数据 | `sites[].clock.sync_state ∈ {locked, holdover, unsynced}` 与逐行的 `time_quality` 把这条落到每一行报告上（§4.2） |
| 05 §6.2.3 | 坐标产物记录坐标版本与转换参数 | `PositionReport` 带 `crs / coord_version / enu_origin` 三件（§4.3） |
| 01 §6.2 | 模型的输入可以是真值、可以是上游模型输出、可以是实测 | §1.3 的合法性依据 |
| 01 §3 | M / E / V 三维正交同时声明 | 四个新组件全部声明 `model_layer = M2`、`model_level = E2`、`credibility = V2`（未经实测校准） |
| 01 §8 | 跨层一致性：M3 归约与 M1/M2 直接计算的偏差超容差 = 发现 | §9 的蒙特卡洛判据就是这条的执行形式：经验 σ 必须对得上预算 σ |
| EM-S-05 §10.14 | 单站测向误差预算分量 | §3.2 的七分量表 |
| EM-S-06 §10.4–§10.7 | 测向线加权最小二乘交汇、几何质量按最大张角分级 | §3.4 与移植的 `locate_aoa` |
| EM-S-07 §10.3–§10.5、§10.8–§10.9 | 到达时间误差预算、参考站选取、可行性门、时统质量 TQ-1..TQ-4 | §3.3、§3.4 与移植的 `locate_tdoa` |
| 02 §7.6、§7.7 | IQ 观测量提取应输出到达时间与方位；多站同步与阵列采样后置 | 本方案在 M2 层先把这两个观测量的**接口与误差语义**定下来，M3 的真估计器后续替换 |
| 09 §7 | 结果页与信号视图 | 本线不加结果页页签；定位评价随 C-9 一起做（L-9） |
| 10 §2.1 | 九个槽位 | 扩为十一个（§2.1），既有九个的 id、顺序、语义不变 |
| D-049 ③ | `ScenarioSource` 每轮必产出至少一帧，否则调度器会静默覆盖下游缓冲 | §2.3 的节点顺序与 §3.1 的块长一致性检查都由此而来 |

### 1.2 现状与差距（2026-09-09 核实）

| 现状 | 事实 | 差距 |
|---|---|---|
| 多源 | **参数帧层已经支持**：`ScenarioSource` 对场景里每个辐射源开一个 `link:<emitter_id>` 输出口（`engine/src/scenario.cpp:174-185`），`Scenario::cross_check` 逐站逐源检查，`links.jsonl` 已经是站 × 源全交叉 | 典型链路只接一个源：`ChainState.siteId / emitterId` 是标量（`web/src/chain/model.ts:173-174`），`compile()` 只连 `link:${chain.emitterId}`（`compile.ts:59-63, 108`），试验设置栏是两个单选下拉（`ChainView.tsx:222-235`）。**自由画布里其实已经能手工展开多源**，只是没人这么用 |
| 多站 | 场景格式允许多站（`sites` 至少 1 个），`geo/` 的链路预算逐站算 | 被一条守卫挡死：`engine/src/channel.cpp:125-133`「首期只支持单站场景」，多站场景一律 configure 失败。守卫的理由是「接收天线增益取唯一站点的」，而 C-2 之后天线增益已经拆到 `AntennaGain` 里去了，这条守卫已经名不副实 |
| 场景绑定 | `scene_binding` 必须且只能带 `entity_id` 或 `site_id` 之一（`diagram_json.cpp:695-697` 与 schema 的 `oneOf`） | 多站场景里的 `SceneBoundChannel` 需要同时说清「哪个源」和「哪个站」，二选一不够用 |
| 测向 | **没有任何生产者**。帧里的 `aoa_az_deg / aoa_el_deg` 是真值，只给 `AntennaGain` 用来查方向图（`engine/src/antenna.cpp:250-251`） | 七种端口里没有报告类端口；产品目录里没有方位与位置 |
| 定位 | 同上 | — |
| 显示 | `docs/display-route.md` §3 已冻结「测向线与到达时间差定位用两种颜色区分」「二倍标准差误差椭圆走 Canvas 叠加层」的语义，§4 明写「它们的生产者都还不存在」 | 叠加层、色值、图层开关、读数面板都要新建 |
| 可移植资产 | emcore 有完整的三站测向 + AOA 交汇 + TDOA 实现与四份黄金基准（`direction_finding` / `aoa_location` / `tdoa_location` / `geo_fix`），且踩过一次 CEP 公式错误并修正（`√trace → 0.5887·(σmax+σmin)`，EM-C-UAV 09 号方案 W4） | 求解器吃经纬度并用 111320 常数做等距圆柱投影，与本项目的严格 ENU 口径不同（D-009）；需要改成平面坐标接口，legacy 路径只守 golden |

### 1.3 真值—算法边界（本方案合法性的正式声明）

这一节是本方案里唯一需要向评审解释清楚的地方，因此单列。

**约束原文**。04 §5.2 要求「真值只进评价器，不进被测算法」。10 报告 §4.5 已经按这条把 `Evaluator` 的真值口做成独立通路：检测器与识别器看不到 `tx_on / tx_center_Hz`。

**本方案做的事**。`DirectionFinder` 从参数帧里读 `aoa_az_deg`（站点看辐射源的真方位）与链路电平，加一个按误差预算抽出来的高斯扰动，作为「量测方位」输出。字面上它读了真值。

**为什么合法**。三条：

1. **它是模型，不是被测算法**。01 §6.2 明确模型的输入可以是真值。M2 效应级模型的定义就是「不复现物理过程，只复现物理过程的统计结果」——雷达检测概率模型直接吃信噪比给 Pd，也没人要求它先做匹配滤波。EM-S-05 的 E2 档给的就是这样一个误差预算模型，emcore 的 `computeDirectionFinding` 是同一形态。
2. **被测对象没有变**。04 §5.2 保护的是「不许让被测算法偷看答案」。本线的被测对象仍是 M3 侧的检测与识别（C-3 / C-4），它们的输入仍只有 IQ。测向与到达时间是**新加的观测量生产者**，本身不参加检测识别的评价。
3. **替换路径已经留好**。P1 的阵列估计器（MUSIC / 相位干涉）将来从 `ArrayIQStream` 出发算方位，输出**同一个 `BearingReport` 端口**，框图形状与下游一件不改。到那时被测对象才轮到测向算法，`truth_consumed` 随之变成 `false`。

**因此必须声明的三件事**（写进组件目录、模型卡与每一行产品）：

| 声明 | 落点 |
|---|---|
| `model_layer = "M2"`、`model_level = "E2"`、`credibility = "V2"` | 组件目录条目与每行的 `trace` |
| `truth_consumed = true` | `BearingReport` / `ToaReport` / `PositionReport` 每一行的顶层布尔字段 |
| 「本行的误差散布来自建模的误差预算，不代表任何真实测向设备的实测性能」 | 模型卡 `models/locate/README.md`，以及 §9 里对蒙特卡洛判据的解释 |

**评价口径的后果**（L-9 与 C-5 执行）：定位指标（CEP、椭圆覆盖率、GDOP）在评价页上必须标注为「模型自洽性检查」而不是「定位精度指标」。它检查的是「抽样散布与预算 σ 是否一致」，即 01 §8 的跨层一致性，不是「这套设备能定多准」。

---

## 2. 链路与槽位

### 2.1 十一个槽位与三个实例维度

10 报告 §2.1 的九个槽位保持原样，尾部加两个：

| # | 槽位 id | 环节名 | 组件 | 实例维度 `per` | 可旁路 | 备注 |
|---|---|---|---|---|---|---|
| 1 | `tx` | 辐射源 | `SceneEmitterSource` / `FileReplaySource` | **源** | 否 | 一个源一份波形，**共享给所有站**（§2.2） |
| 2 | `tx_ant` | 发射天线 | `AntennaGain(role=tx)` | **链路**（源 × 站） | 否 | 离开角逐链路不同 |
| 3 | `ch` | 传播信道 | `SceneBoundChannel` / `FreeSpaceChannel` | **链路** | 否 | 路损、时延、多普勒逐链路不同 |
| 4 | `rx_ant` | 接收天线 | `AntennaGain(role=rx)` | **链路** | 否 | 到达角逐链路不同 |
| — | （隐含）| 叠加 | `Superposition` | **站** | — | 仅 N > 1 时出现 |
| 5 | `rx_fe` | 接收机前端 | `ReceiverFrontEnd` | **站** | 否 | 一站一台接收机 |
| 6 | `adc` | ADC | `AdcQuantizer` | **站** | 否 | |
| 7 | `ddc` | DDC | `DDC` | **站** | 是 | |
| 8 | `chan` | 信道化 | `Channelizer` | **站** | 是（缺省旁路）| |
| 9 | `det` | 检测识别评价 | `EnergyDetector` | **站** | 否 | |
| **10** | **`df`** | **测向** | **`DirectionFinder`** | **站** | **是** | 新增；每站一个测向机 |
| — | （隐含）| 到达时间 | `ToaEstimator` | **站** | — | 仅 `loc` 启用且 `method` 含 `tdoa` 时出现 |
| **11** | **`loc`** | **多站定位** | **`MultiSiteLocator`** | **单例** | **是（缺省旁路）** | 新增；全图唯一 |

> **2026-09-09 补（D-054）**：本表的 `per` 列说的是「编译出几个节点」。参数归谁是**另一件事**，
> 由 `SlotDef.owner` 单独声明：`tx` / `tx_ant` 归**源**，`rx_ant` 及其后归**站**，`ch` 与 `loc` **全图共用**
> （传播信道共用是用户明确要求的）。两者在 `tx_ant` 与 `rx_ant` 上分开：节点按链路展开 N×K 份，
> 但发射天线长在无人机上、接收天线长在站上，按 `per` 取参数会把「改一架机的天线」变成「改一条链路的天线」。

三个实例维度的取值：

- **源**（`emitterIds`，长度 N）：用户在试验设置栏勾选，缺省全选场景里的辐射源。
- **站**（`siteIds`，长度 K）：同上，缺省全选场景里的站点。
- **链路**（source × site，长度 N×K）：不单独选，由前两者的笛卡尔积决定；与 `geo::Scenario` 的 `link_id = "<site_id>-<emitter_id>"` 口径一致。

**N、K ≤ 8**。上限来自固定可选口的数量（§6.4），演示场景够用；超过就在频率计划面板里报错，不静默截断。

### 2.2 图的形状：N 源 × K 站

```
                       ┌── link:e1 ──┬─→ tx_ant__e1__s1 → ch__e1__s1 → rx_ant__e1__s1 ─┐
   scn__s1 (site-1) ───┤             ├─→ …                                             ├→ sup__s1 → rx_fe__s1 → adc__s1 → [ddc__s1] → [chan__s1] → det__s1
   (report_entities)   └── link:eN ──┘                                                 ┘        │
        │                                                                                       └→ (S4 观测点 s4__s1)
        ├─→ df__s1 (scene1..sceneN)  ─────────────────────────────────────────────────→ b1 ┐
        └─→ toa__s1 (scene1..sceneN) ─────────────────────────────────────────────────→ t1 ┤
                                                                                            ├→ loc → positions.jsonl
   scn__s2 … scn__sK  同上（report_entities = false）                              b2..bK, t2..tK ┘

   tx__e1 … tx__eN     ── 每个源一份波形，扇出到全部 K 个站的 tx_ant__e__s
```

四条形状约定，每条都有理由：

1. **辐射源按源实例化，不按链路**。同一个源发出的是**同一份**波形，不同站收到的差别全在传播（时延、多普勒、路损）。若每条链路各生成一份波形，同一源在不同站的载波相位与突发时刻会各自独立，TDOA 就失去物理意义（时差是同一份信号走不同路程造成的）。因此 `tx__<e>` 一份，扇出到 K 条链路。
2. **发射天线按链路实例化**。同一个源对不同站的**离开角**不同，方向图增益因此不同。`AntennaGain(role=tx)` 消费该链路的 `aod_az/el`，所以必须一链一份。
3. **叠加在接收天线之后、前端之前**。物理上电磁场在天线口面叠加，接收机只有一条通道。放在前端之后就得给每个源配一台接收机，与实际相反；放在信道之前则叠加了不同链路的信号却还没施加各自的天线增益。
4. **场景参数源每站一个**。`ScenarioSource` 绑定站点，一个站一份帧流（每个源一个 `link:<e>` 口）。`report_entities`（发 `entity` / `link` 事件）**只在第一个 `scn` 上开**，否则 K 份重复的实体事件会把 `track.jsonl` 撑成 K 倍并让前端画 K 遍。

### 2.3 节点 id 与顺序规则

**id 规则**（`nodeId(base, ...instances)`）：只有**取值多于一个**的维度才加后缀，后缀之间与前缀之间都用双下划线 `__`：

| 情形 | `tx` | `ch` | `rx_fe` | `df` | `loc` |
|---|---|---|---|---|---|
| N=1, K=1 | `tx` | `ch` | `rx_fe` | `df` | `loc` |
| N=3, K=1 | `tx__uav-1` | `ch__uav-1` | `rx_fe` | `df` | `loc` |
| N=1, K=3 | `tx` | `ch__site-1` | `rx_fe__site-1` | `df__site-1` | `loc` |
| N=3, K=3 | `tx__uav-1` | `ch__uav-1__site-1` | `rx_fe__site-1` | `df__site-1` | `loc` |

链路维度的后缀顺序固定为 **源在前、站在后**（`ch__<emitter>__<site>`），与 `link_id` 的 `<site>-<emitter>` 顺序**不同**是有意的：节点 id 按「先分源、再分站」读起来与图的形状一致，而 `link_id` 沿用 `geo/` 已有的口径不改。

选这个方案而不是 `ch@uav-1@site-1` 或 `ch:uav-1` 的原因：节点 id 的正则是 `[a-z0-9_-]{1,64}`（`diagram_json.cpp:25, :134`），`@` 与 `:` 都不合法。双下划线在既有槽位 id 里不出现（`tx_ant` / `rx_fe` 都是单下划线），所以 `splitNodeId()` 可以「按已知槽位前缀 + `__` 切分」无歧义地反解。

**顺序规则**（`nodes[]` 数组的顺序，决定编译输出的字节）：

```
scn__<site>            按 siteIds 顺序
tx__<emitter>          按 emitterIds 顺序
每个站（外层）× 每个源（内层）：tx_ant__<e>__<s>, ch__<e>__<s>, rx_ant__<e>__<s>
每个站：sup__<s>, rx_fe__<s>, adc__<s>, [ddc__<s>], [chan__<s>], [bg, mix], det__<s>, [df__<s>], [toa__<s>]
loc
```

站在外层、源在内层，是为了让同一个站的一整条接收链在文件里连续出现，人读框图 JSON 时按站分块。

**随机种子链**不受影响：种子按 `nodes[]` 顺序派生（`run.seed` → 逐节点），顺序固定则复现固定（铁律 9）。

### 2.4 N = K = 1 的退化证明

这是最重要的一条兼容性约束，`compile.test.ts:247` 已有一个「缺省链编译文本与 `DEFAULT_CHAIN_TEXT` 逐字节相同」的单测守着。退化按以下四条同时成立：

1. **无后缀**：三个维度都只有一个取值，`nodeId()` 不加后缀，全部节点 id 与今天相同。
2. **无 `sup`**：`Superposition` 仅在 N > 1 时插入；N = 1 时 `rx_ant.out` 直接接 `rx_fe.in`。
3. **`df` 与 `loc` 缺席**：两个新槽位缺省都是旁路（`df` 缺省旁路是因为单站测向对既有演示没有增量；`loc` 缺省旁路是因为它需要 ≥ 2 站）。旁路的槽位不出现在 `nodes[]` 里，与 `chan` 今天的处理相同。
4. **`template_ref.version` 不升**：v1 文档在 v2 槽位表下解出的 `ChainState` 里 `siteIds = [解出的 siteId]`、`emitterIds = [解出的 emitterId]`、`slots.df.bypass = true`、`slots.loc.bypass = true`，再编译回去逐字节相同。**版本升号反而会让既有框图解不开**，所以不升。

### 2.5 三种信号源模式下的限制

| 模式 | N > 1 | K > 1 | 理由 |
|---|---|---|---|
| 全合成 `synthetic` | ✔ | ✔ | 本方案的主用例 |
| 实测回放 `replay` | ✘ | ✘ | 回放片段是**已经过完整接收链的 S4 数据**，它自带一个录制时的站位与一个未知的辐射源集合；把它复制到 K 个站等于假装同一份录音在三个地方同时被收到，物理上不成立。编译时直接拒绝并说明 |
| 混合增强 `mixed` | ✔（合成目标）| ✘ | 背景片段同理只对应一个站。K > 1 时拒绝；N > 1 只作用于合成目标那一支，背景仍在唯一站的 S4 处相加 |

拒绝在 `plan.ts` 的频率计划检查里给出（面板上第 9 项 `stations`），不等到引擎报错。

### 2.6 观测点的实例化

观测点 id 在 K > 1 时带站后缀：`s4` → `s4__site-2`。规则与节点 id 一致（K = 1 时无后缀）。

- `tapOpId(tap, site, K)`：K = 1 → `'s4'`；K > 1 → `'s4__site-2'`。
- `tapLabel(opId)`：解析后缀 → `「S4 主产品 · site-2」`。结果页页签、信号页头、观测点一行三处共用（沿用 10 报告 §5.7 定的同一套名字）。
- 勾一个观测点，**每站各产出一份**。观测点一行在 K > 1 时提示「每站各一份」，避免用户以为勾了一个只出一份。

`S0` 是例外：它锚在 `tx` 上，而 `tx` 按**源**实例化，所以 N > 1 时 `s0` 带的是源后缀 `s0__uav-1`。这是唯一一个后缀维度不是站的观测点，在 §5.2 的角标上也照此显示（`S0` 卡片写 `×N`，其余写 `×K`）。

---

## 3. 组件语义、参数与解析锚点

四个新组件全部 `model_layer` 按下表声明，`credibility = V2`（有解析锚点与蒙特卡洛自洽，无实测校准）。

### 3.1 `Superposition`（channel / M3 / E2 / EM-B-09）

**语义**：把 N 路复基带样点逐样点相加。

| 项 | 内容 |
|---|---|
| 输入 | `in1 .. in8`，`IQStream`，全部 `optional = true` |
| 输出 | `out`，`IQStream` |
| 参数 | `min_inputs`（缺省 2）：连上的口少于这个数即 `configure` 失败。防止「以为接了三路其实只接了一路」而无声出错 |
| 元数据 | `clip_count` 取各路之和（与 `AddMixer` 同法）；标定按 D-047 的「都标定才标定、来源取弱者」逐路归并；`state` 取各路最差 |
| 错误 | 各支路的**采样率、中心频率、`start_sample`、块长**任一不一致即 `Step::Error`，写明是哪两路的哪一项对不上 |

**为什么不复用 `AddMixer`**：`AddMixer` 是两口（`a` / `b`）组件，已进目录黄金基准并被混合增强模式使用；把它改成八口会改动既有条目的端口表，属于铁律 10 意义上的基准变更，而收益只是省一个类。新建一个组件，`AddMixer` 一字不动。

**为什么块长不一致要报错而不是截断**：08 报告约定四——调度器每轮**无条件覆盖**下游缓冲（`graph.cpp:241-249`）。截断意味着较长那一路的尾部样点被下一轮覆盖掉，样点丢了还不报错。编译器侧统一派生块长，组件侧比对，两道闸。

### 3.2 `DirectionFinder`（algorithm / **M2** / E2 / EM-S-05）

**语义**：对本站可见的每条链路，按误差预算给出一个含噪的方位量测。

| 项 | 内容 |
|---|---|
| 输入 | `scene1 .. scene8`（`SceneParamFrame`，全 optional）；`det`（`DetectionList`，optional，**首片接了即报「待 C-3」**并拒绝） |
| 输出 | `out`，`BearingReport` |
| 内部参数 | `scenario_path`、`scenario_id`、`site_id`（由装载器按 `scene_binding` 注入，D-037） |

**信噪比的算法**（不额外给参数，全部由帧与场景派生）：

```
Prx_dBm  = tx_power_dBm + tx_gain_dBi + rx_gain_dBi − path_loss_dB      （帧 + 场景派生）
N_dBm    = noise_floor_dBm_per_Hz + 10·log10(B)                          （帧；B = 站的 fs_Hz）
SNR_dB   = Prx_dBm − N_dBm
```

`tx_power_dBm` 与两端天线增益从场景里取（与 `SceneBoundChannel` 同一口径，D-049 ⑤：帧里的 `path_loss_dB` 只装纯传播损耗）。`noise_floor_dBm_per_Hz` 直接用帧里的值——它由 `geo/link_budget.cpp` 按 `−174 + 10·log10(T/290) + nf` 算出，与 `ReceiverFrontEnd` 实际注入的噪声同一常数（C-2 已核实一致）。

**误差预算七分量**（EM-S-05 §10.14），线性相加方差、偏差单列：

| 分量 | 参数 | 缺省 | 语义 |
|---|---|---|---|
| 方法固有 | `sigma_method_deg` | 1.5 | 测向体制本身的分辨力（比幅法的通道一致性、干涉仪的基线量化） |
| 信噪比 | `sigma_snr_ref_deg` @ `snr_ref_dB` | 2.0 @ 10 | `σ_snr = σ_ref·√(SNR_ref_lin / max(SNR_lin, 1))`；与 emcore 的 `σ_base/√SNR` 同形，只是把参考点显式化 |
| 标校残差 | `sigma_cal_deg` | 0.5 | 阵列 / 通道标校后的剩余系统误差 |
| 平台姿态 | `sigma_att_deg` | 0.3 | 站址方位基准误差 |
| 多径 | `sigma_mp_los_deg` / `sigma_mp_nlos_deg` | 0.5 / 5.0 | 按帧的 `line_of_sight` 二选一。**D3 遮挡接入前，帧里的 LOS 恒为真**，所以 NLOS 分量实际不生效，这一点写进模型卡 |
| 同频混叠 | `sigma_mix_deg` | 8.0 | 同站同频多源时给最强源之外的源加上（判据见下） |
| 系统偏差 | `bias_deg` | 0.0 | 不进方差，直接加在方位上；模型卡说明它模拟未标校的固定指向偏差 |

```
σ_total = √(σ_method² + σ_snr² + σ_cal² + σ_att² + σ_mp² + σ_mix²)
bearing_meas = wrap360(aoa_az_deg + bias_deg + σ_total · N(0,1))
```

**同频混叠判据**：同一站、同一时刻，若两条链路的 `tx_center_Hz` 之差小于站的 `fs_Hz` 的一半（即落在同一接收带内），且电平差小于 `mixture_separation_dB`（缺省 10），则**较弱的那条**标 `mixture = true`、加 `σ_mix`、`use_policy = "low_weight"`。较强的那条不受影响。理由：比幅 / 干涉测向在两个同频信号功率接近时会指向能量质心，功率差大时弱信号被强信号压住。这是本方案里唯一一处「多源对测向的影响」，也是 §9 第 13 项要验的现象。

**质量分档 DF-Q**（`q_thresholds_deg`，缺省 `[2, 5, 15, 45]`）：`σ_total < 2°` → `DF-Q1`；`< 5°` → `DF-Q2`；`< 15°` → `DF-Q3`；`< 45°` → `DF-Q4`；否则 `invalid`。分档只看 σ 不看 SNR（SNR 已经进了 σ），比 emcore 的 `dfQuality(snr, σ)` 双判据干净。

**门控**：帧的 `tx_on = false`、或 `SNR < min_snr_dB`（缺省 3）时，该链路本帧输出一行 `df_result_state = "invalid"`、`reasons = ["tx_off"] / ["snr_below_min"]` 的报告，**不是不输出**（铁律 15：不静默降级；下游据此知道「这一时刻这条链路没有量测」而不是「数据丢了」）。

**报告速率** `report_rate_Hz`（缺省 10）：按样点序号推进（D-049 ③ 的同一口径），不按墙钟。

### 3.3 `ToaEstimator`（algorithm / M2 / E2 / EM-S-07）

**语义**：给出各站对同一辐射源的到达时刻量测。它是**隐含节点**，用户不选、不配参数——TDOA 的到达时间只应该有一种口径，暴露成槽位只会让人误以为可以换。

| 项 | 内容 |
|---|---|
| 输入 | `scene1 .. scene8`（optional） |
| 输出 | `out`，`ToaReport` |
| 参数 | `min_snr_dB`（3）、`sigma_floor_ns`（0.3）、`report_rate_Hz`（10） |
| 站钟 | **全部来自场景** `sites[].clock`；缺 `clock` 即 `configure` 失败，**不默认 0**（铁律 15：默认一个完美时钟会让 TDOA 结果好得不真实） |

```
toa_true = delay_s（帧）                                   传播时延
σ_pick   = 1/(2π·B·√max(SNR_lin,1))                        相关峰拾取，B = 站的 fs_Hz
σ_total  = √(σ_pick² + σ_floor² + σ_sync² + σ_rxdelay²)
toa_meas = toa_true + bias_ns·1e-9 + rx_delay_ns·1e-9 + σ_total·N(0,1)
```

**与 emcore 的差别（有意）**：emcore 的 `sigPick` 把带宽写死成 10 MHz 常数（`kNominalBandwidth_Hz`）。本方案改用**站的实际采样率**作相关带宽，因为典型链路里 500 kS/s 与将来 20 MS/s 的时差精度差 40 倍，写死常数会给出与配置无关的假精度。emcore 的常数只保留在 `geo::legacy::toa_sigma_s()` 里守 golden。

**必须写进模型卡的一条**：M2 的 σ_pick 是解析式，与采样率的关系是模型给的，不是仿真出来的。真正的 M3 相关法到达时间估计需要 IQ 相关，在 500 kS/s 下时差分辨率约 2 µs ≈ 600 m，做不出有意义的 TDOA；要做 M3 的 TDOA 至少需要 10 MS/s 以上（P2）。

**时统质量 TQ**（EM-S-07 §10.3，沿用 emcore 分档）：按站钟的 `sync_sigma_ns` 与拟合残差分 TQ-1..TQ-4。`sync_state = "unsynced"` 的站直接给 TQ-4，并在融合节点里剔除（§3.4）。

### 3.4 `MultiSiteLocator`（algorithm / M2 / E2 / EM-S-06 + EM-S-07）

| 项 | 内容 |
|---|---|
| 输入 | `b1 .. b8`（`BearingReport`）、`t1 .. t8`（`ToaReport`），全部 optional |
| 输出 | `out`，`PositionReport` |
| `method` | `aoa`（缺省）/ `tdoa` / `aoa_tdoa` |

**参数**：

| 参数 | 缺省 | 语义 |
|---|---|---|
| `min_crossing_angle_deg` | 10 | 测向线最大张角低于此值即 `geometry_quality = degenerate`，仍出解但标降级 |
| `geometry_condition_threshold` | 15 | GDOP 上限，超过标 `degenerate` |
| `propagation_speed_mps` | 299792458 | 新代码用精确光速（D-009）|
| `max_tdoa_feasibility_margin_m` | 50 | `\|Δr\| > 基线 + 此值` 即可行性违例；**降级不裁**（EM-S-07 §10.4，emcore 同法） |
| `reference_station_rule` | `best_snr` | TDOA 参考站选取 |
| `weighting` | `correlated_reference` | 见下 |
| `sync_quality_threshold` | 3 | TQ 差于此档（即 TQ-4）的站剔除，剔除的站进 `outlier_sites` |
| `time_tolerance_s` | 1e-6 | 各站报告按时刻配对的容差 |

**两种 TDOA 加权**（本方案的一处有意偏离 emcore）：

- `independent_pairs`（= emcore 现行）：把各站对的距离差量测当作互相独立，`w_i = 1/(c²·(σ_i² + σ_0²))`。参考站的噪声进了每个站对，站对之间因此**是相关的**，按独立处理是一个口径上的近似。
- `correlated_reference`（**本方案缺省**）：显式构造 (K−1)×(K−1) 的量测协方差 `R = c²·(diag(σ_i²) + σ_0²·11ᵀ)`，用高斯消元求 `R⁻¹`，法方程用 `HᵀR⁻¹H`。

两种都实现、都进模型卡，并在 §9 的蒙特卡洛里并列给出迹比。**这个差异是模型口径差异，不是缺陷**，不许通过调参让两者一致（铁律 10）。emcore 的 golden 用 `independent_pairs` 回放。

> **2026-09-09 实测修正**（本节初稿的判断是错的，见 §11.3）：原写「按独立处理会低估协方差 → 椭圆偏小、结果偏乐观」，真跑下来方向相反——`independent_pairs` 报的协方差**比实际散布大**（五种站数与 σ 配比下迹比 0.755–0.915），是**保守**而不是乐观；站数 ≥ 4 时它的估计本身也略差（没用上噪声的相关结构）。`correlated_reference` 的迹比 1.004–1.013，自洽。验收判据据此改为「相关口径 ∈ [0.95, 1.05]，独立口径 < 0.95 且小于相关口径」。

**`aoa_tdoa`**：先用 AOA 解算，把结果作为 TDOA 的 Gauss-Newton 初值（emcore 的 `initialGuess` 参数就是为此留的），再把两组量测拼进同一个加权最小二乘。输出 `method = "aoa_tdoa"`，`participating_sites` 取并集。

**站数不足**：`aoa` < 2 站、`tdoa` < 3 站时输出 `state = "invalid"`、`reasons = ["insufficient_stations"]` 的一行，不是不输出。

### 3.5 解析锚点

四个锚点，进 `engine/tests/test_locate.cpp`，是移植正确性的独立验证（与 golden 对拍互补，emcore README W4 的教训：解析锚点与统计校验缺一不可）。

| 用例 | 判据 | 推导 |
|---|---|---|
| AOA 两站正交交汇，各距目标 1 km，σ_θ = 1° | σ_pos = **17.4532925 m**；2σ 半轴 = **34.906585 m**；CEP = 1.1774·σ = **20.5495 m** | 单站测向线的横向不确定度 = R·σ_θ = 1000·(π/180)。两条正交测向线，两个方向各由一条线定，协方差是 σ²I，圆形 → CEP = 1.1774σ（Rayleigh 中位数），与 `0.5887·(σ_max+σ_min)` 在圆形下恒等 |
| TDOA 三站等边三角形、源在质心，不加权 | GDOP = **√(8/9) = 0.942809** | 三站等边、源在质心时三个视线单位向量夹角 120°；以任一站为参考的两行雅可比 `û_i − û_0` 构成的 `(HᵀH)⁻¹` 迹为 8/9 |
| σ_θ 预算 | `√(Σσ²)` 逐位；`bias` 单列不进方差 | 直接核对公式 |
| FSPL 交叉核对 | 沿用 G-1 已有的 100.052008 dB @ 2.4 GHz / 1 km | 保证移植后的 `geo/` 没有动到既有链路预算 |

蒙特卡洛判据见 §9。

---

## 4. 产品格式

三种报告都是 JSONL，每行一个 JSON 对象，必带 `t_s`（逻辑仿真时刻）。落盘规则与 `links.jsonl` 相同：运行器惰性建文件（有第一行才建），逐行 flush。

### 4.1 `BearingReport` 与 `bearings.jsonl`

```json
{"t_s":2.0,"site_id":"site-1","emitter_id":"uav-1","link_id":"site-1-uav-1",
 "bearing_deg":131.842,"bearing_std_deg":1.702,"elevation_deg":8.31,
 "snr_dB":24.6,"level_dBm":-58.2,"df_quality":"DF-Q1","df_result_state":"valid",
 "use_policy":"normal","method":"amplitude_compare","bias_deg":0.0,
 "sigma":{"method":1.5,"snr":0.502,"cal":0.5,"att":0.3,"multipath":0.5,"mixture":0.0},
 "line_of_sight":true,"mixture":false,"truth_consumed":true,
 "state":"valid","reasons":[],
 "trace":{"model_id":"EM-S-05","model_version":"1.0.0","model_level":"E2",
          "model_layer":"M2","credibility":"V2","parameter_version":"chain-v1",
          "trace_id":"…"}}
```

| 字段 | 说明 |
|---|---|
| `df_result_state` | `valid` / `degraded` / `invalid` / `not_applicable`（铁律 15 四态）。与顶层 `state` 的区别：`state` 是**这一行数据本身**的状态，`df_result_state` 是**这次测向裁决**的状态。数据有效但裁决无效（信噪比不够）是常见组合 |
| `use_policy` | `normal` / `low_weight` / `exclude`。下游融合据此加权；`exclude` 的行不进解算但仍落盘 |
| `sigma` | 六个分量分列，便于在模型卡与评价页上说明「σ 是怎么来的」 |
| `truth_consumed` | 恒 `true`（§1.3）。P1 的阵列估计器接同一端口时改为 `false` |
| 抽稀键 | `link_id`（读端按时间窗 + `stride` 抽稀时，每个 `link_id` 独立抽） |

### 4.2 `ToaReport`

不单独落盘（它是隐含节点的中间量），但作为端口数据结构存在，并在 `positions.jsonl` 的 `residuals` 里留下痕迹。字段：

```
{t_s, site_id, emitter_id, link_id, toa_s, toa_std_s,
 sigma:{pick, sync, rxdelay, floor}, snr_dB,
 time_quality:"TQ-1", sync_state:"locked", truth_consumed:true,
 state, reasons, trace}
```

`sync_state` 直接抄场景里站钟的声明值（`locked` / `holdover` / `unsynced`），`time_quality` 由 σ 与状态共同分档。**这两个字段是 05 §6.2.2「失锁、补零、重对齐不得当作连续数据」在本线的落地形式**：`holdover` 的站给出的时差仍可用但降级，`unsynced` 的站被融合节点剔除。

### 4.3 `PositionReport` 与 `positions.jsonl`

```json
{"t_s":2.0,"emitter_id":"uav-1","method":"aoa",
 "lon":116.4072,"lat":39.9931,"crs":"EPSG:4326","coord_version":"wgs84-2026",
 "enu_origin":{"lon":116.4005,"lat":39.9880,"alt_m":0.0},
 "cov_m2":[412.7,-88.3,301.5],
 "ellipse":{"semi_major_m":45.2,"semi_minor_m":31.8,"rotation_deg":-27.4,
            "scale":"2sigma","confidence":0.865},
 "cep_m":22.6,"gdop":1.83,"geometry_quality":"good","time_quality":null,
 "participating_sites":["site-1","site-2","site-3"],"reference_site":null,
 "residuals":[{"site_id":"site-1","value":0.31,"unit":"deg"},…],
 "outlier_sites":[],"truth_consumed":true,
 "state":"valid","reasons":[],"trace":{…}}
```

三点说明：

1. **`ellipse.scale` 与 `confidence` 显式写出**。2σ 椭圆在**二维**下的包含概率是 `1 − exp(−2) = 86.47%`，不是一维的 95.4%。em-demo 的注释写「2σ (~95%)」是把一维置信搬到了二维，本方案纠正并把 `confidence: 0.865` 写进每一行，避免下游再算错（§9 的验收判据 [0.80, 0.93] 就是按 86.5% 定的）。
2. **`cov_m2` 是 ENU 平面的上三角** `[P_xx, P_xy, P_yy]`，单位 m²。给出协方差而不只给椭圆，是因为 L-9 的评价要算马氏距离。
3. **`enu_origin` 与 `coord_version`** 是 05 §6.2.3 的要求：椭圆的旋转角是相对 ENU 东向的，脱离原点声明就没有意义。

### 4.4 端点与 WebSocket 事件

| 层 | 变更 |
|---|---|
| 结果端点 | `server/src/products/routes.ts` 的 `JSONL_KINDS` 加 `bearings` / `positions`，复用 `readJsonlWindow`；查询参数在 `t0/t1/stride` 之外加 `site_id` / `emitter_id` / `method` 过滤（与 `links` 的 `link_id` 过滤同法） |
| WS 事件 | 新事件类型 `bearing` / `position`，payload = 行去掉 `t_s`（`t_s` 已在信封里）。服务端事件转发不设类型白名单（`hub.ts:231` 按 `type` 透传），**不需要改服务端代码** |
| 索引 | 两种 JSONL 产品沿用既有的「读端以文件长度定行数」口径（D-046 ③），不建二进制索引 |

---

## 5. 界面

### 5.1 试验设置栏：站点与目标改复选

今天是两个单选下拉（站点、目标）。改成两组复选框：

- `data-field="site:<id>"` / `data-field="emitter:<id>"`，选场景时缺省全选。
- 至少各选一个；取消到零个时禁止（提交按钮变灰并说明）。
- 复选框旁显示该站 / 该源的一行摘要（站：站名 + 天线增益；源：机型 + 中心频率 + 发射功率）。

### 5.2 卡片徽标与实例查看器

十一张卡片按**每行四张、蛇形**排成 **4 + 4 + 3** 的三行，每张右上角一个 `data-slot-count` 徽标。

> **2026-09-13 撤销徽标（D-064）**：用户定「中栏下拉决定当前框图的链条与编辑焦点，卡片不显示 ×2 / ×3；左栏勾选决定参与计算的实例」。下表的徽标规则与 `instanceBadge()` 已删除，卡片摘要、右栏面板与「待填」都按中栏下拉选中的那条链的实体算；实例化的份数仍由编译决定（§2.2 不变），只是不再画在卡片上。

**蛇形**（2026-09-09 用户指定）：第 0 行从左往右，第 1 行**从右往左**，第 2 行又从左往右。好处是链条连续可读——一行读到头，下一张卡片就在**正下方**（第 0 行末尾与第 1 行开头同在第 4 列），视线不必从行尾甩回行首。行内连接件随行方向翻成 `→` 或 `←`，行末换成向下的转折 `↓`。

排布由纯函数 `snakePos(i, total, cols)` 给出（`web/src/chain/ChainView.tsx`），它是版式的唯一真相来源：改列数只需改它与 `.chain-strip` 的 `grid-template-columns` 两处。卡片在各自的网格列里**居中**，连接件绝对定位到两张卡片的边缘之间——不用「卡片 + flex 撑开的箭头」那种写法，是因为蛇形下相邻两行的卡片会被挤到列的两侧、上下对不齐。

（10 报告 §5.2 定的是九个环节按 3 × 3；槽位加到十一个之后 3 列会排成四行、末行只剩两张。）实测 1680 / 1920 / 2048 / 2560 四种视口宽度都是 4 + 4 + 3、无横向溢出、卡片仍是 176 × 128 px；连接件贴卡片边差 0 px、纵向中心差 0 px，转折竖线与卡片中心差 0 px。

| 槽位 | 徽标 | 例（N=3, K=3） |
|---|---|---|
| `tx` | `×N` | `×3` |
| `tx_ant` / `ch` / `rx_ant` | `×N·K` | `×9` |
| `rx_fe` / `adc` / `ddc` / `chan` / `det` / `df` | `×K` | `×3` |
| `loc` | `K 站` | `3 站` |

N = K = 1 时不显示徽标（画面与今天相同）。

**参数按槽位共享**（本方案的核心简化）：九个站的接收机共用一组噪声系数与增益，不是九组。实例之间的差别只来自场景（站址、站钟、天线朝向、发射功率、航迹）。这样做的理由是 D-051 的原话——「注重实用性，灵活性可适度降低」：让用户逐站配接收机会把参数量乘以 K，而演示里九台接收机本来就是同型号。

**派生量的显示**要指明是哪个实例的。工具条给「查看实例：源 ▾ 站 ▾」两个下拉，只影响卡片正文里派生参数的读数（如 `ch` 卡片上的距离与路损、`rx_ant` 卡片上的到达角），不影响编译结果。K > 1 时 `rx_fe.nf_dB` 与 `rx_ant.gain_dBi` 之类由场景带出的量转为**派生只读**，N > 1 时 `tx_ant.gain_dBi` 同理。

> **2026-09-09 修正（D-054）**：本节前两段被 D-054 取代，理由与结论如下，实现以 D-054 为准。
>
> 一、**两个下拉从「只影响派生量显示」升级为配置焦点**。用户指示框图页要成为「场景内实体电子设备参数配置的主界面」，
> 于是选中某架无人机或某个站之后，右栏的参数、由场景带出的设备参数、卡片上的派生读数一起切换。
> 运行范围不受它影响——谁参与仍由左栏的多选决定，一条任务照样跑 K 站 N 源。
>
> 二、**「按槽位共享」放宽为「按型号共享 + 可逐实体单独设置」**。场景新增可选字段 `equipment_model`
> （辐射源缺席时回退 `platform_type`），同型号的设备默认共用一套参数——本节「九台接收机本来就是同型号」的
> 论证因此仍然成立，只是不再靠「不给第二种可能」来保证。存储只有两层（共用底值 + `byEntity` 逐实体覆盖），
> 型号是参数行上的**编辑范围**而非存储层级：`parseChain()` 拿不到场景文档，型号进状态会让反解随场景载入与否
> 而变，`compile(parseChain(doc)) === doc` 当场失效。
>
> 三、**本节承诺的「场景带出的量转派生只读」终于兑现，但不是只读**：`tx_ant.gain_dBi` / `rx_ant.gain_dBi` /
> `rx_fe.nf_dB` 改由场景**逐实体**带出，在框图页**可编辑**——编辑写的是场景文件而不是框图。
> 实施时发现，此前这三项一直是与场景并存的第二份真理源：`demo-03` 的 `uav-3` 场景里写着 0 dBi，
> 而链路强制三架机共用 2 dBi，`engine/src/antenna.cpp:169` 只认组件参数，于是**场景里逐源的天线增益被静默忽略**，
> 该源链路预算高估 2 dB。修正走了正式基准变更（铁律 10），详见 D-054 与 WORKLOG 2026-09-09。

### 5.3 场景编辑器：布目标

今天四个工具里只有「布站」没有「布目标」，加源只能手改 JSON。补一个：

- `SceneTool` 加 `'emitter'`，`MapToolbar` 加工具按钮，`SceneView` 点击地图时分支。
- `scenarioOps.ts` 加 `addEmitter` / `removeEmitter`，照 `addSite` 的模式：新源复制 `emitters[0].emission` 作为参数模板，建一条只有单个航点的航线（静止），id 按 `uav-<n>` 递增。
- 顺带修一处硬连：`SceneView.tsx:159-163` 点击目标时选中的永远是 `sites[0]`，改为选中被点的辐射源。
- 辐射源表单加一个只读的「链路（K）」表，逐站列出距离、方位、路损——这是把 `derivedLinks()` 已经算出来的东西显示出来。

### 5.4 地图叠加层：测向线与误差椭圆

新文件 `web/src/scene/layers/fixOverlay.ts`，一块 `<canvas class="cuav-fix-overlay">` 盖在 `map.getContainer()` 上，`pointer-events: none`，按设备像素比缩放，坐标用 `map.project`。沿用 G-5 的 20 Hz 定频驱动（`useSituation` 的 tick），不在每帧地图事件里重画。

| 元素 | 画法 |
|---|---|
| 测向线 | 从站址沿 `bearing_deg` 画；**有定位解时画到定位点再延伸 20%**，无解时画固定 3 km。`df_result_state` 非 `valid` 用虚线；`use_policy = "exclude"` 不画 |
| ±2σ 楔形 | 以测向线为中心、半角 `2·bearing_std_deg` 的扇形，低透明度填充。σ 大时一眼看得出「这条线不可信」 |
| 定位点 | 小十字 + `ctx.ellipse(cx, cy, a_px, b_px, rot)` 的 2σ 椭圆，12% 填充 + 实线描边 |
| 标签 | 只在 `?dev=1` 下显示（站名、σ、CEP、GDOP）。D-039：界面不主动解释 |

**地图拆除是异步事件驱动的**（D-049 ⑩ 的教训）：叠加层的每个入口先查地图还活着，否则整棵界面树会被卸载。

### 5.5 色值标定

`web/src/style/situation.ts` 的 `SIT` 加三个色：

| 键 | 值 | 用途 |
|---|---|---|
| `aoa` | `#b45309` | AOA 定位点与椭圆 |
| `tdoa` | `#0e7490` | TDOA 定位点与椭圆 |
| `fusion` | `#6d28d9` | `aoa_tdoa` 融合解 |

**必须用 D2-4 的同一个对比度脚本，对陆地 / 水面 / 建筑三种底色实测 ≥ 3:1**，结果回填 `docs/display-route.md` §4 的色值表。em-demo 的 `#f97316`（橙）与 `#22d3ee`（青）是为深色底图调的，在 Airports 浅色底图上对比度不足，**不沿用**（D-017）。

测向线用站点色的降饱和版本，让「哪条线是哪个站的」不必看标签。

### 5.6 读数面板

`ObjectForm` 的辐射源表单加两块：

- 「测向」：每站一行 `站名 · 方位 xxx.x° · σ x.xx° · DF-Q1`，非 `valid` 的行标灰并写原因。
- 「定位」：`方法 / 经纬度 / CEP / GDOP / 几何质量 / 参与站 / 时统质量`，`method` 有多个解时并排。

---

## 6. 格式与接口变更清单

### 6.1 端口类型与兼容矩阵

`PortType` 由 7 种增至 10 种：`BearingReport`、`ToaReport`、`PositionReport`。`can_connect()` 规则不变（仍是纯对角，同类型才连），`port_compat` 由 7×7 = 49 行变 10×10 = 100 行，既有 49 行**逐字不变**（新类型排在枚举末尾）。

### 6.2 `SceneParamFrame` 的身份字段

加三个显式字段：`link_id`、`site_id`、`emitter_id`（`types.h:137-163`，生产者 `scenario.cpp:236`）。

**为什么不从 `trace_id` 里解析**：`trace_id` 是溯源字符串，格式属于溯源约定不属于数据契约；让 `DirectionFinder` 去解析它等于在两个模块之间建立一条隐式协议，改溯源格式就会静默改坏测向。显式字段三个字符串，代价可忽略。

### 6.3 场景绑定：双绑定与多站守卫

| 变更 | 落点 | 内容 |
|---|---|---|
| 双绑定放宽 | `diagram_json.cpp:695-697` 与 `docs/schemas/diagram.schema.json` 的 `oneOf` → `anyOf` | 由「必须且只能带 `entity_id` 或 `site_id` 之一」改为「至少带一个；两个同时出现时，组件目录必须同时声明这两个内部参数」。后半句是新加的闸：只有真正需要双绑定的组件（`SceneBoundChannel`）才接受两个键 |
| 多站守卫替换 | `channel.cpp:125-133` | 旧：站点数 ≠ 1 即失败。新：`site_id` 为空且场景单站 → 取唯一站（**旧行为原样保留，既有框图不变**）；`site_id` 为空且场景多站 → 报「多站场景的场景绑定信道必须绑定 site_id」；`site_id` 非空 → `find_site` |
| 目录 | `SceneBoundChannel` 条目加内部参数 `site_id` | 这是目录黄金基准里唯一一处**既有条目**的变更（§7.3） |

### 6.4 可选输入口与 `check_wiring`

固定可选口带来一个新问题：`Superposition` 声明了 `in1..in8` 全部 optional，那么「一个都没连」在 `Graph::validate()` 眼里是合法的。

加一个虚函数：

```cpp
// component.h
virtual bool check_wiring(const std::vector<std::string>& wired_inputs, std::string& err) const {
    (void)wired_inputs; (void)err; return true;
}
```

`Graph::validate()` 在既有的悬空口检查之后对每个节点调一次，失败映射到 `GraphFault::PortOptional` → 装载器错误码 **`port_optional`**。这个错误码是 `docs/diagram-format.md:110` 里已经预留但没启用的那个，因此**错误码总数仍是 21，不增**。

各组件的实现：`Superposition` 要求 ≥ `min_inputs` 个；`DirectionFinder` / `ToaEstimator` 要求 ≥ 1 个 `scene*`；`MultiSiteLocator` 按 `method` 要求 ≥ 2 个 `b*`（aoa）或 ≥ 3 个 `t*`（tdoa）或两者（aoa_tdoa）。

### 6.5 场景格式：`sites[].clock`

新增可选对象：

```json
"clock": {"sync_sigma_ns": 3.0, "bias_ns": 0.0, "rx_delay_ns": 0.0,
          "rx_delay_sigma_ns": 0.5, "sync_state": "locked"}
```

| 字段 | 语义 |
|---|---|
| `sync_sigma_ns` | 站钟同步 1σ；GPS 驯服 / 共视同步的固定多基地网约 3 ns（≈ 0.9 m 距离差） |
| `bias_ns` | 固定钟差（未标定的系统偏差） |
| `rx_delay_ns` / `rx_delay_sigma_ns` | 接收通道群时延及其不确定度 |
| `sync_state` | `locked` / `holdover` / `unsynced`（05 §6.2.2） |

落点：schema 的 `$defs.site.properties.clock`；`engine/src/scenario_json.cpp` 的 `parse_clock()`；`geo/include/cuav_geo/scenario.h` 的 `struct Clock` 进 `Site`；`test_scenario.cpp` 三例。

**语义声明**（写进 `docs/scenario-format.md`）：在 `time.basis = "LogicalSim"` 下，这些值是**建模的**同步误差，不是任何真实设备的时统指标；`ToaReport` 的 `sync_state` / `time_quality` 是它在每一行上的体现。真实站钟与 GNSS 驯服属 05 P2 的 `DeviceStatus`。

同时把 `docs/scenario-format.md:48` 的「首期单站，多站只作结构预留」改为：**多站自 D-053 起启用；同一框图里参与的各站 `fs_Hz` 与 `center_Hz` 必须一致**（与装载器 `diagram_json.cpp:348` 的跨节点同采样率约束同口径）。

### 6.6 观察者与运行器

`IRunObserver` 加 `on_bearing(const BearingReport&)` 与 `on_position(const PositionReport&)`；`engine/tools/cuav_run/runner.cpp` 惰性建 `bearings.jsonl` / `positions.jsonl` 并各发一条 `bearing` / `position` 事件——与 `on_link` 今天的做法完全相同，不新造机制。

**随机性**：`DirectionFinder` 与 `ToaEstimator` 在 `init()` 里按 `SceneEmitterSource` 的同一模式建子流 `sub_rng_ = Xoshiro256pp(rng.next_u64())`，保证种子链只依赖 `run.seed` 与 `nodes[]` 顺序（铁律 9）。

### 6.7 错误码

不新增。`scenario`（19 个时加的）与预留的 `port_optional` 覆盖本线全部失败模式：

| 失败 | 错误码 |
|---|---|
| 多站场景的信道没绑站 | `scene_binding` |
| 站数不足 / 输入口连得不够 | `port_optional` |
| TDOA 但场景里的站没有 `clock` | `param` |
| 参与站采样率不一致 | `scenario` |
| N 或 K > 8 | `param` |

---

## 7. 参考实现移植与基准变更

### 7.1 移植清单

| 源（`C-UAV Model Demo/emcore/`） | 目标（`geo/`，零第三方依赖；单测寄放 `engine/tests/`） |
|---|---|
| `src/core/geo.cpp` 的 `localFrame` / `covarianceToEllipse` / `invSym2x2` / `localToGeo` | `geo/include/cuav_geo/fix_geometry.h`：`covariance_to_ellipse`、`inv_sym2x2`、`GeometryQuality`；`geo::legacy::local_frame_111320()` **只供 golden 回放** |
| `src/models/locate_aoa.cpp` | `locate_aoa.h`：`aoa_solve_plane(vector<AoaPlaneObs{x_m, y_m, bearing_deg, sigma_deg, weight}>) → AoaPlaneSolution`（两遍加权最小二乘，R ≥ 100 m、σ ≥ 0.1° 的钳位保留）；`legacy::aoa_localization_lonlat()` = 111320 投影 + 平面解，守 golden |
| `src/models/locate_tdoa.cpp` | `locate_tdoa.h`：`tdoa_solve_plane(obs, ref_rule, weighting, init*)`，Gauss-Newton ≤ 6 步；两种加权；可行性门降级不裁；`legacy::toa_sigma_s()`（10 MHz 带宽、0.3 ns 底噪的原常数） |
| `src/models/direction_finding.cpp` | `legacy::true_bearing_planar()` 只为 golden；引擎侧的 `DirectionFinder` 用帧里的 `aoa_az_deg`（严格 ENU 算出的），另加一测：在 AOI 尺度上两种口径的方位差 < 0.5° |
| σ_θ 预算（新写，无对应源） | `geo/include/cuav_geo/df_error.h`：`sigma_total_deg(DfErrorBudget)`、`df_quality_grade(sigma, thresholds)` |
| `tests/golden/{direction_finding, aoa_location, tdoa_location, geo_fix}.json` | 拷入 `tests/golden/`，`_meta` 加 `origin` 与 `ported_to` 两个键；`engine/tests/test_locate_golden.cpp` 相对误差 ≤ 1e-9；文件头注明来源与 golden 对应关系（铁律 13） |
| — | Python 第二实现 `algos/reference/{aoa_fix.py, tdoa_fix.py}`：从 `bearings.jsonl` + 场景重算并与 `positions.jsonl` 逐值对拍（显式循环累加，D-046 ⑧） |

### 7.2 平面坐标接口与 D-009

emcore 的求解器吃经纬度、内部用 `111320 · cos(φ)` 的等距圆柱投影。本项目的口径是严格 ENU（D-009：「移植模块保留原常数守 golden；新写代码统一 `c = 299792458` 与严格 ENU；禁止顺手统一」）。

做法：**把求解器的接口改成平面坐标**（吃米，不吃经纬度）。于是

- 数学部分（最小二乘、Gauss-Newton、协方差 → 椭圆）与投影解耦，一份代码两处用；
- golden 回放走 `geo::legacy::local_frame_111320()` 投影 + 同一个平面求解器，逐位守住 1e-9；
- 引擎侧走 `default_geodesy().to_enu(..., origin = 第一个参与站)` 投影 + 同一个平面求解器，用严格 ENU。

**守卫**：`scripts/build-all.sh` 加一条 `grep -rn 'legacy::' engine/src geo/src`，只允许出现在 `*_golden*.cpp` 与 `legacy_*.cpp` 里。这样「顺手用了 legacy 常数」在构建阶段就被拦下。

### 7.3 基准变更（铁律 10）

| 基准 | 变化 | 判定 |
|---|---|---|
| `tests/golden/component-catalog.json` | **重生成一次**。差异三类：① 新增 4 个组件条目；② `port_types` 7 → 10、`port_compat` 49 → 100 行（既有 49 行逐字不变）；③ **既有条目 `SceneBoundChannel` 加内部参数 `site_id`** | 允许，逐项列在本表。`test_catalog_golden.cpp:26` 是整表比对，与 C-1 同法：先记预期差异再重生成 |
| 全部产品黄金基准（`product-window.json` 等） | **零变化** | 新组件不进既有框图；`Superposition` 只在 N > 1 时出现 |
| `tests/golden/scenario-track-demo-01.json` | **零变化** | `demo-01` 场景一字不动 |
| `slice1 / slice2 / replay` 三份示例框图 | **零变化**，继续作回归夹具 | |
| 新增 | `tests/golden/scenario-track-demo-03.json` + 四份移植 golden | |
| `DEFAULT_CHAIN_TEXT` | **零变化** | §2.4 的退化证明，`compile.test.ts:247` 守 |

---

## 8. 实施步骤与切片

### 8.1 L 线（06 §9H）

| # | 步骤 | 交付物 | 验收 | 依赖 |
|---|---|---|---|---|
| L-0 | 方案定稿与回填 | 本报告；CLAUDE.md 的 D-053 与状态表；06 §0.2 / §0.3 / §1.2 / §9E / §9H / §14；10 报告修正表；`docs/concept-model-layers.md` | 文档互引一致；§1.3 真值边界与 §10 不做各有结论 | D-051、D-052 |
| L-1 | 格式与接口 | 三种端口 + 100 行兼容矩阵；帧三身份字段；双绑定放宽；多站守卫替换；`check_wiring` + `port_optional`；观察者两回调 + 运行器两文件；`sites[].clock`；五份 docs | 目录基准只有 §7.3 列的预期差异；`demo-01` 航迹与全部产品基准零变化；ctest 与五套 e2e 无回归 | L-0 |
| L-2 | `Superposition` + 多源模板 | 组件；`model.ts` 的 `per`、`compile.ts` 的实例 id / 顺序 / `sup`、`parseChain`、`plan.ts` 多源检查；复选目标与 `×N` 徽标；`addEmitter` + 「布目标」；`demo-03` + 黄金航迹 | 1×1 三模式往返逐字节且与 `DEFAULT_CHAIN_TEXT` 逐字节；3×1 往返；S4 同频双源合成线 = 两条链路预算的线性和（**`stat=mean` 且窗 ≥ 2 s**，± 0.3 dB）；`start_sample` 不一致即报错 | L-1 |
| L-3 | `DirectionFinder` + 多站模板 | 组件 + `geo/df_error.h`；`df` 槽位；`siteIds[]`、`scn__*`、站后缀、观测点实例化、`bindOf` 三态、`report_entities` 单开；频率计划第 9 项；`×K` 徽标、实例查看器、4 行版式；`bearings.jsonl` + 端点 + WS；`sceneStore` + 测向线；读数面板；模型卡 §DF | 3×3 往返逐字节；每站 S1 = 该站链路预算（≤ 0.05 dB）；每个 (站, 源) 都有报告行；1e4 帧的经验 σ 与预算差 ≤ 3%；同频双源在近站出现 `mixture`；色值对比度 ≥ 3:1 实测 | L-2 |
| L-4 | `MultiSiteLocator(aoa)` + 移植 | `geo/{fix_geometry, locate_aoa}` + golden 回放；`loc` 槽位、频率计划第 10 项、`check_wiring`；`positions.jsonl` + 端点 + WS；AOA 椭圆；`aoa_fix.py` | golden 相对误差 ≤ 1e-9；三个解析锚点 17.4533 / 34.9066 / 20.5495；协方差迹比 ∈ [0.95, 1.05]；与 Python 逐值一致；真值落 2σ 椭圆内 ≈ 86.5% ± 3%（n ≥ 1000） | L-3 |
| L-5 | `ToaEstimator` + `MultiSiteLocator(tdoa, aoa_tdoa)` | 组件（隐含 `toa__<s>`）；`geo/locate_tdoa` 两种加权 + golden；TQ 分档、可行性门、`unsynced → TQ-4` 剔除；TDOA 色；`tdoa_fix.py`；模型卡 §TOA / §TDOA | golden ≤ 1e-9；GDOP 锚点 0.942809；`correlated_reference` 迹比 ∈ [0.95, 1.05]；`demo-03` 的 `site-3` 行 TQ ≥ 2；`demo-01` + tdoa → `--validate` 报 `param`「无站钟」并落到「多站定位」卡片 | L-4 |
| L-6 | 服务端与回看 | 两个 JSONL 端点 + 过滤；`useSituation` 回看拉取；定位读数块；图层开关 | 服务测试的假夹具可读；刷新后已结束任务的地图仍画线与椭圆 | L-3、L-4 |
| L-7 | e2e 与回归夹具 | `tests/e2e/slice6-smoke.mjs`；`tests/regression/diagrams/chain-3x3-{aoa,tdoa}.json`；`slice4-smoke` 更新为 11 环节 / 4 行断言 | §9 全过；既有五套 e2e 全绿 | L-2…L-6 |
| L-8 | 文档收口 | `docs/` 八份；08 报告附录的组件 / 端口 / 错误码计数；WORKLOG；CLAUDE.md 里程碑 | 文档与 `cuav_run --catalog` 逐项核实 | L-7 |
| L-9 | （随 C-5 / C-9）定位评价 | `Evaluator` 加 `localization` 指标进 `metrics.json`；「评价」页签的定位表 | 与 Python 参考逐值一致 | C-5、C-9、L-5 |

### 8.2 切片 ⑥（06 §9E 追加）

| 切片 | 步骤 | 切完在浏览器里看到 |
|---|---|---|
| **⑥a 多源叠加** | L-0、L-1、L-2 | 场景页用「布目标」放下第二个目标；框图页辐射源卡带 `×2`；S4 瀑布上两条同频谱线随两个源的几何各自起伏，第三条突发线闪烁 |
| **⑥b 多站测向与交叉定位** | L-3、L-4、L-6 | 勾三个站；十一个环节蛇形排成 4 + 4 + 3，卡片带 `×3`；地图上三条测向线随目标转动，交汇处一个 AOA 椭圆；左栏读得到方位与 CEP |
| **⑥c 时差定位与融合** | L-5、L-7、L-8 | 「多站定位」切到 `tdoa` 出第二种颜色的椭圆；`aoa_tdoa` 两个解并存；`site-3` 的保持态让时统质量变化；`demo-01` 选 tdoa 时卡片上报「无站钟」 |

执行顺序 ⑥a → ⑥b → ⑥c，每片收口跑全量回归并按 06 §13 第 4 条回填文档。

### 8.3 与 C 线的关系

- **首片的测向门控** = 帧的 `tx_on` 且 SNR ≥ `min_snr_dB`。这是 M2 效应模型的合法输入（§1.3），不是「偷看真值做检测」。
- **C-3 落地后**，`DirectionFinder` 的 `det` 口可以接实际检出作门控。这要求 `Detection` 带频率与源标识——**这是 C-3 字段增补的理由之一**（今天的 `Detection` 只有 `start_sample / frame_index / statistic / threshold / hit`，见 `component.h:27-38`）。
- **识别（C-4）** 只在 `BearingReport` 里预留一个 `signal_role` 字段位，本期恒为空。
- **对 10 报告的四处修正**（让 C-3 / C-5 不按单站设计，§11 列表）。

---

## 9. 验证与验收

### 9.1 单元层

| 层 | 判据 |
|---|---|
| 移植 golden | 四份，相对误差 ≤ 1e-9 |
| 解析锚点 | AOA 17.4532925 m / 34.906585 m / 20.5495 m；TDOA GDOP 0.942809；σ 预算逐位；FSPL 100.052008 dB |
| DF 蒙特卡洛 | 1e4 次 @ SNR 20 dB：经验 σ 与预测 σ 差 ≤ 3%，均值偏差 ≤ 3σ/√n |
| AOA 蒙特卡洛 | 1e4 次三站：协方差迹比 ∈ [0.95, 1.05]；经验 50% 半径 / CEP ∈ [0.9, 1.1]（emcore W4 的教训：解析锚点与统计校验缺一不可） |
| TDOA 两种加权 | `correlated_reference` 迹比 ∈ [0.95, 1.05]（实测 1.004–1.013）；`independent_pairs` 在同一样本上迹比 **< 0.95 且小于相关口径**（实测 0.755–0.915，即协方差保守偏大），数值进模型卡，**不调参使其一致** |
| 复现 | 同种子两次运行的 `bearings.jsonl` 逐字节相同 |
| 前端 | `compile.test.ts` 加 1×3 / 3×1 / 3×3 往返与 `splitNodeId` 用例 |
| 服务 | `routes.test.ts` 两个新端点的假夹具各一例 |

### 9.2 端到端（`tests/e2e/slice6-smoke.mjs`，`cdp.mjs` 驱动）

| # | 断言 | 判据 |
|---|---|---|
| 1 | `demo-01` 缺省链 9 + 2 环节、1 站 1 源、`loc` 旁路 | `app.chain.siteIds = ['site-1']`、`slots.loc === 'bypass'` |
| 2 | 缺省链编译文本与 `DEFAULT_CHAIN_TEXT` 逐字节相同 | 守既有基准 |
| 3 | 切到 `demo-03`：站与源复选框各 3 个、缺省全选 | `[data-field^="site:"]` |
| 4 | 徽标：辐射源 `×3`、传播信道 `×9`、前端 `×3`、多站定位 `3 站` | `[data-slot-count]` |
| 5 | 11 张卡片蛇形排成 4 + 4 + 3；双数行从右往左；转折处上下同列；不撑出横向滚动条；仍纵向居中 | 复用 `slice4` 的量测 |
| 6 | 频率计划十项全过 | `data-check` |
| 7 | 提交、通过校验、跑完 | `finished` |
| 8 | `links.jsonl` 9 条；`track` 每实体每时刻 1 行 | 计数 |
| 9 | 每站 `s1__site-k` 的带内功率 = 该站链路预算（≤ 0.05 dB） | `slice4` 的带内功率法 |
| 10 | S4 上同频双源的合成线 = 两条链路预算的线性和 ± 0.3 dB | `stat=mean`、窗 ≥ 2 s（**同频两音有多普勒差拍，短窗不收敛**） |
| 11 | `bearings.jsonl` 覆盖 9 个 (站, 源)；`uav-3` 的行随突发与开关和 `invalid(snr_below_min)` 交替 | 分组统计 |
| 12 | `uav-1` 的全部 `valid` 行：`\|bearing − 真值\| ≤ 3σ` 占比 ≥ 99%；`(bearing − 真值)/σ` 的样本方差 ∈ [0.9, 1.1] | 真值取同时刻 `/links` 的 `azimuth_deg` |
| 13 | 近站对同频双源至少出现一次 `mixture` 且 `use_policy = "low_weight"` | — |
| 14 | `positions.jsonl`(aoa) 的**有效档**（最小交会角 ≥ 15° 且无低权重站）：真值落 2σ 椭圆内 ∈ [0.80, 0.93]（实测 82.5–87.0%）；经验 50% 半径 / CEP ∈ [0.85, 1.15]（实测 0.920–1.029）。降级档另列并断言每行写明 `crossing_angle_below_min` | 真值取 `/track`；分档理由见 §11.3 第 3 条 |
| 15 | 切 `method=tdoa` 重跑：`time_quality` 全部非 null 且写明参考站；中位误差米级（实测 7.95 m）；未标定钟差造成可见的系统偏差（实测 4.49 m）；几何良好且扣掉系统偏差后 2σ 覆盖 ∈ [0.80, 0.93]（实测 85.3%） | 分解理由见 §11.3 第 4 条 |
| 16 | DOM 里有 `canvas.cuav-fix-overlay`；探针 `situation.bearings.length === 9`、`positions.length ≥ 1` | `situationSnapshot()` |
| 17 | `SIT.aoa` / `SIT.tdoa` 对三种底色的对比度 ≥ 3:1 | D2-4 的同一函数 |
| 18 | `demo-01` + `tdoa` → `--validate` 报 `param`，错误落到「多站定位」卡片并标 `✕` | — |
| 19 | 3×3 框图存 / 取逐字节相同 | 同 `slice4` |
| 20 | 全程无未捕获异常 | — |

---

## 10. 不做与风险

### 10.1 不做（留给 05 的 P1 / P2 与后续）

| 项 | 去处 | 理由 |
|---|---|---|
| 阵列测向（MUSIC、相位干涉） | 05 P1，端口 `ArrayIQStream` | 需要多通道 IQ 与阵列流形，超出首期单站单通道（04 §12.1） |
| IQ 相关的真到达时间估计 | 05 P2，端口 `MultiSiteIQSet` | 500 kS/s 下时差分辨率约 2 µs ≈ 600 m，做不出有意义的 TDOA；需要 ≥ 10 MS/s |
| 真实站钟、GNSS 驯服、`DeviceStatus` | 05 P2 | 本方案的站钟是建模值（§6.5） |
| 频差定位（EM-S-08） | 后续 | 无参考实现；需要长相干积累 |
| 跨站关联（EM-S-11）与多假设 | 后续 | 本方案假定「同一 `emitter_id` 的报告属于同一目标」，即关联已知。多源同频时这是一个真实的简化，写进模型卡 |
| `TrackReport` 航迹滤波 | 05 P2 | 定位点逐帧独立，不做卡尔曼 |
| NLOS 几何驱动的测向多径误差 | D3 之后 | `sigma_mp_nlos_deg` 参数已留，但帧里的 LOS 现在恒为真 |
| 用户自定义站点组合模板 | P2 | 与 09 §6.11 的「子系统封装」同批 |

### 10.2 风险

| 风险 | 量级 | 处置 |
|---|---|---|
| 节点数随 N×K 膨胀 | 3×3 = 58 个节点 | 演示上限 8×8；频率计划面板给节点数读数；超 8 报错 |
| 叠加支路块长不一致被静默覆盖 | 会丢样点且不报错 | 编译器统一派生块长 + 组件侧比对即 `Error`，两道闸（§3.1） |
| `report_entities` 重复导致 `track.jsonl` 涨 K 倍 | 前端画 K 遍 | 编译时只在第一个 `scn` 上开，加单测 |
| 相关加权与独立加权的差异被误当缺陷 | 迹比差约 10–20% | 两种口径并列进模型卡与验收表，`independent_pairs` 保留只为守 emcore golden |
| M2 的定位精度被当成设备指标 | 对外解释风险 | §1.3 的三条声明 + 评价页标注「模型自洽性检查」 |
| 同频双源的谱线读数在短窗下不收敛 | e2e 第 10 项会假失败 | 判据显式要求 `stat=mean` 且窗 ≥ 2 s |

---

## 11. 决策与修正记录

### 11.1 本方案确立的决策（记为 D-053）

见 §0 的十二条 + 不做清单。

### 11.2 对既有文档的修正

| 文档 | 修正 |
|---|---|
| CLAUDE.md `:71` | 「05 P0 八个端口本期只做命名预留」改为「`BearingReport` / `PositionReport` 已按 M2 效应级实现（D-053），其余五个仍只做命名预留；另补充 05 名单外的 `ToaReport`」 |
| 06 §1.2 | `ToaReport` 加入「观测量归约」行；`PositionReport` 从「后置预留」行移出。**这一处与 CLAUDE.md `:71` 原本就有口径出入**（06 把 `BearingReport` 放在归约行，CLAUDE.md 说全部预留），本次一并统一 |
| `docs/concept-model-layers.md` EM-S-05/06/07/08 行 | 「首期只作为接口与误差语义参考，实现放在 P1、P2」改为「EM-S-05 / 06 / 07 自 D-053 起以 M2 效应级实现（纯软件多站，05 §3.2 允许）；阵列与 IQ 相关实现仍在 P1 / P2；EM-S-08 不做」 |
| `docs/scenario-format.md:48` | 「首期单站，多站只作结构预留」→ 多站自 D-053 起启用（§6.5） |
| `docs/display-route.md` §4 | 「测向线与误差椭圆的生产者都还不存在」→ 自 D-053 起有生产者；补三个新色值的实测对比度 |
| 10 报告 §2.1 | 九个槽位 → 十一个 |
| 10 报告 §3.2 | `AntennaGain.scene_bindable` 写作 true，与 `engine/src/antenna.cpp:68` 的实际值 false 不符，改为 false |
| 10 报告 §4.2 | `detections.jsonl` 加 `node_id` 与 `site_id`；`on_detection` 回调加 `node_id`。多站下每站一个检测器，不带节点标识就分不清是哪一站检出的 |
| 10 报告 §4.5 | `truth.jsonl` 加 `emitter_id` 与 `site_id`；`Evaluator` 由「绑一个源」改为「绑一个站、吃该站全部链路帧」；`metrics.json` 按 `site_id` 分节并预留 `localization{}` 段 |
| 10 报告 §4.6 | 产品清单加 `bearings.jsonl` / `positions.jsonl` |
| 08 报告附录 | 组件数、端口数、错误码数三处计数随实现更新（L-8） |

### 11.3 与计划的偏离（2026-09-09 实施中记录）

| # | 计划里的说法 | 实测 | 处置 |
|---|---|---|---|
| 1 | §3.4：`independent_pairs` 把参考站噪声当独立，**低估**协方差、结果偏乐观 | 方向相反。五种站数与 σ 配比（3/4/5 站，拾取与同步各占主导）下，`independent_pairs` 的迹比 0.755–0.915，即报的协方差**比实际散布大**，是保守；`correlated_reference` 迹比 1.004–1.013 自洽 | §3.4 改写并加实测表；单测断言改为「独立口径 < 0.95 且小于相关口径」。两个口径都保留，**不调参使其一致**（铁律 10） |
| 2 | §3.4：`min_crossing_angle_deg` 的判据是「测向线**最大**张角低于此值即判几何退化」 | 参数名叫「最小交会角」而比的是最大张角，名实不符；更要命的是最大张角**看不见近简并**——「两条近乎平行的线 + 一条好线」的最大张角有 51–90°，被 emcore 的分级判成 good，而这批解的 2σ 椭圆覆盖率只有 **45%** | `aoa_geometry_grade`（emcore 口径）一字不动以守 golden；**另加**量 `min_crossing_deg`（最小两两交会角，把测向线当无向直线折到 [0, 90]），报告行加 `min_crossing_angle_deg`，降级判据改用它。单测钉住「最大张角 165° 判 good 而实际交会角只有 15°」 |
| 3 | §9.2 第 14 项：`positions.jsonl`(aoa) 对 `uav-1` 真值落 2σ 椭圆内 ∈ [0.80, 0.93] | `uav-1` 只有 75.5%。三步排查：Python 参考独立重算 600 个解、相对误差 1.68e-12（流水线没错）→ 对称几何下 σ 取 1.5°/4°/8.18° 覆盖率都在 86% 上下（σ 大本身不是原因）→ 打印真方位发现 `uav-1` 与其中两站近乎共线（210.66° / 213.81°，差 3.2°） | 验收改按**有效档**取：最小交会角 ≥ 15° 且无低权重站，实测 **87.0%**、经验 50% 半径 / CEP = 0.920；降级档 80.5% 另列，并断言「低于门限的每一行都写明 `crossing_angle_below_min`」 |
| 4 | §9.2 第 15 项只要求 `time_quality` 非 null | 时差定位的 2σ 覆盖只有 35%。按目标拆开：`uav-2` / `uav-3` 扣掉系统偏差后是 **87.5% / 86.5%**，偏差 4.4–4.5 m 与 `site-3` 的 `bias_ns = 20`（20 ns × c = 6 m 距离差）对得上——**协方差本身没问题，不能表达的是那个未标定的固定钟差** | 这是 `bias_ns` 存在的意义，作为特性保留并写进模型卡。验收改为三条：中位误差米级（实测 7.95 m）、系统偏差可见（4.49 m）、几何良好且扣掉偏差后 2σ 覆盖 ∈ [0.80, 0.93]（实测 85.3%） |
| 5 | §3.3：到达时间的相关带宽沿用 emcore 的 10 MHz 标称值 | 写死常数会给出与配置无关的假精度（500 kS/s 与 20 MS/s 差 40 倍） | 组件改用**站的实际采样率**；`legacy::toa_sigma_s` 保留原常数只为守 golden |
| 6 | §3.4：`MultiSiteLocator` 需要站址，按「至少绑一个对象」的规则绑站 | 「绑一个站」对全图唯一的融合节点没有语义 | 改为**站址随报告走**：`BearingReport` / `ToaReport` 加 `site_lon / site_lat / site_alt_m`，融合节点因此是纯函数式的，不绑场景、不读场景文件。与「身份靠报告自带」（§0 ⑦）是同一条原则 |

**实现中修掉的、与本方案无关的既有缺陷**（详见 WORKLOG 同日条目）：框图页换场景只改引用不载入场景文档；「场景已同步」的闸只看 id 不看载入状态；换场景后 `scenario_ref.sha256` 不回填；多站的观测点 id 带实例后缀导致「重新运行」提示不消失；`slice4-smoke` 里读的 `app.task.id` 根本不存在（断言一直空转）；`probeApp` 逐项挑字段，新加的探针字段不加就看不见；`ChainView` 的同步 `useEffect` 位于早退之后，框图一旦解不开就违反 hooks 顺序直接崩。

---

## 附录 A. 3 源 × 3 站编译骨架（节点与连线，省略参数）

```
nodes:
  scn__site-1 (ScenarioSource, bind site-1, report_entities=true)
  scn__site-2 (ScenarioSource, bind site-2, report_entities=false)
  scn__site-3 (ScenarioSource, bind site-3, report_entities=false)
  tx__uav-1 tx__uav-2 tx__uav-3            (SceneEmitterSource, bind emitter)
  ── 站 1 ──
  tx_ant__uav-1__site-1  ch__uav-1__site-1  rx_ant__uav-1__site-1
  tx_ant__uav-2__site-1  ch__uav-2__site-1  rx_ant__uav-2__site-1
  tx_ant__uav-3__site-1  ch__uav-3__site-1  rx_ant__uav-3__site-1
  sup__site-1  rx_fe__site-1  adc__site-1  det__site-1  df__site-1  toa__site-1
  ── 站 2、站 3 同上 ──
  loc (MultiSiteLocator)

edges（站 1 部分）:
  scn__site-1.link:uav-1 → tx_ant__uav-1__site-1.scene
  scn__site-1.link:uav-1 → ch__uav-1__site-1.scene
  scn__site-1.link:uav-1 → rx_ant__uav-1__site-1.scene
  scn__site-1.link:uav-1 → df__site-1.scene1
  scn__site-1.link:uav-1 → toa__site-1.scene1
  （uav-2 → scene2、uav-3 → scene3，同法）
  tx__uav-1.out → tx_ant__uav-1__site-1.in → ch__uav-1__site-1.in → rx_ant__uav-1__site-1.in
  rx_ant__uav-1__site-1.out → sup__site-1.in1
  rx_ant__uav-2__site-1.out → sup__site-1.in2
  rx_ant__uav-3__site-1.out → sup__site-1.in3
  sup__site-1.out → rx_fe__site-1.in → adc__site-1.in → det__site-1.in
  df__site-1.out  → loc.b1
  toa__site-1.out → loc.t1
  （站 2 → b2/t2，站 3 → b3/t3）

节点数 = 3(scn) + 3(tx) + 27(链路三件) + 3×6(站内六件) = 51；加 loc = 52
（不含 ddc / chan 两个旁路槽位；启用则 58）
```

## 附录 B. `demo-03` 场景要点

不动 `demo-01`（切片 ② 的回归夹具），也不抢 C-8 的 `demo-02`（宽带场景）。

| 项 | 取值 |
|---|---|
| 观测区域 | 北京亚运村（同 `demo-01`） |
| 站点 | 三站三角形：`site-1` 北、`site-2` 西南、`site-3` 东南，基线约 2.2 km |
| 站钟 | `site-1` 3 ns locked、`site-2` 5 ns locked、`site-3` 10 ns **holdover** + `bias_ns = 20` |
| 辐射源 | `uav-1` tone 27 dBm 穿越三角形；`uav-2` **与 uav-1 同频** tone 24 dBm 绕圈（制造同频混叠）；`uav-3` burst 20 dBm 悬停，30 s 关 40 s 开 |
| 采样率 | 500 kS/s（与 `demo-01` 同口径）|
| 铁律 4 校验 | `\|Δf\| + B/2 + 保护带 = 48828 + 200000 < 250000 = Fs/2` ✔ |
| 黄金航迹 | `scripts/gen_scenario_track_golden.py … demo-03 … --out tests/golden/scenario-track-demo-03.json`；`test_scenario.cpp` 对拍 |

## 附录 C. 依据条款索引

| 主题 | 条款 |
|---|---|
| 端到端对象含测向定位 | 04 §5.1 |
| 真值只进评价器 | 04 §5.2；辨析见本报告 §1.3 |
| 模型输入可以是真值 | 01 §6.2 |
| 纯软件多站可先行 | 05 §3.2 |
| 测向 P1 / 多站定位 P2 | 05 §3.1 |
| 端口抽象与四态降级 | 05 §6.2.1、§6.2.3 |
| 四重时间与失锁语义 | 05 §6.2.2；铁律 3 |
| 单站测向误差预算 | EM-S-05 §10.7、§10.8、§10.14 |
| AOA 交汇与几何质量 | EM-S-06 §10.4–§10.7 |
| TDOA 误差预算、参考站、可行性门、时统质量 | EM-S-07 §10.3–§10.5、§10.8–§10.9 |
| 跨层一致性 | 01 §8；04 §16.3 |
| 参数帧口径与调度器覆盖 | 08 §9；D-049 ③ |
| 常数策略 | D-009；emcore README |
| 第三方代码边界 | 铁律 13 |
