# 07. 遮挡落点与坐标基座

> **版本** v1.0，2026-09-17。**定位**：把两项从 2026-09-02 挂到今天的 OPEN 拍死，并定清楚**建筑遮挡怎么接进已经跑起来的 `geo/` 帧生产端**。这是 D3-0，切片 ⑤ 的设计前置。**依据**：01 §3（M/E/V 三维正交分级）、§3.5、§8.3；04 §5.1、§7.3（传播模型简化策略）、§7.4、§9.2（溯源）、§12.2、§15.3；05 §5.2、§6.2.3、§25；概念模型 EM-P-04（城市建筑遮挡）、EM-P-13（传播模型选择与降级）、EM-T-04（建筑材质）；08 报告 §9、§15；12 报告 §2.2、§4.4；13 报告 §11；EM-C-UAV 08（覆盖区域遮挡计算关键技术）；决策 D-002、D-005、D-009、D-010、D-013、D-033、D-035、D-049、D-053、D-058、D-059、D-061；铁律 1、2、5、6、8、9、10、11、12、14、15。**本方案的结论记为决策 D-074。**
>
> **用户拍板（2026-09-17，两条）**：
> ① **遮挡计算落点 = 浏览器 TypeScript 复算，并与 C++ 黄金基准对拍**（备选的「引擎算、浏览器只显示」未采纳）；
> ② **坐标基座 = vendored GeographicLib**（备选的「vendored foundation」与「自写闭式转正」未采纳）。
>
> 两条都与实施方的推荐不同，选项、代价与推荐理由原样留在 §2.1 与 §3.1，供日后对账；**本报告此后一律按用户的决定展开，不再复述比较**。
>
> **2026-09-17 当日复核一次**：用户问「拿 foundation 当权威也没问题吧，需要换成 GeographicLib 吗」。答：foundation 当**离线对拍件**没问题且代价近零，但当**运行时基座**要拖 66 个 `.cpp`（只有 16 个与坐标有关，§3.1）。把三个选项连同新量到的链接闭包数字重新摆过之后，**用户维持 vendored GeographicLib**，本报告不改。
>
> 本方案只定语义、接口、口径、步骤与验收，不含实现记录；实现中核实到的偏差按 06 §14 体例另记修正。量化数字一律「原型阶段验证值」（D-028）。

---

## 0. 一页结论

| # | 问题 | 结论 |
|---|---|---|
| 1 | 今天的视距是什么 | **一个字面量**。`geo/src/link_budget.cpp:33` 写着 `out.line_of_sight = true;`，`geo/` 下没有任何遮挡代码，`data/scene/beijing-yayuncun/buildings.geojson` 的 **47582 栋楼一栋也没进过链路计算**（D-058 §1.1 已核实过一次，本次逐行复核仍然成立） |
| 2 | 非视距在界面上缺的是什么 | **只缺生产者**。`line_of_sight` 从链路帧一路通到 `links.jsonl`、WS `link` 事件、`sceneStore`、链路线着色（`web/src/scene/layers/situation.ts:186`）与焦点卡的 ● 徽标。非视距红 `#b91c1c` 在 D2-4 标定过对比度（对陆地 5.79:1），**至今一次没画出来过** |
| 3 | 遮挡贵不贵 | **不贵**。拿北京亚运村 47546 栋真实建筑实测：单次射线 **0.6–6.7 µs**（0.5 → 9.5 km），桶网格建一次 10.5 ms，GeoJSON 解析 144 ms。整条任务的遮挡预算 ≈ 单核的 **1.7%** |
| 4 | 打开 E3 之后演示会变成什么样 | 站点 30 m 时 **76–100% 的几何判为非视距**、刀口损耗均值 **29–35 dB**。demo-01 那条航线会呈现「起飞被挡 37 dB → 过顶视距 → 远去后 32 dB」，末端带内信噪比从 33.8 dB 掉到 **1.4 dB**。**物理上对，演示上也好看**；E1 仍是缺省（D-058），评价基准因此零变化 |
| 5 | 遮挡算在哪（拍板一） | **两边都算：`geo/` 的 C++ 是真理源，浏览器用 TypeScript 复算**，两侧同守 `tests/golden/occlusion.json` 的 148 例。由此立三条纪律，见 §2.2 |
| 6 | 坐标基座用什么（拍板二） | **vendored GeographicLib**（`Geocentric` + `LocalCartesian` 及其依赖闭包），放 `third_party/geographiclib/`，署名进 `THIRD-PARTY-NOTICES`。`IGeodesy` 的实现从一个变两个，`ClosedFormWgs84` **保留**作独立第二实现与对拍件，不删（D-035 就此回收） |
| 7 | 换基座要付什么 | **一次真正的基准变更**。实测（拿 foundation 作权威实现的替身）：demo-01 两条航段弦长差 1.2e-10 / 2.7e-10 m，**相对 1e-13**。航迹黄金基准按 double 往返精度打印，相对 1e-13 必然翻动最后两三位——**三份航迹基准都要重冻**；产品 `.f32` 那一侧 1e-13 远低于 float32 的 1.2e-7，**大概率逐字节不变但必须用字节比对去证**（§3.4） |
| 8 | 帧结构改不改 | **不改**。遮挡走 `extra_loss_dB`（`extra += knifeEdge_dB`），`path_loss_dB = free_space_dB + extra_loss_dB` 的恒等式照旧；`link_budget.h` 头注承诺的「只改 `extra_loss_dB` 与 `line_of_sight` 两处」在这一步逐字兑现。`SceneParamFrame` 一个字段不加 |
| 9 | 遮挡能不能直接写进 `frame(k)` | **能**，而且必须。它是几何的纯函数，满足 `frame()` 「可任意乱序、任意次数调用，结果逐位相同」的硬不变量；这正是它与统计阴影的区别——阴影有序列状态，所以 D-058 ⑧ 要在 `init()` 里一次算完 |
| 10 | `line_of_sight` 怎么定义 | `= !blocked`，即**几何上有没有被楼切断**，与损耗大小无关。掠射 0.2 dB 的情形也算非视距——这是事实，不替用户折算（D-039「只摆事实」） |
| 11 | E3 与 E2 的效应怎么共存 | **加两道闸**（D-058 已有两道，本报告补第三、第四道）：**E3 与统计阴影互斥**、**E3 与城市经验互斥**——两者都是建筑遮挡的统计等效，和确定性绕射同时开就是同源双计。`configure()` 报错并说明，不静默禁用 |
| 12 | 投影常数怎么办 | 照 D-053 的先例：`110540 / 111320·cosφ` 只留在 `geo::legacy` 里守 golden，引擎喂严格 ENU。**差多少已经量了**：亚运村处纬向 −0.445%、经向 −0.138%，折到 demo-01 整条航线的刀口损耗上**最大 0.1 dB** |
| 13 | 单元测试怎么在没有场景数据包的机器上跑 | `buildings.geojson` 不入 git，但 **`tests/golden/occlusion.json` 自带那 12 栋楼**，golden 对拍因此自足；整份 47546 栋只在有数据包的机器上跑，缺了就**明说跳过、不当作通过**（C-11 的先例，D-073 ③） |

---

## 1. 现状核实

### 1.1 今天的视距是一个字面量

```
geo/src/link_budget.cpp:33
    out.line_of_sight = true;   // 显式平地假设，D3 接入遮挡后在此改
```

`geo/src/` 下十个文件（`activity / df_error / fix_geometry / geodesy / kinematics / link_budget /
locate_aoa / locate_tdoa / propagation / scenario`）没有一个字涉及建筑。`geo/include/cuav_geo/`
十个头同理。D-058 在 2026-09-10 核实过这件事并把 E3 的枚举位留了出来
（`PropLevel::E3`，`geo/src/propagation.cpp:167` 收到即报错）；本次逐行复核，结论不变。

### 1.2 非视距这条显示链路早就通了，只差生产者

| 环节 | 位置 | 状态 |
|---|---|---|
| 帧字段 | `geo::LinkBudget::line_of_sight` | 有，恒 `true` |
| 进参数帧 | `engine/src/scenario.cpp:344` | 有 |
| 进链路帧 | `engine/src/scenario.cpp:378` | 有 |
| 落盘 | `data/runs/<task>/links.jsonl` | 有（`docs/api-versions.md` §4 读数表列了它） |
| 进浏览器 | `web/src/scene/sceneStore.ts:26,311` | 有 |
| 链路线着色 | `web/src/scene/layers/situation.ts:186` `['case', ['get','los'], SIT.linkLos, SIT.linkNlos]` | 有 |
| 焦点卡徽标 | `web/src/scene/cards/Cards.tsx:56-57` | 有 |
| 时间轴统计 | `web/src/shell/Timeline.tsx:32` 数 `losOk` | 有 |
| 色值标定 | `docs/display-route.md:69` `linkNlos = #b91c1c`，对陆地 5.79:1 / 建筑 4.97:1 / 水面 4.60:1 | **2026-09-06 标定完成，至今零次渲染** |

这和 D-061 ⑩ 记的「链路线因标识拆分错从未画出」是同一类事：**通路建好了，生产者没上线**。
本步上线生产者之后，界面侧不需要为「链路级非视距」写任何新代码。

### 1.3 可用资产（本次逐件核实在盘）

| 资产 | 位置 | 规模 | 用途 |
|---|---|---|---|
| `IMapQuery` 接口 | `C-UAV Model Demo/emcore/include/emcore/map/imap_query.h` | 73 行 | 模型与地图之间的唯一边界（05 §3.1） |
| `LocalSceneAdapter` | 同上 `map/local_scene_adapter.{h,cpp}` | 74 + 266 行 | 100 m 桶网格 + Amanatides-Woo DDA，命中语义「侵入最深的等效单刀口」 |
| `segmentOcclusion` | 同上 `models/occlusion.{h,cpp}` | 29 + 30 行 | 只做刀口衍射物理，几何全在适配器 |
| `fresnelV` / `knifeEdgeLoss_dB` | 同上 `models/propagation.cpp` | 两个函数 | R-2 明确留给本步移植（`tests/golden/propagation.json` 的 `_meta.kept` 写着） |
| 遮挡黄金基准 | 同上 `tests/golden/occlusion.json` | **148 例 + 12 栋楼**，3184 行，`tolerance_rel 1e-9` | 两侧对拍的唯一判据 |
| 传播黄金基准余下六组 | 同上 `tests/golden/propagation.json` | `fresnelV` 108 例、`knifeEdgeLoss_dB` 14 例 | 本步启用（R-2 只取了大气与降雨两组） |
| TypeScript 祖本 | `C-UAV Model Demo/em-demo/src/models/occlusion.ts` | 261 行 | C++ 就是从它移植的；本步的浏览器侧是**回迁祖本**，不是新写 |
| 建筑几何 | `data/scene/beijing-yayuncun/buildings.geojson` | 15909168 B、47582 要素、sha256 `269a8674…` | 渲染与遮挡的单一来源（铁律 11） |

`occlusion.json` 自带那 12 栋楼，是「黄金基准自足」的关键：单测不依赖 15.9 MB 的数据包。

### 1.4 实测：遮挡库在 47546 栋真实建筑上的代价

把 emcore 快照的原码直接编出来，喂北京亚运村的建筑集（`MultiPolygon` 36 件本次先跳过，
见 §7.3），站点置于观测区域中心，目标绕圈 720 个方位：

| 一次性代价 | 值 |
|---|---|
| `buildings.geojson` 用 nlohmann 解析 | **144 ms** |
| 抽出遮挡要的字段 | 5 ms |
| `setBuildings` 建桶网格（47546 栋、343832 个顶点） | **10.5 ms** |
| 合计（只在选了 E3 时发生一次） | **≈ 160 ms** |

| 单次射线 | 0.5 km | 2 km | 5 km | 8 km | 9.5 km |
|---|---|---|---|---|---|
| 耗时 | 0.6 µs | 1.6 µs | 3.4 µs | 5.7 µs | **6.7 µs** |

**整任务预算**：K × N ≤ 64 条链路 × 20 Hz 帧率 × 2 次（`frame(k)` 每个 k 被调两次，
D-058 ⑧）× 6.7 µs ≈ **17 ms/s**，即单核的 **1.7%**。将来 V-5 的覆盖场 200 × 200 = 40000
条射线约 **0.27 s**。**性能不是这一步的约束**，这一点要在报告里写死，免得后面有人为它做无谓的优化。

### 1.5 实测：打开 E3 之后这个演示会变成什么样

非视距比例与刀口损耗均值，随**站点天线高度**（AGL）与目标高度、距离变化：

| 站点天线 | 目标 30 m / 2 km | 目标 80 m / 5 km | 目标 150 m / 9.5 km |
|---|---|---|---|
| 10 m | 98.8% 挡，36.1 dB | 99.0% 挡，35.2 dB | 98.9% 挡，35.0 dB |
| **30 m（demo 现值）** | 95.7% 挡，32.0 dB | 91.7% 挡，31.6 dB | 88.8% 挡，31.8 dB |
| 60 m | 86.2% 挡，22.6 dB | 76.8% 挡，18.9 dB | 76.9% 挡，17.6 dB |

**站点天线高度是这里最敏感的一个参数**，从 30 m 抬到 60 m 能把损耗均值砍掉一半。它是场景
参数（`sites[].position.alt_m`），不是模型参数——本步不动示例场景。

demo-01 那条航线（站 30 m、机 50 → 160 m）逐点：

| 位置 | 距离 | 刀口损耗 | 侵入深度 | 带内信噪比（E3） | 带内信噪比（E1） |
|---|---|---|---|---|---|
| 起飞点 | 210 m | 37.0 dB | 26.2 m | 20.3 dB | 57.3 dB |
| 过顶 | 34 m | **0（视距窗口）** | 0 | 73.2 dB | 73.2 dB |
| 第一航段末 | 799 m | 28.4 dB | 19.4 m | — | — |
| 航线末端 | 3155 m | 32.4 dB | 34.4 m | **1.4 dB** | 33.8 dB |

也就是说 E3 一开，瀑布上会出现「被楼挡住 → 过顶时拔起三十几 dB → 远去后重新沉下去」的
三段式，末端逼近检测门限。**这正是切片 ⑤ 想要的可见效果**（06 §9E：「建筑后方 NLOS 时
瀑布功率跌落」），而且它是算出来的不是编出来的。

---

## 2. 拍板一：遮挡计算落点

### 2.1 选项与代价（留档）

| | A 引擎算、浏览器只显示 | **B 浏览器 TS 复算并守同一份 golden（用户选定）** |
|---|---|---|
| 切片 ⑤ 的前端代码量 | 零 | 移植 `occlusion.ts` + 桶网格 + TS 侧 golden 对拍 |
| 物理真理源数量 | 1 | 2（C++ 为准，TS 守同一份 golden） |
| 浏览器代价 | 0 | 实测 `JSON.parse` 15.9 MB = 72 ms、堆 +41 MB |
| 交互探测（悬停即出视距） | 需一次往返或新端点 | 本地即时 |
| 覆盖场 / 精度场（V-5，切片 ⑨） | `cuav_run --field` 网格产品，实测 0.27 s | 浏览器本地铺 |
| 「点选建筑两侧一致」验收 | 布目标跑两次，或补探测端点 | 直接点 |

实施方当时推荐 A（理由：切片 ⑤ 前端零代码、少一个要长期同步的真理源）。**用户选定 B。**
往后按 B 展开。

### 2.2 选定 B 之后立的三条纪律

选 B 等于接受「同一套物理有两份实现」。项目里这已经是第四次了（`reduce.ts` 对服务端归约、
`analytic.ts` 对 `energy_detector.py`、`firSpecs.ts` 对冻结抽头表），每一次都靠同一套办法
守住。这一次把它写成明文纪律：

1. **C++ 是真理源，TS 是复算**。任何改动的顺序固定为：改 C++ → 重生成 golden → TS 对拍通过。
   反过来（先改 TS）一律不允许，哪怕只是"界面上先看看效果"。
2. **TS 不得有 C++ 没有的分支**。浏览器需要而引擎不需要的东西（比如按视窗裁剪、按缩放级别
   降采样）只能写在**调用侧**，不能写进复算函数本身；复算函数的输入输出与 C++ 一一对应。
3. **两侧同守一份文件**：`tests/golden/occlusion.json` 由 C++ doctest 与 web `node:test`
   各读一遍，判据都是 `rel ≤ 1e-9`。web 侧读仓库里的 golden 已有先例
   （`product-window.json`、`analytic-pd.json`、`firSpecs.test.ts` 直接读 `models/**/fir_*.json`）。

**一处要当心的实现差异**：em-demo 祖本 `occlusion.ts` 的外环是**闭合**的
（`IndexedBuilding.xs` 注释写「闭合」），而 C++ `LocalSceneAdapter` 的约定是**首尾不闭合**
（闭合环由遍历隐式补齐）。两者在算法上等价，但顶点数差一个，逐位对拍时会暴露。移植时
**以 C++ 的约定为准**，TS 侧改成不闭合。

祖本的另一处要改：投影原点 `LAT0 / LON0` 是**写死的西安常数**（`34.3506 / 108.9405`，
em-demo 的观测区域）。本项目的观测区域是北京亚运村，且 golden 是在西安原点上导出的——
**原点必须参数化**，不能留成模块常数。

### 2.3 浏览器侧的数据通路与懒加载

| 项 | 做法 |
|---|---|
| 几何来源 | **同一份 `buildings.geojson`**（铁律 11）。MapLibre 已经为渲染拉过它一次（在 worker 里）；复算要的是主线程里的一份可算几何，**不复用 MapLibre 的内部副本**（那是私有结构，拿它等于把渲染实现细节焊进物理） |
| 何时加载 | **懒加载**：只有用户打开遮挡相关的显示（探测开关 / 覆盖场）才取。场景页平时不付这 72 ms 与 41 MB |
| 索引 | 与 C++ 同构的 100 m 桶网格；建一次缓存住，场景切换或数据包换了才重建 |
| 与主 store 的关系 | 走独立模块，不进 `useReducer` 主 store（D-049 ⑩ 的先例：高频量与大对象一律外挂） |
| 失败时 | 取不到或解析失败 → 显示「遮挡数据未加载」并**不画任何遮挡结论**，不退回「一律视距」（铁律 15） |

### 2.4 D4「渲染—物理同源」在这条路上怎么验

D4 的退出条件是「同一 GeoJSON 驱动渲染与遮挡；探针无副作用」。选 B 之后它变得**更容易验
也更值得验**，因为两件事第一次真的在同一个进程里：

- **同源**：断言渲染图层的数据源 URL 与复算模块的数据源 URL 是同一个字符串，且要素数一致；
- **一致**：在地图上点一栋楼的两侧各取一点，向焦点站各算一次——**楼后那一侧必须非视距、
  楼前那一侧必须视距**，这就是 06 §9E 切片 ⑤ 写的「点选建筑两侧一致」，选 B 之后它是一次
  点击就能验的事；
- **无副作用**：探针只读，复算不写任何 store（沿用 `probe.ts` 的既有约定）。

---

## 3. 拍板二：坐标基座

### 3.1 选项与代价（留档，含本次实测）

| | A vendored foundation | **B vendored GeographicLib（用户选定）** | C 自写闭式转正 + foundation 冻成 golden |
|---|---|---|---|
| 入库代价 | **实测 153 个文件 / 1886299 B**，其中 66 个 `.cpp`；闭包里捎带轨道预报、太阳系历、日志系统、输入解析器。**且它不收缩**（2026-09-17 复量）：从 `UtEllipsoidalEarth.cpp` 起按未定义符号逐轮补，加到 35 个 `.cpp` 时仍剩 57 个未定义符号，一路要到 66 个才链得上。这 66 个里**真正与坐标 / 椭球有关的只有 16 个（327297 B）**，其余是轨道天体 12 个（408956 B）、输入文件解析 19 个（348808 B）、回调与实体 8 个（280796 B）、日志异常栈回溯 8 个（98771 B） | 只取 `Geocentric` + `LocalCartesian` 及其闭包，量级远小于 A | 零 |
| 能不能直接编 | **不能**，要补两个快照里根本不存在的宏 `MODEL_API` 与 `UT_DEPRECATED`（本次补了两行就编过了） | 独立工程，自带 CMake | — |
| 与现有实现的一致性 | **实测 1e-9 m**（纳米级） | 预期同量级，D3-1 实测后回填 | — |
| 现有实现的往返精度 | — | — | 实测 ≤ **0.9 µm**（0–10 km 高度）；foundation 自己在海平面反而差一点（−4.6e-8 m） |
| 新增第三方件与署名 | 有（已获授权，D-004） | **有**（MIT/X11） | 无 |

实施方当时推荐 C（理由：物理上换不回任何东西，零新依赖，权威性靠对拍而不是靠引入）。
**用户选定 B。** 往后按 B 展开。

### 3.2 取哪几件、放哪、怎么核对

| 项 | 定法 |
|---|---|
| 取哪几件 | `Geocentric`（LLA ↔ ECEF）、`LocalCartesian`（ECEF ↔ 站心 ENU）及其**最小依赖闭包**（`Constants` / `Math` / `Config`）。**不取**大地线求解（`Geodesic`）、UTM/MGRS、磁场、重力——本项目的距离口径是 ECEF 弦长（D-049 ①），不需要大地线 |
| 放哪 | `third_party/geographiclib/`，与 `third_party/foundation/` 平级；保留原文件头与 `LICENSE.txt` |
| 许可 | MIT/X11，署名进仓库根 `THIRD-PARTY-NOTICES`（与 `ws`、doctest、nlohmann 同法） |
| 怎么取 | **一次联网动作**，与 `scene/fetch_osm_buildings.py` 同一性质（铁律 6「建库可联网、运行不联网」）。取来之后**记录版本号与压缩包的 sha256** 进 `THIRD-PARTY-NOTICES` 与 D3-1 的 WORKLOG 条目；此后构建零网络 |
| 构建 | 不引它的 CMake 工程，**只把需要的 `.cpp` 加进 `cuav_geo` 的源文件列表**（同 vendored doctest / nlohmann 的做法）。`geo/` 的「零第三方依赖」这句话随之作废，**三处表述要一并改**：`geo/include/cuav_geo/geodesy.h:15`「本库零第三方依赖」、`scenario.h:7`「这样 `geo/` 保持零第三方依赖」、CLAUDE.md 引擎那条里的「`geo/` 仍可独立构建……它零第三方依赖」；`propagation.h:9` 的「**本文件**零第三方依赖」是文件级的，仍然成立、不改。**这是要主动改掉的既有表述，不能让它留着自相矛盾** |
| 平台 | MSVC / GCC 双平台都要过（铁律 16）。GeographicLib 是标准 C++，风险低，但仍在 D3-4 一并验 |

**选它而不选 foundation 的两条理由**（2026-09-17 复核时补记）：① foundation 为一次 LLA↔ECEF 换算要拖进一个会开文件、写日志、抓栈回溯的子系统（上表），GeographicLib 是单一用途的大地测量库，闭包小得多、没有这些行李；② **垂直基准那项 OPEN 将来可能用得上它的 `Geoid`**（EGM96 / EGM2008 的大地水准面高），foundation 没有对应件。首期是显式平地假设（铁律 2）用不到，但这是唯一一条「将来可能真的需要」的功能性理由；代价是它要额外的格网数据文件，届时按离线资产另行登记。

**权威性这件事要说实话**：三家实现（自写闭式、foundation、GeographicLib）在本项目的尺度上互相差 1e-9 米量级，**谁也不比谁更准**；选 vendored 的第三方件换来的是出处叙事与将来的功能余量，不是精度。这一条写在这里，免得日后把「用了 GeographicLib」当成精度论据。

**一处必须先核实的前提**：GeographicLib 的近年版本要求 **C++11 及以上**，而本项目
与 emcore 保持 C++14（CLAUDE.md「C++14 起（与 emcore 兼容，提升标准 OPEN）」），
两者相容。若取到的版本要求 C++17，则**先改 CMake 的标准再说**，并把这件事记成一条修正——
不许为了编过而悄悄提标准（那是 OPEN 项，要单独拍板）。

### 3.3 `IGeodesy` 的第二实现与 `ClosedFormWgs84` 的去留

`geo/include/cuav_geo/geodesy.h` 从 G-1 起就为这一天留好了接口：

```
// 坐标基座接口。D-035 的替换点：D3-0 换实现时只改工厂，调用方不动。
class IGeodesy { ... };
```

做法：

1. 新增 `class GeographicLibGeodesy : public IGeodesy`，`name()` 返回 `"geographiclib-<版本>"`；
2. `default_geodesy()` 改为返回它；
3. **`ClosedFormWgs84` 保留，不删**。理由有三：① 它是独立第二实现，两家对拍是这一步唯一
   能证明「换对了」的手段（同 DSP 那条线「Python 独立第二实现」的精神）；② 它零依赖，
   是将来第三方件出问题时的退路；③ 删掉等于把 G-1 那套已经通过验收的代码连同它的单测一起
   丢掉，没有任何收益。**但它不再是缺省**，头注里「D-035 的临时件」这句话要改写成
   「独立第二实现与对拍件」；
4. 新增单测：两家在 AOI 尺度（lat 39–41、alt 0–10 km）上逐点对拍，**判据 1e-6 m**——
   这是一个刻意放宽于 1e-9 的工程判据，理由写在模型卡里：两家用不同的反算算法
   （我们是 Bowring 闭式，GeographicLib 用 Vermeille / Ferrari 一类），
   本来就不该期望逐位相同，能到微米级就说明两边都对。

### 3.4 这是一次真正的基准变更

`default_geodesy()` 被**运动学**与**链路几何**两处用着：

```
geo/src/kinematics.cpp:44,53-55   航段起终点 → ECEF → 弦长 → 段时轴
geo/src/kinematics.cpp:66         速度 = 位移 / 用时
geo/src/scenario.cpp:307,312      逐帧位置的 ECEF
geo/src/scenario.cpp:347          回望视线角
```

段时轴一变，二分定位出来的插值比例就变，**每一个航迹样点都会动**。本次实测了这个扰动的量级
（拿 foundation 作权威实现的替身，因为 GeographicLib 尚未在盘）：

| 量 | 自写闭式 | foundation | 差 | 相对 |
|---|---|---|---|---|
| demo-01 第一航段弦长 | 1008.963107448167 | 1008.963107448286 | 1.19e-10 m | 1.2e-13 |
| demo-01 第二航段弦长 | 2365.778998562844 | 2365.778998562579 | 2.65e-10 m | 1.1e-13 |
| 站 → 起飞点斜距 | 211.074389159746 | 211.074389159265 | 4.81e-10 m | 2.3e-12 |

后果分两面，**必须分开说，不能合并成一句「几乎没变」**：

| 面 | 结论 | 怎么证 |
|---|---|---|
| 航迹黄金基准 | **必变**。`tests/golden/scenario-track-demo-0{1,2,3}.json` 按 double 往返精度打印（`116.40356690036484`），相对 1e-13 的扰动必然翻动最后两三位 | 重冻三份，验收判据改为**逐点相对差 ≤ 1e-12**（不是逐字节相同），并核对样点数不变（361 / 122 / 723） |
| 产品 `.f32` | **大概率不变**：路损相对变化 2.3e-12 → `20·log10` 后约 2e-11 dB → 幅度相对 1e-12，远低于 float32 的 1.2e-7 | **拿换基座前那一版建出的 `cuav_run` 直接对拍**，逐字节比较 `slice1 / slice2 / 缺省链 / demo-02 / demo-02-ddc` 的全部产品文件——与 R-1 收口时「什么都没变」用的是同一套办法（D-058 实测面） |
| 浏览器航迹预览 | 不受影响 | 它的容差本来就是 1e-6 度（golden 的 `tolerance.position_deg`） |
| 检测 / 特征 / 识别 / 真值 / 评价 | 大概率不变，同 `.f32` 一路 | 同上，字节比对 |

**这一步单独成一个提交**（铁律 10：基准变更不得夹在别的改动里）。

### 3.5 D-035 的回收口径

D-035 原文：「G-1 先用自写 WGS-84 闭式放在 `IGeodesy` 接口后，D3-0（07 报告）再换
foundation / GeographicLib 并对拍回收」。本报告兑现它：**换 GeographicLib，自写闭式降为
第二实现与对拍件**。CLAUDE.md 的 OPEN 表「坐标基座实现」一行随之销项。

---

## 4. 遮挡怎么接进帧生产端

### 4.1 接口：`LinkFrameSource` 多一个地图查询

```
bool build(const Scenario& s, const std::string& site_id, const std::string& emitter_id,
           double update_rate_Hz, std::string& err,
           const PropagationConfig& cfg = PropagationConfig(),
           const IMapQuery* map = nullptr);          // ← 新增，缺省 nullptr = 平地假设
```

- `map == nullptr` 且 `cfg.level == E3` → `build()` 失败并说明「E3 需要建筑几何，
  未提供地图查询」（铁律 15，不静默退回平地）；
- `map != nullptr` 且 `cfg.level != E3` → 地图不参与计算（合法，因为同一个引擎进程里
  K × N 个 `LinkFrameSource` 共享一份适配器，而档位是全图共用的）。

**`IMapQuery` 与 `LocalSceneAdapter` 放 `geo/`**（`geo/include/cuav_geo/map.h` +
`geo/src/local_scene_adapter.cpp`），与 emcore 的分层一致：物理只依赖接口，几何在适配器。

### 4.2 为什么可以直接写在 `frame(k)` 里

`geo::LinkFrameSource::frame(std::uint64_t k) const` 的类注释把「可任意乱序、任意次数
调用，结果逐位相同」定为**硬不变量**。遮挡满足它，因为它是（几何 + 建筑集）的纯函数——
同一个 `k` 算几次都一样。

**对比统计阴影为什么不行**：阴影是一阶自回归序列，`frame(k)` 依赖 `frame(k-1)`，
所以 D-058 ⑧ 必须在 `init()` 里按帧序一次算完存成向量。遮挡没有这个问题，
**不要照着阴影的样子给它也做预算**——那会白白占一份内存，还会在场景可编辑时引入
「建筑变了但缓存没失效」的一类错。

代价：`frame(k)` 每个 `k` 被调两次（出帧一次、上报一次），所以射线数是 2 倍。
实测预算 1.7% 单核，**不做记忆化**（记忆化要引入可变状态，与 `const` 和上面那条
不变量冲突，为 1.7% 不值得）。

### 4.3 `line_of_sight` 的定义与 `extra_loss_dB` 的并入

```
OcclusionResult o = segmentOcclusion(*map, tx_lla, rx_lla, frequency_Hz);
g.line_of_sight = !o.blocked;                 // 几何事实
// combine() 内部：extra_dB += o.obstructionLoss_dB;   included_loss_terms += "diffraction"
```

三条口径：

1. **`line_of_sight = !blocked`，与损耗大小无关**。掠射只损 0.2 dB 也算非视距。理由：
   这个布尔量的含义是「几何上有没有被楼切断」，界面上它只决定链路线颜色，
   而颜色要摆的是事实，不是实施方替用户挑的一个门限（D-039）；
2. **损耗只进 `extra_loss_dB`，不进 `free_space_dB`**——恒等式
   `path_loss_dB = free_space_dB + extra_loss_dB` 在任何档下都成立（D-058 ⑦），
   `link_budget.h` 的承诺逐字兑现；
3. **单程一份，不乘 2**。emcore 的头注写着「雷达双程 ×2，单向链路 ×1」，本系统是
   电子侦察单向链路，**取 ×1**。这一条要写进模型卡，因为它是从雷达代码里移植过来时
   最容易顺手抄错的地方。

### 4.4 `included_loss_terms` 增一个取值

D-058 ⑥ 定了 `free_space / ground_reflection / urban_mean / shadow / weather`，
本步增 **`diffraction`**。它进 `link` 事件与 `links.jsonl`，随溯源走。

---

## 5. E3 档位语义与第三、第四道闸

### 5.1 E3 包含哪几项

| 效应 | E1 | E2 | **E3** |
|---|---|---|---|
| 自由空间（EM-P-01） | ✔ | ✔ | ✔ |
| 多普勒、时延 | ✔ | ✔ | ✔ |
| 地面双径（EM-P-02） | — | 可选（主模型二选一） | **可选** |
| 城市经验（EM-P-05） | — | 可选（主模型二选一） | **禁止**（闸四） |
| 统计阴影（EM-P-08） | — | 可选 | **禁止**（闸三） |
| 大气与降雨（EM-P-07） | — | 可选 | 可选 |
| **建筑刀口绕射（EM-P-04）** | — | — | **✔ 本档的定义性效应** |

于是 E3 下 `prop_primary ∈ {free_space, two_ray}`、`shadow = false`、`env` 不再参与损耗
（只作记录）。

### 5.2 闸三：E3 与统计阴影互斥

统计阴影建模的就是「**建筑与地物遮挡造成的、随位置慢变的电平起伏**」（EM-P-08）。
E3 把这件事**确定性地**算了出来，再叠一层统计等效就是同源双计——与 D-058 ⑤ 闸二
「城市经验取含阴影裕度档时再开阴影即报错」是同一条规则的另一个实例。

处置：`PropagationConfig::validate()` 里 `level == E3 && shadow` → 返回 false，
报文说明「E3 已按建筑几何确定性地算出遮挡损耗，统计阴影是同一效应的统计等效，
二者不得同时开启；要统计等效请退回 E2」。**不静默禁用**（铁律 15）。

### 5.3 闸四：E3 与城市经验互斥

城市经验（EM-P-05 的 log-distance）的路损指数 `n` 与 `X_env` 就是**建筑密度的经验拟合**。
同理，报错并说明，建议改用 `free_space` 或 `two_ray` 作主模型。

**这条闸同时消掉一个隐患**：D-058 ④ 把 `shadow_sigma_dB` 的缺省定成「按 `env` × 视距
查表」，而 `line_of_sight` 此前恒真，表里非视距那一列从来没被取到过。E3 上线后
`line_of_sight` 才第一次会是 `false`——闸三既然禁掉了 E3 + 阴影，这一列在 E2 档下仍按
原样（恒视距）取值，行为与 2026-09-10 之前逐数值相同，**不构成基准变更**。这一点要在
实施时用字节比对确认，不能只靠推理。

### 5.4 前端与引擎的分工

| 闸 | 前端 `planChecks` | 引擎 `configure()` |
|---|---|---|
| E3 需要场景与建筑 | 查得到：观测区域数据包在不在 | 查得到：`map == nullptr` |
| E3 + 阴影 | 查得到 | 查得到 |
| E3 + 城市经验 | 查得到 | 查得到 |
| E3 + `FreeSpaceChannel`（定参） | 查得到（第 11 项 `propagation`） | **查不到**（`configure()` 看不见别的节点的类型，D-059 已如实记过） |

前端把 E3 的置灰去掉，档位说明由「待 D3（切片 ⑤）」改为列出它包含哪几项
（`ch` 卡片的既有版式，12 §5.1）。

---

## 6. 投影与常数

### 6.1 148 例 golden 是在什么投影上导出的

`_meta` 写得很清楚：原点西安 `(108.9405, 34.3506)`，投影常数 `110540 m/degLat`、
`111320·cos(lat0) m/degLon`，`tolerance_rel 1e-9`，footprint 首尾不闭合。

### 6.2 这套常数偏多少（本次实测）

| 位置 | 真值（WGS-84） | legacy | 偏差 |
|---|---|---|---|
| 39.99°N 纬向 | 111034.4 m/deg | 110540 | **−0.445%** |
| 39.99°N 经向 | 85406.3 m/deg | 85288.6 | **−0.138%** |
| 34.3506°N 纬向 | 110928.7 m/deg | 110540 | −0.350% |
| 34.3506°N 经向 | 92003.5 m/deg | 91905.8 | −0.106% |

把常数换成亚运村处的真值，重跑 demo-01 整条航线：**命中的楼一栋没变，刀口损耗最大差
0.1 dB**（31.1 → 31.0、32.4 → 32.3），其余逐点相同。

### 6.3 分法（照 D-053 的先例）

| 谁 | 用什么投影 | 为什么 |
|---|---|---|
| golden 对拍（C++ 与 TS 两侧） | `geo::legacy` 里的 110540 / 111320·cosφ | 守 148 例 `rel ≤ 1e-9`（铁律 10、D-009） |
| 引擎实际运行 | **严格 ENU**（`default_geodesy()` → `LocalCartesian`） | 铁律 1 |
| 浏览器复算 | 与它当次的用途一致：对 golden 时用 legacy，对引擎结果时用严格 ENU | 两侧口径必须同步，否则 TS 与 C++ 的"复算"会差 0.1 dB |

`LocalSceneAdapter` 的接口因此**改吃平面米**（与 D-053 把定位求解器改成平面坐标接口
同构），经纬度 → 米的投影由调用方做。`geo::legacy::project_occlusion()` 是唯一保留
旧常数的地方，`scripts/build-all.sh` 里加一条 `grep legacy::` 守卫（沿用 D-053 ⑪ 建好的那条）。

### 6.4 `fresnelV` 里的 3e8

emcore 的 `fresnelV` 用 `kSpeedOfLight = 3e8`（`core/units.h:9` 注明「与 TS 参考实现一致，
黄金向量逐位可比」），而本项目新写代码统一 `c = 299792458`（D-009）。两者相对差 6.9e-7，
落到刀口损耗上是 1e-5 dB 量级——**物理上无关紧要，1e-9 的 golden 上却是硬伤**。

处置同 §6.3：`geo::legacy::fresnel_v()` 保留 3e8 守 golden 的 108 例；
**引擎路径也调用它**——理由是刀口衍射这一整套（`fresnelV` + `knifeEdgeLoss_dB` +
适配器几何）是一个被 148 例 golden 整体钉住的模块，只换其中一个常数会让模块内部
自相矛盾（几何用严格 ENU、波长用旧光速）。R-2 当时预判「D-009 的常数冲突要到 D3 移植
`fresnelV` 时才出现」，出现了，按 D-009 的原则处理：**移植模块保留原常数，禁止顺手统一**。
这条偏离要写进模型卡与 `geo/README.md` 的常数表。

---

## 7. 建筑数据通路

### 7.1 从 `buildings.geojson` 到 `IMapQuery`

```
data/scene/<aoi>/buildings.geojson
   └─ engine/src/buildings_json.cpp   （nlohmann，与 scenario_json.cpp 同法；geo/ 不碰 JSON）
        └─ std::vector<geo::Building>
             └─ geo::LocalSceneAdapter::set_buildings()
                  └─ 各 LinkFrameSource 共享一份 const IMapQuery&
```

`cuav_run` 侧：`--scene-root` 已经在用（`engine/src/diagram_json.cpp:409-416` 会核对
观测区域清单哈希），建筑文件的路径与 sha256 都在 `manifest.json` 的 `products[]` 里
（`buildings.geojson`，15909168 B，sha256 `269a8674…`）。**读进来之后核对 sha256**，
对不上即失败并说明（铁律 8、15）。

### 7.2 `buildings.geojson` 不入 git 的后果

`.gitignore` 的 `data/**` 规则把它排除了（`manifest.json` 里也明写 `in_git: false`）。
后果与处置：

| 场合 | 能不能跑 | 处置 |
|---|---|---|
| `geo/` 单测（148 例 golden） | **能** | golden 自带 12 栋楼 |
| web 单测（TS 侧 148 例） | **能** | 同上 |
| 引擎组件级单测 | **能** | 用 golden 的 12 栋楼搭一个最小场景 |
| `tests/regression` 的 E3 算例 | 要真实建筑集 | **缺数据时明说跳过、不当作通过**（D-073 ③ 的先例） |
| e2e | 要真实建筑集 | 同上 |

### 7.3 `MultiPolygon`、无效要素、估算高度

| 情形 | 实测计数 | 处置 |
|---|---|---|
| `Polygon` | 47546 | 外环进遮挡，内环（孔）忽略 |
| `MultiPolygon` | **36** | **每个子多边形的外环各当一栋**，共用同一个 `id` 加序号后缀；孔同样忽略 |
| `height_m ≤ 0` 或顶点 < 3 | 本数据集 0 件（最矮 1.0 m） | 适配器已有的剔除逻辑照旧，剔除数进 notes |
| 估算高度 `src=est:area` | 78.55%（D-027b） | **照常参与计算**，`src` 随数据层走、界面不解释（铁律 14、D-042b） |

孔忽略是一处会让遮挡**偏保守**（算多了）的简化，写进模型卡。

### 7.4 懒加载与 `--validate`

`--validate` 今天约 9 ms（D-042 实测）。建筑**只在 `prop_level == E3` 时才读**，
校验路径不读，9 ms 不受影响。E3 任务的启动多付约 160 ms。

---

## 8. 声明过的简化（模型卡须原样收录）

这些全部来自 emcore / em-demo 的原始注释，移植时**照抄不改**，并逐条给出它偏保守还是
偏乐观：

| # | 简化 | 偏向 |
|---|---|---|
| 1 | 高度按 AGL、地面视为平面（`getTerrainHeight` 返回常数） | 与铁律 2 的显式平地假设一致；起伏地形下不定 |
| 2 | 侵入量用**竖直距**近似视线的垂线距 | 低俯仰角下成立；高俯仰角（过顶）时高估侵入 → **偏保守** |
| 3 | 多栋楼只取**侵入最深的一栋**的单刀口 | 一条 3 km 的城市路径可能穿过几十栋楼，多刀口（Deygout / Bullington）会更大 → **偏乐观** |
| 4 | `d1 / d2` 下限钳到 1 m | 端点贴障碍时 Fresnel 参数发散的工程钳位 |
| 5 | 建筑孔（内环）忽略 | **偏保守** |
| 6 | 站点或目标落在楼的投影内即排除该楼（自遮挡） | 「架在楼顶」的合理处置；但「在楼里」会被当成楼顶 |
| 7 | 不做穿透、不做反射、不做材质 | 铁律 12；EM-T-04 留 P3 |
| 8 | 光速取 3e8（§6.4） | 1e-5 dB 量级 |

第 3 条与第 5 条方向相反，**不要把它们说成"整体偏保守"或"整体偏乐观"**——量级未测，
模型卡里只列清单不给合成结论。

---

## 9. 格式与接口变更清单

### 9.1 `geo/`

| 变更 | 说明 |
|---|---|
| 新增 `include/cuav_geo/map.h` | `IMapQuery` / `Building` / `RaycastResult` / `MaterialInfo` / `BBox` / `Vec3`（自 emcore 移植，文件头注明来源与 golden 对应） |
| 新增 `src/local_scene_adapter.cpp` | 桶网格 + DDA；**接口改吃平面米** |
| 新增 `src/occlusion.cpp` + 头 | `segment_occlusion()` |
| `propagation.h/.cpp` | 增 `legacy::fresnel_v()`、`knife_edge_loss_dB()`；`PropagationTerms` 增 `diffraction_dB`；`included_loss_terms` 增 `diffraction`；`PropagationConfig::validate()` 增闸三、闸四 |
| `link_budget.h/.cpp` | `link_geometry()` 增 `const IMapQuery*` 与频率参数；`line_of_sight` 由几何给出 |
| `scenario.h/.cpp` | `LinkFrameSource::build()` 增 `const IMapQuery*` |
| `geodesy.h/.cpp` | 增 `GeographicLibGeodesy`；`default_geodesy()` 改指向；`ClosedFormWgs84` 头注改写 |
| `README.md` 与三处头注 | 「零第三方依赖」改写（`geodesy.h:15`、`scenario.h:7`、CLAUDE.md 引擎条；`propagation.h:9` 的文件级表述不改）；常数表增 3e8 与 legacy 投影两条 |

### 9.2 `engine/`

| 变更 | 说明 |
|---|---|
| 新增 `src/buildings_json.cpp` | GeoJSON → `std::vector<geo::Building>`，核对 sha256 |
| `src/diagram_json.cpp` | E3 时按观测区域清单加载建筑并注入；E3 且无 `--scene-root` → 报错 |
| `src/scenario.cpp` | 把适配器传给各 `LinkFrameSource`；`link` 事件增 `diffraction_dB`（可选字段） |
| CMake | 链接 `third_party/geographiclib` 的源文件 |

### 9.3 `web/`

| 变更 | 说明 |
|---|---|
| 新增 `src/scene/occlusion/{adapter,occlusion,legacy}.ts` | 自 em-demo 祖本回迁，原点参数化、外环改不闭合 |
| 新增 `src/scene/occlusion/occlusion.test.ts` | 读 `tests/golden/occlusion.json`，148 例 `rel ≤ 1e-9` |
| 新增 建筑几何的懒加载模块 | 独立于主 store |
| `chain/plan.ts` | E3 不再置灰；增「E3 需要观测区域数据包」「E3 与阴影 / 城市经验互斥」检查项 |
| 探测交互 | 点选地图任一点 → 对焦点站算视距与损耗（切片 ⑤ 的「点选建筑两侧一致」） |

### 9.4 `docs/` 与模型卡

| 文件 | 变更 |
|---|---|
| `docs/display-route.md` §5 | 「落点待定」改为本报告的结论；§6.3 待写清单划掉两条 |
| `docs/display-products.md` | `links.jsonl` 字段增 `diffraction_dB`，`included_loss_terms` 增取值 |
| `docs/component-catalog.md` §8 | `ScenarioSource` 的 `prop_level` 说明改写；E3 的约束写明 |
| `docs/scene-package.md` | 建筑进引擎的通路与 sha256 核对 |
| `models/channel/README.md` | 增 EM-P-04 一节：公式、解析锚点、§8 的八条简化、单程 ×1、常数偏离 |
| `THIRD-PARTY-NOTICES` | 增 GeographicLib |

### 9.5 `tests/golden/`

| 文件 | 来源 |
|---|---|
| `occlusion.json` | 自 emcore 拷入，`_meta` 记来源与去处（同 R-2 对 `propagation.json` 的做法） |
| `propagation.json` | 启用 `fresnelV` 108 例与 `knifeEdgeLoss_dB` 14 例，`_meta.kept` 改写 |
| `geodesy.json` | **新增**：`ClosedFormWgs84` 与 `GeographicLibGeodesy` 的对拍表，判据 1e-6 m |
| `scenario-track-demo-0{1,2,3}.json` | **重冻一次**（§3.4），判据逐点相对差 ≤ 1e-12 |

---

## 10. 实施步骤（06 §7 D3 线重排）

| 步骤 | 做什么 | 完成判据 |
|---|---|---|
| **D3-0** | 本报告 | 两项 OPEN 销项，D-074 写入 CLAUDE.md |
| **D3-1** | vendored GeographicLib | 闭包文件数与 sha256 记录在案；`GeographicLibGeodesy` 与自写闭式对拍 ≤ 1e-6 m；`build-all.sh` 全绿 |
| **D3-2** | **换基座的基准变更**（单独提交） | 三份航迹基准重冻，逐点相对差 ≤ 1e-12、样点数不变；全部产品文件对换基座前那一版**逐字节比对**并如实记录结果 |
| **D3-3** | 移植 `IMapQuery` / `LocalSceneAdapter` / `segment_occlusion` / `fresnelV` / `knifeEdgeLoss_dB` 到 `geo/` | **148 例 golden `rel ≤ 1e-9`**；`fresnelV` 108 例、`knifeEdgeLoss_dB` 14 例同判据；解析锚点：掠射 `v = 0` → 6.03 dB |
| **D3-4** | 建筑进引擎（`buildings_json.cpp`、注入、sha256 核对、懒加载） | 真实建筑集 47546 栋加载 < 200 ms；`--validate` 仍约 9 ms |
| **D3-5** | 接进帧生产端（`line_of_sight`、`extra_loss_dB`、`diffraction`、闸三闸四） | **E1 / E2 档下全部产品与航迹基准逐字节不变**（拿上一版 `cuav_run` 对拍）；E3 下 demo-01 复现 §1.5 那张表 |
| **D3-6** | 浏览器 TS 复算 + 同一份 golden 对拍 | web 侧 148 例 `rel ≤ 1e-9`；懒加载后场景页首屏不多付 72 ms |
| **D3-7** | 界面：E3 放开、点选探测、链路线第一次变红 | 「点选建筑两侧一致」可一次点击验；`slice2` / `slice8` e2e 增断言 |
| **D3-8** | MSVC / GCC 构建 | 两平台各建一次，golden 全过（铁律 16；本项目首次在目标平台验证 C++ 侧） |
| **D4** | 渲染—物理同源验收 | §2.4 三条 |

### 10.1 切片 ⑤ 的验收（06 §9E）

| 项 | 判据 |
|---|---|
| 可见效果 | 建筑后方非视距时瀑布功率跌落、链路线变红 |
| golden | 148 例 `rel ≤ 1e-9` |
| 点选一致 | 楼两侧各取一点，一侧视距一侧非视距 |
| 平台 | MSVC / GCC 构建过 |
| **零回归** | E1 / E2 档下产品与航迹基准逐字节不变（D3-2 的基准变更除外，且它单独成一个提交） |

---

## 11. 风险与坑

| # | 风险 | 处置 |
|---|---|---|
| 1 | **换基座的扰动可能比预期大**：本次用 foundation 作替身实测 1e-13，GeographicLib 未必同量级 | D3-1 先对拍出真实量级再决定 D3-2 的判据；若相对差大于 1e-11，说明取错了实现或用错了 API，**查根因不调判据**（铁律 10） |
| 2 | **两份物理实现漂移**：TS 改了 C++ 没改，或反过来 | §2.2 三条纪律；CI 里两侧 golden 都跑，任一侧红即整体红 |
| 3 | **E3 一开演示看起来像坏了**：80–100% 非视距、末端信噪比 1.4 dB | E1 仍是缺省；E3 的卡片说明里列清包含哪几项；站点天线高度作为场景参数可调（§1.5） |
| 4 | **legacy 投影与严格 ENU 混用**：golden 路径与引擎路径用错投影 | 只在 `geo::legacy` 里保留旧常数；`grep legacy::` 守卫；两条路径各有单测 |
| 5 | **`fresnelV` 的 3e8 被人"顺手统一"** | D-009 明令禁止；常数表与模型卡各记一处；golden 会当场变红 |
| 6 | **孔与 `MultiPolygon` 处理不一致**：C++ 与 TS 各按各的来 | 两侧都只吃 `buildings_json` 归一化之后的结构；归一化只写一份规则（§7.3），TS 侧读同样归一化后的数据 |
| 7 | **`buildings.geojson` 不在盘时测试静默通过** | 明说跳过并计数（D-073 ③） |
| 8 | **浏览器懒加载没懒住**：模块被顶层 import 拖进首屏 | e2e 断言场景页首屏不发起 `buildings.geojson` 的第二次请求 |
| 9 | **C++ 标准可能要提** | §3.2；提标准是 OPEN 项，要单独拍板，不许为了编过悄悄改 |
| 10 | **`sites[].antenna.pattern` 与遮挡无关却容易被混谈** | 方向图是 C-2 的事（`AntennaGain`），遮挡不碰它；模型卡里分开写 |

---

## 12. 验证与验收

### 12.1 黄金基准

| 基准 | 例数 | 判据 | 谁守 |
|---|---|---|---|
| `occlusion.json` | 148 | `rel ≤ 1e-9` | C++ doctest **与** web `node:test` |
| `propagation.json` `fresnelV` | 108 | `rel ≤ 1e-9` | C++ |
| `propagation.json` `knifeEdgeLoss_dB` | 14 | `rel ≤ 1e-9` | C++ |
| `geodesy.json`（新） | 待定 | `≤ 1e-6 m` | C++ |
| 三份航迹基准 | 361 / 122 / 723 | 逐点相对差 ≤ 1e-12（D3-2 重冻后恢复逐字节） | C++ |

### 12.2 解析锚点

| 锚点 | 期望 |
|---|---|
| 刀口 `v = 0`（视线擦过刀口顶） | `6.9 + 20·log10(√(0.01+1) − 0.1)` = **6.03 dB** |
| 刀口 `v = −0.78` 及以下 | 0 dB（模型的截止） |
| FSPL @ 2.4 GHz / 1 km | 100.05 dB（G-1 已验，此处复核） |
| 热噪声 @ 1 MHz / NF 6 | −108 dBm |

### 12.3 本报告已经取得的实测（原型阶段验证值，macOS）

| 量 | 值 |
|---|---|
| 单次射线（47546 栋真实建筑） | 0.6 / 1.6 / 3.4 / 5.7 / 6.7 µs @ 0.5 / 2 / 5 / 8 / 9.5 km |
| 桶网格构建 | 10.5 ms |
| `buildings.geojson` nlohmann 解析 | 144 ms |
| `buildings.geojson` 浏览器 `JSON.parse` | 72 ms，堆 +41 MB |
| 自写闭式往返（0–10 km） | 纬度 ≤ 8e-12 deg、高程 ≤ 0.9 µm |
| 自写闭式 vs foundation | ECEF 差 **1e-9 m**；demo-01 航段弦长相对差 **1.2e-13 / 1.1e-13** |
| foundation 抽取闭包 | **153 个文件 / 1886299 B**，66 个 `.cpp`，需补 `MODEL_API` 与 `UT_DEPRECATED` 两个宏 |
| legacy 投影 vs 真值（39.99°N） | 纬向 −0.445%、经向 −0.138%；折到 demo-01 刀口损耗 **≤ 0.1 dB** |
| E3 下非视距比例（站 30 m） | 76–100%，刀口损耗均值 29–35 dB |
| demo-01 末端带内信噪比 | E1 33.8 dB → E3 **1.4 dB** |

---

## 13. 不做的事

| 项 | 归属 |
|---|---|
| 多刀口绕射（Deygout / Bullington / ITU-R P.526 多障碍） | P3 |
| 建筑穿透与材质（EM-T-04） | 铁律 12；P3 |
| 反射与统计多径、`ChannelPathSet` 的实际启用 | P3（D-058 ⑫） |
| 地形遮挡（DEM 参与 LOS） | 铁律 2 的平地假设不变；高分辨率 DEM 与垂直基准两项 OPEN 未决 |
| 射线追踪与预计算缓存（EM-P-11 / P-12） | P3 |
| 覆盖场 / 精度场（V-5） | 切片 ⑨；本步只把它需要的复算能力备好 |
| 遮挡进测向误差（NLOS 多径引起的测向偏差） | D-053「不做」清单里原本就写着「等 D3」，本步仍不做——它要的是多径角度谱，不是单刀口 |
| 站点天线高度的自动建议 | 本步只把敏感性量出来（§1.5），不替用户改场景 |

---

## 附录 A. 本次调研的可复现命令

调研用的一次性基准不入库，做法记在此处以便复核：

1. 建筑导出：读 `data/scene/beijing-yayuncun/buildings.geojson`，按 `id base_m height_m n lon lat…`
   逐行导成文本；
2. 基准程序：对 `C-UAV Model Demo/emcore/` 的 `map/local_scene_adapter.cpp`、
   `models/occlusion.cpp`、`models/propagation.cpp`、`core/geo.cpp` 直接 `c++ -std=c++14 -O2`，
   喂上一步的文本；
3. foundation 闭包：从 `UtEllipsoidalEarth.{hpp,cpp}` 起按 `#include "..."` 做传递闭包；
   补 `ModelApi.hpp`（定义 `MODEL_API` / `MODEL_EXPORT` / `UT_DEPRECATED`）即可 `-std=c++17` 编过；
4. 自写闭式：`c++ -std=c++14 -O2 -I geo/include <harness>.cpp geo/src/geodesy.cpp`。

## 附录 B. 依据条款索引

| 条款 | 用在哪 |
|---|---|
| 01 §3、§3.5 | E3 属 E 精度维的第三档，直接写进 `model_level` |
| 04 §7.3 | 传播模型简化策略；首期建筑只用于 LOS/NLOS 与附加损耗 |
| 04 §15.3 | 基准不得静默变 |
| 05 §3.1 | `IMapQuery` 是模型与地图之间的唯一边界 |
| 05 §25 | 几何 / 材质 / 无线参数分管 |
| EM-P-04 | 城市建筑遮挡，本步的定义性模型 |
| EM-P-13 §10.9 | `included_loss_terms` 与防重复计损 |
| 08 §9、§15 | 帧生产端与 `frame()` 的不变量 |
| 12 §2.2、§4.4 | 已有的两道闸；E3 两头处理 |
| EM-C-UAV 08 | 覆盖区域遮挡计算的关键技术与桶网格 DDA 的来历 |
| 铁律 1 / 2 / 5 / 6 / 8 / 9 / 10 / 11 / 12 / 14 / 15 | 分见正文 |
| D-002 / D-005 / D-009 / D-010 / D-013 / D-033 / D-035 / D-049 / D-053 / D-058 / D-059 / D-061 | 分见正文 |
