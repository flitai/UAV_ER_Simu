[project]
name = "cuav-tools"
version = "0.1.0"
description = "804 C-UAV 内部数据工具：IQ 转换与摸底质检。不是运行时依赖，不进交付包。"
requires-python = ">=3.11"
dependencies = ["numpy>=2.0", "h5py>=3.11"]

[tool.uv]
package = false
