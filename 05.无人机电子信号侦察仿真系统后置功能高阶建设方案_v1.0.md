# 无人机电子信号侦察仿真系统后置功能高阶建设方案

> 文档版本：V1.0  
> 编制日期：2026年9月2日  
> 文档属性：后续能力储备与可选扩展建设方案  
> 编制依据：[《无人机电子信号侦察仿真系统实施方案》V1.0](无人机电子信号侦察仿真系统实施方案_v1.0.md)  
> 适用前提：首期基线已完成或已具备稳定的IQ、模型、组件、数据和试验接口  
> 建设取向：需求牵引、独立成包、适度指标、数据校准、逐级扩展、可验收可退出

## 1. 编制目的

本方案面向《无人机电子信号侦察仿真系统实施方案》V1.0中明确后置、预留、简化或不纳入首期验收的能力，形成一套可供后续技术交流、项目论证、经费申请、任务拆分和商务承接使用的高阶建设方案。

本方案所称“高阶”，是指在首期单站、单通道、离线或准实时IQ闭环基础上，向多通道、多站、复杂场景、半实物、集中运行和专业模型生态等更完整的系统能力扩展，不等同于追求极高性能指标或无限物理精度。

方案主要解决以下问题：

- V1.0中哪些功能尚未实施，后续如何形成完整能力；
- 各后置功能能否独立立项、独立交付和独立验收；
- 哪些能力可共用首期平台，哪些能力需要新增软硬件；
- 如何避免阵列、多站、射线追踪和半实物等高风险方向失控；
- 在经费分档条件下，客户应优先采购哪些扩展包；
- 如何使用实测数据、校准数据和冻结基准证明扩展能力有效；
- 如何为后续承接保留合理技术空间，同时不承诺不可验证的高指标。

本方案不是对首期V1.0的重复建设，也不默认所有扩展包一次性实施。推荐采用“公共扩展底座＋独立专业能力包＋按需硬件包”的组合方式，根据客户任务、数据、设备和预算选择建设范围。

## 2. 与首期V1.0的关系

### 2.1 首期基线假设

本方案默认首期已形成或将在后续扩展启动前形成以下基线：

- 自主框图编辑、参数配置、运行控制和结果展示；
- 单站、单通道、离线或准实时的分块IQ运行引擎；
- 合成信号、实测回放和混合增强三类信号源；
- 2至3类重点无人机辐射源模板；
- 基础传播、天线、接收机、ADC、DDC和信道化模型；
- DDC后、信道化前宽带复基带IQ主产品；
- 实测IQ导入、质量检查、检索、回放和版本管理；
- 基础检测、识别、特征提取和算法插件接口；
- 参数扫描、结果对比、报告和全链路追溯；
- 标准算例、冻结基准和模型证据包机制。

若上述基线尚未稳定，应先补齐接口、数据和验证欠项，再启动对应扩展包。后置项目不应以重写首期平台作为默认路线。

### 2.2 后置功能来源

V1.0中后置能力主要来自以下四类表述：

1. **可选扩展范围**：阵列、多站、复杂城市传播、半实物、硬件加速、多用户计算、更大模型库和模型发布生态；
2. **首期明确不追求内容**：协议级复现、个体指纹、全城市连续IQ、精细器件模型、全模型实时、阵列多站和硬件在环；
3. **模型简化项**：阵列互耦、平台遮挡、详细互调杂散、ADC精细非理想、复杂动态背景等；
4. **接口预留项**：阵列多通道IQ、多站同步IQ、定位报告、硬件设备、GPU/FPGA和集中服务接口。

### 2.3 建设边界继承

后置建设继续继承V1.0的基本边界：

- 以接收机观测域等效和科研任务有效性为主要目标；
- 以复基带IQ为高精度数据形态，不进行GHz载波逐周期仿真；
- 只对重点区域、重点频段、重点站点和重点时间窗使用高粒度模型；
- 模型必须有数据、机理或设备依据，不能以不可验证细节代替可信度；
- 实时、多通道和长时间不能同时默认取最大值；
- 不因增加硬件而取消软件参考链、离线复现和数据追溯；
- 不因形成后置方案而默认将所有可选功能纳入合同必验范围。

## 3. 后置能力总体清单与优先级

### 3.1 能力包划分

| 编号 | 后置能力包 | 主要能力 | 建议优先级 | 是否可独立立项 |
|---|---|---|---:|---:|
| P0 | 公共扩展底座与准备度治理 | 多通道、多站、设备、任务和校准公共接口 | 必需前置 | 是，但宜并入首个扩展包 |
| P1 | 相干阵列与AOA测向 | 阵列IQ、阵列误差、校准、测向和波束形成 | 高 | 是 |
| P2 | 多站同步与融合定位 | TDOA、FDOA、AOA融合、误差建模和定位评价 | 高 | 是 |
| P3 | 城市传播增强 | 2.5D/3D场景、有限阶射线追踪、缓存和实测校准 | 中高 | 是 |
| P4 | 数字半实物与射频传导闭环 | 算法板卡、SDR、真实接收机、时频与射频校准 | 中高 | 是，建议分A/B/C子包 |
| P5 | 辐射源与接收机高级模型库 | 更多信号模板、授权协议级模型、精细接收机效应 | 中 | 是 |
| P6 | 高级侦察算法与连续跟踪 | 开集识别、弱信号、多源分离、关联与跟踪 | 中 | 是 |
| P7 | 集中计算与多用户试验服务 | 任务队列、资源管理、并行计算和集中运维 | 中 | 是 |
| P8 | 数据资产深化与模型发布生态 | 数据集治理、模型审签、受控共享和组件发布 | 中 | 是 |
| P9 | 受控OTA与高可信试验条件 | 屏蔽环境、真实设备无线接入和专项仪器 | 条件建设 | 是，仅在明确任务时启动 |

### 3.2 推荐建设顺序

推荐顺序不是固定时间表，而是风险和依赖关系：

```text
首期V1.0稳定基线
        ↓
P0 公共扩展底座与准备度审查
        ↓
P1 阵列测向 ─────┐
                  ├→ P2 多站融合定位
P3 城市传播增强 ─┘
        ↓
P4A 数字半实物 → P4B 射频传导 → P9 受控OTA（可选）
        ↓
P5/P6 专业模型与算法持续扩充
        ↓
P7/P8 集中运行、数据治理与模型生态按用户规模扩展
```

P1、P3、P5和P7可在P0完成后并行建设。P2可以先使用纯软件多站数据开展，不必等待SDR到货。P4必须先做数字接口，再做射频传导。P9不作为P4基础验收的必选项。

### 3.3 能力触发原则

每个扩展包至少满足以下三个条件后才建议立项：

- 有明确科研问题或装备验证对象；
- 有可获得的输入数据、设备参数或可执行补采计划；
- 有能够在项目周期内形成的验收方法和参考基线。

仅有“未来可能用到”但缺少用户、数据和验收问题的能力，应保留接口，不宜立即采购高价设备或开展精细建模。

## 4. 总体建设目标

### 4.1 总体目标

在首期自主框图IQ仿真平台基础上，建设可按任务选择的阵列、多站、城市传播、半实物、专业模型和集中服务能力，使系统能够支撑以下后续科研活动：

1. 生成和处理相干阵列多通道IQ，开展单站测向和波束形成试验；
2. 生成、导入和处理多站同步IQ，开展TDOA、FDOA和多源融合定位；
3. 在重点城市区域生成具有可解释主导路径的时变复信道；
4. 将软件IQ链与算法板卡、SDR或真实侦察接收机连接，形成数字或射频闭环；
5. 扩充有证据支撑的辐射源、接收机和复杂环境模型；
6. 支撑开集识别、多源分离、连续关联和跟踪等高级算法评价；
7. 支撑多用户集中提交任务、共享受控算力和管理大规模试验结果；
8. 建立模型、数据、校准和算法的审签发布、版本复核和持续升级机制。

### 4.2 建设结果形态

后置建设完成后，系统不是单一“高配版本”，而是形成三种可组合结果：

- **软件能力包**：模型、算法、框图组件、场景工具和评价工具；
- **设备能力包**：SDR、时频、算法板卡、射频调理、屏蔽和测试设备适配；
- **证据能力包**：实测数据、校准文件、标准算例、模型证据和验收报告。

客户可只采购一个专业软件包，也可采购软件、设备和数据校准的组合包。各包应具有清晰依赖、接口和交付边界。

## 5. 总体技术架构

### 5.1 扩展架构

```text
┌────────────────────────自主应用层────────────────────────┐
│ 场景/航迹 │ 阵列/站点 │ 框图/试验 │ 设备管理 │ 评价/报告 │
└───────────────────────────┬──────────────────────────────┘
                            │
┌────────────────────扩展试验编排与资源层───────────────────┐
│ 多时间尺度调度 │ 多通道同步 │ 任务队列 │ 资源配额 │ 降级策略 │
└───────────────────────────┬──────────────────────────────┘
                            │
┌────────────────────专业模型与算法组件层───────────────────┐
│ 阵列/校准 │ 多站/定位 │ 城市传播 │ 高级接收机 │ 高级识别 │
│ 航迹关联 │ 实测信道   │ 设备模型 │ GPU组件    │ FPGA组件 │
└───────────────┬───────────────────────────┬──────────────┘
                │                           │
┌───────────────▼──────────────┐  ┌─────────▼────────────────┐
│ 软件IQ与集中计算基础设施      │  │ 半实物与真实设备基础设施   │
│ CPU/GPU │ NVMe │ 存储 │ 网络 │  │ SDR │ 时频 │ RF │ 算法板卡 │
└───────────────┬──────────────┘  └─────────┬────────────────┘
                │                           │
┌───────────────▼───────────────────────────▼────────────────┐
│ 数据、校准、真值与证据资产层                                │
│ 多通道IQ │ 多站IQ │ 城市数据 │ 校准文件 │ 基准集 │ 模型证据 │
└────────────────────────────────────────────────────────────┘
```

### 5.2 关键架构判断

- 首期`IQStream`继续作为单通道基础端口；新增`ArrayIQStream`和`MultiSiteIQSet`，不改变原有IQ样本语义；
- 阵列、多站和硬件组件必须携带时间质量、同步状态和校准版本；
- 射线追踪输出优先生成路径参数、信道冲激响应或抽头集合，再由IQ引擎在重点窗口施加；
- 高频IQ处理尽量保持在同机内存、共享内存、GPU显存或专用高速流接口内；
- 集中服务负责任务、资源、权限和结果，不把每个细粒度IQ组件远程化；
- 软件参考链始终保留，硬件输出必须能与同一输入条件下的软件结果对比；
- 模型精度、运行范围和资源预算必须显式选择，系统允许自动降级但不得静默降级。

## 6. P0 公共扩展底座与准备度治理

### 6.1 建设目标

P0用于把首期面向单通道的接口扩展为可承载阵列、多站、城市传播和设备接入的稳定底座。P0不追求形成独立业务效果，但决定后续扩展是否需要反复改造平台。

### 6.2 建设内容

#### 6.2.1 数据端口扩展

新增或冻结以下数据类型：

| 数据类型 | 主要内容 | 关键元数据 |
|---|---|---|
| `ArrayIQStream` | 同一阵列的相干多通道IQ | 阵元位置、通道顺序、公共时钟、幅相校准 |
| `MultiSiteIQSet` | 多站同步或可对齐IQ集合 | 站址、时间基准、时钟质量、固定时延、站点状态 |
| `ChannelPathSet` | 多径路径或抽头集合 | 时延、复增益、多普勒、AOA/AOD、有效时间 |
| `CalibrationSet` | 设备或通道校准数据 | 设备号、通道、频段、温度、日期、方法、有效期 |
| `BearingReport` | 测向结果 | 方位/俯仰、误差、算法、质量、时间 |
| `PositionReport` | 定位结果 | 坐标、协方差/误差椭圆、方法、参与站点、质量 |
| `TrackReport` | 连续航迹 | 状态、协方差、关联信息、更新时间、质量 |
| `DeviceStatus` | 外部设备状态 | 锁相、溢出、丢样、温度、增益、固件版本 |

#### 6.2.2 时间与同步语义

- 区分逻辑仿真时间、文件采集时间、设备硬件时间和外部统一时间；
- 记录时间戳来源、分辨率、估计不确定度和连续性；
- 支持采样计数、帧序号、PPS历元和触发事件之间的映射；
- 支持固定时延、群时延、时钟偏差、漂移和跳变模型；
- 任何用于TDOA的数据必须能够给出站间时间质量状态；
- 时间失锁、补零和重对齐不得被当作正常连续数据。

#### 6.2.3 坐标和姿态语义

- 统一大地坐标、地心地固坐标、局部东北天坐标和阵列坐标；
- 明确角度正方向、零方位、俯仰定义和姿态旋转顺序；
- 站点、阵元、天线相位中心和无人机位置应可追溯到同一坐标版本；
- 城市模型、航迹真值和定位结果应记录坐标转换参数和精度等级。

#### 6.2.4 设备抽象

统一抽象以下设备角色：

```text
IQSource：软件波形、文件、网络、SDR、真实接收机
IQSink：文件、软件接收机、算法组件、SDR、外部处理机
ChannelProvider：软件信道、预计算信道、射频信道模拟器
ClockProvider：逻辑时钟、设备内部时钟、外部10 MHz/PPS/触发
CalibrationProvider：静态校准、频率相关校准、在线校准结果
```

#### 6.2.5 扩展准备度检查

P0应提供准备度检查工具，对拟启动的能力包自动或人工确认：

- 首期接口版本是否满足要求；
- 是否存在可用实测数据和真值；
- 硬件频段、带宽、通道和时钟是否匹配；
- 预计数据率、缓存、网络和存储是否可承受；
- 是否有校准方法和测试设备；
- 是否涉及射频发射、敏感数据或第三方许可；
- 验收指标是否与输入条件绑定。

### 6.3 交付与退出条件

- 扩展数据字典和接口规范；
- 时间、坐标、同步和校准规范；
- 设备适配SDK与模拟设备；
- 多通道、多站端口的标准算例；
- 扩展准备度检查清单；
- 原首期项目和数据能够兼容加载；
- 未安装任何专业扩展包时，首期功能不受影响。

## 7. P1 相干阵列与AOA测向能力包

### 7.1 建设目标

面向单站无人机信号测向研究，形成阵列多通道IQ生成、导入、校准、测向和评价闭环。默认建设4通道工程基线，可按已有设备扩展至8通道；不以大规模阵列、宽带全频段实时测向为默认目标。

### 7.2 主要建设内容

#### 7.2.1 阵列几何与流形模型

- 均匀线阵、均匀圆阵和用户导入阵列；
- 阵元三维位置、姿态、极化和相位中心；
- 窄带解析导向矢量；
- 频率相关阵列流形表；
- 可选实测阵列流形导入；
- 阵元失效、通道旁路和阵列重构。

#### 7.2.2 多通道IQ生成与导入

- 由信号到达方向、多径角度和通道响应生成相干IQ；
- 多通道统一分块、采样计数和时间戳；
- 多通道文件和实时流导入；
- 通道增益、相位、固定时延和噪声差异；
- 局部振荡器和采样时钟共用或独立配置；
- 短时丢样、通道错序和失锁等故障注入。

#### 7.2.3 阵列误差与校准

- 通道幅相误差；
- 电缆和滤波器群时延差；
- 阵元位置误差；
- 简化互耦矩阵；
- 频率相关校准表；
- 离线校准、校准应用和有效期管理；
- 校准前后测向结果对比。

首版互耦模型以测量矩阵或低阶参数化模型为主，不开展全平台电磁全波仿真。平台遮挡可使用方向图区间修正或实测查表。

#### 7.2.4 测向与波束形成算法

建议内置以下研究基线：

- 相位差或比幅测向基线；
- 常规波束形成；
- Capon/MVDR波束形成；
- MUSIC测向；
- 宽带信号分子带测向或聚焦处理；
- 多径和低信噪比条件下的质量评估；
- 用户测向算法插件接口。

不要求一次性覆盖全部超分辨算法。算法数量服从“具有统一输入、统一质量指标和可对照基线”的要求。

#### 7.2.5 结果评价

- 方位角和可选俯仰角误差；
- 中位绝对误差、RMSE和大误差概率；
- 测向成功率、更新率和首次结果时间；
- 角分辨能力和多源可分辨率；
- 校准前后性能变化；
- SNR、带宽、多径、阵元失效和模型失配下的退化曲线。

### 7.3 推荐工程口径

| 项目 | 推荐基线 | 可选扩展 |
|---|---|---|
| 阵列通道数 | 4通道 | 8通道；更大规模专项论证 |
| 常用复采样率 | 5至20 MS/s | 40或50 MS/s短窗 |
| 工作方式 | 离线、准实时 | 硬件条件满足后的实时 |
| 阵列类型 | 线阵、圆阵、用户导入一种 | 多阵列模板和实测流形 |
| 测向维度 | 方位角为主 | 方位＋俯仰 |
| 多源数量 | 1至2个主目标 | 3个以上专项验证 |
| 校准 | 幅相和固定时延 | 频率/温度相关校准 |

受控仿真或实验室校准场景中，可将单目标方位测向中位绝对误差不大于3°至5°作为参考目标；外场和复杂多径场景不直接沿用该数值，应按场景分组报告误差分布和置信区间。该参考值需在阵列口径、信号带宽、SNR和校准条件明确后冻结。

### 7.4 启动条件

- 明确阵列结构、通道数、频段和典型信号；
- 至少有仿真真值或转台/已知方位数据；
- 若接入真实阵列，具备阵列流形或通道校准条件；
- 用户测向算法的输入格式和评价方法明确；
- 多通道IQ数据率和存储策略已经核算。

### 7.5 交付物

- 阵列几何、流形、误差和校准组件；
- 相干多通道IQ生成与导入组件；
- 2至4种测向/波束形成基线算法；
- 阵列场景编辑和测向结果展示；
- 标准角度扫描、低SNR、多径和通道失配数据集；
- 校准工具、校准样例和测向验证报告；
- 阵列模型卡和用户算法接入示例。

## 8. P2 多站同步与融合定位能力包

### 8.1 建设目标

面向多站协同侦察研究，形成多站同步IQ、到达时间差、频率差、方位交汇和融合定位闭环。默认以3站、局部重点区域、短时间窗和离线/准实时为基线，不承诺在任意站形、任意信号和任意城市环境中达到统一高精度。

### 8.2 主要建设内容

#### 8.2.1 多站场景与站点模型

- 3站基线和可选4站配置；
- 站址、海拔、天线相位中心、姿态和视场；
- 各站独立接收机、增益、噪声和频偏；
- 站点在线、离线、降级和通道异常状态；
- 站间网络传输延迟和抖动统计；
- 目标航迹、速度和工作状态真值。

#### 8.2.2 同步与误差模型

每个站点独立支持：

- 时间戳偏差、采样时钟偏差和漂移；
- 本振频偏和漂移；
- 天线、馈线、滤波和数字处理固定时延；
- GNSS驯服、公共时钟或独立时钟状态；
- PPS跳变、失锁、重同步和校准残差；
- 时间质量、频率质量和校准可信度标记。

#### 8.2.3 多站观测量提取

- TOA和相关峰；
- TDOA及相关峰质量；
- FDOA及频率估计质量；
- 各站AOA输入；
- 接收功率和可用性辅助量；
- 多路径候选峰和异常值标记。

#### 8.2.4 定位与融合算法

- TDOA双曲线定位基线；
- FDOA定位基线；
- 多站AOA交汇；
- AOA/TDOA/FDOA加权融合；
- 非线性最小二乘或扩展卡尔曼滤波；
- 误差椭圆、协方差和几何精度因子；
- 站点选择、异常观测剔除和降级定位；
- 用户定位算法插件接口。

#### 8.2.5 连续定位与航迹

- 按1至10 Hz结果频率进行连续更新；
- 航迹初始化、维持、丢失和重捕；
- 测向/定位结果与无人机目标的关联；
- 单点误差、连续性和航迹平滑误差分开评价；
- 记录每次定位使用的站点、观测量和校准版本。

### 8.3 推荐工程口径

| 项目 | 推荐基线 | 说明 |
|---|---|---|
| 站点数量 | 3站 | 4站作为增强，不默认更多 |
| IQ通道 | 每站1通道 | 可与P1组合为站内阵列 |
| 常用采样率 | 5至20 MS/s | 高采样率仅用于短窗 |
| 处理方式 | 离线/准实时 | 实时需P4和网络条件 |
| 定位方法 | TDOA＋AOA之一 | FDOA按目标运动和信号稳定性选配 |
| 区域范围 | 重点区域或试验场 | 不以全城市连续定位为默认范围 |
| 定位输出 | 点位＋误差椭圆＋质量 | 不只输出单一坐标 |

在良好站形、可用信号、受控同步误差和明确真值条件下，可将CEP50不大于50米或不劣于用户既有基准算法作为工程参考目标。复杂NLOS、弱信号、窄站角或外场独立时钟条件下，应按场景等级报告误差分布，不使用单一数值覆盖全部情况。

### 8.4 启动条件

- 至少3个站点的坐标、时间基准和接收配置明确；
- 有可用于相关或频差估计的信号片段；
- 有目标位置/航迹真值或可控仿真真值；
- 站间同步方式和误差上界可说明；
- 若使用实测多站IQ，必须能够确认样本时间和通道固定时延；
- 若缺少同步实测数据，项目范围应限定为软件仿真与方法验证。

### 8.5 交付物

- 多站场景、同步误差和独立接收机组件；
- 多站同步IQ生成、导入和对齐工具；
- TOA/TDOA/FDOA和AOA融合定位组件；
- 误差椭圆、站形和连续航迹展示；
- 标准站形、时钟误差、NLOS和站点失效数据集；
- 多站定位基准算法、验证报告和适用范围说明；
- 与P1、P3和P4组合的接口样例。

## 9. P3 城市传播增强能力包

### 9.1 建设目标

在首期自由空间、双径和统计衰落模型基础上，面向重点城区、典型街区和关键航迹增加建筑几何驱动的传播能力。默认采用2.5D/简化3D场景、有限阶主导路径、预计算缓存和实测校准，不建设全城市、全频段、全时段、无限阶的高精度电磁数字孪生。

### 9.2 建设内容

#### 9.2.1 城市场景数据处理

- 导入地形、建筑轮廓、高度和道路数据；
- 建立统一坐标、场景切片和空间索引；
- 建筑按用途或表面类型配置工程材质类别；
- 提供几何质量、缺失区域和数据版本报告；
- 支持重点区域的人工修正和场景冻结；
- 视觉模型与传播模型分开管理。

#### 9.2.2 传播模型分级

| 等级 | 能力 | 典型用途 |
|---|---|---|
| U1 | 建筑LOS/NLOS判断＋附加损耗 | 大批量快速筛选 |
| U2 | 直达＋地面/建筑一次反射＋简化绕射 | 默认工程级重点航迹 |
| U3 | 有限二阶反射、一次绕射/穿透、角度和时延结构 | 重点街区和算法验证 |
| U4 | 实测校准参数、残差模型和场景专用证据 | 外场复盘和专项验收 |

U3、U4不是全场景默认等级。运行时应根据区域、任务、性能预算和输入数据质量选择等级。

#### 9.2.3 有限阶射线追踪

- 直达路径；
- 地面和建筑镜面反射；
- 简化刀刃或统一绕射模型；
- 可选低阶穿透损耗；
- 路径能量阈值、数量上限和最大阶数；
- 路径时延、复增益、多普勒、AOA和AOD；
- 相干或非相干合成语义；
- 超时、数据不足和不适用时自动降级。

默认不实现漫反射全细节、粗糙面随机全波散射、室内精细传播和多阶无限递归。

#### 9.2.4 动态信道与IQ耦合

- 沿无人机航迹以10至100 Hz更新传播参数；
- 对相邻位置的路径进行连续匹配和插值；
- 生成抽头延迟线或路径集合；
- 在指定IQ窗口施加时变时延、复增益和多普勒；
- 为阵列测向提供到达角和角扩展；
- 为多站定位提供NLOS偏差和路径质量标记；
- 避免与首期建筑遮挡、双径和统计阴影重复计损。

#### 9.2.5 预计算、缓存和降级

- 按频段、场景版本、站点网格和高度层预计算；
- 按航迹关键点缓存主导路径；
- 支持空间和时间插值；
- 记录缓存命中、过期和模型版本；
- 计算超预算时降级到U2或U1；
- 降级结果携带原因和可信度，不静默替换。

#### 9.2.6 实测校准

- 使用路径损耗、接收功率、时延扩展和LOS/NLOS标签；
- 重点校准建筑附加损耗、反射损耗、绕射残差和阴影分布；
- 训练数据与独立验证路线或区域隔离；
- 输出参数适用频段、建筑类型和区域边界；
- 对数据无法辨识的材质参数使用工程类别，不声称真实物性反演。

### 9.3 推荐工程口径

| 项目 | 推荐口径 |
|---|---|
| 重点区域 | 单个1至5平方公里典型街区，按包扩展 |
| 场景精度 | 建筑轮廓和高度正确的2.5D/LoD2级为主 |
| 路径阶数 | 默认一次反射；重点任务可到有限二阶 |
| 频段 | 先覆盖1至2个重点频段 |
| 更新率 | 传播参数10至100 Hz，IQ按原采样率处理 |
| 运行方式 | 预计算＋缓存＋在线插值，离线/准实时 |
| 校准 | 至少1条标定路线＋1条独立验证路线或等效数据分组 |

与独立实测数据比较时，可将典型区域路径损耗中位绝对误差8至12 dB、LOS/NLOS趋势和时延扩展等级一致作为初始工程参考。具体门限应根据采集设备、站点密度、场景数据质量和频段重新冻结，不以少量点位的最优结果代表全区域。

### 9.4 启动条件

- 明确重点区域、频段、站点和航迹；
- 具有可合法使用的建筑/地形数据；
- 有路径损耗、接收功率或时延特征的实测依据，或同意先做U1/U2工程验证；
- 明确射线模型输出是最终路径损耗还是路径分量；
- 具备缓存空间和场景版本管理能力。

### 9.5 交付物

- 城市场景导入、检查、切片和版本工具；
- U1至U3传播组件及U4校准接口；
- 主导路径、信道抽头、缓存和降级组件；
- 城市传播参数编辑和可视化工具；
- 典型区域场景包、校准参数和冻结测试集；
- 传播模型卡、性能报告和适用范围说明；
- 面向P1测向和P2定位的多径输出样例。

## 10. P4 数字半实物与射频传导闭环能力包

### 10.1 建设目标

将首期软件IQ链与真实算法硬件、SDR或侦察接收机连接，验证数据接口、处理时延、硬件非理想、动态范围和算法一致性，并使用回采数据校准软件模型。该能力分为P4A数字半实物、P4B射频传导闭环和P4C多设备同步扩展，按顺序建设。

### 10.2 P4A 数字半实物

#### 10.2.1 接入对象

- FPGA算法板卡；
- DSP或嵌入式处理设备；
- 真实侦察算法服务器；
- 接收机厂商提供的数字IQ接口；
- 用户自研高速采集或处理设备。

#### 10.2.2 建设内容

- 文件IQ到实时IQ流转换；
- 以太网、PCIe或设备专用接口适配；
- 启动、停止、复位、触发和状态读取；
- 流量控制、环形缓存、背压和丢包处理；
- 固定时延和处理抖动测量；
- 软件参考算法与硬件输出对照；
- 设备驱动、固件和FPGA镜像兼容矩阵；
- 设备异常隔离和安全退出。

网络带宽按实际数据率选择。20 MS/s、4通道复数int16约为320 MB/s，不需要为了峰值宣传直接配置100GbE；当通道数、采样率或设备接口确有需要时再采用25/100GbE。

### 10.3 P4B 射频传导闭环

#### 10.3.1 推荐链路

```text
软件IQ/实测回放
  → 发射端SDR或矢量信号源
  → 固定衰减/DC Block/可编程衰减
  → 功分合路/干扰注入/可选射频信道模拟器
  → 限幅保护
  → 接收端SDR或真实侦察接收机
  → 回采IQ/检测识别结果
  → 与软件参考链对比及模型校准
```

#### 10.3.2 建设内容

- SDR波形下载、连续流和回采；
- 中心频率、采样率、带宽、增益和触发控制；
- 射频衰减、合路、隔离、限幅和功率联锁；
- 噪声底、动态范围、阻塞、削顶和AGC恢复试验；
- 发射和接收通道幅频、相频及群时延校准；
- IQ满量程与物理功率标尺映射；
- 软件IQ、射频回采IQ和真实设备结果三方对比；
- 实测残差回灌接收机模型。

实验室默认使用50 Ω同轴传导。未经审批不得连接开放空间发射天线。

### 10.4 P4C 多设备同步扩展

- 多台SDR共用10 MHz、1 PPS和可选硬件触发；
- 多设备定时启动和样本计数对齐；
- 通道固定时延、幅相和启动重复性标定；
- 锁相、失锁和温度状态监测；
- 为P1阵列和P2多站提供真实同步数据；
- 只在基线两设备闭环稳定后增加设备数量。

### 10.5 推荐工程口径

| 项目 | 基线建议 | 增强条件 |
|---|---|---|
| 数字半实物 | 1类外部设备、1种主接口 | 每新增设备单独适配验收 |
| SDR数量 | 2台收发闭环 | 多站/阵列再增配 |
| 射频通道 | 2至4通道 | 8通道需重新核算 |
| 常用带宽 | 20 MHz级连续 | 50 MHz短窗；100 MHz按设备实测 |
| 连续运行 | 典型配置30分钟稳定流 | 长时任务专项验收 |
| 网络 | 10/25GbE优先 | 多通道高带宽再使用100GbE |
| 同步 | 公共参考＋PPS＋固定时延校准 | 高稳定度时钟按定位需求配置 |
| 信道 | 软件信道＋衰减网络 | 商业射频信道模拟器按需采购 |

### 10.6 启动条件

- 首期IQ格式、时间戳、功率标尺和设备抽象已冻结；
- 明确待接设备、频段、带宽、通道数和接口；
- 有设备技术资料、SDK和可用测试样机；
- 有射频功率预算、输入保护和校准方法；
- 实验室供电、散热、接地、网络和安全条件满足；
- 射频发射、频谱使用、数据保密和设备操作权限已完成审批。

### 10.7 交付物

- 数字设备适配器和实时流组件；
- SDR收发控制、回采和设备状态组件；
- 射频调理、保护和校准链路；
- 软件/硬件一致性对比工具；
- 设备、驱动、固件和接口兼容矩阵；
- 校准文件、校准流程和有效期管理；
- 数字半实物和射频传导验收报告；
- 操作、安全、故障恢复和资产管理文档。

## 11. P5 辐射源与接收机高级模型库

### 11.1 建设目标

在首期2至3类重点辐射源和工程等效接收机基础上，按用户真实任务扩充模型覆盖度和模型细节。该能力包采用“每类模型一个证据包、一个验收数据集、一个适用边界”的滚动建设方式，不以模型数量作为唯一绩效。

### 11.2 辐射源模型扩展

可按以下层级建设：

| 层级 | 建设内容 | 启动依据 |
|---|---|---|
| S1 类别扩展 | 新增工作频段、带宽、突发和频率活动模板 | 有代表性实测IQ |
| S2 状态扩展 | 待机、建链、遥控、图传、返航等活动状态切换 | 有状态标注或同步日志 |
| S3 波形结构 | 调制、帧结构、导频和同步结构等可控模型 | 有授权技术资料或可验证依据 |
| S4 发射机特征 | 频偏、相噪、功放非线性、杂散、启动瞬态 | 有多批次实测数据和设备隔离条件 |
| S5 指纹研究 | 个体差异分布、跨设备/接收机鲁棒性评价 | 有多设备、多接收机、独立测试数据 |

对未知私有协议、加密内容和敏感跳频序列，继续采用脱敏模板、统计波形或授权实测片段回放。没有可靠依据时不声称完成协议级或个体级真实复现。

### 11.3 接收机高级模型扩展

- 频率相关噪声系数和增益；
- 镜像响应和零中频残差；
- 二阶/三阶互调的工程等效模型；
- 邻频阻塞、压缩和AGC恢复过程；
- 频率相关IQ不平衡；
- ADC采样时钟偏差、抖动和简化非线性；
- 杂散和本振泄漏模板；
- 多档设备配置和实测标定参数；
- 真实设备残差模型。

高级接收机模型应支持单因素启停和参数旁路，防止多项非理想叠加后无法解释。器件级参数不足时，采用输入输出曲线或实测残差，不进行晶体管级建模。

### 11.4 复杂背景与干扰扩展

- 分频段、地点和时段的实测背景库；
- 动态频谱占用和背景信号到离事件；
- 多源同频、邻频、宽带和脉冲干扰组合；
- 近远效应、阻塞和互调触发场景；
- 背景数据脱敏、片段拼接和重复性控制；
- 干扰强度和算法性能之间的响应曲线。

本能力包面向侦察接收机和算法鲁棒性研究，不把通信压制效果或实际干扰运用指标作为默认建设范围。

### 11.5 推荐交付粒度

一次扩展建议选择：

- 新增2至4类有实测依据的辐射源；或
- 1类重点辐射源的状态级/波形结构级深化；或
- 1至2型接收机模板的高级非理想标定；或
- 1个重点频段的复杂背景库。

不建议在一个合同包中同时承诺大量型号、协议级建模、个体指纹和多接收机精细标定。

### 11.6 交付物

- 新增或升级模型组件；
- 对应实测、合成和独立测试数据清单；
- 模型卡、参数库和模型证据包；
- 特征、趋势和算法性能对比报告；
- 适用范围、禁用范围和可信度等级；
- 旧版本兼容和回归测试结果。

## 12. P6 高级侦察算法与连续跟踪能力包

### 12.1 建设目标

在首期基础检测识别算法之上，增加面向真实科研难点的高级算法基线和统一评价能力。平台自带算法仍以证明数据可用、提供公平基线和支撑用户算法对比为定位，不扩展为完整作战侦察软件。

### 12.2 可选建设内容

#### 12.2.1 弱信号和复杂占用检测

- 多尺度时频检测；
- 短突发和低占空比检测；
- 自适应噪声底估计；
- 同频重叠和邻频泄漏条件下检测；
- 多帧积累和候选事件合并；
- 检测置信度和不确定性输出。

#### 12.2.2 多源分离与参数估计

- 时频掩膜和候选信号分割；
- 中心频率、带宽、功率、持续时间联合估计；
- 可选盲源分离或波束域分离；
- 强弱信号遮蔽场景评价；
- 参数估计误差和失败原因标记。

#### 12.2.3 开集识别与未知信号研究

- 已知类识别基线；
- 未知拒识和误接纳评价；
- 相似类别混淆分析；
- 跨日期、地点、接收机的域外测试；
- 未知样本聚类和人工复核队列；
- 模型置信度校准。

#### 12.2.4 信号源与目标关联

- 检测事件合并为辐射活动轨迹；
- 测向、定位和时频特征联合关联；
- 多假设或概率关联的轻量实现；
- 关联保持、切换和冲突解释；
- 与目标航迹的受控关联，不将弱证据直接解释为确定身份。

### 12.3 评价原则

- 按信号类别、SNR、场景和接收机分组统计；
- 训练、调参、内部测试和冻结验收数据隔离；
- 不只报告总体准确率；
- 同时报告虚警、漏检、未知误接纳、置信度和算法运行成本；
- 算法升级不得静默改变旧基准；
- 用户算法和平台基线使用相同输入、切片、真值和评价脚本。

### 12.4 交付物

- 约定范围内的高级算法基线；
- 用户算法容器或插件接入样例；
- 开集、重叠、弱信号和跨域冻结测试集；
- 统一评价工具、指标字典和报告模板；
- 算法模型卡、训练说明和回归报告。

## 13. P7 集中计算与多用户试验服务能力包

### 13.1 建设目标

当用户数量、任务量或数据量超过单机管理能力时，在不改变框图和模型语义的前提下，增加集中任务提交、资源调度、结果管理和运维能力。基线面向实验室或单位内部5至10名用户，不默认建设跨地域大规模云平台。

### 13.2 建设内容

- 用户、角色、项目和资源配额；
- 框图任务打包、提交、排队、取消和重试；
- CPU、GPU、内存、NVMe和许可证资源声明；
- 任务级并行和参数扫描调度；
- 模型依赖、运行镜像和兼容版本检查；
- 大文件就近计算和结果摘要回传；
- 运行状态、耗时、资源、失败原因和告警；
- 结果检索、对比、报告和生命周期管理；
- 节点离线、任务失败和资源不足时的恢复；
- 管理审计、备份和容量预警。

### 13.3 推荐工程口径

| 项目 | 推荐基线 |
|---|---|
| 用户规模 | 5至10名注册科研用户 |
| 并发交互用户 | 3至5名 |
| 并发重任务 | 2至4个，按资源自动限制 |
| 计算节点 | 1个主计算节点，可扩至2至3个 |
| 数据管理 | 首期数据中心升级，不另建全量数据湖 |
| 高可用 | 任务可重试、数据可恢复；不默认双活 |
| 部署范围 | 内网集中部署 |

### 13.4 资源扩展原则

- 先用实测任务剖析CPU、GPU、存储和网络瓶颈；
- GPU仅用于确有收益的FFT、FIR、信道、阵列或学习算法；
- 多通道IQ优先尽早抽取，减少跨节点搬运；
- 单个高吞吐IQ处理链尽量不跨多台服务器拆分；
- 参数扫描使用任务级并行；
- 存储按原始证据、热数据、结果和归档分层；
- 不以GPU数量、峰值算力或存储总容量代替端到端吞吐验收。

### 13.5 交付物

- 集中任务服务、调度器和资源管理界面；
- 计算节点代理、任务打包和运行环境；
- 用户权限、配额、审计和项目隔离；
- 任务监控、告警、重试和结果管理；
- 容量规划、备份恢复和运维文档；
- 典型任务吞吐和并发验证报告。

## 14. P8 数据资产深化与模型发布生态能力包

### 14.1 建设目标

把首期数据和模型版本管理扩展为可持续运营的资产体系，使新增数据、参数、算法和组件能够经过质量检查、验证、审签、发布、回滚和受控共享。该能力适合多团队长期使用，首个扩展项目不必建设公开插件市场。

### 14.2 数据资产深化

- 原始证据母库、研发标定库、冻结基准库和用户科研库分区；
- 多通道、多站、回采、校准和城市传播数据类型；
- 数据质量等级、标签审签和真值可信度；
- 按设备、日期、地点、架次、接收机和频段隔离数据集；
- 数据派生关系、哈希、版本和删除/归档策略；
- 大文件分段、摘要索引和按时间频率窗口检索；
- 敏感字段脱敏和分级导出；
- 外部介质导入检查和审计。

### 14.3 模型发布生态

建议建设内部“模型组件目录”，而非面向公众的开放市场。每个组件发布前应完成：

1. 清单、依赖和许可检查；
2. 接口、单位、时间和坐标语义检查；
3. 单元和标准算例测试；
4. 性能和资源预算测试；
5. 实测或参考数据验证；
6. 模型卡、适用范围和可信度等级审查；
7. 数字签名、版本发布和回滚准备；
8. 高风险组件的沙箱或独立进程隔离。

### 14.4 受控共享

- 项目内共享默认开启，跨项目共享需授权；
- 实测IQ、站点坐标、设备能力和协议信息单独分级；
- 支持仅共享模型输出或脱敏特征，不必共享原始IQ；
- 所有导入、下载、复制、发布和停用行为保留审计；
- 跨单位共享和分布式数据湖仅在管理制度、网络和数据权属明确后建设。

### 14.5 交付物

- 数据目录、质量、标签、版本和血缘管理工具；
- 多通道、多站和校准元数据扩展；
- 模型组件目录、审签工作流和签名机制；
- 受控共享、导出审批和审计功能；
- 数据治理、模型发布和运维制度文档；
- 数据与模型资产盘点报告。

## 15. P9 受控OTA与高可信试验条件

### 15.1 建设定位

P9用于必须验证真实天线、无线接口、整机耦合或无法通过传导链路覆盖的问题。该能力投资和安全要求较高，不作为一般后置项目的默认配置。

### 15.2 建设内容

- 屏蔽箱或吸波屏蔽室；
- 低功率、受控距离和受控频段的OTA链路；
- 真实无人机遥控、图传设备或侦察整机接入；
- 天线、转台、馈线和空间路径校准；
- 可选矢量信号源、频谱分析仪、网络分析仪和功率计；
- 可选商业射频信道模拟器或高一致性仪器平台；
- RF上电联锁、权限、频率白名单和功率上限；
- OTA结果与传导结果、软件结果的对照。

### 15.3 启动门槛

只有以下条件同时满足时才建议建设：

- 已证明数字半实物和射频传导不能回答目标问题；
- 有明确整机、天线或无线链路验收对象；
- 屏蔽、计量、射频安全和频谱合规条件具备；
- 设备和仪器的采购、校准、维保和使用频次能够支撑投资；
- 有专门试验操作规程和责任人员。

### 15.4 不建议默认采购的设备

- 大型商业射频信道模拟器；
- 大型吸波暗室；
- 大规模同步SDR阵列；
- 超出重点频段和带宽的高端仪器；
- 仅凭厂家峰值参数、尚无对应验收任务的设备。

## 16. 跨能力包公共设计

### 16.1 多保真运行策略

系统应同时保留快速、工程和校准三档运行方式：

| 档位 | 典型模型组合 | 使用场景 |
|---|---|---|
| F1 快速筛选 | 单通道、统计传播、简化误差、参数级结果 | 大批量场景筛选 |
| F2 工程验证 | 4通道/3站、工程多径、主要设备误差、短窗IQ | 日常科研和算法验证 |
| F3 校准复盘 | 实测流形/时钟/城市/设备参数、硬件回采 | 重点事件和专项验收 |

默认使用F2。F3只在校准数据包络内声明较高可信度。

### 16.2 时空频裁剪

- 空间：只对重点街区、关键链路和参与站点升阶；
- 时间：由检测事件、任务窗口或用户标记触发高采样率IQ；
- 频率：只对目标子带保留高采样率，尽早DDC和抽取；
- 通道：只保留参与当前测向或定位的有效通道；
- 模型：每次试验只启用能影响目标指标的非理想因素；
- 数据：中间IQ默认环形缓存，按事件或诊断需要固化。

### 16.3 降级和失败语义

系统至少支持以下状态：

- `valid`：满足输入、校准和模型适用条件；
- `degraded`：使用低一级模型、部分站点或过期校准，结果可用但可信度降低；
- `invalid`：关键输入或同步条件缺失，不能安全输出结果；
- `not_applicable`：模型不适用于当前频段、带宽、阵列或场景。

任何降级必须记录触发原因、替代模型、结果影响和恢复建议。

### 16.4 安全与合规

- 软件仿真和离线数据处理为默认方式；
- 射频试验以同轴传导和屏蔽环境为主；
- 发射频率、功率、持续时间和设备连接受权限与联锁控制；
- 实测数据、设备信息、站点位置和授权协议参数分级管理；
- 第三方算法、MATLAB运行时、硬件SDK和驱动许可在采购前核实；
- 模型输出不得超出数据依据宣称型号级、协议级或个体级真实性。

## 17. 实施路线与阶段门

### 17.1 总体实施策略

建议将后置建设分为五个可停止阶段。每阶段形成可独立使用成果，客户经费或需求变化时可在阶段门结束，不影响已完成能力。

### 17.2 阶段H0：准备度审查与方案冻结

建议周期：1至1.5个月。

主要任务：

- 核验首期基线和遗留问题；
- 明确客户优先科研任务；
- 盘点阵列、多站、城市、设备和实测数据条件；
- 完成数据率、计算量、存储和设备接口核算；
- 选择首批扩展包和非建设项；
- 冻结验收场景、数据和参考算法；
- 形成详细设计、WBS和采购技术条件。

退出条件：

- 扩展范围和依赖经双方确认；
- 至少一个扩展包具备数据和验收条件；
- 关键设备可获得性和许可无阻断问题；
- 指标均绑定明确场景，不存在全域高指标承诺。

### 17.3 阶段H1：纯软件专业能力扩展

建议周期：4至6个月，可并行实施P1、P2、P3中的一个或两个基础包。

主要任务：

- 完成P0公共接口；
- 建立阵列或多站纯软件IQ闭环；
- 建立城市工程传播或高级模型基础能力；
- 完成相关算法基线和评价工具；
- 建立标准算例和冻结数据；
- 完成首轮性能优化。

退出条件：

- 不依赖外部硬件即可完成专业任务闭环；
- 理论算例、误差注入和退化趋势正确；
- 结果可追溯到数据、校准、模型和算法版本；
- 性能边界和不适用条件明确。

### 17.4 阶段H2：实测校准与可信度提升

建议周期：2至4个月，可与H1后半段重叠。

主要任务：

- 导入多通道、多站、城市路线或设备实测数据；
- 完成数据质量和真值审查；
- 标定阵列流形、时钟误差、传播参数或设备模型；
- 使用独立数据验证；
- 形成模型证据包和适用包络；
- 冻结验收参数和容差。

退出条件：

- 校准数据与独立验证数据隔离；
- 关键误差来源可解释；
- 在约定场景下达到参考容差或优于既有基线；
- 不能达到的场景已经降级或从范围中剔除。

### 17.5 阶段H3：数字半实物与射频传导

建议周期：4至6个月，仅在P4启动时实施。

主要任务：

- 完成设备适配和实时流；
- 接入算法硬件或真实设备数字接口；
- 接入两台SDR和时频参考；
- 建设射频衰减、保护和校准链；
- 完成软件、硬件和回采结果对比；
- 使用硬件残差更新软件模型。

退出条件：

- 典型配置连续运行稳定；
- 丢样、失锁、过载和异常可检测；
- 功率、时间和通道校准可追溯；
- 软件与硬件差异形成可解释残差报告；
- 射频安全措施通过检查。

### 17.6 阶段H4：集中运行与持续运营

建议周期：3至5个月，在用户和任务规模达到条件时实施。

主要任务：

- 部署任务队列、资源配额和集中数据服务；
- 建设模型审签、签名和发布流程；
- 完成多用户权限、审计、备份和恢复；
- 建立持续数据导入、模型升级和回归机制；
- 开展用户培训和试运行。

退出条件：

- 多用户任务和数据相互隔离；
- 模型与数据发布经过审签；
- 典型并发任务达到约定吞吐；
- 备份、恢复、升级和回滚演练通过。

### 17.7 阶段H5：受控OTA或专项高可信验证

仅在P9启动条件满足时实施，周期和投资按场地及设备单独论证。H5不作为其他扩展包完成的必要条件。

## 18. 工作包与责任分解

| 工作包 | 主要内容 | 主要输出 | 主要责任角色 |
|---|---|---|---|
| WP0 总体与准备度 | 范围、依赖、架构、指标和阶段门 | 详细方案、需求和接口基线 | 系统总体 |
| WP1 公共接口 | 多通道、多站、时间、坐标和校准 | 数据字典、SDK和标准算例 | 平台/数据组 |
| WP2 阵列测向 | 阵列IQ、流形、校准和AOA | P1软件包和验证报告 | 阵列算法组 |
| WP3 多站定位 | 同步、观测量、融合和航迹 | P2软件包和验证报告 | 定位算法组 |
| WP4 城市传播 | 场景、射线、缓存和校准 | P3软件包和场景包 | 传播建模组 |
| WP5 半实物 | 设备、网络、SDR、RF和时频 | P4设备包和校准报告 | 硬件集成组 |
| WP6 高级模型 | 辐射源、接收机和背景 | P5模型包和证据包 | 信号建模组 |
| WP7 高级算法 | 检测、识别、关联和评价 | P6算法包和测试集 | 算法组 |
| WP8 集中平台 | 任务、资源、用户和运维 | P7服务和运维包 | 平台/运维组 |
| WP9 数据与发布 | 数据治理、审签、签名和共享 | P8治理工具和制度 | 数据/质量组 |
| WP10 V&V | 理论、仿真、实测、硬件和用户验收 | 测试集、报告和证据 | 独立测试组 |

系统总体负责范围和跨包接口；专业组不得单方面改变IQ、时间、坐标和功率语义；硬件组不得以设备已连通替代业务闭环验收；测试组应独立管理冻结数据和验收脚本。

## 19. 周期、团队与工作量建议

### 19.1 单包估算

以下为已有首期平台可复用条件下的工程估算，不含客户数据补采、采购到货等待和大型场地建设：

| 能力包 | 建议周期 | 参考工作量 | 典型团队 |
|---|---:|---:|---:|
| P0 公共扩展底座 | 1.5至2.5个月 | 8至15人月 | 3至5人 |
| P1 阵列测向 | 4至6个月 | 18至28人月 | 4至6人 |
| P2 多站定位 | 5至7个月 | 24至36人月 | 5至7人 |
| P3 城市传播增强 | 5至8个月 | 24至40人月 | 5至7人 |
| P4 数字/射频半实物 | 6至9个月 | 30至50人月 | 6至8人 |
| P5 高级模型库 | 4至8个月/批 | 20至40人月/批 | 4至7人 |
| P6 高级算法 | 4至7个月 | 20至35人月 | 4至6人 |
| P7 集中计算 | 4至6个月 | 18至30人月 | 4至6人 |
| P8 数据与发布生态 | 4至6个月 | 18至30人月 | 4至6人 |

多个能力包并行时，公共接口、测试、数据和项目管理工作量可部分复用，因此不能简单相加。若首期接口不稳定或需要重构，工作量应另行增加。

### 19.2 推荐核心团队

- 项目经理/系统总体：1人；
- 平台和运行引擎：2至3人；
- 信号、阵列或定位算法：2至4人；
- 城市传播与场景：1至2人；
- 数据与校准：1至2人；
- 硬件、网络和射频集成：2人，P4阶段投入；
- 测试与质量：1至2人；
- 用户领域专家：按评审、数据解释和验收阶段参与。

不建议为全部后置能力长期维持一个大团队。推荐以平台、数据和测试人员为稳定核心，专业模型和硬件人员按扩展包投入。

## 20. 性能与技术指标建议

### 20.1 指标制定原则

- 所有指标同时说明硬件、模型组合、采样率、通道数、窗口长度和场景条件；
- 优先验收正确性、可重复性和退化趋势，再验收速度；
- 软件仿真、实验室传导和外场结果使用不同容差；
- 不以厂家峰值带宽、GPU峰值算力或网络标称速率作为系统验收值；
- 指标应在H0数据摸底和基准测试后冻结；
- 参考指标未冻结前不得直接写成全场景刚性合同条款。

### 20.2 建议基础指标

| 类别 | 建议参考口径 |
|---|---|
| 阵列通道 | 4通道基线，8通道可选 |
| 多站数量 | 3站基线，4站可选 |
| 常用IQ采样率 | 5至20 MS/s多通道；40至50 MS/s短窗 |
| 100 MS/s能力 | 单通道文件兼容和短窗处理，不要求全部扩展模型实时 |
| 阵列测向 | 受控校准场景中位绝对误差3°至5°参考 |
| 多站定位 | 良好站形受控场景CEP50不大于50米参考 |
| 城市传播 | 独立实测路径损耗中位绝对误差8至12 dB参考 |
| 射线追踪 | 重点区域、有限阶、预计算和缓存，不要求全城实时 |
| 半实物连续流 | 典型带宽和通道配置连续30分钟，无未标记丢样 |
| 多用户 | 5至10名用户、2至4个重任务并发基线 |
| 可重复性 | 同版本、参数、数据和种子结果一致；硬件结果在校准容差内 |
| 追溯性 | 所有输出关联模型、数据、校准、设备和算法版本 |

“无未标记丢样”是指任何丢样、补零或不连续均能检测和记录，并不等同于所有异常情况下绝对零丢包。

### 20.3 不设置统一高指标的项目

以下项目不建议在缺少条件摸底时设置全局固定数值：

- 任意城市环境下的测向和定位精度；
- 任意私有协议的识别率；
- 任意设备的射频指纹个体识别率；
- 全频段、全通道、全模型同时实时；
- 全城市射线追踪刷新率；
- 任意硬件设备接入时延；
- 跨接收机、跨地点和跨日期的统一算法准确率。

这些指标应按对象、数据、场景和设备形成专项验收表。

## 21. 测试、校准与验收体系

### 21.1 六级验证

1. **接口与解析验证**：数据类型、单位、维度、时间、坐标和版本；
2. **理论算例验证**：阵列相位差、几何时延、频差、路径和坐标转换；
3. **组件验证**：边界、异常、随机性、复位、校准和降级；
4. **软件链集成验证**：多通道、多站、传播和算法端到端闭环；
5. **实测/硬件对比验证**：校准数据、回采数据和独立实测数据；
6. **用户任务验收**：使用客户典型科研流程完成任务、报告和复现。

### 21.2 标准算例建议

#### 阵列测向

- 已知单音和宽带信号角度扫描；
- 通道幅相误差和校准恢复；
- 双目标角分辨；
- 低SNR、多径和阵元失效；
- 实测阵列IQ回放。

#### 多站定位

- 已知站址和静止目标TDOA；
- 运动目标FDOA；
- 时钟偏差、漂移和固定时延；
- 站点失效和异常观测；
- 良好/较差站形对比；
- 多站实测IQ回放。

#### 城市传播

- 开阔区LOS；
- 单建筑遮挡；
- 街谷一次反射；
- 屋顶绕射；
- 动态航迹路径连续性；
- 射线超时降级和缓存命中；
- 标定路线与独立验证路线。

#### 半实物

- 标准IQ实时流；
- 启停、重连、背压和丢包；
- SDR频率、功率和带宽；
- 衰减扫描、阻塞、削顶和AGC恢复；
- 公共时钟、PPS、触发和失锁；
- 软件IQ与射频回采IQ残差。

### 21.3 验收原则

- 每个能力包至少有一个完整业务闭环，不以页面或组件数量验收；
- 关键结果必须同时给出质量标记或不确定度；
- 实测标定数据与冻结验收数据隔离；
- 未达到适用条件时应正确降级或拒绝输出；
- 新能力不得破坏首期功能和旧项目兼容性；
- 所有测试可在交付环境按清单复现；
- 硬件采购验收与业务能力验收分开进行；
- 不能验证的高级能力不纳入“已完成”声明。

### 21.4 各包最低验收闭环

| 能力包 | 最低验收闭环 |
|---|---|
| P1 | 已知方向→阵列IQ→校准→AOA→误差统计 |
| P2 | 已知航迹→多站IQ→TDOA/AOA/FDOA→定位→误差椭圆 |
| P3 | 城市场景→主导路径/抽头→接收IQ→与独立实测特征对比 |
| P4 | 软件IQ→外部设备/SDR→回采IQ或算法结果→参考链对比 |
| P5 | 新模型→合成/回放→特征与算法对比→证据包 |
| P6 | 冻结数据→高级算法/用户算法→统一评价→回归报告 |
| P7 | 多用户提交→排队/运行→结果→审计与恢复 |
| P8 | 数据/模型导入→质量与审签→发布→使用→回滚 |
| P9 | 受控OTA→真实设备结果→传导/软件对照→安全记录 |

## 22. 交付成果

### 22.1 软件与组件

- 公共扩展接口、SDK和设备抽象；
- 选购能力包对应的模型、算法和框图组件；
- 多通道、多站、城市、设备和集中任务界面；
- 数据质量、校准、评价和报告工具；
- 自动化测试、性能诊断和故障定位工具；
- 安装、升级、回滚和兼容转换工具。

### 22.2 数据与参数

- 标准合成多通道/多站IQ；
- 约定范围内的实测、回采或校准数据；
- 城市场景数据包和传播参数；
- 阵列流形、通道幅相、固定时延和设备校准文件；
- 模型参数库、冻结验收集和文件哈希；
- 训练、标定、测试和验收数据划分清单。

### 22.3 硬件与设施

按选购范围交付：

- 算法板卡或真实设备适配接口；
- SDR、时频、网络和射频调理设备；
- 校准器材、线缆、转接、限幅和负载；
- 可选屏蔽环境和测试仪器；
- 设备资产、序列号、固件、驱动和校准记录。

### 22.4 文档与证据

- 扩展需求规格和总体/详细设计；
- 数据、时间、坐标、同步和校准规范；
- 模型卡、算法卡和模型证据包；
- 设备接口、射频安全和操作规程；
- 测试大纲、测试报告、校准报告和验收报告；
- 安装、用户、管理员、开发和运维手册；
- 第三方许可、依赖和兼容矩阵；
- 培训材料和典型案例。

## 23. 投资策略与可选组合

### 23.1 投资原则

- 软件、数据和校准优先于大规模硬件；
- 优先采购能够直接回答验收问题的设备；
- 可借测、租用或复用现有设备时，不强制采购；
- 商业信道模拟器和大型屏蔽设施后置；
- 服务器、GPU和存储根据实测瓶颈扩容；
- 每个设备采购包必须同时包含接口、校准、测试和运维成本；
- 报价按能力包拆分，避免单一总价掩盖可裁剪范围。

### 23.2 推荐组合

#### 组合A：测向科研增强包

组成：P0＋P1＋P6部分能力。

适合：用户重点研究阵列测向、波束形成和多径鲁棒性，已有或计划获得4通道相干IQ。

不必采购：多站设备、商业信道模拟器和大型集中存储。

#### 组合B：多站定位研究包

组成：P0＋P2＋P3基础级，可选P1。

适合：用户重点研究站点部署、TDOA/AOA融合和城市NLOS影响。

第一步可纯软件建设；只有需要真实同步验证时再增加P4C。

#### 组合C：半实物验证包

组成：P0＋P4A＋P4B，可选P1或P2。

适合：用户需要验证算法板卡、SDR、接收机动态范围或硬件实现一致性。

推荐先配置两台SDR和2至4通道传导链，不默认建设OTA。

#### 组合D：城市复杂场景包

组成：P0＋P3＋P5背景扩展＋P6部分能力。

适合：用户重点研究建筑遮挡、街谷多径、弱信号和算法域差异。

以1至5平方公里重点区域为一个建设单元，可滚动增加区域。

#### 组合E：持续运营平台包

组成：P7＋P8，并选择至少一个专业能力包。

适合：5名以上用户长期共享平台、数据和模型，存在批量任务、审签和升级需求。

不建议在没有稳定专业模型和数据资产时单独先建设。

#### 组合F：综合增强包

组成：P0＋P1＋P2＋P3＋P4A/B＋P7＋P8，P5/P6按专题滚动建设。

适合：经费较充足、用户和设备条件明确，希望形成“软件仿真—实测校准—半实物验证—集中科研”完整体系的客户。

P9仍作为专项选配，不自动纳入综合增强包。

### 23.3 建议商务拆分

后续承接时，可将报价和合同拆成以下四类：

1. 平台软件与接口开发；
2. 专业模型、算法和数据校准；
3. 硬件设备、集成和射频校准；
4. 培训、驻场、数据治理和持续升级服务。

这样可以在客户经费变化时保留核心软件和专业能力，不因单项硬件预算不足导致整体项目无法实施。

## 24. 客户需提供或确认的条件

### 24.1 通用条件

- 首期软件、模型、接口和数据版本；
- 后置项目优先解决的3至5个科研问题；
- 重点频段、带宽、采样率、通道数和记录时长；
- 数据保密、导入导出和部署网络要求；
- 用户算法、设备和第三方软件接口资料；
- 现有服务器、GPU、存储、网络和仪器资源；
- 验收更重视算法、模型、硬件还是使用流程。

### 24.2 阵列与多站条件

- 阵元或站点几何、坐标和姿态；
- 时钟、PPS、触发和同步方式；
- 通道幅相、固定时延和校准数据；
- 多通道/多站IQ及其元数据；
- 已知方位、位置或航迹真值；
- 真实设备可否在项目期内联调。

### 24.3 城市传播条件

- 重点区域和频段；
- 地形、建筑、高度和材质类别数据；
- 站址、航迹和天线信息；
- 路测或外场接收功率、时延、多径和LOS/NLOS记录；
- 场景数据权属、更新和交付范围。

### 24.4 半实物条件

- 待接设备型号、接口、SDK、驱动和固件；
- 频段、最大输入输出功率和安全限制；
- 实验室机柜、供电、散热、接地和网络；
- 射频器材、测试仪器和校准周期；
- 频谱合规、保密和设备操作授权。

## 25. 主要风险与控制措施

| 风险 | 可能后果 | 控制措施 |
|---|---|---|
| 首期接口未稳定即扩展 | 多包重复改造 | 先执行P0准备度审查和接口冻结 |
| 阵列只有理想流形 | 测向结果过于乐观 | 增加幅相、时延、互耦和校准残差 |
| 多站只考虑理想时延 | 定位精度虚高 | 独立建模时钟、频偏、固定时延和失锁 |
| 缺少多站真值 | 无法验证定位 | 先做软件真值；外场能力不作高可信声明 |
| 射线追踪范围过大 | 算力和周期失控 | 重点区域、有限阶、缓存和自动降级 |
| 三维场景好看但不可信 | 传播结果缺少依据 | 几何、材质、无线参数分开管理并实测校准 |
| 模型重复计损 | 传播损耗失真 | 明确射线路径与双径、遮挡、阴影的替代关系 |
| 硬件采购过早 | 型号不匹配或闲置 | H0/H1后按实测接口和数据率复核采购 |
| 只看SDR标称带宽 | 连续流丢样 | 端到端核算主机、总线、网络、存储和驱动 |
| SDR互连过载 | 设备损坏 | 固定衰减、限幅、联锁和上电最大衰减 |
| 未经批准开放辐射 | 合规和干扰风险 | 传导优先，OTA必须屏蔽和审批 |
| 高级协议/指纹无依据 | 交付争议 | 采用授权资料和独立数据，限定适用包络 |
| 多用户平台先于业务建设 | 投资利用率低 | 用户、任务和数据量达到阈值后启动P7/P8 |
| 全量多通道IQ长期保存 | 存储和安全失控 | 环形缓存、事件固化、摘要归档和分级授权 |
| 第三方依赖无法再分发 | 交付受阻 | 采购前完成许可、版本和替代路线审查 |
| 指标脱离场景 | 合同难以验收 | 指标绑定频段、SNR、站形、数据和硬件配置 |

## 26. 运维与持续升级机制

### 26.1 版本管理

- 平台、模型、算法、数据、场景、校准、驱动和固件分别版本化；
- 任一试验记录完整依赖清单；
- 校准文件到期或设备变化时提示重新校准；
- 新版本必须在首期基准和对应扩展基准上回归；
- 支持模型、算法和设备适配器单独回滚。

### 26.2 校准维护

- 阵列和射频通道按频段、设备、通道和温度管理；
- 多站固定时延和时钟质量按设备变更重新验证；
- 城市传播参数随场景数据和实测批次升级；
- 校准有效期不宜只按日期判断，还应考虑设备维修、线缆更换和固件升级；
- 过期校准可用于复盘，但结果标记为降级。

### 26.3 服务建议

可形成以下持续服务：

- 每年1至2次模型和数据基线升级；
- 新增辐射源或新接收机专项建模；
- 外场数据导入、质量检查和校准服务；
- 设备驱动、固件和操作系统兼容维护；
- 用户算法接入和统一评价；
- 年度回归、容量和安全审计；
- 专题培训和典型案例更新。

## 27. 合同与验收边界建议

### 27.1 宜明确写入合同的内容

- 选购能力包和明确不包含能力；
- 支持的频段、通道、站点、区域和设备清单；
- 输入数据和客户配合条件；
- 典型验收场景、硬件配置和数据集；
- 软件、模型、数据、设备和文档交付清单；
- 第三方许可和数据权属；
- 参考指标转为正式指标的冻结时间；
- 因客户数据、设备或场地不到位时的替代验收方式。

### 27.2 不宜直接承诺的内容

- 所有无人机型号和私有协议全覆盖；
- 个体射频指纹在任意接收机和环境下稳定识别；
- 全城市、全频段、全站点、全时长连续IQ实时生成；
- 任意复杂NLOS条件下固定高精度定位；
- 任意设备零适配接入；
- 无限通道、无限站点或无限并发；
- 通过购置商业设备自动获得高可信仿真；
- 缺少实测依据时达到型号级或器件级物理真实性。

### 27.3 建议变更机制

以下事项应进入正式变更流程：

- 新增频段、设备、协议或无人机型号；
- 阵列通道、站点数或实时带宽显著增加；
- 重点城市区域扩大或场景精度提高；
- 从软件仿真升级为射频或OTA；
- 从离线/准实时升级为硬实时；
- 新增跨单位数据共享、公开模型发布或高可用要求。

## 28. 推荐基线方案

综合科研价值、实施难度和后续承接可行性，推荐将第一批后置项目基线冻结为：

> P0公共扩展底座＋P1四通道阵列测向＋P2三站软件定位＋P3一个重点街区工程传播＋P4A一种数字半实物接口，并配套实测校准、统一评价和冻结基准。

该基线具有以下特点：

- 覆盖V1.0最核心的未实施能力；
- 以纯软件和已有数据为主，可先形成成果；
- 阵列、多站、城市和硬件接口能够相互组合；
- 不依赖大型屏蔽室、商业信道模拟器和大规模SDR采购；
- 性能以4通道、3站、重点区域和短窗IQ为主，工程上可控；
- 后续可自然扩展到P4B射频传导、P7集中计算和P8模型生态；
- 每个能力包均可单独报价、验收和退出。

若客户经费有限，优先选择P1或P2中的一个专业包，并配套P0和必要数据校准。若客户已有真实接收机或算法板卡且验证需求迫切，可优先选择P4A，但仍需保留软件参考链。若客户经费充足，建议先完成H1/H2再集中采购硬件，避免设备规格先行锁死技术路线。

## 29. 最终结论

V1.0后置功能具备继续承接的技术和工程基础，但应避免把它们合并成一个“大规模、高精度、全实时”的不可控项目。最可行的路线是：

1. 以首期自主框图、分块IQ、数据中心和组件规范为稳定底座；
2. 先补齐多通道、多站、时间、坐标、设备和校准公共语义；
3. 将阵列测向、多站定位、城市传播和半实物分别建设为独立能力包；
4. 使用4通道、3站、1至5平方公里重点区域和短窗IQ作为合理工程起点；
5. 先完成纯软件闭环，再使用实测和硬件数据校准；
6. 采用有限阶射线追踪、预计算、缓存和降级机制控制城市传播成本；
7. 采用数字半实物优先、射频传导其次、受控OTA最后的硬件路线；
8. 高级协议、指纹、精细接收机和算法能力按数据证据滚动扩展；
9. 用户和任务规模达到条件后再建设集中计算、数据湖和发布生态；
10. 所有指标与具体场景、数据、设备和校准条件绑定。

按照该方案，可在不推翻首期成果的前提下，形成清晰、可裁剪、可报价、可交付、可验收的后续项目储备。客户经费允许时，可从单个专业能力包起步；条件成熟时，再组合为软件仿真、实测校准、半实物验证和持续运营一体化的无人机电子信号侦察仿真能力体系。

## 附录A V1.0后置功能追踪矩阵

| V1.0后置或简化内容 | 本方案承接位置 | 推荐处理 |
|---|---|---|
| 相干阵列多通道IQ和AOA测向 | P0、P1 | 4通道起步，实测校准后扩展 |
| 多站同步IQ及TDOA/FDOA | P0、P2 | 3站软件基线，硬件同步选配 |
| 复杂城市几何射线追踪 | P3 | 重点区域、有限阶、缓存和降级 |
| SDR或真实接收机半实物 | P4 | 数字接口→射频传导→多设备同步 |
| GPU、FPGA或专用硬件加速 | P4、P7 | 按实测瓶颈和实时任务使用 |
| 多用户集中计算和任务排队 | P7 | 5至10名用户基线，内网部署 |
| 更大规模辐射源模型库 | P5 | 每批2至4类，模型证据包验收 |
| 用户自定义插件市场和审签发布 | P8 | 先建内部组件目录和审签流程 |
| 协议级模型 | P5 | 仅在授权资料和验证数据具备时建设 |
| 个体射频指纹 | P5、P6 | 作为研究能力，不作无条件型号承诺 |
| 精细平台电磁耦合和阵元互耦 | P1 | 测量矩阵或低阶等效，不做默认全波仿真 |
| 详细互调、镜像、杂散和器件效应 | P5 | 输入输出曲线或实测残差建模 |
| ADC抖动、孔径和精细非线性 | P5 | 有明确设备和算法需求时增加 |
| 复杂城市动态背景 | P5 | 实测背景库＋活动模型 |
| AOA、TDOA、FDOA和融合定位 | P1、P2 | 统一观测量、质量和评价接口 |
| FPGA实时实现 | P4 | 先软件参考，再算法板卡在环 |
| 分布式数据湖和跨单位共享 | P8 | 管理制度和数据权属明确后建设 |
| 全带宽、多通道实时 | P4、P7 | 仅对冻结配置专项优化，不作为全局指标 |
| 受控OTA和真实设备无线接入 | P9 | 有明确必要性和屏蔽条件后建设 |

## 附录B 后置项目立项检查表

### B.1 业务与范围

- [ ] 已明确要解决的科研或装备验证问题；
- [ ] 已选择具体能力包；
- [ ] 已列出不在本次建设范围内的功能；
- [ ] 已定义至少3个典型使用场景；
- [ ] 已明确用户数量和使用频次。

### B.2 数据与真值

- [ ] 已获得或计划获得所需IQ、场景或设备数据；
- [ ] 数据格式和元数据可解析；
- [ ] 具有方位、位置、航迹、站址或设备状态真值；
- [ ] 标定与验收数据能够隔离；
- [ ] 数据权属、保密和交付范围明确。

### B.3 技术与设备

- [ ] 首期接口和版本稳定；
- [ ] 数据率、计算、网络和存储已经核算；
- [ ] 设备接口、SDK、驱动和许可可获得；
- [ ] 校准设备和方法可用；
- [ ] 射频安全、屏蔽和频谱合规条件满足。

### B.4 验收与交付

- [ ] 每项指标已绑定场景、设备和输入条件；
- [ ] 已确定软件参考算法或既有基线；
- [ ] 已确定降级、无效和不适用判定；
- [ ] 已列出软件、模型、数据、硬件和文档交付物；
- [ ] 已明确客户条件不到位时的替代验收方式。

## 附录C 建议参考文件

- [无人机电子信号侦察仿真系统实施方案V1.0](无人机电子信号侦察仿真系统实施方案_v1.0.md)
- [基于MATLAB/Simulink的无人机信号侦察仿真系统可行性与技术路线V3.0](基于MATLAB_Simulink的无人机信号侦察仿真系统可行性与技术路线_v3.0.md)
- [城市反无人机电子侦察IQ仿真两阶段建设方案](城市反无人机电子侦察IQ仿真两阶段建设方案.md)
- [基于本项目需求的支持面向城市反无的电子侦察仿真的硬件建设方案](基于本项目需求的支持面向城市反无的电子侦察仿真的硬件建设方案.md)
- [信号侦察模型信号级与IQ级模拟能力说明](信号侦察模型_信号级与IQ级模拟能力说明.md)
- [射线追踪传播模型概念模型](EM-P-11%20射线追踪传播模型概念模型.md)
- [单站测向模型概念模型](EM-S-05%20单站测向模型概念模型.md)
- [TDOA定位模型概念模型](EM-S-07%20TDOA%20定位模型概念模型.md)
- [FDOA定位模型概念模型](EM-S-08%20FDOA%20定位模型概念模型.md)
