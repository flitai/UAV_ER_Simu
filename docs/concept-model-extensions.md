# 概念模型的 IQ 相关字段扩展

**状态**：骨架，全部待写。

**依据**：02 号方案 §8 的七条规则；`CLAUDE.md`「工程约定」一节。

---

## 1. 本文件的边界（已冻结）

`C-UAV Model Demo/概念模型/` 是**只读快照**。对既有概念模型的任何 IQ 相关字段扩展，
一律写入本文件，**不修改快照里的原文档**。

概念模型库必须保持「不生成 IQ」的边界，这是 EM-B-09 的假设 A-001。原始 IQ 的处理只由
「IQ 观测量提取」组件承担。

## 2. 扩展规则（待写）

02 号方案 §8 给出了七条规则。**待写**：逐条抄录并说明本项目如何执行。

## 3. 扩展记录（待写）

每条扩展记录以下内容：

| 列 | 含义 |
|---|---|
| 被扩展的模型编号 | 例如 EM-B-09 |
| 新增字段名与类型 | |
| 语义与单位 | 单位遵循国际单位制（米、赫兹、分贝毫瓦、度、秒） |
| 依据 | 02 §8 的第几条，或本项目的哪条决策 |
| 影响到的下游产物 | |
| 提出日期与提出人 | |

目前没有已登记的扩展。

## 4. 待写清单

- [ ] 第 2 节 七条规则的执行说明
- [ ] 慢变参数端口 `SceneParamFrame` 的字段定义（决策 D-013 新增，携带链路几何、视距与
      非视距判定、各项损耗分量、噪声底、天线复增益、扫描窗口，以及有效起止时间、更新率
      和溯源字段）
