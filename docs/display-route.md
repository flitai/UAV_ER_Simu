# 地图与态势显示技术路线

**状态**：骨架。路线选择已由决策 D-002、D-008、D-010、D-017 冻结，本文件把这些决策展开成
可执行的照搬清单。色值标定与遮挡计算落点待写。

**依据**：`CLAUDE.md` 的「地理场景 / 态势显示子线」一节；决策 D-002、D-005、D-008、D-010、
D-017；06 备忘录 §5 至 §8。

---

## 1. 三条来源的分工（已冻结）

| 来源 | 贡献什么 | 明确不采用什么 |
|---|---|---|
| `/Users/zhiyu/CC/Airports/` | 底图的图层顺序与全部显示风格，逐项照搬 | — |
| `C-UAV Model Demo/em-demo/` | 态势图层的图形语义，以及离线加载机制 | 它的 protomaps 深色主题、`protomaps-themes-base` 依赖 |
| `C-UAV Model Demo/emcore/` | 遮挡计算的实现与 148 例黄金基准 | — |

## 2. 从 Airports 照搬的清单（已核实行号）

以下行号于 2026-09-03 在 `/Users/zhiyu/CC/Airports/index.html`（共 1883 行）核实存在。
照搬时只把原生 JavaScript 切成 TypeScript 模块，**不改任何视觉参数**。

| 项目 | 位置 | 内容 |
|---|---|---|
| 底图样式函数 | 第 810 行 `protomapsStyle()` | 60 层浅色样式与配色表，含机场要素 |
| 字形加载 | 第 726 行与第 729 行 | 自定义协议 `localglyphs://`，拉丁字符走带符号距离场字形，中日韩字符交给系统字体 |
| 山体阴影 | 第 1218 行 | 阴影参数 |
| 建筑三维拉伸 | 第 1296 行 `addBldg3d()` | 颜色 `#e6e1da`，最小显示层级 14，在层级 14 到 14.7 之间渐显 |
| 相机与控件 | 第 1375 行 `newMap()` | 俯仰角 55 度，导航控件开启俯仰角可视化 |
| 建筑探测 | 第 1281 行 `probePmBldg()` | 瓦片建筑层探测 |
| 全局探针 | 第 1769 行 `window.__probe` | 调试用探针，接入本项目时必须确认无副作用（里程碑 D4） |

观测区域**之内**的建筑改用同源的 `buildings.geojson` 拉伸，但外观参数与 Airports 的瓦片
建筑层保持一致；观测区域**之外**保留 Airports 的瓦片建筑层。

任何视觉上偏离 Airports 的地方，都必须记一条决策。

## 3. 从 em-demo 沿用的图形语义（已冻结）

- 威胁等级用红、黄、绿三色；效应染色的优先级高于威胁等级
- 测向线与到达时间差定位用两种颜色区分
- 覆盖热力图用 viridis 色带分五档，叠加按数值变化的不透明度
- 等检测概率包络线用移动方块算法生成，画在建筑之上
- 平面位置显示扫描、测向线、二倍标准差误差椭圆走 Canvas 叠加层，用地图的投影函数换算像素坐标
- 状态更新用固定频率的定时器（20 赫兹），每帧提交一次状态
- 无人机图标用 SVG 光栅化后以 `source-in` 方式染色，用 `icon-rotate` 表示航向
- 航迹做抽稀

参考文件：`em-demo/src/components/{MapView,CoverageControl,SignalPanel}.tsx`、
`em-demo/src/models/coverage.ts`、`em-demo/src/simulation/engine.ts`。

## 4. 色值重新标定（2026-09-06 完成，切片 ② 的 D2-4）

em-demo 的色值是为深色底图调的，例如测向线用橙色 `#f97316`、到达时间差定位用青色
`#22d3ee`。本项目的底图是 Airports 的浅色样式，这些颜色在浅色底上对比度不足。

标定结果落在 `web/src/scene/style/situation.ts` 的 `SIT` 表。判据取 WCAG 2.1 SC 1.4.11：
非文本图形元素对相邻背景至少 **3:1**（带文字的读数另按 4.5:1，读数在右栏不在图上）。
底图上会与态势元素相邻的三种背景取自 `web/src/scene/style/colors.ts` 的 `PM` 表：
陆地 `#f6f2e9`、建筑 `#e6e1da`、水面 `#c8dced`。

| 用途 | 色值 | 陆地 | 建筑 | 水面 |
|---|---|---|---|---|
| 站点 `site` | `#1e40af` | 7.81:1 | 6.71:1 | 6.20:1 |
| 目标 `target` | `#be123c` | 5.63:1 | 4.83:1 | 4.46:1 |
| 航迹 `trail`（画 0.5 不透明度） | `#be123c` | 同上 | 同上 | 同上 |
| 视距链路 `linkLos` | `#15803d` | 4.49:1 | 3.86:1 | **3.56:1** |
| 非视距链路 `linkNlos` | `#b91c1c` | 5.79:1 | 4.97:1 | 4.60:1 |
| 规划航线 `route`（虚线） | `#475569` | 6.78:1 | 5.83:1 | 5.38:1 |
| 航点 `waypoint` | `#334155` | 9.27:1 | 7.96:1 | 7.35:1 |
| 测量 `measure` | `#7c2d12` | 8.39:1 | 7.20:1 | 6.66:1 |
| 测向线 `bearing` / 交叉定位 `aoa`（D-053）| `#b45309` | 4.49:1 | 3.86:1 | **3.57:1** |
| 时差定位 `tdoa`（D-053）| `#0e7490` | 4.80:1 | 4.12:1 | 3.81:1 |
| 融合解 `fusion`（D-053）| `#6d28d9` | 6.36:1 | 5.46:1 | 5.05:1 |
| 告警区 `zoneAlert` 与入圈目标的红环变体（D-061）| `#b91c1c` | 5.79:1 | 4.97:1 | 4.60:1 |
| 告警区 `zoneWarning`（D-061）| `#92400e` | 6.35:1 | 5.45:1 | 5.04:1 |
| 高度立柱（D-061，α 0.6 的拉伸体）| `#be123c` | 同目标 | 同目标 | 同目标 |
| 链路距离标注与目标标签（D-061，文字按 4.5:1）| `#48423a` 加白晕 | 8.88:1 | 7.63:1 | 7.05:1 |

### 4.1 瀑布上的检测框与识别标签（V-8，随 C-9，2026-09-15）

信号页「叠加检测」把每个突发画成时频框，框色与框上的标签名随识别结果走（`web/src/results/labels.ts`）。
判据与上表**分两条**，缘由是背景不同：上表的背景是三种固定的底图色，这里的背景是整条 viridis 色带
（`#440154` 深蓝 → `#fde725` 亮黄），**任何单一颜色都不可能对整条色带都达到 3:1**。所以：

1. **边界的可见性不靠类别色**，靠三道描边里的外白（4 px）与内墨（1 px）——十个 viridis 锚点上
   `max(白, 墨)` 最低 **3.33:1**（在 `#1f9e89` 那一档），过 3:1 门槛；中间 2 px 的类别色只管区分类别。
2. **类别色量的是标签文字**对它自己那块浅色底衬（`rgba(250,248,245,0.92)` 压在 viridis 上）的对比度，
   按 WCAG 文字的 4.5:1 量，取十个锚点里的最小值。

| 标签 | 名 | 色值 | 文字对底衬（十锚点最小） |
|---|---|---|---|
| `video_link` | 图传 | `#1d4ed8` | 5.37:1 |
| `telemetry_burst` | 遥测 | `#854d0e` | 5.49:1 |
| `rc_hopping` | 遥控 | `#6d28d9` | 5.70:1 |
| `cw_beacon` | 信标 | `#115e59` | 6.08:1 |
| `noise` | 噪声 | `#57534e` | 6.12:1 |
| `unknown` / 没有识别行的段 | 未知 | `#a33333` | 5.47:1 |

库外标签（清单派生的类别等）用 `#57534e` 并原样写出标签名，不为没见过的标签编颜色。
颜色从来不是唯一线索：框上写标签名，突发表、识别表与时间线第三行也都写（09 §12「形状 + 文字 + 颜色」）。
同一张表在结果页「检测识别」的时间线第三行复用。

最低的一项是视距绿对水面 3.56:1，仍高于 3:1 门槛。**视距绿 / 非视距红这对语义沿用 em-demo**
（第 3 节已冻结），只是把色相压深、饱和度降下来适应浅色底。

图标：无人机与站点各一个单色 SVG，运行时光栅化后 `source-in` 染色并描一圈白色光晕，
用 `map.addImage` 注册，不引精灵图集——只有两个图标，维护一张图集不划算，也多一个随包文件（铁律 6）。
无人机图标朝上画，靠 `icon-rotate` 表航向；图源 96 px、`pixelRatio: 3`，`icon-size` 0.75–0.8 得 24–26 CSS px。

**测向线与误差椭圆自 D-053（2026-09-09）起有生产者**：`DirectionFinder` 出 `bearings.jsonl`、
`MultiSiteLocator` 出 `positions.jsonl`，画在 `web/src/scene/layers/fixOverlay.ts` 的 Canvas 叠加层上
（第 3 节冻结的语义就是它）。画法：测向线从站址沿量测方位画，有定位解时画到解算点再延伸 20%、
没有则画固定 3 km；`±2σ` 楔形低透明度填充，裁决非 `valid` 画虚线，`use_policy = exclude` 不画；
定位点画十字加 2σ 椭圆（12% 填充），三种方法三个色；标签只在 `?dev=1`（D-039，界面不主动解释）。
上面三行色值按 D2-4 的同一判据实测，最低 3.57:1 过 3:1 门槛；em-demo 的橙 `#f97316` 与青 `#22d3ee`
是为深色底调的，**不沿用**（D-017）。

**仍然没有的**：威胁分级红黄绿、效应染色、覆盖场 viridis、等检测概率包络——
它们的生产者都还不存在（第 3 节列的是将来的语义，不是现在画得出来的东西）。

## 5. 遮挡计算（落点已定，2026-09-17，D-074）

从 `C-UAV Model Demo/emcore/` 移植到本项目 `geo/` 目录的三部分：

1. `IMapQuery` 接口，含地形高度查询、建筑查询、射线求交、材质查询
2. `LocalSceneAdapter`，100 米桶网格加数字微分分析线段遍历，命中语义是「侵入最深的等效单刀口」
3. `models/occlusion`，ITU-R P.526 刀口衍射

遮挡是软的，不是二值遮挡：雷达用信干噪比减去二倍衍射损耗，单程链路用接收功率减去衍射
损耗；实时计算与覆盖场使用同一个注入点。

移植后必须通过两项验证才算可信：148 例黄金基准对拍，相对误差不超过 1e-9；以及独立的
解析锚点，例如 2.4 吉赫兹、1 千米处的自由空间路径损耗约等于 100.1 分贝。

**落点（用户 2026-09-17 拍板，07 报告 §2）**：**两边都算**。`geo/` 的 C++ 是真理源，浏览器用
TypeScript 复算，两侧同守 `tests/golden/occlusion.json` 的 148 例、判据都是相对误差不超过 1e-9。
浏览器侧的实现自 `em-demo/src/models/occlusion.ts` 祖本回迁（C++ 当初就是从它移植的），
移植时两处必改：外环由「闭合」改成 C++ 约定的「首尾不闭合」，投影原点由写死的西安常数改成参数化。

由此立三条纪律：

1. **C++ 是真理源**。改动顺序固定为改 C++ → 重生成黄金基准 → TypeScript 对拍通过；反过来不允许。
2. **TypeScript 不得有 C++ 没有的分支**。浏览器需要而引擎不需要的东西（按视窗裁剪、按缩放降采样）
   只能写在调用侧，复算函数的输入输出与 C++ 一一对应。
3. **建筑几何懒加载**。只有打开遮挡相关的显示才取；实测 `JSON.parse` 15.9 MB 用 72 毫秒、
   堆增加 41 兆字节，场景页平时不付这笔。取不到就显示「遮挡数据未加载」并不画任何遮挡结论，
   不退回「一律视距」。

投影常数照 D-053 的先例分家：`110540 / 111320·cos(纬度)` 只留在 `geo::legacy` 里守黄金基准，
引擎实际运行喂严格的站心地平坐标。两者的偏差已经量过：亚运村处纬向 −0.445%、经向 −0.138%，
折到 demo-01 整条航线的刀口损耗上最大 0.1 分贝，命中的楼一栋没变。

**链路级的非视距在界面上不需要新代码**：`line_of_sight` 从链路帧一路通到 `sceneStore` 与
链路线着色，非视距红 `#b91c1c` 在第 4 节标定过对比度却一次没画出来过——缺的只是生产者。
**生产者自 D3-5（2026-09-18）上线**：`line_of_sight` 由建筑几何给出，不再恒真。
界面上要看到它还差一步——E3 在前端仍拦着，D3-7 去置灰。

### 5.1 落地情况（2026-09-18，D3-5 / D3-6）

| 侧 | 落点 | 守什么 |
|---|---|---|
| C++（真理源） | `geo/src/{local_scene_adapter,occlusion}.cpp`、`geo/src/link_budget.cpp` 接线、`engine/src/buildings_json.cpp` 解析 | 148 例 + 真实建筑集五条射线 |
| 浏览器复算 | `web/src/scene/occlusion/{frame,adapter,occlusion,geojson,store}.ts` | 同一份 148 例 + 同一份五条射线 |

三条纪律照办。另外**核实到 148 例不够用**：它用的是 12 栋合成楼加 legacy 投影，
**测不到**解析规则、严格站心平面帧与四万多栋楼上的桶网格。因此新增
`tests/golden/occlusion-aoi.json`（北京亚运村真实建筑集五条射线，C++ 产生、浏览器对拍），
实测两侧最差相对差 **9.5e-13（损耗）/ 1.3e-12（侵入）**，判据 1e-9。
两侧的解析计数也逐项对上：47582 要素 → **47662 栋**、忽略内环 345、剔除 0。

**懒加载懒的是数据不是代码**：整个遮挡模块在产物里只占 **835 字节**（gzip 增 0.57 kB），
而 15.9 MB 的 `buildings.geojson` 在场景页首屏一个字节也不取。
`slice2` 有一条断言首屏 `app.occlusion.status === 'idle'`——那是「懒住了」唯一的自动化证据。

**与 C++ 有意的一处不同**：几何类型不认识时 C++ 报错（本项目的建筑集只会有 `Polygon` 与
`MultiPolygon`，出现别的说明上游变了），浏览器计数并跳过——同一份文件还要交给 MapLibre 渲染，
一个坏要素不该让整幅图白掉。计数进 `stats.skippedGeometry`，说得出来，不是静默丢。

## 6. 移植落地情况（2026-09-04，里程碑 D1）

第 2 节的照搬清单已全部落到 `web/src/scene/`：

| 项目 | 落点 | 验证方式 |
|---|---|---|
| 配色表与名称字段 | `style/colors.ts` | 见下 |
| 底图样式函数 | `style/protomaps.ts` | **与 Airports 原函数求值结果逐层比对，60 层的 id、顺序、过滤条件、线宽档位、色值、标注参数完全一致**，固化为黄金基准 `tests/golden/airports-basemap-style.json`，由 `protomaps.test.ts` 守着 |
| 离线字形 | `style/glyphs.ts` + `public/vendor/glyphs.js` | 字形图集逐字节拷入，署名随包 |
| 山体阴影 | `layers/hillshade.ts` | 参数照搬，端到端测试检查图层存在 |
| 建筑三维拉伸 | `layers/buildings3d.ts` | **数据源换成同源 GeoJSON**（铁律 11），外观参数与 `addBldg3d()` 一致 |
| 相机与控件 | `init.ts` | 端到端测试检查相机落在观测区域内 |
| 探针 | `probe.ts` | 只读，无写操作；端到端测试即由它驱动 |

### 6.1 精灵图：不需要（D1-4）

Airports 的样式没有 `sprite` 字段，底图不用任何图标。态势图层的无人机、站点等图标在 D2
用 `map.addImage` 运行时注册，不引入精灵图集。

### 6.2 与 Airports 的并排比对结果

同一底图文件、同一相机（中心 116.405, 39.99，缩放 14.2，俯仰 55 度）、同一 1200 × 713 视口，
两边都只留底图（关掉三维建筑、山体阴影，隐藏侧栏与控件），逐像素比较：

| 指标 | 结果 |
|---|---|
| 容差 8 以内的像素 | 99.83% |
| 逐位完全相同的像素 | 93.72% |
| 平均绝对差 | 0.15（满量程 255） |

**不是逐位相同，也不应该是**：本项目用 MapLibre GL 5.24.0，Airports 用 5.6.1（决策 D-008
有意锁定同 em-demo 的版本），两版在文字排布与抗锯齿上有亚像素差别；另外参照页还多 5 个它
自己的机场要素图层。判据取"看起来是不是同一张图"，99.83% 是通过。

### 6.3 仍待写

- [x] 第 4 节 浅色底图上的态势色值标定表（2026-09-06，切片 ②）
- [x] 第 5 节 遮挡计算落点（2026-09-17，07 报告 / D-074：浏览器复算并与 C++ 黄金基准对拍）
- [x] 坐标基座的实现选择（2026-09-17，07 报告 / D-074：vendored GeographicLib，自写闭式保留作对拍件）
- [~] 建筑数据进浏览器的帧率实测：渲染那一份由 MapLibre 在工作线程里拉；**复算那一份 2026-09-17 量过**——
      `JSON.parse` 72 毫秒、堆 41 兆字节，懒加载即可，暂不需要按瓦片切分。几何单一来源不变（铁律 11）
- [x] V-2 新增元素的色值标定（2026-09-12：告警区两色、立柱、标注四行已回填第 4 节表；`warning` 取 `#92400e` 而不是候选的 `#a16207`，为与测向线的 `#b45309` 拉开）
- [x] 链路线从未画出的缺陷（2026-09-12 随 V-1 修：`splitLinkId` 按 `derivedLinks(doc)` 精确匹配；`slice2-smoke` 加渲染断言，实测 1 个要素）
