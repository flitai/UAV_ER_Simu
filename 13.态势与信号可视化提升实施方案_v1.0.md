# 13. 态势与信号可视化提升实施方案

> **版本** v1.0，2026-09-12（§13 场景页信息分层于同日追加，D-062）。**定位**：把场景页与结果页的表达提到指挥中心水准——**数据一件不新造**。链路帧、每站测向报告、三种定位解、dBm 标定的频谱与瀑布今天都有，缺的是把它们放到目标身上（卡片）、放到地图上（看得见的叠加）、放到时间轴上（可拖可放）。作为独立的 **V 线**，第一个切片 ⑧「一屏讲清楚」插在切片 ⑦ 之后、④b（C-3 检测升级）之前，可与 C-3 并行。**依据**：04 §3.1（场景配置）、§8.1（主界面）、§8.2（运行与回放）；05 §5.2；09 §2（总体框架）、§3.2（运行组与时间游标）、§4.4（三栏规则）、§5（场景视图）、§7（结果视图）、§11（键鼠）、§12（视觉与文案）、§13（四态表现）；10 §5.7；11 §5；12 §5；决策 D-001、D-017、D-030、D-039、D-042b、D-043、D-047、D-048、D-049、D-051、D-053、D-054、D-058、D-060；铁律 1、2、6、8、10、14、15。**本方案的结论记为决策 D-061。**
>
> **用户指示（2026-09-12）**：「Aaronia AG 的反无解决方案给了我很好的启发，关于反无的态势、和系统的界面设计，特别是信号的可视化展示和方式对我们这个项目有很多借鉴之处」；「之前实现的 demo 案例 C-UAV Model Demo，其中的界面排版还是可以有很多借鉴的」。
>
> **用户拍板（2026-09-12，六条）**：① 先写方案、决策与步骤线，**代码待排期**；② V 线第一个切片「一屏讲清楚」= V-1 目标卡片栈、V-2 地图叠加加重、V-3 时间轴，加上「采用了最近任务却没跟着载入它的场景」这个缺口；③ 时间轴做成**全宽条**，不挤在顶栏；④ **圆形告警区进本切片**（要改场景格式，但它是「威胁红黄绿」语义唯一可靠的生产者）；⑤ 目标卡片栈**常驻右栏**，不做浮动面板；⑥ em-demo 式的底部信号条**留到 V-10**（要先把信号视图从单例改成按实例持有）。
>
> 本方案只定表达、数据来源、版式、格式变更与步骤，不含实现记录；实现中核实到的偏差按 06 §14 体例另记修正。量化数字一律「原型阶段验证值」（D-028）。

---

## 0. 一页结论

| # | 问题 | 结论 |
|---|---|---|
| 1 | 学谁、学什么 | 三组参考。**Aaronia AARTOS**（`ref/Aaronia AG/`，25 图 2 视频）的八种表达：指挥中心一屏、目标卡片、加重的地图叠加、站点方位玫瑰、多频段总览加分段放大、多站网格瀑布、余辉密度频谱、时差定位面板（§1.1）。**em-demo**（`C-UAV Model Demo/em-demo`）的版式：左图标工具栏、中央地图、地图下方固定信号条、右栏「目标列表表格 + 目标详情分节」、悬浮播放条（§1.2）。**T.META 概念图**（EM-C-UAV 工程 `UI/` 目录三张）：顶栏计数卡、地图上目标卡、右侧目标列表（§1.3） |
| 2 | 我们缺什么 | **不缺数据，缺表达**。链路帧（距离、方位、路损、时延、多普勒）、每站测向报告（σ 与质量档）、三种定位解与 2σ 椭圆、dBm 谱都在产品文件与端点里；界面上态势元素太轻、右栏空着、一次只看一个观测点、没有时间轴（§1.4）。另有两处顺带发现（§6）：刷新后采用了最近任务却载入缺省场景；**链路线从未画出**（链路标识按最后一个连字符拆分，`site-1-uav-1` 拆成 `site-1-uav` 与 `1`） |
| 3 | 本期做哪些 | 切片 ⑧「一屏讲清楚」= **V-1 目标与站点卡片栈、V-2 地图叠加加重（含圆形告警区）、V-3 全宽时间轴** + 两处缺口。⑨（V-4 站点方位图、V-5 精度场与覆盖场）、⑩（V-6 网格瀑布、V-7 余辉、V-8 标签条随 C-9、V-9 全带总览随 ④c）、⑪（V-10 全景一屏含底部信号条、V-11 视觉密度）**待排期**（§7、§10） |
| 4 | 版式 | 守 09 §4.4 三栏：场景页右栏改为**常驻的目标列表 + 卡片栈**，选中对象时该对象的表单叠在栈顶，取消选中回到栈。**全宽时间轴条**作 `.app` 四行网格的第三行（`var(--topbar-h) 1fr auto auto`，抽屉之上，在视图区与底部抽屉之间，场景页与结果页可见）——**不是**可停靠窗口、不是浮动面板（09 §2 明文不做，D-060 同理）（§2） |
| 5 | 卡片写什么 | 只写事实：机型、频段、发射状态、经纬高、航向、速度、每站一行的距离 / 方位 / 路损 / 视距 / 接收电平 / 信噪比 / 测向方位 ± 2σ 与质量档、定位方法 / CEP / 几何质量、在哪个告警区内。**不写「威胁 %」「概率 %」**——它们没有生产者，铁律 15 不许编；「概率」留给 C-4 的识别后验。接收电平与信噪比按链路帧算：`P_rx = P_tx + G_t + G_r − L`，`N = −174 + nf + 10·log10 fs`，与引擎 `locate.cpp:255-257` 同式（§3） |
| 6 | 告警区 | 场景对象 `zones[]`，本期**只做圆形**：`{id, name, kind ∈ alert \| warning, shape: circle, center, radius_m, alt_max_m?}`。引擎收下并原样保存、不解释（与 `equipment_model` 同一先例，D-054）；服务端 `PUT` 不用改（只查 `scenario_id` 与 `aoi.id`）；schema、格式文档、引擎键表三处**同一提交**。目标进圈即图标换红环变体、列表行与卡片加「告警区内」徽标——这是 `docs/display-route.md` §3 冻结的「威胁红黄绿」语义**第一次有生产者**（§4.6） |
| 7 | 时基与回放 | 时间轴的时刻 = 引擎逻辑时间 `t_s`（0 到 `duration_s`）。信号页游标 `signal.cursor_t_s` 是相对产品起点 `index.t0_s` 的秒数，换算 `cursor = t − t0_s`。地图回放要一份**按时间索引的历史**——今天 `sceneStore` 只留每个键的最后一帧（`replaceFromTrack` 把整段历史折成一帧，`sceneStore.ts:155-170`），拖到过去无从取值；新增按键排序的历史数组与 `snapshotAt(t)`（§5） |
| 8 | 采用任务要载入它的场景 | 根因：`bootstrap` 里场景作业恒取清单第一项、任务作业并发且互不知情（`actions.ts:31-39, 62-68`）；`task.json` 只有 `scenario_sha256` 没有 `scenario_id`——`manager.ts:611/692` 早已算出、`:264` 解构时丢掉了。修法两行服务端加一处前端（§6.1） |
| 9 | 链路线为什么没画出 | `useSituation.ts:134-136` 与 `ObjectForm.tsx:377-379` 按最后一个 `-` 拆 `link_id`，而站与源 id 本身含 `-`。改为用 `derivedLinks(doc)` 生成的 `${site}-${emitter}` 精确匹配，不拆字符串（§6.2） |
| 10 | 不学什么 | 深色主题与彩虹色图（D-017 浅色底图、viridis 感知均匀，瀑布画布本来就是深的）；可停靠的块窗口系统（那是界面版的自由画布）；没有生产者的威胁 % 与概率 %；卫星影像底图（离线体积与授权不合算，山体阴影 + 三维建筑是对应物）；DroneID 解码、干扰、光电、车载方舱（04 电侦范围外） |
| 11 | 基准变更 | **只有一处**：`demo-03` 加一个告警区，文件哈希变 → 重生成 `tests/golden/scenario-track-demo-03.json`（723 个样点必须逐点相同，只 `scenario_sha256` 变，D-054 先例）+ 两份 `tests/regression/diagrams/chain-3x3-{aoa,tdoa}.json` 的 `scenario_ref.sha256`。**`demo-01` 不动**，故 `DEFAULT_CHAIN_TEXT`（内嵌 demo-01 哈希 `72b8e8d0…`）逐字节不变；组件目录与产品黄金基准零变化（§9） |
| 12 | 怎么验收 | 新 `tests/e2e/slice8-smoke.mjs`（约 30 项）：卡片读数与 `links` / `bearings` 端点在游标时刻的行逐值相等、拖时间轴后图标位置等于 `track` 该时刻行、目标入圈徽标与图标变体切换、刷新后场景跟任务、链路线渲染要素 ≥ 1、新色值三种底色对比度 ≥ 3:1；既有六套 e2e 全绿（§11） |
| — | **不做** | 底部信号条与信号视图实例化重构（V-10）；多站网格瀑布（V-6）、余辉（V-7）、精度场（V-5）、方位图（V-4）；多边形告警区；按地理位置自动分区；顶栏 KPI 计数卡（计数放在时间轴条左端，不再挤顶栏） |

---

## 1. 依据、参考与现状

### 1.1 Aaronia AARTOS 的八种表达

素材在 `ref/Aaronia AG/`（27 份，只读；第三方宣传材料，**不入库**，附录 A 有清单）。网站本身被反爬拦住，方案文字来自 Aaronia 官网与几篇报道。

| # | 它们怎么做 | 素材 | 本质 | 本方案落点 |
|---|---|---|---|---|
| 1 | 指挥中心一屏：地图、右侧目标卡片栈、频谱与瀑布并排常显 | C2 1、X7 2、X2 视频 | 演示不切页，一屏看见「在哪、是谁、信号什么样」 | §2 版式；底部信号条留 V-10 |
| 2 | 目标卡片：机型剪影、名称与 ID、频段、探测器、坐标；小方位盘；三根竖条能量 dBm / 概率 % / 威胁 %；距离与地面距离 | X7 2、HAWK 1、HAWK 2 | 每个目标一张固定版式的卡，读数不用点选 | V-1（§3）；三根竖条只留能量，另两根没有生产者 |
| 3 | 地图叠加：测向线是宽的半透明色带、沿距离渐隐；目标图标带圆环，旁边三行读数；告警圈红、预警区绿、传感器覆盖黄；航迹红折线 | X7 2、X2 视频、C2 1 | 叠加元素比底图「重」，层次一眼分开 | V-2（§4） |
| 4 | 站点方位玫瑰：扇区 × 频段的径向矩阵；「信号强度定位」的方位对功率曲线 | C2 2、DF4 1、DF4 2 | 单站的角度信息自己有一张图 | V-4（§7，待排期） |
| 5 | 多频段总览加分段放大：顶部全带频谱标编号频段框，下方八个放大面板各带频谱与瀑布，面板顶部是频段 / 业务 / 信道号标签条 | X7 1 | 宽带里同时盯多个窄带，标签告诉你每段是什么 | V-8、V-9（§7，随 C-9 / ④c） |
| 6 | 网格瀑布：多站同频段瀑布并排，共用时间轴与色标 | X7 1 下半、DF9 1 | 多站对比靠并排不靠切换 | V-6（§7） |
| 7 | 余辉密度频谱，叠最大保持填充与实时线 | DF4 2、DF2.1、DF2.2 | 突发与跳频在密度图上一眼可见 | V-7（§7） |
| 8 | 时差定位面板：脉冲瀑布框出脉冲、每站状态灯、「脉冲详情」与「时差站对表」；时差仿真器在地图上画精度热力场并标「最佳精度 68.9 m」 | DF2.2、DF9 1、HAWK 1 | 定位不只给一个点，还给「为什么是这个精度」 | V-5（§7，待排期） |

其余素材（车载方舱、光电转塔、干扰机、雷达三维禁飞区、视频叠加）属反制与光电范畴，在 04 电侦范围外，不借。

### 1.2 em-demo 的版式

EM-C-UAV 工程的《软件使用说明》§三 给出的线框：

```
┌─────────────── 顶部状态栏 ───────────────┐
│工│                                  │ 目 │
│具│        2.5D 城市地图             │ 标 │
│栏│   (浮动: 参数/覆盖/效应面板)     │ 列 │
│  │  [仿真控制条]                    │ 表 │
│  ├──────── 频谱瀑布 + 检测表 ───────┤    │
└──┴──────────────────────────────────┴────┘
```

| 可借 | 出处（em-demo 文件） | 本方案落点 |
|---|---|---|
| 右栏「目标列表表格（ID / 速度 / 高度 / 航向 / 状态）+ 目标详情分节（运动状态 / 电磁特征 / 雷达探测 / 信号侦测 / 建筑遮挡 / AOA / TDOA / 定位体制对比）」，分节是二列「标签 — 值」栅格 | `src/components/TargetList.tsx`、`TargetDetail.tsx` | V-1 卡片版式（§3.3） |
| 地图上目标标签 `${id} · ${alt}m`，白字深晕；选中环 `circle-radius 20 / stroke 2` | `src/components/MapView.tsx` 的 `target-label` 与选中环图层 | V-2 选中环（§4） |
| 测向线透明度随测向质量分档；椭圆虚线描边 + 低透明填充 + 发光中心点 + 标签写 CEP / GDOP | `MapView.tsx` 的 `drawBearingLines` / `drawAOAFixes` | V-2 测向带与椭圆（§4） |
| 悬浮播放条：⟲ 重置、▶ / ⏸、倍速 0.5–10× | `src/components/SimControls.tsx` | V-3 时间轴条右端（§5） |
| 顶部状态栏计数（跟踪中 / 已发现 / 威胁 / 受扰 / 已毁伤）与设备在线数 | `src/components/StatusBar.tsx` | V-3 时间轴条左端的计数（§5），只放有生产者的数 |
| 地图下方固定 200 px「单站瀑布 + 网络融合检测表」 | `src/components/SignalPanel.tsx` | **V-10**（用户拍板 ⑥） |

不借的：深色主题（`--bg-primary #0a0e17`）与为深底调的色值（`#f97316` 橙、`#22d3ee` 青，D-017 已定不沿用）；浮动面板（覆盖控制、参数面板、效应面板三块悬浮在地图上，09 §2 明文「没有可拖拽的浮动面板」）；前端自算物理（em-demo 的 `models/` 在浏览器里算路损与定位，本项目物理在 `geo/`，浏览器只显示，D-033）。

### 1.3 T.META 指挥中心概念图

EM-C-UAV 工程 `UI/` 目录的三张 `detail_image_*.png`（当时做 em-demo 的参考）：顶栏一排计数卡（出现 / 进入 / 干扰会话）与设备在线数；地图上每个站点一个覆盖扇区与告警圈，目标图标带编号标签；点选目标弹出卡（时间、经纬、高度、航向、速度、RCS，以及「以中心点计」的地面距离、方位、俯仰）；右侧目标列表（ID / 速度 / 高度 / 航向 / 动作）。它证实了两件事：**目标卡的字段集与 Aaronia 一致**（身份 + 运动 + 相对站点的几何），**目标列表是表不是卡**——列表用来扫、卡用来读。

### 1.4 现状（2026-09-12 截图，2560 × 1440，demo-03 三站三源任务跑完）

| 页面 | 现状 | 差距 |
|---|---|---|
| 场景页 | 底图与 47582 栋建筑完整；态势元素轻：站点 4 px 圆点 + 24 px 图标（`icon-size 0.75`）、测向线 1.5 px、±2σ 楔形填充 α 0.1、目标图标 25.6 px（`icon-size 0.8`）、椭圆 1.5 px（`fixOverlay.ts:122-177`、`layers/situation.ts:142-168`）；右栏空态写「在左栏或地图上选一个对象」（`ObjectForm.tsx:157-161`）| 不点选什么读数都没有；三架机三个站在图上分不出谁是谁 |
| 结果页 | 频谱 + 瀑布 + 包络条是仪表级的（纵轴 dBm、RBW、marker，D-048）| 六个观测点页签轮着点；无余辉；无检测标签（C-3、C-4 未做）；无多站并排 |
| 顶栏 | `t 20.0 s / 20.0 s ×2.2` 读数 + 3 px 细进度条（`RunGroup.tsx:21-27`）| 没有可拖的时间轴；活动（起飞 / 开机 / 悬停 / 巡航）只在左栏列表里；09 §3.2 承诺的「运行结束后变为可拖时间游标」未做，且那一格只有 170 px |
| 缺口一 | 刷新后页面采用了最近任务（demo-03，三站），场景页却载入了缺省场景 demo-01，地图上只有 site-1 的测向线 | §6.1 |
| 缺口二 | 地图上没有站到目标的链路线（截图可证），链路读数的几何回退也取不到站 | §6.2 |

### 1.5 可用资产（都已存在，本方案只是把它们摆出来）

| 资产 | 位置 | 给谁用 |
|---|---|---|
| 实时态势小 store：实体 / 链路 / 测向 / 定位 / 航迹，20 Hz 定频取值 | `web/src/scene/sceneStore.ts`、`useSituation.ts` | V-1、V-2、V-3 |
| 回看端点：`GET /api/v1/results/{task}/{track\|links\|bearings\|positions}?t0&t1&stride`，闭区间取窗、按键抽稀 | `server/src/products/routes.ts:38-68` | V-3 回放缓冲 |
| 测向线与椭圆的 Canvas 叠加层 | `web/src/scene/layers/fixOverlay.ts` | V-2 |
| 图标光栅化与色表（对陆地 / 建筑 / 水面三种底色实测对比度 ≥ 3:1） | `web/src/scene/style/situation.ts` | V-2 新元素同一判据 |
| 浏览器航迹预览（与 C++ 同式、逐点对拍）与活动预览 | `web/src/scene/editor/preview.ts` 的 `RoutePreview` / `ActivityPreview` | V-3 未运行时也能拖 |
| 信号页时间游标 | `AppState.signal.cursor_t_s`，`signal/cursor` 动作，`commands.ts:23-39` | V-3 联动 |
| 派生链路 | `scenarioOps.ts:50-58` 的 `derivedLinks(doc)` | §6.2 修法、V-1 归属 |
| 测向 / 定位读数的既有小面板 | `ObjectForm.tsx:293-341` 的 `EmitterFixes` | V-1 目标卡的直接前身 |

---

## 2. 总体版式

```
┌──────────────────────────────────────────────────────────────────────────────────────────┐
│ 顶栏 48   ▣ │ 默认项目 › 典型链路 › t-… ■ ● 有效 │ ▶ 运行 │ t 20.0 s / 120 s ×2.2 │ [场景][框图][结果][数据] │ ⋯ │
├──────────────────────────────────────────────────────────────────────────────────────────┤
│ 左栏 280 ┃ 中栏 地图（工具条：选择 布站 布目标 航点 测量 布告警区 │ 图层 ▾ │ 平视） ┃ 右栏 320 / 400   │
│ 数据包    ┃                                                                  ┃ 目标 (3)          │
│ 场景对象树┃        ◉ site-1 ═══ 1.83 km ═══▶ ✈ uav-1 ┃ 立柱                     ┃ ┌ uav-1 多旋翼 ▲ │
│  站点 (3) ┃        ╲ 测向色带渐隐         (红环=告警区内)                        ┃ │ 120 m 20 m/s 228°│
│  辐射源(3)┃         ╲                    ◯ 告警区 600 m（红虚线圈）                ┃ │ site-1 1.83 km  │
│  告警区(1)┃          ◉ site-2   ⊕ 定位椭圆 2σ                                  ┃ │  −68.2 dBm 视距  │
│  航线 (3) ┃                                                                  ┃ │  方位 228.4°±2.1°│
│  活动 (9) ┃  N↑ ├──┤ 500 m                                    (V-10 插槽：底部信号条) ┃ │ 定位 aoa CEP 8 m│
│  链路 (9) ┃                                                                  ┃ └────────────────┘
│           ┃                                                                  ┃ 站点 (3) …        │
├──────────────────────────────────────────────────────────────────────────────────────────┤
│ 时间轴 40  目标 3 · 站 3 · 视距 9/9 · 测向 9 · 定位 3 │ ▲●  ◆    ▶   ●    ▼   ┃ 0 ──●───── 120 s │ t 41.3 s │ ⏸ 1× 2× 5× │ ☐ 跟随 │
├──────────────────────────────────────────────────────────────────────────────────────────┤
│ 底部抽屉 28  [日志][告警][数据流][资源]       116.4052°E 39.9901°N │ WGS-84 · AGL · LogicalSim │ ● 已连接 seq 78110 │
└──────────────────────────────────────────────────────────────────────────────────────────┘
```

| 规则 | 内容 | 依据 |
|---|---|---|
| 三栏不变 | 左 280 / 中 剩余 / 右 320（2560 下 1.25×，`reducer.ts:16`），可收起、可拖、最小值不变 | 09 §4.4 |
| 右栏内容 | 场景页右栏默认 = **目标列表 + 目标卡片栈 + 站点卡列表**；选中任一对象时该对象的表单（既有 `ObjectPanel`）叠在栈顶并带「返回」，取消选中或 `Esc` 回到栈；框图页与结果页右栏不变 | 用户拍板 ⑤ |
| 时间轴条 | `.app` 网格由 `var(--topbar-h) 1fr auto` 改为 `var(--topbar-h) 1fr auto auto`，第三行是时间轴条（40 px，1920 宽下折叠左端计数）；**不放进 `.views`**（那是绝对定位层，会盖地图）；`--topbar-h` 不动（toast 位置与网格都依赖它，`app.css:26, 187`） | 用户拍板 ③；`app.css:26-28` |
| 可见性 | 时间轴条在场景页与结果页显示，框图页与数据页 `hidden`（视图切换不卸载，09 §4.2） | — |
| 底部信号条插槽 | 中栏地图容器下方预留 `scene-strip`，本期高度 0；V-10 落地时放紧凑信号视图 | 用户拍板 ⑥ |
| 不做 | 浮动面板、可停靠窗口、多标签页；深色主题 | 09 §2；D-017；D-060 |

---

## 3. V-1 目标与站点卡片栈

### 3.1 数据源与归属

| 数据 | 来源 | 说明 |
|---|---|---|
| 目标身份与配置 | 场景文档 `emitters(doc)`（`scenarioOps.ts:22`）：`id / name / platform_type / equipment_model / emission{center_Hz, bw_Hz, tx_power_dBm, antenna_gain_dBi, polarization}` | 静态 |
| 站点身份与配置 | `sites(doc)`：`id / name / equipment_model / position / antenna.gain_dBi / receiver{fs_Hz, center_Hz, bw_Hz, nf_dB} / clock` | 静态 |
| 目标运动 | `sceneStore.entities`（`EntitySample{t_s, id, lon, lat, alt_m, heading_deg, speed_mps, tx_on, center_Hz}`，`sceneStore.ts:11`） | 实时或回放 |
| 链路读数 | `sceneStore.links`（`LinkSample{link_id, line_of_sight, distance_m, azimuth_deg, elevation_deg, path_loss_dB, delay_s, doppler_Hz, state}`，`:23`） | 键 `link_id` |
| 测向 | `sceneStore.bearings`（`BearingSample{site_id, emitter_id, link_id, bearing_deg, bearing_std_deg, snr_dB, level_dBm, df_quality, df_result_state, use_policy, mixture}`，`:42`） | 自带身份三件 |
| 定位 | `sceneStore.positions`（`PositionSample{emitter_id, method, lon, lat, semi_major_m, semi_minor_m, rotation_deg, cep_m, gdop, min_crossing_angle_deg, geometry_quality, time_quality, participating_sites, state}`，`:57`） | 键 `emitter_id:method` |

**链路归属不拆字符串**：`links.jsonl` 的行只有 `link_id`（`"<site_id>-<emitter_id>"`，`geo/src/scenario.cpp:250`），而站与源的 id 本身含 `-`。卡片按 `derivedLinks(doc)`（`scenarioOps.ts:50-58`，站点 × 辐射源全交叉，`id = ${s.id}-${e.id}`）的 id **精确匹配** `sceneStore.links` 的键，得到每个目标对每个站的那一行。这也是 §6.2 的修法。

**不用** `AppState.entities` / `AppState.links`：`types.ts:172-173` 是 `Record<string, never>` 的死存根，永远为空。订阅方式沿用 `useSyncExternalStore(sceneStore.subscribe, sceneStore.get, sceneStore.get)`（`ObjectForm.tsx:294, 375`）。

### 3.2 派生读数的公式

卡片上有两个不在任何产品行里的数，按链路帧当场算，与引擎的算法一字不差：

| 读数 | 公式 | 引擎同式 |
|---|---|---|
| 接收电平 `P_rx`（dBm） | `tx_power_dBm + antenna_gain_dBi(源) + antenna.gain_dBi(站) − path_loss_dB` | `engine/src/locate.cpp:255`（`DirectionFinder`）；`channel.cpp:268-269`（施加到 IQ 的同一常数） |
| 噪声功率 `N`（dBm） | `−174 + nf_dB + 10·log10(fs_Hz)` | `geo/src/link_budget.cpp:23` 的 `thermal_noise_dBm_per_Hz` + `locate.cpp:256` 的带宽积分（带宽取站的 `fs_Hz`，与 `ToaEstimator` 一致） |
| 信噪比 | `P_rx − N` | `locate.cpp:257` |

`path_loss_dB` 是纯传播损耗（不含发射功率与天线增益，D-049 ⑤），所以这三个数是**链路读数**而非任何观测点的实测值；卡片标签写「接收电平」「信噪比」即可（D-039：不写解释句），模型卡与本节记明它们是按帧算的。与 D-054 一致，`G_t` / `G_r` / `nf` 都从场景逐实体取。

### 3.3 版式

右栏自上而下三块（em-demo 的「列表扫、卡片读」）：

**目标列表**（表，一行一个辐射源）：机型剪影（按 `platform_type` 四选一的单色 SVG，随 `makeIcon` 的方式光栅化）、`id` 与名称、高度、速度、航向、发射（`tx_on` 实心 / 空心圆）、最近站距离、告警区徽标。点行 = `dispatch({type:'scene/select', selection:{kind:'emitter', id}})`，与地图点图标（`SceneView.tsx:172`）是同一个动作，所以列表、卡片、地图三处选中天然一致。

**目标卡**（分节，二列「标签 — 值」栅格，照 em-demo `TargetDetail` 的 `Section` / `Field`）。默认展开选中的目标；无选中时展开第一个，其余折叠成一行标题：

| 节 | 字段 | 来源 |
|---|---|---|
| 身份 | 机型剪影 + `name`、`equipment_model`（缺席不显示）、中心频率、带宽、发射功率、天线增益、极化 | 场景 |
| 运动 | 经度、纬度、离地高（标 AGL，铁律 2）、航向、速度、发射（开 / 关） | `entities` |
| 各站（K 行） | 站名、距离、地面距离（`distance · cos(elevation)`）、方位、俯仰、路损、视距 / 非视距、**接收电平**、**信噪比**、测向 `方位 ± 2σ` 与 `DF-Q` 档、小方位盘 | `links` + `bearings` + §3.2 |
| 定位（每种方法一行） | 方法、经纬度、CEP、GDOP（`aoa` 行的 `gdop` 实为 `rms_trace_m`，单位 m；`tdoa` 行无量纲——**按行标单位**，不混）、几何质量、时统质量、参与站、最小交会角 | `positions` |
| 告警区 | 在哪个区内（区名与 `kind`）；不在则本节不出现 | §4.6 判定 |

小方位盘：40 px 的 SVG，真北朝上，量测方位画一根箭头、±2σ 画一个扇区（同 `fixOverlay` 的楔形），`df_result_state` 非 `valid` 画虚线。**不画真值方位**（D-039；真值只在 `?dev=1` 叠一根灰针）。

**站点卡列表**（K ≤ 8，一行一站，可展开）：站名、`equipment_model`、`fs / center / nf`、链路数、对各目标的 `DF-Q` 档、站钟 `sync_state`（有 `clock` 时）。

所有数值工程计数法（09 §12：`2.44 GHz`、`−68.2 dBm`、`1.83 km`、`6.1 µs`）；无解释句；四态徽标沿用 09 §13.2（链路帧非 `valid` 时该站行加斜纹底）。

### 3.4 探针与断言

`app.cards[]`：每目标 `{id, platform_type, inZone: string | null, sites: [{site_id, distance_m, azimuth_deg, path_loss_dB, rx_dBm, snr_dB, bearing_deg, sigma_deg, df_quality}], fixes: [{method, cep_m}]}`；`app.siteCards[]`：`{id, links, worst_quality}`。断言：不点选即 `cards.length === emitters.length`；`rx_dBm` 等于按 §3.2 用端点行重算的值（容差 1e-6）。

---

## 4. V-2 地图叠加加重

### 4.1 逐元素的改法

| 元素 | 今天 | 改成 | 落点 |
|---|---|---|---|
| 测向带 | 楔形填充 α 0.1（`bearing_std_deg > 0.05` 才画）+ 1.5 px 中心线 | **渐隐色带**：楔形半角取 `max(2σ, 5 px 在射线中点所张的角)`（σ 很小时也有可见宽度），沿射线用 `createLinearGradient` 从站址 α 0.35 线性降到末端 α 0.05；中心线 1 px α 0.6；`df_result_state` 非 `valid` 中心线虚线、色带减半；`use_policy = exclude` 不画（不变）。长度规则不变（有解画到解算点再延伸 20%，无解 3 km） | `fixOverlay.ts:122-144` |
| 定位椭圆 | 1.5 px 描边 / α 0.12 填充 / 中心十字 | 2.5 px 描边 / α 0.15 填充 / 中心 4 px 实心点带白晕；三种方法三个色不变（`SIT.aoa / tdoa / fusion`） | `fixOverlay.ts:148-177` |
| 目标图标 | 25.6 px（`icon-size 0.8`，96 px 画布 `pixelRatio 3`） | **40 px**（`icon-size 1.25`，或画布改 120 px 保持 1.0）；SVG 加一圈白环（今天的白晕只有 4 px 位移）；发射关闭仍 α 0.45 | `style/situation.ts:39-48, 53-94`；`layers/situation.ts:157-168` |
| 目标选中环 | 无 | 新图层 `cuav-target-ring`：`circle-radius 24 / stroke 2 / 目标色`，`filter` 按 `scene.editor.selection` 的 id（em-demo 的选中环） | `layers/situation.ts` 新增 |
| 目标标签 | 无 | 新图层 `cuav-target-label`：`${id} · ${alt} m`，白晕 1.5，`text-offset [0, 1.8]`；只拉丁字符与数字（`PM_FONT` 只有拉丁字形，`colors.ts:28`） | `layers/situation.ts` 新增 |
| 站点图标 | 24 px（`icon-size 0.75`） | 32 px（`icon-size 1.0`），白环同上 | `layers/situation.ts:150-156` |
| 链路线 | 2.4 px、**从未画出**（§6.2） | 3 px；新图层 `cuav-link-label`（`symbol-placement: line-center`，`text-field` 取 `setLinks` 预格式化的 `label` 如 `1.83 km`，白晕 1.5）；09 §5.2 早已规定「线旁标距离」 | `layers/situation.ts:108-117, 222-232`；归属 `useSituation.ts:134-136` |
| 高度立柱 | 无 | 新源 `cuav-target-pole`：目标位置一个 2 m × 2 m 的小方块，`fill-extrusion-height = alt_m`（离地高，铁律 2；平地假设与 LOS 同一口径）、`fill-extrusion-base 0`、目标色 α 0.6。俯仰视角下就是「从地面升起的柱子」，一眼看出高度；**图标仍在地面点**（MapLibre 符号层没有高程），标签写着高度数 | `layers/situation.ts` 新增 |
| 告警区 | 无 | §4.6 | — |
| 图层菜单 | 山体阴影 / 站点航线目标链路 / 测向线与定位椭圆 / 开发者分色 | 加「告警区」「高度立柱」两项 | `MapToolbar.tsx:62-74`；`SceneView.tsx:38-42` |

### 4.2 色值

新增元素的颜色必须过 D2-4 的同一判据（WCAG 2.1 SC 1.4.11，非文本图形对相邻底色 ≥ 3:1，底色取陆地 / 建筑 / 水面三种，`docs/display-route.md` §4），结果回填 §4 表。候选（实施时实测，过不了就换）：

| 用途 | 候选 | 备注 |
|---|---|---|
| 告警区 `alert` | `#b91c1c`（与 `linkNlos` 同色，红） | 圈线 2 px 虚线 + α 0.08 填充；目标入圈的红环用同色 |
| 告警区 `warning` | `#a16207`（琥珀，比 `bearing` 的 `#b45309` 更黄以免混淆） | 同上 |
| 高度立柱 | `SIT.target`（`#be123c`）α 0.6 | 不新增色 |
| 链路标注 | `SIT.ink` 字 + 白晕 | 文字按 4.5:1 |

### 4.3 告警判定

每个 20 Hz tick（`useSituation.ts:114-172`）对每个实体、每个圆形告警区算一次：`chord_distance(entity, center) ≤ radius_m` 且（无 `alt_max_m` 或 `alt_m ≤ alt_max_m`）。命中即：目标图标换 `cuav-drone-alert` 变体（`makeIcon` 多注册一个红环版本）、列表行与卡片加「告警区内 · <区名>」徽标、时间轴条左端计数加「告警 n」。判定纯前端、纯几何，**不进引擎、不进产品**（告警区是显示语义，不是物理）；将来要进评价（如「进入告警区到首次检出的时延」）再由 C-5 的评价器读场景文件算。

### 4.4 探针与断言

`layers` 含 `cuav-target-ring / cuav-target-label / cuav-target-pole / cuav-link-label / cuav-zone-fill / cuav-zone-line`；`cuav-link-line` 的 `queryRenderedFeatures` ≥ 1；`app.zones[]`；`app.cards[i].inZone`。**`slice2-smoke` 第 92–95 行的七图层断言表必须补上新图层 id**，否则新图层静默失败而断言仍绿。

### 4.5 性能

图层增加五个、源增加三个，都是每 tick `setData` 一次的小 GeoJSON（≤ 8 目标、≤ 64 链路）；Canvas 叠加层的渐变每帧建 ≤ 72 个 `createLinearGradient`，2K 下应仍在 D-048 的「地图空闲时无 > 50 ms 长任务」预算内，实施时用 `app.perf.longTasks` 实测。

### 4.6 告警区 `zones[]`：场景格式的唯一改动

场景文件顶层加**可选**键 `zones`（数组，可空、可缺席）：

```json
"zones": [
  { "id": "z-stadium", "name": "国家体育场告警区", "kind": "alert", "shape": "circle",
    "center": { "lon": 116.3906, "lat": 39.9928 }, "radius_m": 600, "alt_max_m": 300 }
]
```

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `id` | id | 是 | 与站点 / 辐射源同一正则，全文件唯一（`cross_check`） |
| `name` | string | 是 | 显示名 |
| `kind` | `alert` \| `warning` | 是 | 两档；显示色见 §4.2 |
| `shape` | `circle` | 是 | 本期只允许圆；枚举留给多边形（不做） |
| `center` | `{lon, lat}` | 是 | WGS-84 度（铁律 1）；不带高，圈是地面圈 |
| `radius_m` | number > 0 | 是 | 米 |
| `alt_max_m` | number ≥ 0 | 否 | 离地高上限（AGL，铁律 2）；缺席 = 不限高 |

三处**同一提交**（否则 `PUT /api/v1/scenarios/{id}` 对带 `zones` 的文件一律 400，因为服务端把语义校验交给 `cuav_run --scenario-track` 看退出码，`scenarios.ts:184-211`）：

| 处 | 改法 | 先例 |
|---|---|---|
| `docs/scenario-format.md` | §2 表加一行（体例照 `activities` 行）、新增 §「告警区」；§9 待写先登记 | `equipment_model`（D-054）、`clock`（D-053） |
| `docs/schemas/scenario.schema.json` | 顶层 `properties` 加 `zones`（`additionalProperties: false`，`required` **不加**）；`$defs.zone` | 同上 |
| `engine/src/scenario_json.cpp` | 顶层键表 `kTop`（`:321-323`）加 `zones`；新 `parse_zone`（键表 `id / name / kind / shape / center / radius_m / alt_max_m`，枚举显式 if / else 不给缺省，与 `parse_activity` 同法）；`geo::Scenario` 加 `std::vector<Zone> zones`（`scenario.h:137-162`）；`cross_check` 查 id 唯一。**引擎只收下、不解释**，与 `equipment_model` 的注释同一句话 | `scenario_json.cpp:149-155, 187-193` |
| `server/` | **不改**：`PUT` 只查 `scenario_id` 与 `aoi.id`（`scenarios.ts:162-182`） | — |
| `tests/unit/test_scenario_example.py` | schema 校验自然覆盖；规范序列化守卫不变 | — |
| 前端 | `scenarioOps.ts` 加 `zones(doc) / addZone / removeZone / moveZone`；`SceneSelection` 加 `{kind:'zone'; id}`；工具条加「布告警区」（点中心放下，缺省半径 500 m，半径 / 上限高 / 类别在表单改）；对象树加「告警区」组；`deviceFields` 不涉及 | `addSite`（`scenarioOps.ts:70`） |

`demo-03` 加一个告警区作演示（位置在三站包围的居民区内，让 `uav-2` 的航线穿过）；`demo-01` **不加**（§9）。

---

## 5. V-3 全宽时间轴条

### 5.1 内容

| 段 | 内容 | 数据 |
|---|---|---|
| 左端计数 | `目标 3 · 站 3 · 视距 9/9 · 测向 9 · 定位 3 · 告警 1`，全是当前时刻的事实（em-demo 状态栏的做法，只放有生产者的数）；1920 宽下折叠 | `sceneStore` 快照 |
| 轨道 | 0 到 `duration_s`；运行中已到达部分实色、未到达部分淡色；活动标记：`takeoff ▲ / cruise ▶ / hover ◆ / land ▼ / tx_on ● / tx_off ○ / hop ⇅`，悬停出 `t / 事件 / 实体`；将来检测突发画成色带（C-3） | `activities(doc)`（`scenarioOps.ts:30`）；`task.duration_s` |
| 游标 | 可拖；拖动即进入回放；游标处标 `t` | `timeStore` |
| 右端 | `t 41.3 s / 120 s`、▶ / ⏸、倍速 1× / 2× / 5×、「跟随实时」勾选（em-demo `SimControls`） | — |

### 5.2 时基与状态

| 项 | 约定 |
|---|---|
| 时基 | 时间轴的 `t` = 引擎逻辑时间 `t_s`（`task.t_s` 的同一时基，0 到 `duration_s`）。信号页游标 `signal.cursor_t_s` 是相对产品起点 `index.t0_s` 的秒数（`MarkerBar.tsx:22`、`renderer.ts:583` 都做 `t0s + cursor_t_s`），换算 `cursor = t − t0_s` |
| 状态落点 | 新外部小 store `web/src/shell/timeStore.ts`（同 `cursorStore.ts` 的 `useSyncExternalStore` 范式）：`{ t: number \| null, mode: 'live' \| 'replay', playing: boolean, speed: 1 \| 2 \| 5 }`。拖动按指针频率写它，**不进主 reducer**（`signal/cursor` 走主 reducer 会让整棵树重渲染，D-048 ⑩ 的教训） |
| 与地图 | 20 Hz tick 读 `timeStore`：`live` 用「每键最新」（今天的行为），`replay` 用 `sceneStore.snapshotAt(t)` |
| 与信号页 | 拖动中每 100 ms、松手时一次 `dispatch({type:'signal/cursor', t_s: t − t0_s})`；信号页的 `←/→` 与点击改 `signal.cursor_t_s` 时反向写回 `timeStore`（带来源标记防回环）。**2026-09-13 补**：播放推进时同样每 100 ms 墙钟同步一次（原实现只在暂停与到头时同步，播放中信号页整段不动，用户实测发现）；游标跑出回看窗口时窗口平移跟随（跨度不变，前进落在窗底 1/4、后退落在窗顶 1/4，夹在数据范围内），否则任务结束时收口的末尾窗口之外什么都画不出来；防回环改为记「时间轴自己写过的游标值」而不是比当前 t——播放中 t 每帧都在走，按数值比会把播放拉停 |
| 模式切换 | 新任务开始（`task/created` / `task/adopt` 运行中）→ `live`；任何拖动 / 播放 → `replay`；勾「跟随实时」→ `live`。任务结束且索引收尾（D-048 ② 的同一判据）后可播放 |
| 未运行 | 没有任务时也能拖：地图用 `RoutePreview.stateAt(t)` 与 `ActivityPreview`（`preview.ts:142, 188`）预览航迹与发射状态，与 C++ 逐点同式（G-1 对拍过）；此时卡片只有静态字段 |
| 键 | `Space` 播放 / 暂停（场景页与结果页、焦点不在输入框）、`Home` / `End` 跳首尾；`←/→` 仍是信号页的一帧步进（09 §11 不变）；都避开保留键 |

### 5.3 回放缓冲

今天 `sceneStore.replaceFromTrack` 把整段历史折成「每键最后一帧」（`sceneStore.ts:155-170`），`trails` 只留经纬不留时刻，`lastT` 写了没人读——所以地图只能显示最后一刻。改法：

- `sceneStore` 增加按键排序的历史数组：`history.track: Map<id, EntitySample[]>`、`history.links: Map<link_id, LinkSample[]>`、`history.bearings: Map<link_id, BearingSample[]>`、`history.positions: Map<emitter:method, PositionSample[]>`；运行中由 `push*` 累积、结束后由 `replaceFrom*` 整批填（既有两条路径都保留）。
- `snapshotAt(t)`：每键二分取最后一条 `t_s ≤ t`，返回与今天 `get()` 同形的快照；`live` 快照仍是「每键最新」。
- 量级：demo-03 120 s × 10 Hz 上报 × 9 链路 ≈ 1.1 万行链路 + 3600 行实体 + 1.1 万行测向，内存几 MB，二分每 tick ≤ 30 次，够。
- 航迹画到 `t` 为止（回放时航迹不「剧透」未来）。

### 5.4 09 报告的修订

09 §3.2 写的「运行结束后同一位置变为可拖**时间游标**，三视图联动」在顶栏那一格（170 px）放不下活动标记，本方案改为全宽条；顶栏保留读数与「跟随实时」按钮。记为 09 v1.10 修订行。

### 5.5 探针与断言

`app.timeline{t, mode, playing, speed, markers}`；断言：拖到 `t` 后 `app.entities[i].lon/lat` 等于 `track` 端点 `t0=t1=t` 最近一行；`app.signal.cursor_t_s === t − t0_s`（容差 1e-6）；`Space` 后 `app.timeline.t` 单调增且 `playing === true`。

### 5.6 实施记录（2026-09-12，V-3 完成后追加）

| 项 | 方案原文 | 实际落地 | 缘由 |
|---|---|---|---|
| 命令文件名 | `timeStore.ts` 一个文件 | `timeStore.ts`（状态）+ `timelineOps.ts`（命令与换算）+ `Timeline.tsx`（组件） | 命令文件本想叫 `timeline.ts`，macOS 文件系统不分大小写、与 `Timeline.tsx` 撞名，tsc 报 TS1149；Windows 同样不分大小写，故改名而不是靠开发机侥幸 |
| 「现在该画哪一帧」 | 地图 tick 读 `timeStore` 按模式取快照 | 抽成 `web/src/scene/situationView.ts` 的 `currentSituation(doc)` 与 `situationRev()`，地图 tick、卡片栈、探针、时间轴计数**四处共用** | 三处各自取快照会各说各话；探针看到的必须是画面上那一帧，e2e 才比得了 |
| 防回环 | 「带来源标记」 | 数值相等（1e-6）即不再写回，两个方向都是 | 两个时基只差一个常数 `t0_s`，比数值比标记更简单且无状态 |
| 活动标记 | `activities(doc)` 全部 | 只列 `t_s ≤ 时长` 的（`timelineMarkers`） | 任务 70 s 而场景 120 s 时，70 s 之后的活动不在这次运行里，画上去是假的 |
| 左端计数 | 「全是当前时刻的事实」 | 同上，按 4 Hz 刷新（与卡片同节奏） | 每 tick 逐实体算入圈与逐链路数视距，20 Hz 没必要；e2e 里在 250 ms 内连做三步会读到上一格的数，故 e2e 不对计数下时序断言 |
| 「最新一帧」 | — | = `track` 文件最后一行（10 Hz 航迹是 69.9 s，不是任务时长 70 s） | e2e 第一版按任务时长断言而失败，改比文件末行 |
| 播放起点 | — | live 下按播放从 0 起；replay 下从当前 t 起，到头自动停并把游标同步到末尾 | — |
| 探针 | `app.timeline{t, mode, playing, speed, markers}` | 再加 `source ∈ live \| replay \| preview` | 断言要分得清画面来自历史快照还是航迹预览 |
| 断言容差 | 位置「等于 `track` 端点最近一行」 | 拖动目标 30 s，实际 `t` 按指针像素定（1.5 px 容差 0.084 s），位置比的是 `track` 里 `t_s ≤ t` 的最后一行，1e-9 度 | 指针位置到 t 的换算有像素量化 |

---

## 6. 两处顺带发现与修法

### 6.1 采用了最近任务，却没载入它的场景

**现象**（2026-09-12 截图）：刷新后页面采用了最近任务（demo-03 三站三源，`observation_points` 有 `s1__site-1..3`），场景页却载入了 demo-01，地图上只有 site-1 的测向线、另两站不存在，右栏读数全对不上。

**根因**（逐行核实）：

| 环节 | 事实 |
|---|---|
| `bootstrap`（`web/src/shell/actions.ts:12-70`） | 六个作业并发；场景作业（`:31-39`）恒 `loadScenarioInto(store, list[0].scenario_id)`，服务端按 `scenario_id` 升序（`resolve.ts:295-296`），永远是 demo-01；任务作业（`:62-68`）`adoptTask(tasks[0])`，两者互不知情 |
| `adoptTask`（`actions.ts:87-102`） | 只碰 `task / context.taskId / signal / ws`，不碰 `scene.*` |
| `TaskRecord`（服务端 `store.ts:40-72`） | 只有 `scenario_sha256`（`manager.ts:301` 从 `scenario_ref.sha256` 抄），**没有 `scenario_id`**；`prepareDiagram()` 明明算出并返回了 `scenarioId`（`manager.ts:611, 692`），`submit()` 在 `:264` 解构时丢掉了 |
| `useSceneSync`（`ChainView.tsx:66-113`） | 发现链路场景与场景页不一致时**有意不改**场景页（三道闸都关着），于是两边静默各说各话 |

**修法**（V-1 第一步，先修再做卡片）：

1. 服务端：`TaskRecord` 加可选 `scenario_id?: string`，在 `manager.ts:301` 旁从 `prep.scenarioId` 写入；`readTask` 对缺键宽容（`store.ts:171-180`），旧 `task.json` 照常读。**是标识不是路径**，D-037 无虞。`docs/api-versions.md` §3.1a 记字段。
2. 前端：`TaskRecord` 类型加字段；`adoptTask` 在 `task/adopt` 之后，若 `rec.scenario_id` 存在且与 `scene.scenario.id` 不同，`await loadScenarioInto(store, rec.scenario_id, alive)`。
3. `bootstrap` 改序：先取任务并采用（含载场景），**没有任务或旧记录无该字段**才回退 `list[0]`——不再两路并发各载一份。
4. e2e（`slice8-smoke`）：跑完 demo-03 任务后 `Page.reload`，`app.scene.scenarioId === 'demo-03'` 且 `app.context.taskId` 不变。

### 6.2 链路线从未画出

**现象**：截图上没有站到目标的链路线；`slice2-smoke` 只断言 `cuav-link-line` 图层存在与 `app.links` 数据（`slice2-smoke.mjs:92-95, 175`），不断言渲染出要素。

**根因**：`link_id = site_id + "-" + emitter_id`（`geo/src/scenario.cpp:250`，如 `site-1-uav-1`），前端两处按**最后一个** `-` 拆（`useSituation.ts:134-136`：`lastIndexOf('-')`；`ObjectForm.tsx:377-379` 同法），得到 `site-1-uav` 与 `1`，`sites(doc).find` 找不到站，`return` 掉——链路线的 GeoJSON 永远是空集合；`LinkReadout` 的几何回退（未运行时的距离 / 方位）同样取不到站，只有引擎在跑时靠 `st.links.get(linkId)` 的活值才有读数。

**修法**：两处都改为按 `derivedLinks(doc)` 的 `{id, site, emitter}` 精确匹配（站与源 id 在文档里都已知，`${site}-${emitter}` 无歧义）；`slice2-smoke` 加一条「`cuav-link-line` 的 `queryRenderedFeatures` ≥ 1」。**可选的后续**（本期不做）：`links.jsonl` 行与 `link` 事件补 `site_id / emitter_id` 两键（`runner.cpp:208-223`；`LinkFrame` 自 D-053 ⑥ 起已带这两个字段），`JSONL_FILTERS.links` 加两键——那是 API 整洁度的事，D-058 加三键的先例可循。

---

## 7. 后续切片提要（待排期，只记方向与已核实的约束）

### 7.1 ⑨ V-4 站点方位图、V-5 精度场与覆盖场

- **V-4**：选中站点时右栏出两张图：该站对各目标的测向历史极坐标直方（按 σ 加宽）、方位—电平曲线；数据 `GET .../bearings?site_id=`（`routes.ts:64` 已支持该过滤）。真值方位只在 `?dev=1` 叠。
- **V-5**：`cuav_run --field` 新子命令。已核实的约束：`Mode` 枚举加值（`runner.h:30`）与 `parse_args` 的交叉守卫（`runner.cpp:668-684`，`--out` 今天只配 `--run`，`:670` 要放开）；**`geo::Scenario` 里没有观测区域范围**（`scenario.h:142-143` 只有 `aoi_id` 与清单哈希），网格边界要从 `data/scene/<aoi>/manifest.json` 的 `aoi.bbox` 读，`check_aoi_manifest` 只哈希不解析（`scenario_json.cpp:464-480`）；`geo/` 没有 `P_rx / N / SNR`（§3.2 的三式要在子命令里再算一遍）；AOA 的精度量是 `rms_trace_m`（米）、TDOA 是无量纲 GDOP（`locate.cpp:838, 952`），逐格分列不混；网格产品**不塞进 `<op_id>/` 的时间行契约**（`parseIndex` 要 `sample_rate_Hz` 与行数追加计数，`meta.ts:75-109`），作任务级文件 `field.<name>.f32` + 索引家族另定 `{nx, ny, x0, dx, y0, dy, crs, enu_origin}`，端点照 `metrics.json` 那 20 行的样子加（`routes.ts:144-164`）；`fake_engine.mjs` 要认新子命令（`:46` 对未知选项直接失败）；`--field` 代码不得用 `legacy::`（`build-all.sh:27-38` 守卫）。锚点：golden 配置（三站等边、源在质心）的 GDOP 0.942809 在网格对应格上复现；站址处场值等于链路预算。显示：viridis 五档 + 值域不透明度 + 等检测概率包络（`display-route.md` §3 早已冻结）。

### 7.2 ⑩ 信号页：V-6 网格瀑布、V-7 余辉、V-8 标签条、V-9 全带总览

- V-6：同一观测点的 K 站瀑布并排，共用时间轴与色标、量程取并集，几何一致性查索引（09 §7.4 三条规则直接用）；单幅 945 × 1024 抽取实测 26 ms（D-046），三幅并排预算 ≤ 300 ms。
- V-7：迹线模式加「余辉」——最近 N 帧的功率对频点二维直方按出现密度着色，叠最大保持填充与实时线；前端环缓冲即可。
- V-8：随 C-9，瀑布时频框加识别标签与颜色、面板顶部频段标签条（Aaronia X7 1 的版式）。
- V-9：随 ④c（DDC / 信道化到位、S4 变宽带后才有意义）：顶部全带频谱标编号频段框、下方每框一个放大面板；频段计划表来源标 `assumed` / `paper`。

### 7.3 ⑪ V-10 全景一屏、V-11 视觉密度

- V-10：场景页地图下方的信号条（选中站的 S4 瀑布 + 包络，紧凑模式无频谱）。前置是**信号视图实例化重构**：`signalBuffer`（`buffer.ts`）、`viewStore`（`viewStore.ts`）、`signalHooks` 三个模块级单例改为按实例持有，`SignalRenderer` 已经是按实例的（`SignalView.tsx:20-22`）。
- V-11：2K 下图标、线宽、字号整体上一档，数值统一等宽；与 V-2 一起做的部分本期已含。

---

## 8. 格式与接口变更清单（实施时落，本方案只定义）

| 处 | 变更 | 随哪一步 |
|---|---|---|
| `docs/scenario-format.md` + `docs/schemas/scenario.schema.json` | `zones[]`（§4.6）；§9 待写本次先登记 | V-2 |
| `engine/src/scenario_json.cpp`、`geo/include/cuav_geo/scenario.h` | 顶层键表加 `zones`、`parse_zone`、`Scenario::zones`、`cross_check` | V-2 |
| `server/src/tasks/{store,manager}.ts`、`docs/api-versions.md` §3.1a | `task.json.scenario_id` | V-1 |
| `docs/api-versions.md` §3.1b | 顺带补齐端点表（今天只列 `track\|links\|detections`，代码已有 `bearings / positions / features / recognitions / truth`） | V-1 |
| `docs/display-products.md` | 顺带补 `metrics.json` 进目录树、`bearings` 行的 `site_lon / site_lat / site_alt_m`、`positions` 行的 `min_crossing_angle_deg`（文档漂移） | V-1 |
| `docs/display-route.md` §4 | 新色行（告警区两色、立柱、标注）实测对比度后回填；§6.3 待写本次先登记 | V-2 |
| 09 报告 | §3.2、§5.3 引用块 + v1.10 修订行 | V-0（本次） |
| `tests/e2e/slice2-smoke.mjs` | 七图层断言表补新 id；加「链路线渲染要素 ≥ 1」 | V-2 |
| `tests/e2e/slice8-smoke.mjs` | 新建 | V-3 收口 |

---

## 9. 基准变更（铁律 10）

| 基准 | 变不变 | 处置 |
|---|---|---|
| `tests/golden/scenario-track-demo-03.json` | **变**（`demo-03` 加告警区，文件字节变，`scenario_sha256` 随之变） | 单独一步：先加区，用 `scripts/gen_scenario_track_golden.py --scenario … --rate 2` 重生成，**核对 723 个样点逐点相同**（只允许 `scenario_sha256` 变；任何样点变都是发现，禁止调参），再改两份 `tests/regression/diagrams/chain-3x3-{aoa,tdoa}.json` 的 `scenario_ref.sha256`；`slice6-smoke` 里若有哈希字面量同步 |
| `tests/golden/scenario-track-demo-01.json`、`DEFAULT_CHAIN_TEXT` | **不变** | `demo-01` 不加告警区（它是缺省链路的绑定场景，哈希 `72b8e8d0…` 内嵌在 `default.ts` 里） |
| 组件目录黄金基准 | **不变** | 本切片不加组件参数 |
| 产品黄金基准（`product-window.json` 等） | **不变** | 不碰引擎的数值路径 |
| `slice2 / slice4 / slice6` 的数值硬断言 | **不变** | 只加图层与渲染断言 |

---

## 10. 实施步骤与切片

V 线步骤表见 06 §9J（V-0 至 V-11）。切片：

| 切片 | 步骤 | 位置 | 状态 |
|---|---|---|---|
| ⑧ 一屏讲清楚 | V-0 方案 ✔ → V-1 卡片栈（先修 §6 两处缺口）→ V-2 叠加加重（含 `zones[]` 与基准变更单独收口）→ V-3 时间轴 → `slice8-smoke` 收口 | 插在 ⑦ 之后、④b 之前；与 C-3 可并行 | 待用户排期 |
| ⑨ 精度场与方位图 | V-4、V-5 | ④b 之后视演示需要 | 待排期 |
| ⑩ 信号页多站与余辉 | V-6、V-7；V-8 随 C-9、V-9 随 ④c | 随 C 线 | 待排期 |
| ⑪ 全景一屏 | V-10（含信号视图实例化重构）、V-11 | 最后 | 待排期 |

纪律：V-1 的两处缺口**先修**（卡片要对的场景与对的链路，否则卡片读数从第一天就是错的）；V-2 的基准变更**单独一步**收口；每步以 e2e 断言 + WORKLOG 条目收口，不留「等下一条线」的半成品（06 §9E 纪律）。

---

## 11. 验证与验收

### 11.1 `tests/e2e/slice8-smoke.mjs`（约 30 项，真引擎，demo-03）

| 组 | 断言 |
|---|---|
| 卡片 | 不点选即 `app.cards.length === 3`、`app.siteCards.length === 3`；每张卡每站的 `distance_m / azimuth_deg / path_loss_dB` 与 `GET .../links?t0=t&t1=t` 最近一行逐值相等（容差 1e-6）；`rx_dBm` 等于 `tx + Gt + Gr − 路损`（用场景与端点行重算）；`bearing_deg / sigma_deg` 与 `bearings` 端点行相等；点列表行后 `app.scene.selection.kind === 'emitter'` 且地图选中环出现 |
| 叠加 | `layers` 含六个新 id；`cuav-link-line` 渲染要素 ≥ 1；`cuav-target-pole` 要素的 `height` 等于该实体 `alt_m`；`app.zones.length === 1` |
| 告警区 | `uav-2` 穿过告警区期间 `app.cards[1].inZone === 'z-…'`、图标变体为 `cuav-drone-alert`；离开后恢复 |
| 时间轴 | 拖到 `t` 后实体位置等于 `track` 该时刻行；`app.signal.cursor_t_s === t − t0_s`；`Space` 后 `app.timeline.playing === true` 且 `t` 单调增；勾「跟随实时」回 `live` |
| 采用任务 | 跑完后 `Page.reload`，`app.scene.scenarioId === 'demo-03'`、`app.context.taskId` 不变、卡片仍是三张 |
| 色值 | 新色对陆地 / 建筑 / 水面对比度 ≥ 3:1（脚本同 D2-4） |
| 通用 | 页面无外网请求、无未捕获异常、默认 DOM 无 `[data-dev]` |

### 11.2 既有回归

`slice2` 加一条渲染断言后全绿；`slice1 / slice4 / slice6 / scene-smoke` 不改断言全绿；web / server / engine 单测全绿；`build-all.sh` 全过（含路径与 ASCII 检查）；`demo-03` 航迹基准重生成后 723 样点逐点相同。

### 11.3 性能（原型阶段验证值，实施时填）

2K 下地图空闲时无 > 50 ms 长任务；拖动时间轴 20 Hz 不掉帧；`app.perf.longTasks` 为证。

---

## 12. 不做与风险

**不做**：见 §0 末行。另：不做多边形告警区（圆足够演示，多边形留待有需求）、不做告警区随时间生效 / 失效、不做「威胁等级」的三档裁决（只有「在区内 / 不在」两态，因为只有这一个生产者）。

**风险与对策**：

| 风险 | 对策 |
|---|---|
| 拖动时间轴让整棵界面树重渲染 | 外部 store + 节流（§5.2），信号页游标 100 ms 一次 |
| `fill-extrusion` 立柱在平视（pitch 0）下不可见 | 可接受，写明；平视本来就看不出高度 |
| `PM_FONT` 只有拉丁字形，地图标注不能用汉字 | 链路标注只写数字与单位；目标标签只写 `id` 与高度 |
| 告警区三处不同提交会让 `PUT` 全 400 | 同一提交；先跑 `cuav_run --scenario-track` 再改 e2e |
| `derivedLinks` 精确匹配要求「站 id + `-` + 源 id」无歧义 | 站与源 id 在文档里都已知，按已知集合匹配，不猜分隔位置 |
| 回放缓冲在长任务上增长 | 本期任务 ≤ 180 s、上报 ≤ 10 Hz，几万行；超 1e6 行再做分页取窗（B-7 端点本来就按窗取） |
| 新色值过不了 3:1 | 候选表（§4.2）只是起点，实测过不了就换，不降判据 |

---

## 13. 场景页信息分层（V-3b，2026-09-12，D-062）

> 切片 ⑧ 收口后用户看着场景页说：「信息组织有些混乱，信息互相交错，缺乏层次，成为了信息的堆砌。请重新梳理和设计。」对着截图核实成立（§13.1）。本节是重新梳理后的设计与落地记录：**D-061 的数据与表达元素一件不减，只改组织与权重**。用户认可 §13.2 的三条原则与 §13.3 的版式后拍板实施。

### 13.1 诊断（2026-09-12 截图，2560 × 1353，demo-03 跑完 70 s）

| 区域 | 当时放的内容 | 条数 | 问题 |
|---|---|---|---|
| 左栏 | 数据包（范围、建筑数、高度中位、最高、底图日期、署名）+ 场景头 + 站点 3 + 辐射源 3 + 航线 14 行（每个航点带高度速度）+ 活动 8 行 + 告警区 1 + 派生链路 9 行 | 约 50 行，一屏半 | 五种性质的东西平铺在一棵树里：静态数据包事实、可编辑的场景对象、对象内部的明细、时间事件、派生关系 |
| 右栏 | 目标表 3 行 + 展开的目标卡（身份 6 项、运动 4 项、各站 3 × 6 项 + 3 个方位盘、定位 5 项）+ 两张折叠卡 + 站点卡 3 张（各带采样率、噪声系数、天线增益） | 约 45 个读数 | 配置项与观测量混在一张卡里；三张卡可同时展开；站点卡重复左栏的站点与框图页的参数 |
| 地图 | 3 站 + 3 目标标签 + 3 航迹 + 9 链路线 + 9 距离标注 + 9 测向色带 + 3 椭圆 + 3 立柱 + 1 告警圈 | 约 50 个图元 | 全部同权重同时画，九条测向色带叠成中间一大片褐色，看不出谁是主角 |
| 顶栏 + 时间轴 | 顶栏 `t 70.0 s / 70.0 s ×5.3`，时间轴条右端又一个 `t 70.0 s / 70.0 s` | — | 同一读数两次 |

同一事实的重复次数：uav-1 的高度 4 处（目标表、卡头、运动节、地图标签）；链路 3 处（树、地图、卡片）；站点 3 处（树、站点卡、每张目标卡的各站行）；活动 2 处（树、时间轴标记）。所有文字 12 px 同一字重，没有层级。

### 13.2 三条组织原则

1. **按「用户在问什么」分栏。** 左栏回答「场景是怎么配的」，只放可编辑的对象和它的表单；右栏回答「现在发生了什么」，只放观测量；时间轴回答「什么时候」。一栏一种性质。
2. **总览 → 列表 → 详情三级，详情一次只看一个。** 地图与时间轴计数是总览，右栏目标列表是第二级，焦点卡是第三级；任何时候只有一个焦点对象展开。
3. **一个事实在屏上只出现一次；明细按需展开。** 航点、活动、身份参数折叠进所属对象，不在树上平铺。

### 13.3 版式

```
顶栏  项目 › 框图 › 任务 ● 状态   ▶ 运行                场景 框图 结果 数据    （场景页与结果页不再有 t / 时长）
┌──────────────┬──────────────────────────────────────┬──────────────────┐
│ 左栏 · 场景   │ 地图                                  │ 右栏 · 态势       │
│ 场景名 · 时长  │ [图层▾][平视][测量]      [编辑场景][保存] │ 焦点卡（一张）     │
│ ◉ 站点 3      │                                       │  名称 · 告警徽标   │
│ ✈ 辐射源 3    │  焦点目标：全套叠加                      │  高度·速度·航向·发射│
│ ○ 告警区 1    │  其他目标：图标 + 航迹 + 细链路线          │  各站表 3 行       │
│ ───────────   │                                       │  定位一行 · 位置一行│
│ 选中对象的表单  │                                       │  身份 ▸（折叠）     │
│  航线 N 航点 ▸ │                                       │ 目标列表 3 行      │
│  活动 N ▸     │                                       │ 站点 3 行          │
│ 数据包 ▸      │                                       │                  │
└──────────────┴──────────────────────────────────────┴──────────────────┘
时间轴条  计数 | 轨道 | 读数 · 播放 · 跟随实时
抽屉  日志 告警 数据流 资源（数据包明细在这里）
```

### 13.4 逐栏改法

**左栏（配置）**

| 原来 | 改成 | 理由 |
|---|---|---|
| 树里平铺 14 行航点 | 收进辐射源表单的「航线 · N 航点 ▸」，默认折叠；点一行即选中该航点，航点表单带「返回辐射源」 | 航点是目标的内部明细，不是场景一级对象 |
| 树里平铺 8 行活动 | 收进辐射源表单的「活动 · N ▸」；时间轴上的标记已是它的主显示 | 时间事件的自然位置是时间轴 |
| 树里 9 行派生链路 | 删掉；`SceneSelection` 去掉 `link` | 链路是算出来的关系，地图与焦点卡已在显示 |
| 数据包信息块常驻 6 行 | 底部一行「数据包 北京亚运村周围 · 47,582 栋 ▸」，默认折叠；明细同一份放抽屉「资源」页签 | 静态背景事实，看一次就够 |
| 选中对象的表单叠在右栏卡片之上 | 表单接在左栏树的下方，带「取消选择」 | 编辑属于配置，与树同栏；右栏不再被表单顶掉 |

改完左栏一级只剩 7 行加一个表单，不用滚动。

**右栏（观察）**

| 原来 | 改成 | 理由 |
|---|---|---|
| 目标表 + 多张可同时展开的卡 | 顶部一张**焦点卡**，下面紧凑的目标列表，一行一目标；焦点 = 选中的辐射源，没选中取第一个 | 详情一次一个 |
| 卡头、运动节、目标表三处重复高度速度 | 焦点卡第二行一句话：`132 m · 20.0 m/s · 航向 55.7° · 发射 开 · 2.4405 GHz` | 一个事实一处 |
| 身份节 6 项常驻 | 折叠为「身份 ▸」，在卡底 | 机型、带宽、功率、极化是配置，不随时间变 |
| 各站三段各 6 项加三个方位盘 | 一张表 `站 · 距离 · 电平 dBm · 信噪比 dB`，每站第二行 `测向 ± 2σ · DF 档`，视距用行首色点，单位写在列头 | 同构的数据用表，一眼能对比三站 |
| 站点卡带采样率、噪声系数、天线增益 | 站点一行一站：`亚运村北站 · DF-Q3 · locked · 链路 3` | 这三个参数是配置，框图页与左栏表单才是它们的家 |

**地图（分焦点与背景）**

| 图元 | 焦点目标 | 其他目标 |
|---|---|---|
| 图标 + 标签 | 40 px，标签 `id · 高度` | 40 px，只标 id |
| 航迹 | 2.4 px | 1.2 px，更淡 |
| 链路线 + 距离标注 | 3 px 带标注 | 1 px 不标注（`cuav-link-label` 加 `filter`） |
| 测向色带 | 画 | 不画 |
| 定位椭圆 | 画 | 只画中心点 |
| 高度立柱 | 画 | 画 |

图层菜单加「全部目标叠加」，需要看全貌时打开。3 × 3 场景的测向色带从 9 条降到 3 条。

**顶栏与工具条**

| 原来 | 改成 |
|---|---|
| 顶栏 `t / 时长 × 实时因子` | 场景页与结果页不显示（时间轴条已有）；框图页与数据页没有时间轴，读数保留 |
| 观察工具与编辑工具混在一条 | 观察组「图层 / 平视 / 测量」常驻；编辑组「选择 / 布站 / 布目标 / 航点 / 布告警区 / 撤销 / 重做」收在「编辑场景」开关后面，拖动站点与航点也只在编辑模式；「保存场景」常驻 |

**字级三档**：标题 14 px 加粗（`--fs-title`）、读数 13 px 等宽（`--fs-read`）、标签 12 px 灰（`--fs-label`）。

### 13.5 落地记录（2026-09-12）

| 件 | 落点 |
|---|---|
| 左栏 | `web/src/scene/LeftColumn.tsx`（新）、`ObjectTree.tsx`（三组）、`ObjectForm.tsx`（`RouteSection` / `ActivitySection` 折叠段，链路读数与测向定位段删除）、`ScenePackagePanel.tsx`（`ScenePackageDetails` 共用）、`shell/Drawer.tsx` 资源页签 |
| 右栏 | `web/src/scene/cards/Cards.tsx` 的 `SituationPanel`（焦点卡 / 目标列表 / 站点行）；`RightColumn.tsx` 只剩它 |
| 焦点 | `web/src/scene/focus.ts` 的 `focusTargetId`，右栏、地图 tick、探针 `app.scene.focus` 三处共用 |
| 地图 | `useSituation.ts`（`focusEmitter` / `allOverlays`）、`layers/situation.ts`（`TargetPoint.focus`、`LinkLine.focus`、航迹 focus、链路标注 `filter`）、`layers/fixOverlay.ts`（`focusId`） |
| 工具条与壳 | `MapToolbar.tsx`（两组 + `data-act="edit-mode"` + 图层「全部目标叠加」）、`SceneView.tsx`（编辑模式、拖动门禁）、`shell/RunGroup.tsx`（有时间轴的页不显示读数） |
| 样式 | `app.css`：三档字级、`.focus-card` / `.site-table` / `.tlist` / `.site-row` / `.form-details`，旧卡片样式删除 |

实测（macOS，原型阶段验证值）：`slice8-smoke` 40 → **42 项**、`slice2-smoke` 28 → **29 项**，六套 e2e **272 项**全绿；web 204 项；服务与引擎未动。截图核对（2560 × 1440）：左栏 7 行加表单一屏放下；地图上只有焦点目标的三条测向色带与椭圆。

**两处与拍板时的方案不同**：① 焦点卡各站表去掉「真值方位」与「路损」两列——320 px 的右栏放不下七列（第一版测向列被截在「272」，去掉两列后仍被截，最后测向另起一行）；测向列已是同一方向的量测，真值方位只是答案；电平 = 发射 + 天线 − 路损，路损留在探针里；② 「保存场景」不收进编辑组——脏了随时要能存。

**顺带修一处探针的假错**：`window.__probe()` 里 `map.areTilesLoaded()` 在 MapLibre 重建某个数据源瓦片表的一瞬会抛 `TypeError`（`Object.values(sourceCache._tiles)` 碰到 undefined）；那是它的过渡态不是地图错误，探针改为那一拍按「未加载完」报、不记 `errors`，并把异常的调用栈前几帧记进 `errors`（此前只记消息，查不出来源）。

---

## 附录 A. 参考素材清单

`ref/Aaronia AG/`（27 份，只读，**不入库**——第三方宣传素材；建议 `.gitignore`）：`Aaronia AG C2 1/2/3.jpg`（指挥中心地图 + 视频 / 干扰盘 + 八向频谱 / 块图编辑器）、`Aaronia AG AARTOS X7 1/2/3.jpg`（多频段总览与网格瀑布 / 指挥中心目标卡 / 块图）、`Aaronia AG DF2.1.jpg`（暗色路网 + 频谱 + 方向功率谱）、`DF2.2.jpg`（时差定位面板）、`DF4 1.jpg`（山地 + 方位玫瑰 + 方位电平曲线）、`DF4 2.jpg`（余辉频谱与参数栏）、`DF9 1.jpg`（时差定位面板续）、`HAWK anti drone 1/2.jpg`（时差仿真器精度场 / 指挥中心 + 瀑布）、`X2 MOBILE.jpg`、`x2 MOBILE 1.jpg`、`x2 MOBILE TABLET ANALYZER.jpg`（便携整机）、`Robin3.jpg`（雷达三维禁飞区，不借）、`Sector_Jammer.jpg`、`Jammer_video_2.webm`（干扰，不借）、`X2 MOBILE.webm`（目标卡与告警圈动画）、`AARTOS_Unimog.jpg`、`Zetros_Shelter_*.png`、`Sprinter_*.png`（车载，不借）、`08-08-_2019_11-12-07-Kopie.jpg`、`addons-header_web_high.jpg`、`parallax_df4_01.png`（硬件外观，不借）。

em-demo（`C-UAV Model Demo/em-demo/src/components/`）：`App.tsx`、`TargetList.tsx`、`TargetDetail.tsx`、`SignalPanel.tsx`、`SimControls.tsx`、`StatusBar.tsx`、`Toolbar.tsx`、`EffectPanel.tsx`、`CoverageControl.tsx`、`MapView.tsx`。EM-C-UAV 工程 `UI/`：`detail_image_31f4c7 / 6c7d9e / a1ac39 .png`、`detail_image_e0c90c.jpeg`（三屏指挥中心）。现状截图：本会话临时目录（不入库）。

## 附录 B. 依据条款索引

| 条款 | 用在 |
|---|---|
| 04 §3.1、§8.1、§8.2 | 场景配置、主界面、运行与回放 |
| 05 §5.2 | `SceneParamFrame` 是卡片读数的来源 |
| 09 §2、§3.2、§4.4、§5、§7、§11、§12、§13 | 版式、时间游标、三栏、场景页、结果页、键鼠、文案、四态 |
| 10 §5.7 | 检测识别页签（V-8 的落点） |
| 11 §5 | 测向线与椭圆的既有画法、读数面板 |
| 12 §5 | 传播档位卡片（不动） |
| D-017、D-039、D-042b、D-043 | 浅色底图；界面只摆事实不解释 |
| D-046、D-047、D-048 | 视窗抽取、dBm、信号视图约定 |
| D-049 ⑤ ⑩ | 路损不含功率与增益；高频量不进主 store |
| D-053、D-054 | 测向 / 定位产品；场景逐实体带出设备参数；`equipment_model` 先例 |
| D-058 | 加键先例；`included_loss_terms` |
| D-060 | 不做界面版的自由画布 |
| 铁律 1、2、6、8、10、14、15 | 坐标与方位、AGL、离线、溯源、基准、标记留数据层、不静默 |
