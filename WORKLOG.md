# 工作过程日志（804 C-UAV 无人机电子信号侦察仿真系统）

本文件记录每个任务节点的实际过程、核实到的事实和由此产生的待办。写作要求：用完整句子，
不使用未在本文件内定义过的内部简称，每个结论都写清楚是"实测得到"还是"引自文档"。

新条目追加在文件末尾，按日期正序排列。文件顶部的「当前状态快照」在每次追加后同步更新。

---

## 当前状态快照（更新于 2026-09-18，**切片 ⑤ 遮挡接入已走完 D3-0 至 D3-7**（决策 D-074，07 号报告 1062 行）：两项从 2026-09-02 挂到今天的未决事项拍死——遮挡计算落点 = 浏览器 TypeScript 复算并与 C++ 黄金基准对拍、坐标基座 = vendored GeographicLib。**D3-1** 取 GeographicLib 2.5.2（上游自 2.6 起要 C++17，本项目 C++14）；**D3-2** 换基座收口，推翻报告两处预测（产品 `.f32` 不是逐字节不变，根因是纯单音谱的数值相消底；判据「逐点相对差 ≤ 1e-12」超差，顶到的是航向角，换判据形状而不是放宽），下游结论一个未变；**D3-3** 遮挡三件移植进 `geo/`，148 例黄金基准全过；**D3-4** 建筑几何进引擎（懒加载只在 E3，`--validate` 不受影响），又推翻报告三处（栋数 47546 → **47662**；加载 210–242 ms 过了 200 ms 的线，因为那条线漏算了读盘与 sha256 核对；加载点按 §7.4 放在 `init()`）；**D3-5** 遮挡接进链路预算——`line_of_sight = !blocked`、刀口损耗单程 ×1 只进 `extra_loss_dB`、补闸三闸四，**07 §1.5 那张调研表逐行复现且出自保存的典型链路**（起飞 211.1 m / 36.98 dB、末端带内信噪比 33.7 → 1.4 dB、180 s 全程 89.3% 判非视距），**E1 / E2 零回归、零基准变更**；**D3-6 浏览器侧复算已落地**——148 例两侧各守一遍（29 例判非视距，同数），并**核实到 148 例不够用**（12 栋合成楼加 legacy 投影，测不到解析规则、严格站心平面帧与四万多栋楼的桶网格），新增 `tests/golden/occlusion-aoi.json` 真实建筑集五条射线两侧同守（最差 **1.3e-12**，判据 1e-9），建筑几何懒加载（**懒的是数据不是代码**：模块只占产物 835 B，15.9 MB 首屏不取）。**D3-7 界面已放开**——框图页 E3 去置灰并列出「自由空间 + 建筑遮挡」、闸三闸四进前端镜像、新增第 14 项检查「E3 有观测区域建筑几何」；场景页工具条加**视距探测**（点地图任一点对焦点站算视距与刀口损耗，**楼 62 m / 143 m 外：楼前视距、楼后 37.1 dB**，切片 ⑤ 的「点选建筑两侧一致」一次点击就验完）；**链路线第一次画成红的**（`slice2` 跑一条 3 秒的 E3 链，demo-01 头 7.2 秒全程非视距）。顺带改掉 D3-5 欠的一句话：`prop_level` 的参数描述还写着「待 D3，本版本收到即报错」，而它就是下拉的悬停提示——**代码改了、面向用户的那句话没改**，这条线上第二次踩到。下一步 **D3-8（MSVC / GCC 构建）**。引擎 **311** doctest + 17 ctest、web **263**、server 127、七套 e2e **368** 项、标准算例 20 项、E3 回归 7 项全绿。此前 **C 线十二步全完（C-11 / D-073）**：04 §15.2 标准算例从保存的典型链路跑通，十二项完成十项）

**所处阶段**：P0「需求冻结与数据摸底」，进行中。**整体处于原型系统阶段**（D-028）：
公开数据集只用于验证技术途径、软件架构与功能实现，**其量化结论一律不作交付指标**，
甲方数据到货后按同样脚本重跑修订。

**代码进度**（更新于 2026-09-06）：工程骨架（06 §2 S-1 至 S-7）之上，**桥接线 B 已全部完成**（B-1 至 B-7），
**切片 ①「一条链到频谱」与切片 ②「场景里飞起来」都已在浏览器里收口**（U-1 + U-3；G-0 至 G-5）。
**切片 ②（2026-09-06）**：08 报告定稿（v1.1；2026-09-07 改写为 v2.1，1326 行）；`geo/` 从占位变成真库（WGS-84 闭式换算、航线闭式求值、
自由空间链路预算）；引擎新增四个场景运行时组件（目录 8 → 12）与场景注入、SHA-256、场景 JSON 装载；
`cuav_run --scenario-track` 与航迹黄金基准；服务端场景读写端点；前端七个态势图层与场景编辑器。
两条硬断言达成：三时刻位置与黄金航迹差 **0**（容差 1e-6 度）、两时刻带内功率差与几何路损差偏差 **0.074 dB**（容差 0.5）。
引擎 `engine/`：组件注册表与目录、框图 JSON 装载器、观察者与观测点产品、`SpectrumAnalyzer`（与 Python 与 MATLAB `pwelch` 三方互证）、
`cuav_run` 可执行（B-1 至 B-4、P1-4a）、**场景运行时**（G-2 / G-3：`ScenarioSource`、`SceneEmitterSource`、`SceneBoundChannel`、`FreeSpaceChannel`），114 项单测 + 8 项 ctest 全绿。
`geo/`：`ClosedFormWgs84`（LLA↔ECEF↔ENU，Bowring 闭式）、`Route`（段时轴 + 二分定位闭式插值）、链路预算、场景结构与帧发生器，零第三方依赖。服务 `server/`：任务管理器（提交→同步校验→排队→子进程→终态，取消、
退出收尾、重启对账，B-5）、WebSocket 推送 / 补取（`/ws` 订阅、`GET .../events?since`、`product_row` 二进制帧，B-6）与**视窗抽取端点**
（`GET /api/v1/results/...` 按时间窗、频段、像素与统计量归约产品文件，B-7）与**场景读写端点**（`GET/PUT /api/v1/scenarios/{id}`，语义校验交引擎，G-4），110 项测试全绿，运行时依赖只有 `ws`。
前端 `web/`：2.5D 离线底图（D1）；**U-1 三视图壳完成（2026-09-06）**：hash 路由、`useReducer` store、REST / WS 客户端（退避重连、`since` 补取、二进制行帧）、顶栏面包屑与两正交徽标、运行组、底部抽屉日志与状态条、框图 JSON 页、结果页信号页头（纵轴 `dBm`）、探针 `app`；**U-3 信号视图完成（2026-09-06，D-048）**：频谱在上瀑布在下共用频率轴、最新行在顶、包络竖条与色带、右栏仪表；跟随模式画环里最近 H 行（WS 二进制帧，离屏画布位移追加），缩放 / 平移 / 游标转回看（B-7 抽取），任务结束且索引收尾后自动转回看重取；M1 / M2 marker 与读数、时间游标、`M` / `Delete` / `←→` 键、导出 PNG / CSV；前端归约与服务端同式并守同一黄金基准；**切片 ② 的场景视图完成（2026-09-06，D-049）**：七个态势图层（站点、规划航线、航点、目标图标随航向旋转、航迹抽稀、链路线按视距着色）、
高频量走独立小 store 且地图 20 Hz 定频取值、结束后从 `track` / `links` 端点整批回放、场景编辑器（四工具、对象树含派生链路组、
表单带 SI 前缀输入、撤销重做、保存回传落盘哈希）、浏览器航迹预览与 C++ 同式并逐点对拍；态势色值在浅色底图重标定（D2-4 销项）。
95 项单测、三条 e2e（14 + 39 + 27 项）全绿。**dBm 纵轴链已打通（D-047）**：引擎内部功率单位 mW，两个公开数据集的标定常数已估出并刷进 4714 份清单（DroneRFb-DIR −1.6 dBm model、DroneRFa −50.0 dBm paper，原型阶段验证值）；界面只标 dBm，来源只在 `?dev=1`。IQ 散点无数据源（观测点不产 `iq` 产品，D-040 ③）未做。`tools/` 的 IQ 转换与质检、`algos/reference/` 的
能量检测参考实现与两个标定脚本 49 项单测全绿。`scripts/build-all.sh`（web、server、engine、geo、路径与 ASCII 检查）全过。
**辐射源模板线（T，D-045，2026-09-05）**：用户问「现有数据集能否作为将来模拟特定无人机信号的基础」，核实为 04 §7.2 的既定路线、代码一件未做；
定三种模式（首选特征等效再生）、供给边界（数据集未记录的量都是场景配置项）与回放模式的信噪比上限；规范草案 `docs/emitter-template.md`，
步骤表 06 §9F。**本期只记录不实现**，T-0 探针待排期。
**典型链路线（C，D-051，2026-09-07）**：用户指示框图不再自由组合、先给固定的典型框架并给出检测识别与评价。10 号方案定框图页默认为典型链路视图（04 §5.1 九环节固定、三种信号源模式、只改参数），U-2 自由画布降为高级模式，框图格式只加 `template_ref`；拍板 D-050 三问；新增特征提取、模板匹配识别、评价器与真值口径；步骤表 06 §9G（C-0 至 C-11），切片 ④ 重排为 ④a–④d。**C-1 与 C-2 已完成（2026-09-07）**：C-1 格式与接口——端口类型 6 → 7（`RecognitionList`）、`PortSpec.optional`、框图 `template_ref`、`SceneParamFrame` 七个新字段、`BlockMeta.clip_count`、场景 `emission.polarization`、错误码 `template`。C-2 三件手写组件——`AntennaGain`、`ReceiverFrontEnd`、`AdcQuantizer` 落地，两个既有组件加参且缺省保旧行为；**组件目录 12 → 15，04 §8.1 的六类首次全部有组件**（D-050 指出的天线与接收机缺位就此销项）。口径等价实测：`demo-01` 上「辐射源按发射功率出电平 + 两个天线 + 纯路损信道」与切片 ② 的「折在一处」给出同一接收电平，差 < 0.001 dB。**C-6 与 C-7 也已完成（2026-09-07），切片 ④a 收口**：框图落盘端点与结果端点；框图页默认改为九环节固定链，自由画布降为 `#/diagram/canvas` 的高级模式。浏览器里的两条硬断言：S1 实测 −51.60 dBm 对链路预算 −51.15 dBm（差 0.45 dB），S2 底噪实测 −119.37 dBm 对解析 −119.35 dBm（差 0.02 dB）。**2026-09-08 补掉一处用户实测发现的导航缺口（D-052）**：画布工具条加「回到典型链路」、解不开时先说明再确认且不静默丢改动、`Alt+2` 与顶栏「框图」回框图页默认形态；实测纠正一条判据——**删**节点仍解得回链，只有**加**了模板之外的节点才解不开。**同日第二轮反馈也已修**：用户「S0–S5 勾上和不勾看不出区别」——取证后确认链路是通的（S1 −52.61 dBm 对 S2 −32.61 dBm），问题在界面：观测点做成了脱离链条的一行复选框且名称只在悬停提示里（偏离 10 §5.1 版式图）、勾选后没说要重新运行、结果页换观测点只在可收起的左栏。三处已改：观测点一行改写全名（`S0 辐射源输出`…`S5 子信道`，链条结构不动）、工具条给「重新运行后生效」、结果页中栏加观测点页签且默认选主产品 S4；中栏版式一并收口——链条区改为占满高度的面板并纵向居中，九个环节按 3 × 3 排、行距 56 px、卡片保持 176 × 128 而连接件拉伸铺满列宽，换行时行首不再有孤立箭头。S2 / S3 / S4 读数相同不是缺陷，是 DDC 与信道化未实现（M-2 / M-3）的真实后果。全量回归：引擎 142 项 doctest + 8 项 ctest、服务 117 项、web 150 项、五套端到端 14 + 39 + 27 + 30 + 50 = 160 项全绿。下一步切片 ④b：C-3 检测升级。
**C-5 完成（2026-09-14，D-067）**：`Evaluator`（绑站、第一个接受部分输入的节点、真值 = 活动开关 ∩ 波形门控 ∩ 频段 / 清单类别 / 无）、`truth.jsonl` + `metrics.json`（`sites[]` 按站分节）、`task.json.metrics_summary`、框图页评价子环节与结果页右栏评价卡、`evaluate.py` 逐值对拍；跨层一致性算例 ① 典型链路实例（突发源）首跑即过：解析 Pd 0.7164 对蒙特卡洛 0.7026，差 0.0138。引擎 249 + 13、服务 127、web 216、六套 e2e 312 项全绿。下一步 C-9。

**定位线（L，D-053，2026-09-09）**：用户问「场景中辐射源可能有多个；单站要做识别与测向；多站可做交叉定位或时差定位——框图里如何体现、如何设置」。11 号方案（v1.0，807 行）定：多源按源分支、接收天线后叠加成一路（新组件 `Superposition`）；多站 = 典型链路加站点维度、尾部加「测向」与「多站定位」两槽位（9 → 11）；测向与到达时间是 **M2 效应模型**，取真值的合法边界正式声明在报告 §1.3（每行 `truth_consumed`）；新增端口 `BearingReport / ToaReport / PositionReport` 与固定可选口；`sites[].clock` 与时统质量；emcore 的 AOA / TDOA / 测向与四份 golden 移植到 `geo/` 且求解器改平面坐标接口。新增 L 线（06 §9H）与切片 ⑥，插在 ④a 与 ④b 之间。**L-0 至 L-8 全部完成，三片全收口**：`Superposition`、`DirectionFinder`、`ToaEstimator`、`MultiSiteLocator` 四件组件（目录 15 → 19），`demo-03` 三站三源场景，emcore 的 `geo` / `locate_aoa` / `locate_tdoa` 移植（255 例 golden 相对误差 ≤ 1e-9，求解器改平面坐标接口、legacy 常数只守 golden 并加了构建守卫），地图上的测向线与定位椭圆。**硬断言**：同频双源合成功率对预算和差 −0.001 dB、逐站三源叠加 ≤ 0.013 dB；AOA 锚点 17.4532925 / 34.906585 / 20.5495 m 与 TDOA GDOP 0.942809 逐位命中；测向经验 σ 差 0.80%、1800 行归一化误差方差 1.0225；AOA 有效档 2σ 覆盖 87.0%（理论 86.5%）；时差中位误差 7.95 m、扣掉钟差偏差后 85.3%。**三条实测发现**（emcore 的几何分级看不见近简并配置、未标定的固定钟差是协方差表达不了的系统偏差、两种时差加权的方向与计划假设相反）见 D-053 追加段与模型卡 `models/locate/README.md`。L-9 定位评价随 C-5 / C-9。

**传播线（R，D-058，2026-09-10）**：用户指示传播信道要反映所处传播环境、地理空间与建筑物分布带来的变数，并在框图里从简单到细致地选择考虑哪些因素。逐行核实到**两个信道变体今天在物理上是同一个自由空间模型**（`geo/src/link_budget.cpp` 里 `line_of_sight` 与 `extra_loss_dB` 都是字面常量，全仓库零遮挡零绕射零多径零阴影实现），47582 栋建筑没进过任何链路计算，测向误差预算的非视距多径项因此从未生效。12 号方案（v1.0）定：档位 E1（= 今天的行为，缺省）/ E2（地面双径**或**城市经验二选一 + 统计阴影 + 大气降雨，各可单独开关）/ E3（建筑遮挡与刀口绕射，待 D3，枚举保留但界面置灰、引擎报错）；**算在 `geo/` 帧生产端、配在 `ch` 卡片**（槽位表 `proxy` 代理 `ScenarioSource` 的参数）；防重复计损 = 至多一个替代型主模型 + 界面单选 + 运行时报错两道闸；链路报告附 `included_loss_terms`；帧结构一个字段不加。顺带改正 CLAUDE.md 三处「`ChannelPathSet` 首期即启用」的错误表述（实为零生产者零消费者，与 08 §15 一致）。步骤表 06 §9I（R-0 至 R-7），切片 ⑦。**R-0 至 R-7 已于同日全部完成**：五个效应落地、天气两式移植 emcore（64 例 golden rel ≤ 1e-9）、双径 A1–A5 / 阴影 C1–C5 / 城市经验 B1–B4 全过；槽位表 `proxy` 让传播参数配在卡片上、算在帧生产端；E3 界面置灰 + 引擎报错两头都做。**E1 缺省档的产品字节与上一版提交逐字节相同**（拿上一版建出的 `cuav_run` 直接对拍），slice2 的 0.074 dB 与 slice6 的 0.002 / −0.013 / 0.001 dB 一个数不动。模型卡 `models/channel/README.md`。**同日续（D-059）**：按用户实测撤掉传播信道的「自由空间（定参）」变体——框图页上能跑的配置一定有场景，手填固定距离在这里永远是错的选择；组件本身保留供标准算例与自由画布用，黄金基准零变化。引擎 208 + 8、服务 124、web 195、六套 e2e 261 项全绿。

**实体设备参数配置（D-054，2026-09-09）**：用户指示框图页上方加「选择无人机」「选择侦测站」两个下拉、
按实体切换参数，使其成为**场景内实体电子设备参数配置的主界面**，传播信道单独设置且共用。四条拍板：
下拉只切换配置焦点不改运行范围；引入设备型号字段（同型号共用一套、可逐实体单独设置）；场景视图对象表单保留并共用同一套字段定义；
改完自动保存场景。落地：`SlotDef` 分出 `owner`（实例化维度与参数归属维度分离），`SlotConfig` 加 `byEntity` 两层持久、
型号作编辑范围不进状态（`parseChain` 拿不到场景，写进去会破坏往返逐字节），场景加可选 `equipment_model`，
`tx_ant.gain_dBi` / `rx_ant.gain_dBi` / `rx_fe.nf_dB` 改由场景逐实体带出以消掉双真理源，场景保存抽到 `shell/actions.ts` 并去抖自动触发。
**两处实测发现**：① 场景里逐源的天线增益一直被静默忽略——`demo-03` 的 `uav-3` 场景写 0 dBi 而链路强制 2 dBi，
链路预算高估 2 dB，`slice6` 的预算式也硬编码成 2 因而两边错得一样；修正后每站 S1 对预算和差 **0.002 / −0.013 / 0.001 dB**，
AOA 有效档 2σ 覆盖 87.0% → **82.5%**（带内）；② `demo-03` 从未按 D-049 ⑧ 的规范形式落盘（7497 → 7473 字节），
第一次自动保存即打掉三处场景哈希，已重生成航迹基准（723 样点逐点相同）、更新夹具哈希并加了一道守卫。
全量：引擎 174 + 8、服务 119、web 175、场景 schema 9、六套 e2e 234 项全绿；组件目录与产品黄金基准零变化。

**可视化线（V，D-061，2026-09-12）**：用户看过 Aaronia AARTOS 素材（`ref/Aaronia AG/`）与 em-demo 版式后要求借鉴态势与信号可视化。13 号方案（v1.0）定：**数据不新造、只改表达**——场景页右栏常驻目标列表 + 卡片栈（每站一行距离 / 电平 / 信噪比 / 测向 ± 2σ，只摆事实）、地图叠加加重（测向渐隐色带、40 px 目标图标与选中环、链路距离标注、高度立柱、圆形告警区 `zones[]` 作为威胁色唯一生产者）、全宽时间轴条（`.app` 第四行，时基 = 引擎逻辑时间，联动地图回放与信号游标）；两处顺带发现——刷新后采用了最近任务却载入缺省场景（`task.json` 无 `scenario_id`），链路线因 `link_id` 按最后一个 `-` 拆分从未画出。新增 V 线（06 §9J）与切片 ⑧「一屏讲清楚」（V-1 → V-2 → V-3，插在 ⑦ 之后、④b 之前），⑨–⑪ 待排期。**同日用户说「继续」，切片 ⑧ 全部实现并收口**：V-1a 两处缺口、V-1 卡片栈、V-2a 告警区进格式（基准变更单独收口：`demo-03` 航迹基准 723 样点逐点相同、只哈希变，`demo-01` 与 `DEFAULT_CHAIN_TEXT` 不动）、V-2 叠加加重、V-3 全宽时间轴条 + 回放缓冲 + 信号游标联动；`slice8-smoke` 40 项，六套 e2e 269 项。**随后用户指出场景页「信息堆砌」，同日按 D-062 重新分层**（左栏 = 配置、右栏 = 观察、地图分焦点与背景、编辑工具收进开关），`slice8` 42 项、六套 e2e 272 项。
**数据进度**：已获得两个公开数据集，均已完成摸底核实，但都尚未转换格式、尚未入库、
许可条款尚未确认。

| 数据集 | 本地规模 | 关键特征（均为实测核实） | 状态 |
|---|---|---|---|
| DroneRFb-DIR | 64 GB / 4690 片（全集） | 单通道 80 兆样点每秒；固定 10 米；摆放不动；出版方已划分训练测试集；62 片纯背景；正交性近乎完美 | 已摸底 |
| DroneRFa | 21 GB / 22 片（全集约 560 GB） | 双通道 100 兆样点每秒；三档距离区间；飞行中 5–15 米每秒；24 型 7 品牌；正交支路幅度不平衡 0.54–0.95 | 已摸底并完成路损验证 |

DroneRFa 上得到的量化结果（DA-6 已完成重算，见同日第四条日志）：**2.4 吉赫兹城市户外链路的
路损指数约为 2**——`T0010` 六片给出 n = 1.93（逐对组合极差 0.12、标准差 0.05；三档最小二乘
1.95），`T0011` 三片给出 1.69，两机型宽基线一致，验证了路损与机型无关。**但主导不确定度是
距离区间标注本身**：真实距离比只能确定在 2.0 至 7.5 之间，同一实测降幅对应 n = 1.29 至 3.75，
比片间抖动大一个数量级以上，所以只能冻结粗档结论。片间重复性 0.3 至 1.3 分贝；底噪 −24.3
分贝经三条独立路径互相验证；5.8 吉赫兹通道在 7 个文件上突发帧占比全部为 0.0%，该频段放弃。

**两个数据集已全部转换入库**：DroneRFb-DIR 4690 个产物 70 GB、DroneRFa 24 个产物 14 GB，
4714 份清单逐一核对，回读对拍失败 0、质量状态异常 0（全部 `degraded`，原因一致：采集参数
来自论文而非设备记录）。格式规范 `docs/iq-format.md` 第 3、4 节已定稿。
**验收子集已冻结**：DroneRFb-DIR 2487 片（出版方测试集减去 26 片已用过的），含 6 架从未在
训练集出现的个体 1580 片；**DroneRFa 没有可用验收集**，本地 22 片全部已用于拟合，只能从
后续补下的片里划并当天冻结。清单 `data/iq/measured/holdout.manifest.json`。

**显示子线进度**（更新于 2026-09-04）：观测区域定为北京亚运村周围（用户指定，D-021），范围经实测比较后
由用户定为 20 × 20 km（`116.288,39.900,116.522,40.080`，D-024），区内 50201 栋建筑（按 id 去重）；底图与 DEM 按用户"128 GB
没有问题"的指示整份作为共享资产使用（D-022），并已**整份拷入本项目自持**（约 145 GB，D-023），
`data/basemap/{planet.pmtiles, dem/}` 是真实文件而非软链，本项目运行时不再依赖 Airports 工程的路径。
副本与来源逐字节一致（底图全文件 sha256、DEM 索引哈希两项均已核对）。
**建筑数据的两项实测**（D-025）：z15 要素带稳定 id，跨瓦片合并按 id 做；但瓦片 `height` 字段把真实标注与
planetiler 按层数推导的值混在一起，区内 76.9% 无高度、19.4% 是推导值、真实标注上界仅 3.7%，干净的来源标注
只能靠建库阶段拉 Overpass 原始标签。
**D0 场景数据包已完成**（2026-09-04，七步全过）：`buildings.geojson` 47582 栋、15909168 字节；高度来源
`osm:height` 1.74% / `osm:levels` 19.59% / `est:area` 78.55%；与原始 OSM 按编号精确对齐 99.72%、面积中位差
+0.008%，与底图自带建筑层逐像素交并比 0.993 至 0.999。数据包入口 `data/scene/beijing-yayuncun/manifest.json`。
**几何可用，高度不可用于验收指标**（近八成是估算值）。下一步是显示子线 D1。用 Airports 查看器在无头浏览器里实测：
包内底图经 Range 服务可加载并渲染出亚运村一带的道路、建筑拉伸与标签，切片、整份来源文件、包内副本三者的
画布像素逐位相同。

**已经拿到的、只有真实数据才能给出的结论**（详见 2026-09-04 两条日志）：真实城市 2.4 GHz
背景下，按白噪声假设设的门限虚警率超标 232 倍；同样虚警率要求下，能检出的最弱目标要比
白噪声环境强 23.6 至 38.9 分贝，频段选择本身值 15 分贝；门限在一半背景上标定换到另一半就
失效 7 倍，故静态门限不成立。

**最大的未决变量**：甲方（用户方）自己的实测 IQ 数据仍未提供，八项摸底问题只在公开数据集
上回答过。两个公开数据集都**没有绝对功率标定**（缺馈线损耗与一次标定常数），因此分贝毫瓦
类验证一律做不了；也都**没有视距与非视距标签、没有建筑几何与高度**，因此跨层一致性算例
② 建筑遮挡与 ③ 双径仍然不可做。这两块只能由甲方自采数据或补采计划补上。

**显示子线 D1 已完成**（2026-09-04）：前端可离线渲染观测区域的底图、山体阴影与建筑三维拉伸。
底图样式与 Airports 逐层等价（黄金基准守着），并排比对容差 8 内 99.83%。应用服务实现了 PMTiles 依赖的
HTTP Range（30 项测试），端到端 14 项断言全过。下一步是 D2 态势图层。

**路线图已于 2026-09-04 修订为 v1.1**（见同日「路线图修订」条目）：用户定后续重点为人机交互界面、
真实地理环境下的场景与目标活动生成、B/S 框图模型、波形与频谱可视化、后端计算，并追问 MATLAB 的
优势是否用上（此前没有：参考实现全为 Python，P1-8 未排期）。06 备忘录新增 B 桥接、U 界面、G 场景、
M MATLAB 四条步骤线与五个纵向切片；P1-5、P1-6 撤销，P1-8 由 M-2 兑现，D2 并入 G-5。四份规范骨架
已落地（框图、场景、显示产品、组件目录，字段已冻结）。

**当前位置与下一步**（2026-09-06）：切片 ① 与切片 ② 都已收口。切片 ② 的画面：载入亚运村的 demo 场景，
地图上有站点、规划航线与航点；提交场景绑定的框图后，无人机图标沿航线动、拖出航迹、链路线随之伸缩，
瀑布上的谱线随距离渐暗；任务结束后从产品文件整批回放。地图上可以点选布站、拖动航点、测量、撤销重做、保存场景。
下一步是切片 ③「拖出框图」（09 报告后半的框图画布交互 → U-2 React Flow 画布 → U-3 包络与散点余项）。
公开数据集这条线已全部做完；**跨层一致性算例 ① 在两批独立数据上都通过**（检出率偏差 0.0016–0.0050，容差 0.05–0.10），引擎侧整链复现待切片 ④。待用户出面的事项：甲方实测数据。

---

## 日志条目

### 2026-09-03 · 公开数据集 DroneRFb-DIR 摸底

#### 1. 本次做了什么

用户把一个公开数据集放进了项目根目录，路径是 `Datasets1-DroneRFb-DIR/`，并询问在拿到
甲方实测数据之前能否先用这个数据集推进项目。

本次工作是对该数据集做实际核实（不是照抄它自带的说明文件），具体包括：遍历目录统计文件
数量与体积、读取标签文件统计各类样本分布、用 HDF5 库打开 `.mat` 文件查看内部结构、抽取
若干文件计算数值统计量与功率谱、以及从随附论文中提取采集参数。

核实所用环境：macOS，`uv run --with h5py --with numpy python`。数据集本身没有被修改。

#### 2. 这个数据集是什么（以下均为实测核实结果）

数据集全名 DroneRFb-DIR，出自浙江大学，论文发表于《电子与信息学报》2025 年第 47 卷第 3 期，
论文数字对象标识符（DOI）为 `10.11999/JEIT240804`，数据托管于 SciDB 平台，数据集编号
`84cf9101e739402784b1396783881202`。其用途是"非合作无人机个体识别"，即不仅要判断有没有
无人机、是什么型号，还要区分同型号的不同架次个体。

| 核实项 | 实测结果 |
|---|---|
| 总容量 | 64 GB |
| 文件数量 | 4690 个，其中训练集 2177 个、测试集 2513 个 |
| 文件格式 | MATLAB v7.3 格式，其实体是 HDF5，内部用 gzip 压缩 |
| 文件内部结构 | 根节点下只有两个数据集，名为 `I` 和 `Q`，各自形状为 1 行 4000000 列，数据类型为 32 位浮点数 |
| 元数据 | **完全没有**。文件内既无采样率、无中心频率、无时间戳，也无接收机增益等任何属性 |
| 数值的本质 | 抽查 7 个文件（训练集 4 个、测试集 3 个），全部样点都是 2 的负 15 次方的整数倍，且绝对值不超过 32767。这说明底层是 16 位整数，只是以浮点形式存放 |
| 信号电平 | 峰值在满量程以下 15.4 分贝到 30.2 分贝之间，有效值在满量程以下 39.1 分贝到 47.2 分贝之间。没有出现削顶（饱和） |
| 正交支路质量 | 同相支路与正交支路的标准差之比在 1.0000 到 1.0014 之间，互相关系数接近 0。说明正交性良好，没有明显的镜像干扰或直流泄漏 |
| 突发结构 | 以 51.2 微秒为一帧统计，无人机信号文件中约有 10% 的帧功率高于中位数 6 分贝以上；纯背景文件中几乎没有高于中位数 10 分贝的帧 |

以下参数**只写在论文里，数据文件本身不携带**，引用时必须标明来源是论文而非设备记录：

| 参数 | 论文给出的值 |
|---|---|
| 接收设备 | USRP-2955 软件无线电设备 |
| 采样率 | 80 兆样点每秒 |
| 中心频率 | 2.44 吉赫兹（覆盖 2.4 到 2.48 吉赫兹） |
| 每个片段时长 | 50 毫秒（与 4000000 个样点、80 兆样点每秒相互吻合） |
| 收发距离 | 固定 10 米 |
| 采集姿态 | 无人机与其遥控器放在同一位置，**摆放不动，没有飞行**；接收机位置固定 |
| 视距与非视距的差别 | 论文称两者信噪比大致相差 20 分贝 |
| 无人机信号构成 | 图传信号带宽 20 兆赫兹、频率位置接近 2.48 吉赫兹；飞控信号逐帧跳频，但带宽与持续时长固定 |
| 片段之间的时间关系 | 论文明确说明：每个片段内部的采样点时间连续，但**片段之间时间不连续** |

样本的分类情况：共 6 种机型，每种 3 架个体，外加 1 类纯背景（现场没有无人机开机）。
机型对应关系为 A 代表大疆 Mavic 3 Pro，C 代表 Mini 2 SE，D 代表 Mini 4 Pro，E 代表 Mini 3，
F 代表 Air 3，G 代表 Air 2S，B 代表背景。训练集只含每种机型的第 1、2 架（共 12 架）加背景；
测试集含全部 18 架加背景，也就是多出 A3、C3、D3、E3、F3、G3 这 6 架**从未在训练集中出现过
的个体**。纯背景片段合计 62 个（训练集 37 个、测试集 25 个）。

#### 3. 这个数据集接在本项目仿真链条的哪一个位置

本项目的仿真链条按先后顺序可以展开成十个环节：

```
① 波形模板与业务活动   ② 复基带波形生成   ③ 时变复信道（把传播参数施加到波形上）
                                    ↓
④ 样本级噪声与干扰注入   ⑤ 接收机采样链（噪声系数、增益、自动增益控制、量化）   ⑥ 模数转换与数字下变频
                                    ↓
                    ★ S4 观测点：数字下变频之后、信道化之前的宽带复基带 IQ ★
                                    ↓
⑦ 信道化   ⑧ IQ 观测量提取（功率谱、瀑布图、能量检测、突发统计、参数估计）
                                    ↓
⑨ 信号级裁决（检测、特征提取、识别）   ⑩ 批量蒙特卡洛统计（检测概率曲线、混淆矩阵）
```

DroneRFb-DIR 的每个文件，本身就是 ★ 这一点的产物：USRP 设备已经把 2.44 吉赫兹的射频信号
下变频到复基带，带宽 80 兆赫兹，且没有做信道化。这与 CLAUDE.md 铁律第 4 条定义的主产品
（数字下变频之后、信道化之前的宽带复基带 IQ）完全一致。

因此它的接入位置是确定的：**从 ★ 往右，即第 ⑦ 到第 ⑩ 环节**。

反过来说，它无法用于验证第 ① 到第 ⑥ 环节。原因不是数据质量不好，而是逻辑上不成立：
这批数据本身就是①到⑥的运算结果，而产生它的输入参数（真实距离、各项损耗、发射功率、
接收增益）恰恰是这个数据集没有记录的。用一个结果去验证产生它的过程，前提是知道过程的
输入，这里不满足。第 ① 到 ⑥ 环节仍然只能用合成数据验证，即自行设定参数、生成 IQ、
再检查输出是否符合解析公式。

#### 4. 它能支撑什么，不能支撑什么

**能支撑的（因为只需要"IQ 本身长什么样"就够）**

- 第 ⑦ 到 ⑩ 环节的算法验证，包括频谱与瀑布图、能量检测、突发统计、参数估计、识别。
- 用真实文件尺寸对读取与流水线做压力测试。单个文件 16 兆字节、对应 50 毫秒，总量 64 GB，
  这种规模能压出分块大小、内存映射、背压、按视窗抽取降采样等工程问题；用几百千字节的
  合成文件永远压不出来。
- 提供真实的城市电磁背景。原计划的算例用的是高斯白噪声，而真实的 2.4 吉赫兹城市环境里
  充满 WiFi 与蓝牙的突发信号，是非平稳、非高斯的。这 62 个纯背景片段可以直接用作背景，
  叠加我们自己合成的目标信号——目标是自己造的，所以信噪比等"标准答案"完全可控，而背景
  是真的。这个组合恰好绕开了该数据集"上游参数未知"的短板。
- 作为 IQ 文件格式规范的第一个真实样本。它还提供了一条反面教材：采集参数必须外挂在元数据
  里，否则一旦与论文分离，数据就无法解释。

**不能支撑的（因为需要知道"这段 IQ 是怎么来的"）**

| 做不了的事 | 具体原因 |
|---|---|
| 任何关于绝对功率的验证 | 文件里的数字是模数转换器的量化码，不是分贝毫瓦。要换算成分贝毫瓦，需要天线增益、馈线损耗、接收机增益设置，再加一次标定，这四样一个都没有。因此只能比较"这片比那片强 6 分贝"，无法给出"接收功率是负 70 分贝毫瓦"。而链路预算校验（发射功率减去路径损耗减去遮挡损耗等于接收功率）要求等式两边都是绝对功率，所以做不了 |
| 任何关于几何的验证 | 全部 4690 个片段是同一个几何：距离固定 10 米、接收机固定、无人机摆放不动。数据集里根本不存在"几何变化导致信号变化"这一维度的信息，而这正是本系统要模拟的核心因果关系。因此建筑遮挡的移植验证（里程碑 D3、D4）用不上它 |
| 双径传播的验证 | 没有高度信息，没有距离变化，双径特征无从体现 |
| 多普勒与运动估计 | 无人机没有飞行，不存在多普勒 |
| 跨片段的时间分析 | 片段之间时间不连续且没有绝对时间戳。单个片段内部做的事（频谱、突发检测、跳频跟踪）都成立；跨片段做的事（长时间活动规律、多片拼接、行为模式）都不成立 |
| 覆盖甲方关心的目标集 | 只有 2.4 吉赫兹一个频段，机型全部是大疆消费级无人机，覆盖不到 5.8 吉赫兹与其他机型 |
| P0 阶段的冻结事项 | 观测区域范围、甲方接收机的实际参数、首批频段与机型清单，一个都没有解决 |

**一处需要修正的说法**：遮挡相关的验证并非"完全用不上"。论文给出了"视距与非视距信噪比
相差约 20 分贝"这一个数字，可以用作量级合理性检查——我们的建筑遮挡模型在类似的城市短距
场景下算出的附加损耗，应当落在十几到二十几分贝这个量级。若算出 3 分贝或 60 分贝，说明
模型或参数有问题。这属于"体检"而非"校准"，但成本几乎为零，值得做。

#### 5. 命名与文档中的陷阱（务必记住，极易搞反）

| 陷阱 | 说明 |
|---|---|
| `IN` 与 `OUT` 的含义与直觉相反 | 文件名中的 `IN` 代表**视距**采集，`OUT` 代表**非视距**采集。不是室内与室外 |
| `S0` / `S1` / `S2` 不是场景编号 | 它是**采集时间序号**。文件名格式为「机型与个体编号 + 视距标志 + 采集时间序号 + 片段序号」，例如 `A1_IN_S0_slice_2.mat` |
| 论文中的一处笔误 | 论文第 2.2 节称数据"存储为单精度浮点型(2字节)"。单精度浮点是 4 字节，实测也确认文件中是 32 位浮点。推测"2 字节"指的是原始的 16 位整数 |
| 测试集的答案是公开的 | `test_labels.txt` 第一列是带标签的原始文件名，第二列是测试集编号 0 到 2512。也就是说测试集的真值可以直接从这个文件读出 |
| 目录名与数据集名不符 | 解压出来的目录叫 `twin_droneRFa`，与数据集名 DroneRFb-DIR 对不上。来源不明，入库时应改名 |

#### 6. 五项可立即开工的任务（不依赖甲方数据）

**第 1 项：打通读取与显示的完整通路**
读取一个 `.mat` 文件，转换为复数 16 位整数交织格式，送入引擎做分块流读取，输出功率谱与
瀑布图，再由前端按视窗抽取后显示。
验收标准：屏幕上能辨认出论文图 3 标注的几样东西——接近 2.48 吉赫兹处一块 20 兆赫兹宽的
图传信号、随机跳频的窄带飞控信号、WiFi 的 20 兆赫兹驼峰、蓝牙的窄条。能看出来即说明
读取、解码、抽取、显示这条通路是通的。

**第 2 项：核对观测量提取的正确性**
论文给出了两个可核对的量：图传信号带宽 20 兆赫兹且频率接近 2.48 吉赫兹；飞控信号逐帧
跳频但带宽与持续时长固定。用我们的参数估计组件跑出中心频率、带宽、突发时长，与这两个量
比对。
验收标准：估计出的图传带宽与 20 兆赫兹的偏差在可接受范围内，频率位置落在 2.48 吉赫兹
附近。这是难得的**外部真值**——不是我们自己造的标准答案，而是第三方标注的。对不上就说明
我们的估计器有问题。

**第 3 项：在真实城市背景下标定虚警率（建议最先做）**
用 62 个纯背景片段测量能量检测器的虚警率。
预期结果：按高斯白噪声假设推导出的检测门限，在真实城市背景上虚警率会明显超标，因为真实
背景中的 WiFi 与蓝牙是突发的、非平稳的、非高斯的。
验收标准：给出超标的具体倍数，并给出修正后的门限。这个数字只有真实数据能得到，是"合成
数据会给出乐观结论"这一判断的第一个硬证据。

**第 4 项：跨层一致性算例的真实背景版本**
原计划的算例是"自由空间加高斯白噪声，比对功能级解析检测概率公式与 IQ 级蒙特卡洛结果"。
升级为"真实背景加合成目标"：目标信号由我们自己生成（信噪比、带宽、突发时长等参数完全
可控），背景用真实数据。
验收标准：产出真实背景下的检测概率曲线，与白噪声版本的曲线并排比较，给出两者的差值。

**第 5 项：性能压力测试**
80 兆样点每秒属于 CLAUDE.md 中"仅做文件兼容与短窗"的那一档（常用档是 5 到 50 兆样点每秒）。
用它测量端到端吞吐量与延迟。
验收标准：给出兆字节每秒的吞吐数字与端到端延迟，据此判断准实时模式的能力上限。

依赖顺序：第 1 项是第 2、3、4 项的前提；第 3 项与第 4 项之间，第 3 项在前（先有可信的门限，
再谈检测概率曲线）；第 5 项可与其他任务并行。

#### 7. 入库之前必须完成的事项

1. **格式转换**。放到 `data/iq/measured/dronerfb-dir/`，转换为复数 16 位整数交织格式，
   并配一份符合 SigMF 规范的外挂元数据。转换脚本中必须加断言，检查每个样点确实是 2 的
   负 15 次方的整数倍（本次抽查 7 个文件全部通过），转换完成后逐位回读对拍，证明无损。
2. **元数据标明来源**。采样率、中心频率、设备型号这些参数来自论文而不是数据文件，
   元数据中必须标注来源为论文，不能伪装成设备自带记录。时间连续性标志要写明"片段内部
   连续、片段之间不连续"。
3. **冻结验收子集**。直接采用出版方划分好的测试集 2513 个片段作为验收子集，另外把
   A3、C3、D3、E3、F3、G3 这 6 架训练集中从未出现的个体单独标出，记录文件哈希。
   出版方已划分好的好处是：不必自己划分，也就不会在划分时不小心把同一架次的数据同时
   放进训练集和验收集。
4. **文件名改为纯 ASCII**。目录名 `twin_droneRFa` 与随附的中文文件名 PDF 都要改。
5. **确认许可条款**（唯一可能影响能否使用的硬性风险）。SciDB 的数据集页面是动态渲染的，
   本次未能取到许可文本。内部研发使用并引用论文 DOI 应无问题，但**能否随交付系统一起
   分发必须先确认**。

#### 8. 本条目产生的待办

- [ ] 确认 DroneRFb-DIR 的许可条款，特别是能否随交付系统分发（阻塞项，需人工访问 SciDB 页面）
- [ ] 编写格式转换脚本，含无损断言与回读对拍
- [ ] 冻结验收子集并记录哈希
- [ ] 在 CLAUDE.md 的「资源清单 · 只读边界 · 复用边界」表中增加本数据集一行
- [ ] 在 CLAUDE.md 的里程碑表中更新 P0 状态，并考虑是否需要新增一条决策记录
- [ ] 甲方实测数据与 8 项数据摸底问题仍未回复，继续跟进

---

### 2026-09-03 · 工程骨架 S-1 至 S-7

#### 1. 本次做了什么

按 06 备忘录 §2 的步骤表建立工程骨架。S-3（建立本文件）在上一个任务节点已完成，本次做了
其余六步。每一步都做到「能实际跑起来」，不是只建空目录。

#### 2. 产物

| 步骤 | 产物 | 结果 |
|---|---|---|
| S-1 | 目录树，全部纯 ASCII 无空格 | 共 40 个目录 |
| S-2 | `.gitignore` | 用 `git check-ignore` 逐条验证过忽略与放行规则 |
| S-4 | `docs/` 八份文件 | `README`、`iq-format`、`api-versions`、`scene-package`、`display-route`、`concept-model-layers`、`concept-model-extensions`、`decisions/README`，合计 442 行 |
| S-5 | `tests/` 五级目录与回归清单 | `tests/regression/README.md` 列全 04 §15.2 十二项与跨层一致性四项，并逐项标注公开数据集能否支撑 |
| S-6 | 五个子工程 | `web`、`server`（npm）、`engine`、`geo`（CMake）、`scene`（uv） |
| S-7 | 两个脚本 | `scripts/check-ascii.sh`、`scripts/build-all.sh` |
| 附加 | 每个顶层目录一份 README | 写明该目录的用途、适用的铁律、已冻结项与未决项 |

纳入版本控制的共 75 个文件、692 KB。已用 `git diff --cached` 逐项确认：64 GB 的公开数据集、
1.5 GB 的只读参考快照、`node_modules`、`dist`、`build`、`.DS_Store` 全部未进入暂存区。

#### 3. 实测验证结果（不是"应该能跑"，是实际跑过）

| 子工程 | 命令 | 结果 |
|---|---|---|
| `web` | `npm run build` | 通过。类型检查加打包，产物 190.63 KB（gzip 后 60.20 KB），耗时 49 毫秒 |
| `server` | `npm run build` | 通过 |
| `server` | 启动后请求 `/api/v1/health` | 返回 200 与 `{"status":"ok",...}`；未知路径返回 404 |
| `engine` | `cmake --build` 加 `ctest` | 通过，1 个测试全过 |
| `geo` | `cmake --build` 加 `ctest` | 通过，1 个测试全过 |
| 全部 | `scripts/build-all.sh` | 全绿 |
| ASCII | `scripts/check-ascii.sh` | `web/dist` 与 `data/scene` 均合规 |

#### 4. 核实过的引用（工作协议 §13.2 要求）

**Airports 的行号**（`/Users/zhiyu/CC/Airports/index.html`，共 1883 行）：CLAUDE.md 引用的
七处全部存在——`protomapsStyle()` 在第 810 行，山体阴影在 1218 行，`addBldg3d()` 在 1296 行，
`newMap()` 在 1375 行，`probePmBldg()` 在 1281 行，`window.__probe` 在 1769 行，
`localglyphs://` 在 726 与 729 行。已抄录进 `docs/display-route.md`。

**依赖版本**：全部在 npm 源上核实存在后才写入，且写死精确版本号不用范围前缀。

| 包 | 锁定版本 | 上游最新 | 说明 |
|---|---|---|---|
| `maplibre-gl` | 5.24.0 | 6.7.0 | 决策 D-008 有意锁定，与 em-demo 保持同版本 |
| `pmtiles` | 4.4.1 | 4.5.0 | 同上 |
| `react` / `react-dom` | 19.2.8 | 19.2.8 | — |
| `vite` | 8.2.2 | 8.2.2 | — |
| `typescript` | 6.0.3 | 7.0.2 | 与 em-demo 的 6.0 对齐。上游已到 7.x，属低风险可提升项 |

#### 5. 遇到的问题与处理

**TypeScript 6 下 `server` 的 Node 全局类型不自动生效。** 装了 `@types/node` 之后，
`tsc` 仍报找不到 `process`、`console` 与 `node:http`。在 `tsconfig.json` 里显式写
`"types": ["node"]` 后通过。记下来是因为 `engine`、`geo` 之外的任何新 Node 工程都会踩同一个坑。

#### 6. 需要注意的风险

1. **开发机的 Node 是 25.8.2，不是长期支持版本**（主版本号为奇数）。两个 `package-lock.json`
   都是在这个版本下生成的。交付平台基线冻结 Node 长期支持版本之后，必须重新生成并重新验证。
2. **全部构建只在 macOS 上验证过**。按决策 D-015 与 D-016，macOS 结果**不作为验收依据**。
   MSVC 与 GCC 的首次构建验证分别安排在 D3-4 与 P2 阶段。
3. **git 仓库还没有第一次提交**。75 个文件目前只是暂存状态。

#### 7. 偏离步骤表之处（已登记进 06 备忘录 §14）

`.gitignore` 在步骤表列出的排除项之外，增加排除了 `Datasets1-DroneRFb-DIR/`。原因是该
公开数据集 64 GB，在步骤表编写时尚未放入项目根目录。

#### 8. 本条目产生的待办

- [ ] git 第一次提交（等待确认后执行）
- [ ] Node 长期支持版本冻结后重新生成两个 `package-lock.json` 并重新验证
- [ ] `server/src/range.ts` 实现 HTTP Range 文件服务（D1 阶段的前置条件）
- [ ] `docs/` 各份规范中标记「待写」的章节，随对应实现阶段补齐

---

### 2026-09-03 · 按公开数据集入手的事实修订 06 备忘录

#### 1. 起因

06 备忘录全篇有一个隐含前提：**手上没有任何真实 IQ**。因此凡涉及实测的步骤，措辞一律是
「数据到位后」，位置一律排在最后。2026-09-03 入手 DroneRFb-DIR 之后，这个前提只对了一半：
甲方数据仍未提供，但已有一批可解析的真实 S4 观测点 IQ。

**改的是顺序，不是技术路线。** 阶段 0 的退出条件与 P1 的退出条件均未变动。

#### 2. 改了哪九处

| # | 位置 | 改动 |
|---|---|---|
| 1 | §0.5 | 实测数据行的阻塞对象由 P1-9 改指 P1-9b |
| 2 | §0.5 | 新增「DroneRFb-DIR 许可条款」一行；表后新增**防线一**：不得因为有了公开数据集就放松索取甲方数据 |
| 3 | §3 | 阶段 0 的「实测数据」行：摸底工具 `tools/iq_survey` 解除阻塞，可先在公开数据集上开发验证，状态改 `[~]` |
| 4 | §9 P1-1 | 拆成两批定稿：容器、样点编码与元数据结构现在可定；功率与标定口径单列，仍被甲方数据阻塞。状态改 `[~]` |
| 5 | §9 P1-9 | 拆成 P1-9a（公开数据集回放，不阻塞）与 P1-9b（甲方数据回放，仍等数据） |
| 6 | §11.2 | 标题由「入库当天必做」改为「任何 IQ 数据集入库当天必做」，并补充质检结果必须落到四态语义、元数据缺失一律判 `degraded` |
| 7 | §11.3 | 参与时点拆成两条并行数据线：公开数据集线（现在起）与甲方数据线（待到货）；新增 §11.4 |
| 8 | §12 | 「实测等效」层注明公开数据集对跨层一致性四项算例的支撑范围 |
| 9 | §14 | 补一条修正记录，说明本次调整的原因与范围 |

#### 3. 新增的 §11.4

新增七个编号步骤 DS-1 至 DS-7，使数据集接入进入「每完成一步更新状态」的机制：
许可确认、转换脚本、外挂元数据、冻结验收子集、摸底工具跑通、虚警率标定、真实背景加合成
目标。除 DS-1 需要用户确认许可外，其余六步均不需要用户输入。

同时写入两项内容：

**转换体积与子集策略。** 全部 4690 片转成复 int16 交织需 75 GB（每片 16 MB），加原始
64 GB 共 139 GB。先转最小可用子集约 200 片、约 3.2 GB（62 片全部背景 + 训练集每个个体
5 片 + 测试集抽样 80 片），管线跑通后再按需扩大。

**防线二：该数据集不得与任何地理场景绑定展示。** 它没有位置信息，摆到地图上任一地点都是
编造，违反铁律 14。附带说明：它采自杭州，而临时观测区域用的是西安，本来也对不上。

#### 4. 顺手修掉的一处原有缺陷

§9 的 P1-1 行内 `|Δf| + B/2 + 保护带 < Fs/2` 中的竖线未转义，会把该表格行切成 6 列而不是
4 列，导致表格渲染错位。改成 `\|Δf\|` 后全篇表格列数一致（已用脚本逐表校验）。

这是备忘录原文就有的问题，不是本次改动引入的。

#### 5. 一个已核实的判断依据

把 DroneRFb-DIR 对着 04 §10.6 的八项质检逐项核对：八项中六项可跑通，两项（第 1 项元数据
一致性、第 7 项元数据必填项）必然失败，因为该数据集文件内没有任何元数据。

这正是把 DS-5 排在前面的理由：**它是比「完美数据集」更好的第一个测试用例**，因为它强制
`tools/iq_survey` 走「缺元数据判 degraded 而不是崩溃、也不是拿默认值顶上」这条路径，
恰好是铁律 15 要求的四态语义。若第一个测试数据集是完整的，这条路径就永远没被验证过。

#### 6. 本条目产生的待办

- [ ] DS-1 许可确认（需用户出面，阻塞该数据集能否进入交付物）
- [ ] 其余待办并入 06 备忘录 §11.4 的 DS 步骤表，不在此重复

---

### 2026-09-03 · 公开数据集 DroneRFa 摸底、路损验证与补数清单

#### 1. 本次做了什么

用户在同一天下午把第二个公开数据集的一部分放进项目根目录，路径是 `Datasets2-DroneRFa/`，
并询问它与前一个数据集的差异、对本项目是否更有价值。此后用户分四批补充文件，每批之后
都做一次实测核实与"还缺什么"的判断。

核实所用环境：macOS，`uv run --quiet --with h5py --with numpy python`，论文用 `pdftotext -layout`
抽取。数据集本身没有被修改（例外见第 7 节，有两片文件被误删）。

本条目是这一路工作的补记。工作发生在 2026-09-03 中午到下午，当时未按 D-006 的纪律当场落盘，
会话末尾因服务端 529 错误中断。本条目的所有数字均从当次会话记录中逐条取回，不是凭印象重写。

#### 2. 这个数据集是什么

数据集全名 DroneRFa，与前一个数据集 DroneRFb-DIR 出自同一课题组（浙江大学）的前期工作，
论文数字对象标识符为 `10.11999/JEIT230570`，数据托管于 SciDB 平台，数据集编号
`34f0a91e8a544904998b8fdc44477380`。用途是低空无人机的检测与型号识别（不是个体识别）。

以下是**实测核实结果**：

| 核实项 | 实测结果 |
|---|---|
| 本地体积与片数 | 21 GB，22 片（用户分四批补充，另有两片一度被误删后重下，见第 7 节） |
| 文件格式 | MATLAB v7.3 格式，其实体是 HDF5，内部用 gzip 压缩 |
| 文件内部结构 | 根节点下有四个数据集，名为 `RF0_I`、`RF0_Q`、`RF1_I`、`RF1_Q`，各自形状为 1 行 1.5 亿列，数据类型为 64 位浮点数 |
| 元数据 | **完全没有**，与前一个数据集相同 |
| 数值的本质 | 量化步长为 2 的负 15 次方，说明 64 位浮点底下仍是 16 位整数，转成复 int16 交织**无损** |
| 信号电平 | 峰值在满量程以下 34 到 43 分贝。折算有效位数约 9 到 10 位，比前一个数据集（满量程以下 15 到 30 分贝、约 11 到 12 位）窄，这是收发距离从 10 米拉到 40 至 150 米的直接后果 |
| 正交支路质量 | **同相支路与正交支路的标准差之比为 0.54 到 0.95**（三个文件、五个抽样位置均存在），互相关接近 0，直流偏置很小。详见第 5 节 |
| 单文件解压体积 | 约 4.8 GB；转成复 int16 交织后约 1.2 GB（双通道全转）或约 600 兆字节（只转 RF0） |

以下参数**只写在论文里，数据文件本身不携带**，引用时必须标明来源是论文而非设备记录：

| 参数 | 论文给出的值 |
|---|---|
| 接收设备 | USRP-2955 软件无线电设备 |
| 采样率 | 100 兆样点每秒 |
| 通道 | **双通道同时采集**，中心频率 2440 兆赫兹与 5800 兆赫兹 |
| 每片时长 | 1.5 秒（与每通道 1.5 亿点、100 兆样点每秒相互吻合） |
| 接收增益 | **50 分贝** |
| 接收天线 | **全向垂直，增益 3 dBi** |
| 收发距离 | **三档区间标注**：`D00` 为 20 至 40 米、`D01` 为 40 至 80 米、`D10` 为 80 至 150 米 |
| 采集姿态 | **无人机在飞行中**，速度 5 至 15 米每秒 |
| 时间连续性 | 每 1000 万点（即 0.1 秒）内连续，块之间有连续性损伤（采—存交替模式） |
| 类别 | 24 种机型加背景，覆盖 7 个品牌；其中只有 **9 类是城市户外运动采集**并带有效距离标注，其余 15 类是城市室内固定采集、距离约 2 米。论文未给出这 9 类的名单 |
| 训练测试划分 | **没有**，须自行划分 |
| 全集体积 | 约 560 GB；论文自己建议用不小于 1 TB 的硬盘 |

文件命名规则（引自论文表 3，已用本地文件逐一验证）：户外飞行类为
`T<机型码>_D<距离码>_S<频段状态与片段序号>.mat`，室内固定类**没有 `D` 字段**，形如
`T<机型码>_S<频段状态与片段序号>.mat`。`S` 是 4 位：最高位是频段状态标志，`0` 表示初始通信
在 915 兆赫兹或 2.4 吉赫兹、`1` 表示切换到 2.4 或 5.8 吉赫兹；低 3 位是片段序号 `000` 到 `111`。

**做距离对照时 `S` 的最高位不能变**，否则频段状态跟着变了、对照不成立。用户最初提供的三片
之所以不构成对照组，正是这个原因：近的那片是 `S1110`（无人机已切到 5.8 吉赫兹，2.4 吉赫兹上
只剩微弱飞控信号），远的那片是 `S0100`（无人机在 2.4 吉赫兹上工作），实测结果因此是反的——
近的突发帧占比只有 2%，远的有 15.2%。

#### 3. 与 DroneRFb-DIR 的差异

| 维度 | DroneRFb-DIR（Datasets1） | DroneRFa（Datasets2） |
|---|---|---|
| 定位 | 个体识别（同型号分架次） | 检测与型号识别 |
| 采样率 | 80 兆样点每秒 | 100 兆样点每秒 |
| 通道 | 单通道 | 双通道同时采集 |
| 中心频率 | 2.44 吉赫兹 | 2440 兆赫兹加 5800 兆赫兹 |
| 每片时长 | 50 毫秒（400 万点） | 1.5 秒（每通道 1.5 亿点） |
| **距离** | 固定 10 米 | **三档区间标注** |
| **运动状态** | 摆放不动 | **飞行中，5 至 15 米每秒** |
| 机型 | 6 型 18 架，全是 DJI | 24 型加背景，7 个品牌 |
| 接收增益与天线 | 无记录 | 50 分贝、3 dBi（均引自论文） |
| 时间连续性 | 片内连续、片间不连续 | 每 0.1 秒内连续、块间有损伤 |
| 训练测试划分 | 出版方已划好 | 无，须自行划分 |
| 正交支路质量 | 标准差比 1.0000 至 1.0014 | **0.54 至 0.95，同相支路系统性偏小** |
| 体积 | 64 GB（全集，本地已有全部） | 全集约 560 GB（本地 22 片、21 GB） |

**它补上的缺口**：一是距离维度，二是运动与多普勒，三是部分标定量（接收增益与天线增益）。
其中距离维度直接改变了 06 备忘录 §11.1 关于 P3 的 B 组「多距离场景」的结论，见第 6 节。

**它比前一个差的地方**：一是信号电平低约 15 分贝、有效位数少约 2 位；二是正交支路幅度
不平衡明显；三是没有出版方划好的训练测试划分，须自行按机型、距离档、采集批次分层，
且按 CLAUDE.md 的规矩入库即冻结、记哈希。

**仍然不够做绝对功率标定。** 虽然论文记录了接收增益与天线增益，但缺馈线损耗与一次功率
标定常数，量化码换算到分贝毫瓦这条路依然走不通，只是比前一个数据集近了一步。

#### 4. 四项实测量化结果

##### 4.1 路损指数 n = 1.90，片间重复性 0.3 至 1.3 分贝

这是本项目到目前为止唯一一个来自真实数据的定量物理结果，因此把算法口径完整记下。

**算法**（可复现的口径，与当次会话所用脚本一致）：

1. 读取：每个文件取 6 个采样偏移 `[0, 3000万, 6000万, 9000万, 1.2亿, 1.46亿]`，每处取 200 万点（0.02 秒）。这样做是因为单文件 1.5 亿点、解压 4.8 GB，整片读入不现实，而单点抽样又可能撞上突发间隙。
2. 帧化：FFT 长度 8192，汉宁窗，不重叠。单帧功率取帧内功率谱之和的 10 倍常用对数。
3. 平均功率谱：各偏移的功率谱线性域累加后除以总帧数，再取 10 倍常用对数。
4. **底噪估计：对平均功率谱在频率维取第 10 百分位。**
5. 带内功率：取相对中心频率 +2 至 +12 兆赫兹，即 2442 至 2452 兆赫兹，该频段已先行确认为无人机图传信号。
6. 突发帧占比：帧功率高于其中位数 6 分贝以上的帧所占比例。
7. 距离取区间中点：`D00` 取 30 米、`D01` 取 60 米、`D10` 取 115 米。
8. 路损指数：`n = −Δ(dB) / (10 · lg(d2/d1))`。

**样本清单**（`T0010` 即 DJI Phantom 4 Pro，全部取 `S0xxx` 即 2.4 吉赫兹频段状态）：

| 距离档 | 文件 | 图传带内功率高于底噪 |
|---|---|---|
| `D00` 20–40 米 | `T0010_D00_S0101.mat` | 27.2 分贝（**当时该档只有这 1 片**） |
| `D01` 40–80 米 | `T0010_D01_S0001.mat` / `T0010_D01_S0011.mat` | 17.4 / 18.8 分贝（片间相差 1.3 分贝） |
| `D10` 80–150 米 | `T0010_D10_S0000.mat` / `T0010_D10_S0100.mat` | 15.9 / 16.3 分贝（片间相差 0.3 分贝） |

`D00` 到 `D10`（30 米到 115 米）实测降幅 11.1 分贝，自由空间预期降幅 11.7 分贝，
折算**路损指数 n = 1.90**（自由空间为 2.00）。片间重复性 0.3 至 1.3 分贝。

**局限，必须与结论一起引用**：

- 相邻两档之间的比较不可靠。跨机型对照给出的数字是矛盾的：`T0010` 的 `D01` 到 `D10` 变化为 −2.0 分贝，而 `T0011`（DJI MATRICE 200，样本 `T0011_D01_S0000.mat` 与 `T0011_D10_S0000.mat`）为 −6.8 分贝，折算 n = 2.42，自由空间预期为 −5.7 分贝。两者相差 4.8 分贝。原因不是机型（路损是传播特性，与机型无关），而是**距离标注是区间且无人机在飞行**：`D01` 与 `D10` 的中点比只有 1.9 倍，而区间自身宽度就有 2 倍，相邻档的比较被区间的不确定性淹没。
- 因此这批数据**支持"路损指数约为 2"这个粗档结论，不支持逐米的距离—损耗曲线拟合**。
- 计算 n 时 `D00` 只有 1 片，分母只有单片支撑，给不出完整误差范围。当时判定「差 `T0010_D00_S0111.mat` 这一片」，用户已于同日补入，**该缺口现已补齐但尚未重算**（见第 9 节待办）。

##### 4.2 底噪 −24.3 分贝，两条独立路径互相验证

| 来源 | RF0 通道底噪估计 |
|---|---|
| 三片纯背景实测（`T0000_D00_S0000` / `S0100` / `S0111`） | −24.3 / −24.2 / −24.3 分贝 |
| 之前用含无人机的片子按第 4.1 节算法估算 | −24.3 / −24.3 / −24.1 / −24.2 分贝 |

两条路径完全一致，说明「对平均功率谱在频率维取第 10 百分位」这个底噪估计方法是对的，
可以放心用在有强信号的片子上。

**这里记一个踩过的坑**：早先曾用时间维度的帧功率中位数当底噪，在近距离档被信号污染了
8.5 分贝。频率维分位数与时间维中位数不是一回事，`tools/iq_survey` 实现时必须用前者。

另有一项提醒：这三片"背景"并不是纯噪声，峰值高出底噪 39.6 至 40.4 分贝，突发帧占比 0.0%
至 2.5%，里面有 WiFi 与蓝牙。`S0111` 最干净（0.0%），`S0000` 最脏（2.5%）。做虚警率标定时
要按干净程度分别处理，不能混成一批。

##### 4.3 5.8 吉赫兹通道零突发，该频段可以放弃

在 7 个文件上测过 RF1 通道（`T0010` 的三档距离共 6 片，加 `T0011_D10_S1100` 1 片），
每个文件 6 个抽样位置：

| 文件 | 距离档 | RF1 帧功率跨度 | RF1 突发帧占比 |
|---|---|---|---|
| `T0010_D00_S1000` | 20–40 米（最近） | 3.1 分贝 | **0.0%** |
| `T0010_D00_S1111` | 20–40 米（最近） | 4.6 分贝 | **0.0%** |
| `T0010_D01_S1010` | 40–80 米 | 1.5 分贝 | 0.0% |
| `T0010_D01_S1110` | 40–80 米 | 0.4–0.7 分贝 | 0.0% |
| `T0010_D10_S0100` | 80–150 米 | 0.4–0.7 分贝 | 0.0% |
| `T0010_D10_S1111` | 80–150 米 | 1.0 分贝 | 0.0% |
| `T0011_D10_S1100` | 80–150 米 | 0.4–0.7 分贝 | 0.0% |

**连最近的 20 至 40 米档都收不到 5.8 吉赫兹信号**，而同样抽样的 RF0 通道帧功率跨度达 18.4
分贝、突发帧占比达 15.2%。RF1 的峰值高出底噪 32 至 36 分贝但完全不随时间变化，是本振泄漏
或杂散，不是通信信号。

两个后果：一是不为 5.8 吉赫兹下载全集，"双频段"这个卖点在这套采集配置下不成立；二是转换
时只转 RF0，每片从 1.2 GB 降到约 600 兆字节。**注意这条取舍的理由是数据本身没有内容，不是
省存储**（后者见项目记忆 `no-space-saving-tradeoffs`）。

证据边界：实测覆盖的是 `T0010`（另有 `T0011` 一片）。推断到其他 DJI 机型属推断不是实测，
元数据里要这样标。若要复核，最省的做法是取任一其他机型的 `D00` 加 `S1xxx` 单片验证。

##### 4.4 `T10010` 的通道映射是反的，文件名有两种形态

`T10010`（FrSky X20 飞控器）实测确认了论文的说明，其通道与频率的映射与其余文件相反：

| 通道 | 中心频率 | 突发帧占比 | 帧功率跨度 |
|---|---|---|---|
| RF0 | **915 兆赫兹** | 0.0% | 4.4 分贝 |
| RF1 | **2440 兆赫兹** | **40.0%** | **29.6 分贝** |

信号在 RF1，RF0 是空的。**元数据的中心频率必须按机型编号判断，不能统一套用**：`T10010`
与 `T10100`（Taranis Plus）是 915 加 2440 兆赫兹，其余是 2440 加 5800 兆赫兹。

`T10010` 的 RF1 峰值高出底噪 50.8 分贝、突发帧占比 40%，是两个数据集里**信号最强、最活跃
的通道**，做波形模板与观测量提取的调试样本比任何一片无人机数据都好用。

同时确认文件名有两种形态：`T10010_S0000.mat` 与 `T10110_S0000.mat` 没有 `D` 字段。这印证了
论文所说的 15 类室内固定采集省掉了 `D`。**转换脚本必须处理这两种形态。**

#### 5. 实测发现的一处数据缺陷：正交支路幅度不平衡

同相支路与正交支路的标准差之比实测为 0.54 至 0.95（三个文件、五个抽样位置均存在，
并已排除短窗假象），互相关接近 0，直流偏置很小，所以是**纯幅度不平衡，不是相位误差**。
按 04 §10.6 第 3 项，这在质检中必须判 `degraded`。

**根因未查明，不编。** 但这里有一个反转值得记住：P3 明确要建模接收机非理想特性（相噪、
正交不平衡、自动增益控制、削顶）。前一个数据集的正交性太完美（标准差比 1.0000 至 1.0014），
无法用于校准正交不平衡模型；这一份有实测的不平衡量，**可能正是该模型的校准素材**——前提
是先确认它是接收链的真实特性，而不是数据生产环节的错误。

#### 6. 对 P3 四组标定数据的影响

前一个条目判定 P3 的 B 组「多距离场景」在 DroneRFb-DIR 上"完全不能"，并称之为硬伤。
DroneRFa 把这个缺口部分补上了：

| 组 | 用途 | DroneRFb-DIR | DroneRFa |
|---|---|---|---|
| A 高信噪比参考 | 参考波形与特征模板 | 部分可以（10 米视距） | 部分可以（`T10010` RF1 峰值高出底噪 50.8 分贝） |
| B **多距离场景** | 标定路损、遮挡与距离相关的模型 | 完全不能 | **粗档可以**：三档距离实测 n = 1.90；但只支持"路损指数约为 2"，不支持逐米曲线 |
| C 背景同邻频 | 真实干扰环境下的检测门限 | 可以（62 片纯背景） | 可以（本地 3 片背景，含 WiFi 与蓝牙） |
| D 独立验证 | 未参与调参的验收集 | 可以（出版方已划分） | 须自行划分，入库即冻结 |

**仍然只能由甲方数据或补采计划补上的部分**：绝对功率标定（缺馈线损耗与一次标定常数）、
逐米的路损曲线（需精确 GPS 距离记录）、视距与非视距标签与建筑几何（DroneRFa 无 LOS/NLOS
标签、无高度、无航迹记录，故跨层一致性算例 ② 建筑遮挡与 ③ 双径仍然不可做）。

#### 7. 一次不可逆的数据损失

用户在 13:59 说"先把 `T10010_S0000.mat` 和 `T10110_S0000.mat` 去掉吧"，我据此执行了
`rm`，随后用户说明本意并非删除原文件（"我也不差存储空间啊……别删了"）。核查本地时间机器
快照只有系统更新快照，没有覆盖这两个文件存在期间的快照，macOS 的 `rm` 也不进废纸篓，
**两片文件无法恢复，只能重下**。用户已于当日 15:13（本地时间）重新下载这两片。

由此产生的两条纪律已写入项目记忆 `no-space-saving-tradeoffs`：不以省存储为理由建议删除、
抽样或切片；删除原始数据前一律先确认，即使用户上一句说了"去掉"。

删除前生成了一份补数清单文件 `data/iq/measured/dronerfa/download-list.md`，按论文的命名
规则枚举命名空间（不是"确认存在"的文件列表，SciDB 上实际提供的片段序号是稀疏的，下载时
与实际文件列表取交集）。清单在本条目写作时按当前本地 22 片重新生成了一次。

#### 8. 一份格式预演切片与一处 `.gitignore` 漏洞

删除前从 `T10010_S0000.mat` 的 RF1 通道切出 2000 万点（0.2 秒）存为项目规定的复 int16
交织格式，作为 DS-2 与 DS-3 的一次小规模预演：先扫 7 个候选窗口取突发帧占比最高的一个
（偏移 2000 万点处，占比 43.4%），断言全部样点为 2 的负 15 次方整数倍（峰值码 2468，
即满量程以下 22.5 分贝），写盘后逐位回读对拍通过。同时配一份 `*.manifest.json` 外挂元数据，
把采集参数标 `source=paper`、通道映射反转写入备注、时间连续性写"每 1000 万点内连续、块间
有损伤"、绝对功率标"未标定"并写明原因、质量状态标 `degraded` 并列三条理由。

**格式预演的结论：外挂元数据这条路走得通，逐位无损与四态语义都能落地。**

同时修掉一处真实漏洞：`.gitignore` 原先只排除 `Datasets1-DroneRFb-DIR/`，
`Datasets2-DroneRFa/` 一直处于未跟踪状态，下一次 `git add -A` 会把 21 GB 吸进仓库。
已改为通配 `Datasets*/`，并验证：两个数据集目录下任意文件被忽略、切片 `.iq` 被忽略、
切片的 `.manifest.json` 不被忽略（是元数据，应入库）。

用户随后说明切片数据不必保留（原文件已重下）。**2026-09-03 补记时用户确认删除**，切片
`T10010_S0000_RF1_excerpt.iq` 与其 `*.manifest.json` 已删除（删除前核实过 `T10010_S0000.mat`
在盘、切片不是唯一副本），本节记的预演结论即该切片留下的全部价值。

#### 9. 本条目产生的待办

- [ ] 重算路损指数并给出误差范围：`D00` 档现已有两片（`S0101`、`S0111`），`T0011` 现已有三档齐全（`D00`/`D01`/`D10` 的 `S0000`），4.1 节的两处局限（`D00` 单片、跨机型矛盾）都可以再测一次。这是 P3 版本冻结的前置。
- [x] 确认 `data/iq/measured/dronerfa/` 那份 80 兆字节切片与其清单的去留 —— 2026-09-03 用户确认删除，已删（见第 8 节）。
- [ ] DroneRFa 的许可条款确认，与 DroneRFb-DIR 的 DS-1 并列，需用户出面。
- [ ] 其余步骤并入 06 备忘录 §11.5 的 DA 步骤表，不在此重复。

---

### 2026-09-03 · DA-6 路损指数重算：粗档冻结值与它真正的不确定度来源

#### 1. 起因与做法

上一条日志记的路损指数 n = 1.90 有两处明确局限：`D00` 档当时只有 1 片，给不出误差范围；
跨机型对照用的是相邻档（`D01` 到 `D10`），两个机型差了 4.8 分贝、彼此矛盾。用户随后补入
`T0010_D00_S0111.mat` 与 `T0011_D00_S0000.mat`，两处缺口都已补齐，本次重算。

**算法口径与上一条日志 4.1 节完全一致，一个参数都没改**：每文件 6 个采样偏移、每处 200 万点；
FFT 长度 8192、汉宁窗、不重叠；平均功率谱由各偏移在线性域累加后除以总帧数；底噪取平均功率谱
在**频率维**的第 10 百分位；带内取相对中心频率 +2 至 +12 兆赫兹（2442 至 2452 兆赫兹）。
本次新增的只有统计处理：片间抖动、逐对组合、最小二乘拟合、以及距离区间的敏感性分析。

脚本落库为 `scripts/da6_pathloss.py`，样本清单写死在脚本里，可原样复跑。

#### 2. 实测结果

`T0010`（DJI Phantom 4 Pro），RF0 通道，全部取 `S0xxx` 即 2.4 吉赫兹频段状态，6 片：

| 距离档 | 文件 | 图传带内功率高于底噪 | 片间极差 |
|---|---|---|---|
| `D00` 20–40 米 | `T0010_D00_S0101` / `T0010_D00_S0111` | 27.2 / 27.6 分贝 | **0.4 分贝** |
| `D01` 40–80 米 | `T0010_D01_S0001` / `T0010_D01_S0011` | 17.4 / 18.8 分贝 | 1.3 分贝 |
| `D10` 80–150 米 | `T0010_D10_S0000` / `T0010_D10_S0100` | 15.9 / 16.3 分贝 | 0.3 分贝 |

`T0011`（DJI MATRICE 200），同样条件，三档各 1 片：`D00` 22.7 分贝、`D01` 19.7 分贝、
`D10` 12.8 分贝。

九个文件的底噪估计全部落在 −24.2 至 −24.3 分贝，这是继背景片与含信号片之后**第三次**
独立验证「频率维第 10 百分位」这一底噪估计方法。

| 计算方式 | `T0010` | `T0011` |
|---|---|---|
| `D00`→`D10` 逐对组合（4 组 / 1 组，距离取区间中点 30→115 米） | n = 1.87 / 1.93 / 1.94 / 1.99，**均值 1.93**，极差 0.12，标准差 0.05 | n = **1.69** |
| 三档最小二乘（中点口径） | n = 1.95，残差均方根 1.7 分贝 | n = 1.68，残差均方根 1.0 分贝 |
| 三档最小二乘（区间几何均值口径） | n = 1.93 | n = 1.67 |
| 实测降幅 / 自由空间预期降幅 | −11.3 / −11.7 分贝 | −9.8 / −11.7 分贝 |

#### 3. 三个结论

**结论一：片间抖动很小，不是不确定度的主要来源。** 四种配对组合给出的 n 只差 0.12，
标准差 0.05；换成区间几何均值口径，n 从 1.95 变到 1.93，几乎无差别。

**结论二：主导不确定度是距离标注本身，比片间抖动大一个数量级以上。** `D00` 是 20 至 40 米、
`D10` 是 80 至 150 米，两者的真实距离比只能确定在 **2.0 到 7.5** 之间。同一个实测降幅
（`T0010` 为 −11.3 分贝）在这个比值范围内对应的 n 是 **1.29 到 3.75**；`T0011` 的
−9.8 分贝对应 **1.12 到 3.27**。

因此可以冻结的只有一句话：**这批数据支持"2.4 吉赫兹城市户外链路的路损指数约为 2"这个粗档
结论**。写进 P3 模型卡时必须同时记明：1.93 这个数字的前提是"距离取区间中点"这一计算假设，
它的精度不代表数据的精度；要一条带真实置信区间的路损曲线，仍然只能靠带精确 GPS 距离记录的
甲方自采数据。

**结论三：上一条日志记的跨机型矛盾，已经查明是相邻档比较的产物，不是真矛盾。**
上次只能用 `D01`→`D10` 比较，两个机型给出 0.72 与 2.42，差 4.8 分贝。现在两个机型都有了
宽基线（`D00`→`D10`），给出 1.93 与 1.69，差 0.24——**路损与机型无关这一物理预期得到验证**。
相邻档比较依然不可靠，本次两个机型的相邻档结果仍然发散（`T0010` 为 3.07 与 0.72，
`T0011` 为 1.00 与 2.42），原因是相邻档中点比只有 1.9 至 1.92 倍，而区间自身宽度就有 2 倍，
比较量被区间不确定性淹没。**做距离标定一律用宽基线，不用相邻档**，这一条写进 DA-3 的元数据
说明与 P3 模型卡。

#### 4. 与上一条日志的差异

上一条记的 n = 1.90 基于 `D00` 单片；补入第二片后 `D00` 档均值由 27.2 升到 27.4 分贝，
n 相应变为 1.93。**这不构成基准变化**（铁律 10），因为上一条记的是"首轮实测值"并已注明
`D00` 单片的局限，本次是按预定的 DA-6 步骤补齐样本后的重算，两个数字对应的样本集不同，
都如实保留在日志里。

#### 5. 本条目产生的待办

- [ ] P3 模型卡起草时，把「n ≈ 2 粗档、中点假设、区间边界 1.1–3.8」三件事一并写入，不得只写 1.93。
- [ ] `T0011` 三档各只有 1 片，其片间抖动未知。若要给第二机型也带上误差范围，按下载清单优先级 3 补片。
- [ ] 相邻档不可用于标定这一条，写进 DA-3 的元数据说明。

---

### 2026-09-03 · DS-2/DA-2 转换脚本与 DS-5/DA-5 摸底工具

#### 1. 本次做了什么

按用户指定的顺序做三件事：先把 `docs/iq-format.md` 的第 3、4 节定稿（否则转换脚本写出来的
格式就成了事实标准，规范反而要跟着代码走），再写转换脚本与摸底工具，最后在两个公开数据集
的真实文件上跑通并做验证。

产出：

| 路径 | 内容 |
|---|---|
| `docs/iq-format.md` 第 3、4 节 | 容器、样点编码、分段与索引、文件命名；十四个顶层键的元数据字段表、来源标注机制、八项质检键名与取最差规则、SigMF 对应 |
| `tools/iq_format/{manifest,writer,readers}.py` | 清单 schema 与校验、流式分段写盘器、两个数据集的源适配器 |
| `tools/iq_convert.py` | DS-2 / DA-2 |
| `tools/iq_survey.py` | DS-5 / DA-5，04 §10.6 八项质检 |
| `tests/unit/test_iq_tools.py` | 32 项单测，用合成 HDF5 夹具，不依赖几十 GB 数据集，可进 CI |

#### 2. 规范定稿时定的几件事

- **容器用「裸样点文件加旁挂清单」**，不用自带文件头。理由是铁律 7 要求分片续传，服务端按 HTTP Range 取任意区间时，样点偏移到字节偏移必须是常数乘法（`byte = sample × 4`），文件头会让每次换算都要先解析；另外这与 SigMF 的两文件结构同构，互转不搬样点。
- **字节序固定小端**，清单必须显式写并在读取时校验，**不做自动探测**——探测会在数据损坏时给出看似合理的错误解释。
- **单段上限 67108864 复样点（256 MiB）**；分段是存储行为，不得与采集的时间不连续混为一谈。
- **来源标注 `field_sources`**：从字段路径到 `measured / paper / derived / assumed / absent` 的映射，必须覆盖采样、频率、时间、通道、功率五类下的每个叶子字段。这是两个数据集"参数只在论文里"这一现实的直接产物。
- **`not_applicable` 与 `valid` 严格分开**：单通道数据的第 6 项（多通道对齐）是"不适用"，不是"通过"。整体状态取八项中最差，`not_applicable` 不参与。

#### 3. 单测覆盖了什么

32 项，其中这几项是刻意写的反面用例：

- 源数据不是 2 的负 15 次方整数倍时**必须抛错中止**，且不留下任何半成品文件（铁律 10）。
- 清单空骨架**必须验不过**——必填项为空正是要拦的东西（铁律 15）。
- 缺旁挂清单时摸底工具**判 degraded 而不是崩溃**，也不拿默认值顶替。
- 篡改清单里的哈希 → 第 8 项 `invalid`；改样点数 → 第 1 项 `invalid`。
- 同内容不同文件名 → 第 8 项报"完全重复"。
- 文件名含中文或空格 → 写盘器拒绝（铁律 15）。
- `IN` 解析为视距、`OUT` 解析为非视距（这个陷阱在 06 §11.4 里已经写明，单测把它钉死）。
- DroneRFa 的距离必须是**区间**，`distance_point_m` 为 `null`（D-019）。
- `T10010` 的 RF0 是 915 兆赫兹、RF1 是 2440 兆赫兹，与其余文件相反。

#### 4. 真实数据上的结果

转了 6 个产物：DroneRFb 三片（一片有信号的训练集、一片背景、一片测试集），
DroneRFa 三片（`T0010_D00_S0101` 的 RF0、`T10010_S0000` 的 RF1、`T0000_D00_S0111` 的 RF0）。
合计 1.8 GB，全部逐段回读对拍通过、无损断言通过。

| 产物 | 样点数 | 段数 | 峰值 dBFS | 耗时 |
|---|---|---|---|---|
| `dronerfb_A1_IN_S0_slice_1_CH0_S4` | 4000000 | 1 | −18.1 | 0.2 s |
| `dronerfb_background_slice_1_CH0_S4` | 4000000 | 1 | −30.8 | 0.1 s |
| `dronerfb_0_CH0_S4`（测试集） | 4000000 | 1 | −19.4 | 0.1 s |
| `dronerfa_T0010_D00_S0101_RF0_S4` | 150000000 | 3 | −22.9 | 5.1 s |
| `dronerfa_T10010_S0000_RF1_S4` | 160000000 | 3 | −18.1 | 5.3 s |
| `dronerfa_T0000_D00_S0111_RF0_S4` | 140000000 | 3 | −36.9 | 4.3 s |

**测试集的真值解析验证通过**：DroneRFb 测试集文件名是纯数字（`0.mat`），真值只存在于
`test_labels.txt`。工具反查到 `0.mat` 对应原始文件 `D1_IN_S2_slice_47`，即 DJI Mini 4 Pro
第 1 架、视距、第 2 次采集、第 47 片。**这条路径要是没实现，测试集 2513 片就全是无真值数据。**

八项质检的结论在两个数据集上一致：六项 `valid`、第 6 项 `not_applicable`、
**第 7 项 `metadata_required_units` 一律 `degraded`**，原因是十个采集类字段的来源是
`paper` 或 `absent`（采样率、中心频率、有效带宽、时间基准、连续性、站点、天线、增益、
AGC、标度）。整体状态因此一律 `degraded`。这正是 DS-5 当初被排在前面的理由：
**"缺元数据判 degraded 而不崩溃"这条路径，在真实数据上第一次被走通了。**

#### 5. 两个新的实测事实

##### 5.1 DroneRFa 的片长不是固定的 1.5 秒

上一条日志按 3 个抽样文件与论文记的是"每片 1.5 秒、每通道 1.5 亿点"。本次把 22 片全查了
一遍，实际是：

| 片长 | 片数 | 对应时长 |
|---|---|---|
| 1.3 亿点 | 1 | 1.3 s |
| 1.4 亿点 | 6 | 1.4 s |
| 1.5 亿点 | 8 | 1.5 s |
| 1.6 亿点 | 5 | 1.6 s |
| 1.7 亿点 | 2 | 1.7 s |

**全部是 1000 万点的整数倍，即 13 到 17 个采集块。** 这与论文所说"每 1000 万点内连续、
块间有损伤"的采—存交替模式吻合：片长取决于该次采集攒够了多少块。RF0 与 RF1 的长度在
每个文件里都相同。

修正上一条日志的表述：**1.5 秒是标称值，不是实测值**；任何按固定片长做的批处理假设都会出错，
转换脚本因此按实际形状读，不写死长度。

##### 5.2 正交幅度不平衡在**噪声路径**上，信号路径是平的

上一条日志记的是"标准差比 0.54 至 0.95，根因未查明"。本次用整片统计跑出来的结果很反常：
背景片 0.7335 判 degraded，而两片有信号的（0.9732、0.9981）都判通过——**同一台设备、同一次
采集，含信号的片反而"更平衡"，这不合理**，所以做了下面这个判别实验。

把同一个文件的帧按功率排序，分别取最安静的一成与最强的一成，各自算标准差比：

| 产物 | 整片 | 安静帧 | 强帧 |
|---|---|---|---|
| `T0000_D00_S0111`（背景） | 0.719 | 0.736 | 0.816 |
| `T0010_D00_S0101`（无人机） | 0.984 | 0.804 | 0.994 |
| `T10010_S0000` RF1（最强信号） | 0.999 | 0.881 | 0.999 |
| `dronerfb_A1_IN_S0_slice_1`（对照） | 1.000 | 1.001 | 1.000 |
| `dronerfb_background_slice_1`（对照） | 0.998 | 0.998 | 0.996 |

**结论**：DroneRFa 的不平衡只出现在噪声占优的帧上，信号越强整片比值越接近 1。这说明
不平衡在**接收/存储链路的噪声路径**上，被测信号本身是平衡的。DroneRFb 同为 USRP-2955
采集，两种帧上都是 1.000，**所以这不是设备型号的固有特性**，而与 DroneRFa 那次采集的
配置或数据生产环节有关。

**这个发现直接改了工具的判据（D-020）**：第 3 项质检改用安静帧的标准差比，整片值降为
参考量报出。改判据之后，三片 DroneRFa 全部被正确标出（安静帧 0.69 至 0.90），而按整片
判会漏掉两片。同一形态写成了单测 `test_survey_judges_imbalance_on_quiet_frames_not_whole_file`，
以后不会退回去。

对 DA-7 的意义：根因仍未查明，但**范围缩小了一大截**——要查的是噪声路径而不是整条链路，
且可以拿 DroneRFb 当"同型号设备、无此现象"的对照组。若最终确认是接收链特性，它就是 P3
正交不平衡模型的实测校准素材；若是数据生产环节的问题，则只能标记并弃用。

#### 6. 判据里的一个通用处理

直流偏置、标准差比、互相关这三个量在短文件上本身就有统计涨落（零直流假设下样本均值的
标准差是 σ/√N，互相关是 1/√N，标准差比是 1/√(2N)）。固定阈值会把短窗的正常涨落误报成
设备缺陷——写单测时就撞上了：24576 点的随机数据直流比达 0.9%，逼近 1% 的固定阈值。

处理办法是**判据取「固定阈值」与「5 倍统计标准差」中较宽的一个**。文件越长下限越小，到真实
数据的量级（1 亿点以上）下限约为万分之零点五，判据实际就退化成固定阈值。这样短窗不误报、
长文件不放水。

#### 7. 本条目产生的待办

- [ ] 批量转换尚未做。当前只转了 6 个产物用于验证工具。全量转换是一次批处理作业（DroneRFb 4690 片、DroneRFa 22 片×每片约 600 MB），**决定因素是时间不是空间**，等用户定何时跑。
- [ ] DS-4 / DA-4 冻结验收子集尚未做，须在批量转换的同时记 sha256。
- [ ] DS-6 真实背景虚警率标定：现在背景片已可转换、观测量统计已在摸底工具里，可以开工。
- [ ] DA-7 噪声路径不平衡的根因判定，按 5.2 节缩小后的范围查。
- [ ] `docs/iq-format.md` 第 5 节（功率与标定口径）仍被甲方数据阻塞，不动。

---

### 2026-09-03 · D0-1 / D0-2 北京亚运村观测区域与底图：两条用户指示改变了产物形态

#### 1. 本次做了什么

按 06 备忘录 §4 执行 D0-1（定观测区域）与 D0-2（底图）。计划阶段原定沿用 em-demo 的西安范围，
用户在批准计划时否决，改为**北京亚运村周围**；执行到一半用户又指出**"我可以用大的呀，128 GB 没有
问题"**，底图因此不再按观测区域裁切，而是把整份全球文件登记为共享资产。两条指示都已落实并记为
决策 D-021、D-022（`CLAUDE.md`），06 备忘录 §14 记了一条修正。D0-5（DEM）因同一决策一并完成。

| 用户指示 | 改变了什么 |
|---|---|
| "不放在西安，放在北京亚运村周围" | 观测区域中心改为 (116.405, 39.990)；em-demo 的 13,594 栋西安 Overpass 建筑集不再能作 D0-4 对拍基准 |
| "我可以用大的呀，128 GB 没有问题" | 底图与 DEM 不按区域拆分，整份登记到 `data/basemap/`；裁切件降为测试夹具；"缩小到区域之外一片空白"这个继承的坑随之消失；交付包体积因此多出约 145 GB，交付介质须在阶段 0 平台基线里一并定 |

#### 2. 产物

| 路径 | 内容 | 入 git |
|---|---|---|
| `scene/aoi/beijing-yayuncun.json` | 观测区域定义：bbox `116.345,39.945,116.465,40.035`（约 10.23 × 9.99 km），中心、坐标系、地名核实证据、替换条件 | 是 |
| `scene/register_basemap.py` | 登记整份全球底图与 DEM：软链、身份清单 | 是 |
| `scene/fetch_tiles.py` | 按观测区域裁切 zoom 0 至 15 的切片，六项自检，写清单；默认拒绝覆盖已有产物 | 是 |
| `data/basemap/planet.manifest.json` | 底图身份：137370745450 字节，全文件 sha256 `b4c4674234ae75c8…`，planetiler 0.10.2，OSM 快照 2026-08-17T04:00:00Z（序号 122085），9 个矢量图层 | 是 |
| `data/basemap/dem.manifest.json` | DEM 索引：zoom 0 至 8 共 87381 个文件、7442617745 字节，九层均完整 | 是 |
| `data/basemap/{planet.pmtiles, dem}` | 指向 Airports 工程的软链（目标机上放真实文件） | 否 |
| `data/scene/beijing-yayuncun/basemap-slice.pmtiles` | 切片：206 块瓦片、4342046 字节，sha256 `a0a40f550bea6d1e…` | 否 |
| `data/scene/beijing-yayuncun/basemap-slice.manifest.json` | 切片清单，含源文件哈希与六项自检结果 | 是 |
| `data/scene/beijing-yayuncun/verify-airports-view-z14p5.png` | 本条第 4 节的渲染验证图（真实 OSM 数据） | 否 |
| `docs/scene-package.md` | 目录布局改为"共享资产 + 区域目录"，三份清单字段表，建库脚本调用方式 | 是 |

#### 3. 观测区域中心的核实与范围的取法

中心位置不是凭记忆定的。用 `pmtiles tile` 从全球底图取 z12、z13、z14 三块瓦片，gzip 解压后
提取矢量瓦片里的中文串，`亚运村街道`、`亚运村全民健身园`、`安慧桥`、`奥林匹克公园`、`大屯`、
`慧忠路`、`北辰路`、`安立路` 全部落在 z12 瓦片 (3372, 1550) 的范围 116.367–116.455 E、
39.977–40.044 N 内，中心取 (116.405, 39.990) 与之相符。

范围大小 04 号方案附录 B 没有规定，取了与原西安范围同量级的约 10 × 10 km，标 PROVISIONAL。
三档候选的切片体积（用整份底图时与范围无关）：

| 范围 | 尺寸 | 切片瓦片数 | 切片体积 |
|---|---|---|---|
| `116.355,39.955,116.455,40.025` | 8.5 × 7.8 km | 142 | 3.4 MB |
| **`116.345,39.945,116.465,40.035`（采用）** | 10.2 × 10.0 km | 206 | 4.3 MB |
| `116.325,39.93,116.485,40.05` | 13.7 × 13.3 km | 334 | 6.0 MB |

#### 4. 实测验证结果（不是"应该能跑"，是实际跑过）

| 项目 | 命令或方法 | 结果 |
|---|---|---|
| 估算 | `fetch_tiles.py --aoi beijing-yayuncun --estimate` | 206 块、4.3 MB，与手工 dry-run 一致 |
| 切片自检 | 脚本内六项 | bounds 覆盖 bbox；zoom 0–15；条目数 206 等于 dry-run；`buildings` 层 zoom 11–15；中心 z15 瓦片 (26979, 12406) 解压 22177 字节且含 `buildings` 层名。全部通过 |
| 切片可复现 | 同参数裁两次 | 两次 sha256 相同（`a0a40f55…`） |
| 整份底图 Range 服务 | Airports `serve.py` 拷贝件，`curl -r 0-15` | `206 Partial Content`，`Content-Range: bytes 0-15/137370745450` |
| 渲染 | 无头 Chrome 加载 Airports 查看器拷贝件（初始视角改亚运村、强制离线、开 3D 建筑与山体阴影） | 样式 67 层，瓦片加载完成，可见要素 1756 个，无错误；画布导出 1114 × 813，可辨认「亚运村街道」「大屯街道」「国家体育场」「奥体中心体育场」「北四环中路」「安立路」「北辰中心花园」等标签，建筑按 Airports 外观拉伸 |
| 切片与整份一致 | 两种底图各导出一次画布 | 两张 PNG 的 sha256 相同（`f0ba7dac…`），证明区域之内二者无差别 |
| ASCII 检查 | `scripts/check-ascii.sh data/scene data/basemap scene` | 通过 |

无头验证只在 macOS 开发机上做过，按 D-015 / D-016 不作验收依据；D1 的正式验收截图另做。
Airports 工程本身未被修改，查看器拷贝件与打过补丁的服务器都在会话临时目录。

#### 5. 无头浏览器截图的三个坑（D1 的端到端测试会再遇到）

1. **`load` 事件太早**：Chrome 的 `--screenshot` / `--dump-dom` 在页面 `load` 时就动手，此时
   MapLibre 还没开始拉瓦片，截出来是空白。解决：让服务器提供一个延时 25 秒才返回的脚本，页面末尾
   引用它，把 `load` 推迟到瓦片加载完成之后。
2. **`--virtual-time-budget` 不加载瓦片**：探针显示样式挂上了但 `loaded=false`，网络请求在虚拟时间下不推进。
3. **合成器截图与 `toDataURL` 都拿不到 WebGL 内容**：`requestAnimationFrame` 正常在跑（约 57 帧/秒），
   但 MapLibre 每帧后缓冲被清空。解决：建图时开 `preserveDrawingBuffer: true`，导出前调
   `map.redraw()` 做一次同步重绘，再 `getCanvas().toDataURL()`。

#### 6. 与并行会话的协调

同一时间另一个会话在做 DS-2 / DA-2 转换脚本与 `tools/`，已占用决策编号 D-020 并修改了
`CLAUDE.md`、06 备忘录 §11 与本文件。本任务的两条决策因此编为 D-021、D-022；提交时对三份共享
文档只暂存本任务的改动，另一会话的改动留在工作区由其自行提交。

#### 7. 本条目产生的待办

- [ ] 观测区域**范围大小**待用户确认或调整（改 `scene/aoi/beijing-yayuncun.json` 后 `--force` 重跑即可）。
- [ ] D0-3 建筑解码：首先验证 z15 建筑要素有无稳定 id；顺带实测北京亚运村范围的 OSM 高度覆盖率。
- [ ] D0-4 对拍基准：建库阶段用 em-demo `fetch-buildings.mjs` 同样的 Overpass 查询拉北京 bbox。
- [ ] D0-6 区域总清单须按 sha256 引用 `data/basemap/` 两份清单。
- [ ] D1-1 Node 版 Range 文件服务，语义对齐 Airports `serve.py`；D1 端到端测试沿用第 5 节的截图方法。
- [ ] 交付介质：整份底图与 DEM 约 145 GB 随包，阶段 0 平台基线里一并定。

---

### 2026-09-04 · 底图与数字高程模型拷入本项目自持

#### 1. 本次做了什么

用户指示："底图与 DEM 约 145 GB 也拷贝到本项目目录中，避免后续出现资源路径的问题。"

在此之前，`data/basemap/planet.pmtiles` 与 `data/basemap/dem` 是两条指向
`/Users/zhiyu/CC/Airports/tiles/` 的软链，本项目因此在运行时依赖另一个工程的绝对路径。
本次把两份资源整份拷入，改为本项目自持的真实文件，并把脚本默认路径、三份清单与全部文档
一并改到包内路径。记为决策 D-023。

**动手前先确认了一件事**：那两条路径当时确实是软链。如果直接对软链路径 `cp`，写入会顺着
软链落到 Airports 的原文件上，等于覆盖来源。因此脚本里先 `test -L` 判定、`rm` 掉软链、
再拷贝；两次拷贝的第一行输出都记录了来源文件仍是 137370745450 字节。

#### 2. 实测结果（全部实际跑过）

| 项目 | 方法 | 结果 |
|---|---|---|
| 底图拷贝 | `cp`，137370745450 字节 | 1 分 52 秒（约 1.2 GB/s） |
| DEM 拷贝 | `cp -R`，87381 个文件 | 1 分 31 秒 |
| 是真拷贝而非克隆 | 可用空间、inode、块数 | 可用空间由 1.1 TiB 降到 1.0 TiB；inode 48880276 → 53666467 不同；块数各 268302240。是各自独立的副本，不是 APFS 克隆 |
| 底图内容一致 | `shasum -a 256` 全文件 | `b4c4674234ae75c8fea8a10c23f1cce49626155f56a437eda89b006f1e02b2b7`，与来源一致 |
| DEM 内容一致 | 索引哈希（"相对路径 + 字节数"有序列表的 sha256） | `13b213a4781971e24c1752c5835a1966bd575d90cc951b23d18b41d71dede52d`，与来源一致；87381 个文件、7442617745 字节，zoom 0 至 8 九层均完整 |
| 来源完好 | 拷贝后复查 | 底图仍 137370745450 字节，DEM 仍 87381 个文件 |
| 磁盘占用 | `df` | 本机剩余 1.0 TiB，两份副本合计约 145 GB |
| git 未受影响 | `git status` 计时 | 0.056 秒（新增 87381 个被忽略的文件后仍是毫秒级），无需改 `.gitignore` |
| 切片仍可复现 | 用包内底图重跑 `fetch_tiles.py --force` | 206 块瓦片、4342046 字节，sha256 `a0a40f55…`，与用来源文件裁出的完全相同 |
| 包内路径可服务 | 对 `data/basemap/planet.pmtiles` 发 `Range: bytes=0-15` | `206 Partial Content`，`Content-Range: bytes 0-15/137370745450` |
| 包内路径可渲染 | 无头浏览器加载 Airports 查看器拷贝件，源指向包内文件 | 67 层样式加载完成、瓦片加载完成、1756 个可见要素、无错误；导出画布与 2026-09-03 用来源文件渲染的那张 sha256 相同（`f0ba7dac…`） |

#### 3. 代码与清单的改动

- `scene/fetch_tiles.py`：默认底图源由 `/Users/zhiyu/CC/Airports/tiles/planet.pmtiles` 改为
  包内 `data/basemap/planet.pmtiles`。
- `scene/register_basemap.py`：原来的 `link_or_note()` 只会建软链，遇到真实文件会报错退出。
  改为 `place()`，判定三种存放形态并写入清单的新字段 `storage`：`in_place`（包内自持真实
  文件）、`symlink`（包内路径是软链）、`external`（资源在包外）；另加 `--origin` 与
  `--dem-origin` 两个参数，把副本的来源路径记进清单备查（铁律 8 的溯源要求）。
- 三份清单重新生成：`data/basemap/planet.manifest.json`、`data/basemap/dem.manifest.json`
  的 `dev_link_target` 由 Airports 路径变为空、`storage` 为 `in_place`；
  `data/scene/beijing-yayuncun/basemap-slice.manifest.json` 的 `source.path` 改为包内路径。

#### 4. 一处如实记录：文件权限

副本忠实继承了来源的权限位：底图与每个 DEM 瓦片都是 `-rwx------`（属主可读写执行、其他人
无权限），目录是 `drwx------`。数据文件带可执行位是来源本身的历史遗留，不是本次造成的。
本次**没有**改动权限，因为开发机上服务与文件同属一个用户，不影响使用。若将来内网集中部署
时应用服务以另一个账号运行，需要在部署脚本里统一放开读权限；Windows 交付不受影响。

#### 5. 本条目产生的待办

- [ ] 交付介质须装得下这约 145 GB，阶段 0 冻结平台基线时一并定（此条已在 2026-09-03 条目里
      提出，本次资源进包后成为硬约束）。
- [ ] 内网集中部署若以独立账号运行服务，部署脚本需统一放开 `data/basemap/` 的读权限。
- [ ] 此后代码、脚本、配置一律不得再引用 `/Users/zhiyu/CC/Airports/` 路径；`CLAUDE.md`
      资源清单里那两行只作来源留档。

---

### 2026-09-04 · AOI 定为 20 × 20 km，并测出建筑 id 与高度来源两项事实

#### 1. 本次做了什么

用户问「AOI 范围大小按 20 × 20 km 如何」。没有凭经验答，先对两个候选范围各解码一遍 z15
`buildings` 图层比较代价，据此给出建议；用户拍板后改 AOI 定义、重裁切片、更新联动文档。
过程中顺带销掉了 06 备忘录里标了很久的「首个技术未知点」，并发现瓦片高度字段的一个陷阱。

产出：决策 D-024（范围）、D-025（两项实测事实）；脚本 `scene/probe_buildings.py` 落库。

#### 2. 范围比较的实测数据

| 项目 | 10 × 10 km | 20 × 20 km | 倍数 |
|---|---|---|---|
| z15 瓦片 | 132 | 529 | 4.01 |
| `buildings` 要素（含跨瓦片重复） | 15706 | 58647 | 3.73 |
| 按 id 去重后的建筑 | 13564 | 50201 | 3.70 |
| 切片（zoom 0 至 15，全部层级） | 206 块、4342046 字节 | 749 块、11147792 字节 | 2.57 |

面积是 4.0 倍，建筑只多 3.7 倍。底图体积完全不受影响，因为底图已是整份全球文件（D-022）。
按 em-demo 西安那份 13594 栋对 6.9 MB 的比例外推，20 × 20 km 的 `buildings.geojson` 约 25 MB。
遮挡桶网格（100 米桶）由 100 × 100 变成 200 × 200 个桶。真正按面积 4 倍增长的是覆盖场的
格点数，同分辨率下由 25 万点变成 100 万点，这一条记给 D3。

**取 20 × 20 km 的理由不是代价而是物理**：单站置于中心时，10 × 10 km 到边界只有 5 公里、
到角上 7 公里，多半小于 2.4 吉赫兹图传链路的可探测距离，那样画出来的覆盖包络会被 AOI 边界
截断，等于用框子冒充物理边界。20 × 20 km 给到轴向 10 公里、角向 14 公里。

新范围 `116.288,39.900,116.522,40.080`，实测 19.96 × 19.97 km，中心 (116.405, 39.990) 不变。

#### 3. 两项实测事实

**其一：z15 建筑要素带稳定 id。** 20 × 20 km 内 58647 个要素**无一缺 id**，按 id 去重后
50201 栋（跨瓦片重复 8446 个）。06 备忘录 §4 把「Protomaps z15 建筑 feature 有无稳定 id」
列为首个技术未知点、要求 D0-3 首先验证，现在销项：跨瓦片合并直接按 id 做，原定的备用路径
（共享边或缓冲相交合并）不必再写。

**其二：瓦片的 `height` 字段把两种来源混在一起，比表面数字差得多。** 23.1% 的要素带
`height`，乍看远好于西安的 1.0%。但取值分布露了马脚：5.0 米一个值就占了全部 height 值的
47.6%，而且 5、8、11、14、17、20、23、26、29、32 是一串公差为 3 的等差数列——这正是
planetiler 按 `building:levels × 3 + 2` 推导的结果（1 层 → 5 米，2 层 → 8 米，依此类推）。
按「不小于 5 的整数且模 3 余 2」把它们挑出来：

| 高度来源 | 要素数 | 占全部 58647 个要素 |
|---|---|---|
| 无任何高度信息 | 45103 | 76.9% |
| 疑似由 `building:levels` 推导 | 11364 | 19.4% |
| 其它取值（真实标注的上界） | 2180 | 3.7% |

**这个判据只是启发式**，一栋真高 20 米的楼会被误判成推导值，反过来也可能有真实标注恰好落在
序列上。所以瓦片本身给不出干净的 `src`：原定 D0-3 里「`height` 有 → `src=osm:height`」这条
规则如果照写，会把 19.4% 的推导值当成实测标注，违反铁律 14（合成与估算必须标记）。已把 D0-3
的规则改为先标 `src=tile:height`，真实来源等 D0-4 拉 Overpass 原始标签后再定。D0-4 那次拉取
因此有了第二个用途，不只是重建对拍基准。

#### 4. 验证

| 项目 | 结果 |
|---|---|
| 探测脚本可复跑 | `scene/probe_buildings.py` 落库后复跑，58647 / 50201 / 0 无 id / 11364 推导，与临时脚本逐项相同 |
| 切片自检 | 六项全过；749 块与 dry-run 一致；`buildings` 层 zoom 11 至 15；中心瓦片含该层 |
| 渲染回归 | 用 20 km 切片重跑无头渲染，画布 sha256 `f0ba7dac…`，与 10 km 切片、整份底图两次渲染的结果**三者逐位相同**（视窗在两个范围内都被完整覆盖，本应如此，作为回归判据） |
| ASCII 检查 | `data/scene`、`data/basemap`、`scene` 均合规 |

#### 5. 本条目产生的待办

- [ ] D0-3 建筑解码：按 id 合并；`src` 先标 `tile:height`，不得直接标 `osm:height`。
- [ ] D0-4 拉 Overpass 时必须同时取 `height` 与 `building:levels` 原始标签，用于把 `src`
      分清「真实标注 / 层数推导 / 面积估算」三档。
- [ ] 覆盖场格点数按面积 4 倍增长，D3 设计报告里给出分辨率与耗时的取舍。

---

### 2026-09-04 · DS-6 真实背景虚警率标定

#### 1. 本次做了什么

先把两个数据集的全部背景片转换入库（DroneRFb-DIR 62 片：训练集 37 片加测试集 25 片，
测试集的背景片文件名是纯数字，靠 `test_labels.txt` 反查出来；DroneRFa 3 片），
再写能量检测器的参考实现与标定脚本，最后跑出虚警率。

| 路径 | 内容 |
|---|---|
| `algos/reference/energy_detector.py` | 能量检测器**参考实现**：检测量定义、H0 分布与门限、噪声估计。P1-4 的 C++ 版本须复现它的结果 |
| `algos/reference/ds6_false_alarm.py` | DS-6 标定脚本，含合成对照与逐频段扫描 |
| `tools/iq_format/store.py` | 本项目格式的读取端（写在 `writer.py`，读在这里，别处不再手写 `np.fromfile`） |
| `tests/unit/test_energy_detector.py` | 12 项单测 |
| `data/iq/measured/ds6-false-alarm-report.md` | 报告 |

#### 2. 检测器的口径（改动即为基准变化）

按帧长 1024 切帧、**不加窗、不重叠**；每帧 DFT 后在 M 个频点上取能量，
除以噪声估计得归一化检测量 Λ，H0 下 Λ ~ Gamma(M,1)/M、均值为 1，
虚警率 Pfa(η) = Q(M, M·η)，Q 是正则化上不完全伽马函数。

**不加窗是刻意的**：汉宁窗会让相邻频点相关，H0 的卡方自由度就不再是 2M，解析虚警率随之失效。
摸底工具算功率谱用汉宁窗是为了看谱形，两处口径不同且各自写明了理由。

Q 函数自己实现，不引入 scipy。整数 a 有精确闭式 `Q(n,x) = e^{-x}·Σ_{k<n} x^k/k!`，
单测拿它对拍：级数与连分式两条路径在 a = 4、16、128 上的相对误差都是 1e-10 量级。

噪声估计取**逐频点的帧维中位数除以 ln2**（指数分布的中位数是 σ²·ln2）。

#### 3. 一处推错了又改正的地方

原先在文档与单测里写「突发会把中位数估计拉低」，**方向反了**。被抬高的帧排到分布顶端，
剩余样本的中位数落在原分布的 0.5/(1−d) 分位上，所以是**拉高**：

| 突发占空比 | 中位数落在原分布的分位 | 估计偏差 |
|---|---|---|
| 5% | 0.5263 | **高估 0.33 dB** |
| 10% | 0.5556 | 高估 0.68 dB |
| 20% | 0.6250 | 高估 1.51 dB |

单测实测 1.0780，与解析值 1.0780 吻合。方向要紧：**高估噪声意味着门限偏高、虚警率偏低**，
所以下面测到的超标是保守的，真实情况只会更差。

#### 4. 结果

先看合成复高斯白噪声对照——这一项验证的是实现，不是数据：

| 目标虚警率 | DroneRFb 口径实测 | DroneRFa 口径实测 |
|---|---|---|
| 1e-2 | 9.67e-3 | 1.00e-2 |
| 1e-3 | 9.04e-4 | 1.05e-3 |
| 1e-4 | 1.11e-4 | 8.81e-5 |

全部落在目标值的两成以内，实现与门限公式没问题。**真实背景**（全频段合并）：

| 数据集 | 目标虚警率 | 实测虚警率 | **超标倍数** | 门限需抬高 |
|---|---|---|---|---|
| DroneRFb-DIR（62 片，242172 帧/频段） | 1e-2 | 2.47e-1 | **24.7 倍** | +22.7 dB |
| | 1e-3 | 2.32e-1 | **231.7 倍** | +32.6 dB |
| | 1e-4 | 2.22e-1 | **2222.7 倍** | +34.3 dB |
| DroneRFa（3 片，419895 帧/频段） | 1e-2 | 4.85e-2 | **4.8 倍** | +7.3 dB |
| | 1e-3 | 2.65e-2 | **26.5 倍** | +24.5 dB |
| | 1e-4 | 2.08e-2 | **207.7 倍** | +30.2 dB |

06 备忘录 §11.4 DS-6 当初的预期是「按高斯白噪声假设推出的门限会明显超标」，
**结论方向对，量级比预期大得多**：不是几倍，是两个到三个数量级，且实测虚警率在两成上下饱和。

饱和是重尾造成的：目标定得越严，白噪声假设给出的门限越贴近噪声均值，而真实背景里
「高出噪声十几分贝」的帧比比皆是，判决几乎全部落在门限之上，于是实测值不再随目标下降。

#### 5. 逐频段：干扰不是均匀铺满的

DroneRFb 背景，中心 2.44 GHz，10 MHz 一段（Λ 均值在 H0 下应为 1）：

| 绝对频率 | 频段内 WiFi 信道跨度 | Λ 均值 | 实测虚警率（目标 1e-3） |
|---|---|---|---|
| 2400–2410 | 信道 1 | 14.78 | 3.34e-1 |
| 2410–2420 | 信道 1 | **43.12** | 3.18e-1 |
| 2420–2430 | 信道 1、6 | 9.87 | 2.73e-1 |
| 2430–2440 | 信道 6 | **5.19** | 1.87e-1 |
| 2440–2450 | 信道 6 | 6.77 | 1.67e-1 |
| 2450–2460 | 信道 11 | 11.99 | 1.70e-1 |
| 2460–2470 | 信道 11 | 15.34 | 2.40e-1 |
| 2470–2480 | 信道 11 | 9.03 | 1.64e-1 |

**不是「三个信道都最差」**：信道 1 跨度内最高（43.1），信道 11 跨度次之，而信道 6 跨度内
反而最低（5.2）。合理解释是那次采集环境里信道 1 与 11 有活跃接入点、信道 6 没有。

这正是真实背景与白噪声假设的区别：**干扰有具体的占用格局，而这个格局只能实测得到，
不能从模型假设推出**。若虚警率与频率完全无关，反倒说明实现或标定出了问题。

#### 6. 三条必须与结论一起引用的限制

1. 这里的「虚警」指**检测器在没有无人机时判为有信号**。背景里确实有 WiFi 与蓝牙在发射，
   所以这些判决在物理上不是无中生有，而是把别的发射源当成了目标。能量检测本身不区分信号
   种类，这是它的固有局限，也正是 M2 层要靠特征与识别来补的原因。
2. 噪声估计用逐频点帧维中位数。**若某频段占空比超过一半，中位数估的就不再是噪声而是
   「信号加噪声」**，门限被抬高、虚警率被低估。所以本次的超标倍数是**下界**。
3. 加上第 3 节那条 0.33 至 0.68 dB 的高估，两条都使虚警率偏低。真实情况只会更差。

#### 7. 这个数字往下游怎么用

- **修正门限比理论门限高 20 至 35 dB**，意味着在这两批真实背景里，纯能量检测要达到设定
  虚警率，只能对比 WiFi 还强 20 dB 以上的信号动作。这不是「调一下门限就解决了」，
  而是该频段上能量检测的**可用灵敏度被环境抬高了这么多**。
- 跨层一致性算例 ① 因此要分两种背景各做一遍：白噪声背景（解析 Pd 曲线可对拍）与真实背景
  （只能给经验曲线）。两者的差距就是本条日志的数字，**这个差距本身是 P3 要交付的结论之一**。
- DS-7（真实背景加合成目标）现在具备全部前置：背景源已入库、检测器与门限口径已定。

#### 8. 本条目产生的待办

- [ ] DS-7：把合成目标按可控信噪比注入真实背景，出检测概率曲线，与白噪声版本并排比较。依赖 P1-3 的合成源，或先用参考实现直接生成。
- [ ] 逐频段结论只在这两次采集上成立。甲方数据到货后必须在其自己的环境里重测——**电磁环境不可移植**。
- [ ] 检测器目前只有单帧判决，没有多帧累积与门限自适应（CFAR 的常见做法）。P1-4 实现时要一并考虑，届时本条的数字是对照基线。

---

### 2026-09-04 · DS-7 真实背景加合成目标：检测概率曲线与跨层算例 ① 首次通过

#### 1. 本次做了什么

把可控强度的合成目标注入真实背景，测检测概率随信噪比的变化，并与白噪声背景并排比较。
一次覆盖两件事：04 §15.2 标准算例第 10 项「实测背景加合成目标」，以及**跨层一致性算例 ①**
（解析检测概率公式对 M3 蒙特卡洛）。

新增 `algos/reference/ds7_pd_curves.py`，检测概率的解析式加进 `energy_detector.py`，
单测由 12 项增至 17 项。报告 `data/iq/measured/ds7-pd-curves-report.md`。

#### 2. 公式侧是自己选定的，不是现成的

EM-S-02 概念模型只要求模型输出检测概率，并允许「解析近似或查表」（K-EM-CM-S-002-002），
**没有钉死闭式**。所以跨层算例 ① 的公式侧由本项目显式选定（D-026），与检测量定义严格配套：

| 目标类型 | 解析式 | 说明 |
|---|---|---|
| 随机型（带限噪声，类图传） | `Pd = Q(M, M·η/(1+s))` | 信号把每个频点功率等比放大 (1+s)，分布只是 H0 的 (1+s) 倍 |
| 确定型（单音） | `Pd = Σ_k Poisson(k; M·s)·Q(M+k, M·η)` | 非中心卡方写成中心卡方的泊松混合，即广义 Marcum Q 的级数形式 |

第二式这样写的好处是**不引入第二套特殊函数**，只依赖已经对精确闭式验证过的 Q。
两式在 s → 0 时都精确退化为虚警率，单测钉住。

#### 3. 三个口径上的讲究

**信噪比的分母取「安静时底噪」而不是「背景平均功率」。** 真实背景的平均功率被 WiFi 突发主导，
用它作分母会把环境的恶劣程度悄悄折进信噪比里，两种背景就不可比了。用安静底噪作分母，
同一个信噪比数值在两种背景下代表同样强度的目标，环境差异就干净地落在门限与检测概率上。

**门限分三档，把两种代价拆开**：① 解析门限；② 在测试数据自身上标定的门限（理想上界，
代表纯环境代价）；③ 在另一半背景上标定、拿到测试半区用的门限（现实做法，额外含标定迁移代价）。

**标定与测试各用一半帧、互不重叠**，否则门限会拟合到测试数据上。

#### 4. 跨层一致性算例 ① 通过

白噪声背景下，解析公式与蒙特卡洛的检出率最大绝对偏差：

| 频段 | 随机型 | 确定型 |
|---|---|---|
| 2410–2420 MHz | 0.0022 | 0.0016 |
| 2430–2440 MHz | 0.0050 | 0.0040 |

04 §16.3 建议的检出率容差是 5 至 10 个百分点，**实测偏差比它小两个数量级**。

这是 D-014 记的 P1 退出条件之一第一次真的跑出来。范围要说清楚：**通过的是参考实现侧**
（Python 参考检测器 + 解析式）；P1 退出条件要求的是引擎侧（C++ 分块流组件）复现同一结果，
那要等 P1-2、P1-4 落地后重跑。本次结果是届时的对照基线。

#### 5. 环境代价：23.6 到 38.9 分贝

达到检出率 0.9 所需的信噪比（目标虚警率 1e-3）：

| 频段 | 目标波形 | 白噪声背景 | 真实背景（理想标定） | **环境代价** |
|---|---|---|---|---|
| 2430–2440 MHz（最静） | 随机型 | −3.3 dB | +20.3 dB | **+23.6 dB** |
| | 确定型 | −3.4 dB | +19.9 dB | **+23.2 dB** |
| 2410–2420 MHz（最吵） | 随机型 | −3.3 dB | +35.6 dB | **+38.9 dB** |
| | 确定型 | −3.4 dB | +34.9 dB | **+38.3 dB** |

**同一台接收机、同一个检测器、同样的虚警率要求，换到真实城市背景，能检出的最弱目标要强
23 到 39 分贝。** 频段选得好不好，本身就值 15 分贝。

顺带一个结果：随机型与确定型只差 0.3 至 0.7 分贝。能量检测本来就只看总能量、不看能量怎么
分布，所以「把能量集中在一个频点」这件事对它几乎没有好处——这也解释了为什么要往特征检测走。

#### 6. 一个没预料到的发现：门限标定换一批数据就失效

第 ③ 档门限（在另一半背景上标定）拿到测试半区，实测虚警率是目标值的 **7.0 到 7.2 倍**。

做了一个判别实验：把同一批帧**随机打散**后再分半标定，虚警率回到 9.83e-4 与 1.05e-3，
基本达标。两种做法的唯一差别是分法——按时间与文件顺序分，还是打散分。

**所以失配来自背景本身随时间与地点漂移，不是统计涨落。** 这条结论直接影响工程实现：
静态门限在真实环境里不成立，必须用滑动噪声估计或恒虚警率处理。P1-4 实现能量检测时，
「单帧固定门限」只能作基线，不能作交付形态。

还要注意代价出现的位置：迁移失配**不体现在灵敏度上，而体现在虚警率上**。第 ③ 档门限比理想
门限低，所以检出率反而略高（所需信噪比低 0.7 至 1.5 分贝），代价是虚警率高出目标 7 倍。
两者是同一件事的两面：门限定低了。

#### 7. 为什么不单列突发一档

对「按帧取总能量」的检测器，**同一帧平均信噪比下突发与连续等价**：帧内能量只取决于总能量，
与它在帧内怎么分布无关。单测 `test_burst_equals_continuous_at_same_frame_snr` 实测验证了
这一点（占空比 0.1 的突发与等平均功率的连续信号，检出率差 < 0.03）。

突发的真实影响是另外两条，都可解析给出，不需要单列曲线：只有含突发的帧才有检出机会；
突发短于帧长时能量被摊薄，等效帧平均信噪比下降 10·lg(占空比)。

#### 8. 本条目产生的待办

- [ ] 引擎侧实现后重跑跨层算例 ①（P1-2、P1-4 之后），与本次的参考基线对拍。
- [ ] P1-4 的能量检测组件要按第 6 节的结论做成滑动噪声估计或恒虚警率，不能是静态门限。
- [ ] 跨层算例 ② ③ 仍不可做（两个公开数据集都无视距/非视距标签、无高度与航迹），等甲方数据。
- [ ] 本次只用了 DroneRFb 背景。DroneRFa 背景（100 MS/s、另一次采集）应当重跑一遍，看环境代价的量级是否随采集环境变化——DS-6 显示它比 DroneRFb 缓和。

---

### 2026-09-04 · D0-3 建筑解码：47582 栋，几何与底图逐像素对拍

#### 1. 本次做了什么

按 06 备忘录 §4 执行 D0-3：从本地全球底图的 z15 瓦片解码 `buildings` 图层，跨瓦片按 id 合并，
产出 `data/scene/beijing-yayuncun/buildings.geojson`。这份文件是建筑几何的**单一来源**，
同时驱动三维拉伸渲染与遮挡桶网格（铁律 11），所以几何对不对必须验到底，不能只看"渲染出来
像那么回事"。

产物：`scene/decode_buildings.py`、`buildings.geojson`（47582 栋、15919279 字节、
sha256 `65ae4dce…`）、`buildings.manifest.json`。

#### 2. 四条处理规则与它们的依据

| 规则 | 依据 |
|---|---|
| 跨瓦片按 id 合并，同 id 的碎片做**并集还原** | D-025 实测 id 稳定。不能任选一片：跨瓦片的建筑在每块瓦片里都被裁过，任选会沿瓦片边界留下一条直边，那是假墙，会直接污染遮挡计算 |
| 只保留 `kind=building` | `building_part` 是同一栋楼的子体量，保留会重复计入；`address` 实测是点要素（几何类型 1），不是轮廓 |
| 瓦片 `height` 一律标 `src=tile:height` | D-025：瓦片的 `height` 把真实标注与 planetiler 按 `building:levels x 3 + 2` 推导的值混在一起，标 `osm:height` 会把 19.4% 的推导值说成实测（铁律 14） |
| 无高度的按占地面积估高，标 `src=est:area` | 分档沿用 em-demo `fetch-buildings.mjs` 的 `building=yes` 面积档。瓦片没有用途标签（实测 `kind_detail` 对 `building` 全为空），用不上它的用途档 |

与 em-demo 参考实现有两处**有意偏离**，已写进脚本文档字符串以免被当成缺陷：估高的伪随机
种子取质心而不是轮廓首顶点（并集后顶点顺序可能随几何库版本变化，质心不会，产物因此可复现）；
本产物有 id 而 em-demo 那份没有，id 是后续"只升级高度、不动几何"的前提。

#### 3. 产物统计

| 项目 | 值 |
|---|---|
| 建筑数 | 47582（合并前 54735 片，其中 6447 个 id 跨瓦片） |
| 体积 / 顶点 | 15919279 字节 / 347045 个顶点，平均每栋 7.3 个 |
| 高度来源 | `tile:height` 10195（21.4%）、`est:area` 37387（78.6%） |
| 高度分位 | q10 5 m、q50 20 m、q90 38 m、p99.9 100 m、最高 528 m；超 100 m 的 47 栋、超 200 m 的 16 栋 |
| 占地面积 | q50 667 m²，合计 49.7 km²（占瓦片对齐覆盖区 464 km² 的 10.7%） |
| 多边形形态 | 带洞 254 栋，多重多边形 36 栋 |
| `base_m` | **全部为 0** |

`base_m` 恒为 0 不是缺陷而是规则的结果：瓦片里带 `min_height` 的 824 个要素**全部**是
`building_part`，而 `building_part` 按规则剔除。这一条已写进 `docs/scene-package.md`，
真正的架空层高度要等 D0-4 的 Overpass 标签。

#### 4. 几何验证：与底图自带建筑层逐像素对拍

先做了两项分析检查：投影往返误差为 0.0000 m；最高的那栋 528 m 建筑底面实测 80.6 × 76.9 m，
与中信大厦的实际尺寸吻合。**但不能用记忆里的地标坐标当真值**——我最初拿来对照的中信大厦
经纬度与产物差了约 300 m，方向与量级恰好是 GCJ-02 相对 WGS-84 的偏移特征，说明可疑的是那个
参考坐标而不是数据。这类核对一律只能用同源或已知基准，不能凭记忆。

真正的判据是把解码结果与**底图自己渲染的建筑层**在同一相机下逐像素比。做法：把两层都设成
纯红色填充，各渲染一帧，取画布像素算交并比。

第一轮得到覆盖率 77.9%、外溢 10.0%，看着像是几何偏小。查根因发现是**测试本身的缺陷**：
Airports 样式给建筑层单独设了描边色 `PM.bldgCase`，我只把填充改成红色，描边仍是原色，于是
每栋楼一圈描边像素被判成"只在解码结果"。把两层描边色统一后重测：

| 视角 | 交并比 | 只在瓦片层 | 只在解码结果 |
|---|---|---|---|
| 亚运村中心 z16 | 0.9928 | 248 | 909 |
| 亚运村中心 z19 | 0.9964 | 241 | 336 |
| 中信大厦 z18 | 0.9994 | 151 | 50 |

三个视角的双向残差都在几百像素、约 16 万交集像素的千分之几量级，是抗锯齿边的量级。**结论：
解码几何与底图自带建筑层一致。** 证据图 `verify-buildings-overlay-z16.png` 留在产物目录（不入 git）。

#### 5. 一条方法上的教训

第一轮那个 77.9% 如果当成"基本吻合"接受，或者反过来当成"几何有偏移"去调参，都会出错。
差异的根因是测试的渲染配置不对等，不是被测对象。**指标不对先查测量方法，再查被测物**——
这与铁律 10「超差 = 发现，查根因，禁止调参使其通过」是同一条纪律。

#### 6. 本条目产生的待办

- [ ] D0-4 拉 Overpass 原始标签（需联网）：既是对拍基准，也是把 `tile:height` 拆成
      `osm:height` / `osm:levels` 和填 `base_m` 的唯一途径。
- [ ] D0-6 区域总清单：按 sha256 引用共享底图、DEM 与本产物。
- [ ] D0-7 质量报告。
- [ ] 15.9 MB 的 GeoJSON 直接进浏览器在 D1 要实测帧率；若吃力则考虑按瓦片切分，但**几何单一
      来源不变**（铁律 11），切分只是传输形式。

---

### 2026-09-04 · D0-4 / D0-6 / D0-7：拉原始标签对拍，D0 场景数据包完成

#### 1. 本次做了什么

把 D0 剩下的三步做完：D0-4 从 Overpass 拉原始 OSM 标签并与瓦片解码结果对拍，D0-7 出质量报告，
D0-6 出数据包入口清单。**这是本项目至今唯一一次联网动作**，只发生在 `scene/` 的建库脚本里
（铁律 6），运行阶段与交付环境仍然不联网。

新增三个脚本：`scene/fetch_osm_buildings.py`、`scene/quality_report.py`、`scene/make_manifest.py`。

#### 2. 对齐方式：按编号，不是按几何

对拍的前提是两侧要素能一一对上。瓦片要素的 id 是个很大的整数（例如 35184971636750），
实测其编码为 `(类型 << 44) | OSM 编号`，类型 2 是 way、3 是 relation：47231 个 way 去掉类型位
后落在 4974233 至 1549914082，351 个 relation 落在 563404 至 21091607，都在 OSM 编号的合理
区间。**因此可以按 (类型, 编号) 精确对齐，不需要几何近邻匹配**，也就不会有匹配阈值这种可调
参数混进来。

#### 3. 对拍结果

拉到 47572 个原始要素、34.4 MB。第一个镜像返回 504，第二个 6.1 秒完成。

| 项目 | 结果 |
|---|---|
| 精确对上 | 47449，瓦片侧 99.72%、OSM 侧 99.74% |
| 只在一侧 | 瓦片侧 133，OSM 侧 123 |
| 面积相对差 | 中位 +0.008%，绝对值 q90 1.19%、q99 4.01%，超 5% 的 297 栋（0.63%） |
| 总占地 | 47979536 对 48207452 平方米，相差 −0.473% |
| 顶点数 | 338768 对 332902，即 101.8% |

**两侧各百余个对不上，根因是数据时间不同**：瓦片来自 OSM 快照 `2026-08-17T04:00:00Z`，
原始标签是 `2026-09-04T02:57:36Z` 的实时数据，中间 18 天的编辑。这条写进了报告的"三条不可比
因素"一节，放在所有差异数字之前——不先说清不可比因素，后面的差异就会被误读成管线缺陷。

#### 4. 一处写错又改对的结论

质量报告的模板里我预先写了一句"顶点数比原始少的部分是瓦片简化的代价"，而实测是 **101.8%**，
比原始**多**。数字与结论矛盾，说明结论是我预设的，不是算出来的。

查根因：81.6% 的建筑顶点数与原始完全相同；多出顶点的 4666 栋中位多 2 个，少顶点的 3978 栋。
抽样取证——**多出顶点的建筑里横跨两块瓦片的占 56.6%，而顶点数相同的对照组只占 1.7%**，所以
多出来的顶点来自跨瓦片合并的接缝，少掉的才是瓦片简化。已把报告改成由数据算出结论并附上这组
对照，不再写死。

教训与前一条日志同源：**报告里的每句结论都必须由当次数据算出来，模板不能预设结论。**

#### 5. 高度来源定案（D-027）

| 来源 | 数量 | 占比 |
|---|---|---|
| `osm:height` 真实标注 | 828 | 1.74% |
| `osm:levels` 层数折算 | 9322 | 19.59% |
| `tile:height` 对不上原始标签 | 56 | 0.12% |
| `est:area` 面积估算 | 37376 | 78.55% |

D-025 用瓦片启发式判据估的是"推导值约 19.4%、真实标注上界 3.7%"，实际是 19.59% 与 1.74%：
**方向正确、上界成立**，但只有原始标签能给出可用于标注 `src` 的定论。

层数折算固定用 **层数 × 3 + 2 米**，与 planetiler 一致（+2 计入女儿墙与屋面构筑物，对绕射
是有效遮挡高度），**与 em-demo 的 × 3.0 有意差 2 米**，已在脚本与规范里写明。层数标注为 0 或
负数的（实测 9 栋）视为无效，退回面积估算，不按 planetiler 那样当成 1 层。

`base_m` 仍然几乎全零（47582 栋里只有 9 栋非零）：原始 OSM 在该区域只有 7 个要素带
`min_height`、4 个带 `building:min_level`。架空层信息在数据源里就没有，这不是管线问题。

#### 6. 端到端验证

用产物的 `height_m` 驱动三维拉伸，压在离线底图上渲染（`verify-buildings-3d-z15p4.png`，
不入 git）：安慧里一带的高层住宅、北四环沿线的低层、奥体一带的大底盘建筑层次分明，按 `src`
分色后可以一眼看出近八成是浅色的估算件。加上此前的两项几何验证（与底图建筑层交并比
0.993 至 0.999、与原始 OSM 面积中位差 +0.008%），几何与高度都走通了端到端。

#### 7. D0 收口

七步全部完成。数据包入口是 `data/scene/beijing-yayuncun/manifest.json`，它把区域定义、共享
资产（按 sha256 引用，不复制）、7 项区域产物的哈希、两个 OSM 时刻的溯源链、以及复跑命令串成
一条可核对的链。ASCII 检查全部合规。

**这个数据包能支撑什么、不能支撑什么**：几何可用于视距与遮挡计算；**高度不可用于验收指标**，
近八成是估算值。要做建筑遮挡的跨层一致性算例（算例 ②），仍然缺的是甲方提供的真实建筑高度，
或者在小范围内人工核对的高度。这一条与数据线上"实测 IQ 仍缺绝对功率标定"是同一性质的欠项：
可以推进管线，不能据此下定量结论。

#### 8. 顺手修掉的一处忽略规则缺口

暂存时发现数据包入口文件 `manifest.json` **没有进版本控制**。根因是 `.gitignore` 的放行规则写的是
`!data/**/*.manifest.json`，这个模式匹配不到不带前缀的 `manifest.json`——`*` 后面必须还有内容。
已补一条 `!data/**/manifest.json` 并加注释说明为什么不能省。

这是本项目第二次在 `.gitignore` 上踩坑（上一次是公开数据集目录未排除）。教训是**放行规则必须
逐个用 `git check-ignore -v` 实测**，不能靠读模式判断。

#### 9. 本条目产生的待办

- [ ] D1-1 Node 版 Range 文件服务，语义对齐 Airports `serve.py`。
- [ ] D1 阶段实测 15.9 MB 建筑数据进浏览器的帧率；吃力则按瓦片切分，几何单一来源不变。
- [ ] 建筑高度的真值来源（甲方数据或小范围人工核对），这是算例 ② 的前置。
- [ ] 若 AOI 或 OSM 快照更新，按 `manifest.json` 的 `reproduce` 字段整条重跑。

---

### 2026-09-04 · DS-7 在 DroneRFa 背景上重跑：环境代价可迁移，但"平均占用度"不可用

#### 1. 为什么要重跑

DroneRFb 上测出的环境代价是 23.6 至 38.9 分贝。这个数字要能写进设计指标，先得回答一个问题：
**它是"城市 2.4 吉赫兹的普遍量级"，还是"那一次采集地点的特殊情况"？** 只有在另一次独立采集
上重跑才能分辨。两个数据集用**同一对绝对频段**，结果才可直接比较。

#### 2. 结果：两次采集相差约 6.4 分贝，但结论的形状完全一致

达到检出率 0.9 所需的信噪比（目标虚警率 1e-3，理想标定门限）：

| 频段 | 目标波形 | DroneRFb 环境代价 | DroneRFa 环境代价 | 差 |
|---|---|---|---|---|
| 2410–2420 MHz | 随机型 | +38.9 dB | +32.4 dB | 6.5 |
| | 确定型 | +38.3 dB | +31.8 dB | 6.5 |
| 2430–2440 MHz | 随机型 | +23.6 dB | +17.3 dB | 6.3 |
| | 确定型 | +23.2 dB | +16.8 dB | 6.4 |

**两个频段、两种波形，差值都是 6.3 到 6.5 分贝，一致得出乎意料。** 可以这样用：
环境代价的量级（十几到近四十分贝）是共性，具体数值随采集环境整体平移，平移量在本次两批
数据之间是 6.4 分贝。**写设计指标时应当给区间并注明它随环境平移，不能给单值。**

其余三项结论在 DroneRFa 上同样成立：

| 项 | DroneRFb | DroneRFa |
|---|---|---|
| 跨层一致性算例 ① 最大偏差 | 0.0016–0.0050 | 0.0027–0.0044 |
| 解析门限在真实背景的实际虚警率（目标 1e-3） | 2.07e-1 至 2.74e-1 | 1.01e-2 至 5.83e-2 |
| 门限跨半区标定的失配 | 7.0–7.2 倍 | 1.4–2.4 倍 |
| 随机型与确定型之差 | 0.3–0.7 dB | 0.6–0.7 dB |

**算例 ① 在一批独立数据上再次通过**，这比单批通过更有意义：它说明对拍成立与具体背景无关。
门限迁移失配在 DroneRFa 上明显缓和（1.4–2.4 倍对 7 倍），与它背景更干净一致，但方向相同，
所以"静态门限不成立"这条结论不变。

#### 3. 一处表面矛盾，查清了

DS-6 报的逐频段 Λ 均值里，DroneRFa 的 2430–2440 一格（8.68）比 2410–2420 一格（4.36）**更高**，
即"更吵"；而 DS-7 测出的环境代价却是 2430–2440 明显**更低**（17.3 对 32.4 分贝）。两者看似矛盾。

逐层查下来的结果：

1. 先怀疑底噪估计的窗口。实测**探针（开头 8192 帧）与整片估计完全一致（差 0.0 分贝）**，
   不是这个原因。
2. 再怀疑分段读取有 bug。逐 1000 万点扫描该文件，**读取正常**。
3. 真正的原因：那一格的 Λ 均值 8.68 由**一个文件独自拉高**——三片背景的 Λ 均值分别是
   1.32、2.51、**22.64**；而那个文件的高值又集中在**两段各约 0.1 秒的强发射**上
   （该处 Λ 均值 165、峰值 1611，其余时段只有 1.2）。

所以两个数字都没错，只是量的东西不同：**Λ 均值被极少数强事件主导，而检测门限由分布的高分位
决定。** 结论是一条方法学纪律：

> **不要用平均占用度预测检测代价。** 频段选择应当看高分位（99 分位、虚警率），
> 不看均值。DS-6 报告已补上 99 分位一列与这段说明。

顺带补了另一个原先没量的东西：**逐频段的安静底噪本身也不同**（同数据集内可比）。
DroneRFa 的 2430–2440 底噪比 2410–2420 高 2.0 分贝，DroneRFb 高 0.7 分贝——说明前者的干扰
更"平稳"，一部分被吸收进了底噪。把底噪差与门限抬升加在一起，两个数据集里 2430–2440 依然
都是更好的频段，**排序结论不受影响**。

#### 4. 本条目产生的待办

- [ ] 环境代价写进任何设计指标时，必须带区间与"随环境平移"的说明，不得给单值。
- [ ] 频段选择的判据统一改用高分位，均值只作参考。这条要带进 P1-4 的组件设计。

---

### 2026-09-04 · 批量转换全部入库，并冻结验收子集（DS-4 / DA-4）

#### 1. 批量转换

| 数据集 | 产物数 | 体积 | 说明 |
|---|---|---|---|
| DroneRFb-DIR | 4690 | 70 GB | 训练集 2177 + 测试集 2513，全部单通道 |
| DroneRFa | 24 | 14 GB | 22 片的 RF0，外加 `T10010` 两片的 RF1（该机型通道映射相反，信号在 RF1） |

**4714 份清单逐一核对：回读对拍失败 0，质量状态异常 0**（全部为 `degraded`，原因一致，
都是采集参数来自论文而非设备记录）。实测速度与之前外推吻合：DroneRFb 单片约 0.15 秒。

#### 2. 冻结验收子集

清单 `data/iq/measured/holdout.manifest.json`（入库，工具 `tools/freeze_holdout.py`）。

**DroneRFb-DIR：验收集 2487 片。** 用出版方划好的测试集（2513 片），减去 26 片：

| 排除的片 | 数量 | 理由 |
|---|---|---|
| 测试集里的纯背景 | 25 | DS-6 虚警率标定与 DS-7 检测概率测量用它们做过背景源与门限标定 |
| `D1_IN_S2_slice_47` | 1 | 转换脚本首次跑通时的验证样本。它并未参与任何参数选择，仍按保守原则排除 |

2487 + 26 = 2513，与出版方测试集片数核对一致。其中**从未在训练集出现的 6 架个体
（A3/C3/D3/E3/F3/G3）共 1580 片**，单列出来作个体识别泛化能力的关键子集——出版方给这 6 架
的片数（236 至 313）明显多于已见个体（51 至 106），显然是有意为之。

**DroneRFa：没有可用的验收集，一片都没有。**

这不是疏忽，是入手时还没有冻结机制的直接后果：本地 22 片**全部**已经用于探索与拟合——
DA-6 路损指数、DA-7 正交不平衡、DS-6 与 DS-7 的背景源。按项目记忆 `holdout-freeze-at-ingest`
的规矩，**用过的数据不能追认为验收集**，否则验收结果不作数。

补救办法只有一条：验收集从后续按 `download-list.md` 补下的片里划，**下载当天立即冻结，
冻结前不得用于任何分析**。这条已写进冻结清单的 `next_action` 字段。

#### 3. 检查工具

`--check` 双向验证过：
- 拿训练集的 171 个产物检查 → 「无交集，可用于调参」。
- 故意拿验收集里的 `dronerfb_1000_CH0_S4` 去检查 → 「相交 1 个，不得用于调参」。

拦截同时按 `data_id` 与 `content_sha256` 两条，**换个文件名也拦得住**。

冻结工具对已存在的清单直接拒绝覆盖，并提示「冻结只做一次，要重做须先说明理由并留档」。

#### 4. 入库方式随之改了一处（D-027）

原先 `.gitignore` 把所有 `*.manifest.json` 都放进版本控制。产物只有几个时没问题，
到 4714 个就是**近五千个小文件、约 19 MB**，既不好查也不好比。改成：

| 入库 | 不入库 |
|---|---|
| 每批一份合并索引 `index.manifest.json`（每产物一行：标识、来源、真值摘要、样点数、`content_sha256`、质量状态） | 逐产物清单 `<data_id>.manifest.json` |
| 冻结清单 `holdout.manifest.json` | 样点文件 `<data_id>.iq` |

逐产物清单由 `tools/iq_convert.py` 确定性重生成，**索引里的 `content_sha256` 足以证明盘上的
产物与入库时是同一份**。这更贴合 CLAUDE.md「大文件不入 git，只入索引与元数据」的原意。
工具 `tools/build_batch_index.py`。

#### 5. 本条目产生的待办

- [ ] DroneRFa 的验收集：补下新片时当天冻结。这件事必须在下一次动 DroneRFa 数据之前做。
- [ ] 后续任何拟合、门限标定、模型训练，跑之前先过一遍 `--check`，把结果记进 WORKLOG。

---

### 2026-09-04 · P1-2 引擎核心：分块流与调度落地，能量检测与参考实现逐帧对拍通过

#### 1. 本次做了什么

按 D-028 定的顺序（先打通薄的端到端切片，再补厚设计），从引擎这一端开工。
`engine/` 从空骨架变成可跑的分块流引擎，23 项单测全绿。

| 文件 | 内容 |
|---|---|
| `include/cuav/types.h` | 样点块与块元数据、端口类型、四态、溯源八件套、`SceneParamFrame` |
| `include/cuav/random.h` | `IRandom` 接口与 xoshiro256++ 实现（铁律 9：随机性显式注入） |
| `include/cuav/dsp.h` | 基 2 FFT、fftshift、正则化上不完全伽马、门限求解 |
| `include/cuav/graph.h` | 有向无环图、连线校验、拓扑排序、单线程调度 |
| `include/cuav/components/sources.h` | `ToneSource`、`NoiseSource`、`FileReplaySource` |
| `include/cuav/components/processing.h` | `AddMixer`、`EnergyDetector`、`DetectionSink` |

第三方件 vendored 自 emcore（doctest 与 nlohmann/json），保留原文件头，构建零网络（铁律 13）。
占位文件 `src/placeholder.cpp` 与冒烟测试按其自身注释的约定删除，版本接口挪进 `types.h`。

#### 2. 几条规矩落成了可执行的代码，不再只是文档里的话

- **D-013 的连线规则在 `connect()` 里执行**：`IQStream` 与 `SceneParamFrame` / `ChannelPathSet` 直连会被当场拒绝并给出理由，而不是等运行时出怪结果。单测覆盖。
- **四态沿块元数据向下传播**：`degrade()` 累积理由，且**降级不能把已判 `invalid` 的块"治好"**（单测钉住）。文件回放源把清单里的 `degraded` 与"量化码未标定"两条理由都带给下游。
- **缺必填参数一律报错，不拿默认值顶替**（铁律 15）：`ToneSource` 缺 `total_samples`、`FileReplaySource` 缺 `manifest_path`、清单字节序不是小端，都是配置期失败。
- **末尾不足一帧的样点直接丢弃并记状态说明，不补零**——补零会在频谱上造出假信号。
- **输入口必须全连**：留空口会让组件收到空输入而静默产出错误结果，所以 `validate()` 直接拒绝。

#### 3. 调度器有个真 bug，测试逼出来了

第一版跑三组件链路时报"调度停滞"。原因是**上游结束后没有把"结束"传给下游**：源停产后，
下游的输入缓冲永远空着，既不会被调度也不会收尾，于是全图卡死。

修法是给每个节点算出上游集合，当"上游全部结束且本节点输入缓冲已排空"时，本节点也收尾。
这是数据流调度的标准终止条件，第一版漏了。**如果当时只测单组件，这个 bug 会一直留到接服务时才炸。**

#### 4. 一条被测试逼出来的真实局限

原本写了个"单音加噪声必然检出"的用例，结果检出率是 0。查下来不是 bug：
**噪声估计取逐频点的帧维中位数，若信号从第一帧起就一直在，它就成了"底噪"的一部分。**

这不是缺陷而是这套判据的固有性质，于是拆成两个用例把行为钉住：

| 用例 | 结果 |
|---|---|
| 连续满带信号（从第一帧就在） | 检出率 < 0.05，**信号被中位数估计吸收** |
| 信号在探针窗口之后才出现 | 检出率 = 有信号帧占比 0.75，误差 2% 以内 |

工程含义写进了代码注释与 P1-4 的待办：**估底噪必须有一段无信号的窗口，或者用带外参考频段**。
这与 DS-7 得到的"静态门限不成立、要做恒虚频率处理"是同一件事的两个侧面。
`ToneSource` 因此加了起止样点参数，突发算例（04 §15.2 第 3 项）也要用它。

#### 5. 与参考实现的对拍：跨层算例 ① 的引擎侧第一次跑通

黄金基准由 `algos/reference/gen_engine_golden.py` 生成。**两侧从同一个种子各自生成输入**，
所以不需要往版本库塞二进制夹具——代价是要在 Python 里复刻 xoshiro256++ 与 Box-Muller，
这份复刻与 `engine/src/random.cpp` 逐行对应，改动即为基准变化。

| 项目 | 参考实现（Python，float64） | 引擎（C++，内部 float32） | 相对偏差 |
|---|---|---|---|
| 频段内频点 | 51 | 51 | 一致 |
| 门限 η | 1.354259521075 | 1.354259521075 | **2.3e-15** |
| 帧数 | 4000 | 4000 | 一致 |
| **命中帧数** | 39 | 39 | **逐帧一致** |
| 检测量最大值 | 1.5238738912 | 1.5238738962 | **3.3e-9** |

判据分三档并写进黄金文件：门限 1e-9（两侧同为 float64 同算法）、逐帧检测量 1e-5
（引擎内部按规范用 float32）、判决结果逐帧一致。**实测门限已到机器精度，检测量偏差比判据
严 4 个数量级**；判据仍留在 1e-5，因为不同编译器与平台的 float32 累加次序可能不同，
CI 要在 Windows 与 Linux 上也过。

范围要说清楚：这验证的是**组件级**复现。P1 退出条件要求的是整条链路在引擎里跑通并与
M1/M2 公式对拍，那还需要 P1-4 的观测量提取与 P1-5 的服务接口。

#### 6. 现在还没有的东西

背压与多线程调度、共享内存抽象、`SceneParamFrame` 的"施加"类组件、功率谱输出组件、
MATLAB Coder 链路。这些不是遗漏，是薄切片刻意不做的部分——接口里给它们留了位置
（组件不假设块大小、`Step::Idle` 表示"本轮没产出但没结束"、端口类型系统已含参数流）。

#### 7. 本条目产生的待办

- [ ] P1-5 服务最小接口：提交框图 JSON、查任务、WebSocket 状态、按视窗抽取谱数据。
- [ ] 功率谱输出组件（`SpectrumFrame`），前端最小页要靠它。
- [ ] 引擎在 Windows（MSVC）与 Linux（GCC）上首次构建验证——目前只在 macOS 上跑过，按 D-015 不作验收依据。

---

### 2026-09-04 · D1 离线底图上前端：样式逐层照搬并与 Airports 逐像素对拍

#### 1. 本次做了什么

把 D0 产出的场景数据包接到自己的前端上，完成 D1 的五个步骤。结束时浏览器里已经能离线看到
北京亚运村的底图、山体阴影和 47582 栋建筑的三维拉伸，数据全部经本地服务的 Range 请求取。

| 步骤 | 产物 |
|---|---|
| D1-1 Range 文件服务 | `server/src/{range,static,index}.ts`，零运行时依赖，30 项测试 |
| D1-2 web 骨架 | `web/src/scene/`，MapLibre 5.24.0 + pmtiles 4.4.1 随包 |
| D1-3 样式移植 | `style/{colors,protomaps,glyphs}.ts`、`layers/{hillshade,buildings3d}.ts`、`init.ts` |
| D1-4 精灵图 | 确认不需要：Airports 样式无 `sprite` 字段 |
| D1-5 探针 | `probe.ts` 的只读 `window.__probe()`，加 `tests/e2e/` 三件套 |

#### 2. 样式移植怎么验的：不看截图，看求值结果

"照搬不改"这件事不能靠肉眼比截图。做法是把 Airports `index.html` 里 `var PM = {` 到
`protomapsStyle()` 结束的整段抽出来在 Node 里求值，再把本项目移植的 TypeScript 模块求值，
两个样式对象逐层比 JSON 字符串：

| 项目 | 结果 |
|---|---|
| 图层数量 | 60 对 60 |
| 图层 id 与顺序 | 完全一致（顺序决定压盖关系，不能只比集合） |
| 逐层 JSON 不同的层数 | **0** |
| 数据源、字形地址、版本 | 一致 |

结果固化成黄金基准 `tests/golden/airports-basemap-style.json`，由 `protomaps.test.ts` 守着
（铁律 10：基准不得静默变）。以后谁改了色值或线宽档位，测试立刻挂。

#### 3. 与 Airports 的并排比对：99.83%，且不该是 100%

同一底图文件、同一相机、同一 1200 × 713 视口，两边都只留底图，逐像素比较：

| 指标 | 结果 |
|---|---|
| 容差 8 以内 | 99.83% |
| 逐位完全相同 | 93.72% |
| 平均绝对差 | 0.15（满量程 255） |

**不逐位相同是预期之内**：本项目用 MapLibre GL 5.24.0，Airports 用 5.6.1（决策 D-008 有意
锁定与 em-demo 同版本），两版在文字排布与抗锯齿上有亚像素差别；参照页还多 5 个它自己的机场
要素图层。判据取"看起来是不是同一张图"。

第一次跑出来只有 24.39%，原因是参照页 286 像素宽的侧栏没被隐藏，整幅地图右移了。隐藏侧栏、
搜索框、署名条与控件后才是可比的。**又一次是量法的问题，不是被测物的问题**——这已经是本项目
第三次踩同一类坑（前两次是建筑对拍的 77.9% 与顶点数结论），所以把"指标不对先查量法"写进了
`tests/e2e/README.md`。

#### 4. Range 服务的语义与三条路由规则

Range 按 RFC 9110 §14 实现，语义对齐 Airports `serve.py`。**多区间请求不支持**，按无 Range
处理返回 200 与完整内容——这是允许的降级，但必须写明，不能让调用方以为拿到的是部分内容。

三条路由规则都是有意为之，各有测试：

1. `/data/` 与 `/api/` 下的未知路径**一律 404，绝不回退首页**。第一版没有这条，结果请求
   `data/iq/`（本服务按铁律 7 不暴露原始 IQ）拿到的是 200 与一页 HTML，既掩盖错误又让人
   误以为该路径存在。
2. 单页应用回退**只对没有扩展名的路径生效**。缺失的静态资源要 404，不能返回 HTML 让浏览器
   在解析脚本时才报出莫名其妙的错误。
3. 路径穿越防护的安全契约写成"要么 null，要么落在根内"。原先的单元测试断言 `..` 一律返回
   null，实测发现绝对路径里的 `..` 会被规范化折掉、结果仍在根内——同样安全。**断言应该写
   性质而不是具体返回值**，写死返回值等于把实现细节当成契约，已改。

#### 5. 一个因为目录名有空格而静默失败的坑

服务编译后 `node dist/index.js` 起不来，日志一个字都没有，进程直接退出。根因是入口判定写成
`import.meta.url === \`file://${process.argv[1]}\``：仓库根目录名 `804 C-UAV` 含空格，
`import.meta.url` 把它编码成 `%20`，字符串比较永远不相等，于是 `start()` 从未被调用。
改用 `pathToFileURL(process.argv[1]).href` 后正常。凡是拼 `file://` 的地方都要注意。

#### 6. 端到端测试用自建调试协议客户端（D-029）

`tests/e2e/cdp.mjs` 一百余行，零依赖，只用 Node 内置的 `fetch` 与 `WebSocket`，覆盖开页面、
轮询探针、取截图三件事。理由是交付环境不联网、依赖须随包，为三件事引入一整套浏览器自动化
框架不划算。三个无头截图的坑一并写进了 `tests/e2e/README.md`。

`scene-smoke.mjs` 的 14 项断言全过：探针可用、WebGL2 可用、62 个图层（底图 60 + 建筑 + 山体
阴影）、三个数据源都挂上、瓦片加载完成、5613 个可见要素、地图无错误、相机落在观测区域内、
侧栏显示区域名、**估算高度有显式标注**（铁律 14，侧栏写明"高度中 78.6% 为按占地面积的估算值，
不得用于验收指标"）。

#### 7. 测试结果

| 范围 | 结果 |
|---|---|
| `server` 单元与集成 | 30 项全过 |
| `web` 样式黄金基准 | 6 项全过 |
| 端到端 | 14 项全过 |
| `web` 构建 | 通过，产物 1.26 MB（gzip 346 KB），ASCII 检查合规 |

`scripts/build-all.sh` 当前不全绿，失败的是 `engine` 的一项组件测试
（`test_components.cpp:210`，命中率 0.7505 对期望 0.6667）。那是并行会话尚未提交的工作，
不属于本次改动，未去动它。

#### 8. 本条目产生的待办

- [ ] D2 态势图层：状态流 schema、图层模块、Canvas 叠加层、**浅色底图上的色值重标定**。
- [ ] 建筑数据进浏览器的帧率实测。当前 15.9 MB 的 GeoJSON 一次性加载，端到端跑下来没有卡顿，
      但没有量过帧率；若吃力则按瓦片切分传输，几何单一来源不变（铁律 11）。
- [ ] 前端产物 1.26 MB 未做代码分割，Vite 已警告。单机模式下从本地加载不算问题，
      但打包给 Windows 时应复核首屏时间。
- [ ] Node 长期支持版本冻结后，`web` 与 `server` 的 `package-lock.json` 要重新生成并重验。

---

### 2026-09-04 · 清掉入库文件里的机器相关绝对路径（新增铁律 17）

#### 1. 起因

用户问仓库目录名 `804 C-UAV` 带空格会不会影响后面的工作。查证时发现空格本身影响有限——远端
仓库名是 `UAV_ER_Simu`，克隆、持续集成检出与交付包都不带空格，所以它只存在于开发机上——但
顺带扫出了一个更严重的问题：**入库文件里有写死的绝对路径**。用户随即定下规矩：路径必须是
项目内的相对路径，否则项目整体迁移或跨平台就会出问题。这条写成了铁律 17。

#### 2. 扫出了什么

写了 `scripts/check-paths.sh` 作判据：出现 `/Users/`、`/home/<用户名>`、`X:\Users\` 即不合规；
系统目录（`/Applications/`、`/usr/bin/`、`C:/Program Files/`）不算，那是查找浏览器、编译器
这类系统件的正当位置。首次运行命中 7 处，分三类：

| 类别 | 处数 | 后果 | 改法 |
|---|---|---|---|
| 脚本写死仓库路径 | 2 | **在任何别的机器上都跑不了**，与空格无关 | 从 `__file__` 推导根目录；数据集位置另给环境变量 `CUAV_DRONERFA_DIR` |
| 产物清单存绝对路径 | 4 | 迁移后清单不再可核对，也不可复跑 | 新增 `repo_path()`，仓库内路径一律存相对形式 |
| 注释引用外部工程的绝对路径 | 3 | 换台机器读者跟不过去 | 只写工程名与文件名，实际位置由 CLAUDE.md 资源清单单点记录 |

还顺手修了一处清单不该有的东西：`extract.command` 记的是 `/opt/homebrew/bin/pmtiles`，
换平台同样失效，改成只记工具名 `pmtiles`，工具版本另有 `tool_version` 字段。

#### 3. 例外只有一个，并且写进了检查器

数据清单里记录**外部历史来源**的 `origin` 与 `dev_link_target` 保留绝对路径。它们回答的是
"这份副本当初从哪里拷来"，是留档而不是可解析的位置；副本与来源逐字节相同这件事由 sha256
保证，换机器后哈希仍然有效。检查器对这两个字段名显式放行，清单里也写明"任何代码都不得解析它"。

#### 4. 顺带修掉的一个生成器缺陷

`scripts/gen_dronerfa_download_list.py` 改完路径后复跑，产出的 `download-list.md` 把一段
**手工更新过的说明覆盖回了脚本里内嵌的旧文字**。根因是生成器内嵌了叙事性段落，而那段话在
提交 `1867d67` 里被人工改过，脚本没跟着改。

改法不是把新文字再抄进脚本，而是**让生成器只陈述它能核实的事实**：那段现在按目录里实际的
样点文件数与清单数生成（当前 71 个与 25 份），叙事性的过程记录一律留在 WORKLOG。复跑两次
验证幂等。这条教训与本项目此前几次同源：**凡是能过期的结论都不能预先写死在生成器里。**

#### 5. 关于目录名里的空格

结论是**保留，并当成探针**。理由已写进 CLAUDE.md 的「继承的坑」：远端仓库名不含空格，所以
空格不会传播到流水线与交付物；而 Windows 上的真实路径常含空格与中文，本机带空格能让"路径
没加引号、`file://` 没做编码"这类问题在开发阶段就暴露。2026-09-04 服务静默不启动正是这样被
抓到的。代价是两条纪律：脚本里路径一律加引号；拼 `file://` 一律用 `pathToFileURL`。

#### 6. 验证

| 项目 | 结果 |
|---|---|
| `scripts/check-paths.sh` | 通过，已接入 `scripts/build-all.sh` |
| 三份场景清单重新生成 | `source.path` 现为 `data/basemap/planet.pmtiles` |
| 建筑产物 | 重新解码后仍为 47582 栋，统计量不变 |
| 生成器幂等 | 连跑两次，产物一致 |
| ASCII 检查 | 通过 |

#### 7. 本条目产生的待办

- [ ] 新写任何脚本前先想一遍路径来源；提交前 `scripts/check-paths.sh` 会拦。
- [ ] 其它目录（`tools/`、`algos/`、`engine/`）由并行会话维护，本次未逐个复核其内部约定，
      但检查器覆盖全仓库，有问题会在下次全量构建时暴露。

---

### 2026-09-04 · 路线图修订 v1.1：把界面、场景生成、框图、信号可视化与 MATLAB 提到当前工作面

#### 1. 起因

用户定后续工作重点：前端人机交互界面的设计、真实地理环境下的反无电子侦察场景与目标活动的生成、
面向信号级仿真的 B/S 框图模型设计、信号波形与频谱的可视化、后端模型计算，并问计划是否需要修订。
中途追问「MATLAB 的优势发挥出来了吗」。

#### 2. 核实到的事实

- 06 备忘录的步骤表只细化到 P1；P2（画布、组件库、参数表单、WS 补取、SQLite、上传、Windows 包）
  只有一段话没有编号。按 06 的「下一步」本应做 D2 态势图层。
- 代码现状（实测盘点）：`web/src` 只有场景视图，`App.tsx` 硬返回 `<SceneView/>`，无路由、无状态
  管理、无 WebSocket 客户端、无框图画布、无任何波形或频谱绘制、无态势图层，`api/ diagram/ signal/`
  三个目录是 `export {}` 占位。`server/src` 零运行时依赖，只有 Range 静态服务与三个只读端点，无
  WebSocket 服务端。`engine/` 只有静态库与测试可执行，没有任何可执行入口，与服务零接口；
  `IComponent` 没有参数描述与按名构造的注册表，`configure()` 只收两个 `map`。`geo/` 纯占位，
  `models/` 六个子目录全空。
- 参考实现 em-demo 的场景是写死的常量（`simulation/scenario.ts`），`simulation/engine.ts` 在浏览器里
  按 20 Hz 沿航点插值推进，`EquipmentPanel` 只能原地改设备参数，没有在地图上布站或画航线的编辑功能。
- 04 对场景生成几乎没有条款，只在 §3.1 提到「配置无人机工作状态、位置、航迹和传播条件」；04 §8.1
  引用的主界面交互原型 html 不在盘上（全盘搜索无结果）。
- MATLAB：参考实现与黄金向量全部是 Python numpy；P1-8「MATLAB Coder 链路」在 06 里有条目但没排进
  任何步骤；`matlab/` 目录空；缺的 DDC、信道化、接收滤波原计划手写 C++。开发机有
  `/Applications/MATLAB_R2025a.app`，工具箱 coder、dsp、comm、signal、phased、rf、fixedpoint、compiler
  齐全，`matlab` 不在 PATH。03 §11.3.1 与 04 §8.5 都把「Coder 生成 C/C++」定为最终产品主路线，
  03 §11.8 明确不建议 MATLAB Engine 作最终运行方式。

#### 3. 判断：计划需要修订，六处

| 用户重点 | 原计划的处理 | 差距 |
|---|---|---|
| 人机交互界面 | 无步骤；P1-6 是「任务 JSON 提交表单」 | 界面被压到 P2，且 P2 无步骤表 |
| 场景与目标活动生成 | 04 与 06 都无归属：D2 只显示状态流，D3-5 消费航迹但没人生产 | 整条线缺失 |
| B/S 框图模型 | 画布留到 P2 报告 09；框图 JSON 无规范；引擎无注册表 | 既无数据模型也无 UI |
| 波形与频谱可视化 | 只提 PSD 与瀑布 | 缺时域包络、观测点、视窗抽取的产品格式 |
| 后端模型计算 | 引擎与服务零接口；P1-3 缺 5 个组件 | 浏览器提交到结果没有桥 |
| MATLAB 优势 | 未发挥 | 新增 M 线，DSP 组件改走 Coder 主路线 |

前端与运行时不出现 MATLAB 是 04 §8.5 与 03 §11.8 有意为之；MATLAB 的优势在模型生产与验证侧，
而那一侧此前没有开工。

#### 4. 用户拍板与新决策

四项拍板均取推荐项：框图画布 React Flow；引擎桥接子进程 `cuav_run`（不做 N-API，不做引擎内嵌
HTTP）；场景运动学与链路预算放 `geo/` C++ 库加引擎组件 `ScenarioSource`；切片顺序链路 → 场景 →
画布 → 合流。写入 CLAUDE.md 决策日志 D-030 至 D-036。顺手修掉决策日志里两条 D-027 重号，后者改
D-027b。

#### 5. 修订内容

- 06 备忘录升 v1.1：§0.2 路线图加 B 桥接、U 界面、G 场景、M MATLAB 四条线与五个纵向切片；新增
  §9A 至 §9E 步骤表；P1-5、P1-6 撤销由 B / U 替代；P1-8 由 M-2 兑现；P1-3 的 DDC、信道化、接收滤波
  改走 Coder；D2 并入 G-5；D3-5 移交 G-2；D3-3 的自由空间路损锚点前移到 G-1；§10 P2 缩为剩余项；
  §14 记修正。P1 退出条件只把「从浏览器提交」改为「从框图画布提交」，技术路线与阶段 0 退出条件不变。
- CLAUDE.md：文档体系表加 07/08/09 报告与四份新规范；已定表加引擎运行形态、框图画布、WS 库、
  场景运行时、DSP 组件生产路线；OPEN 表删框图画布、加 MATLAB Runtime 与 Coder 许可；里程碑表加
  B / U / G / M 四行并改 P1、P2、D2、D3；环境与命令补 `cuav_run` 与 MATLAB；对上游文档修正加两条。
- 四份规范骨架落地，字段已冻结：`docs/diagram-format.md`（节点、端口、连线、参数、观测点、场景
  绑定、运行参数）、`docs/scenario-format.md`（站点、辐射源、航线、活动时间线、到参数帧的契约）、
  `docs/display-products.md`（产品目录、定长行二进制与索引、抽取端点、WS 二进制帧）、
  `docs/component-catalog.md`（六类、端口兼容矩阵、参数描述、实现形态），以及 `docs/schemas/` 下
  两份 JSON Schema。`docs/api-versions.md` 3.3 端点表与第 4 节 WS 事件信封冻结。
- `matlab/README.md`、`models/README.md`、`tests/regression/README.md` 补实现形态与算例输入约定。
- 本次只动计划文件与规范骨架，不动业务代码。修订后实测 `scripts/build-all.sh` 全绿：web、server、engine 单测
  （`cuav_engine_unit` 通过）、geo 冒烟、路径检查、ASCII 检查全部通过；D1 条目记录的那项引擎组件测试失败已不复现。
- 顺手修一处检查器缺陷：`scripts/check-paths.sh` 提交后会扫到自己（注释与判据里有 `/Users/` 字样，
  而 `*.sh` 在扫描范围内），此前只在它未入库时跑过所以没暴露；改为 `git grep` 的 pathspec 排除自身。

#### 6. 风险与坑（先记，切片实施时逐条销）

- Windows：`spawn` 路径含空格与中文（数组参数不走 shell）；子进程 stdout 按 UTF-8 字节写与解析；
  服务崩溃留孤儿进程（任务目录写 pid，启动时对账）；读正在追加的产品文件（行定长、索引最后写、
  读端只读已索引行）。`cuav_run` 与 `cuav_geo` 新代码的 MSVC 首次构建提前到切片 ② 末尾。
- 离线：React Flow 与 `ws` 锁版本进 `package-lock` 与 THIRD-PARTY-NOTICES；e2e 保留「页面无外网请求」断言。
- 性能：15.9 MB 建筑常驻加 20 Hz 实体事件加瀑布 Canvas。三视图共享单个地图实例；信号页不挂载时
  瀑布不绘；实体更新合并到 rAF；频谱行走 WS 二进制（约 80 KB/s）；瀑布用离屏 Canvas 位移追加。
  单线程引擎跑 5 MS/s 加 FFT 不承诺实时，界面同时显示逻辑时间与墙钟。
- 复现性：参数帧必须按样点序号推进，按墙钟则同种子不可复现。
- 基准：组件目录黄金基准只比「已有条目不变」，改动条目即记决策。
- 合规：合成场景徽标常驻并进截图（铁律 14）；回放源 `scene_bindable=false`，画布与 `--validate`
  双重拒绝（防线二、三）。
- 需求：09 报告顶替缺失的原型 html，标 PROVISIONAL，原件到手后对账。
- 架构：`SceneParamFrame` 每链路一个输出口，多辐射源时端口数随场景变，目录用 `dynamic_ports` 描述。

#### 7. 本条目产生的待办

- [ ] 切片 ① 从 B-1 组件注册表与参数描述开始；并行 M-1 `matlab/` 骨架与 `pwelch` 黄金向量。
- [x] 2026-09-04 用户确认：Coder 生成的 C 代码随交付不受单位许可条款限制，M-0 销项。DS-1 / DA-1 许可仍未确认。
- [ ] 08 报告在切片 ① 后、切片 ② 前定稿；09 报告在切片 ③ 前。
- [ ] `data/scene/beijing-yayuncun/quality-report.md` 里的「D-027」指建筑高度分档，即改号后的 D-027b；
      该文件由生成器产出，下次重跑生成器时同步改引用，本次不手改生成物。

---

### 2026-09-04 · 09 报告前半、M-0 销项、B-1 组件注册表

#### 1. 起因

用户批准路线图修订后指示：先出 09 报告的前半部分，再开 B-1；并确认 Coder 生成的 C 代码随交付不受
单位许可条款限制。

#### 2. 09 报告前半

`09.主界面三视图与框图画布交互设计_v1.0.md`，382 行，§0 至 §14。定了：一个窗口、一条顶栏、三个视图、
一个底部抽屉；04 §8.1 的「左组件库 / 中画布 / 右参数」是框图视图内部布局而不是全应用布局；顶部试验
上下文的八个字段与联动规则；三视图只隐藏不卸载、地图实例不重建；场景视图、框图视图、结果视图各自的
三栏线框；底部抽屉四页签；store 形状与数据流；探针 `app` 子对象与切片 ① 的六条 e2e 断言；键鼠与
视口基线；视觉与文案约定；四态与丢帧在界面上的表现；与 04 §8.1、03 §11.9 的逐条对照；八项 PROVISIONAL
待确认。§15 列了后半（画布与信号视图交互）要写的内容，切片 ③ 前补。

#### 3. M-0 销项

工具链上一条目已核实；许可由用户确认不受限。CLAUDE.md OPEN 表删该项，06 §0.5 删输入行，§9D M-0 记
`[x]`，附录 A 改为「已确认」。

#### 4. B-1 组件注册表与参数描述（切片 ① 第一步）

做了什么：

- `engine/include/cuav/param_spec.h`：`ParamSpec`（名、四种类型、单位、必填、上下限含开闭、默认值、枚举、
  说明、跨参数约束文字）与链式构造。
- `engine/include/cuav/component.h`：`ComponentInfo`（类型、六类类别、显示名、说明、M/E、`model_id`、版本、
  端口、参数、`scene_bindable`、`stateful`、`implementation`、`source_ref`、动态端口）；`IComponent::describe()`
  带默认实现，六个组件各自重写。
- `engine/include/cuav/registry.h`、`src/registry.cpp`：`Registry` 按名构造，重复注册拒绝；`validate_params()`
  按描述挡未知参数、类型错位、越界、枚举取值、缺必填，报错一定含参数名；`create_configured()` 三步合一；
  `builtin_registry()` 注册六个内置组件。
- `engine/include/cuav/catalog.h`、`src/catalog.cpp`：`catalog_json()` 按 `docs/component-catalog.md` 输出
  `cuav-catalog/1`，`port_compat` 由 `can_connect()` 六乘六全枚举导出并附拒绝理由；`validate_catalog_entry()`
  做目录自检。
- `engine/tests/test_registry.cpp`：7 个用例。

实测：

| 项 | 结果 |
|---|---|
| 引擎单测 | 30 个用例、523 项断言全绿（原 23 个不变） |
| 目录导出 | 六个组件按类型名排序；兼容矩阵 36 行、6 行为真、30 行带 D-013 拒绝理由；两次导出逐字节相同 |
| 一致性 | 五个组件只给必填项即可构造；去掉任一必填，描述与 `configure()` 两道闸都拒绝且报错含参数名；所有默认值在自己的范围内 |
| 越界样例 | `amplitude = -1`、`pfa = 1.0`、`sample_rate_Hz = 0`、未知键 `amplitud`、文本参数按数值给，全部被拒且报错含参数名 |
| 经注册表构造的链 | 单音加噪声到能量检测跑通，后半段强单音的检出率大于 0.3 |

两点设计取舍：

- 注册表是显式对象不是全局单例，`builtin_registry()` 每次返回一份新的；用户插件将来往自己的 `Registry` 里
  `add()`，与 04 §15.2 第 12 项「用户算法插件」的接口方向一致。
- `describe()` 与 `configure()` 是同一套规则的两道闸而不是合并成一道：前者按表挡住可静态判断的错误，后者做
  跨参数约束（奈奎斯特、频段上下限、2 的幂）。合并会让组件的校验逻辑迁到描述表里，描述表就不再是纯数据。

#### 5. 本条目产生的待办

- [ ] P1-4a `SpectrumAnalyzer`（Welch → `SpectrumFrame`，用 `dsp::fft_inplace`），以 M-4 的 `pwelch` 黄金向量验收。
- [ ] B-3 观察者与 `ObservationTap`；随后 B-2 框图 JSON 装载器（`docs/diagram-format.md`）。
- [ ] M-1 `matlab/` 骨架与 `pwelch` 黄金向量导出，与上两项并行。
- [ ] B-4 生成首版目录黄金基准 `tests/golden/component-catalog.json`。
- [ ] 09 报告后半（画布与信号视图交互）在切片 ③ 前补；前半的八项 PROVISIONAL 待用户确认。

---

### 2026-09-04 · 09 报告评审与 v1.1；回放源改用 data_id

#### 1. 起因

用户要求以电磁仿真人机交互专家的角度评审 09 报告前半。评审给出十二条「必须改」与十二条「建议改」，
用户指示「必须改，改到位」。

#### 2. 评审结论

v1.0 立住了应用壳（三视图不卸载、上下文单例、目录驱动组件库、四态不静默、探针可断言），但还不是电磁
仿真的人机交互设计：场景视图没有「链路」对象，只是带航点的地图编辑器；信号视图没有频谱仪的仪表约定；
没有用户与任务流。另有两处硬伤会直接返工：`Ctrl+1/2/3` 是 Chromium 保留的标签页切换键，页面拦不住；
回放源以 `manifest_path` 作用户参数，服务器路径会进框图 JSON 与参数面板，违反 04 §8.6，且数据中心「只列
索引」没有把数据接进框图的途径，回放流程是断的。

#### 3. v1.1 的改动（09 §17 有逐条记录）

| 条 | 改法 | 落点 |
|---|---|---|
| ① 保留键 | `Alt+1/2/3`；列出不绑定的浏览器保留键；`Ctrl+S` 须 `preventDefault` | §4.2、§11 |
| ② 路径 | 回放源只写 `data_id`，服务端装载时解析成内部参数；数据中心加「用于回放」 | §6、§7.3；D-037 |
| ③ 顶栏 | 面包屑「项目 › 试验 › 任务」加运行组；场景、框图、种子、模式收进试验详情；1366 折叠顺序 | §2、§3 |
| ④ 徽标 | 运行态（图标）与结果四态（形状加文字加颜色）两个正交徽标，定组合规则 | §13.1 |
| ⑤ 链路 | 对象树加派生的「链路」组；地图画链路线按视距着色；右栏读数距离、方位、俯仰、路损、时延、多普勒、帧有效期、四态，随游标变化 | §5.4 |
| ⑥ 地图要素 | AOI 边界常显不可关、比例尺、指北针、鼠标经纬度 | §5.2 |
| ⑦ 仪表约定 | 纵轴 `dBFS（未标定）`、参考电平与动态范围、射频绝对频率轴、RBW、迹线模式、marker、瀑布方向、色带图例、导出 | §7.2 |
| ⑧ 双游标 | 频率 marker 与时间游标在频谱、瀑布、包络上联动；框选缩放 | §7.2 |
| ⑨ 检测叠加 | 命中帧画成时频框叠在瀑布上 | §7.1、§7.2 |
| ⑩ 撤销重做 | 场景与框图各一个 50 步撤销栈，`Ctrl+Z / Ctrl+Y` | §4.3 |
| ⑪ 溯源 | 任务、节点、观测点、链路帧共用的溯源面板，六件套加 M/E/V，可导出 JSON | §9 |
| ⑫ 用户与流程 | 三类用户；全合成、实测回放、混合增强三条主流程的视图路径与反馈；四种空状态 | §1 |

随手并入的建议：工程计数法显示与输入、状态条基准 `WGS-84 · AGL · LogicalSim`、图层开关去重、通知策略、
示例框图下拉、保存带版本说明、模式中文标签、实现细节移附录 A。未并入的建议留后半：对比模式细节。

#### 4. 引擎与规范的连带改动

- `ParamSpec` 加 `internal` 标志与 `internal_only()`；目录 JSON 输出 `"internal": true`。
- `FileReplaySource`：用户参数 `data_id`（必填），`manifest_path` 转内部参数；`configure()` 两者都收，缺
  `manifest_path` 时报「应由装载器按 data_id 解析注入」；`data_id` 进块溯源的 `trace_id`。直接构造的测试与
  工具不受影响。引擎 30 项单测、535 项断言全绿。
- `docs/diagram-format.md` 加「参数值不得是服务器路径」规则与回放节点写法；`docs/component-catalog.md`
  加 `internal` 字段；`docs/api-versions.md` 任务提交端点写明解析与 400 规则、数据中心两个端点、WS 加
  `link` 事件并把 `task.state` 拆成运行态与结果态、日志路径替换；`docs/display-products.md` 加
  `links.jsonl` 与 `/links` 端点。06 §9A B-2、B-3、B-5 与 §9B U-1、U-3、§9C G-2 对应修订。

#### 5. 本条目产生的待办

- [ ] 09 §15 的十三项 PROVISIONAL 待用户确认，尤其瀑布方向、`dBFS（未标定）` 一刀切、验收集片段允许回放。
- [x] B-2 装载器实现 `IDataResolver` 与 `internal` 参数拒绝（2026-09-05，见同日「B-2」条目）；B-5 服务端解析 `data_id` 待做。
- [~] G-2 观察者加 `on_link` 与 `links.jsonl` 产品；`/links` 端点的**读取层**已于 2026-09-06 随 B-7 落地，缺的只是生产者。
- [ ] 切片 ① 继续：P1-4a `SpectrumAnalyzer`、B-3 `ObservationTap`、M-1。

---

### 2026-09-05 · 切片 ①：频谱分析组件、观测点产品、MATLAB 黄金向量三方互证

#### 1. 起因

用户指示「继续下一步」。按 06 §9E 切片 ① 与上一条目的待办：P1-4a `SpectrumAnalyzer`、B-3 观察者与
`ObservationTap`、并行 M-1 `matlab/` 骨架与 M-4 的 `pwelch` 黄金向量。

#### 2. P1-4a 频谱分析组件

- 口径：Welch 平均的**功率谱**（不是功率谱密度）。每段加周期窗，DFT 后取 |X|²，各段平均，除以 (Σw)²，
  零频居中；复单音峰值等于其功率 A²，满量程单音读 0 dBFS；未标定一律 dBFS（D-020）。四种周期窗
  hann / hamming / blackman / rect，重叠比例与每帧段数可配。
- 计算在 double 里做：`dsp::fft_inplace` 加了 double 版本，旋转因子逐点直接求值不递推；float 版本原样保留，
  能量检测的黄金基准依赖它（铁律 10）。`SpectrumFrame.psd_dB` 改为 double，`PortData` 改为一次交出多帧
  `spectra`，因为调度器只在有新输入时调 `process()`，组件必须把当前块能切出的帧一次全交出。
- 块长无关：同一输入按 1 块与按 97 样点小块喂，四帧逐位相同。收尾时不满一帧的段平均后交出并标降级，
  丢弃的尾样点只算「从未被任何一段覆盖」的（有重叠时 head 之后的 nfft − hop 个样点已进过上一段）。

#### 3. 三方互证（M-4 首项）

输入是闭式确定性信号（三个单音、一个直流、48 个按黄金分割摆放的低幅单音），量化成 float32 后以精确十进制
写进 `engine/tests/golden/spectrum_welch.json`，公式只作说明；这样避免在 MATLAB 里复刻 xoshiro256++。

| 一方 | 实现 | 对拍结果 |
|---|---|---|
| Python | `algos/reference/gen_spectrum_golden.py`，numpy.fft 手写 Welch | 基准 |
| MATLAB | `matlab/golden/gen_spectrum_golden.m`：`pwelch(x, hann(nfft,'periodic'), nfft−hop, nfft, fs, 'twosided', 'power')` 再 `fftshift`；并与 `matlab/ref/cuav_welch_power.m` 手写参考互证 | 与 Python 逐频点差在 1e-12 × 峰值以内（相对误差为 0）；pwelch 与手写参考最大相对误差 3.7e-16；R2025a Update 1 |
| C++ 引擎 | `SpectrumAnalyzer` | 与 Python、MATLAB 均 rel ≤ 1e-9；峰值 bin 160 三方一致，−5.4177 dBFS |

**踩到的坑**：MATLAB `pwelch` 的 `'centered'` 对偶数 nfft 给 `(−fs/2, fs/2]`，奈奎斯特频点放正端，与 `fftshift`
（numpy 与 MATLAB 自身）的 `[−fs/2, fs/2)` 差一格。首次对拍相对误差 7.0e4 就是这一格。改为 `'twosided'` 再
`fftshift`，已写进 CLAUDE.md「继承的坑」与 `matlab/README.md`。

另一个事实：JSON 交换时 MATLAB 一方不能改写 Python 写的文件，`jsonencode` 只保留 15 位有效数字，会破坏
float32 精确值的十进制表示；MATLAB 结果写到 `spectrum_welch.matlab.json`。

#### 4. B-3 运行观察者与观测点产品

- `observer.h`：`IRunObserver{on_progress, on_entity, on_link, on_log, on_product_row}` 与 `ProgressInfo`、
  `EntityState`、`LinkFrame`；`Graph::run(rng, observer)` 重载，init 前对每个节点 `attach()`，每轮回调进度。
- `platform.{h,cpp}`：目录逐级创建、文件原子替换、字节序探测、路径拼接；`#ifdef _WIN32` 只在这一处（铁律 16）。
- `components/tap.h`：`ObservationTap` 接任一 IQ 输出口，写 `spectrum.f32`（每行 nfft 个 float32 dBFS）与
  `envelope.f32`（每行 min、max、rms）及索引 JSON（`docs/display-products.md` §2 的字段，含 `notes`、
  `producer`、被观测信号的 `trace`）；索引每 64 行刷一次、收尾原子替换；每行同时回调 `on_product_row`。
  与 `SpectrumAnalyzer` 共用 `WelchAccumulator`，写出的谱行与其输出转 float32 逐位相同。
- 设计取舍：末行段数不足、末桶样点不足、丢弃尾样点是流结束的自然结果，**记进索引 `notes`，不降级**；
  产品级 `state` 只取被观测信号的四态。`iq/` 产品本步未做，组件暂不声明该参数（目录只增不改）。
- 实测：单音加噪声 10340 样点 → 40 行谱、21 行包络（末桶 340/500 样点）；同种子两次运行两种产品文件
  逐字节相同，换种子即不同；观察者收到 40 + 21 行回调，谱行逻辑时间单调。

#### 5. M-1 `matlab/` 骨架

`ref/`、`golden/`、`coder/`（空，待 M-2）、`run_all.m`（路径从文件位置推导）、`run_matlab.sh`（经环境变量
`MATLAB_ROOT`，铁律 17）。开发机实测 `matlab -batch` 一次约一分钟。

#### 6. 状态

引擎 37 个用例、12157 项断言全绿（含三方对拍）。路径与 ASCII 检查通过。八个组件进注册表。

#### 7. 本条目产生的待办

- [x] B-2 框图 JSON 装载器：按 `docs/diagram-format.md` 装 `Graph`，`observation_points[]` 并联成 `ObservationTap`，
      `IDataResolver` 解析 `data_id`，`internal` 参数出现即拒（2026-09-05 完成，见同日「B-2」条目）。
- [x] B-4 `cuav_run`：`--catalog`（生成首版目录黄金基准）、`--validate`、`--run`；stdout JSON 事件带 `seq`（2026-09-05 完成，见同日「B-4」条目）。
- [ ] M-4 余项：能量检测与 ADC 量化的 MATLAB 参考与黄金向量。
- [ ] 观测点 `iq/` 产品（按 `docs/iq-format.md` 写 `.iq` 与旁挂清单）。
- [ ] 产品行的 `t_s` 目前只对逻辑时间基准成立；回放源接入后要按块元数据的时间基准写索引。

---

### 2026-09-05 · 公开数据集许可确认（DS-1 / DA-1 销项）

用户确认：DroneRFb-DIR 与 DroneRFa 两个公开数据集**没有任何使用约束与署名约束**，可随交付系统分发。
06 §0.5 许可行、§11.4 DS-1、§11.5 DA-1 均记为完成。实施方此前未能自动取到 SciDB 页面的条款原文
（动态渲染），本条以用户确认为准。数据中心页仍按惯例显示数据集来源与编号，这是溯源要求（铁律 8），
不是许可要求。D-028 的口径不变：公开数据集只用于验证技术途径，量化结论不作交付指标。

---

### 2026-09-05 · 09 报告十三项暂定拍板（v1.2）

#### 1. 拍板

用户对 09 §15 的十三项暂定逐项表态：五项改定，八项采纳原案。

| # | 改定 | 报告落点 |
|---|---|---|
| 3 | 数据中心是独立页面，顶栏直达（`#/data`，`Alt+4`），不并入结果视图 | §3.3、§4.1、§7.3 |
| 9 | 视口基线按 2K 考虑：取 2560 × 1440 为基线，最小 1920 × 1080，折叠规则从 1920 起 | §2、§11 |
| 10 | 瀑布最新行在顶部、向下滚；频谱图紧贴瀑布上方，共用频率轴且列像素对齐 | §7.1、§7.2、§10 |
| 11 | 纵轴以 dBm 为目标：先按可得依据估一个标定常数并带来源徽标（实测 / 论文 / 假定 / 模型），没有任何常数才退到 `dBFS（未标定）`，绝不显示不带来源的 dBm | §7.2 标定常数行、§7.3、§13.2 |
| 13 | 验收集片段允许回放，界面提示（原案） | §7.3 |

「2K」按国内常用含义取 QHD 2560 × 1440；若指 DCI 2K 2048 × 1080 需另说。

#### 2. 连带改动

- `docs/display-products.md`：产品索引 `scale ∈ {dBm, dBFS}`，新增 `calibration{offset_dB, source, note}`；行值在有常数时已含偏移。
- `docs/api-versions.md`：数据中心端点返回 `calibration`。
- 06：U-3 验收改为「纵轴以 dBm 开头并带来源，合成链为『标定：模型』」；U-4 改为独立页；§11.2 加「入库当天估标定常数」规则，两个公开数据集的估算列为 DS-8 / DA-8 待办。
- CLAUDE.md：D-038。D-020 的「未标定」判据不变，显示层多了「带来源的估算常数」这一档。

#### 3. 同日销项

用户确认 DroneRFb-DIR 与 DroneRFa 没有任何使用与署名约束，DS-1 / DA-1 完成（见上一条目）。

#### 4. 本条目产生的待办

- [ ] DS-8 / DA-8：按论文参数（DroneRFa 接收增益 50 dB、天线 3 dBi；DroneRFb-DIR 论文所给参数待查）与满量程假设，为两批数据估算 `calibration_offset_dB`，来源标 `paper`，写进 `index.manifest.json` 并在 `docs/iq-format.md` §5 补字段定义。
- [ ] 引擎侧：`SpectrumAnalyzer` 与 `ObservationTap` 增加 `calibration_offset_dB` 与来源参数，产品索引写 `scale = dBm` 与 `calibration`；合成链定内部功率约定并标 `model`。排在 U-3 之前。
- [ ] 09 后半（切片 ③ 前）按 v1.2 的布局写画布与信号视图交互。

---

### 2026-09-05 · 系统定位再确认（D-039）

用户再次强调：本系统是技术验证的原型系统，用于技术路线的验证和功能演示，目的是证明本方在反无人机电子
侦察方面的行业理解和技术实力。这是 D-028「原型阶段」的延续，新增的是**目的**：不只是把链路跑通，还要
让每个环节能讲出背后的模型与依据。写进 CLAUDE.md 开头与决策 D-039、06 §0.1。

对后续工作的直接含义：切片 ①–④ 的可见链路仍是第一优先；09 报告里的链路读数、频谱仪约定、溯源面板从
「锦上添花」升为「行业理解的证据」，不得为省事砍掉；P2 的权限审计、分片上传、并发只做演示所需的最小集；
合成徽标、估算高度警示、「原型阶段验证值」这些标注保留并在演示时主动讲出来。

---

### 2026-09-05 · D-039 修正：可解释性对内、界面不展示溯源、标注低调

用户补充：「每个环节讲得出模型与依据」对内很必要，可在设计文档里强调，但系统里不给用户展示，这样更像交付
系统而不是实验系统；真实数据与合成标注同理，演示时不主动讲。

落法：铁律 8 的溯源数据仍随每个产物与事件走，不变；界面只在开发者模式（地址加 `?dev=1`，顶栏出「DEV」小标）
才有「溯源」按钮，默认 DOM 里没有任何溯源入口，e2e 加断言。合成场景标注由地图右上角的醒目徽标改为底图署名旁
的小字，估算高度比例改为默认折叠的数据包属性，铁律 14、15 的「显式标注、不静默」仍满足，只是呈现强度降低。
09 报告升 v1.3，CLAUDE.md 开头与 D-039 ②③ 相应改写（上一提交 `879da99` 只含 CLAUDE.md，其说明所述的
09 与 06 改动由本提交补上），06 §0.1 与 G-5 行同步。

---

### 2026-09-05 · B-2 框图 JSON 装载器

#### 1. 本次做了什么

新建 `engine/include/cuav/diagram_json.h`（130 行）与 `engine/src/diagram_json.cpp`（735 行），把 `docs/diagram-format.md`
定义的框图文件（`cuav-diagram/1`）装成可运行的 `Graph`：

- **结构校验**手写等价于 `docs/schemas/diagram.schema.json`：未知键在顶层、节点、连线、观测点、`run`、`scenario_ref`、`trace`
  七处一律拒绝；id 与端口名按正则；参数值只许 number / string / bool；`run` 的种子、时长、时间基准按规范取值。
- **节点**按注册表 `describe()` 分流参数（bool → 数值表 0/1，enum/string → 文本表），内部参数出现即拒（D-037）；
  `total_samples` 按 `run.duration_s × sample_rate_Hz` 四舍五入补齐、显式值超时长即拒；`run.block_size` 给未写块长的节点；
  `data_id` 经 `IDataResolver` 换成 `manifest_path` 注入；然后 `Registry::create_configured()` 构造。
- **连线**交给 `Graph::connect`；`graph.h` 新增 `LinkFault` / `GraphFault` 失败分类与两个重载，装载器据此把失败映射成带
  `{code, node_id, port, message}` 的 `DiagramError`，连线规则仍只在 Graph 一处解释，装载器不猜报文。
- **观测点**在 `IQStream` 输出口后并联 `ObservationTap`（图内名字 `op:<id>`），不改用户的边；`products` 映射到
  `spectrum` / `envelope` 开关，`iq` 明确拒绝。
- **两种解析器**：`MapDataResolver`（内存表或解析旁挂 `cuav-resolved/1`）、`IndexDataResolver`（读 `index.manifest.json`，
  按 `<索引目录>/<data_id>.manifest.json` 定位并核对存在，多份索引里同一 `data_id` 位置不同即拒）。
- `ObservationTap` 缺 `out_dir` 的检查从 `configure()` 挪到 `init()`：装载器不给 `out_dir` 即「只校验」模式，观测点照常构造
  并校验参数，运行到 `init` 才拒且报文含 `out_dir`。`test_tap.cpp` 相应改一处断言。
- 夹具 `engine/tests/diagrams/slice1_tone_noise_psd.json`：`docs/diagram-format.md` §8 示例的逐字副本。测试
  `engine/tests/test_diagram_json.cpp`（620 行，13 个用例）。

#### 2. 核实到的事实与踩到的坑

- 示例框图的参数名与端口名（`ToneSource.offset_Hz`、`AddMixer.a/b`、`SpectrumAnalyzer.in`、`window` 枚举）与现有目录一致，
  装载运行通过；`docs/diagram-format.md` §10 第一条待办的引擎侧一半销项，目录黄金基准仍待 B-4。
- **踩坑一（悬空指针）**：`IComponent::outputs()` 按值返回，`find_port(node->outputs(), port)` 拿到的是临时对象里的指针，
  表达式结束即失效。后果是观测点挂在 `SpectrumFrame` 口上没被 `observation_port` 拦住，读到垃圾类型后一路走到并联连线才被
  Graph 拒成 `graph` 兜底码。首轮测试抓到，改为先落局部变量再取指针。C++14 对「按值返回容器再取内部指针」没有语言级保护，
  后续评审要专门盯这一类写法。
- **踩坑二（检查顺序）**：`Graph::connect` 先查自环再查端口与占用，所以 `mix.out → mix.a` 报 `cycle` 而非 `input_occupied`。
  顺序本身合理（自环是更根本的错误），测试按引擎实际顺序改写，`input_occupied` 用 `noise.out → mix.a` 触发。
- 首批组件里没有 `SceneParamFrame` 输入口的组件，D-013 拒连测试用测试专用 `FakeApplier` 注册进临时 `Registry`；场景绑定
  分支用 `FakeBindable`。这也证明装载器只读注册表、不硬编码组件清单（D-030）。
- 数据索引记录里没有路径字段，`data_id → 旁挂清单` 的规则只由索引顶层 `directory` 与 `note` 的命名约定隐式给出；
  `IndexDataResolver` 把它写成代码并核对文件存在。
- 工作树在开工时是干净的（`git status` 0 项），快照里「约 50 个文件未提交」是过时描述，已删。

#### 3. 拍板（D-040，用户批准 B-2 计划即拍板）

| # | 口子 | 处置 |
|---|---|---|
| a | `data_id` 解析结果怎么到引擎 | 解析旁挂 `diagram.resolved.json`（`cuav-resolved/1`），框图副本永远不含内部参数；引擎对内部参数无条件拒绝 |
| b | `--validate` 时观测点缺 `out_dir` | `out_dir` 检查挪到 `init()`；装载器不给 `out_dir` 即只校验模式，不落盘 |
| c | `products` 含 `iq` | 本版本拒绝（`product_unsupported`），不静默忽略 |
| d | 错误码 | 首版 17 个，表进 `docs/diagram-format.md` §4；`Graph` 增加失败分类供映射 |

连带改动：`docs/diagram-format.md`（状态行、§3 补齐规则、§4 错误码表、§5 `iq` 与禁写参数、新 §9 解析旁挂、§10 待办）；
`docs/api-versions.md` B-5 行改为「写解析旁挂」；06 §0.3、§9A B-2 `[x]`、B-4 加 `--resolved / --data-index`、B-5 措辞、§14 记修正；
`engine/README.md` 现状表；CLAUDE.md 里程碑 B 行与 D-040。

#### 4. 量化结果（原型阶段验证值）

- 引擎单测 37 → 50 个用例，12157 → 12564 项断言，全绿；`scripts/build-all.sh`、`check-paths.sh`、`check-ascii.sh` 全过。
- 新增 13 个用例覆盖 17 个错误码中的 16 个（`graph` 是兜底码，正常路径不可达）。
- 示例框图：`total_samples` 补齐 2000000；谱 1953 行（1024 点一段，尾巴 448 样点记备注）、包络 489 行（4096 样点一桶）；
  只校验模式运行到 `init` 被拒，报文含 `out_dir` 与 `op:s4`。
- 回放夹具 5000 样点经 `MapDataResolver` 装载运行，产品索引 `trace_id = FileReplaySource:fx_replay_1`，谱 4 行。

#### 5. 本条目产生的待办

- [x] B-4 `cuav_run`：`--catalog`、`--validate <框图>`、`--run <框图> --out <dir> [--seed N] [--resolved <旁挂> | --data-index <索引>...]`；
      错误事件直接 `to_json(DiagramError)`；`--seed` 覆盖 `run.seed` 须写进事件；`--catalog` 生成 `tests/golden/component-catalog.json`。（2026-09-05 完成，见同日「B-4」条目）
- [ ] 回放节点的时长截断：`FileReplaySource` 的采样率读清单后才知道，`run.duration_s` 对它暂不生效（整片回放或显式 `max_samples`），B-4 或 P1-3 余项补。
- [ ] B-5：服务端写解析旁挂而不是含内部参数的框图副本（`docs/api-versions.md` 已改）；`docs/schemas/resolved.schema.json` 用到时一并写。
- [ ] 代码评审关注项：按值返回的容器再取内部指针（本条目踩坑一）。

---

### 2026-09-05 · B-4 `cuav_run` 可执行

#### 1. 本次做了什么

新建 `engine/tools/cuav_run/{runner.h, runner.cpp, main.cpp}`。逻辑放在静态库 `cuav_runner`，`main.cpp` 只做 argv 与进程退出码，
这样 doctest 能直接驱动 `run()` 检查事件流，不必起子进程。三个子命令：

- `--catalog`：输出 `catalog_json()`（`dump(2)`），先对每个组件跑 `validate_catalog_entry()`，自检不过就不输出。
  用它首次生成 `tests/golden/component-catalog.json`（8 组件、21763 字节）；新测试 `test_catalog_golden.cpp` 按
  `docs/component-catalog.md` §5 的「已有条目不变」比对：端口类型与兼容矩阵相同、基准里每个组件的 `type / ports / params` 相同，新增组件允许。
- `--validate <框图>`：`load_diagram_file()` 只校验模式（不给 `out_dir`，不落盘）；成功一条 `validate` 事件（节点、连线数、观测点、`run`），
  失败一条 `error` 事件（`to_json(DiagramError)`），退出码 2。
- `--run <框图> --out <目录> [--task-id] [--seed N] [--resolved <旁挂> | --data-index <索引>...] [--progress-interval-ms N]`：
  建目录、打开 `events.jsonl`、建解析器、装载、运行。事件依次为 `task.state running`（含种子与来源、节点、观测点、`started_utc`）、
  `log`、`progress`（墙钟节流，默认 100 ms）、每行一条 `product_row`（不带数据）、结束 `task.state finished`（结果四态、轮数、墙钟、
  实时因子、逐节点状态、`ended_utc`）；运行失败发 `error {code: run_failed, node_id}` 与 `task.state failed`，退出码 3。
- 事件信封与 WS 文本帧同构 `{seq, task_id, type, t_s, payload}`；stdout 与 `events.jsonl` 逐行 flush、逐字节相同。诊断走 stderr。
- 配套改动：`ObservationTap` 每写一行就 `fflush`（此前只在每 64 行更新索引时刷），服务端才能按 `product_row` 事件的 `row_index`
  立刻从文件读到这一行；`platform::utc_now_iso8601()`（`gmtime_s` / `gmtime_r` 放在平台层）；`LoadedDiagram.edge_count`。
- ctest 新增三项可执行冒烟：`cuav_run --catalog`、`--validate` 切片 ① 框图、`--run` 切片 ① 框图到构建目录。

#### 2. 核实到的事实与踩到的坑

- 三个测试写错、运行器本身没改：`--catalog` 的输出是多行目录 JSON 不是事件流，测试助手按行解析当然炸；把回放源塞进示例框图时
  没清掉单音的参数，装载器按键名顺序先撞到未知参数 `amplitude` 报 `param`，轮不到 `manifest_path` 的 `internal_param`；
  观测点用的是自己的 `nfft`（缺省 1024）不是下游分析器的 256，3000 样点只有 2 行谱。三处都改测试的预期。
- `progress` 回调每轮一次，运行 2 MS 样点默认块长约 31 轮，默认 100 ms 节流后只剩 1 到 2 条；`--progress-interval-ms 0` 时条数 ≥ 轮数
  （收尾轮也算）。因为节流按墙钟，**事件流不承诺逐字节复现，产品文件才承诺**（D-041 ③）。
- 运行失败的报文以节点名开头（`graph.cpp` 的「X 初始化失败 / 处理失败 / 收尾失败」），运行器据此把 `node_id` 找回来给画布高亮；
  匹配要求名字后紧跟空格，避免 `mix` 撞上 `mix2`。
- `task_id` 缺省取 `--out` 的末级目录名，与 `data/runs/<task_id>/` 的约定吻合；校验模式取 `diagram_id`，装载失败时取文件名主干。

#### 3. 约定（D-041）

事件信封与 `events.jsonl` 镜像、`product_row` 不带数据 + 观测点逐行刷盘、`progress` 墙钟节流、退出码 0–4 与结果四态正交、
`--seed` 覆盖须写来源、`--catalog` 不加 `generated_at`、`--scenario-track` 留 G-2。全文见 CLAUDE.md D-041 与 `docs/api-versions.md` §4.1。

#### 4. 量化结果（原型阶段验证值）

- 引擎单测 50 → 57 个用例，12564 → 27388 项断言，全绿；ctest 4 项（单测 + 3 冒烟）全过；`scripts/build-all.sh`、路径与 ASCII 检查全过。
- 切片 ① 框图 `--run`：2442 条 `product_row`（谱 1953 + 包络 489），`events.jsonl` 与 stdout 逐字节相同，序号 1 起连续，
  `task_id` 取目录名；同种子两次运行 `spectrum.f32` 逐字节相同；`--seed 7` 时 `seed_source = cli` 并有覆盖日志。
- 回放框图经 `--data-index` 与 `--resolved` 两种入口产品逐字节相同；两者都不给时 `error data_id` + `task.state failed`，退出码 2，
  失败也镜像到 `events.jsonl`。
- 目录黄金基准 `tests/golden/component-catalog.json`：8 组件、6 端口类型、36 行兼容矩阵、21763 字节。

#### 5. 本条目产生的待办

- [ ] B-5 服务任务管理器：`spawn('cuav_run', [...])` 数组参数不走 shell；任务目录 `data/runs/<task_id>/`；提交时写解析旁挂
      `diagram.resolved.json` 并以 `--resolved` 传入；读 stdout 事件转 WS，`product_row` 按偏移读文件转二进制帧；
      `GET /api/v1/components` 缓存 `--catalog` 输出；Windows 上路径含空格与中文、子进程 stdout 编码、孤儿进程三项风险（06 §9E）要专门验。
- [ ] `cuav_run` 的取消：目前没有信号处理，服务端取消只能杀进程；产品索引 `rows` 最后一次更新可能落后不到 64 行，B-5 收尾时按文件长度修正或让引擎收 SIGTERM 后写索引。
- [ ] `--scenario-track`（G-2）；`detection` 事件（P1-4d）；回放节点的时长截断（B-2 遗留）。

---

### 2026-09-05 · D-042b：界面不解释建筑高度来源与估算（原编号 D-042 与 B-5 条目重号，改为 D-042b）

用户看到当前场景视图后指出：本系统本来就是演示系统，并不保证建筑物高度是实际值，用户与本方都不关心，
没必要在界面上过多解释；只要有比较合理的数据可用于后续计算即可。

落法：`SceneView` 侧栏删去黄色估算警示框与来源占比行，「按高度来源分色」开关与占比只在 `?dev=1` 开发者模式出现
（带 `data-dev`，与 D-039 的开发者模式同一入口）；端到端断言由「估算高度有显式标注」改为「界面不解释估算高度且无
`[data-dev]`」，默认页 14 项全过，带 `?dev=1` 时同一断言按预期报出 1 个开发者元素；死样式 `.scene-warn` 删除。
铁律 14 改写为「标记留在数据层，界面不解释」：`buildings.geojson` 的 `src` 字段、场景包清单与 `docs/scene-package.md`
的记录一律不动，任何计算与统计仍能区分估算值；「不得用于验收指标」写在文档与模型卡里。09 报告升 v1.4（§0 第 9 条、
§5 示意图、§5.1、§14）。合成场景的小字署名暂按 D-039 保留，用户若也不要再去掉。

前端 6 项单测不变；`web/dist` 已重建，运行中的服务直接托管新产物。

---

### 2026-09-05 · D-043：去掉「合成场景」小字署名

用户问清「合成场景的小字署名」是什么（09 报告为切片 ② 预留的、底图署名旁的三个字，`synthetic = true` 时出现）后指示去掉。
这是 D-042b 的延伸：演示系统不必向用户解释站点与航迹是编的还是录的。落法：场景文件的 `synthetic` 字段保留并随产物走
（数据层，铁律 14）；界面不做任何标记。改动全在文档：09 报告 v1.5（§0 第 9 条、§3 试验详情、§5 示意图与 §5.3、§12、
§14 探针去掉 `badges.synthetic`），06 §9C G-5 验收项、§9E 切片 ② 与风险摘要、§13「不做」第 5 条改为「不把合成结果当实测
结论写进文档」，`docs/scenario-format.md` 的 `synthetic` 字段说明，CLAUDE.md 定位段、G 行与 D-043。代码尚未写到 G-5，无代码改动。
至此界面上不再有任何「向用户解释数据有多不完美」的元素；后续这类元素默认不做，除非用户点名要。

### 2026-09-05 · B-5 服务任务管理器

#### 1. 本次做了什么

新建 `server/src/tasks/{engine,resolve,store,manager,routes}.ts`（06 §9A 原写 `{manager,runner,store}`，按职责收成五个）与测试夹具
`fake_engine.mjs`、`testkit.ts`；`index.ts` 接入任务路由并在 `start()` 里做任务扫描、对账与退出处理器。端点按
`docs/api-versions.md` §3.1a：`GET /api/v1/components`（缓存 `--catalog`，附 `generated_at`）、`POST /api/v1/tasks`、
`GET /api/v1/tasks[/{id}]`、`POST /api/v1/tasks/{id}/cancel`；健康检查加 `engine: {available, version?}`。

提交流程：JSON 与限长（1 MB）→ 最小结构检查 → 节点与观测点里出现 `internal` 参数即 400（D-037）→ 回放节点 `data_id` 按
`data/iq/*/*/index.manifest.json` 解析成相对清单路径写旁挂 `diagram.resolved.json`（命中验收集只记 `warnings[]`，D-038）→
落盘 `diagram.json` 并算哈希 → **同步 `cuav_run --validate --task-id <id> [--resolved …]`**，失败删目录、400 带引擎四字段 → FIFO 入队。
`Idempotency-Key` 同键同框图 200 返回同一任务，同键不同框图 409。

运行与终态：`spawn` 数组参数、cwd = 仓库根、所有路径仓库相对纯 ASCII 字面 `/`；stdout 按字节切行去 `\r`；每行先整行脱敏
（清单路径 → `data_id`，剥仓库根前缀含 JSON 转义的 Windows 形式）再解析，折进 `task.json` 与每任务 4096 条环形缓冲，
`subscribe()` / `events(since, limit)` 留给 B-6 加传输。引擎给了 `task.state` 终态就信它；没给则已请求取消 → `cancelled / not_applicable`，
其余 → `failed / invalid` 带 `stderr_tail`，并由服务端补一条 `task.state`（`source = server`）接在引擎序号之后。取消 = SIGTERM → 3 s → SIGKILL。
服务收 SIGINT / SIGTERM 同步杀引擎并把运行中任务标 `failed`「服务停止时任务被中止」；启动扫 `data/runs/*/task.json`，遗留的
`queued / running` 按 `events.jsonl` 尾部最后一条 `task.state` 对账，没有就 `failed`「服务重启时任务未结束」。

规范同步：`docs/api-versions.md` §3.1a（新）、§3.3、§4、§6；`docs/diagram-format.md` §1、§9、§10；`docs/display-products.md` §1、§1.1（新，`task.json` 字段）；
新建 `docs/schemas/resolved.schema.json`；`server/README.md`。

#### 2. 核实到的事实与踩到的坑

- **相对路径 + cwd 是可行的**：核实 `runner.cpp` / `platform.cpp` / `diagram_json.cpp` / `sources.cpp`，`--out`、框图、旁挂、清单、`.iq` 段全部经
  窄字符相对路径打开，`basename_of("data/runs/<id>")` 得 `<id>`；`platform::make_dirs` 只按 `/` 切分，所以引擎参数禁止用 `path.join` 拼。
  这一条把「Windows 上路径含空格与中文」的风险在源头消掉：引擎永远见不到根目录，Unicode 的 cwd 由 Node 处理。
- **真引擎抓到一个假引擎放过的缺陷**：首版把检查用的「精简视图」序列化落盘，观测点丢了 `node / port / products`，真 `--validate` 报
  `schema: 观测点 s4 缺必填字段 node`；同一根因让幂等哈希对不上。改为落盘与哈希都用原始框图。教训：夹具只能守状态机，格式正确性必须靠真引擎的集成测试。
- **终态与退出码之间有几毫秒**：`run_state` 在引擎最后一条 stdout 事件时就成终态，`exit_code` 要等进程关闭。语义上前者才是界面要的，
  所以只改测试等两者都齐。
- `--validate` 缺省 `task_id` 取文件名主干，不显式传会落成 `diagram`；两处都恒传 `--task-id`。
- 只脱敏 `payload.message` 有漏洞：`task.state.reasons[]` 与 `nodes[].notes[]` 复制同一段文字，改为整行替换后再 `JSON.parse`。
- 取消时 `rows_seen` 可能比文件少一行（引擎先刷盘再发事件，杀在两者之间），B-7 读端一律以文件长度为准；`task.json` 只记下界。
- 假引擎写 stdout 必须用 `writeSync`：管道上的 `process.stdout.write` 在部分平台异步，`crash` 模式会丢事件。

#### 3. 约定（D-042）

同步校验即 400、相对 ASCII 路径与 cwd、整行脱敏唯一入口、串行队列、终态判定与服务端补发、退出收尾与对账、服务端不复刻 schema。
全文见 CLAUDE.md D-042 与 `docs/api-versions.md` §3.1a。

#### 4. 量化结果（macOS 开发机，原型阶段验证值）

- 服务测试 30 → 58 项全绿（引擎封装 6、解析与脱敏 4、任务管理器 13、HTTP 端点 4、真引擎集成 1）；`npm run typecheck`、`scripts/build-all.sh`、
  路径与 ASCII 检查全过；引擎 ctest 4 项不变。
- 真引擎经任务管理器：`--validate` 约 9 ms；切片 ① 框图 `rows_seen` 谱 1953 / 包络 489（与 B-4 命令行一致），`events.jsonl` 序号 1 起连续且行数 = `last_seq`，
  墙钟 0.14–0.19 s、实时因子 11–15；事件里无仓库根路径。
- 真服务冒烟（`dist/index.js`，端口 8091）：8 组件目录；幂等 200；真实片段 `dronerfb_0_CH0_S4`（4 M 样点）回放 `finished / degraded`，谱 3906 / 包络 977 行，
  4888 条事件，旁挂写相对路径，`events.jsonl` 与 `task.json` 都不含 `/Users/`；`internal_param`、`input_occupied`（来自引擎）、`data_id` 三种 400 不留目录；
  120 s 长任务跑出 704 行后取消 → `cancelled / not_applicable / SIGTERM`，再取消 409；运行中 SIGTERM 服务 → 退出码 143、任务 `failed`「服务停止时任务被中止」、
  引擎已死（`events.jsonl` 不再增长）；重启后 4 个任务状态如前、幂等键恢复。

#### 5. 本条目产生的待办

- [x] B-6 WS 服务端与补取（2026-09-05 完成，见同日「B-6」条目）：`ws` vendored；`/ws` 订阅 `{subscribe, since}`；`GET /api/v1/tasks/{id}/events?since&limit`（缓冲内走 `events()`，缓冲外读
      `events.jsonl` 经同一脱敏入口）；`product_row` 按 `row_index × row_len × 4` 读 `<op_id>/<kind>.f32` 转二进制帧；心跳与 `dropped`。
- [x] B-7 视窗抽取：读端以文件长度定行数，不信 `rows_seen` 与索引 `rows`（2026-09-06 完成，D-046）。
- [ ] Windows 原生验证（阶段 0 交付平台基线冻结后）：`\r\n` 切行、TerminateProcess 取消、含中文与空格的仓库根。
- [ ] 引擎侧（B-4 遗留）：`cuav_run` 收 SIGTERM 后写索引再退出；回放节点的时长截断。

### 2026-09-05 · B-6 WebSocket 服务端与补取

#### 1. 本次做了什么

随包 `ws 8.21.3`（MIT，精确锁版；`@types/ws 8.18.1` 只在开发期），新建仓库根 `THIRD-PARTY-NOTICES`（D-032）。新建 `server/src/ws/{events,hub}.ts`：
`events.ts` 是二进制帧编解码、连接级报文与按偏移读产品行的 `RowReader`；`hub.ts` 把 `WebSocketServer({noServer})` 挂到 http 服务的 `upgrade` 上，
每个连接一个会话：收 `{subscribe: task_id, since}` → 先 `mgr.subscribe()` 再应答 `subscribed{since, last_seq, run_state}` → 用 `readEvents()` 分批回放
到空批次 → 转实时抽队列（`seq ≤ cursor` 即重复跳过）。`product_row` 在 WS 上一律转二进制帧（`[u32 LE header_len][JSON 帧头补齐到 4 字节][Float32 原字节]`，
帧头 `{seq, task_id, op_id, kind, row_index, row_len, t_s}`），读不到行退回文本事件。背压只丢二进制帧：`bufferedAmount` 超 1 MiB 时不读文件、把序号并进
待告知区间，在发送下一帧之前先发 `dropped{from, to, count}`；文本事件永不丢；超 16 MiB 以 4013 断开。心跳 15 s 一条 `heartbeat{last_seq}` 加 `ping`，
两轮没 `pong` 即 `terminate`。

管理器新增 `readEvents(taskId, since, limit)`：缓冲内切片；缓冲外 `store.readEventLines()` 顺序读 `events.jsonl`（复用 `splitLines`、经同一脱敏器、丢末尾半行）；
文件与缓冲紧接时才拼接；重启后按 `task.json` 合成服务端终态。HTTP 端点 `GET /api/v1/tasks/{id}/events?since&limit` 直接用它，`product_row` 在这里恒为文本。
`reconcile()` 服务端定终态时把 `last_seq` 推到文件末序号 + 1，与进程内 `emitServerState()` 同规则。服务收 SIGINT / SIGTERM 改为先关 WS 连接（1001）再退出。
假引擎每行 `product_row` 前先写 `<op_id>/<kind>.f32`（值 = 行号 × 1000 + 列号），新增 `many`（40 行）与 `norows` 两种模式。

规范同步：`docs/api-versions.md` §3.1a、§3.3、§4（新增 §4.0 实现约定）、§6；`docs/display-products.md` §4；06 §9A B-6、§0.3、§14；`server/README.md`；
CLAUDE.md B 行、环境与命令、D-044。顺手把决策日志里与 B-5 重号的第二条 D-042（界面不解释建筑高度）改为 D-042b（先例 D-027b），09 报告、`SceneView.tsx`、
e2e、本日志与项目记忆里的引用同步改。

#### 2. 核实到的事实与踩到的坑

- `readline` 会把文件末尾没有换行的半行当整行吐出（实测 `['a','b','partial']`），而引擎可能正在写那一行；改用字节级 `splitLines` 并丢弃残片。
- `ws.send()` 不能逐条 `await`：等回调会让 `bufferedAmount` 始终接近 0，积压只是转移到本进程的队列，背压判据失效。一律 fire-and-forget，按 `bufferedAmount` 判断。
- 回放循环要以「空批次」收口而不是「不足 limit」：文件与缓冲之间可能暂时有空洞（缓冲已淘汰、文件尚未含那几行的极短窗口），`readEvents` 遇空洞不拼接、
  返回已有的，下一轮再读文件即可；按「不足 limit」收口会拼出缺号。
- `slow` 模式只有 16 条事件，测不出缓冲淘汰；加 `many` 模式并把测试用的深度压到下限 16。
- `WebSocketServer.close()` 在 `noServer` 模式下不关客户端连接，`http.Server.close()` 也不管已 upgrade 的 socket，必须自己遍历关，测试进程才能退出。
- 服务端补发的终态事件不在 `events.jsonl` 里（那是引擎的文件），重启后缓冲为空，补取必须按 `task.json` 合成；为此对账时要把 `last_seq` 推到文件末序号 + 1，
  否则合成事件的序号与进程内的不一致。
- Node 内置 `WebSocket`（undici）作测试客户端够用：`binaryType = 'arraybuffer'`、`close` 事件带 44xx 码与原因；它自动回 `pong`，漏 pong 断开那项用 `ws`
  客户端的 `autoPong: false`。
- 非 `/ws` 路径的 upgrade 直接写 `HTTP/1.1 404` 并销毁 socket，浏览器侧表现为 1006；普通 HTTP `GET /ws` 仍由路由 404。
- 服务退出时若直接 `process.exit`，客户端只会看到 1006；`installShutdownHandlers` 加了「先等 hub 关连接、最多 1.5 s」的口子，冒烟里客户端收到 1001。

#### 3. 约定（D-044）

帧布局与补齐、`seq = 0` 连接级报文、WS 上 `product_row` 恒二进制 / HTTP 上恒文本、`dropped` 连续且先于下一帧、关闭码、已结束任务的订阅建议。
全文见 CLAUDE.md D-044 与 `docs/api-versions.md` §4.0。

#### 4. 量化结果（macOS 开发机，原型阶段验证值）

- 服务测试 58 → 72 项全绿（WS 帧与读取 4、WS 服务端 9、HTTP 端点 4 → 5）；`npm run typecheck` 过；`scripts/build-all.sh`（含路径与 ASCII 检查）全过；
  端到端 `scene-smoke.mjs` 14 项不受影响。
- 真引擎集成：切片 ① 完成后 WS 从 `since = 0` 回放 2447 条事件（2442 帧二进制 = 谱 1953 + 包络 489）用时 82 ms，序号 1..2447 连续，本机回环无 `dropped`，
  末帧字节与 `spectrum.f32` 末行相同；`--validate` 约 4 ms。
- 真服务冒烟（`dist/index.js`，端口 18086）：提交切片 ① 201；连接 A 订阅时任务已跑到 `last_seq = 66`，收 300 帧后主动断开（最大序号 303）；连接 B 以
  `since = 303` 重连，首条正是 304，收 2142 帧 + 2 条文本到终态用时 126 ms；两段并集 1..2447 无缺号无重复，等于 `task.json.last_seq`；
  `GET .../events?since=303&limit=100` 首条 304、`product_row` 为文本；保持一个订阅连接时向服务发 SIGINT，客户端收到关闭码 1001，服务退出码 130。

#### 5. 本条目产生的待办

- [x] B-7 视窗抽取：读端以文件长度定行数（2026-09-06 完成，D-046）。
- [ ] `readEventLines` 每批从头扫（O(n)），文件超过十万行时加字节偏移记忆。
- [ ] U-1 `web/src/api/ws.ts`：按 §4.0 实现客户端（`seq = 0` 不推进 `lastSeq`、`dropped → lastSeq = to`、缺号即 HTTP 补取、退避重连带 `since`）；
      已结束任务用 `since = last_seq` 订阅、历史走 B-7。
- [ ] Windows 原生验证追加：引擎追加 `.f32` 时服务端并发读同一文件（共享模式）、`terminate()` 后句柄释放；
      再加一项（2026-09-06，B-7）：引擎 `platform::atomic_replace`（`MoveFileExA`）没有重试，服务端若恰在那一瞬打开着 `index.json`，替换会失败并让任务进 `Step::Error`。

### 2026-09-05 · D-045：实测数据集作为辐射源模板——评估、规范草案与 T 线（只记录，不实现）

**起因**。用户提出一个想法：现有数据集能否作为将来模拟特定无人机信号的基础——假定目标无人机与数据集
机型参数匹配，按无人机的距离、方位与发射机性能，用数据集里的信号特征，叠加传播环境与侦察设备性能，
给出针对该无人机的信号级（IQ 级）侦测模拟。随后用户确认「数据集未记录的量都可以按实际情况设置，不是
问题」，并指示先把讨论定下的决策记下来、更新文档与计划，留待指导后续实现。

**核实到的事实**（引自文档）。这条路线在设计文档里早已预留：04 §7.2 定义了三类辐射源模型——参数化
合成、实测片段回放（「高信噪比、元数据完整的实测 IQ 作为辐射源模板，进行循环、拼接、重采样、频移和
功率归一化」）、半经验特征等效（「根据实测数据拟合带宽、谱形、突发时长、重复周期、占空比、频偏和统计
分布，生成不依赖真实协议内容的相似信号」）；04 §4.3 把「混合增强」列为首期投入产出比最高的能力，
§11.5 的公式是「机理模型 + 实测参数分布 + 实测残差或背景片段 = 接收机观测域等效模型」；02 §7.1 要求
波形模板支持「实测片段引用」、§7.2 的波形来源含「实测片段」；03 §12.2.2 给出实测回放 / 实测特征驱动 /
协议机理三层梯子。

**核实到的事实**（实测，代码现状）。代码上一件未做：组件目录 8 个组件全在 source / data / algorithm
三类，channel / antenna / receiver 三类为零；`SceneParamFrame` 有字段但无生产者与消费者；`geo/` 只有
占位文件；`models/` 六个子目录全空；场景规范的 `emission.waveform` 只有 tone / noise / burst；
`FileReplaySource` 没有重采样、增益与频移参数。两个数据集在链路里的位置是 S4 观测点的**输出**：
它们能验证下游，不能反推产生它们的输入参数（发射功率、真实距离、各项损耗、接收增益都没有记录）。

**一次抽样观察**（实测，只用来定探针口径，不作结论）。取 DroneRFb 训练集 6 片背景与 6 个机型各 1 至
2 片，按 8192 点汉宁功率谱、1 MHz 栅格、相对背景中位谱看超出量：背景里 WiFi 信道 1（2401 至 2417 MHz）
高出底噪 13 至 20 dB，是整个 80 MHz 里最吵的一段，与 DS-6 的结论一致；无人机图传在各片里表现为
10 至 20 MHz 宽、高出同站背景 15 至 23 dB 的连通段，**位置随片变化**（同一机型的相邻两片分别落在
2453 至 2471 MHz、2422 至 2428 MHz、2449 至 2460 MHz 等处），说明图传信道在片与片之间切换，模板必须
按片定位频段并汇总成信道集合，不能用一个固定频段；非视距片（`G1_OUT`）同频段的超出量降到 5 至 15 dB。
这些数字来自十几片的目视，未做统计，探针（T-0）做的就是把它们系统化。

**决策 D-045**（用户确认）。① 可以，且是既定路线；② 三种模式都要，首选特征等效为主线，回放模板作
近距离高保真对照，实测背景 + 合成目标作背景；③ 数据集能给波形模板、真实背景、接收机非理想样本、
路损粗档与可实测的视距 / 非视距附加损耗分布；数据集未记录的发射功率、位置与方位、天线增益、噪声系数、
接收增益都是场景与设备的配置项，按实际情况设置即可，不是障碍；模板归一化到单位功率，绝对电平由配置
经链路预算得出，配置值带来源字段随产物走、界面不显示；④ 唯一的技术约束在回放模式：片段自带的底噪与
同频背景随信号一起缩放，回放模板能呈现的最大信噪比 = 模板余量 − 裕度，不能向比录制更高的信噪比推，
往远处推反而越干净；⑤ 公开数据集片段仍不得放到地图上，模板与位置无关、可绑定场景，
`FileReplaySource.scene_bindable = false` 不变，将来的 `TemplateSource` 才可绑定；⑥ 本期只记录不实现。

**一处自我纠正**。计划初稿把回放模式的约束写成「可推距离有上限」，方向反了：片段缩放到更远距离时，
自带的底噪与背景被压到新底噪之下，结果反而更干净；真正的约束是信噪比上限（不能向近处、向高信噪比推）。
规范与决策都按纠正后的表述写。

**写了什么**。新建 `docs/emitter-template.md`（模板定位、三模式、供给表、`cuav-emitter-template/1`
字段、提取口径草案、回放模式的信噪比上限、进入仿真链的规则、验证与校准、待写）；
`docs/concept-model-extensions.md` §3 登记首条扩展（EM-B-09 / EM-T-03 的既有参数允许来源为实测模板，
不新增字段，保持「不生成 IQ」边界）；`docs/scenario-format.md` §4 / §9 登记 `template` 波形类型与
`tx_power_source`；`docs/iq-format.md` §9 登记模板再生产物的元数据；`docs/README.md` 加一行；
CLAUDE.md 新增 D-045、「已定」表「辐射源模板」行、里程碑表 T 行、文档体系 docs 行；06 备忘录新增
§9F T 线（T-0 探针、T-1 规范、T-2 记录、T-3 提取器、T-4 `TemplateSource`、T-5 闭环与模型卡）、
§0.3 树与当前位置、§9D M-5 输入、§11.3 支撑范围、§11.4 / §11.5 角色 ⑥、§14 修正记录。
未写任何代码；`scripts/check-paths.sh` 与 `scripts/check-ascii.sh` 通过。

**下一步**。切片 ① 继续 B-7 视窗抽取 → U-1 三视图壳 → U-3 信号视图，不变。T-0 探针不依赖引擎，
待用户排期；它回填的阈值（占用判据、突发门限、裕度、各机型的模板余量与污染分布、视距 / 非视距附加
损耗分布）决定 `docs/emitter-template.md` §5 何时冻结。

### 2026-09-06 · B-7 视窗抽取端点：从产品文件到浏览器要的那几百个像素

**做了什么**。给应用服务加上回看与缩放需要的抽取端点（06 §9A B-7）。此前浏览器只能收 WebSocket
实时推来的产品行（B-6），一旦断线、缩放或者想看任务开头，就没有任何取数的办法。现在有六条路径：
谱与包络的二维抽取、两种产品各自的索引端点、以及 `track` / `links` / `detections` 三个时间窗
JSON 端点。`scatter` 明确返回 404 并说明理由——观测点本版本根本不产出 `iq` 产品（框图装载器拒绝
`products` 里的 `iq`，D-040 第三条），没有数据源的端点不该假装存在。

代码分七个文件放在 `server/src/products/`，按「纯函数 / 有 I/O」切开：`window.ts` 只做行列选择与
分组边界，`spectrum.ts` 与 `envelope.ts` 只做归约，`source.ts` 按偏移分块读文件，`meta.ts` 读索引
并按文件长度定行数，`jsonl.ts` 是三个 JSON 端点共用的时间窗读取器，`routes.ts` 才碰 HTTP。这样切
不是为了好看：**归约必须能与 Python 参考逐行对译**，掺进 HTTP 或文件句柄就对不了。

**归约怎么定的**（细节进 `docs/display-products.md` §3.1，决策 D-046）。区间一律半开；行按起始时刻
取 `floor` 与 `ceil`，列按 bin 中心 ± 半格；分组边界 `B[g] = floor(g·n/m)` 是纯整数运算，与浮点无关，
所以两种语言必然给出同样的分组。统计量里只有一处需要判断：`mean` **在线性功率域聚合**再转回 dB
（铁律 5：功率聚合在线性域），不是对 dB 值取算术平均——后者在有强信号的组里会把峰值抹平十几分贝。
包络合桶的 rms 按样点数加权，末桶只有在索引已收尾时才按 `last_bucket_samples` 计权。

**逐值一致的验收怎么做的**。参考实现 `algos/reference/product_window.py`，只用标准库。两级验证：
① 黄金基准 `tests/golden/product-window.json`，一份合成谱（37 行 × 64 bin，含 −300 底噪格）与一份
合成包络（23 桶，末桶 37 样点）上的 12 个查询用例；文件里**只存公式与输入的 sha256，不存输入本身**，
TypeScript 侧按同一公式再生成输入、核对哈希后再比输出，比的确实是归约逻辑而不是夹具。
② 真引擎对拍：跑切片 ① 框图产出 1953 × 1024 的谱与 489 桶的包络，对三组视窗分别让两侧算一遍。

**实测结果（macOS 开发机，原型阶段验证值，D-028）**：

| 对拍项 | 规模 | 逐位不同 |
|---|---|---|
| 全窗 `max` 直通 | 1953 × 1024 = 1999872 值 | 0 |
| 时间窗 + 频段 + `mean` | 150 × 308 = 46200 值 | 0 |
| 包络合桶（末桶按 37 样点计权） | 97 × 3 = 291 值 | 0 |

合计 2046363 个值**全部逐位相同**，`mean` 那一组也是——V8 的 `Math.pow` / `Math.log10` 与
macOS libm 在这批数据上给出同一个 float64。测试里仍保留 1 个 float32 ulp 的容差，因为这一致性
是平台相关的，Windows 的 MSVC libm 未必如此；那是留给 D3 首次 Windows 构建时验证的项，不是
现在放宽标准。走 HTTP 再对拍一次（`curl` 取回的响应体与 Python 的 `--out` 文件 `cmp`），
`max` / `mean` / `min` 与包络四组都逐字节相同。

端点时延（三次取中位，含 `curl` 自身开销）：

| 查询 | 时延 |
|---|---|
| 1953 × 1024 全窗 `max` | 26 ms |
| 1953 × 1024 全窗 `mean`（200 万次 `pow`） | 71 ms |
| 5632 × 1024 取 4802 × 873 `max` | 70 ms |
| 489 桶包络取 600（不插值） | 4 ms |
| 索引端点 | 0.5 ms |

**核实到的三件事，两件改了设计**。

其一，**索引在任务开头根本不存在**。`ObservationTap` 只在写完第 64、128… 行和收尾时写索引
（`(rows & 63) == 0`），`init()` 只建 `.f32`。原来 `docs/display-products.md` §2 写的是「读端只读
`rows` 以内的行」，可任务刚开始那几十行连索引都没有，读端连 nfft 和采样率都不知道，什么也读不了。
改了两处：引擎**写完第一行就刷一次索引**（`rows == 1 || (rows & 63) == 0`），把这个窗口从 64 行缩到
毫秒级；服务端在索引确实还没出现时回 **409 `not_ready` 加 `Retry-After: 1`** 而不是 404——运行中的
任务「还没到」不是错误，客户端重试即可。规范 §2 相应改为「读端以文件长度定行数，`rows` 只用来判断
索引是否收尾」，与 §1.1 早已写下的口径统一（原来两处是矛盾的）。

其二，**Python 的 `sum()` 不是顺序累加**。CPython 3.12 起内置 `sum()` 对 float 采用 Neumaier 补偿
求和，numpy 的 `sum` 是成对求和，两者都不是「acc = acc + x」，与 JavaScript 的累加逐位不同。线性域
`mean` 恰好要累加成千上万个数，若参考实现顺手写 `sum(...)`，对拍就会在小数末位上一直差着而查不出
原因。参考实现因此写显式 `for` 循环，并把理由写在文件头和 `algos/README.md` 里。本机实测
`sum([0.1]*10) == 1.0` 为真，可见补偿求和确实在起作用。

其三，**取消的任务是现成的「索引落后于文件」夹具**。盘上 `t20260905-143440-f5f3` 的包络索引记 1344
行而文件有 1392 行，端点如实报 `rows_available: 1392`、`index_final: false`，末桶因此按满桶计权——
这正是第一条规则要处理的情况，不必另造夹具。

**测试**。服务端新增 30 项（合计 102 项全绿）：纯函数的选择与分组、黄金基准 12 用例、文件行源的分块
与短读、索引的就绪语义与字段校验、JSONL 的闭区间与按键抽稀、HTTP 层的状态码与响应头与 HEAD 与 413，
以及「参数校验先于就绪判定」——坏参数在任何服务端状态下都回 400，不被 409 盖住，否则客户端会拿着
一个永远不可能成功的查询按 `Retry-After` 一直重试。
引擎新增 1 项（合计 58 项全绿）：写完第一行后索引已存在且 `rows == 1`。真引擎与 Python 参考的对拍
测试在缺引擎二进制或缺 `uv` 时跳过并说明，照 B-5 的做法。`scripts/check-ascii.sh` 与
`scripts/check-paths.sh` 通过。

**一处自我纠正**。计划里写「`px` 缺省取原始列数与上限中的较小者」，实现时发现单这样定，一个不带
参数的请求在 nfft ≥ 4096 时就会撞上 16 MB 上限直接 413——缺省值把用户送进错误里，是设计缺陷不是
用户的错。改为缺省 `py` 再受 `floor(16 MiB / (列数 × 4))` 约束，**不带参数的请求永远不会 413**；
显式给的 `px` / `py` 超限才 413，并给出按 `sqrt` 缩放的 `suggest`（实测建议值 873 × 4802 = 16768584
字节，确实一次就过）。

**下一步**。桥接线 B 到此七步全部完成，服务端不再是切片 ① 的短板。转前端：U-1 三视图壳（含
`web/src/api/ws.ts`，按 `docs/api-versions.md` §4.0 实现 `seq = 0` 不推进、`dropped → lastSeq = to`、
缺号即 HTTP 补取、退避重连带 `since`），然后 U-3 信号视图——实时行走 WebSocket 二进制帧追加，
缩放与回看走今天这批端点。

**新增待办**。Windows 原生验证时加一项：引擎写索引用的 `platform::atomic_replace`（`MoveFileExA`）
没有重试，若服务端恰在那一瞬打开着 `index.json`，替换会失败并让任务进 `Step::Error`。读端现在只用
`readFile`（开—读—关，毫秒级），撞上的概率很低，但 Windows 上必须实测确认，必要时给引擎侧加重试。

### 2026-09-06 · U-1 三视图壳与 dBm 纵轴链（D-047）：界面像交付系统，电平像真实接收机

**任务**。用户指示「进行下一步 U-1 三视图壳，并且纵轴以 dBm 显示，标定来源徽标不给用户显示，体现真实性」。
开工前探查（三路并行）核实到三件事：前端只有场景视图，路由、store、REST / WS 客户端、信号代码一件没有，
但 09 报告附录 A 与 §10 已把 store 形状、数据流与探针字段写死；**dBm 目前不可达**——引擎 `tap.cpp` 无条件写
`scale = "dBFS"`、不写 `calibration`，两批数据索引与逐文件清单都没有标定常数（DS-8 / DA-8 未做）；以及
**一处缺陷**：`FileReplaySource` 把 int16 量化码直接当样点、未除满量程，回放产物的谱值比真正的 dBFS 高
`20·log10(32768) = 90.31 dB`（盘上 `t20260905-143440-ae58` 的谱最大 +55.7 dB）。没有 golden 依赖回放样点，
按铁律 10 记发现、修正，不算基准变更。

**决策 D-047**（写入 CLAUDE.md）：引擎内部功率单位 = mW，样点 `|x|² = 功率 / mW`，`10·log10` 直接是 dBm；
合成源新增 `level_dBm` / `power_dBm`（与线性参数互斥，目录 `excludes[]`，装载错误码 `param_conflict`）；回放源
在源端按清单 `power.calibration.full_scale_dBm` 换算，无常数只除满量程并保持降级；`AddMixer` 取较弱来源；
观测点索引谱 `scale = dBm` + `calibration`、包络 `sqrt_mW`；**界面只标 `dBm`，来源徽标、常数与出处只在
`?dev=1`**——这是对 D-038「带来源徽标」显示层的修正，与 D-042b / D-043 同向，数据层「绝不产生不带来源的
dBm」不变。示例框图改成真实量级：−70 dBm 单音、−104 dBm 噪声（热噪声 @1 MHz −114 dBm + 噪声系数 10 dB）、
中心 2.44 GHz。

**引擎（实测得到）**。新增解析锚点单测：−70 dBm 单音在 bin 中心的峰值读 −69.9997 dBm；−104 dBm 复高斯噪声在
1024 点 hann 下每 bin 均值 −132.349 dBm，解析值 `−104 − 10·log10(1024) + 10·log10(1.5) = −132.342`（Σw²/(Σw)² =
1.5/N；均值在线性域取，单帧每 bin 是指数分布、中位数比均值低 1.59 dB，所以锚点不用中位数）。示例框图经
`cuav_run` 跑出 1953 行：底噪均值 −132.34 dBm，峰 −70.9 dBm——100 kHz 偏移落在 0.4 bin 处，hann 扇贝损耗
0.9 dB，是物理不是缺陷，示例保留整数频偏。回放夹具修正后谱最大 −34.6 dB（此前 +55.7）。组件目录黄金基准
只增两个参数条目、既有条目逐字节不变，按「已有条目不变」规则重生成。引擎 62 项单测 + 3 项冒烟全绿。

**数据常数 DS-8 / DA-8（原型阶段验证值，D-028）**。脚本 `scripts/ds8_calibration.py`，两种估法：论文法
`P_fs0 − gain_dB`（满量程 0 dB 增益时 0 dBm 是假定值，USRP-2955 规格只给最大输入 +10 dBm 与增益 0–93 dB）与
链路预算反推 `EIRP + G_rx − FSPL − P_sig_dBFS`（EIRP 取 2.4 GHz 图传国内上限 20 dBm、G_rx 3 dBi 均为假定，
距离取论文标注）。结果：

| 数据集 | 常数 | 来源 | 实测依据 |
|---|---|---|---|
| DroneRFb-DIR | −1.6 dBm | model | 出版方训练集视距片每型 2 片共 24 片 12 型（与验收集无交集），突发功率中位 −35.6 dBFS（极差 −39.5 ~ −31.4），底噪中位 −51.7 dBFS；10 m 自由空间预测 −37.2 dBm |
| DroneRFa | −50.0 dBm | paper | D00 档 3 片突发功率中位 −37.0 dBFS，底噪 −42 dBFS；论文法 −50.0 dBm，链路预算法 −9.8 dBm，**两法差 40.2 dB** |

计划里的取舍规则（两法差 10 dB 内取论文法）被这个 40 dB 的分歧否定，改用物理一致性判据：按常数换算的底噪
相对热噪声（−174 dBm/Hz + 10·log10(B)）得出的等效噪声系数必须落在 0–15 dB——论文法隐含 1.1 dB（合理），
链路预算法隐含 41.3 dB（不合理，说明 EIRP 或距离标注不可靠），故 DroneRFa 取论文法。DroneRFb 只有链路预算法，
隐含噪声系数 42 dB，对应近零增益录制（10 m 强信号避免削顶），论文无增益记录无法核对。常数表
`data/iq/measured/calibration.json` 记全部输入与推导；`tools/apply_calibration.py` 把常数刷进 4714 份清单
（`absolute_power = estimated`、`power.calibration`、`field_sources["power.scale"]`，质量保持 degraded，
幂等；与新转换加 `--calibration` 的产物逐字节相同），`tools/build_batch_index.py` 加 `calibration` 块；校验器
加 `estimated` 状态与 scale / 常数自洽检查；工具单测 37 项全绿。标定后的 DroneRFb 回放：每 bin 底噪 −83 dBm
（RBW 78 kHz）、帧峰值 p50 −49.5 / p99 −39.0 dBm——像一台看着 10 m 外无人机的频谱仪。

**前端 U-1**。`web/src/state/{types,reducer,logRing,selectors,store}`、`api/{client,frames,seq,hash,ws}`、
`shell/{route,Router,hotkeys,useHotkeys,badges,format,layout,cursorStore,ColumnLayout,TopBar,ExperimentContext,
RunGroup,ViewSwitch,Drawer,LogPanel,StatusBar,Toasts,AppShell,actions,useTaskStream,useProductIndex}`、
`diagram/{DiagramJsonView,examples}`、`results/ResultsView`、`signal/{buffer,SignalPlaceholder}`、`data/DataCenter`，
`SceneView` 重构为只建一次地图（视图切换用 `visibility` 隐藏 + `inert`，显示时 `resize()`），探针改模块级注册表
并加 `app` 子对象与 `mapInstanceId`。三条实现中发现并修掉的问题：① `npm test` 的 glob 未加引号，一旦出现
`src/state/*.test.ts`，`/bin/sh` 展开 `**` 会漏掉两层深的 `protomaps.test.ts`；② 带 `#/results` 打开时，Router
的 hash 同步 effect 用初始状态 `scene` 把地址栏覆盖回 `#/scene`——改为初始状态按 hash 建、只在解析结果与状态
不一致时才写；③ 采用已结束的任务时折叠历史 `task.state` 事件会弹出过时的「任务完成」提示——加 `silent`
批次。web 43 项单测全绿；`scene-smoke.mjs` 14 项通过（唯一改动：图层 62 → 63，新增 `aoi-boundary`）；新
`slice1-smoke.mjs` 23 项全过：切换视图 `mapInstanceId` 恒为 1，界面提交示例框图后任务 `finished / valid`，
`?dev=1` 钩子断开 WS 后重连、客户端 `lastSeq` 与服务端 `last_seq` 同为 2448、日志无缺号，探针 `scaleLabel`
默认为 `dBm`、开发者模式为 `dBm · 标定：模型`，默认 DOM 无 `[data-dev]` 与「标定：」文字，全部请求都在回环
地址。`scripts/build-all.sh`（含路径与 ASCII 检查）通过。

**一处顺手的文档更正**。09 §10 写「`scene-smoke.mjs` 不改」又要求 `layers` 含 `aoi-boundary`，两条本就冲突；
取后者，报告升 v1.6。

**下一步**。切片 ① 只剩 U-3 信号视图：频谱在上瀑布在下共用频率轴、最新行在顶，实时行走 `signalBuffer`
里的 WS 二进制帧、回看走 B-7 端点，纵轴按索引 `scale` 标 `dBm`。之后切片 ②（G 线）。

**新增待办**。① 前端打开已结束任务时日志面板是空的（以 `since = last_seq` 订阅不回放历史），U-4 结果页
可按需从 `GET .../events` 拉一段历史日志；② `useProductIndex` 只轮询谱索引，包络索引待 U-3 一并接。

### 2026-09-06 · U-3 信号视图（D-048）：一台看得见的频谱分析仪，切片 ① 在浏览器里收口

**任务**。用户指示「进行下一步 U-3」。按计划（已批准）实施：频谱 + 瀑布 + 包络竖条，跟随 / 回看两模式，
仪表面板、marker、游标、导出，探针与 e2e 扩展，文档收工。IQ 散点没有数据源（观测点本版本不产 `iq` 产品，D-040 ③），
不做占位面板；检测叠加与对比观测点留 U-4 / 切片 ③。

**做了什么**。`web/src/signal/` 新增十二个不依赖 React 的模块与四个组件：`viewport.ts`（逐行对译服务端 `window.ts`
的行列选择与分组，加像素映射、缩放平移、请求规划）、`reduce.ts`（客户端归约，与 `spectrum.ts` / `envelope.ts` 同式同序）、
`colormap.ts`（viridis 十锚点 256 级、最近邻上色、丢行斜纹）、`scale.ts`（自动量程、1/2/5 刻度）、`trace.ts`（单帧 / 线性域平均 /
最大最小保持、峰值、Δ 读数）、`envelopeMap.ts`（包络 dB 与像素行 → 组）、`fetcher.ts`（150 ms 去抖、换键中止、代数校验、409 /
413 / 网络重试）、`viewStore.ts`（`cursorStore` 范式的外部 store，Float32 不进主 store）、`waterfall.ts`（位移计划、跟随行图像、
回看整窗图像）、`renderer.ts`（离屏 A / B 画布乒乓、迹线、marker、包络条、色带、轴标、交互几何、导出、结束收口）、`export.ts`、
`commands.ts`（键盘命令）；组件 `SignalView / SignalHead / MarkerBar / InstrumentPanel`；`api/window.ts` 解析 `X-CUAV-*` 头，
`client.ts` 加两个抽取函数；store 的 `signal` 切片放宽占位类型并加五个动作；`useProductIndex` 同时轮询谱与包络索引；快捷键
`M` / `Delete`（`Backspace`）/ `←→`（Shift 十帧）只在信号页且焦点不在输入框时生效；`shell/longTasks.ts` 在 `?dev=1` 计长任务；
`?dev=1` 钩子 `window.__cuav.signal.{zoomTo, reset, csv}` 与 `perf.reset`。环 `Ring` 加每槽 `row_index`，按行号二分取行。
文件名与 06 §9B 原列的不同，按职责收成「画布绘制集中在渲染器、组件只转发事件」，已记 06 §14。

**跟随与回看（实测得到，macOS，2560 × 1440，原型阶段验证值）**。跟随模式 1 行 = 1 设备像素行、频率全带，
新行来自 WS 二进制帧，位移追加；回看走 B-7：2K 视口下画布 1588 × 893 设备像素，缩放后重取 893 × 1024（`px` 1588 但
窗内只有 1024 列，不插值）。前端归约对 `tests/golden/product-window.json` 的 12 个用例逐值一致（max / min / 包络逐位，
mean ≤ 1 float32 ulp）；单行归约与整块归约对单行组逐位相同。切片 ① 单音：峰值 bin 614（理论 614.4），M1 读
2.4401 GHz、−70.90 dBm，底噪约 −132 dBm，与 D-047 的引擎实测一致。包络竖条对恒定单音是一条线（|x| 的 min / max / rms
重合在 −70 dBm 附近），是物理不是缺陷。

**一条设计决定是被实测逼出来的：任务结束即收口**。第一次跟随模式截图停在第 1877 行而数据有 1953 行——切片 ① 的
2447 条事件在 82 ms 内到齐，WS 在突发下丢行帧或断线重连（D-044 的背压策略，运行期间出现过「与服务的连接已断开」提示后自动重连），
环里的尾部与缺口只有文件才完整。于是定：任务到终态且索引收尾后，把当前画面转成回看窗口从 B-7 重取（D-048 ②）。写 e2e 时
又踩到两个竞态：只看任务事件的终态时，最后一次索引可能还没到，用陈旧的 `rows_available` 算窗口，收口停在 1.70 s；只看索引的
`run_state` 时，订阅应答先把运行态标成 finished，此刻回放的行帧还没到、环是空的。最终条件是三条同时成立：索引自身
`run_state` 终态且 `index_final`，且本地已折叠到流里的结束事件（`resultProvisional = false`）。采用已结束的任务时
（`since = last_seq` 不回放）直接进回看取全窗。

**长任务归因（实测得到）**。运行期间曾测到 88–106 ms 的长任务，用 CDP 采样分析器定位：热点是 MapLibre 主线程接收瓦片
（`receive`）、垃圾回收与 WebGL 绘制——是页面打开 0.7 s 就点了运行、地图还在吞瓦片；等地图空闲 3 s 再跑，整任务 22 次
渲染最长 29.9 ms，**没有长任务**。另一处是 U-1 的事件折叠：结果页隐藏时仍有 84 ms 长任务，来自一帧里把 2447 条事件全部
灌进 reducer，改为每帧最多折叠 400 条（`useTaskStream`）。e2e 的长任务断言因此在地图空闲后才 `perf.reset()`。

**e2e（`tests/e2e/slice1-smoke.mjs`，23 → 39 项全过）**。新增：隐藏期累积、显示时从环补画（`liveFrames ≥ 1`）后自动转
回看且窗尾 = 数据末尾（1.999872 / 2）；峰值 ≤ 1 bin；M1 在 2.4401 GHz；频谱与瀑布边界像素相同（[92, 1222]）；包络索引 489 行；
缩放重取 rows 329 = py = H、cols 411 = min(1130, 411)、窗 0.39936–1.600512；`←` 前移 0.001024 s；`M` 放 M2 于 M1 处、
`Delete` 删；CSV 文件名含任务 / 观测点 / 时间窗，2676866 字节 625 行；二次运行 `liveFrames` 1 → 12、结束后收口；地图空闲后
运行期间长任务 0；刷新后已结束任务直接回看全窗；默认页无 `perf`。脚本改为在 stderr 逐条打进度（这次卡住时才能定位）。
写 e2e 踩的三个坑：整份 CSV（7 MB）经调试协议 `Runtime.evaluate` 拉回会挂死，只能在页面内取摘要；等待条件 `lastFetch.t0 ≥ 0.39`
被收口窗口（t0 ≈ 1.09）提前满足，要卡 0.39–0.41；`framesBefore` 必须在点击之前读，示例任务 0.14 s 就跑完。
`web` 单测 43 → 87 项、服务 102 项、`scene-smoke` 14 项、`scripts/build-all.sh`（含路径与 ASCII 检查）全过。

**与计划的偏差**。① 「索引晚于行到达先按环画、轴标『—』」简化为等索引（首行即刷索引，轮询立即返回，延迟亚秒）；② 包络布局
按 09 §7.2「时间游标是同一条横线」定为瀑布右侧竖条，线框中横向的「包络 min / max / rms」行作废（09 v1.7 §17 记）；③ 自动量程
「只扩不缩」并回写面板，避免逐帧抖动。

**文档**。CLAUDE.md：环境行、新增「信号视图」行、U 行、D-048；06：§0.3、§9B U-3、§9E 切片 ①、§14；09 升 v1.7：§1.2 与
§7.1 线框去掉 D-047 之前遗留的「标定：论文」，§7.2 加色图 / 统计量 / 包络 / 结束收口 / 未做五行，§10 探针字段与七条断言，§17；
`docs/display-products.md` §5 前端缓存约定改为单窗缓存、瓦片化留 U-4。

**收尾时记的三处小事，用户当天要求即修（见下条）**。① 页头行数随索引到达变化把瀑布挤矮；② 状态条的 `seq` 不随事件推进；
③ 降级原因没叠到图上。**未修的两条留 U-4**：④ 探针在地图样式未就绪时记 `probe: TypeError`（U-1 的保护，上限 20 条），噪音可再收；
⑤ 打开已结束任务时日志面板为空（U-1 待办不变）。

### 2026-09-06 · U-3 收尾三处小修：页头定行、状态条序号、降级原因叠图

**任务**。用户问上一条「新增待办」是什么意思，核实后指示「这三处都是几行代码的量，现在修」。三条都先复测再动手。

**① 页头行数变化把瀑布挤矮（记错过一次原因）**。收工时记的是「『有效（暂定）』徽标换行」，复测否定：徽标始终在同一行内。
真正的原因是提交任务瞬间信号页的索引被清空、页头只剩主行，索引到达后多出开发者模式的标定行。1400 px 宽实测页头
20 → 43 px、瀑布 353 → 330 px，**只在 `?dev=1` 发生**，默认地址页头恒一行。修法两条：开发者标定行常驻（无常数时显示
「标定：—」），索引摘要也常驻（无索引时显示破折号）；再把主行 `flex-wrap: nowrap`、索引摘要与标定行单行截断
（标定 `note` 是一整段话，窄屏换行会重新引入同一类抖动）。修后三种状态页头恒 44 px、瀑布恒 329 px，画布不再重建。

**② 状态条的 `seq` 不随事件推进**。`WsClient` 只在状态迁移与 `dropped` 时 `emitStatus()`，逐条事件只更新内部
`state.lastSeq` 不上报。于是不断线跑完一个任务，界面右下角一直显示「已连接 seq 0」。**内部 tracker 一直是对的**，
补取、缺号判定、断线重连都不受影响，错的只是这一处读数；端到端测试因为强制断线才触发上报，所以一直没暴露。
修法是序号推进后节流上报（250 ms，突发下每秒至多四次状态提交），关闭时清掉滞留的定时器。加了一项单测。
实测：回放任务跑完显示 seq 4888，合成链跑完显示 2447。

**③ 降级原因叠图（09 §13.2）**。新增纯函数 `productStateNote(index)`：`valid` 或无索引给 null，否则给
「降级 / 无效 / 不适用 + 第一条原因」与色调，渲染器在瀑布左上角画一个带边框的小条，形状 + 文字 + 颜色三重编码。
用真实降级产品验证：提交回放框图 `dronerfb_0_CH0_S4`，产品索引 `state = degraded`、原因「源数据质量状态为 degraded，
理由见清单 quality.reasons」，图上如实叠出，页头徽标同时是「▲ 降级」。这也是首次在界面上看真实实测数据的时频图——
80 MHz 带宽、3906 行、图传突发与跳频清晰可见，M1 读 2.4628 GHz / −64.2 dBm。

**验证**。web 89 项单测（新增两项）、服务 102 项、`slice1-smoke` 39 项、`scene-smoke` 14 项、`scripts/build-all.sh` 全过。

---

### 2026-09-06 · 切片 ②「场景里飞起来」：从场景文件到瀑布上那条渐暗的线（G-0 至 G-5，D-049）

**08 报告先定稿**（`08.引擎分块流与多速率_v1.0.md`，v1.1，583 行）。06 §13 第 3 条要求它在切片 ① 后、
切片 ② 前定稿，理由在动手后立刻显形：它的 §9「参数帧与 IQ 时钟对齐」不是文档工作，是本切片能不能守住
「同种子逐字节复现」的设计前提。报告分三类内容——§2 至 §6、§10、§11 是切片 ① 实现的设计记录；
§7、§8、§12、§13 定「首期不做什么、将来在哪里做」；§9 是先于实现写出的设计，G-1 至 G-3 落地后回填成记录。
§15 列六项暂定（背压时机、多速率群时延口径、`ChannelPathSet` 是否补结构体、共享内存、`AddMixer` 类别错标、
第三方件位置），其中三项在本切片实施中拍定。

**一条在动手前就该知道的调度约束**。开工前把 `engine/src/graph.cpp` 逐行核了一遍，发现两处会决定整套设计：
调度器每轮**无条件**把上游产出覆盖进下游缓冲（`:241-249`），且 `process()` 之后**无条件**清空输入缓冲（`:227-229`）。
推论是：只要 `ScenarioSource` 有一轮不产出，`SceneBoundChannel` 就会因为 `scene` 口没数据而跳过一轮，
下一轮的 IQ 块随即把没被消费的块覆盖掉——**样点丢了，没有任何报错，同种子复现也复现不出问题**。
而 `block_samples / fs < 1 / update_rate_Hz` 时（高采样率、小块长、高更新率）这种情形几乎必然发生。
所以「每轮必产出至少一帧，哪怕重发上一帧」是正确性要求，不是优化；`engine/tests/test_channel.cpp` 里
专门有一条 100 轮的回归测试盯着它。

**G-0 示例场景**。`data/scene/beijing-yayuncun/scenarios/demo-01.scenario.json`：站点在观测区域中心
(116.4050, 39.9900, 30 m)，多旋翼图传下行 2.4405 GHz / 27 dBm，三个航点自西南向东北。
这条航线自然构成一次**过顶后远去**：斜距 211 m → t=15 s 附近最近点 70 m → t=180 s 的 3037 m，
自由空间路损 86.7 → 77.1 → 109.9 dB，接收电平 −54.7 → −45.1 → −77.9 dBm。t=15 s 之后是一段
**32.8 dB 的单调衰落**，瀑布上就是一条越来越暗的线；过顶处多普勒变号，顺带把 `doppler_Hz = −f·(dr/dt)/c`
的符号约定演出来。`tests/unit/test_scenario_example.py` 八项守着它：schema、观测区域清单哈希不得是占位符、
跨引用、航线总时长不得短于仿真时长（否则后半段无人机停在末航点，演示里看着像卡住）、全部位置落在观测区域内。

**窄带观测配置**。本切片没有下变频与信道化（M-2 / M-3，切片 ④），接收机按 500 kS/s 的窄带观测窗建模，
发射带宽随之取 400 kHz。这是原型阶段的显式简化，写进场景文件的 `trace.notes` 与 08 §15，不是模型结论（D-028）。

**G-1 `geo/` 从占位变成真库**。删 `placeholder.cpp`，四个模块：`ClosedFormWgs84`（LLA↔ECEF 用 Bowring 闭式，
不迭代）、`Route`（预算段时轴后二分定位、闭式插值）、链路预算（FSPL、多普勒、时延、热噪声底）、
场景结构与链路帧发生器。**两处对 em-demo 的有意偏离**，都写进了代码注释与 08 §9：

- **距离一律 ECEF 弦长**，不用 em-demo 的球面半正矢（R = 6371000）。两者在 2 km 段上差约 0.5%，即 10 米，
  而航迹对拍的容差是 1e-6 度（约 0.1 米）；弦长闭式无迭代，C++ 与浏览器能逐位一致。
- **按绝对时间闭式求值**，不做 em-demo 的增量积分。增量积分与调用步长耦合，而引擎里的步长由块长决定——
  换块长就换结果，铁律 9 立刻失守。

`geo/` 保持**零第三方依赖**：场景 JSON 的读取放引擎侧（`engine/src/scenario_json.cpp`，复用已 vendored 的
nlohmann），它的 doctest 单测寄放在 `engine/tests/`，自己留一个不依赖 doctest 的裸 main 冒烟测试保证独立可构建。
引擎 CMake 用 `add_subdirectory` 挂上 `geo/` 并链接 `cuav_geo`。

同一步实现了 `cuav_run --scenario-track <场景> [--track-rate] [--scene-root]`：只跑运动学、输出 `entity` 事件流、
不建产品目录。它**不发 progress、不按墙钟节流**，所以 stdout 逐字节可复现——因此它既是黄金基准
`tests/golden/scenario-track-demo-01.json`（361 样点）的生成器，也是服务端 `PUT /api/v1/scenarios/{id}`
的语义校验器（只看退出码）。另加了自写 SHA-256（框图的 `scenario_ref.sha256` 与场景的 `aoi.manifest_sha256`
都要真核对，带四条 NIST 测试向量）。

**一处需要更正的外部结论**：设计评审给的 FSPL 解析锚点写作 100.04578 dB，实际是 **100.052008 dB**
（`20·log10(4π·1000·2.4e9/299792458)`），与 06 §9C G-1 写的 100.05 一致。测试里按精确值断言。

**G-2 场景运行时**。`ScenarioSource` 是 `dynamic_ports` 的第一个使用者：`describe()` 在**未 configure**
的实例上调用（装载器 `:511`），那时端口还不知道，所以目录里只有 `dynamic_ports` 的声明；
`Graph::connect` 在 `configure()` 之后（`:578`），那时 `outputs()` 已按场景的辐射源给出 `link:<emitter_id>`。
`SceneEmitterSource` 按场景 `emission.waveform` 生成波形并**归一化到发射期间的单位功率**（0 dBm）——
这条口径决定了观测点上导通期间读到的就是 `Prx = tx_power + G_t + G_r − L`；按占空比平均归一的话，
瀑布上的突发峰值会比链路预算低 `10·log10(1/duty)`，验证时对不上。它在 `init()` 里取一个 u64 建私有随机子流：
调度的拓扑序等于框图里节点的书写顺序，多个噪声源共用一个 `IRandom` 时调换顺序就会改变样点。
`NoiseSource` 不动——它已有黄金基准（铁律 10）。

装载器新增**场景注入**，与 `data_id → manifest_path` 同法（D-037）：框图只写 `scenario_id` 与
`scene_binding`，路径由 `IScenarioResolver`（旁挂的 `scenarios` 段，或 `cuav_run --scenario`）解析后作内部参数注入。
一次装载只读一次场景文件、只算一次哈希，顺带做四项核对：场景文件字节哈希对框图声明的、观测区域清单哈希对场景声明的
（占位符 `<...>` 一律拒，铁律 15）、框图时长不超场景时长、同一场景的绑定节点必须同采样率。
新增错误码 `scenario`（17 → 18 个）。`cuav_run` 由观察者回调落 `track.jsonl` / `links.jsonl`，惰性开文件、
逐行 flush、每行自带 `t_s` 且以换行结尾——B-7 的读取层把没有换行的末行当残片丢弃，这两条是硬契约。

**G-3 施加类信道**。`SceneBoundChannel` 施加增益、整数样点时延与多普勒相位斜坡。三条不变量：
**不改变流长度**（观测点的行索引、频谱的帧对齐、B-7 的偏移读法都建立在这上面）；相位累加器**跨块连续**
并逐样点回卷（按块回卷会让结果依赖块边界的位置）；增益按帧零阶保持不插值（1 km、15 m/s、10 ms 一帧时
台阶只有 0.0013 dB，而「帧是真值的单位」这条语义比这点台阶重要）。整数样点时延在 `delay_s` 变化处有 ±1 样点跳变，
这是声明过的模型简化，记 `notes` 但**不标降级**——把它标 Degraded 会让整个任务的结果四态变成降级，
把真正的降级信号淹没。`time_basis = FileAcquisition` 的输入直接拒，这是 06 防线二、三在引擎侧的执行点。

**一处口径分叉的拍板**：`SceneParamFrame` 的字段已冻结，而 `Prx` 需要发射功率与收发天线增益三项。
选了「帧只装纯传播损耗，三项作信道的内部参数由装载器自场景注入」——折进 `path_loss_dB` 会让 `link` 事件
与场景视图里的「路损」读数名不副实，给帧加字段则要改冻结结构。三项在首期是全程常量，本来就不该塞进
10–100 Hz 的慢变帧。

**G-5 态势图层**。七层：站点圆点与图标、规划航线、航点、目标图标（随航向旋转）、航迹（抽稀）、链路线（按视距着色）。
图标用 `map.addImage` 运行时注册 SVG 光栅化 + `source-in` 染色 + 白色光晕，不引精灵图集。
**高频量不进主 store**：实体与链路走 `web/src/scene/sceneStore.ts`，地图按 20 Hz 定频取值——
参数帧最高 100 Hz，一条一画就是每秒一百次 `setData`，而 U-3 已经踩过「一帧折叠 2447 条事件 = 84 ms 长任务」。
任务结束后从 `track` / `links` 端点整批回放（突发下 WS 会丢帧，只有文件才完整，与 U-3 的结束即收口同理）。
色值在 Airports 浅色底图上重新标定并写回 `docs/display-route.md` 第 4 节（**D2-4 销项**）：八项色值对三种背景
最低对比度 3.56:1，高于 WCAG 非文本图形的 3:1 门槛；视距绿 / 非视距红这对语义沿用 em-demo，只是压深色相。

**G-4 场景编辑器**。四个工具（选择 / 布站 / 航点 / 测量）、对象树（站点、辐射源、航线、活动四组来自文件，
**链路组是派生对象**不入文件）、右栏表单（工程计数法显示、输入接受 SI 前缀）、撤销重做（整份文档入栈——
场景文件几千字节，记整份比记 diff 简单可靠，也让「一次拖动一步撤销」不必去拼补丁）、保存。
服务端 `server/src/scenarios.ts` 三个端点：清单、读取（响应头带落盘字节的哈希）、写入。
写入的校验分两层，与 B-5 的框图路数一致（D-042）：服务端只查标识与观测区域，**语义先落临时文件再调
`cuav_run --scenario-track` 看退出码**，不过即 400 并带引擎的字段路径、临时文件删掉、盘上原文件不动。

浏览器侧的航迹预览（`web/src/scene/editor/preview.ts`）与 C++ **逐条同式**：ECEF 弦长、段时轴、
闭式插值、经纬高各自线性。`preview.test.ts` 拿黄金基准逐点对拍，**361 个样点差 0**（容差 1e-6 度）。

**三处实现时才暴露的问题**：

1. **换任务时把站点也清了**。`useLiveSituation` 的重置效应最初挂了 `[taskId, map, ready]`，
   地图瓦片要两秒、取航迹只要几十毫秒，顺序必然是「先取到、后清掉」——画面上什么都没有，场景却明明已经载入。
   改成只挂 `taskId`，并且不显式清地图：清空后版本号变，下一次定频 tick 自然画成空。
2. **地图拆除是异步的**。视图切换、换场景、页面卸载都会 `map.remove()`，而 20 Hz 的定频 tick、
   异步的图标注册与 React 效应都可能在那之后还跑一次，那时 `map.style` 已是 null，任何 `getSource` 都会抛——
   **抛在 React 效应里会让整棵界面树被卸载**（React 19 的行为），表现是「点一下站点，整个页面白了」。
   这不是「小心一点就能避免」的，只能在每个图层入口先查地图是否还活着。加了守卫后 e2e 连跑两次稳定。
3. **保存会改变文件哈希**。编辑器保存走 `JSON.stringify(doc, null, 2)`，与手写的排版不同，
   于是「不改内容直接保存」也会让 `scenario_ref.sha256` 失效。定下**场景文件的规范序列化形式就是编辑器那一种**，
   把盘上的示例场景改成规范形式，空操作保存从此是逐字节的空操作。

**验证**（macOS，原型阶段验证值）：

| 项 | 结果 |
|---|---|
| 端到端运行 | 示例框图 500 kS/s × 30 s = 1.5×10⁷ 样点，600 轮，墙钟 **0.82 s**，3892 行产品，300 条 `entity` + 300 条 `link` |
| **两时刻带内功率差 vs 几何路损差** | 实测 6.017 dB 对几何 5.943 dB，**偏差 0.074 dB**（切片 ② 容差 0.5） |
| 绝对链路预算（包络 rms） | t = 5 / 15 / 29 s 三点对 `27+2+3−路损` 偏差 −0.021 / +0.006 / +0.001 dB |
| **三时刻位置 vs 黄金航迹** | 差 **0**（容差 1e-6 度） |
| 浏览器预览 vs C++ 航迹 | 361 个样点逐点相同 |
| FSPL 解析锚点 | 100.052008 dB，时域与峰值 bin 两种口径都在 ±0.01 dB 内 |
| 多普勒 | ±1 kHz 峰值频移 8 个 bin（fs 1 MHz、nfft 8192、bin 122.07 Hz），符号正确 |
| 测试 | 引擎 114 项单测 + 8 项 ctest、服务 110 项、web 95 项、场景 schema 8 项、`scene-smoke` 14 项、`slice1-smoke` 39 项、`slice2-smoke` **27 项**、路径与 ASCII 检查全过 |

**一条必须写明的读数口径**：hann 窗下把谱线邻域的 bin 在线性域加起来，得到的是 **1.5 倍**的单音功率
（等效噪声带宽，`10·log10(1.5) = 1.761 dB`）。所以绝对电平要么读峰值 bin（频偏落在 bin 中心时），
要么用时域功率；**带内功率适合比差值**——那个常数因子在相减时精确抵消，也不受多普勒带来的扇贝损耗影响。
切片 ② 的功率断言因此按带内功率的差值算，而不是 06 §9E 原文的「峰值功率差」（多普勒把谱线在 ±1.2 个 bin 内
推来推去，hann 的扇贝损耗最坏 1.42 dB，比单 bin 峰值守不住 0.5 dB）。口径细化记进 06 §14。

**组件目录 8 → 12**，按「已有条目不变」的规则重生成黄金基准；`AddMixer` 的类别现标 `source` 而它其实是
处理件，列进 08 §15 待拍板，本切片不动代码。

---

### 2026-09-07 · 08 报告改写为 v2.0：从速查笔记变成能从头读到尾的设计报告

**起因**。用户指出「08 报告可读性太差」，要求用「易懂、逻辑性强、语句通顺完整、带前置信息交待的
正规设计报告」重写。v1.1 是在切片 ① 收口、切片 ② 开工之间赶出来的，写法是给作者本人看的速查笔记。

**先做了一次量化诊断，结论与直觉相反**。按字符占比测：08 的散文段落占 45%，反而比 09 的 8%、
04 的 27% 都高。所以问题不是「表格太多、散文太少」——本项目的正规报告本来就是表格为主，这一点不必改。
真正的病灶是另外六个：纯散文平均句长只有 17 个汉字，25 个 `——` 破折号在替因果连接词干活；
§1、§2、§3、§6 都是标题后直接上图或代码，零前置交待；**标题里的「分块流」全文没有定义**，
首次出现就已经在讨论它的性质；`切片 ②`、`B-7`、`G-1…G-3`、`D-049`、`铁律 9`、`S4` 等内部编号全部裸用；
事实过时且自相矛盾；章节长度失衡 11 倍（§9 = 9378 字符对 §12 = 823 字符）。

**一条硬约束决定了改写方式**。代码与规范里有十四处注释按 §9.2、§9.3、§9.4、§15 引用本报告
（`geo/`、`engine/`、`docs/scenario-format.md`、`web/`、CLAUDE.md）。所以 §1 至 §17 与 §9.1 至 §9.8
的编号与主题一律不动，新增内容只进 §0（无人引用）、各节内部与新附录。

**顺带消掉一处悬空引用，而且没动代码**。`engine/src/channel.cpp:134` 的注释引用「08 报告 §9.3 约束 C」，
但 v1.1 的 §9.3 消费端约定是 1–4 编号，根本没有 A/B/C。根因是第 1 条塞了三件事（收帧、保留上一帧、
只前进游标）。改写时把它拆成六条字母编号 A–F，顺序恰好让 **C = 只前进的游标定位**，注释自动指对。
拆条本身正是「解压电报体」要做的事。

**改了什么**。583 行 → 1281 行。新增 §0 前言五节：§0.1 引擎解决什么问题（按 04 §5.4 的三段式首次定义
「分块流」，并给出为什么不能整段处理 IQ 的真实数字——100 MS/s 复 float32 是 800 MB/s，一分钟 48 GB）、
§0.2 读者与读法（三类读者各自的阅读路线）、§0.3 术语与内部编号三张对照表、§0.4 一次运行的端到端叙事、
§0.5 设计摘要。为 §1 至 §17 每节补前置交待与论证段落，**为每条硬约定标注对应的回归测试用例名**。
新增四张图：块走一趟流水线、组件生命周期、调度两轮轮转、参数帧与样点区间对齐的时间轴。
§5 补写切片 ② 的场景注入四道闸（v1.1 完全没提）；§6 把「每轮无条件覆盖下游缓冲」这条语义从 §9.3
提到约定四讲清，§9.3 改为引用它；§10 补 `track.jsonl` / `links.jsonl`；§16 改名「后续工作与架构债」
并分两组；新增附录 C 依据条款索引。破折号 25 → 4。

**订正的过时事实**（逐项核实到文件行号）：`cuav_engine` 源文件 12 → **16** 个（4796 行）、
组件 8 → **12** 个、doctest 62 → **114** 个、ctest 4 → **8** 项、错误码「17 + 1」→ **19** 个、
`platform.h` 「四件」→ **五件**（原文说四件却列了五个）、`PortType` 的位置由 `component.h` 改为
`types.h:40-47`、`PortData` 由「`scene` 是单帧且 `clear()` 漏清，将在 §9 一并改掉」改为已落地的完成时、
附录 B 由 8 条补到 **12** 条（补 `ScenarioSource` 等四件的完整参数）、§15 表头「六项」→ 九项并按 ①–⑨ 顺排。

**顺带发现并订正 CLAUDE.md 的一个计数错误**。D-049 写「新增错误码 `scenario`（17 → 18 个）」，
但 `docs/diagram-format.md` 现有 **19** 个，且 git 显示 HEAD 时表里就已经是 18 个（含 `scene_binding`）。
真实历史是 18 → 19，已订正。

**验证**。`ctest --test-dir engine/build` 8/8 全过；`scripts/check-paths.sh` 通过；
十四处外部引用的锚点（§9、§9.2、§9.3 含约束 C、§9.4、§15 ⑤⑥）逐条复核可解析；
排版检查零弯引号、零 LaTeX、零 mermaid。报告里的每个数字都与 `cuav_run --catalog`、`ctest`、
`docs/diagram-format.md` 的实际输出对齐。

**同日追补：文体再改（v2.0 → v2.1）**。用户指出 v2.0「不能像写代码注释这么写，要按技术设计文档的
方式写」。v2.0 解决的是结构与信息完整性，文体仍是讲解笔记的口吻：设问句（「什么时候该做?」）、
讲解式自指（「先解释背压是什么」「举一个带真实数字的例子」「值得单说」）、口语与比喻
（「立刻炸出来」「悄悄错掉」「Coder 产物是组件的内脏」「兜底」「照跑」），以及 101 处作强调用的
直角引号。v2.1 以 04 号方案为文体基准重写全文：章节标题改为名词短语，改用第三人称陈述，
规范性用语统一为应 / 必须 / 不得，直角引号收缩至 54 处且全部为逐字引用（测试用例名、上游原文、
代码注释），破折号仅存于引文内部，六张图统一编号为图 1 至图 6 并在正文中引用。
章节编号、技术结论与全部事实均未变更；`ctest` 8/8 与 `scripts/check-paths.sh` 复验通过。

---

### 2026-09-07 · U-0 后半：框图画布交互设计（09 报告 v1.8，切片 ③ 的设计前置）

**为什么先写报告而不是直接写画布**。06 §13 第 3 条要求复杂节点先出设计报告，09 自己的 §16 也标了
「后半在切片 ③ 之前补写」。画布要定的东西里有一半是**判据归属**问题（连线合法性查谁、报错谁解释、
哈希谁算），这类问题写在代码里就成了既成事实，写在报告里才好评审。

**编号约束**。`web/src/` 有八处注释按 §2、§3、§4.4、§6、§8 引用 09，因此 §0 至 §15 的编号一律不动。
后半挂在 §6 之下扩成 §6.1 至 §6.11：原「只定框架」的内容保留为 §6.1 与 §6.2，新写九节。
另在 §7 下新增 §7.4。做法与 08 报告改写时相同。

**贯穿后半的一条原则**：画布不自行解释任何规则。组件有哪些、参数叫什么、什么单位范围、
哪两个端口能连、连不上的理由是什么，全部来自 `GET /api/v1/components`，而该目录由
`cuav_run --catalog` 生成。所以画布、服务与引擎三处的校验不可能不一致——画布只是引擎规则的投影。
落到具体条文：§6.5 的连线判据只查目录 `port_compat` 的 36 条全枚举，**拒绝理由用目录原文**，
画布只额外补一句可操作建议（「请在两者之间放置一个施加类组件」）；§6.6 的参数控件由 `ParamSpec`
生成；§6.9 的报错定位按四元组 `{code, node_id, port, message}` 分三档。

**几处需要判断的地方**：

- **互斥参数在界面上根本构造不出来**。目录 `excludes[]` 声明的两个参数（`level_dBm` 与 `amplitude`）
  并排显示、共用一个单选开关。这样错误码 `param_conflict` 在画布路径上不可达，它只用于挡
  手写的 JSON 与外部提交。
- **天线与接收机两类首期为空，但组件库仍显示这两个分组**并标「本期无组件」。04 §8.1 规定的是六类，
  隐藏空分组会让用户以为系统只支持四类。
- **`iq` 产品开关置灰**而不是不显示：观测点本版本对 `products` 含 `iq` 返回 `product_unsupported`
  而非静默忽略（D-040 ③），界面上照此表达。
- **场景哈希不由画布算**。`scenario_ref.sha256` 必须取服务端 `PUT` 响应头 `X-CUAV-Sha256`，
  即落盘字节的哈希；两端各自序列化再各自算哈希必然对不上（D-049 ⑧）。
- **节点上只显示校验态，不显示结果四态**。四态属于块与产品（08 §2），节点不是数据产物。

**类别色实算而非照抄**。六类类别色在浅色底上按 WCAG 2.1 SC 1.4.11 的 3:1 判据算了三种相邻背景
（画布底 `#f4f1ec`、节点白 `#ffffff`、警示底 `#fdf6e3`）的对比度，最低一项是算法绿 `#15803d` 的
**4.45:1**，天线青 `#0f766e` 4.86:1，其余六以上。实测值进表，做法同 `docs/display-route.md` 第 4 节。

**顺带修掉一个自己引入的键位冲突**：初稿给「复制节点」绑了 `Ctrl+D`，但那是 Chromium 的
「收藏此标签页」。改为 `Ctrl+Shift+D`，并把 `Ctrl+D` 加进 §11 的保留键行；`Ctrl+A` 归入
「可绑但必须 `preventDefault`」一类。同时把新加的 `Delete` 行与原有的 `M / Delete` 行去重。

**验证**：`web/src/` 引用的十四个锚点（§2、§3、§4.4、§5.4、§6、§7.1、§7.2、§8、§9、§10、§11、
§13.2、§15、附录 A）逐条复核仍可解析；文体检查零弯引号、零 mermaid、零 LaTeX，
三处破折号全在前半原文与旧修订记录里。09 由 573 行增至 854 行。

**下一步**：U-2 画布实现。React Flow 尚未安装，需 vendored 锁版本并进 `THIRD-PARTY-NOTICES`（D-032）。

---

### 2026-09-07 · U-2 框图画布：从 JSON 文本框到真正的框图（切片 ③ 主体）

**一条原则贯穿全部实现：画布不自行解释任何规则。** 组件有哪些、参数什么类型单位范围、
哪两个端口能连、连不上的理由是什么，全部只从 `GET /api/v1/components` 查，而那份目录是
`cuav_run --catalog` 的输出。所以画布、服务端与引擎三处的校验不可能不一致——画布只是引擎规则的投影。
`slice3-smoke` 的断言据此写法：凡涉及规则的，都拿目录原文比对，而不是把规则再抄一遍到测试里。

**随包依赖**。`@xyflow/react` 12.11.6（MIT）锁版安装，连同 13 个传递依赖
（`@xyflow/system`、`zustand`、`classcat`、`use-sync-external-store`、八个 d3 小包）
一并写进 `THIRD-PARTY-NOTICES`（D-032）。zustand 是 React Flow 的内部状态，
本项目自身的状态管理仍是零依赖 `useReducer`。

**分层**。纯逻辑先行、可单测，UI 在其上：
`web/src/api/catalog.ts`（目录类型、`canConnect` 只查 `port_compat`、六类与类别色、端口形状）、
`web/src/diagram/doc.ts`（文档模型与规范序列化：固定键序、缺省值不写入）、
`layout.ts`（无位置节点的拓扑分层布局）、`format.ts`（工程计数法）。
UI 是 `DiagramView` / `NodeCard` / `Palette` / `ParamPanel` 四个组件。

**几处按 09 §6 落地的判断**：互斥参数并排共用一个单选，于是 `param_conflict` 在画布路径上不可达；
内部参数一个都不渲染（D-037 的执行点）；`iq` 产品开关置灰而非隐藏（D-040 ③）；
节点只显示校验态不显示结果四态（四态属于块与产品，08 §2）；
JSON 编辑器降为开发者模式的「源码」页签（09 §6.2）。

**三处被测试逼出来的订正**：

1. **全库内部参数是 11 处，不是 8。** 2 处路径类加三个场景绑定组件各 3 处。
   这个错数字我先前写进了 08 §4.3 与 09 §6.6，两处已改。对着 `tests/golden/component-catalog.json`
   写断言时才发现——用桩数据测这层没有意义，它的全部价值就是「只查目录」。
2. **连线字段是嵌套的 `{node, port}`，不是扁平的 `from`/`from_port`。** 我按后者写了类型，
   而「与引擎示例字段等价」那条测试**没能抓住它**：两边都经同一个 `parse()` 读，同样地误读，
   序列化出来自然一致，A 与 B 都错成一样就比不出来。补了一条直接对着真文件断言字段形状的测试才炸出来。
   教训：等价性测试要有一端是外部事实，不能两端同源。
3. **`formatEng(500e3)` 给出 `"5 k"`**：去尾零的正则把整数 `"500"` 的零也吃了。
   同时 `parseEng('6.1u')` 有浮点误差（`6.1 * 1e-6 ≠ 6.1e-6`）。两处都改成按十进制指数构造，
   往返测试守着。

**一处设计与实现的对账**。09 §6.4 原写「不做自动布局」，但框图文件里 `position` 是可选字段，
引擎自带的示例与手写框图都没有它，六个节点会全部重叠在 (40, 40)——首次跑起来就是这个样子。
自动布局的边界应当是「不重排作者已摆好的节点」，而不是「没有位置也不给位置」。
按拓扑深度分层排位（正是信号链的阅读顺序），只对没有 `position` 的节点排，排位结果不回写文档，
否则会把「用户没摆过」变成「用户摆过」。09 §6.4 已补这段边界说明。

**顺带修掉一个既有的 e2e 缺陷**：三个冒烟脚本的 `finally` 里 `rm(dir)` 会抛 ENOTEMPTY
（Chrome 退出后仍在写 profile 目录），而结果是在 finally 之后才打印的，于是断言结果被整个吞掉，
`scene-smoke` 看上去像是崩了。改成 `.catch(() => undefined)`。

**验证**：web 131 项单测（新增 36 项）、服务 110 项、引擎 114 项 + 8 项 ctest；
端到端 `scene-smoke` 14、`slice1-smoke` 39、`slice2-smoke` 27、新增 `slice3-smoke` 22 项全绿；
`check-paths.sh` 与 `check-ascii.sh` 通过。浏览器实测：载入切片 ② 示例后画布画出 6 节点 5 连线、
按拓扑深度分 4 列、参数流画虚线、观测点标记在端口下，画布序列化出的框图经 `--validate` 提交后
跑完且结果有效。

**下一步**：切片 ③ 余项是 09 §7.4 的检测叠加与对比观测点；IQ 散点仍待引擎 `iq` 产品（D-040 ③）。

**同日修：拖动滞后（用户反馈「拉动有些滞后，不随光标拉动着走」）**。根因是 U-2 首版的
`onNodesChange` 只在拖动**结束**时提交位置，而节点每帧都从文档重新推导——拖动过程中节点被不断
拽回文档里的旧位置，只有松手才跳过去。改法：**画布在交互期间自己持有节点位置**
（本地 `rfNodes` + `applyNodeChanges`），只在松手时提交一次到文档；撤销栈因此也是一次拖动一步，
而不是每帧一步。同时启用 React Flow 的 `snapToGrid` 16 px，与 09 §6.4 的网格约定一致。

**改这一处时引进又修掉一个更严重的缺陷**，值得记：加上「文档变了就重建本地节点」的 effect 之后，
整个界面白屏，React error #185（Maximum update depth exceeded）。根因是
`const errors = validation && !ok ? validation.errors : []` 这个**行内空数组字面量**——
它每次渲染都是新身份，于是 `errByNode → derived → effect → setRfNodes → 再渲染` 成环。
改为 `useMemo` 且空值取模块级常量 `NO_ERRORS`。教训：一旦有 effect 依赖某个 `useMemo` 的产物，
该 memo 的**全部**依赖都必须身份稳定，行内 `[]` / `{}` 字面量是最容易漏掉的一个。
这个白屏是 `slice3-smoke` 当场发现的（页面 90 秒没 ready），不是靠手点发现的。

**回归**：`slice3-smoke` 增至 25 项，新增三条盯着这个症状——拖到一半（尚未松手）时节点必须已经跟过去、
松手后落在 16 px 网格、一次拖动只入一步撤销栈。web 131 项单测与另外三套 e2e 无回归。

**同日再修：框图节点穿透隐藏浮到场景地图上（用户反馈截图）**。09 §4.2 规定视图隐藏用
`visibility: hidden` 而不是 `display: none`，理由是地图容器尺寸不能归零、瀑布画布要在隐藏期继续累积。
但 `visibility` 虽然继承，**后代可以把自己改回 `visible`**，而 React Flow 正是这么做的：
它量到节点尺寸后给每个节点写行内 `visibility: visible`（`@xyflow/react/dist/esm/index.js:2363`）。
于是切到场景页后，框图节点仍然画在地图上。

改法：隐藏的视图另加 `opacity: 0`。opacity 加在祖先上后代无法覆盖，而 `display` 仍然不动，
09 §4.2 的两条理由不受影响。该坑已写进 09 §4.2。

回归断言把证据一并打出来：`host opacity=0 visibility=hidden，节点行内 visibility=visible`
——正好证明祖先的 hidden 确实被覆盖了，靠的是 opacity 兜住。另加一条「切回框图页后恢复可见」。
`slice3-smoke` 增至 27 项；另外三套 e2e 与 web 131 项单测无回归。

**同日补：`docs/diagram-canvas-guide.md`（框图画布操作说明与约束，186 行）**。用户要「操作说明和限制与约束」。
写之前把设计与实现逐条对了一遍，因为 09 §6 里有几项**定了但还没实现**，照着设计写会误导使用者。
对出四类，全部据实分开写：

- **已实现的操作**：组件库点选与拖放、连线与三档反馈、参数面板（工程计数法、互斥单选、待填标记）、
  观测点（右键打点、点标记开面板、`iq` 置灰）、场景绑定与动态端口、错误定位与单击跳转、撤销重做。
- **画布当场拒绝的约束**（第 4 节）：端口类型必须相同、IQ 流与参数流不得直连、一个输入口一条边、
  观测点只能挂 IQ 输出口、互斥参数只能填一个、内部参数不出现。
- **首期的边界**（第 5 节）：**框图不落盘**（`Ctrl+S` 只标记本页，服务端没有框图端点，刷新即丢；
  已提交的任务在 `data/runs/<任务号>/diagram.json` 里能取回）、天线与接收机两类无组件、
  `iq` 产品不可选、实体下拉依赖场景已载入、回放源不可绑定场景、无子系统与旁路。
- **设计已定但尚未实现**（第 5.7 节）：`Ctrl+Z`/`Ctrl+Y` 还没接到框图上（`mapHotkey` 里没有）、
  双击空白弹组件搜索、拖动对齐参考线、右键菜单（当前是直接在第一个 IQ 输出口打点）、
  框图与场景的反向高亮。

**写说明的过程中发现并补掉一个功能空洞**：观测点标记没有点击处理，建完之后一旦取消选中，
就再也打不开它的面板改产品与参数了。已加 `onTapClick`，`slice3-smoke` 增两条断言
（点标记开面板、`iq` 开关置灰）。共 29 项。

**顺带订正两处初稿写错的**：错误标记不是「改该节点即清除该节点的」，而是任何编辑清空全部校验结果；
示例框图是三种，名字按 `EXAMPLES` 原文写。

---

### 2026-09-07 · D-050：天线与接收机两类组件缺位的定性（只记决策，设计另起对话）

用户看过 `docs/diagram-canvas-guide.md` 第 5.2 节「天线与接收机两类没有组件」后追问：
「辐射源发射机和天线、接收机和天线都需要在框图中，为何现在没有」。核实后记 D-050，
并按用户指示「在新的对话中先做设计，保存决策记忆」——**本轮不写任何组件代码**。

**核实到的事实**（不是照着路线图答的）：

- **不是遗漏，是切片顺序。** 06 §9E 的切片 ④「合流与厚化」写的就是「完整 S0→S6 链可搭」；
  `models/` 早已按 `radiator/channel/antenna/receiver/adc-ddc/channelizer` 六个环节建好目录。
- **但代价大于「两个空分组」。** 天线增益折进了信道：`engine/src/channel.cpp:122-123` 自场景取
  辐射源 `antenna_gain_dBi` 与站点 `antenna.gain_dBi`，`:237` 施加 `tx_power + G_t + G_r − 路损`。
  场景里的 `antenna.pattern` 字段**声明了但全库无一处使用**（grep 确认），
  所以方向图、指向、副瓣、极化损耗全部缺席，一律按各向同性标量增益。
- **接收机完全没有**：无噪声系数、无增益与 AGC、无 ADC 量化与削顶、无模拟滤波。
  噪声是独立 `NoiseSource` 加 `AddMixer` 按用户给的绝对电平手兑的，不是由噪声系数推出。
  场景帧其实算了 `noise_floor_dBm_per_Hz`，但 `channel.cpp:55` 注明它只供链路读数、不驱动注入。
- **现观测点名为 `s4`，严格说不是 S4**：没有接收机前端与 ADC/DDC，示例里三个节点同为 500 kS/s，
  等于把接收机建模成一个 500 kS/s 的窄带观测窗、发射带宽取 400 kHz。该简化本身已显式记在
  场景 `trace.notes` 与 08 §15，但 S1 / S2 / S3 在当前链路上无从生成。

**补齐的最小集四件**：`AntennaGain`（antenna）、`ReceiverFrontEnd`（receiver，噪声系数生热噪声）、
`AdcQuantizer`（receiver，产出 S3）、`DDC`（receiver，产出真正的 S4）。
**前三件是算术简单件，按 D-036 手写 C++ 即可，不必等 MATLAB；只有 DDC 卡在 M-2 的 Coder 产物。**

**三个必须先拍板的问题**（P1-0b 设计要解决的）：天线方向图用查表还是解析式、指向与极化损耗本期做不做；
接收机噪声与现有 `NoiseSource` 是取代还是并存；ADC 削顶的降级标记口径。
其中第二条若定为取代，**会改变现有示例框图与跨层算例 ① 的基准**，按铁律 10 必须走正式的基准变更流程，
不得顺手改——这也是先设计后实现的直接理由。

**登记处**：CLAUDE.md 决策日志 D-050；06 §9B 新增步骤行 P1-0b 并更新 §0.3 当前位置；
自动记忆 `next-antenna-receiver-design.md` 与 MEMORY.md 指针（供新对话开工时直接接上）。

---

## 2026-09-07 · 10 号方案：典型链路框图与检测识别评价（C-0，D-051）

**起因**。用户在 U-2 画布交付后指出：自由组合的框图用起来麻烦，希望先给出一个典型的框架——04 §7 从辐射源发射机到接收机到 DDC 与信道化的那条链——规定下来，用户只在每个环节上编辑参数、运行，得到信号的检测识别与评价；注重实用性，灵活性可适度降低。要求给出这部分的实施方案以指导后续实现。

**取证（均为实测核对，非照路线图作答）**。
- 目标链九个环节对照引擎目录：天线、接收机前端、ADC、DDC、信道化五个环节引擎里一件组件都没有；检测只有固定探针帧噪声估计的 `EnergyDetector`，`detections.jsonl` 端点读取层就绪但无生产者；**识别与评价在整个路线图里没有任何步骤行、没有任何代码、没有任何参考实现**（`algos/README.md` 末行「用户插件接口与评价基线尚未开始」）。
- 09 报告里「模板」有两个含义：§1.2 / §6.1 的「新建 › 全合成 / 实测回放 / 混合增强模板」是系统预置的起手链，本期承诺但从未实现；§6.11 / §16 排到 P2 的是用户把自建链存为模板，需要改框图格式。用户要的是前者的加强版（固定不可增删），与后者不冲突。
- 框图目前不落盘（`docs/diagram-canvas-guide.md` §5.1），固定链路加只改参数的工作流必须先解决保存与再开。
- 框图格式「未知键一律拒绝、三处同规则」与「参数值只有四种标量类型」两条硬约束，决定了方向图查表这类需要数组的东西首期进不了框图，只能走版本号引用外部文件。
- 切片 ② 示例里手兑的噪声 `−111 dBm` 恰好等于 `−174 + 6 + 10·log10(500 kS/s)`，说明「由噪声系数生成噪声」可以与现有示例逐值衔接，不是另一套口径。

**结论与产物**。`10.典型链路框图与检测识别评价实施方案_v1.0.md`（845 行）：§0 十条结论；§2 九个槽位、三种模式下的槽位状态、S0–S6 落点、采样率与频率计划（一处填多处派生，04 §9.3 六项检查）、槽位三态（启用 / 旁路 / 不适用）；§3 各环节组件语义（`AntennaGain` 解析式方向图 + 指向 + 五档极化表；`ReceiverFrontEnd` 噪声系数生噪 + 增益 + 频偏 + IQ 不平衡 `K1/K2` 形式 + 直流；`AdcQuantizer` 位数 / 满量程 / 削顶计数；DDC 与信道化的参数面；既有三件加参且缺省保旧行为；`SceneParamFrame` 增七字段、`BlockMeta.clip_count`），并在此拍板 D-050 三问；§4 检测（带删截滑动噪声估计 + 突发分段）、特征（M3 观测量提取）、识别（EM-S-04 E2 模板加权匹配 + 开放集未知，只到 `signal_role` 层，模板库 v1 取值标 `assumed`）、评价（真值来自场景参数帧或数据清单、只进评价器；帧级 / 突发级 / ROC 门限扫描 / 混淆矩阵；`metrics.json` 与 Python 参考逐值对拍）；§5 典型链路视图三栏、槽位卡片、频率计划面板、校验映射、框图落盘端点、自由画布去处、结果页四页签、探针；§6 格式与接口变更清单（`template_ref`、`RecognitionList`、`PortSpec.optional`、内部参数 11 → 16、错误码 19 → 21）；§7 基准变更（只有组件目录黄金基准更新一次，产品基准零变化，三份示例原样保留）；§8 C 线 C-0 至 C-11 与切片 ④ 重排为 ④a–④d；§9 验收清单；§10 不做与风险；附录给模板描述符骨架、全合成模式编译后的框图、`metrics.json` 结构、模板库 v1。

**回填**。CLAUDE.md：文档表加 10 报告、「已定」表加「框图形态」、里程碑表加 C 线并改 U 行、决策日志 D-051；06：§0.2 路线图加 C 线与切片 ④ 重排、§0.3 当前位置、§9B U-2 / U-4 / P1-0b 三行、§9E 切片 ④ 行、新增 §9G、§14 修正记录；09：v1.9 说明（§6、§7.1 开头各一段，§17 修订行），正文编号不动；`docs/diagram-format.md` §10 与 `docs/component-catalog.md` §7 加待写项；记忆文件更新。

**设计里刻意留下的两处「先记不做」**：DDC 与信道化在 Coder 产物到位前允许槽位旁路，旁路状态在页头与 `task.json` 明示，不隐藏；模板库 v1 的区间与权重是假定值，识别准确率的验收只对合成波形成立，甲方数据到货后按 04 §11.3 标定升版。

**下一步**：切片 ④a——C-1 格式与接口 → C-2 天线 / 前端 / ADC 三件（手写 C++，不等 MATLAB）→ C-6 框图落盘 → C-7 典型链路视图 → C-8 `demo-02` 场景。

---

## 2026-09-07 · C-1 典型链路的格式与接口（D-051）

10 号方案定稿后的第一步实现。C-1 只动契约不动模型：把后面九步要用的端口类型、可选输入口、
框图字段、参数帧字段与场景字段一次加齐，使 C-2 至 C-10 不必再回头改结构。

**改了什么**（引擎 → 前端 → 规范，同一件事三处同规则）。

| 项 | 落点 | 要点 |
|---|---|---|
| 端口类型 `RecognitionList` | `engine/include/cuav/types.h`、`types.cpp`、`catalog.cpp`、`web/src/api/catalog.ts` | `port_types` 6 → 7、`port_compat` 36 → 49；**既有 36 行逐字未变**（回归对拍确认）。生产者与消费者待 C-4、C-5 |
| `PortSpec.optional` | `component.h`、`graph.cpp`、`catalog.cpp` | 未连线的可选输入口不算悬空（`validate` 跳过），调度器也不等它的数据（`run_impl` 按实际连线判断就绪）。目录里只在为真时写出该键，既有条目字节不变 |
| 框图 `template_ref` | `diagram_json.{h,cpp}`、`docs/schemas/diagram.schema.json`、`web/src/diagram/doc.ts`、`docs/diagram-format.md` §7.1 | 三键 `{template_id, mode, version}`，取值错误报新错误码 `template`，缺字段与未知键仍报 `schema`。引擎只校验取值不解释语义 |
| `SceneParamFrame` 七字段 | `types.h`、`geo/{scenario.h,scenario.cpp}`、`engine/src/scenario.cpp` | 离开角、到达角、平台航向、`tx_on`、`tx_center_Hz` |
| `BlockMeta.clip_count` | `types.h`、`processing.cpp` | 削顶计数随块走；`AddMixer` 两路相加 |
| 场景 `emission.polarization` | `geo/scenario.h`、`scenario_json.cpp`、两份 schema、`docs/scenario-format.md` | 五档枚举可缺省，缺省 `vertical`；站点 `antenna.pattern` 由「解析后丢弃」改为真正入库 |

**实现时核实到的三件事**（都写进了代码注释，免得日后当缺陷查）。

1. **可选输入口必须同时改两处，只改校验会死锁。** `Graph::validate` 跳过未连的可选口之后，
   `run_impl` 仍按「每个输入口都要有数据」判断就绪，节点会永远等一个不会来的输入，
   最后落到「调度停滞」报错。所以就绪判断改为按**实际连了线的**输入口来算；
   由此还得出一条：一个只剩未连可选口的节点等同于源，每轮都该被调度。
   两种情形各写了一个单测（不连时照跑且收不到该口数据，连上时照收）。

2. **离开角不能由到达角取反推出。** 站点看辐射源的方位与辐射源看站点的方位在短基线上近似相差
   180 度，但俯仰角之和并不为零——地球曲率与高差都在里面。所以 `geo/` 里对每一帧另调一次
   反向 `look_angles`，而不是省一次计算去取反。单测把这条写成断言：方位近似互反（容差 1e-3），
   俯仰**不等于**取反。

3. **`AddMixer` 的削顶计数必须相加而不是取一路。** 混合增强模式下 ADC 只在合成目标那一路上，
   取哪一路当"主"都会漏计。两路相加在既有框图上恒为 0 + 0，因此不动任何基准。

**基准变更**（铁律 10，依据 D-051）。`tests/golden/component-catalog.json` 更新一次，
差异只有一种新端口类型与 13 条新兼容行；更新前先跑了一遍旧基准对拍，确认十二个组件的
`ports` 与 `params` 逐字未变，`demo-01` 的航迹基准与产品基准零变化。`tests/golden/README.md`
与 `docs/component-catalog.md` §5 的记录同步更新。

**验证**：引擎 121 项单测（新增 7 项）+ 8 项 ctest；服务 111 项（新增 1 项真引擎 `template_ref`
往返与拒绝）；web 133 项（新增 2 项键序与往返）；场景一致性 8 项；四套 e2e 全绿
（`scene` 14、`slice1` 39、`slice2` 27、`slice3` 29）；`check-paths.sh` 与 `check-ascii.sh` 通过。

**下一步**：C-2 天线、接收机前端、ADC 三件手写组件与既有两件加参，解析锚点见 10 报告 §3.2、§3.4、§3.5。

---

## 2026-09-07 · C-2 天线、接收机前端与 ADC 三件（D-051 / D-050）

D-050 里「天线增益折在信道里、接收机完全没有」这两条到此销项。三件都是算术简单件，按 D-036
手写 C++，不等 MATLAB Coder；DDC 与信道化仍卡在 M-2 / M-3。

**建了什么**。

| 组件 | 类别 / 档 | 要点 |
|---|---|---|
| `AntennaGain` | antenna / M3·E1 | 解析式方向图（全向常量增益；定向按 `12·[(Δaz/θaz)² + (Δel/θel)²]` 衰减、截止于副瓣底，`Δ = θ/2` 处恰为 3 dB 即半功率波束宽度的定义）；指向可固定或随平台航向；五档极化失配表与馈线损耗**只在接收端各计一次**；`scene` 是可选输入口，接了按帧里的离开角 / 到达角逐样点施加，不接按常量方向并记一条说明 |
| `ReceiverFrontEnd` | receiver / M3·E2 | 噪声系数生等效输入热噪声、总增益、本振频偏、IQ 幅相不平衡、直流偏置；私有随机子流；`noise_mode = none` 供混合增强模式 |
| `AdcQuantizer` | receiver / M3·E2 | I、Q 各按 `bits` 位均匀量化，`full_scale_dBm` 绑定码范围与物理功率；削顶逐块计数 |

两个既有组件加参数，**缺省一律保持旧行为**：`SceneEmitterSource.emit_at_tx_power`（真时按场景
`tx_power_dBm` 出电平，使 S0 读到发射功率本身）、`SceneBoundChannel.gain_mode`（`path_loss_only`
只施加路损）。观测点索引新增 `clipped_samples`。两份模型卡写在 `models/{antenna,receiver}/README.md`。

**D-050 三个待拍板问题的落地结论**（设计在 10 报告 §3，这里记实现时的取舍）。

1. **方向图用解析式**。框图参数只允许四种标量，查表方向图必须走「版本号引用外部文件」那条路，
   而首期没有任何实测方向图可填——写一个查表接口再喂进去编的数，比写解析式更不诚实。
   取 EM-B-07 §10.3 表里「定向 / E1 抽象」那一档，查表的 E2 档随 P3。

2. **接收机噪声由噪声系数生成，取代手兑噪声，且不动任何既有基准**。做法是：本组件是新增件，
   既有示例框图一个字没改。数值上两者本来就自洽——切片 ② 示例里手兑的 −111 dBm，正是
   `−174 + 6 + 10·log10(500000)` = −111.01 dBm，即 500 kS/s、噪声系数 6 dB 的解析值。
   热噪声常数**复用** `geo/link_budget.cpp` 的 `thermal_noise_dBm_per_Hz()`，只有一处定义，
   于是参数帧里的链路噪声底读数与实际注入的噪声逐项一致，界面上两个数不会打架。
   代价是与物理值 −173.975 差 0.025 dB，这是既有的工程惯例，跟随它比另立一套更好。

3. **削顶是数据标记不是降级**。逐块进 `BlockMeta.clip_count` 与 `state_reasons`，四态不变；
   累计进观测点索引 `clipped_samples`；只有全程比例超过 `degrade_clip_ratio`（缺省 1%）才在
   `flush()` 里降级。理由同 08 报告 §9.5 对整数样点时延跳变的处置。

**实现时核实到的四件事**。

1. **拆开增益必须证明等价，不能只证明各自正确。** C-2 把 `tx_power + G_t + G_r − L` 这一个乘法
   拆成四个组件各管一段。判据不是「每件算得对」，而是**两条链在同一帧上给出同一个电平**。
   在 `demo-01` 上实测：折在一处与拆开四件的接收电平差 **< 0.001 dB**（C-2 验收线是 0.05 dB）。
   这条对拍写成了 `test_channel.cpp` 的「增益口径等价」，它同时守住 `emit_at_tx_power` 与
   `gain_mode` 两个新参数。

2. **ADC 的满量程测试不能取 `0.999 × A`。** 二进制补码的正端比负端少一个码，正端最大幅度是
   `(2^(bits−1) − 1)·q` 而不是 `A = 2^(bits−1)·q`。8 位时 `0.999·A = 127.87·q` 四舍五入到 128，
   越界削顶 3189 个样点，量化信噪比因此低了 0.59 dB。改取 `(2^(bits−1) − 1)·q` 后，
   8 / 12 / 14 位三档都在解析值 `6.02·bits + 1.76 dB` 的 0.5 dB 内。位数越低这个边界越要紧。

3. **必填参数第一次出现在字符串那一路，测试骨架跟着要改。** `AntennaGain.role` 是第一个必填的
   枚举参数。`test_registry.cpp` 的「只给必填即可构造」原来假定必填项都是数值，
   只喂 `Num` 那张表，于是新组件一律构造失败。改为两张表都喂、去掉必填项时两张表都删。
   这条假设此前一直成立，是因为此前所有必填项恰好都是数字。

4. **组件按类型名排序时 `AdcQuantizer` 在 `AddMixer` 之前**（`c` < `d`）。写测试里的期望列表时
   按脑内字母序排会错，得按字节序。

**基准变更**（铁律 10，依据 D-051）。`tests/golden/component-catalog.json` 更新一次：新增三个
组件、两个既有组件的 `params` 各多一项。产品黄金基准（谱、包络、航迹、视窗抽取）**零变化**，
三份示例框图一字未改。更新前先跑了一遍旧基准对拍，确认差异只有上述五处。

**验证**：引擎 142 项单测（新增 21 项）+ 8 项 ctest；服务 111 项；web 134 项（catalog 测试由
「天线与接收机为空」改为「六类都不为空」，并新增一条断言可选口只有 `AntennaGain.scene` 带标记）；
四套 e2e 全绿；`check-paths.sh` 与 `check-ascii.sh` 通过。解析锚点逐条见两份模型卡的第 6 节。

**下一步**：C-6 框图落盘与结果端点，或 C-3 检测升级（两者互不依赖，都不挡 C-7）。

---

## 2026-09-07 · C-6 框图落盘、C-7 典型链路视图（切片 ④a 收口，D-051）

用户要的那件事到此可见：**打开框图页看到的是一条固定的九环节链，改几个参数点运行就出结果**，
不再需要拖组件、连线。

**C-6 服务端**。`server/src/diagrams.ts` 四个端点（列表、读、写、删），框图落 `data/diagrams/`，
小文件入库。写入与任务提交**共用同一段代码**——把 `manager.ts` 里「最小结构检查 → 内部参数
检查 → `data_id` 与 `scenario_id` 解析 → 解析旁挂」抽成 `prepareDiagram()`，两处对同一份框图
必然给出同一个判断。规范序列化形式与场景文件同法，因此「不改内容的保存」是逐字节的空操作。
坏框图先落 `.tmp`、过了引擎 `--validate` 才原子改名，盘上的好框图不会被覆盖。
结果端点同批加了 `features` / `recognitions` / `truth`（JSONL）与 `metrics`（整文件），
读取层先行、生产者随 C-4 / C-5，与当初 `track` / `links` 的做法一致。

**C-7 前端**。核心设计是一句话：**链路视图是框图文档的投影，不是第二份状态**。
`parseChain()` 从 `diagram.text` 解出九个槽位，改完由 `compile()` 编译回一份普通框图再走
`diagram/setDoc`。于是撤销重做、脏标记、引擎报错定位全部沿用 U-2 已有的那一套，一行没重写。
引擎与服务端也完全不知道「典型链路」这回事——它们看到的永远是一张普通框图。

| 文件 | 职责 |
|---|---|
| `web/src/chain/model.ts` | 槽位表：哪个环节用哪个组件、节点 id 叫什么、哪些参数从场景带出。编译、反解与界面三处都读它 |
| `web/src/chain/plan.ts` | 频率计划与 04 §9.3 的六项检查，纯函数 |
| `web/src/chain/compile.ts` | `compile()` / `parseChain()` / `switchMode()` |
| `web/src/chain/{ChainView,SlotCard}.tsx` | 三栏界面：左试验设置与频率计划、中九张卡片与观测点行、右参数面板 |
| `web/src/chain/examples/default.ts` | 内置缺省链路，由 `_gen.ts` 生成、由单测守住 |

路由 `#/diagram` 指典型链路、`#/diagram/canvas` 指自由画布；两者是同一个视图的两种形态，
不是第四个视图，所以三视图切换仍然只有三个去处。`Ctrl+S` 与工具条「保存」接到 C-6 的端点，
此前它只标记本页、刷新即丢。

**实现时核实到的四件事**。

1. **默认参数不给前端增益，整条链的输出就只剩量化噪声。** 第一次跑通时 S4 的谱峰是 −141 dBm、
   底噪随时间乱漂——因为 −111 dBm 的热噪声落在 −20 dBm 满量程、14 位 ADC 的最低有效位之下
   （1 LSB 相当于 −102 dBm）。这不是缺陷而是模型如实反映了一个没有增益的接收机。
   由此给频率计划**加了第六项检查**「ADC 量化噪声不淹没热噪声」：它只用本地参数
   （噪声系数、增益、采样率、位数、满量程）就算得出来，不需要几何，因此在没有场景的模式下
   同样有效。缺省链路的接收机增益定为 20 dB，余量 15 dB。

2. **保护带取 5% 而不是设计里写的 10%。** `demo-01` 的窄带观测配置是 400 kHz 带宽落在
   500 kS/s 采样率里，两侧各剩 50 kHz——按 10% 算，本项目自己的演示场景就会卡在 04 §9.3 的
   严格不等号上。5% 让这条检查仍然拦得住真正贴边的配置，又给既有窄带场景留出两倍余量。

3. **参数的键序必须取目录里的顺序，否则往返不是逐字节相同。** 新建的链与反解回来的链，
   参数进 `params` 的先后不同（一个是「用户值在前、恒定值在后、派生值追加」，另一个是
   「反解出来的全在一起」）。改为按目录的参数顺序排、目录之外的按名排在后面。
   这条是被 `compile(parseChain(doc)) === doc` 的单测逼出来的。

4. **回放模式不能看场景。** 频率计划原来无条件读 `s.scene.scenario.doc`，于是回放模式下
   把场景的采样率当成了回放数据的采样率。改为回放模式下不读场景；检测频段算不出来时
   **不覆盖用户填的值**，也不拿 0 顶替——顶替会让引擎报「频段上下限颠倒」，错误指向检测器
   而不是指向真正缺的那一项（铁律 15）。

**切片 ④a 的验收数字**（macOS，原型阶段验证值，浏览器里实测）：

| 判据 | 实测 | 解析 | 差 | 线 |
|---|---|---|---|---|
| S1 接收电平 | −51.60 dBm | −51.15 dBm（链路预算，距离 140.4 m） | 0.45 dB | 1.5（hann 扇贝损耗最坏 1.42） |
| S2 底噪 | −119.37 dBm | −119.35 dBm（−174 + nf + 10·log10 fs + 增益 − 10·log10 nfft + 等效噪声带宽） | **0.02 dB** | 0.5 |
| 拆开与折在一处的口径 | — | — | < 0.001 dB | 0.05（C-2 单测） |

**e2e 的两处订正**。① React 的 `onBlur` 实际监听 **`focusout`**，只派发 `blur` 事件不触发，
调了一次才发现（表现是等待超时而不是报错，很容易误判成别的问题）。
② 端到端用例必须对「服务端上一次留下的状态」免疫：勾观测点要写成「确保勾上」而不是「切换」，
收尾要把本次保存的框图删掉——否则第二次跑就红。

**既有 e2e 的入口改动**：`slice1` / `slice2` / `slice3` 都要显式进 `#/diagram/canvas` 才能用
示例框图下拉；`slice3` 的两条断言随 C-1 / C-2 放宽为「兼容矩阵是端口类型数的全枚举」与
「空分组数与目录一致」，并新增一条「六类都有组件（D-050 销项）」。

**验证**：引擎 142 项单测 + 8 项 ctest；服务 116 项（新增 5 项）；web 149 项（新增 16 项）；
五套 e2e 全绿——`scene` 14、`slice1` 39、`slice2` 27、`slice3` 30、**新增 `slice4` 25**；
`check-paths.sh` 与 `check-ascii.sh` 通过。

**下一步**：切片 ④b——C-3 检测升级（带删截的滑动噪声估计与 `detections.jsonl`）→ C-4 特征与识别
→ C-5 真值与评价 → C-9 结果页的检测识别与评价两个页签。

---

## 2026-09-07 · 用户实测反馈：画布上的连线几乎全没画出来

**现象**（用户截图）：框图页的自由画布里，典型链路的九个节点都在，但只画出一条连线。

**根因**（复现取证，不是猜的）。跑着的应用服务是**引擎重建之前**启动的，
`Engine.catalog()` 在进程内只取一次，于是它一直吐 12 个组件的旧目录。画布查不到
`AntennaGain` / `ReceiverFrontEnd` / `AdcQuantizer`，`portsOf()` 返回空端口，
没有端口 React Flow 就把连到这些节点的每一条边**静默丢掉**。逐项复现：

| 服务 | 目录 | 节点 | 连线 | 把手 |
|---|---|---|---|---|
| 旧（重建前启动） | 12 个组件、6 种端口类型 | 8 | **1** | 7 |
| 新（重建后启动） | 15 个组件、7 种端口类型 | 8 | **9** | 17 |

每个节点的把手数在旧目录下是 `scn 1、tx 1、tx_ant 0、ch 3、rx_ant 0、rx_fe 0、adc 0、det 2`
——四个新组件一个把手都没有，这就是截图里那条唯一的连线（`scn → ch.scene`，两端都是老组件）。

**改了三处**（画布本身没有缺陷，改的是「不静默」与「不必人工重启」）。

1. **服务端：引擎二进制变了就重取目录**（`server/src/tasks/engine.ts`）。用二进制的大小与
   修改时间做指纹，与缓存时的指纹不同即作废重取；取不到指纹（文件不在）就沿用缓存。
   开发时重建引擎不必再重启服务。单测：把假引擎包成带 shebang 的可执行文件当 `bin`，
   改写它之后 `catalog()` 必须返回新对象。

2. **画布：目录里没有的组件必须显眼**（`NodeCard.tsx`、`DiagramView.tsx`）。
   节点画成红框并写明「组件目录里没有 X：没有端口，连到它的线画不出来」；
   画布下方另有一条横幅，列出全部未知类型与**因此没画出来的连线条数**，并指出
   「引擎重建过而应用服务没重启时会这样」。此前这是一次静默降级（铁律 15）。

3. **典型链路视图的措辞**（`chain/model.ts`）。`UNAVAILABLE_REASON` 表里没有的组件原来一律
   说「本期未实现」——目录旧了的时候这句话把人引到错路上。改为 `unavailableReason()`：
   表里有的照旧（DDC 待 M-2 等），表外的说「组件目录里没有 X，引擎重建过而应用服务没重启时
   会这样，重启服务即可刷新目录」。

**顺带修一处仓库污染**。核实时发现 `demo-01.scenario.json` 被改成了 3 个站点 6 个航点，
且 site-1 的位置与首航点也被移动过——是那次**中途被打断**的 `slice2-smoke` 留下的：
它先改场景、跑完再存回原样，被打断就停在改完的状态。engine 的航迹黄金基准对拍与
web 的 `slice2.test.ts` 都因此报哈希对不上。按原始内容重建并**先核对哈希再写盘**
（重建结果 `72b8e8d0…` 与基准逐字符相符）。同时给 `slice2-smoke` 的 `finally` 加了一道检查：
收尾时比对场景文件与基准的哈希，不符就报出来并提示用 git 恢复——不再等到别的测试才发现。

**验证**：引擎 142 项单测 + 8 项 ctest、服务 117 项、web 149 项、五套 e2e（14 + 39 + 27 + 30 + 25）
全绿；旧目录下横幅与红框如实出现，新目录下 9 条连线全画出。

---

## 2026-09-08 · 用户实测发现：进了自由画布回不到典型链路视图（待修，另起对话）

**问题**：用户问「进入自由画布后如何回到典型链路视图」。核实结论是——**只有浏览器后退键，
或手动把地址改成 `#/diagram`**。画布工具条上没有回去的入口。

**实测矩阵**（`http://127.0.0.1:8080/`，逐条在浏览器里点出来的，不是读代码推的）：

| 做法 | hash | 落在哪个视图 |
|---|---|---|
| 初始进 `#/diagram` | `#/diagram` | 典型链路 |
| 点「展开为自由画布」 | `#/diagram/canvas` | 自由画布 |
| 按 `Alt+2` | `#/diagram/canvas` | **仍是自由画布** |
| 点顶栏「框图」按钮 | `#/diagram/canvas` | **仍是自由画布** |
| 浏览器后退键 | `#/diagram` | 典型链路 |
| 地址栏改 `#/diagram` | `#/diagram` | 典型链路 |

**根因**：`Alt+2` 与顶栏按钮都派发 `{type: 'ui/navigate', view: 'diagram'}` 而不带 `canvas`，
reducer 里 `diagramCanvas: a.view === 'diagram' ? (a.canvas ?? s.ui.diagramCanvas) : …`
于是保持原值；Router 的回写又因为 `cur.canvas === s.ui.diagramCanvas` 而不改 hash。
两处各自都对，合起来就是「切视图不改子形态」，人被困在画布里。

**为什么当初没做**：10 报告 §5.6 写的是「展开为自由画布（单向，展开后不能折回）」。
那句话说的是**文档**的单向性——在画布里自由增删过节点的框图可能已经不是九环节链的形状，
`parseChain()` 解不回槽位。但它与**导航**是两回事：没改动时理应能直接回去，
改动过也该给个明确说法，而不是让人找不着北。这是设计措辞被实现照字面执行了，记为一处修正。

**待修（另起对话，用户 2026-09-08 指示）**，三条：

1. 画布工具条加「回到典型链路」按钮，与「展开为自由画布」对称，`data-action="open-chain"`。
2. 按下时先 `parseChain(parse(text))`：解得开就直接回；解不开（画布里增删过节点）就明说
   「当前框图已不是典型链路的形状，回去需要新建一条链，当前改动会丢」并要用户确认，
   **不得静默丢弃用户的编辑**（铁律 15）。
3. `Alt+2` 与顶栏「框图」按钮回到框图页的**默认形态**（典型链路）：`ui/navigate` 在没有
   显式给 `canvas` 时按 `false` 处理。要进画布仍走按钮或直接用地址。

改动落点：`web/src/diagram/DiagramView.tsx`（按钮）、`web/src/state/reducer.ts`（`ui/navigate`）、
`web/src/shell/route.ts` 与 `Router.tsx`（若需要）、`tests/e2e/slice4-smoke.mjs`（补往返断言）、
`docs/diagram-canvas-guide.md` 与 10 报告 §5.6 的措辞。

---

## 2026-09-08 · 导航缺口已修：画布与典型链路之间双向可达（D-052）

按同日上一条记下的三条修法逐条落地。**三条都在浏览器里从画布出发实测过**，不是读代码推的。

**改了什么**

| # | 落点 | 改动 |
|---|---|---|
| 1 | `web/src/diagram/DiagramView.tsx` | 画布工具条最左加「← 回到典型链路」（`data-action="open-chain"`），与典型链路那边的「展开为自由画布」对称；解不开时在工具条下方展开一条黄色确认条（`data-chain-back-ask`），两个按钮 `open-chain-confirm` / `open-chain-cancel` |
| 2 | 同上 | 判据是 `parseChain(parse(s.diagram.text))`，用**原始文本**而不是画布那份 `doc`——后者经 `autoLayout` 补过 `position`，是渲染用的加工品 |
| 3 | `web/src/state/reducer.ts` | `ui/navigate` 的 `diagramCanvas` 由 `a.canvas ?? s.ui.diagramCanvas` 改为 `a.canvas ?? false`：不带 `canvas` 的导航 = 回框图页的默认形态 |
| 4 | `web/src/state/selectors.ts` | 探针 `app.diagram` 加 `canvas`。原来只有 `app.chain.canvas`，而 `chain` 探针在文档解不开时只剩 `{template: null}`，端到端读不到子形态 |
| 5 | `web/src/chain/ChainView.tsx`、`web/src/app.css` | 「展开为自由画布」的 `title` 由「展开后不能折回典型链路」改为指向回程；确认条样式 |

**一处实测纠正了原计划的措辞**。上一条待办里写的是「解不开（画布里增删过节点）」，实测下来
**删节点仍然解得开**：`parseChain` 对没出现的槽位按「可旁路的记旁路、不可旁路的保持缺省」处理
（`compile.ts:268-271`），删掉 ADC 之后照样解回一条链。真正解不开的是**加**了模板之外的节点
——`byNode` 里查不到就 `return null`（`compile.ts:247`）。端到端用例因此改为从组件库点一个
`NoiseSource` 进去（8 → 9 个节点），而不是删一个。

**确认后并不丢改动**。「仍要回去」只是导航，落在典型链路页那条既有的「不是本模板」提示上
（`data-chain-foreign`，C-7 时就有），由用户显式点「新建典型链路」才真的换掉文档，或者点
「在自由画布打开」原样回来。所以铁律 15 的「不静默丢弃」是**两道**闸而不是一道：画布这边先
说清楚，典型链路那边再要一次显式动作。端到端断言把这一点钉住了——确认之后节点数仍是 9。

**实测结果**（`slice4-smoke` 25 → 34 项，新增九条）：

| 断言 | 结果 |
|---|---|
| 画布工具条有「回到典型链路」 | 通过 |
| 解得开就直接回、不弹确认，hash 落 `#/diagram` | 通过 |
| 顶栏「框图」按钮从画布回到默认形态 | 通过 |
| `Alt+2` 从画布回到默认形态 | 通过 |
| 加一个模板之外的节点后不再是典型链路 | 通过（8 → 9 节点，`template null`） |
| 解不开时先说明再问，按一下不走人也不丢改动 | 通过 |
| 选「留在画布」后改动原样还在 | 通过（9 节点，`canvas true`） |
| 确认后回到框图页、给出新建的出路、改动到此仍未丢 | 通过（9 节点，`foreign true`） |
| 全程无未捕获异常 | 通过 |

**全量回归**（macOS，原型阶段验证值）：引擎 142 项 doctest（34815 条断言）+ 8 项 ctest、
服务 117 项、web 150 项、五套端到端 14 + 39 + 27 + 30 + 34 = 144 项，全绿；
`scripts/check-paths.sh` 与 `scripts/check-ascii.sh` 均通过。开工前核实过 8080 上的服务
（PID 24690，2026-09-07 23:30 启动）晚于引擎二进制（21:57 构建），且引擎与服务的源码都不比
各自产物新；服务按请求读 `web/dist`，重建前端后无须重启（页面上的 bundle 名与本次构建一致）。

**文档措辞同步改了四处**：10 报告 §0 表第 4 行与 §5.6（并在 §11 修正表补一行记因由）、
09 报告 §17 修订行、`docs/diagram-canvas-guide.md`（头部说明、入口段、新增 §2.8、快捷键表）。
WORKLOG 上一条的历史记录不改。顺带在画布说明的头部把 §5.2「天线与接收机两类没有组件」
标为过时（C-2 已落地 15 个组件），正文留到 C-11 全篇改写时一并订正。

**下一步**：切片 ④b，C-3 检测升级（滑动噪声估计与恒虚警、突发分段、`detections.jsonl`）。

---

## 2026-09-08 · 观测点勾选「看不出区别」的三处修复

用户实测反馈：「S0–S5 勾上和不勾我看不出有什么区别」。先查再改，结论是**链路本身是通的**，
勾选确实生效，但效果全落在别处，框图页上一点看不见。

**先取证**（浏览器里实跑一遍，不是读代码推的）：勾 S1、S2 后框图的 `observation_points`
由 1 个变 3 个，`task.json` 记 `["s1","s2","s4"]`，结果页左栏列出三条，读数也确实不同——
S1 峰值 −52.61 dBm（链路预算）、S2 −32.61 dBm（前端 +20 dB 增益后）。

**四个原因，前三个是界面的问题，第四个是物理事实**：

| # | 原因 | 性质 |
|---|---|---|
| 1 | 勾选只改框图，产品要**下次运行**才有；界面只写「会产出频谱与包络」，没说要重新运行 | 缺提示 |
| 2 | 勾完框图页上什么都不变；观测点做成了脱离链条的一行复选框，名称只在 `title` 悬停提示里 | **实现偏离设计**：10 §5.1 版式图写的是「圆点：观测点 S0…S5」 |
| 3 | 结果页的观测点列表只在左栏，而左栏可以收起，收起后换不了观测点 | 单点入口 |
| 4 | S2 / S3 / S4 读数本来就几乎一样：DDC 与信道化未实现（M-2 / M-3），S4 直接取 ADC 输出，ADC 只量化不改电平。实测 S2 与 S4 峰值都是 −32.61 dBm | 不是缺陷 |

**改了前三件**（第四件等 Coder 产物到位自然分开）：

| # | 落点 | 改动 |
|---|---|---|
| 1 | `web/src/chain/model.ts` | `TAP_ANCHOR` 加 `short` 短名；新增 `TAP_AFTER_SLOT`（槽位 → 挂在其输出口之后的观测点）与 `tapLabel()`（`s4` → `S4 主产品`，链上与结果页共用一套名字） |
| 2 | `web/src/chain/ChainView.tsx` | 观测点**保持链条下方独立一行、链条结构不动**（用户 2026-09-08 定），但写全名：`S0 辐射源输出`…`S5 子信道`，2K 下一行放得下。连接箭头由「卡片之前」挪到「卡片之后」，消除折行时行首的孤立箭头 |
| 3 | 同上 | 工具条加「观测点与当前任务不一致（现在是 …），重新运行后生效」，没有在先的任务时不提示 |
| 4 | `web/src/results/ResultsView.tsx` | 中栏顶上加一排观测点页签（写全名），左栏列表保留作详情 |
| 5 | `web/src/state/reducer.ts` | `firstSpectrumOp` 改为 **S4 优先**：勾了中间观测点不该把默认视图从主产品挪走 |
| 6 | `web/src/signal/SignalHead.tsx` | 页头的观测点也走 `tapLabel`，与页签同一套名字（原来显示裸 `s1`） |

**中栏版式一并收了**（同一轮用户指示「卡片放大、纵向居中或让链条区占满高度，把空白吃掉」，
随后又指示「把框图的上下间距拉开，并且三个框图一行」，再指示「框图太大了，保持之前的大小」）：
链条与观测点行放进一块占满工具条以下高度的面板 `.chain-body` 并纵向居中；`.chain-strip` 由一行
九张的 flex 改为**三列网格**，九个环节排成 3 × 3，行距 56 px；**卡片 176 × 128、字号加一档**
（中途放大到 190 px 高、宽随列自适应封顶 420 px，用户看过判定太大，退回）。
卡片退回小尺寸后三张会挤在中间、两侧空一大片，因此把**连接件改成横向拉伸**
（`.chain-arrow` 是 `flex: 1` 的细线加箭头），卡片不变大而链条仍横贯中栏。
实测中栏 1786 px，链条区 496 px 高、上下各余 346 px，面板底部只余 8 px。
**10 §5.1 的版式图仍画作一行，那是示意**，实际排布以 §5.2 新增的那条为准。

**中间走过一版又撤回**：先按 10 §5.1 版式图把观测点做成链上卡片之间的圆点（六个各占 54 px，
卡片因此收到 152 px），实现并通过 e2e 后，用户定为**不动链条结构**、只把编号换成全名。
撤回时保留了那一版顺带修好的两处：连接箭头挪到卡片之后（孤立箭头），以及三处共用的 `tapLabel()`。

**实测结果**（`slice4-smoke` 34 → 46 项，新增十二条）：

| 断言 | 结果 |
|---|---|
| 观测点写全名而不只写编号 | 通过（`S0 辐射源输出 | S1 接收天线端 | … | S5 子信道`） |
| 六个全名在 2K 基线下仍是一行 | 通过 |
| 九个环节按 3 × 3 排 | 通过（3 个 / 3 个 / 3 个） |
| 行与行的上下间距拉开（≥ 32 px） | 通过（56 px） |
| 链条区占满中栏高度、链条纵向居中 | 通过（底部余 8 px；上 346 / 下 346 px） |
| 换行时下一行不以孤立箭头开头 | 通过（3 行） |
| 刚跑完、观测点没动过时不提示 | 通过 |
| 取消一个观测点后当场说明要重新运行 | 通过 |
| 勾回去之后提示消失 | 通过 |
| 结果页中栏有观测点页签，写的是名字不是 `op_id` | 通过 |
| 默认选中主产品 S4 | 通过 |
| 左栏收起后页签还在、点得动、页头随之改 | 通过 |
| 勾上的中间观测点读数确实不同 | 通过（S1 −51.60、S2 −31.60，差 **20.00 dB** = 前端增益） |

**全量回归**（macOS，原型阶段验证值）：引擎 142 项 doctest + 8 项 ctest、服务 117 项、
web 150 项、五套端到端 14 + 39 + 27 + 30 + **50** = 160 项，全绿（`slice4` 连跑三遍稳定）；
两项出包前检查通过。

**踩到并修掉一处端到端用例的竞态**：两次出现「50 项失败 1 项」但重跑不复现。查因是
`page.waitFor` 轮询的是 store 探针，探针先于 React 重绘——「等状态变了再立刻读 DOM」会偶发读到
上一帧。新增 `waitDom()` 轮询 DOM 本身，把三处「状态变了看界面是否跟着变」的断言改为等 DOM，
并去掉两处靠固定 `sleep(400)` 的写法。改后连跑三遍、加上此前的五遍共八遍全绿。

**顺带核实的一件事**（解释了用户最初那张截图为什么没有左右栏）：`reducer.ts:25`
`leftCollapsed: innerWidth < 1920`——窗宽不足 1920 CSS px 时两侧栏默认收起，中栏因此占满宽度、
链条折成两行。这是既有的设计行为，不是缺陷。

10 报告 §5.2 / §5.7 补上落实条款，§11 修正表记一条「版式图 vs 实现」的偏离。

**下一步**：切片 ④b，C-3 检测升级。中栏那块空白已按版式吃掉（面板 + 居中），将来若要在
链条下面放链路读数或 S4 频谱预览，`.chain-body` 里直接加一段即可，不必再动版式。

---

## 2026-09-09 · L-0 多源多站测向定位方案定稿（11 号报告 v1.0，D-053）

**用户提问**：「场景中的辐射源可能有多个；单个的侦测站要做到信号的识别与测向；多个测向站
可以对同一个信号源做交叉定位或者时差定位。这些在框图里面如何体现、如何设置？」用户随后拍板
四个岔路：交付范围 = 设计 + 纯软件第一切片；测向做到 M2 效应级；多源按源分支再叠加；多站
加站点维度并在尾部加两个槽位。

**开工前核实到的现状**（全部实测，不是引自文档）：

| 面 | 核实结果 |
|---|---|
| 多源 | 参数帧层**已经支持**：`engine/src/scenario.cpp:174-185` 对场景里每个辐射源开一个 `link:<emitter_id>` 输出口，`links.jsonl` 已是站 × 源全交叉。挡住的是典型链路视图——`web/src/chain/model.ts:173-174` 的 `siteId` / `emitterId` 是标量，`compile.ts:59-63, 108` 只连一个源 |
| 多站 | 被 `engine/src/channel.cpp:125-133` 一条守卫挡死（「首期只支持单站场景」）。该守卫的理由是「接收天线增益取唯一站点的」，而 C-2 之后天线增益已经拆到 `AntennaGain` 里，守卫已名不副实 |
| 测向与定位 | **没有任何生产者**。帧里的 `aoa_az_deg` 是真值，只给 `AntennaGain` 查方向图用（`antenna.cpp:250-251`）；七种端口里没有报告类端口；`docs/display-route.md` §3 已冻结「测向线 / 2σ 椭圆」的语义，§4 明写「它们的生产者都还不存在」 |
| 可移植资产 | emcore 有完整的三站测向 + AOA 交汇 + TDOA 实现与四份黄金基准（`direction_finding` / `aoa_location` / `tdoa_location` / `geo_fix`），且踩过一次 CEP 公式错误并修正过（`√trace → 0.5887·(σmax+σmin)`） |
| 上游依据 | 04 §5.1 的端到端对象末环节本来就写着「测向或定位」，但 §12.1 / §21 定首期单站；05 把测向归 P1、多站定位归 P2，**但 §3.2 明写「P2 可以先用纯软件多站数据开展」**——这句话是本线不必等 P1 / P2 的依据 |

**方案要点**（`11.多源多站测向定位实施方案_v1.0.md`，807 行，十二条结论见 §0）：

- **多源按源分支、接收天线后叠加**。前四个环节（辐射源 / 发射天线 / 传播信道 / 接收天线）
  按源或按链路实例化，新组件 `Superposition` 在接收天线端把 N 路复基带相加，此后仍是一路
  IQ。理由是物理对应：天线口面上是电磁场的线性叠加，接收机只有一条通道。
- **多源里辐射源只按源实例化、不按链路**。同一个源发出的是同一份波形，不同站的差别全在
  传播。若每条链路各生成一份，同一源在不同站的载波相位与突发时刻会各自独立，**TDOA 就
  失去物理意义**——时差本来就是同一份信号走不同路程造成的。
- **多站 = 典型链路加站点维度**，槽位 9 → 11（尾部加「测向」与「多站定位」）。
- **测向与到达时间是 M2 效应模型**，报告 §1.3 把「取真值是否合法」正式论证并落成三条声明
  （`model_layer=M2` / `credibility≤V2`、每行 `truth_consumed=true`、模型卡写明散布来自建模的
  误差预算）。04 §5.2「真值只进评价器」约束的是被测的 M3 算法，本线是新加的观测量生产者。
- **固定可选口取代动态端口**（`in1..8` / `scene1..8` / `b1..8` / `t1..8`）：零格式改动，
  身份靠帧与报告自带的 `site_id / emitter_id / link_id`。配套启用**已预留但一直没用过的错误码
  `port_optional`** 与新虚函数 `IComponent::check_wiring()`，错误码总数仍是 21。
- **`template_ref.version` 不升**：v1 文档是 v2 槽位表的真子集，N=K=1 时新槽位缺席、节点无
  后缀，逐字节反解再编译回去相同。**升版号反而会让既有框图解不开**。
- **实例后缀选双下划线 `__`**：节点 id 正则是 `[a-z0-9_-]{1,64}`，`@` 与 `:` 都不合法；而
  既有槽位 id 里只有单下划线（`tx_ant` / `rx_fe`），双下划线因此可无歧义地切分。
- **移植 emcore 时把求解器改成平面坐标接口**（吃米不吃经纬度）：数学与投影解耦，golden 走
  `geo::legacy` 的 111320 常数投影回放、引擎走严格 ENU，一份代码两处用。这是 D-009「移植模块
  守原常数、新代码用严格 ENU、禁止顺手统一」在本线的具体落地方式；`build-all.sh` 加一条
  `grep legacy::` 守卫，防止有人顺手在新代码里用了 legacy 常数。
- **TDOA 缺省加权改为 `correlated_reference`**。emcore 把各站对当作独立，而参考站的噪声进了
  每一个站对，按独立处理会低估协方差、椭圆偏小。两种口径都实现、都进模型卡、验收时并列给出
  迹比，**不许调参使二者一致**（铁律 10）。
- **2σ 椭圆的二维置信是 86.5% 不是 95%**（`1 − exp(−2)`）。em-demo 的注释写「2σ (~95%)」是把
  一维置信搬到了二维。每行报告显式写 `ellipse.scale = "2sigma"` 与 `confidence = 0.865`，
  验收判据「真值落椭圆内的比例」按 [0.80, 0.93] 定。

**三个解析锚点**（进 `engine/tests/test_locate.cpp`，与 golden 对拍互补）：AOA 两站正交交汇、
各距 1 km、σ_θ = 1° 时 σ_pos = 1000·π/180 = **17.4532925 m**、2σ 半轴 **34.906585 m**、
CEP = 1.1774σ = **20.5495 m**；TDOA 三站等边、源在质心时不加权 GDOP = √(8/9) = **0.942809**。

**记录**：决策 D-053 进 CLAUDE.md（文档表加 11 报告、`:71` 端口预留口径改写、已定表加
「多源多站表达」、里程碑表加 L 行、上游修正段加 PROVISIONAL 声明、EM-S-05/06/07 归属行改写）；
06 备忘录 §0.2 路线图加 L 线、§0.3 加当前位置行、§1.2 端口表两行改写、§9E 加切片 ⑥ 三行、
新增 **§9H L 线（L-0 至 L-9）**、§14 记一条修正；10 报告改五处并加「补：D-053 对本方案的修正」
一节；`docs/concept-model-layers.md` 的 EM-S 行改写。

**顺带发现并统一了一处口径出入**：06 §1.2 把 `BearingReport` 列在「观测量归约」流里，而
CLAUDE.md `:71` 说 05 P0 的八个端口**全部**只做命名预留——两处从 D-030 起就对不上，这次一并
统一为「`BearingReport` / `PositionReport` 本期实现，其余五件仍只预留，另补 05 名单外的
`ToaReport`」。

**下一步**：切片 ⑥a — L-1 格式与接口（三种端口、帧身份字段、双绑定放宽、多站守卫替换、
`check_wiring`、`sites[].clock`、观察者两回调），然后 L-2 `Superposition` 与多源模板。

## 2026-09-09 · L-1 格式与接口（三种报告端口、多站解锁、站钟）

按 11 报告 §6 逐项落地，一次改完引擎、schema 与四份规范。

**端口类型 7 → 10**。`BearingReport` / `ToaReport` / `PositionReport` 加在枚举末尾，
`can_connect()` 规则一字未改（仍是纯对角），于是 `port_compat` 由 49 行变 100 行而**既有 49 行
逐字未变**——这一点用脚本逐键对比核实过，不是靠肉眼看。三种报告的结构体（含六分量测向 σ、
四分量到达时间 σ、误差椭圆与残差）写进 `component.h`，`PortData` 相应加三个向量并在 `clear()`
里一并清空。

**帧的身份三件**。`SceneParamFrame` 加 `link_id / site_id / emitter_id` 三个字符串，由
`ScenarioSource` 直接从 `LinkFrameSource` 取。没有走「从 `trace_id` 里解析」那条路：`trace_id`
是溯源约定不是数据契约，让消费者解析它等于在两个模块之间建一条隐式协议，将来改溯源格式会静默
改坏测向。

**多站守卫替换**。`channel.cpp` 原来是「站点数 ≠ 1 即 configure 失败」，理由写的是「接收天线
增益取唯一站点的」——而 C-2 之后增益已经拆到 `AntennaGain`，典型链路用 `gain_mode =
path_loss_only`，这条守卫早就名不副实。改为三分支：`site_id` 空且单站取唯一站（**旧行为原样
保留**，既有框图一字不用改）、空且多站报错、非空按 id 找。

**双绑定放宽**。`scene_binding` 由「必须且只能二选一」改为「至少一个」（schema `oneOf →
anyOf`）。**过程中发现并修掉一处闸门被绕过的写法**：起初把「两个键同时出现须目录同时声明两个
内部参数」这道闸写在 `inject_scene` 里，但那个函数开头有一句「组件没有 `scenario_path` 内部参数
就早退」，于是不吃场景的组件（测试里的 `FakeBindable`）带两个键会从早退溜过去。闸门提到早退
之前才真正生效——单测里那个组件正好是这种，等于替我把这个洞踩了出来。

**`check_wiring` 与 `port_optional`**。固定的可选输入口取代动态端口，代价是「一个口都没连」在
`Graph::validate` 眼里也合法。加一个虚函数让组件自己声明「到底要连几个」，失败映射到
**`docs/diagram-format.md` 里预留了但一直没用过的错误码 `port_optional`**，因此错误码总数仍是
21，不增。调用点放在悬空口检查**之后**：必填口没连是更基本的错误，先报那个报错才指得准。

**站钟 `sites[].clock`**（可选）：`sync_sigma_ns` 必填，另有 `bias_ns` / `rx_delay_ns` /
`rx_delay_sigma_ns` / `sync_state ∈ {locked, holdover, unsynced}`。缺席时 `has_clock` 为假——
时差定位组件据此报错，**不默认一个完美时钟**（默认 0 ns 会让 TDOA 结果好得不真实，铁律 15）。
在 `LogicalSim` 下这些是**建模的**同步误差，规范里写明了它不是任何真实设备的时统指标。

**运行器**加 `on_bearing` / `on_position` 两个回调，惰性开 `bearings.jsonl` / `positions.jsonl`
并各发一条事件，与 `on_link` 完全同法，没有新造机制。事件载荷 = 行去掉 `t_s`（信封里已有）。

**基准变更**（铁律 10，逐项核实后重生成一次）。`tests/golden/component-catalog.json` 的差异用
脚本逐键比对，确认只有三类：① `port_types` 7 → 10 且既有前缀不变；② `port_compat` 49 → 100 且
既有 49 行内容**零变化**；③ `SceneBoundChannel` 追加内部参数 `site_id`，既有 8 个参数一个未改。
**产品黄金基准与 `demo-01` 航迹基准零变化**，`DEFAULT_CHAIN_TEXT` 零变化。

**前端**：`PortType` 加三种，把手形状给报告族一个新的六边形（前四种形状已被占，颜色之外还要靠
形状区分，09 §6.3 色弱可辨）；三处计数断言随之更新（10 种端口、100 行矩阵、全库内部参数 11 → 12）。

**顺带修掉 `slice4-smoke` 的一处偶发竞态**。「观测点与当前任务不一致时提示重新运行」这一项五次
里红一次：`hadTask` 读自 store 探针而 `stale` 读自 DOM，探针先于 React 重绘。这与 2026-09-08 那次
修过的是同一个根因，当时漏了这一处；改用既有的 `waitDom()` 等 DOM 本身，连跑三遍全绿。

**回归**（macOS，原型阶段验证值）：引擎 **145 项** doctest（新增站钟三例与多站一例）+ 8 项 ctest、
服务 117 项、web 150 项、五套端到端 14 + 39 + 27 + 30 + 50 = 160 项，全绿；`scripts/build-all.sh`
含路径与 ASCII 两项检查全过。

**下一步**：L-2 `Superposition` 与多源模板。

## 2026-09-09 · L-2 多路叠加与多源模板（切片 ⑥a 收口）

**引擎侧：`Superposition`**（channel / M3 / E2 / EM-B-09）。八个输入口 `in1..in8` 全部可选，
逐样点相加，落点在**接收天线之后、接收机前端之前**。选这个落点是因为物理对应：天线口面上是
电磁场的线性叠加，接收机只有一条通道；放在前端之后等于给每个源配一台接收机。

不改 `AddMixer` 而新建一个类：`AddMixer` 已进目录黄金基准并被混合增强模式使用，把它从两口改成
八口属于铁律 10 意义上的基准变更，而收益只是省一个类。

**四项一致性一旦不符就报错，绝不截断**：采样率、中心频率、首样点序号、块长。理由是 08 报告
约定四——调度器每轮**无条件覆盖**下游缓冲，截断意味着较长那一路的尾部样点被下一轮覆盖掉，
样点丢了还不报错。同理，已连上的口本轮没数据就整轮 `Idle`：少加一路等于凭空少了一个辐射源。

**前端：三个实例维度**。`SlotDef.per ∈ {emitter, link, site, single}`，`ChainState` 的
`siteId / emitterId` 两个标量改成 `siteIds[] / emitterIds[]`。节点 id 只在**取值多于一个**的维度
上加 `__` 后缀，因此 N = K = 1 时一个后缀都不出现，`DEFAULT_CHAIN_TEXT` 逐字节不变。
`splitNodeId()` 按已知基名切分（长基名优先），`tx` 与 `tx_ant` 不会混：`tx_ant__x` 不以 `tx__` 开头。

**辐射源按源实例化、不按链路**——这条是有理由的，不是省事：同一个源发出的是同一份波形，若每条
链路各生成一份，同一源在不同站的载波相位与突发时刻会各自独立，**时差定位就失去物理意义**。

**S1 的落点随之改**：多源时「接收天线端」是叠加**之后**那一点，接收机看到的就是各源之和；
S0 是唯一按源实例化的观测点（`s0__uav-1`），其余按站。

**两处在真引擎上才暴露出来的设计错误**（前端单测都是绿的，`cuav_run --validate` 一跑就红）：

| 现象 | 根因 | 改法 |
|---|---|---|
| 2 源 1 站的图报「多站场景的场景绑定信道必须绑定 site_id」 | 判据写成了「**选了**几个站」，而引擎看的是「**场景里有**几个站」。demo-03 有三个站，只选一个时信道仍然无从推断 | `bindOf` 改看 `scenario.sites.length`；场景只有一个站时不写 `site_id`，于是 demo-01 的字节不变 |
| 3×3 的图报「`SceneEmitterSource` 只能带 entity_id 或 site_id 之一」 | 我把「多站时同时绑站」加在了所有绑源的节点上，可辐射源本来就该只绑源 | `SlotVariant.bind` 增加第三态 `link`（同时绑源与站），只有 `SceneBoundChannel` 用它。这正好撞上 L-1 那道「目录没同时声明两个内部参数就拒绝」的闸——闸门按预期挡住了错误的绑定 |

**多源叠加的功率对拍（切片 ⑥a 的硬断言）**。`demo-03` 上取 2 源 1 站跑 4 s，S1 观测点全窗
`stat=mean` 抽取，峰值邻域 ±5 bin 线性域求和得 **−64.405 dBm**；扣掉 hann 窗的等效噪声带宽
1.761 dB（D-049 ⑪ 已定的读数口径）得 **−66.166 dBm**。两条同频链路各自的链路预算
（`tx_power + G_t + G_r − path_loss`，路损取窗内功率域平均）分别是 −77.046 与 −66.535 dBm，
线性和 **−66.165 dBm**。**偏差 −0.001 dB**（验收线 ±0.3）。两个源有意同中心频率同频偏，
落在同一个频点上——这既是叠加正确性的最强证据，也是后面测向混叠要用的素材。

**规模实测**：3 源 × 3 站编译出 **45 节点 72 连线 6 观测点**，`cuav_run --validate` 通过。

**场景 `demo-03`**（新，不动 `demo-01`，不抢 C-8 的 `demo-02`）：三站等边三角形，实测边长
2199 / 2203 / 2199 m（外接圆半径 1270 m）；站钟 3 ns 锁定 / 5 ns 锁定 / 10 ns 保持态且固定钟差
20 ns；三个源——`uav-1` 穿越三角形、`uav-2` 与它**同频**且 30 s 关 40 s 开、`uav-3` 悬停发突发
（带宽 200 kHz、频偏 −97656.25 Hz = −200 个 bin，nfft 1024 下落在频点中心不吃扇贝损耗）。
铁律 4 逐站逐源核过：同频两源 248828.1 < 250000，突发源 197656.2 < 250000。
黄金航迹 `tests/golden/scenario-track-demo-03.json`（723 个样点、三个实体）并加了逐点对拍。

**踩到一个不显眼的约束**：活动时间线要求**全局**按时刻非降，不是按辐射源分组——第一版按源分组
写，装载器立刻报「活动时间线必须按时刻非降排序」。生成脚本改为最后统一排序。

**场景编辑器**：加「布目标」工具（`addEmitter` 复制第一个源的发射参数并建一条单航点航线——
静止目标同样要有航线，否则运行时取不到位置）、`removeEmitter`（连同航线与活动一起删，不留
悬空引用）、`moveEmitter`；辐射源表单加「删除目标」与「链路（K 个站）」只读表。
顺带**修掉一处硬连**：点地图上的目标图标原来选中的是 `sites[0]` 拼出的一条链路，多站之后
「第一个站」不再有意义，改为选中被点的那个辐射源。

**频率计划新增两项检查**：`stations`（三种模式下 N、K 的可行组合与 ≤ 8 的上限——回放模式的片段
是已经过完整接收链的 S4 数据，对应一个录制时的站位，复制到多个站等于假装同一份录音在几个地方
同时被收到）与 `sites_consistent`（各站 fs 与中心频率一致）。多源时带宽与边缘两项检查改取
**最难放进接收带**的那个源，取第一个源会放过其它源的越界配置。

**基准变更**（铁律 10）：组件目录黄金基准第二次重生成，脚本逐键核对差异**只有新增
`Superposition` 一条**，既有 15 条与 `port_types` / `port_compat` 零变化。

**顺带修掉两处端到端用例**：`slice2` 的「工具条四件齐全」改为五件（新增布目标）；`slice4` 的
观测点提示断言改为**轮询到 store 与 DOM 自洽再断言**——上一版只等了「有任务就该有提示」一个
方向，另一个方向（没任务却有提示）仍会偶发红。改后连跑六遍全绿。

**回归**（macOS，原型阶段验证值）：引擎 **151 项** doctest + 8 项 ctest、服务 117 项、
web **163 项**、场景一致性 8 项、五套端到端 14 + 39 + 27 + 30 + 50 = 160 项，全绿；
`scripts/build-all.sh` 全过。

**下一步**：L-3 `DirectionFinder` 与多站模板（测向槽位、站后缀在界面上的体现、`bearings.jsonl`
与端点、地图测向线）。

## 2026-09-09 · L-3 单站测向与多站模板（切片 ⑥b 前半）

**`geo/df_error.h`**（新，纯函数零依赖）：EM-S-05 §10.14 的七分量误差预算与 DF-Q 分档。
信噪比项写成 `σ_ref·√(SNR_ref/max(SNR,1))` 而不是 emcore 的 `σ_base/√SNR`——把参考点显式化，
否则「σ_base 对应多少 dB」藏在常数里，换个体制没法解释。质量分档**只看 σ 不看信噪比**
（信噪比已经进了 σ，再判一遍等于把同一个量算两次），比 emcore 的双判据干净。

**`DirectionFinder`**（algorithm / **M2** / E2 / EM-S-05）。八个 `scene` 口全可选，下限由
`check_wiring()` 声明（至少一路），失败映射到 L-1 启用的 `port_optional`。信噪比、发射功率、
两端天线增益、接收带宽全部**从场景派生**，不开用户参数——它们在场景里已经声明过一次，
让用户在测向卡片上再填一遍必然与场景脱节。`tx_on` 为假或信噪比不足时仍出一行，
只是 `df_result_state = invalid` 并写明原因，不是不输出（铁律 15）。

**混叠判据第一版是错的，实测才看出来**。原写「对方 ≥ 本方 − separation 且 对方 ≥ 本方」，
化简后就是「对方更强」——电平差那道闸等于不存在。demo-03 上 1800 行报了 1200 行混叠，
连电平相差 15 dB 的突发源都算了进去。改成三个条件缺一不可：① 两源的**占用带**重叠
（`|Δf| < (B_i + B_j)/2`）且都落在本站接收带内；② 对方更强；③ 电平差 < `mixture_separation_dB`。
改后 747 / 1800 行标混叠，且分布合乎几何：site-1 上 uav-2 最强（−67.3 dBm），uav-1 低 9.2 dB
被标（147 行，随几何变化进出阈值），uav-3 低 6.9 dB 于 uav-1 也被标；site-2 上 uav-1 最强、
uav-2 低 15.3 dB 不算混叠。**「功率差大时弱信号被压住、不是指偏」这条物理判断，只有把
三个条件分开写才落得下来。**

**统计判据（本步的核心，不是「跑得起来」）**。M2 效应模型的正确性判据是「抽出来的散布与它
自己声称的 σ 是不是一回事」，即 01 §8 的跨层一致性。demo-03 三站三源跑 20 s 得 1800 行：
`|误差| ≤ 3σ` 占 **99.56%**，归一化误差的样本方差 **1.0225**（线 [0.9, 1.1]），
均值 **+0.0174**（线 ±0.0707）。单测里另做一次 1e4 次的蒙特卡洛：预算 σ 1.8°、经验 σ 1.81448°，
相对差 **0.80%**（线 3%）。

**逐站的功率合成**（多站版的 L-2 断言）：三个站的 S1 总功率对该站三条链路预算的线性和
（突发源按 20% 占空比计权）偏差分别是 **+0.002 / −0.013 / +0.001 dB**（线 0.05）。

**服务端**：`bearings` / `positions` 两个 JSONL 端点，抽稀键分别是 `link_id` 与
`emitter_id:method`；新增 `JSONL_FILTERS` 表做等值过滤（`site_id` / `emitter_id` / `method`，
多字段取交集），与 `links` 原来那个写死的 `link_id` 过滤统一。服务 119 项测试。

**前端**：`sceneStore` 加 `bearings` / `positions` 两张表与 WS 分支；结束后从文件整批补齐
（突发下 WS 会丢帧，只有文件才完整，与 U-3 的结束即收口同理）；新叠加层
`web/src/scene/layers/fixOverlay.ts` 画测向线（±2σ 楔形、非 valid 画虚线、`exclude` 不画）
与 2σ 椭圆；辐射源表单加测向与定位读数块；图层弹层加开关。

**三个新色值按 D2-4 的同一判据实测**（先用既有的 `site` 7.81/6.71/6.20 与 `linkLos`
4.49/3.86/3.56 核对公式与 `docs/display-route.md` §4 的表一致）：`aoa #b45309` 4.49/3.86/3.57、
`tdoa #0e7490` 4.80/4.12/3.81、`fusion #6d28d9` 6.36/5.46/5.05，最低 3.57:1 过 3:1 门槛。
em-demo 的橙 `#f97316` 与青 `#22d3ee` 不沿用（为深色底调的，D-017）。

**四处只有在多站下才暴露的缺陷，都是实测撞出来的**：

| 现象 | 根因 | 改法 |
|---|---|---|
| 在框图页换场景后，站与源的列表仍是上一份场景的 | 换场景只改了引用**没有载入**场景文档。单站单源时两份场景都只有一个站一个源，看不出差别 | 选场景时同时 `loadScenarioInto`，并把旧的选择作废交给缺省全选重填 |
| 切到三站场景只勾中一个站 | `scene/scenarioLoading` 会**立刻**把 `scene.scenario.id` 改成新场景，而 `doc` 要等请求回来。「场景已同步」的闸只看 id，在 loading 那一帧就放行，效应拿旧场景的列表把选择填上；旧场景的 `site-1` / `uav-1` 在新场景里也存在，于是再也不会重填 | 闸门同时看 id **与 `status === 'ok'`** |
| 换场景后提交一直失败 | `scenario_ref.sha256` 在换场景那一刻只能留空（文件还没读回来），此后再没回填 | 并进同一个同步效应，载入完成后一起补齐 |
| 跑完后「观测点与当前任务不一致」的提示不消失 | 多站的产品目录名带实例后缀（`s1__site-2`），比较时被 `TAP_ORDER.includes` 整批丢掉，于是恒判不一致 | 先去后缀再去重比较 |

**顺带发现两处「一直没生效」的东西**：① 探针的 `app.task.id` **根本不存在**（任务标识在
`app.context.taskId` 里），`slice4` 那条「观测点不一致要提示」的断言因此从写下起就是空转——
切片 ⑥b 留下一个观测点不同的在先任务才暴露；改为从任务端点取实际观测点，判据由「有没有任务」
改成「勾选与在跑的是否一致」，两个方向都可判。② `probeApp` 是**逐项挑**字段的，
`situationSnapshot()` 新加的 `bearings` / `positions` 不加进去就在探针上看不见。

**新增端到端 `tests/e2e/slice6-smoke.mjs`（34 项）**：从缺省单源单站的退化路径、切场景、
复选与实例角标、频率计划两项新检查，到 9 条链路 / 9 组测向报告、`truth_consumed` 与 M2/E2/V2
溯源、统计判据、混叠、逐站功率合成、地图叠加层与图层开关。

**回归**（macOS，原型阶段验证值）：引擎 **158 项** doctest + 8 项 ctest、服务 **119 项**、
web 163 项、六套端到端 14 + 39 + 27 + 30 + 51 + **34** = 195 项，全绿；`build-all.sh` 全过。
组件目录 17 件（新增 `DirectionFinder`，脚本核对既有条目零变化）。

**下一步**：L-4 `MultiSiteLocator(aoa)` 与 emcore 的 `locate_aoa` / `core/geo` 移植。

## 2026-09-09 · L-4 多站交叉定位与 emcore 移植（切片 ⑥b 后半）

**移植三件**（自 `C-UAV Model Demo/emcore/`，铁律 13 的来源头都写在文件头）：
`core/geo.cpp` → `geo/fix_geometry.{h,cpp}`（协方差 → 2σ 椭圆与 CEP、对称 2×2 求逆、几何质量枚举）；
`models/locate_aoa.cpp` → `geo/locate_aoa.{h,cpp}`（两遍加权最小二乘交汇）；两份黄金基准
`geo_fix.json` / `aoa_location.json` 拷入 `tests/golden/`，`_meta` 里加了 `origin` 与 `ported_to`。

**接口改成平面坐标（吃米，不吃经纬度）**，这是 D-009 在本线的落地方式：数学与投影解耦，
golden 走 `geo::legacy::local_frame_111320()`（111320 常数一字不改）回放，引擎侧走
`default_geodesy().to_enu()` 的严格 ENU，**一份数学两处用**。对拍结果：
`geo_fix` 43 组、`aoaLocalization` 6 组、`computeAOAFix` 42 组，全部相对误差 ≤ 1e-9。

`scripts/build-all.sh` 加了一条守卫：`legacy::` 只允许出现在 golden 回放与 `legacy_*.cpp` 里。
**造了一处违规实测过**，确实拦得下来——不然这种守卫写了也不知道有没有用。

**三个解析锚点逐位命中**：两站正交交汇、各距 1 km、σ = 1° 时
σ_pos = **17.4532925 m**、2σ 半轴 = **34.906585 m**、CEP = 1.1774σ = **20.5495 m**。
另钉住二维 2σ 的包含概率 **0.8646647167633873**（1 − e⁻²），不是一维的 95%。

**`MultiSiteLocator`**（algorithm / M2 / E2 / EM-S-06）。**它是纯函数式的**：站址随测向报告走
（`BearingReport` 加了 `site_lon/lat/alt_m`），不绑场景、不读场景文件——「绑一个站」对全图唯一的
融合节点本来就没有语义，而 `scene_binding` 的结构又要求至少绑一个对象。让报告自带位置，
这个矛盾就不存在了，和「身份靠报告自带」是同一条原则。
`method` 只实现 `aoa`；选 `tdoa` / `aoa_tdoa` 时 **configure 直接失败**而不是静默退回 `aoa`——
用户选了时差定位却拿到交叉定位的结果，看不出差别但结论是错的。

**过程中一个自己造的坑**：`DirectionFinder` 起初只走观察者、没往输出口写数据，
于是多站定位一行也出不来（端口上是空的）。两条路径各有各的用处，都要走——
端口给下游的数据流，观察者给运行器落盘发事件。

**Python 第二实现** `algos/reference/aoa_fix.py`（显式循环累加，D-046 ⑧）：
从 `bearings.jsonl` 独立重算并与 `positions.jsonl` 逐值对拍，**600 个解最大相对误差 1.682e-12**。

### 实测发现：emcore 的几何分级看不见「两条近乎平行的线 + 一条好线」

第一次量覆盖率时，`uav-1` 的真值落 2σ 椭圆内只有 **75.5%**（理论 86.5%），而 `uav-2` 是 87.0%、
`uav-3` 是 85.5%。按几何质量拆开更奇怪：被判 **good 的那批反而只有 45%**，fair 的有 89%。

排查用了三步，每一步都排掉一个可能：

1. **是不是流水线错？** Python 参考独立重算，600 个解相对误差 1.68e-12——求解器与 ENU 都对。
2. **是不是「σ 大时线性化协方差偏乐观」？** 用三站 120° 均布的对称几何做蒙特卡洛，
   σ 取 1.5° / 4° / 8.18°，覆盖率分别是 86.2% / 85.9% / 87.0%——σ 大本身**不是**原因。
3. **那是几何。** 打印 `uav-1` 在 t = 10 s 的真方位：**210.66° / 213.81° / 261.79°**——
   其中两站只差 **3.2°**，目标与它们近乎共线。用真实站位做蒙特卡洛复现：覆盖率 69–73%，
   与实测的 75.5% 吻合。

**根因**：emcore 的 `geometryGrade` 按**最大**张角分级（> 60° 判 good）。
「两条近乎平行的线 + 一条好线」的最大张角有 51–90°，看着很开，而真正决定交汇质量的是那对
近简并的线。目标越远，两条线越平行，最大张角反而越大——**所以「good」那批最差**。

**处理**（铁律 10：记录不掩盖，不调参让它达标）：

- `aoa_geometry_grade` 一字不动（它是 golden 守着的 emcore 口径）；
- **另加**一个量 `min_crossing_deg`：最小两两交会角，把测向线当无向直线折到 [0, 90]。
  报告行随之加 `min_crossing_angle_deg`；
- 顺带修正一处名实不符：降级判据原来比的是**最大张角**，而那个参数本来就叫
  `min_crossing_angle_deg`。改成比最小交会角，名实一致，也正好挡住这种近简并配置；
- 单测钉住两件事：① 最大张角 165° 会被判成最好的一档而实际交会角只有 15°；
  ② 最小交会角 3.15° 时 2σ 覆盖率只有 73.9%，**这是伪线性 AOA 估计器的已知局限**。

**改后的实测**（demo-03 三站三源跑 20 s，600 个解）：

| 档 | 样本 | 真值落 2σ 椭圆内 | 经验 50% 半径 / CEP |
|---|---|---|---|
| 有效档（最小交会角 ≥ 15° 且无低权重站） | 200 | **87.0%**（理论 86.5%）| **0.920** |
| 降级档 | 400 | 80.5% | 1.238 |

三个目标的最小交会角中位数：`uav-1` 7.1°（最小 0.1°）、`uav-2` 28.4°、`uav-3` 9.7°——
demo-03 的三角形边长 2.2 km 而目标飞在 1.5–3 km 外，交会角本来就偏小。
系统的责任是把这些解标成 `degraded` 并写明 `crossing_angle_below_min`，而不是让它们看起来达标；
**低于门限的 236 行全部写明了原因**。

**AOA 蒙特卡洛**（三站 120° 均布、σ 1.5°、1e4 次）：协方差迹比 **1.01035**，
经验 50% 半径 / CEP = **1.0063**。两项都验是 emcore README W4 的教训——
CEP 公式曾经写成 √trace，迹对得上而 CEP 差一大截，只有散布校验才发现。

**回归**（macOS，原型阶段验证值）：引擎 **167 项** doctest + 8 项 ctest、服务 119 项、web 163 项、
六套端到端 14 + 39 + 27 + 30 + 51 + **44** = 205 项，全绿；`build-all.sh`（含新的常数策略守卫）全过。
组件目录 18 件（新增 `MultiSiteLocator`，脚本核对既有条目零变化）。

**下一步**：L-5 `ToaEstimator` 与 `MultiSiteLocator` 的 tdoa / aoa_tdoa 两档。

## 2026-09-09 · L-5 到达时间估计与时差定位（切片 ⑥c）

**移植**：emcore `models/locate_tdoa.cpp` → `geo/locate_tdoa.{h,cpp}`，黄金基准
`tdoa_location.json` 拷入 `tests/golden/`。Gauss-Newton ≤ 6 步、0.05 m 早停、可行性门降级不裁、
GDOP 与时统 TQ-1..TQ-4 的分档门限逐字保留；接口同样改成平面坐标，`legacy::toa_sigma_s`
（10 MHz 标称带宽、0.3 ns 底噪两个写死常数）只为守 golden。
对拍：`toaSigma_s` 36 组、`computeTDOAFix` **128 组**，相对误差 ≤ 1e-9。

**对拍过程中定位到两处自己的错**，位置都对得上、只有协方差差两成，正好把它们照了出来：

1. **站钟 σ 读错了地方**：`syncAccuracy_ns` 在**站点配置**里，不在量测行里。第一版从量测行找，
   三站都退回缺省 3 ns，于是位置照样对（它不依赖权重的绝对大小），椭圆与 CEP 差了 20–28%。
2. **漏了第二个入参**：`computeTDOAFix(obs, initialGuess)` 的一半用例给了同帧 AOA 解作初值，
   我一律传空。表现是「给不给初值算出来一样」——奇数组的椭圆差 5e-6。

**`ToaEstimator`**（algorithm / M2 / E2 / EM-S-07），隐含节点：用户不选、不配参数，
编译器只在多站定位启用且方法含时差时自动接上——时差定位的到达时间只应该有一种口径。
**站钟全部来自场景 `sites[].clock`，缺 `clock` 即 `configure` 失败**，报文里写清楚要补哪几个字段
以及「这些是建模的同步误差，不是真实设备指标」。σ_pick 的相关带宽取**本站的实际采样率**
（emcore 写死 10 MHz），因为写死会给出与配置无关的假精度：500 kS/s 与 20 MS/s 差 40 倍。

**`MultiSiteLocator` 扩为三档**：`aoa` / `tdoa` / `aoa_tdoa`（后者用交叉解作时差迭代的初值，
两个 ENU 原点不同，要先换算过去）。时统档差于 `sync_quality_threshold` 的站剔除并进
`outlier_sites`；GDOP 超阈值、可行性违例、时统非 TQ-1 各自写一条降级原因。

**两种加权的实测纠正了计划里的一个假设**。11 报告 §3.4 原写「emcore 把站对当独立，
参考站的噪声进了每个站对，按独立处理会**低估**协方差、结果偏乐观」。真跑下来方向相反：

| 配置 | correlated_reference 迹比 | independent_pairs 迹比 |
|---|---|---|
| 3 站，拾取 2 ns / 同步 5 ns | 1.007 | 0.755 |
| 4 站，同上 | 1.013 | 0.843 |
| 5 站，同上 | 1.004 | 0.915 |
| 4 站，拾取 5 ns / 同步 1 ns | 1.007 | 0.838 |
| 4 站，拾取 1 ns / 同步 20 ns | 1.010 | 0.840 |

`independent_pairs` 报的协方差**比实际散布大**（迹比 < 1），是**保守**而不是乐观；
站数 ≥ 4 时它的估计本身也略差（没用上噪声的相关结构，4 站时实际迹 2.93 对 2.64）。
`correlated_reference` 在五种配置下迹比都落在 1.004–1.013，自洽。
**两个口径都保留、数值都进模型卡，不调参使二者一致**（铁律 10）；11 报告 §3.4 与 §11.3 已改。

**解析锚点**：三站等边、源在质心时不加权 GDOP = √(8/9) = **0.9428090415820634**，逐位命中。

### 实测发现：未标定的固定钟差是协方差表达不了的系统偏差

`demo-03` 三站三源跑时差定位，600 个解，中位误差 **7.95 m**（交叉定位是 200 m 量级，
2.2 km 基线 + 纳秒级站钟下这个量级是对的）。但真值落 2σ 椭圆内只有 35%。

按目标拆开就看明白了：

| 目标 | 系统偏差 | 原始 2σ 覆盖 | 扣掉系统偏差后 | GDOP 中位 |
|---|---|---|---|---|
| `uav-1` | 240.6 m | 0.0% | 11.5% | 7.10 |
| `uav-2` | 4.5 m | 66.0% | **87.5%** | 1.17 |
| `uav-3` | 4.4 m | 39.5% | **86.5%** | 1.39 |

`uav-2` 与 `uav-3` 扣掉偏差后正好落在理论的 86.5% 上——**协方差本身没问题，
不能表达的只是那个固定偏差**。偏差 4.4–4.5 m 与 `site-3` 的 `bias_ns = 20`（20 ns × c = 6 m
的距离差，经几何折算）对得上。这正是场景里 `bias_ns` 存在的意义：未标定的站钟偏差是系统误差，
时差定位对它极其敏感，而误差椭圆只描述随机散布。

`uav-1` 另有原因：它从三角形西南飞到东北，途中最近处离 `site-2` 只有 423 m。
源贴近某一站时双曲几何病态，偏差被放大到 240 m，散布也被低估（92 m 对预测 14 m）——
与 L-4 那条「伪线性估计器在近简并几何下协方差偏乐观」是同一类。系统把这些解标成
`geometry_quality = poor`，验收因此按**几何良好**那一档取：400 个解，扣掉系统偏差后
2σ 覆盖 **85.3%**（理论 86.5%），中位误差 5.5 m。

**Python 第二实现** `algos/reference/tdoa_fix.py`：椭圆与 CEP 最大相对误差
**3.6e-16 / 4.7e-15**（容差 1e-9）。GDOP 单独定 1e-5 的容差并写明理由——它要把落盘的经纬度
换回站心平面，而参考自身的 ENU → 经纬度 → ENU 往返在 2 km 处丢约 1 cm（回代时高程按 0 处理），
GDOP 对位置的敏感度使它体现为 1e-6 量级。**这是参考实现的坐标往返误差，不是两侧算法有差别**；
把它也卡在 1e-9 只会逼人去改容差或改参考，不会发现真问题。

### 顺带修掉一处会直接崩掉界面的 hooks 顺序错误

切时差档时整棵界面树当场白屏（Minified React error #300：本次渲染的 hook 比上次少）。
两个 bug 叠在一起：

1. `parseChain` 忘了跳过新的 `toa` 隐含节点，于是编译出来的框图**反解不回典型链路**；
2. 我在 L-2 加的「按载入场景补齐站点与哈希」那个 `useEffect` 写在
   `if (!parsed) return` 这个早退**之后**——框图一旦解不开就少调一个 hook。

第 2 条是 L-2 留下的潜伏问题，只有在「框图解不开」时才发作，所以此前一直没露头。
改法是把那段逻辑抽成 `useSceneSync(parsed, …)` 并放到早退**之前**调用，`chain` 为 null 时它什么也不做。

**回归**（macOS，原型阶段验证值）：引擎 **173 项** doctest + 8 项 ctest、服务 119 项、web 163 项、
六套端到端 14 + 39 + 27 + 30 + 51 + **52** = 213 项，全绿；`build-all.sh` 全过。
组件目录 **19 件**（新增 `ToaEstimator`；`MultiSiteLocator` 加 5 个参数，按名对比确认既有参数未被改，
只有 `method` 的描述文字从「待 L-5」改成实际语义）。

**下一步**：L-6 服务端与回看的余项、L-7 回归夹具、L-8 文档收口。

## 2026-09-09 · L-6 至 L-8 收口（切片 ⑥ 全部完成）

**L-6** 的内容在 L-3 / L-4 里已随手落地（两个 JSONL 端点与 `JSONL_FILTERS` 等值过滤、
结束后从文件整批补齐测向与定位、定位读数块、图层开关），本步只做了验证：
刷新后已结束任务的地图仍画得出测向线与椭圆，由 `slice6-smoke` 的探针断言守着。

**L-7** 回归夹具 `tests/regression/diagrams/chain-3x3-{aoa,tdoa}.json`（49 / 52 节点，
84 / 96 连线），README 里写明了它们与 `slice6-smoke` 的分工：端到端那套从浏览器出发，
验「用户点得出这个形状」；这两份从命令行出发，验「这个形状的文件引擎还认」，
改框图格式或编译规则时随手跑一遍就能看出有没有改坏。

**L-8 文档收口**：`docs/display-route.md` §4 补三行色值实测（最低 3.57:1）并把「测向线与误差椭圆
生产者不存在」改写为画法说明；`docs/diagram-canvas-guide.md` 补六边形把手与四件新组件；
08 报告的端口枚举 7 → 10、组件数 12 → 19、内部参数 11 → 18 处、`port_optional` 从「预留」
改为启用并说明 `check_wiring`；新建模型卡 `models/locate/README.md`（234 行）；
11 报告 §3.4 与 §11.3 记六条偏离；CLAUDE.md 的 D-053 追加实测修正段、里程碑 L 行由「待验」改「已达成」。

**最终全量回归**（macOS，原型阶段验证值）：

| 层 | 数量 |
|---|---|
| 引擎 doctest | **173 项** |
| 引擎 ctest | 8 项 |
| 服务 | **119 项** |
| web | 163 项 |
| 场景一致性（Python） | 8 项 |
| 端到端六套 | 14 + 39 + 27 + 30 + 51 + **52** = **213 项** |
| 参考实现对拍 | `aoa_fix.py` 600 解 1.68e-12、`tdoa_fix.py` 600 + 545 解 3.6e-16 / 4.7e-15 |

`scripts/build-all.sh`（含新增的常数策略守卫、路径与 ASCII 两项检查）全过。

**切片 ⑥ 到此收口，回到切片 ④b（C-3 检测升级）。** L-9 的定位评价指标随 C-5 与 C-9 一起做。

## 2026-09-09 · 典型链路版式改为每行四张的蛇形排布

用户先说「改成 3 行吧，按每行四个框图来排」，随后又说「框图排序改为蛇形序列更好，
就是第二行直接从右往左排」。两步合起来是：**每行四张，第 0 行从左往右，第 1 行从右往左，
第 2 行又从左往右**，十一个槽位排成 4 + 4 + 3。

蛇形的好处是链条连续可读——一行读到头，下一张卡片就在**正下方**（第 0 行末尾与第 1 行开头
同在第 4 列），视线不必从行尾甩回行首。

**排布交给一个纯函数**：`snakePos(i, total, cols)`（`web/src/chain/ChainView.tsx`），
返回该卡片的行、列，以及「与下一张之间的连接件朝哪边」（`right` / `left` / `down` / `null`）。
它是版式的唯一真相来源，改列数只需改它与 `.chain-strip` 的 `grid-template-columns` 两处。
纯函数好测，`snake.test.ts` 五项把「双数行掉头」「转折处上下同列」「连接件方向随行翻转」
「列数可配且末行短一截时仍成立」「单行时退化成一条直线」都钉住了，不必都跑浏览器。

**连接件改了画法**。原来是「卡片 + `flex: 1` 撑开的箭头」，卡片因此靠列的左边排。
蛇形下这样不行：相邻两行的卡片会被挤到列的两侧，上下对不齐，转折处看不出是同一条链。
改为**卡片在列内居中、连接件绝对定位**到两张卡片的边缘之间；行末换成落在 56 px 行距里的
向下转折 `↓`。

**中途踩了一个自己造的坑**：第一版把横向连接件写成 `left: calc(50% + 半张卡片); right: -4px`，
只铺到本列的边缘就停了——截图一看，箭头悬在两张卡片中间的半空里，离目标还差 130 px。
原因是卡片居中之后，下一列里卡片左侧还有「半列宽减半张卡片」那么大一块留白。
终点改写成负的同一个量（`right: calc(半张卡片 - 50% - 4px)`），线才真正从上一张卡片的边缘
铺到下一张卡片的边缘。**这类几何问题光看代码看不出来，必须截图或量 `getBoundingClientRect`。**

**实测**（`Emulation.setDeviceMetricsOverride` 强制视口；这套无头启动器的 `--window-size`
不改 `innerWidth`，直接量会全是 2560）：

| 视口 | 行 | 横向溢出 | 纵向溢出 | 上 / 下留白 |
|---|---|---|---|---|
| 1680 × 1050 | 4 + 4 + 3 | 0 | 0 | 184 / 184 |
| 1920 × 1080（09 §定的最小基线）| 4 + 4 + 3 | 0 | 0 | 210 / 210 |
| 2048 × 1152 | 4 + 4 + 3 | 0 | 0 | 246 / 246 |
| 2560 × 1440（2K 基线）| 4 + 4 + 3 | 0 | 0 | 390 / 390 |

几何逐项量过：横向连接件两端贴卡片边**差 0 px**、纵向中心**差 0 px**；
转折竖线与卡片中心**差 0 px**，自卡片底延伸满 56 px 正好接到下一行的卡片顶。
读出来的顺序是 `辐射源 → 发射天线 → 传播信道 → 接收天线 ↓ 接收机前端 ← ADC ← DDC ← 信道化
↓ 检测识别评价 → 测向 → 多站定位`。

**端到端断言换了口径**。原来那条「换行时下一行不以孤立箭头开头」在新结构下恒真
（连接件是绝对定位的，每个格子的第一个子元素永远是卡片），留着等于没测。
改成两条真正验蛇形的：① 每行的卡片中心 x 单调（单数行递增、双数行递减）、行内连接件方向
与行方向一致、行末是向下转折而最后一张没有；② 转折处上下同列（实测 1915 vs 1915、595 vs 595）。
另外保留「不撑出横向滚动条」与「仍纵向居中」。

`slice4-smoke` **53 项**、`slice6-smoke` **57 项**、web 单测 **168 项**全过；
其余四套端到端与引擎、服务测试无回归。

文档同步：11 报告 §5.2 / §8.2 / §9.2、06 §9E ⑥b 与 §9H L-3、10 报告 §5.2 与
「补：D-053 对本方案的修正」表。

---

## 2026-09-09（六）框图页成为实体设备参数配置的主界面（D-054）

用户指示：「框图画布面板上方增加选择无人机和选择侦测站两个选择框，根据选择具体的实体，同时
切换相应对应的框图的参数，让该界面成为场景内实体电子设备参数配置的主界面。电磁传播信道不受
无人机和选择侦测站两个选择框的影响，单独设置并共用。」随后拍板四条：两个下拉**只切换配置焦点**、
不改运行范围；共用粒度**引入设备型号字段**（同型号共用一套、可逐实体单独设置）；场景视图的
对象表单**保留**并与框图页共用同一套字段定义；在框图页改完场景参数**自动保存**。

### 动手前核实到的三个缺口

一、**场景里已经逐实体存着的设备参数，在框图页看不到也改不了**。站点的
`receiver.{fs_Hz, center_Hz, bw_Hz, nf_dB}`、`antenna.gain_dBi`、`clock.*`，无人机的
`emission.{center_Hz, bw_Hz, tx_power_dBm, antenna_gain_dBi, polarization, waveform}`
全在场景文件里，但只有场景视图的对象表单能改。

二、**框图槽位的参数所有实体强制一样**。`SlotConfig` 每槽位只有一份 `params`，
`compile.ts` 的 `slotParams(id)` 根本不接收实例参数——这是 D-053 ⑨ 的明确简化。

三、**有两处独立的真理源**。`ReceiverFrontEnd.nf_dB` 与站点 `receiver.nf_dB`、
`AntennaGain.gain_dBi` 与站点／无人机的天线增益各存各的。11 报告 §5.2 第 449 行本来
承诺过要把这些转成场景带出，但从未实现——全仓库只有报告与 CLAUDE.md 提过，`web/src` 零命中。

### 实施后得到的两个必须记下来的发现

**一、场景里逐源的天线增益一直被静默忽略，`uav-3` 的链路预算高估 2 dB。**
`demo-03` 的竞速机 `uav-3` 在场景里写的是 `antenna_gain_dBi: 0`，而回归夹具里
`tx_ant__uav-3__*` 是 2——因为参数按槽位共享，三架机被迫共用一个发射天线增益；
`engine/src/antenna.cpp:169` 又只认组件自己的 `gain_dBi_`（`scene` 口只供角度）。
后果是场景页的链路读数（走场景值）与 IQ 链（走组件值）对同一条链路会给出差 2 dB 的答案。
`slice6-smoke` 此前也没能发现，因为它把预算式硬编码成 `TX[em] + 2 + 3`——**两边错得一样**。

按铁律 10 走正式基准变更：夹具那 6 处 2 → 0、e2e 预算式改逐源取场景值。连带影响实测复核
（不假定）：`uav-3` 电平降 2 dB 会改测向信噪比，进而影响依赖统计散布的几条断言。结果全部
仍在带内，且链路预算那三条**更准了**：

| 断言 | 变更前 | 变更后 | 判据 |
|---|---|---|---|
| 每站 S1 总功率对三条链路预算的线性和 | ≤ 0.013 dB | **0.002 / −0.013 / 0.001 dB** | 0.05 dB |
| AOA 有效档 2σ 椭圆覆盖 | 87.0 % | **82.5 %** | [0.80, 0.93]，理论 86.5 % |
| AOA 有效档经验 50% 半径 / CEP | 0.920 | **1.029** | [0.85, 1.15] |
| 测向归一化误差样本方差 | 1.0225 | **1.0225** | [0.9, 1.1] |
| 时差定位中位误差 | 7.95 m | **7.95 m** | < 30 m |

**二、`demo-03` 从未按规范序列化形式落盘，第一次自动保存就把三处哈希打掉了。**
D-049 ⑧ 早就定了场景文件的规范形式是 `JSON.stringify(doc, null, 2)` 加末尾换行，
`slice2-smoke` 也一直在验 demo-01 的「空操作保存不改变哈希」。但 `demo-03` 是手写的，
浮点写成 `120.0` / `130.0`，规范形式里是 `120` / `130`，相差 24 个字节（7497 → 7473）。
于是端到端用例在界面上改一个字段并自动保存，整份文件被规范化重写，
`tests/golden/scenario-track-demo-03.json` 与两份回归夹具记着的 `46715c5b…` 一起失效——
而这与改的那个字段毫无关系。

这是**陷阱不是回归**：任何人第一次在界面上编辑 demo-03 都会踩到。处置是让不变量真正成立——
保留规范化后的文件；重生成航迹黄金基准（**723 个样点逐点相同，只有 `scenario_sha256` 变**）；
更新两份夹具的 `scenario_ref.sha256`；并在 `tests/unit/test_scenario_example.py` 加一道守卫：
入库的场景文件必须**已经**是规范形式（8 → 9 项）。

### 设计上的三个取舍

**实例化维度与参数归属维度分离。** `SlotDef` 新增 `owner`：`tx` / `tx_ant` 归**源**，
`rx_ant` 及其后归**站**，`ch`（用户明确要求）与 `loc` 归**共用**。`tx_ant` / `rx_ant`
是关键——节点仍按链路展开 N×K 份，但发射天线长在无人机上、接收天线长在站上；
按 `per` 取参数会把「改一架机的天线」变成「改一条链路的天线」，物理上讲不通。

**型号是编辑范围，不是存储层级。** `SlotConfig` 只加 `byEntity` 一层。理由是
`parseChain(doc)` 只拿得到框图、拿不到场景——它在 `DiagramView.tsx:69` 也被调用（判断画布
里的框图能否解回链），那里根本没有场景。把型号分组写进 `ChainState`，反解结果就会随
「场景载没载入」变化，`compile(parseChain(doc)) === doc` 这条逐字节不变量当场失效。

**用户选的范围必须记在组件本地。** 第一版把范围完全做成派生量，实测发现下拉根本选不动：
范围是从「各实体的值一不一致」算出来的，只改下拉不改值，重编译后立刻被归约打回「共用」。
改成记下**意图**、下一次改值时按它写；只有「收回共用」当场生效（要立刻清掉覆盖）。

### 反解的归约算法与往返

按 `owner` 把实例节点归到实体：**同一实体**的多个实例（如一个站对 N 个源的接收天线）参数
仍必须完全一致，不一致即返回 null（那才是被手改过）；**不同实体**允许不同——那正是逐实体设置。
逐参数取众数作共用底值、票数并列时取实体 id 字典序最小者，其余进覆盖。划分唯一、
逐实体有效值一字不变，故 `compile(parseChain(doc)) === doc` 仍逐字节成立。

第 4 步（两层模型）单独收口时验过：两份 3×3 回归夹具往返**逐字节相同**且不产生任何
`byEntity`，`DEFAULT_CHAIN_TEXT` 逐字节不变。第 5 步（场景带出）才允许出现那一处
2 → 0 的预期差异——两步不合并提交，否则往返算法万一有错会被预期差异掩盖。

### 顺带补上的界面缺口

`sites[].clock`（D-053 起有格式）与 `emission.polarization`（D-051 起有格式）此前**全无界面
入口**，demo-03 那三组站钟只能手改 JSON。这次连同设备型号一并放出来，写在共用的
`web/src/scene/editor/deviceFields.ts` 里，场景视图与框图页都读它。`setPath` 相应支持
按需建中间对象（只建对象、不建数组元素——凭下标补一个空站会造出没有 id 的半个对象）
与「写 undefined 即删键」。顺手销掉 `ChainView.tsx` 里 `DERIVED_OF` 对 `model.ts`
`DERIVED_PARAMS` 的重复定义。

### 验证

引擎 **174 项 doctest + 8 项 ctest**、服务 **119 项**、web **175 项**、场景 schema **9 项**、
六套端到端 **14 + 39 + 27 + 30 + 61 + 63 = 234 项**全绿；`scripts/build-all.sh`（含常数策略、
路径可迁移、ASCII 三道守卫）全过。**组件目录黄金基准与产品黄金基准零变化**——本次没碰
组件参数表，`cuav_run --catalog` 与 `tests/golden/component-catalog.json` 逐字段相同。
端到端用例自己清理副作用：`demo-01` 哈希回到 `72b8e8d0…`、demo-03 的临时型号已清空。

`slice1-smoke` 出现过一次偶发失败（未捕获到具体项），随后连跑三次均 39 项全过；
该套件的跟随模式断言对时序敏感，暂记为偶发。

### 同日修：选「实测片段回放」后运行报「未知参数」（用户实测）

用户在全合成链的辐射源卡片上把变体选成「实测片段回放」，点运行报
`[param] 节点 tx（FileReplaySource）未知参数 center_frequency_Hz`，同时上方的「无人机」变成
「（先选场景）」。查清是**两个既有缺陷叠在一起**，都不是 D-054 引入的
（上一个提交里 `slotParams` 就是 `{ ...cfg.params, ...(v.fixed ?? {}) }`，同样会把上一个变体的参数原样带过去）。

**一、换变体时上一个变体的参数照写不误。** 槽位的参数是**按槽位**存的，`parseChain` 又把
由频率计划派生的 `sample_rate_Hz` / `total_samples` / `center_frequency_Hz` 留在状态里；
变体一换，这三个就写进了 `FileReplaySource`。报文指向用户刚选的那个组件，看不出问题出在换变体上。
修法是在 `push()` 这一个收口处过滤：组件目录里没有的参数不写。**不写 ≠ 丢弃**——它们仍留在链路
状态里，换回原变体即恢复；目录还没到手（`spec` 为 null）时不做这个判断，宁可原样写出去让引擎判。
`ch` 槽位的两个变体参数完全不相交，同样会踩，只是默认链路里 `ch` 的参数恰好都是模板固定的才没暴露。

**二、辐射源变体与信号源模式各管各的，造出了设计里没有的状态。** 10 报告 §2.2 的口径是
「一条链、三种信号源模式」——「场景辐射源」= 全合成或混合增强，「实测片段回放」= 实测回放，
变体与模式本来就是同一件事。此前卡片上的变体下拉不改模式，于是出现「模式说全合成、辐射源是回放源」：
回放源不绑场景，`emitterIds` 反解成空，`hasScene` 为假，整条链的场景绑定连同 `scn` 节点**静默消失**，
界面上只剩「无人机（先选场景）」。新增 `switchTxVariant()` 让两个入口联动；由回放换回场景辐射源时
落到全合成（混合增强也用变体 0，从回放态分不出用户想要哪一个，取更基础的那个）。

**三、顺带修掉「切回来场景不认了」。** 进回放模式会把 `chain.scenario` 置空（防线二、三：回放数据与
场景无关），切回来却没人填回去。`useSceneSync` 补一条 `needAdopt`：非回放模式却没有场景引用时，
认下当前载入的那一份。「全合成而没有场景」本来就是跑不起来的状态——`SceneEmitterSource` 要靠场景绑定
拿内部参数。这条对模式下拉那条路径同样生效。

新增单测三项（换变体不写未知参数、目录未到手时不过滤、变体与模式联动）与端到端四项，
web 单测 178 项、六套 e2e 14 + 39 + 27 + 30 + 65 + 63 = 238 项全绿，基准零改动。

### 由此暴露的一个尚未修的既有缺陷：换模式会丢前端参数

写端到端用例时撞到：全合成 → 实测回放 → 全合成走一圈之后，`AdcQuantizer` 的
`full_scale_dBm` 没了，提交时报「缺必填参数」。根因是链路视图的立身之本——
**「链路视图是框图文档的投影，不是第二份状态」**：回放模式下前端六个环节是「不适用」、
不编译进框图，而链路状态每次都从框图重新解出来，于是**不在文档里的参数就不存在了**。
走试验设置栏的模式下拉是同一条路径，问题一样。

这违反铁律 15（不静默丢弃用户输入），但修它要决定「不参与本次编译的槽位，其参数存在哪里」——
框图格式已冻结，`template_ref` 只有三个字段，没有现成的地方放。给出三条路子供用户选：
① 照 D-052 的先例，切模式前先说明会丢哪些参数并要确认；② 给框图格式加一段可选的
「非活动槽位参数」，编译与反解都带上；③ 视图层留一份「上次见过的槽位参数」作恢复用，
但那等于承认存在第二份状态，与本视图的设计前提冲突。

**用户当日选了第 ② 条**（只有它真的不丢东西），已按 D-055 落地，见下条。

### 同日续：框图格式加「非活动槽位参数」，换模式不再丢参数（D-055）

用户在三条路里选了「给框图格式加一段」。落点选在 `template_ref` 下而不是顶层新键：
这一段本来就是 D-051 定义的「还原视图用的线索，不是运行语义」，非活动槽位的参数正是这个性质。

**形状**：`template_ref.inactive_slots = { 槽位标识: { variant?, params?, by_entity? } }`。
逐实体的单独设置（D-054 的 `byEntity`）一并暂存，键名按框图格式的下划线风格写作 `by_entity`。

**写入规则决定了既有框图逐字节不变**：只存用户改过的东西——`variant` 非零、`params` 非空、
`by_entity` 非空；三样都空就不为那个槽位写条目，一个条目都没有就整段不写。缺省链里
`ddc` / `chan` / `df` / `loc` 四个槽位都不活，但参数全空，所以 `DEFAULT_CHAIN_TEXT` 与两份 3×3
回归夹具都是一个字节不动。槽位按链路顺序排、参数名按字母序排——序不稳往返就不是逐字节相同。

**引擎照旧不解释它**，但取值要校验：新增 `check_inactive_slots()`，形状、`variant` 非负整数、
参数值只能是数值 / 字符串 / 布尔。错误码沿用该节既有分工——取值错误报 `template`，未知键报 `schema`。
写这条单测时先按 `template` 断言，跑出来是 `schema`，回查 `docs/diagram-format.md` §7.1
才确认 `schema` 才是对的，改的是断言不是实现。

**实测**：「全合成 → 实测回放 → 全合成」转一圈，ADC 满量程 −20 与前端增益 20 原样回来；
用户勾了旁路的信道化 `channels: 16`、组件还没实现的 DDC `decim: 4` 同样保住；
带 `inactive_slots` 的框图经 `cuav_run --validate` 通过。端到端那一段也因此不必再单独挪到最后跑，
只是仍放在末尾（新建一条干净的链再测，与前面的步骤互不干扰），并加了一条断言：
转一圈回来满量程还在、不再提示待填。

至此 CLAUDE.md OPEN 表的「换模式丢前端参数」销项。全量：引擎 175 项 doctest + 8 项 ctest、
服务 119 项、web 182 项、场景 schema 9 项、六套 e2e 14 + 39 + 27 + 30 + 66 + 63 = 239 项全绿；
`build-all.sh` 全过；组件目录与产品黄金基准、示例场景零改动。

### 同日续：实测录音的挑单（D-056）

D-055 上线后用户试回放模式，报「缺必填参数 data_id」——报错本身是对的（回放源得知道放哪一段），
但**界面上无处可选**。核实：盘上 4714 段录音俱在，服务端启动时也已经把它们全部加载进内存
（`DataIndex`，提交任务时用来把 `data_id` 解析成清单路径），只是**没有任何接口把这份清单给界面**。
「数据中心」整页是 U-4 的活，本次只做挑片段所需的最小集。

**服务端**：新增 `GET /api/v1/datasets?q=&data_id=`。四条约定见 `docs/api-versions.md` §3.1d，
其中两条值得单记：

- **只出摘要、不出任何路径**。`manifestRel` 是服务器路径，一律不进响应（04 §8.6、D-037）。
  写了一条测试专门盯着它：响应文本里不得出现 `manifestRel`、仓库根、`data/iq/` 任何一样。
- **缺省按机型分组抽样**，每组最多 12 条。4714 条全塞进一个下拉既慢又没用——挑片段挑的是机型，
  不是某一片。`total / matched / truncated` 如实给出，界面写明「共 4714 段，匹配 4714 段，
  按机型各列前几段」，不假装列全了（铁律 15）。要找具体某一片用 `q` 筛。

**两批数据的真值字段并不一样**：DroneRFb 有 `visibility` 与精确 `distance_m`，
DroneRFa 只有 `band_state` 与 `distance_range_m`。所以摘要里的 `distance_text` 是一段文字而不是数值——
有精确值给 `10 m`、只有区间给 `20–40 m`、都没有就不给这个键。编一个精确值出来才是真的错。

**前端**：挑单挂在 `ParamPanel` 的 `Field` 里按参数名 `data_id` 分派，典型链路与自由画布因此拿到
同一个控件，不必各写一份。控件是「筛选框 + 下拉 + 说明行」：下拉条目写「机型 · 视距 · 距离 · 样点数 · 验收集　标识」，
机型摆最前——那是挑片段时唯一真正在看的东西。框图里已经填着的标识若不在抽样里，单独查回来补进下拉，
不因为没列到就显示成空。命中验收集的片段标出来并提示「可以回放，但结论不能再当作独立验证」（D-038）。

**顺带修掉一处假失败**：用户截图里「ADC 量化噪声不淹没热噪声 ✗ 缺满量程或采样率，算不出」——
回放模式下 ADC 与接收机前端都是「回放数据已含」、根本不参与计算，噪声系数又自 D-054 起要靠场景逐站带出
而回放模式没有场景，于是这条检查永远落到「算不出」那一支，界面上挂着一个用户怎么也消不掉的红叉。
改为回放模式不做这条检查。原注释说它「不需要几何、在没有场景的模式下同样有效」，那是 D-054 之前的事了。

**实测**：端点对真数据返回 `total 4714`、12 种机型、列出 108 条；浏览器里筛「Mavic」收窄到 12 条，
选中即写进框图、直接点运行并跑完（`finished / degraded`；`degraded` 是数据集固有状态，
采集参数来自论文而非设备记录，D-020）。服务 124 项、web 183 项、
六套 e2e 14 + 39 + 27 + 30 + 72 + 63 = 245 项全绿；`build-all.sh` 全过；黄金基准与示例场景零改动。

**同日收口**：用户看到挑单里那个「这一段在验收集里。可以回放，但它的结论不能再当作独立验证（D-038）」
的警示框，指示「这种话都不要，专业人士自己可以判断」。删掉。

这本来就是 D-042b / D-043 已经定过的线——**界面不解释数据来源与估算、不向用户解释数据有多不完美**，
是我这次又写回去了。**去掉的是替用户下结论的那句话，不是事实**：条目上的「验收集」「数据质量 degraded」
「出版方测试集」照旧写着，那正是「专业人士自己判断」所依据的东西；数据层的 `holdout` 标记也照旧随索引
与产物走（铁律 14）。**本次同时修订 D-038 的「验收集片段允许回放但提示」**：提示那句话不做了。

顺带把同一类啰嗦收紧了两处：清单说明行由「共 4714 段，匹配 4714 段，按机型各列前几段；要找具体一段用
上面的筛选」改为「共 4714 段 · 匹配 4714 段 · 列出 108 段」；槽位面板的「全图共用，不随上方的实体选择
变化」改为「全图共用」。已定表新增一行「界面不替用户下结论」，把这条线固定下来，免得下次又写回去。

### 同日续：辐射源卡片去掉重复的模式开关（D-057）

用户：「在实验设置中已经有模式的选择了，在辐射源框图中就不需要再有一个重复的选择了，
增加了操作入口，让逻辑变复杂了」。

**这是对 D-055 ② 的修正，而且是我上一轮没做彻底的地方**：那次查「模式说全合成、辐射源是回放源」
的根因时，已经写下「同一件事有两个开关，彼此不通气」，却选了让两个开关**联动**
（新增 `switchTxVariant`），没有去掉多余的那一个。留着的代价是实打实的——多一个操作入口、
多一条要维护的联动规则（「由回放换回场景辐射源时落到全合成，因为混合增强也用变体 0，
从回放态分不出用户想要哪一个」这种话本身就是复杂度的味道），而收益为零：
「场景辐射源」= 全合成 / 混合增强，「实测片段回放」= 实测回放，它们本来就是同一件事。

落地：`SlotDef` 加 `variantFrom?: 'mode'`，`tx` 声明它；`SlotCard` 对这类槽位只把当前变体的
**名字**写出来，不给下拉；`switchTxVariant` 随之无人调用，删掉不留死代码；`ChainView` 的
`onVariant` 回到最朴素的一版。**传播信道的变体下拉保留**——它的两个变体（场景绑定 / 自由空间定参）
不对应任何模式，不是重复入口，单测里加了一条守卫钉住「除辐射源外没有别的槽位跟着模式走」。

D-055 的另两项修正照旧有效且**仍然必要**：换模式一样会换 `tx` 变体，所以
`push()` 过滤组件不认识的参数、`useSceneSync` 的 `needAdopt` 都还得在。

单测与端到端都改为对着模式下拉验，并加了两条断言：卡片上没有第二个开关、卡片写的名字跟着模式走。
web 183 项、服务 124 项、六套 e2e 14 + 39 + 27 + 30 + 74 + 63 = 247 项全绿；基准零改动。


---

## 2026-09-10 R-0：传播环境与效应选择的方案（12 号报告 v1.0，D-058）

用户指示：「传播信道这个环节要反映侦测站与无人机目标所处的传播环境、地理空间、建筑物分布带来的变数——
自由空间、城市环境、是否考虑遮挡、绕射、多径，以及其他由传播环境带来的影响与效应；在这个框图里选择要
考虑哪些因素，可以从简单选到细致。」

### 先核实：今天的两个变体在物理上是同一个模型

逐行读 `geo/src/link_budget.cpp`（**全文 68 行**）得到的事实，不是引自文档：

- 全仓库的传播物理只有三项——自由空间路损（`:11-15`）、多普勒（`:17-19, 34-40`）、传播时延（`:21`）。
- `line_of_sight` 是**字面常量** `true`（`:32`，注释写着「显式平地假设，D3 接入遮挡后在此改」）。
- `extra_loss_dB` 是**字面常量** `0.0`（`:60`），`path_loss_dB = free_space_dB + extra_loss_dB`（`:61`）。
- 全量搜 `occlusion / diffract / multipath / shadow / knife / P.526 / two-ray / 遮挡 / 绕射 / 多径 / 阴影`，
  命中**全部是注释与「待做」说明，零实现**。

所以 `SceneBoundChannel`（场景绑定信道）与 `FreeSpaceChannel`（自由空间定参）**差的是距离从哪来，
不是物理**：前者的距离由场景几何算出、后者由用户手填，内核都是 `geo::fspl_dB`。「场景绑定」这个名字
容易让人以为它考虑了环境，其实没有。

两处连带后果：① 测向误差预算里的非视距多径项（`geo/src/df_error.cpp:22`，缺省 5.0°）**从未参与过
任何一次计算**，因为视距恒真——`engine/src/locate.cpp:101-103` 的参数说明自己也写明了这一点；
② 场景数据包里 47582 栋建筑（D0 已完成，15.9 MB）目前只用于三维显示，铁律 11「渲染—物理同源」
在传播这一侧尚未兑现。

好消息是**架构上这是预留好的扩展点**：`link_budget.h` 的头注原文写着「D3 接入建筑遮挡后只改
`extra_loss_dB` 与 `line_of_sight` 两处，帧结构与本接口不变」。本方案照此扩展。

### 三条拍板与方案要点

用户拍板：① 档位 + 可逐项覆盖，选项放右侧参数栏，卡片上只列这一档包含哪几项效应；
② 本期只做框架 + 不需要建筑几何的效应；③ 环境类别放信道参数。

- **档位用 01 的精度等级 E1–E4，直接写进溯源六件套的 `model_level`**（CLAUDE.md 的分级口径规定代码与
  元数据只允许 M / E / V / L 四种字段）。E1 快速抽象 = 今天的行为、**缺省**；E2 工程 = 地面双径
  （EM-P-02）**或**城市经验（EM-P-05）二选一 + 统计阴影（EM-P-08）+ 大气降雨（EM-P-07）各可单独开关；
  E3 精细机理 = 建筑遮挡与刀口绕射，**待 D3（切片 ⑤）**。
- **E3 两头都做**：枚举里保留、界面置灰写「待 D3」、引擎 `configure()` 收到即报错。不隐藏是因为隐藏会
  让人以为这条链只有两档；引擎也拒是因为界面置灰拦不住手写的框图（铁律 15）。这与 DDC「组件不在目录里」
  的处理方式不同——E3 是参数枚举值，不是组件。
- **算在 `geo/`（帧生产端），配在 `ch` 卡片（用户要求的位置）**。三条理由任一条单独成立：分层
  （EM-P-01/02/04/08/13 属 M1/M2 参数供给层，「M3 不得绕过 M1/M2 自行硬编码传播参数」，而
  `SceneBoundChannel` 是 M3 施加器）；读数自洽（场景页链路面板的「路损」取自 `link` 事件、事件由帧生产端
  产出，算在信道里读数就名不副实——`link_budget.h` 头注专门告诫过）；复用（测向误差预算吃的是帧里的
  信噪比，算在生产端它自动跟着变，算在信道里测向仍按自由空间算）。
- **参数跨节点靠槽位表的代理表** `SlotDef.proxy = { type: 'ScenarioSource', node: 'scn', params }`。
  参数只声明一次（在真正使用它们的 `ScenarioSource` 上），`SlotPanel` 渲染 `ch` 时把它们的 `ParamSpec`
  从目录条目取出来一并生成表单；`compile()` 把代理键写到 `scn` 节点、其余写 `ch` 节点，`parseChain()` 反向。
  代理集合固定，划分唯一，`compile(parseChain(doc)) === doc` 逐字节仍成立。
- **防重复计损 = 一条规则 + 两道闸**（EM-P-13 §10.9 的最小落地，不全套实现它的八种 `model_role`）：
  至多一个「替代型主模型」，选中它就由它给出基础损耗、不再单独叠加自由空间。闸一在界面上——双径与
  城市经验做成**单选**而不是两个勾选框，「城市经验 + 自由空间双计」构造不出来；闸二在运行时——城市经验
  取 `mean_with_shadow_margin` 时自带 90% 分位裕度，此时再开统计阴影就是同源双计，`configure()` 报错
  并说明，不静默禁用。
- **帧结构一个字段不加**：替代型主模型也走 `extra_loss_dB`（`extra = L_primary − L_fs`），于是
  `path_loss_dB = free_space_dB + extra_loss_dB` 这个恒等式在任何档位下都成立，头注的承诺逐字兑现。
  链路报告新增 `included_loss_terms`（`free_space / ground_reflection / urban_mean / shadow / weather`
  的子集），进 `link` 事件与 `links.jsonl`。

### 写方案时挖出来的两处，与实施计划的原假设不同

1. **`frame(k)` 的无副作用是硬不变量，阴影不能写成递推**。`geo/include/cuav_geo/scenario.h` 的类注释
   把「build() 之后完全闭式，取值函数无副作用，可任意乱序、任意次数调用，结果逐位相同」写死了，而
   `ScenarioSource::process()` 里对同一个帧序号 `k` 确实会调用 `frame(k)` **两次**（一次出帧给端口、
   一次上报给观察者）。阴影的一阶递推 `X_k = ρ X_{k−1} + …` 若实现在 `frame()` 里会当场违反它。
   处置：序列在 `init()` 里按帧序**一次算完存成 `std::vector<double>`**，`frame(k)` 只查表。
2. **本期移植的两个函数不含光速常数，D-009 的常数冲突在本期不成立**。实施计划把「emcore 用 `3e8`、
   `geo/` 用精确值」列为风险，但本期只移植 `atmosphericLoss_dB` 与 `rainAttenuation_dB`，两者都只用
   频率与距离，没有 `kSpeedOfLight`。冲突要到 D3 移植 `fresnelV` 与刀口衍射时才出现。记在这里免得
   日后当缺陷查。

### 顺带改正一处文档错误：`ChannelPathSet` 并非「首期即启用」

CLAUDE.md 三处（`:72` 的首期边界段、「工程约定」的框图端口条、D-013）称 `ChannelPathSet`「首期即启用」。
核实结果与之不符：它只是 `PortType` 枚举的一个值，**零生产者、零消费者**；`PortData`
（`engine/include/cuav/component.h:164-189`）里有 `iq / detections / spectra / scenes / bearings /
toas / positions` 七个容器，**没有 `paths`**；也没有对应的结构体定义。08 报告 §15 自己写的是
「无（首期不实现），仅占类型位」——两份文档早就对不上，只是没人对过账。

改成：**首期只做类型位与命名预留，实现留 P3。** D-013 的原意（接口从第一天就留好，P3 接多径时不改
端口表）保留在改正后的措辞里，历史行不删。

### 参数来源的诚实交代

`env_class` 的路损指数 `n` 与环境偏置 `X_env`、阴影 σ 表、相关距离 `d_corr` 都是**工程取值**，标 `assumed`
（原型阶段验证值，D-028）。只有两处有出处：地表材质表六行直接采用本项目自有的
`天津机场_地表材质与建筑数据工作流.md` §3.5；天气两式移植 emcore（其上游是 em-demo 的简化 ITU-R）。

**D-019 的「2.4 GHz 城市户外链路 n ≈ 2」不能拿来给 `urban` 行背书**：那批样本是空地准视距链路，落在
`open`–`suburban` 之间；而且 D-019 自己写明「1.93 的前提是距离取区间中点这一计算假设，真实距离比只能
定在 2.0–7.5，同一实测降幅对应 n = 1.29–3.75」。表里 `urban` / `dense_urban` 两行是地面站到街谷内目标的
工程取值，无本项目实测支撑。模型卡按 D-019 的三件事一并写。

天气项的量级也如实写进模型卡：2.4 GHz、20 km → 大气 0.148 dB、25 mm/h 中雨 0.021 dB。**在本项目的频段
与距离上它是 0.1 dB 量级、几乎看不见**；留它是为了框架完整与将来的高频段，不是因为它在演示里重要。

### 交付物与下一步

12 号报告 v1.0；CLAUDE.md D-058 + 文档表 + 已定表 + 里程碑表 R 行 + 上游修正段 + `ChannelPathSet` 三处
改正；06 §0.2 / §0.3 / §9E 切片 ⑦ / 新增 §9I（R-0 至 R-7）/ §14；本条目。

下一步 R-1「组合框架与档位」，**收口条件是「什么都没变」**：缺省 E1 档下 `DEFAULT_CHAIN_TEXT`、
两份 3×3 回归夹具、产品黄金基准、`demo-01` / `demo-03` 航迹基准、六套 e2e 的硬断言
（slice2 的 0.074 dB、slice6 的 ≤ 0.013 dB）必须一个数字不动。这一步不通过就不进 R-2。

---

## 2026-09-10 R-1 至 R-7：传播效应落地，切片 ⑦ 收口（D-058）

### 交付了什么

`geo/propagation.{h,cpp}`（新，零第三方依赖）把传播信道从「一个自由空间模型」变成「一组可选效应」：

| 档 | 包含 | 概念模型 |
|---|---|---|
| E1（缺省） | 自由空间路损 + 多普勒 + 时延 | EM-P-01 |
| E2 | 再加地面双径 **或** 城市经验（二选一）、统计阴影、大气与降雨（各可单独开关） | EM-P-02 / P-05 / P-08 / P-07 |
| E3 | 建筑遮挡与刀口绕射 | EM-P-04，**待 D3（切片 ⑤）** |

界面上：「传播信道」卡片写着这一档包含哪几项，右栏「传播效应 · 全图共用」按当前档位显隐地给出
逐项开关，E3 在下拉里保留但置灰。参数声明在 `ScenarioSource` 上（它是帧生产端，真正用到它们的地方），
经槽位表新增的 `SlotDef.proxy` 显示到卡片上并编译进隐含节点 `scn`。

### R-1 的收口条件是「什么都没变」，靠字节比对证

不是靠「测试还是绿的」——那证明不了缺省档没变。做法是：`git worktree` 检出上一版提交，
另建一个 build 目录编出当时的 `cuav_run`，两个二进制跑同样两份框图，`cmp` 产品文件。

**结果：`slice1` / `slice2` 的 `.f32` 与 `.index.json` 逐字节相同。** slice2 的两时刻功率差偏差
仍是 0.074 dB，slice6 的逐站 S1 对预算和差仍是 0.002 / −0.013 / 0.001 dB，一个数不动。

**这一步当场揪出一处缺陷**：`ScenarioSource::init()` 我原本给每条链路无条件抽一个 `u64` 作阴影子流的种子，
而 `rng` 是全图共享的那一条流——于是后面所有组件（噪声源、接收机前端…）的子种子整体挪位，
产品字节当场对不上。改成**不开阴影就一个数都不取**。这类缺陷只有字节比对抓得住，
因为它不改变任何统计量，只改变具体的随机实现。

### 三处按物理改断言，不是改模型

写锚点测试时有三条先红了。逐条查下来都是**我的期望写错了**，模型是对的：

1. **双径的掠射极限必须用水平极化**。原按 `vertical` 写「`|Γ| → 1`、相长上界 +6.02 dB」，
   实测 0.7° 掠射的水面上 `|Γ_V|` 只有 0.80，上界只到 +5.09 dB。原因是垂直极化有伪 Brewster 凹陷，
   要到极小的掠射角才回到 1；`Γ_H` 则很快趋于 −1（同一角度上 0.997）。改用水平极化后
   `C_2ray ∈ [−20（限幅）, +6.01] dB`，相长在 `d ≈ 814 m`（`Δd = λ/2`）、相消在 `d ≈ 407 m`（`Δd = λ`）。
   另加一条用例把这个现象本身钉住。
2. **`d⁻⁴` 远场渐近要把限幅放宽**。原以为 `d ≫ d_bp` 就能看到 40 dB/十倍程，实测恰好是 20 dB
   ——因为两端的深零点都被缺省的 20 dB 相消限幅削平了。把 `max_fade_depth_dB` 放到 60 之后
   20 km → 200 km 增 **39.99 dB**。限幅本身是 EM-P-02 §10.8 第 3 条要求的（完全相消会给出无限损耗），
   所以这不是缺陷，是限幅的语义，写进模型卡。
3. **25 mm/h 中雨 20 km 是 0.223 dB，不是方案里估的 0.021 dB**，差一个数量级（我心算时把
   `R^1.048` 漏了）。12 号报告的三处数字已改。加上大气的 0.1488 dB，天气项合起来不到 0.4 dB
   ——在本项目的频段与距离上它几乎看不见，模型卡如实写明「留它是为了框架完整与将来的高频段」。

### 界面：代理参数必须「显示什么就写什么」

`compile()` 把传播参数写到 `scn` 节点、`parseChain()` 反着读，这部分照计划做。但第一版把
**全部**代理参数都写出去，端到端立刻红了：用户从 E2 退回 E1 时，`prop_primary = urban_empirical`
还留在状态里被一起写给 `scn`，引擎的 `validate()` 于是（正确地）报「E1 与 prop_primary 冲突」，
整次运行被拒。

处置不是清掉那些参数（那就丢了用户的输入），而是**按面板的显隐规则切成两半**：显示的写进节点，
隐藏的收进 `template_ref.inactive_slots`，切回 E2 原样恢复。这与 D-055 收非活跃槽位参数是同一个做法，
连函数都复用了 `stashInactive`。判据用同一个 `visiblePropParams()`，界面与编译不会分叉。

顺带补了一处探针缺口：`__probe().app.diagram` 原来只给节点数与连线数，读不到参数，
而端到端要核对「这个参数写进了哪个节点」；`window.__cuav.diagramText()` 只在 `?dev=1` 下才有，
多数端到端不开开发者模式。给探针加了 `app.diagram.text`。

### 顺带核实到的两件事

1. **本期移植的两个函数不含光速常数**。实施计划把「emcore 用 `3e8`、`geo/` 用精确值」列为风险，
   但 `atmosphericLoss_dB` 与 `rainAttenuation_dB` 都只用频率与距离。冲突要到 D3 移植 `fresnelV`
   与刀口衍射时才出现。
2. **`demo-01` 的航线会飞到距站 33 m**，小于城市经验模型的参考距离 `d0 = 100 m`，
   于是 300 帧里有 105 帧走「近区退回自由空间」那一支并标 `degraded`，整次运行的结果四态因此是
   `degraded`。这是设计行为（EM-P-05 §10.18 out_of_range 要求降级而不是外推），不是缺陷，
   `included_loss_terms` 那几帧也正确地不含 `urban_mean`。

### 参数来源：说清楚哪些有据、哪些是工程取值

只有两处有出处：地表材质表六行直接取自本项目自有的《天津机场_地表材质与建筑数据工作流》§3.5；
天气两式移植 emcore（上游是 em-demo 的简化 ITU-R P.676 / P.838）。

`env_class` 的路损指数 `n`、环境偏置 `X_env`、阴影 σ 表、相关距离 `d_corr` 全部是**工程取值，标 `assumed`**。
**D-019 的「2.4 GHz 城市户外 n ≈ 2」不能拿来给 `urban` / `dense_urban` 两行背书**：那批样本是
空地准视距链路（无人机在空中），落在 `open`–`suburban` 之间；而且 D-019 自己写明「1.93 的前提是
距离取区间中点这一计算假设，真实距离比只能定在 2.0–7.5，同一降幅对应 n = 1.29–3.75」。
模型卡按 D-019 的体例把这三件事一并写了。

### 本期做不到的，模型卡如实列

`line_of_sight` 仍恒为真，所以阴影 σ 表的 NLOS 一列与测向误差预算的 `sigma_mp_nlos_deg` 都取不到；
04 §7.3 的六项里建筑遮挡与 LOS/NLOS 场景模板两项仍缺（要 D3），抽头延迟线式统计多径要 P3。
模型卡 §6 给出逐项对照，**档位名字不暗示「精细档 = 全都算了」**。

另一条只有前端拦得住的规则：`FreeSpaceChannel`（自由空间定参）不吃场景参数帧，只能用 E1；
引擎的 `configure()` 看不见别的节点的类型，所以由频率计划第 11 项 `propagation` 拦。如实记在 12 §4.5。

### 全量

引擎 **208** 项 doctest（新增 33 项传播用例）+ 8 项 ctest；服务 124 项；web **193** 项（新增 10 项）；
六套 e2e **14 + 39 + 27 + 30 + 86 + 63 = 259 项**（`slice4-smoke` 74 → 86）全绿；`build-all.sh` 全过。

**基准变更只有一处**：组件目录黄金基准重生成一次，差异经脚本逐键核对**只有 `ScenarioSource` 新增的
15 个参数**，`schema_version` / `port_types` / `port_compat` 与其余 18 个组件的 `ports` / `params` 逐字未变。
产品黄金基准、`demo-01` / `demo-03` 航迹基准、`DEFAULT_CHAIN_TEXT`、两份 3×3 回归夹具**零变化**。
新增 `tests/golden/propagation.json`（自 emcore 拷入，本期只用大气与降雨两组 64 例）。

**下一步**：C-3 检测升级与落盘（切片 ④b）。D3（切片 ⑤）落地时 `prop_level = E3` 解禁，
`line_of_sight` 改由几何判定，阴影 σ 的 NLOS 列与 `sigma_mp_nlos_deg` 同时开始生效。

### 同日续：撤掉「自由空间（定参）」信道变体（D-059）

用户看着传播信道卡片上的「待填：distance_m」问：**「为何要给出距离？目标和侦测站的距离是随时变化，
但是可以得到的值，不是应该用这个值吗」**；随后明确：「手填一个固定值完全没有必要，我也没这个需求去
计算一个特定距离的自由空间传播衰减」。

### 核实

他是对的，而且比我上午在 12 报告 §4.5 写的处置更彻底。

那个槽位原有两个变体。「场景绑定信道」按航迹每 1/20 秒重算一次距离，正是用户说的做法；
「自由空间（定参）」要手填一个固定距离——组件自己的说明里其实写着「不绑定场景，供标准算例与
解析锚点对拍用；**随时间变化的链路走场景绑定信道**」，但这句话界面上一个字都不显示，
右栏只写了组件类型名。

关键一条是我上午没想透的：**框图页上能跑起来的配置一定有场景**。回放模式下这个环节整个「不适用」，
全合成与混合增强都必须选中场景、站点与目标才通得过频率计划的 `stations` 检查。也就是说
这个变体在这个页面上**没有一次是对的选择**，摆着就是个陷阱。

我上午的处置是加一道检查拦住「定参 + 高档位」的组合（12 §4.5）。那是在给一个不该存在的选项
加一道闸——按 D-057 定下的那条原则（同一件事不留多余的操作入口），正确做法是把选项撤掉。

### 落地

1. **槽位表里 `ch` 只剩「场景绑定信道」**。卡片上的变体下拉本来的渲染条件就是「变体多于一个」，
   于是下拉自动消失；`distance_m` / `tx_power_dBm` / `tx_gain_dBi` / `rx_gain_dBi` 四项「待填」
   一并消失——它们在场景绑定模式下全部由场景带出。
2. **组件本身保留**。`FreeSpaceChannel` 是标准算例与解析锚点的对拍件（`test_channel.cpp` 与
   `test_receiver_chain.cpp` 在用），自由画布与手写框图里照常可用。组件目录、端口表、
   黄金基准**零变化**——撤的是框图页的一个选项，不是一个组件。
3. **那条只为它存在的判据删掉，不留死代码**（同 D-057 对 `switchTxVariant` 的处置）。
   检查项 `propagation` 保留但改名「传播档位自洽」，只剩与引擎 `PropagationConfig::validate()`
   同源的三条。风险从源头消除，比留一道拦它的闸干净。
4. **解不开的框图照实说缘由**。已有的框图若用了这个变体，`parseChain()` 仍然返回 null
   ——**不硬解、不改写用户的文档**（铁律 15）。但界面从干巴巴的「当前框图不是典型链路」
   改成说明「那个零件撤了，这份框图在自由画布里照常打开与运行」。新增 `RETIRED_VARIANTS`
   与 `retiredNote()` 承载这类说明，下次再撤别的变体照样用。

### 残留风险，如实记

自由画布里仍然可以搭出「场景参数源开着工程档 + 定参自由空间信道」，此时链路读数带着附加损耗
而 IQ 上没有。画布本来就不跑频率计划检查；引擎侧也拦不住——`IComponent::check_wiring()`
只拿得到自己被连上的**输入口名**，而 `ScenarioSource` 根本没有输入口，看不见下游是谁。
这一条属于自由画布的高级用法自负其责，写进模型卡 §7，不假装解决了。

### 全量

web 195 项（新增两条：只剩一个变体、已撤变体的框图不硬解且说得出缘由）；
`slice4-smoke` 86 → **88 项**（卡片上没有变体下拉、不再要求手填距离）；
引擎 208 + 8、服务 124、六套 e2e **261 项**全绿；黄金基准与示例场景零改动。

12 报告 §4.5 改写并标明「2026-09-10 由 D-059 修订」，§8 风险 6、模型卡 §7、
`docs/component-catalog.md` §8、`docs/diagram-canvas-guide.md` §2.9 同步。

### 同日续：删掉自由画布（D-060）

用户：「自由画布功能先删除吧，目前先按这个固定模式来，先把一个完整的信号级仿真的流程跑通。
免得分散精力，自由画布不重要，用户操作起来也很难控制，有点华而不实。后续我想清楚了，再加也不迟。」

### 先划边界：`web/src/diagram/` 里有一半不是画布

动手前逐个查了跨模块引用，结论是这个目录里**只有一半属于画布**：

| 文件 | 归属 | 去留 |
|---|---|---|
| `doc.ts`（210 行） | 框图文档的解析与规范序列化，典型链路、探针、单测全在用 | **留** |
| `format.ts`（66 行） | 工程计数法，典型链路与数据中心在用 | **留** |
| `ParamPanel.tsx`（226 行） | 只有 `Field` / `range` 被典型链路用，其余是画布的整块节点面板 | **拆**：留两件，改名 `Field.tsx` |
| `DiagramView / NodeCard / Palette / layout` + 单测（618 行） | 画布本体 | 删 |
| `DiagramJsonView.tsx`（44 行） | 切片 ① 的过渡形态，**已经无人引用**（被画布的「源码」页签顶替后忘了删） | 删 |
| `index.ts` | 只导出上面两者 | 删 |

顺手查出的那处死代码（`DiagramJsonView`）说明一件事：删功能时**先查引用再动手**，
不然既会误删还在用的，也会漏掉早就没人用的。

### 落地

1. **依赖**：`@xyflow/react` 连同 13 个传递依赖不再随包，`THIRD-PARTY-NOTICES` 记一条说明。
   **D-032 允许 React Flow 随包的那半条作废**，`ws` 那半仍然有效；前端运行时依赖回到四件。
2. **路由**：`Route.canvas`、`ui.diagramCanvas`、`ui/diagramCanvas` 动作、探针里两处 `canvas` 全删。
   旧地址 `#/diagram/canvas` **不特殊处理但也不丢**：`#/diagram` 之后多出来的段一律忽略，
   收藏夹里的旧链接仍落在框图页。顺带把 Router 的写回判据由「解析结果不同」改成
   「规范化后的字符串不同」，于是非规范写法会被 `replaceState` 就地规范回 `#/diagram`——
   否则地址栏里会一直挂着 `#/diagram/canvas`，冒充一个已经不存在的形态。
3. **D-052 连同它守的缺口一起消失**：没有子形态，就不可能被困在某个子形态里。
4. **解不成典型链路的框图：只读展示原文**。`parseChain()` 照旧返回 null（不硬解、不改写用户的文档，
   铁律 15），页面从「在自由画布打开」改成把原文只读放进 `<pre>` 供拷走，加一个「新建典型链路」
   按钮并注明它会**换掉**这份文档。**只读是有意的**：可编辑的原始 JSON 框跟画布一样难以控制，
   这里要的只是一条不丢数据的退路。

### 端到端：两套用例原来靠画布载入示例框图

`slice1` 与 `slice2` 跑的是**手写框图**（切片 ①、② 的示例），它们原先从画布「源码」页签的
「示例框图」下拉里选。画布一删，界面上就没有载入手写框图的入口了。

改走公开端点：`PUT /api/v1/diagrams/{id}` 存进去，再**真刷新页面**，由启动时的
「载入最近保存的框图」捡起来（抽成 `loadDiagramViaApi()`，两套用例各一份），`finally` 里走 HTTP
删掉不留副作用。这里踩到一个坑：**只改 hash 不会重载页面**，而「载入最近保存的框图」是启动时
那一次的事，所以必须显式 `Page.reload`。

**顺带填了两个坑，都属于「端到端框架本身」而不是这次的改动**：

1. **只改 hash 不算导航**，浏览器视之为同文档跳转，页面不重载。先 `Page.navigate` 改 hash
   再 `Page.reload`，又可能在 hash 还没生效时就重载了旧地址——slice2 第一次这么写就停在场景页，
   一直等不到框图。改为在地址上加一次性查询参数 `?_reload=<时间戳>`，一步就是真正的导航。
2. **未保存改动会让 `Page.navigate` 永远不返回**。页面有脏数据时 `beforeunload` 调
   `preventDefault()`，Chrome 弹「离开此页？」，无头下没人点它，用例既不失败也不结束、只是挂着。
   slice2 在布过站之后场景正是脏的。修在 `cdp.mjs` 里：`Page.enable` + 监听
   `Page.javascriptDialogOpening` 自动放行。这条对所有用例都有效，早该有。

`slice3-smoke`（30 项，画布专属）整套删除。`slice4` 里 D-052 的画布往返一段换成五条新断言：
旧地址仍落在框图页、地址被规范回、页面上没有组件库也没有画布入口、
不是典型链路时给出「新建」的出路、原文只读摆得出来且拷得走。88 → 85 项（换掉的比新加的多）。

**地址规范化第一版写错了落点**：放在 Router 的「状态 → hash」那个 effect 里，而
`#/diagram` → `#/diagram/canvas` 是同文档跳转，解析出的视图没变、状态不动、组件不重渲染，
那个 effect 一次也不会触发，地址栏一直挂着 `#/diagram/canvas`。改到 hashchange 处理里才对。

### 文档

`docs/diagram-canvas-guide.md` 改名 `docs/chain-view-guide.md` 并**全篇改写**为框图页的操作说明
（试验设置、配置焦点、参数面板、传播效应、观测点、运行与保存、快捷键、界面当场拒绝的六条、
当前做不到的七条、排查表）。画布时代的操作说明（拖放、连线、组件库、端口把手形状）不保留——
留着一份描述已删功能的说明书，比没有说明书更糟。

10 报告 §0 第 4 条与 §5.6、09 报告 §6 章首各加一条作废说明，**正文不删**：它们记的是当时的
设计过程。09 §6 里有三条经验在典型链路视图里仍然成立并仍在执行，一并注明——
界面不自行解释规则、参数控件由 `ParamSpec` 生成、报错四元组落到具体那张卡片上。

### 全量

前端产物 **1679434 → 1472301 字节**（−202 KiB，−12.3%）。这个数是实测的：把上一版提交检出到
另一个工作树、装上 React Flow、同法打包一次再比对——**先按印象写了「1.42 MB → 1.06 MB，降 25%」，
一测才发现两头都不对**。React Flow 压缩后没有想象中那么大，占的是整包的八分之一。
`web/src` 与 `tests/e2e` 合计 −1353 / +270 行。

web **189** 项；五套 e2e **14 + 39 + 27 + 85 + 63 = 228 项**（`slice3` 30 项已删，`slice4` 88 → 85）；引擎 208 项 doctest + 8 项 ctest；服务 124 项；`build-all.sh` 全过。黄金基准与示例场景零改动。

**恢复成本**：全部改动在一个提交里，git 历史完整。将来想加回来是 revert 加适配，不是重写。

## 2026-09-12 V-0：态势与信号可视化提升的方案（13 号报告 v1.0，D-061）

**起因**。用户把 Aaronia AARTOS 的 25 张截图与 2 段视频放到 `ref/Aaronia AG/`，要求借鉴其态势与信号可视化；随后又指出 em-demo 的界面排版可借鉴。
本条记录方案形成过程与核实到的事实。

**看了什么**（实测得到）。27 份素材逐一看过（视频按每秒抽帧）；aartos-solutions.com 被反爬拦下（HTTP 403，换浏览器标识同样），
方案文字来自 Aaronia 官网与几篇报道；em-demo 的 `App.tsx`、`TargetList / TargetDetail / SignalPanel / SimControls / StatusBar / Toolbar / EffectPanel / CoverageControl / MapView` 逐个读过；
EM-C-UAV 工程 `UI/` 目录的三张 T.META 概念图看过。现状用本机运行中的服务截了三页（场景 / 框图 / 结果，2560 × 1440，demo-03 三站三源任务跑完之后；截图在会话临时目录，不入库）。

**核实到的两处缺口**（实测得到）。① 刷新后页面采用了最近任务（demo-03，三站，观测点 `s1__site-1..3`），场景页却载入了 demo-01，地图上只有 site-1 的测向线：
`bootstrap` 的场景作业恒取清单第一项（服务端按 `scenario_id` 升序）且与任务作业并发，`task.json` 只有 `scenario_sha256` 没有 `scenario_id`——
`manager.ts:611/692` 算出了、`:264` 解构时丢掉。② 地图上从没有站到目标的链路线：`link_id = "site-1-uav-1"`（`geo/src/scenario.cpp:250`），
前端两处按最后一个 `-` 拆（`useSituation.ts:134-136`、`ObjectForm.tsx:377-379`），`node -e` 实测拆成 `["site-1-uav","1"]`，站查不到即 `return`，
链路 GeoJSON 恒空；`slice2-smoke` 只断言图层存在与 `app.links` 数据、不断言渲染，所以一直绿。

**方案怎么定**。三档十一项建议获认可后用户拍板六条（13 报告题头）。第一个切片 ⑧「一屏讲清楚」= V-1 卡片栈 + V-2 叠加加重（含圆形告警区 `zones[]`）+ V-3 全宽时间轴 + 两处缺口；⑨–⑪ 待排期。
三个只读探查代理核实了前端状态与 store、服务端端点与格式、引擎 `cuav_run` 与 `geo/` 的落点（第一批被打断后重派，结论写进 13 报告各节的行号引用）。
关键取舍：全宽时间轴条作 `.app` 网格第四行而不放顶栏那 170 px；告警区判定纯前端几何不进引擎；`demo-01` 不加告警区以保 `DEFAULT_CHAIN_TEXT` 逐字节不变，
只有 `demo-03` 的航迹基准哈希会变（样点必须逐点相同，D-054 先例）。

**改了什么**。新建 13 报告；CLAUDE.md 加 D-061、文档表 13 行、已定表「态势与信号可视化」行、里程碑表 V 行、切片枚举补 ⑥⑦⑧；
06 加 §9J（V-0 至 V-11）、§0.2 树的 V 线、§0.3、§9E 的 ⑧ ⑨ 行、§14；09 §3.2 与 §5.3 引用块、v1.10 修订行；
`docs/scenario-format.md` §9 与 `docs/display-route.md` §6.3 各登记待写项。**代码、schema、黄金基准一件未动**；`ref/` 未跟踪，建议进 `.gitignore`（待用户表态）。

**下一步**。切片 ⑧ 待用户排期：V-1（先修两处缺口）→ V-2（基准变更单独收口）→ V-3 → `slice8-smoke`。

## 2026-09-12 V-1a：两处缺口修好（采用任务载入它的场景；链路线按已知站与源匹配）（D-061）

**改了什么**。① 服务端 `TaskRecord` 加 `scenario_id`（`server/src/tasks/store.ts`），`submit()` 从 `prepareDiagram` 已经算出的
`scenarioId` 写入（此前在解构时丢掉）；`docs/display-products.md` §1.1 加一行。② 前端 `TaskRecord` 加同名可选键；`adoptTask`
在采用任务后若记录带 `scenario_id` 且与当前场景不同就 `loadScenarioInto`；`bootstrap` 把「场景清单」与「最近任务」并成一个作业，
先采用任务、再在场景仍未就绪时退到清单第一项（旧记录没有该键、场景已删除都走这条回退）。③ `scenarioOps.ts` 新增 `splitLinkId(doc, linkId)`，
按 `derivedLinks(doc)` 精确匹配；`useSituation.ts` 的链路线与 `ObjectForm.tsx` 的链路读数改用它，不再按最后一个连字符拆。

**验证**（实测得到，macOS，原型阶段验证值）。服务 125 项（+1：带 `scenario_ref` 的框图记 `scenario_id`、不带的不写该键）、
web 190 项（+1：`site-1-uav-1` 拆成 `site-1` 与 `uav-1`，对不上返回 null）全绿；`slice2-smoke` 27 → **28 项**，
新断言「链路线渲染出要素」实测 1 个要素（改前为 0——这条断言就是为改前的静默失败立的）；`slice6-smoke` 63 项全绿；
跑完 demo-03 任务后新开页面，`app.scene.scenarioId === 'demo-03'`、`sites === 3`，`GET /api/v1/tasks/{id}` 的 `scenario_id = demo-03`。
黄金基准与示例场景零改动。**下一步**：V-1 主体——右栏目标列表 + 卡片栈 + 站点卡（13 报告 §3）。

## 2026-09-12 V-1：目标与站点卡片栈（切片 ⑧，D-061）

**做了什么**。① `web/src/scene/cards/derive.ts`：纯函数把场景文档 + 态势快照算成目标卡与站点卡（每站一行：距离 / 地面距离 /
方位 / 俯仰 / 路损 / 视距 / **接收电平** / **信噪比** / 测向 ± 2σ 与质量档；定位行按方法一行；未运行的行退到浏览器几何、电平与路损为
null 不编），探针 `app.cards / app.siteCards` 用同一份；② `cards/Cards.tsx`：目标列表（表）+ 目标卡（分节二列栅格，机型剪影、
小方位盘）+ 站点卡，右栏按 4 Hz 定频取 `sceneStore.rev` 重算，不按事件频率重渲染；③ `RightColumn.tsx`：卡片栈常驻，选中对象或
量了两点时既有表单叠在栈顶并带「返回卡片」；④ 顺带两处：链路页「选场景」只在**链路自己**换了场景时才清空站与源的选择（此前按
「场景页当前载入的」判，而场景页自 V-1a 起跟着最近任务走，切到已载入的场景会把旧选择原样留下——`slice6` 当场红）；地址栏
`?scenario=` 点名要开的场景，优先于最近任务的场景（与 `?aoi=` 同法，演示与端到端都用得上）。

**验证**（实测得到，macOS，原型阶段验证值）。新 `tests/e2e/slice8-smoke.mjs` **16 项**全绿：三张卡九行的距离 / 方位 / 路损与
`links` 端点最后一行逐值相等（容差 1e-6）、接收电平 = 发射功率 + 两端天线增益 − 路损、信噪比 = 电平 − 带内噪声、测向九行与 `bearings`
端点相等、定位三行 CEP 与 `positions` 端点相等；不点选即 3 行 / 3 卡 / 3 站卡且无空态提示；点行选中 → 表单叠在栈顶 → 返回卡片；
刷新后场景仍是任务的 demo-03；链路线渲染 19 个要素。web 194 项（+4，`derive.test.ts` 对着 demo-03）；服务 125 项；六套 e2e
14 + 39 + 28 + 85 + 63 + 16 = **245 项**全绿。`slice2` 的「站点与航点画在图上」改为轮询：场景自 V-1a 起在采用最近任务之后才载入，
常晚于瓦片就绪，单次查询会抢在地图画出新数据之前（改法与链路线那条相同）。顺带看到框图页上探针会记 20 条 `probe: TypeError`
（地图隐藏时 MapLibre 的查询抛出，被探针捕获记账，不是页面异常；此前就有，本轮未动）。黄金基准与示例场景零改动。

**下一步**：V-2 地图叠加加重（含圆形告警区 `zones[]`，三处同一提交；`demo-03` 加区后航迹基准只哈希变，单独收口）。

## 2026-09-12 V-2a：告警区 `zones[]` 进场景格式，demo-03 加区，航迹基准只哈希变（D-061，基准变更单独收口）

**改了什么**。三处同一提交：`docs/schemas/scenario.schema.json`（顶层 `zones`，`required` 不加；`$defs.zone`）、`docs/scenario-format.md`
（§2 表加一行、新 §6.1、§8 示例加一条、§9 待写销项）、`engine/src/scenario_json.cpp`（顶层键表加 `zones`，`parse_zone` 枚举显式判定不给缺省：
`kind ∈ alert | warning`、`shape` 只许 `circle`、圆心经纬范围、半径为正、`alt_max_m` 可选且非负）；`geo::Zone` / `Scenario::zones`
（`geo/include/cuav_geo/scenario.h`），`cross_check` 查告警区标识唯一。引擎只收下、不解释（`equipment_model` 先例）。`demo-03` 加一个
`z-east`（圆心 116.4105, 39.99，半径 400 m，限高 300 m）：圆心在 uav-2 六边形航线的东侧腿上，uav-1 的第二个航点也在圈内，uav-3 静止点在圈外。
`demo-01` 不动。

**基准变更**（实测得到）。`demo-03` 文件哈希 `dbfe3d7a…` → `04d71717…`；重生成 `tests/golden/scenario-track-demo-03.json`，与上一版逐项比对：
**723 个样点逐点相同，顶层只有 `scenario_sha256` 一个键不同**；两份 `tests/regression/diagrams/chain-3x3-{aoa,tdoa}.json` 的
`scenario_ref.sha256` 同步。仓库里旧哈希无残留。引擎 209 项 doctest（+1，25 条断言）+ 8 项 ctest、场景 schema 9 项、web 194 项、服务 125 项全绿；
`check-paths` 过。产品与组件目录黄金基准零变化。

## 2026-09-12 V-2：地图叠加加重与告警区上图（切片 ⑧，D-061）

**做了什么**。① `layers/situation.ts` 重写：图层 7 → **13**（加 `cuav-zone-fill / zone-line`、`cuav-link-label`（`symbol-placement: line-center`，
只写数字与单位——随包字形只有拉丁字符）、`cuav-target-pole`（2 m × 2 m 小方块的 `fill-extrusion`，拉伸到离地高，平视看不出高度、俯仰下是立柱）、
`cuav-target-ring`（选中环）、`cuav-target-label`（`id · 高度`））；目标图标 25.6 → **40 px**、站点 24 → 32 px；链路线 2.4 → 3 px；
`setLayersVisible` 给图层弹层用；② `style/situation.ts`：`zoneAlert #b91c1c`、`zoneWarning #92400e`（按 D2-4 判据实测 5.79 / 4.97 / 4.60 与
6.35 / 5.45 / 5.04，回填 `docs/display-route.md` §4），入圈变体 `cuav-drone-alert`（同机身外加粗环，染告警红）；③ `fixOverlay.ts`：
测向楔形改成沿射线渐隐的色带（半角 max(2σ, 5 px 所张角)，α 0.35 → 0.05，非 valid 减半）+ 1 px 中心线；椭圆 2.5 px、填 0.15、中心点 4 px 带白晕；
④ 告警区编辑：`scenarioOps` 加 `zones / addZone / removeZone / moveZone / zoneOf`，工具条「布告警区」，对象树「告警区」组，右栏告警区表单
（名称 / 类别 / 圆心 / 半径 / 限高开关），`SceneSelection.zone`；⑤ 入圈判定纯前端几何（弦长 ≤ 半径且不超限高）：卡片 `inZone / inZoneName`
与徽标、列表行徽标、地图图标 `alert` 属性换红环变体，每 tick 逐实体算；⑥ 探针 `scene.zones`；图层弹层加「告警区」「高度立柱」两项。

**验证**（实测得到，macOS，原型阶段验证值）。`slice8-smoke` 16 → **28 项**：任务改跑 70 s（uav-2 在 t ≈ 58 s 拐上东侧腿，末尾离 z-east 圆心约 250 m），
末尾只有 uav-2 在圈内、卡片与列表行带徽标、图标 `alert` 属性只在 uav-2 上、`cuav-drone-alert` 已注册；圈、立柱、距离标注、选中环各渲染出要素；
十三个图层齐全；对象树点告警区出表单；图层弹层关掉告警区后 `visibility = none`。`scene-smoke`（图层 70 → 76）与 `slice2`（工具六件、图层表十三）改断言后全绿；
六套 e2e 14 + 39 + 28 + 85 + 63 + 28 = **257 项**；web 196 项（+2：告警区增删移与进圈判定、卡片进圈）；引擎 209 + 8；服务 125；`check-paths` 过。

**排障记录**。`slice8` 第一版在 ③ 对着 70 s 任务整段取 `links` / `bearings`（几 MB 的 JSON 经调试协议回传）挂死——`cdp.mjs` 的套接字断了不拒挂起的调用，
用例既不失败也不结束，两次各占满 500 s。两处处置：`slice8` 只取末尾半秒（比的本来就是每键最后一行）；`cdp.mjs` 在套接字 `close / error` 时把挂起的调用全部拒掉。
顺带发现 `launchChrome` 固定端口 9333：上一次 Chrome 没被杀干净时新实例绑不上端口、用例会驱动旧实例并留下一串标签页，本轮手工清理，未改框架。

## 2026-09-12 V-3：全宽时间轴条与回放缓冲，切片 ⑧「一屏讲清楚」收口（D-061）

**做了什么**。① `web/src/shell/timeStore.ts`：外部小 store `{ t, mode: live | replay, playing, speed: 1 | 2 | 5 }`，同 `cursorStore` 范式，拖动按指针频率写它、不进主 reducer；
`web/src/shell/timelineOps.ts`：命令与换算（`seekTo / syncSignalCursor / followSignalCursor / goLive / togglePlay / advance / timelineDuration / timelineMarkers`）。
文件不叫 `timeline.ts`，因为 macOS 文件系统不分大小写，与组件 `Timeline.tsx` 撞名，tsc 当场报 TS1149；② `web/src/shell/Timeline.tsx`：`.app` 网格由三行改四行
（`var(--topbar-h) 1fr auto auto`），时间轴条 40 px 在第三行、底部抽屉之上，场景页与结果页可见、框图页与数据页 `hidden`；左端计数（目标 / 站 / 视距 / 测向 / 定位 / 告警，4 Hz 刷新）、
轨道（已到达进度、活动标记 ▲ ▶ ◆ ▼ ● ○ ⇅ 只列时长以内的——任务 70 s 而场景 120 s 时，之后的活动不在这次运行里、指针捕获拖动）、右端 `t / 时长`、▶ ⏸、1× 2× 5×、「跟随实时」；
③ `sceneStore`：每键按 `t_s` 有序的历史（`pushHistory` 乱序插位、每键上限 12 万条）、`snapshotAt(t)`（二分取最后一条 `t_s ≤ t`，航迹只画到 t）、`hasHistory()`；`situationSnapshot(sit?)` 可对任一快照摊平；
④ 新 `web/src/scene/situationView.ts`：`currentSituation(doc)` 是地图 tick、卡片栈、探针与时间轴计数**四处共用**的「现在该画哪一帧」——live 取每键最新（今天的行为），replay 取历史快照，没历史时走
`RoutePreview / ActivityPreview` 预览（`source = preview`，运行前也能拖着看航线）；`situationRev()` 把 sceneStore 版本与时间轴状态合成一个串，四处都按它判断要不要重算；
⑤ 与信号页游标双向：时间轴改 t → `signal/follow off` + `signal/cursor (t − t0_s)`（拖动中 100 ms 节流、松手一次）；信号页 `←/→` 改游标 → 时间轴跟到 `t0_s + cursor`；
防回环靠**数值相等**（1e-6）即不再写回，没用来源标记；勾「跟随实时」两边一起回 live；新任务开始 / 换任务 `timeStore.reset()`；⑥ 快捷键 `Space` / `Home` / `End`（场景页与结果页、焦点不在输入框，
`hotkeys.ts` 加 `timelineActive`）；⑦ 探针 `app.timeline{ t, mode, playing, speed, markers, source }`，`app.entities / links / bearings / positions / cards / siteCards` 改为「画面上那一帧」。

**验证**（实测得到，macOS，原型阶段验证值）。`slice8-smoke` 28 → **40 项**：live 下时间轴计数写着目标 3 · 站 3 · 告警 1、活动标记 8 个与探针一致；用调试协议的鼠标事件把游标拖到 30 s，
`t = 30.0000015`（1.5 px 容差 0.084 s）、信号游标 = t − t0_s 且转回看、图上三架机的位置与 `track` 文件里 30 s 前最后一行逐值相等（1e-9 度）、卡片九行的距离与 `links` 文件同时刻行相等（卡片与地图同一帧）；
空格播放 0.6 s 墙钟前进 0.62 s、再按暂停且暂停时刻同步到游标；Home 回 0；「跟随实时」后图上回到 `track` 最后一行；结果页也有时间轴、按 → 后时间轴跟到 `t0_s + 游标`；框图页无时间轴。
六套 e2e 14 + 39 + 28 + 85 + 63 + 40 = **269 项**全绿；web 196 → **204 项**（历史与快照 2、时间轴命令与标记 5、快捷键 1）；服务 125、引擎 209 + 8 未动；`check-paths` 过。
截图核对（2560 × 1440）：live 下黑游标在末端、进度条铺满；replay 下橙游标在 45 s；活动标记落在轨道上；结果页是同一条。

**两处实测澄清，都不是缺陷**。① 第一版断言「跟随实时后实体时刻 = 任务时长 70 s」失败——`track` 的最后一行是 69.9 s（10 Hz 航迹），「最新一帧」就是文件最后一行，断言改比 `track` 末行；
② 时间轴计数按 4 Hz 刷新，e2e 在 250 ms 内连做 Home → 跟随实时 → 切结果页，读到的是上一格的数（告警 0），单独复现三遍都对，e2e 不对计数下时序断言。

**下一步**：切片 ⑧ 收口。按 06 §9E 顺序下一站是 ④b C-3 检测升级与落盘；切片 ⑨（V-4 方位图、V-5 精度与覆盖场）待排期。

## 2026-09-12 V-3b：场景页信息分层（D-062）

**起因**。切片 ⑧ 收口后用户看场景页：「信息组织有些混乱，信息互相交错，缺乏层次，成为了信息的堆砌。请重新梳理和设计。」
对着 2560 × 1353 的截图盘点（实测得到）：左栏一棵树平铺约 50 行（数据包 6 行、站点 3、辐射源 3、航点 14、活动 8、告警区 1、派生链路 9），一屏半；
右栏约 45 个读数，配置项与观测量混在一张卡里、三张卡可同时展开、站点卡重复框图页的参数；地图约 50 个图元同权重，九条测向色带叠成一片；
顶栏与时间轴条各写一遍 `t / 时长`；uav-1 的高度出现 4 处、链路 3 处、站点 3 处、活动 2 处；全部 12 px 同一字重。
用户认可三条原则（按「用户在问什么」分栏；总览 → 列表 → 详情三级且详情一次一个；一个事实一处）与版式后说「继续」。设计与落地记录在 13 报告 §13。

**做了什么**。① 左栏 = 配置：`ObjectTree` 只剩站点 / 辐射源 / 告警区三组（3 × 3 场景 7 行）；航点与活动折进辐射源表单（`RouteSection` / `ActivitySection`，默认收起，点行选中航点或活动，两种表单加「返回辐射源」）；派生链路不进树，`SceneSelection` 去掉 `link`；
选中对象的表单与「取消选择」条接在树下（新 `LeftColumn.tsx`），右栏不再被表单顶掉；数据包缩成底部一行 `details`（默认折叠），明细同一份放抽屉「资源」页签（`ScenePackageDetails`）。
② 右栏 = 观察（`SituationPanel`）：焦点卡一张（头行身份 + 告警徽标、第二行运动一句话、各站一表 `站 | 距离 | 电平 dBm | 信噪比 dB`，每站第二行 `测向 ± 2σ · DF 档`、定位一行、位置一行、「身份」折叠）+ 目标列表一行一目标（去掉航向列）+ 站点一行一站（去掉采样率 / 噪声系数 / 天线增益——那是配置）。
焦点 = 选中的辐射源，没选中取第一个（`scene/focus.ts`，探针 `app.scene.focus`）。③ 地图分焦点与背景：非焦点目标标签只标识别号、航迹细淡、链路线 1 px 不标距离（`cuav-link-label` 加 `filter`）、不画测向色带、定位只画中心点；图层菜单加「全部目标叠加」。
④ 工具条分组：观察组 图层 / 平视 / 测量常驻，编辑组（选择 / 布站 / 布目标 / 航点 / 布告警区 / 撤销 / 重做）收在「编辑场景」开关后，拖动站点与航点也只在编辑模式；「保存场景」常驻。
⑤ 顶栏在场景页与结果页不再显示 `t / 时长 × 实时因子`（时间轴条已有），框图页与数据页保留。⑥ 字级三档 `--fs-title 14 / --fs-read 13 / --fs-label 12`；旧卡片样式删掉。数据、端点、`derive.ts` 与探针里的卡片数据一件不改。

**验证**（实测得到，macOS，原型阶段验证值）。`slice8-smoke` 40 → **42 项**：焦点卡 = uav-1、目标 3 行、站点 3 行、左栏无表单、树 7 行、数据包折叠、顶栏无时间读数、工具条只露「测量」；入圈徽标只在列表行上、焦点卡（uav-1）没有；
点 uav-2 行 → 表单出现在左栏、焦点卡换成 uav-2 并带徽标、航线 7 航点与活动默认收起、渲染出的距离标注全属于 uav-2、链路线焦点三条粗其余细；取消选择后焦点回 uav-1。
`slice2-smoke` 28 → **29 项**（观察状态只露「测量」，点「编辑场景」后六件齐全；树不再列航点、活动与链路）。六套 e2e 14 + 39 + 29 + 85 + 63 + 42 = **272 项**全绿；web 204 项；服务与引擎未动；`check-paths` 过。
截图核对（2560 × 1440）：左栏 7 行 + 表单一屏放下；右栏焦点卡、列表、站点三块各一眼；地图上只有焦点目标的三条测向色带与椭圆，中间那片褐色没了。

**两处与聊天里的方案不同**。① 焦点卡各站表去掉真值方位与路损两列：右栏 320 px 放不下七列（第一版测向列被截在「272」，去掉两列后仍被截，最后测向另起一行），测向列已是同一方向的量测、真值方位只是答案，电平 = 发射 + 天线 − 路损而路损留在探针里；单位改写在列头、数字裸写。② 「保存场景」不收进编辑组：脏了随时要能存，端到端刷新后也要能点到。

**顺带修一处探针的假错**。`window.__probe()` 里 `map.areTilesLoaded()` 在 MapLibre 重建某个数据源瓦片表的一瞬会抛 `TypeError: Cannot convert undefined or null to object`（`Object.values(sourceCache._tiles)`）。
以前 `errors` 只记消息，查不出来源；这次先把调用栈前几帧记进去才定位到。它是 MapLibre 自己的过渡态不是地图错误，探针改为那一拍按「未加载完」报、不记 `errors`，否则轮询探针的端到端会随机红（2560 宽下 20 s 内复现一次）。

**顺带修时间轴条的一处抖动**。左端计数是 `flex: auto`，「告警 1」变「告警 0」时字重从 600 回到常规，轨道左沿跟着挪几个像素；`slice8` 拖到 30 s 那一项因此出过一次 0.43 s 的偏差。计数改定宽 290 px，轨道不再随数字挪。

**下一步**：④b C-3 检测升级与落盘；切片 ⑨ 待排期。

## 2026-09-12 排期拍板：下一步 C-3 检测升级与落盘，④b 收口后再排切片 ⑨

用户问「④b 的 C-3 与切片 ⑨ 具体指什么」「先做哪个好」。给出的判断：C-3 是主链的下一环——今天典型链路到能量检测就断了，检出只被计数、不落盘，结果页「检测识别」页签是空的，
演示时是看得见的窟窿；C-4 特征识别、C-5 真值评价、V-8 标签条都压在 `detections.jsonl` 上；切片 ⑨（V-4 方位图、V-5 精度场与覆盖场）没有下游依赖，V-5 还要给引擎加子命令与新的网格索引家族，改动面大、演示增量小，等检测真出结果再做覆盖场才有实测依据；
C-3 算法已在 10 报告 §4.2 定稿、Python 参考只加一个模式、黄金基准要求不变，一两天能收口。**用户拍板：C-3 → C-4 → C-5（④b 收口）→ 切片 ⑨；V-4 半天量可随时插入。** 已记入 06 §0.3 与记忆，后续在新对话里从 C-3 开工。

### 2026-09-12 · C-3 检测升级与落盘（D-063，六个提交）

**做了什么**（按提交）：① 引擎——`EnergyDetector` 加 `noise_mode ∈ {probe, sliding}`、`noise_window_frames`、`merge_gap_frames`、`band_power_dBm` 与三个内部参数（`scene_bindable`，只取 `site_id` 作行身份）；`Detection` 追加十个自描述字段；`IComponent::set_node_name()`、`IRunObserver::on_detection / on_detection_summary`；六个新用例；组件目录基准更新一次（脚本核对只有 `EnergyDetector` 一条）。② Python 参考加 `sliding_from_power / segments_from_hits / detect_sliding`（显式逐帧循环）；生成器加 `--mode sliding`（单音按 `ToneSource` 公式逐样点复刻）；新黄金基准 `energy_detector_sliding.json`；probe 基准逐字节不变（`cmp` 守）。③ 运行器 `detections.jsonl` 逐帧、`detections.index.json` 摘要、`detection` 事件按段首帧、终态 `detection_rows`；新夹具 `slice4_detect.json` 与运行器用例；服务端抽稀键 `node_id`、过滤 `hit`、`…/detections/index` 路由。④ 典型链路 `det` 绑站 + 模板固定 `sliding`，缺省链路文本与两份 3×3 夹具重生成并 `--validate`。⑤ 结果页「检测识别」突发列表（`detectionStore` + `segments.ts`）、右栏检测器摘要、信号页「叠加检测」、探针 `app.results.detections`；slice4 85 → 95 项、slice6 63 → 67 项。⑥ DS-6 复跑脚本与报告、模型卡 `models/detection/README.md`、文档回填。

**实测（macOS，原型阶段验证值）**：滑动黄金基准 4000 帧 statistic 最大相对误差 **4.1e-7**（判据 1e-5），命中 1031 / 段 31 / 陈旧 744 帧逐帧相同；H0 暖机后虚警率 **0.0107**（目标 0.01）、均值 1.002；缺省链（demo-01，6 s）最长段起点 **3.015 s**（tx_on 3 s，块粒度 ≤ 0.13 s）、1457 帧、开机前 **1** 帧虚警、陈旧 1200 帧，行文件 1458 命中行；`slice4_detect` 夹具 7812 行 2.7 MB（约 345 B/行）；demo-03 3×3 三站 20 s：site-1 / site-2 / site-3 命中 0 / 1 / 8673（两站从 t = 0 持续的单音被吸收，第三站看到的是同频双音的拍频与突发）；**开销**：同一份 3 站 20 s 框图 sliding 6.41 s 对 probe 4.30 s，每检测器每帧约 0.07 ms，实时因子仍约 3。全量：引擎 **217** 项 doctest + **9** 项 ctest、服务 **126** 项、web **207** 项、六套 e2e 14 + 39 + 29 + 95 + 67 + 42 = **286** 项全绿。

**与计划不同的三处**。一、**DS-6 分半标定复跑没有「不再失效」**：比值 7.27 / 8.30（静态 7.20 / 7.00）。加了分位数与逐片中位数两项诊断后清楚了——两半区在 50% 分位对得上（滑动 1.042 / 1.051），在 99.9% 分位对不上（77 / 101）；尾部是真实的 WiFi / 蓝牙突发，两半文件采自不同时间地点，突发强弱本来就不同，噪声估计归一不掉；滑动估计归一的是主体：安静频段逐片中位数散布 1.32 → 0.52 dB。繁忙频段部分片占用 > 50%（逐片中位数最高 4.49），中位数估计两种口径都跟不上。这改写了对 D-026 的解读（漂移在干扰尾部而非噪声底），按铁律 10 记发现、不调参；报告 `data/iq/measured/ds6-sliding-report.md` 的结论段照实写。二、**暖机期的虚警**：环里只有一两帧时估计极不可靠，分段用例在种子 5 的第 1 帧就命中了一次，起初的断言（全程命中 == 198）因此红了；这是 10 §4.2「环未满时用已有帧、不标降级」的真实后果，断言改为只看暖机之后、段号按行分组比较。三、**观察者签名与索引文件**：报告自描述而不传 `BlockMeta`；行不带 trace、索引带一次——都记进 10 报告 §11 的补条。

**顺带核实的两条**：`tests/regression/diagrams/` 的两份 3×3 夹具没有任何自动消费者（只有 README 的手工 `--validate`），本次用脚本改并校验；`SlotCard` 摘要行原本不看 `v.fixed`，det 卡片会把 `noise_mode` 写成目录缺省 `probe`，与编译进框图的 `sliding` 相反，已改。

**下一步**：C-4 特征与识别（`FeatureExtractor` / `TemplateClassifier`，`features.jsonl` / `recognitions.jsonl`，模板库 v1；`rc_hopping` 一类等 G-6）。C-9 时再做帧粒度列表、时间线条带与识别标签叠加。

### 2026-09-13 · 播放时信号页不动（V-3 遗留，用户实测）

**现象**：结果页信号页签，按底部时间轴的播放，频谱与瀑布不变，只有时间轴游标在走。

**根因两处**（实测得到）：① `advance()` 播放推进时只改 `timeStore.t`，信号游标 `signal/cursor` 只在暂停与到头时同步——拖动是每 100 ms 一次，播放漏了；信号页因此整段收不到游标，迹线不换帧、横线不走。② 任务结束时信号页自动收口到「最后 H 行」的窗口（本机约 2–3 s），游标一旦在窗外就被夹到首行，什么都画不出来；09 报告原写「游标联动信号页时间窗中心」，V-3 只做了游标同步。

**修法**（`web/src/shell/timelineOps.ts`）：播放中每 0.1 s 墙钟 `syncSignalCursor`；`keepCursorInWindow()` 在游标出窗时平移窗口（跨度不变，前进落窗底 1/4、后退落窗顶 1/4、夹在数据范围内），所有走 `syncSignalCursor` 的入口（拖、点、Home / End、播放、点突发行）都受益；防回环改记「自己写过的游标值」——按当前 t 比在播放中必然不等，会把播放拉停（单测钉住）。按播放时先把起点写给信号页。

**验证**：`timelineOps` 单测加两项（web 209 项）；`slice4` 加四项（99 项）：Home 后窗口回到 0–6 s、播放中游标随时间轴前进（0.54 → 1.28 s，时间轴 1.28 s）、全程在窗内跨度不变、空格暂停。翻页本身在 6 s 的示例任务上触发不到（整段装在一个窗口里），由单测覆盖。**一次返工**：第一版的防回环记住「自己写过的值」后不清，Home 写过 0、「跟随实时」清掉游标、再按 `→` 得到 0 就被当成回声吞掉，`slice8` 的「→ 后时间轴跟游标」红了一项；而我把 `grep` 接在 `&&` 前面，有输出就算成功，带着这项红把 `758e1bd` 推了出去。改为回声只认一次、游标清空时也清记录（单测加一段钉住），`slice8` 42 项、`slice4` 99 项重跑全绿后补提交。**顺带**：`timeStore.reset()` 保留倍速，单测跨用例会串——写用例时先把倍速设回 1。

### 2026-09-13 · 链路页右栏参数面板改版（用户实测「排版混乱、说明文字过多」）

**现象**：右栏「模型参数」那组行没有任何样式（`pp-row` 这个类在样式表里根本没定义），标签、输入框、
范围提示 `0 … 360` 挤在一行里随意换行；标签是英文标识加括号单位；「环节 / 组件 / 参数归属」三行说明与
每个模板固定参数各占一行。上半张「设备参数 · 来自场景」卡是按 `.form-row` 网格排的，单位仍折到下一行。

**改法**：① 三张卡统一用 `.form-row` 网格：标签列 88 px（七字标签一行放下）、控件占满、单位与来源标记
（来自场景 / 由频率计划派生）跟在控件后面不换行；② 参数标签换成中文短名（新表 `paramLabels.ts`，
单测守着「目录里每个可见参数都有标签且不超过 8 字」，新组件上目录时补一行），英文标识、说明、范围收进悬停提示；
③ 「环节 / 组件」两行删掉（收进标题的悬停提示），归属只留一行；模板固定的参数合成「模板固定：端 = tx」一行；
传播效应卡的「本档包含」并成一行，E1 的那句说明只在有冲突时以红字出现（那本来就是 `propConflict` 的输出）。

**顺带发现并修掉一处真实缺陷**：数值框用 `formatEng` 显示，只留三位有效数字——1024 显示 `1.02k`，
用户点进去再点出来，失焦即解析写回 1020，nfft 不再是 2 的幂，引擎当场拒；48828.125 会变 48800。
加 `formatEngExact`（12 位有效数字、可无损往返，单测对 12 个值断言 `parseEng(formatEngExact(v)) === v`），
输入框改用它；失焦时值没变就不写回（原来相等的值也会进撤销栈、把框图标脏）。卡片摘要仍用三位的 `formatEng`。

**左栏同法（用户随后指出「左边也有同样的问题」）**：「试验设置」的模式 / 场景 / 站点 / 目标 / 时长 / 种子六行改成同一套 `.form-row`（原来「时长 s」与「种子」挤在一行里换行）；「频率计划」三行读数排成标签 + 值，八条检查每条一行，通过的把依据句收进悬停提示（`title` 与 `data-detail`），不通过才把原因写在行下；`.pp-title / .pp-sub / .pp-warn` 从右栏专用改成全局，两栏一致。e2e 只依赖 `[data-check]` 与 `data-ok`，没动。

**验证**：web 212 项；四套 e2e 重跑（slice4 / slice6 / slice2 / slice8）见下；截图核对右栏四张卡与左栏两张卡。
**一处踩坑**：截图脚本与正在跑的 e2e 共用同一个应用服务，被中断的 slice4 留下的 `chain-default` 框图
把后一套 slice6 的链路带成了 E1 冲突——e2e 之间共享服务端状态，不能与别的页面并行跑；残留框图已删。

### 2026-09-13 · 框图页三处选择的语义定案（D-064）

**起因**：用户看过多站多源下的框图页后问「选无人机 / 选侦测站是否又是多重入口？多选后卡片上的 ×2 ×3 是什么意思？」。
核实：左栏勾选决定参与运行的实体（改框图形状），中栏两个下拉决定右栏配的是哪一台（D-054）；卡片上的 ×N 是
`instanceBadge()` 按槽位的 `per` 算的实例份数（源 / 链路 / 站）。两处不是同一件事，但界面上看不出区别，且单站单机时
下拉只有一个候选；×N 不说明按什么算。

**用户定**：中栏下拉 = 当前框图的链条与编辑焦点；卡片不显示 ×N；左栏勾选 = 参与计算的仿真实例，用于在同一场景下
挑选关注的局部做计算、观察或调试。

**改法**：删 `instanceBadge()` 与 `.slot-count`；卡片摘要按当前链路的实体取有效参数（共用底值 + 单独设置 + 场景带出的值）；
「待填」也按该实体算（`missingParams()` 加 `entityId`，场景给得出的字段不算缺）；中栏那行加「当前链路」引导词与悬停说明。
`slice6` 的四条角标断言换成「三站三源下也不标 ×N」与「两个下拉各列 3 个候选」。文档：CLAUDE.md D-064、11 报告 §5.2 撤销徽标表、
`docs/chain-view-guide.md` 新增 §2.2b。**顺带**：卡片上的三行参数摘要改成「中文短名 + 值」两段（与右栏同一张标签表），词头与单位连写（`2.44 GHz`、`500 kHz`）。

**验证**：web 212 项；`slice6` 65 项（四条角标断言换两条）、`slice4` 99 项全绿；截图核对三站三源下的框图中栏。

### 2026-09-13 · 结果页左栏去重

用户指出结果页左栏「任务」卡（编号、运行态、结果徽标、实验名）与顶栏面包屑重复，「观测点」列表与中栏的观测点页签重复。
中栏页签是 2026-09-08 为「左栏收起后换不了观测点」加的，之后左栏那份没有撤，两处并存了五天。按一个事实一处合并：
观测点只留中栏页签；左栏「任务」卡只放面包屑里没有的事实——种子、时长、实时因子、观测点数、结果原因（`data-task-facts`）。
e2e 只依赖 `[data-op-tab]` 与 `.col.left .rail`，未动。09 报告修订表加 v1.12。第一版把引擎各节点的备注整段铺在「结果原因」里（三站任务是一堵字墙），改为结果有效时折叠成「引擎备注 N 条」、降级 / 无效时才展开成红字列表。**验证**：web 212 项；`slice4` 99 项、`slice1` 39 项全绿（跑在折叠那一步之前的构建上，折叠只改左栏结构，e2e 不碰它）；截图核对单站与三站两种任务。

### 2026-09-13 · 去掉卡片上的「未实现」徽标

用户指着 DDC 与信道化两张卡说「把未实现三个字去掉，不用标记出来」。徽标去掉（`unavailable` 态不再有徽标文字，状态本身不变、观测点置灰逻辑不变）；
顺带把卡片说明里的「待 MATLAB Coder 产物（M-2）」改成「本期旁路，S4 直接取 ADC 输出」——界面上不能出现 MATLAB（D-036），也不向用户解释还没做（D-039 ②）。
`slice4` 的两条断言随之改为「无徽标文字」与「说明写旁路且不含 MATLAB」，100 项全绿；web 212 项。

### 2026-09-13 · 场景选择移到场景页（D-065）

用户指示：场景在场景页选，框图页与结果页跟着走。此前唯一的入口是框图页「试验设置」的下拉（D-053 §5.1），场景页反而没有选择器。
改法：场景页左栏顶部加「场景」下拉（有未保存改动先 `confirm`）；框图页撤掉下拉、只写当前场景；`useSceneSync` 加 `sceneChanged`：
载入的场景与链路引用不同就换引用并按新场景缺省全选站点与目标（旧选择属于上一份场景）。结果页跟着任务与任务自带的场景，不用改。
`slice6` 的「切到 demo-03」改为去场景页切再回框图页并断言框图跟上；`slice8` 靠地址栏 `?scenario=demo-03` 已够，删掉在框图页的那一步。

**顺带挖出一处旧竞态**：`slice8` 删掉框图页那一步后红了——地址栏 `?scenario=demo-03` 打开，场景却落在 demo-01。用 CDP 抓请求发起方堆栈：demo-03 的请求 0.163 s 发出、0.165 s 返回 200，0.166 s 启动作业的**兜底**又去载了清单第一项 demo-01。根因是 `store.getState()` 给的是上一次渲染的状态（`ref.current` 在渲染时才更新），`await` 刚 dispatch 完、React 还没重渲染那一瞬读到的仍是「载入中」，兜底以为点名的场景没载成。这条路自 D-061 ⑨ 加 `?scenario=` 起就有，此前 slice8 在框图页多选一次把它盖住了。修法：`loadScenarioInto` / `adoptTask` 返回是否载入成功，兜底只看返回值不读 store。教训：**dispatch 之后不要立刻 `getState()` 判断刚才那次 dispatch 的结果**，用返回值或等下一帧。

**验证**：web 212 项；`slice8` 42（左栏表单计数改为不计场景选择器）、`slice6` 66（开页点名 `?scenario=demo-01`——框图跟着场景页走之后，盘上最近一次任务若是三站场景，缺省链就不再是 1 站 1 源，这是新规则的真实后果）、`slice2` 29、`slice4` 100 全绿；`?scenario=demo-03#/diagram` 的启动时序用 CDP 复核：只发一次场景请求、场景与框图都落在 demo-03。

### 2026-09-13 · C-4 第一步：`FeatureExtractor` 落地（特征提取 + Python 参考 + 黄金基准）

**起因**：用户问「运行后显示与服务器断开正常吗？降级又是什么意思？」并要下一步计划。核实两件事：① 「与服务的连接已断开」是误报——换订阅时客户端
`subscribe()` 先 `close()`，`close()` 上报一次 `closed`，reducer 对着还是排队态的新任务把它当成真掉线（用真实 WsClient + reducer 复现：状态序列
closed → connected → closed → closed → connected）；已修（提交 415fe5f：换订阅走不上报状态的 `teardown()`，`ws.test.ts` 加一例）。② demo-01 缺省链
的「降级」是 ADC 过载：最近点路损 70.6 dB（约 33 m），接收电平 −38.6 dBm，前端增益 20 dB 后 −18.6 dBm 超满量程 −20 dBm，全程削顶 6.09% > 1%——
是引擎的真实判断，缺省链不改（计划 Q2）。下一步按 06 §0.3 = **C-4 特征与识别**，计划分六步（步骤 0 误报 → 1 特征提取 → 2 模板识别 → 3 引擎级验收 → 4a/4b/4c 服务端与前端 → 5 回填）。

**规划阶段核实到的两条调度风险**（写进 `processing.h` 头注）：调度器每轮按拓扑序跑一遍、节点只在**所有已连输入**都有数据时才跑、输出按赋值覆盖深度 1 的缓冲；
`EnergyDetector::process` 在本块没凑满一帧时返回 Idle（probe 收集期整段 Idle），双输入的下游那一轮被跳过、IQ 块被下一轮覆盖，**静默丢块**。处置三条：
检测器消费了块就产出（没有完成帧时给空列表，黄金基准只比行、`energy_detector*.json` 逐字节不变）；特征提取器核对块的 `start_sample` 首尾相接，断档即报错；
定为约定「消费了输入的轮次必产出」，C-5 的评价器（det + rec 双输入）靠它活。第二条：双输入节点收不到上游 flush 的尾块——sliding 下检测器 flush 不出数据，本步不受影响，C-5 前置契约同上。

**做了什么**：
- 结构：`FeatureRow` / `FeatureVector`、`PortData.features`、`FeatureReport` + `IRunObserver::on_feature`。
- 组件 `FeatureExtractor`（M3 / E2，`model_id = EM-S-03`）：端口 `iq` + `det` → `out`；与检测器同律切帧、按 `frame_index` / `start_sample` 逐帧对齐；段收口 = 新 `segment_id` / 连续未命中 > `merge_gap_frames` / flush；
  **两套谱**——电量（带内功率、信噪比、噪声 = e / Λ）用矩形帧与检测器逐位同源，形状（质心、带宽、平坦度、峰值）用同一帧加周期 Hann 窗：第一版只用矩形帧，
  对不在 bin 上的单音 sinc² 旁瓣把 99% 占用带宽量到几十个 bin，`cw_beacon` 永远配不上，改成加窗后单音 3 个 bin（11.7 kHz @ fs 1e6 / 256）、噪声突发 51 bin 平坦度 0.9988。
  去噪 + 过闸 `noise_gate · n_bin / √F`；「上一段」只由 full / overload 质量的段充当。
- 装载器跨节点检查（连线阶段）：`feat.nfft == det.nfft`、`merge_gap_frames` 相等、检测器必须 `sliding`，错误码 `param` 落在 `feat` 的 `det` 口。
- 运行器：`features.jsonl` 每突发一行（行自带 trace）、`feature` 事件每突发一条、`task.state.feature_rows`。
- Python `algos/reference/features.py`（显式循环累加）；`gen_engine_golden.py --mode features`（同一噪声流 + 四段门控单音 + 一段第二种子的门控噪声；生成器断言没有 bin 卡在闸或累积功率边界 ±1e-4 内）；
  黄金基准 `engine/tests/golden/features.json`（4000 帧、38 段、6 段 full）；`--mode probe / sliding` 输出与既有文件逐字节相同（`cmp` 核过）。
- 目录黄金基准 19 → 20，脚本核对只多 `FeatureExtractor` 一条、端口表与既有组件逐字未变。文档：`display-products.md` §1 / §5.2、`api-versions.md` §4.1、`component-catalog.md`。

**实测（macOS，原型阶段验证值）**：黄金基准逐段对拍首跑即过；组件测试里单音幅度 0.5 时段内每百帧漏三帧（Λ ≈ 2.25 对门限 1.75，信号与噪声的交叉项把 Λ 的散布抬到 0.26），
是物理不是缺陷，测试改用幅度 0.8（Λ ≈ 4.2）。引擎 225 项 doctest + 10 项 ctest、服务 126 项全绿；`check-paths` / `check-ascii` 过。

### 2026-09-13 · C-4 第二步：`TemplateClassifier` 与模板库 v1 落地（识别 + Python 参考 + 黄金基准 + 服务端过滤）

**做了什么**：
- 结构：`RecognitionRow` / `RecognitionCandidate` / `RecognitionList`、`PortData.recognitions`、`RecognitionReport` + `on_recognition`。
- 组件 `TemplateClassifier`（`engine/src/recognition.cpp`，M2 / E2 / V2，`model_id = EM-S-04`）：`in: FeatureVector` → `out: RecognitionList`；
  EM-S-04 §10.5 区间外距离（对数域 `(ln 边界 − ln x)/0.3`、线性域除以半区间宽、缺失特征不计）→ 加权平均综合距离 → `exp(−D/2)` 加未知假设
  `exp(−unknown_distance/2)` 归一成后验 → known / ambiguous / unknown（`unknown_low_quality / unknown_novel / unknown_ambiguous`）；
  只到 `signal_role` 层；消费即产出。库文件 `models/recognition/library-v1.json`（严格结构校验：未知键、区间颠倒、线性特征缺上界、缺权重、
  引用不存在的特征、版本不符都拒），模型卡 `models/recognition/README.md`。
- 装载器：`library_version` → 内部参数 `library_path = <library_root>/library-<v>.json`（`inject_library`，D-037 同法），版本号先过 `^v[0-9]+$`
  （浏览器给的字符串，否则是路径穿越口）；`LoadOptions::library_root` 缺省 `models/recognition`，`cuav_run` 加 `--library-root`（ctest 从构建目录跑要传源码树里的位置）。
- 运行器：`recognitions.jsonl` 每行特征一行、`recognition` 事件、`task.state.recognition_rows`。
- Python `algos/reference/classify.py`（`--write-golden` 出 `engine/tests/golden/recognition.json`：13 行手造特征，覆盖四类中心、缺上一段、
  开放集 novel / ambiguous、低质量、短段与区间端点；两侧 float64，rel ≤ 1e-9）。
- 服务端：`features` / `recognitions` 的抽稀键改为「节点 + 段号」（多站下段号只在站内唯一，只按 `segment_id` 会把两站的第 0 段当一条曲线），
  过滤键 `features: site_id / node_id / quality`、`recognitions: site_id / node_id / result / label`。
- 目录黄金基准 19 → 21（脚本核对只多 `FeatureExtractor` / `TemplateClassifier` 两条）；web 的三条目录守卫随之更新（内部参数 21 → 28、可绑定 6 → 8、九个新参数的中文短标签）。
- 文档：`display-products.md` §5.3、`api-versions.md`（`recognition` 事件、`recognition_rows`、`--library-root`）、`component-catalog.md`。

**两处与 10 报告 §4.4 不同，都是第一次对着真实特征值发现的、写进模型卡 §2 / §3（10 报告 §11 待记）**：
① **平坦度区间**：附录 D 写 cw `[0, 0.2]`、burst 类 `[0.3, 1]`。平坦度按 EM-S-03 是检测频段内原始 PSD 的几何 / 算术均值，对占带内一小部分 bin 的窄信号随
信噪比变（`≈ ρ^f / (1 − f + f·ρ)`，`f` = 信号占带内 bin 的比例、`ρ` = 每 bin 信噪比）：单音在 51 bin 频段里 20 dB 时约 0.3；链路测试里的单音实测 0.297，
按附录 D 判成 unknown。放宽为 cw `[0, 0.5]`（权重 1、带宽权重 3）、burst 类 `[0.1, 1]`；video 占满频段稳定在 0.9 以上不动。
② **接受门限 0.6 → 0.5**：未知假设恒占一份似然 0.135，四模板下正确类即使 `D = 0` 后验也只有 0.55–0.8——0.5 s 单音对 cw 的 `D = 0`、后验 0.594
（telemetry / rc 只在时长与带宽上各差一个量级，`D ≈ 2.5 / 3.3`，似然仍在分母里），0.6 判成 unknown；且 0.6 / 0.2 下 ambiguous 不可达
（`p1 ≥ 0.6` 蕴含领先 ≥ 0.2）。改 0.5 两条一起解决。这两处都是把假定值对到定义的实际取值范围上，不是调参使某次演示通过（库本来就是假定值、第一次落地）。

**顺带**：`--validate` 不给 `--library-root` 时能不能找到缺省库取决于 cwd（ctest 在构建目录、手跑在仓库根），这样的断言不能写进测试，改成指到一个不存在的目录。

**验证**：引擎 233 项 doctest + 11 项 ctest（新增 `cuav_run_validate_slice4_recognize` 带 `--library-root`）、服务 126 项、web 213 项全绿；`check-paths` / `check-ascii` 过。

### 2026-09-14 · C-4 第三步：引擎级验收——三类合成波形的识别准确率

`engine/tests/test_recognition_chain.cpp`：测试专用调度源（背景白噪声 + 按时间表叠加的突发，自带发生器逐样点确定；窄带噪声样突发按
1024 点块在频域随机生成再逆变换，块网格相对突发起点、不与检测器分帧对齐），经 `EnergyDetector(sliding)` → `FeatureExtractor` → `TemplateClassifier`，
带内信噪比 10 dB：cw_beacon = 24 段 0.6 s 单音（fs 500 kS/s）、telemetry_burst = 24 段 10 ms / 100 ms 周期的 40 kHz 窄带突发（fs 500 kS/s）、
video_link = 8 段 0.55 s 全带白噪声（fs 2.5 MS/s，检测频段 ±1.125 MHz 过库里 1 MHz 的下界）。**实测（macOS，原型阶段验证值）**：
56 段全部各出一行 full、突发之外零虚警行；判对 55，准确率 **0.982**（判据 ≥ 0.9）；唯一一个错是 telemetry 的第一段——没有「上一段」，
跳频差与间隔缺失，定频与跳频在其余特征上区间重叠、后验分摊、落 `unknown_ambiguous`（模型卡 §4 已写明：分清跳频与定频至少要两个突发）。
定频突发零个判成 rc_hopping。用时 2.3 s。`rc_hopping` 一类本期没有生成器，验收留 C-8 / G-6。引擎 234 项 doctest + 11 项 ctest 全绿。

### 2026-09-14 · C-4 第四、五步：框图页三节点一张卡、结果页识别列、回填

**框图页**：`feat` / `rec` 做成**挂在检测识别评价卡片里的槽位**（`SlotDef.group`），不单独成卡——编译、反解、参数归属、待填一件不改，只是画法不同；
卡片正文是检测，下面两行子环节各带状态与待填，点哪一行右栏就是哪一个的面板。`compile()` 每站生成 `feat__<site>` / `rec__<site>`（iq 接 S4 尾、det 接检测器、识别接特征），
特征提取的 `nfft` / `merge_gap_frames` 从检测器**派生**（10 §4.3 要求相等，装载器再核对一遍；先剔掉槽位状态里可能残留的旧值再按检测器写）。
`DEFAULT_CHAIN_TEXT` 与两份 3×3 夹具都经真实的 parse → compile 路径再生成（55 / 93 与 58 / 105 节点 / 连线，往返逐字节、引擎 `--validate` 过）；
第一版 e2e 里质心的期望值写成了站中心 2440.5 MHz，实际单音在中心之上 48828.125 Hz（场景的 `waveform.offset_Hz`），改成按场景的值断言。
**结果页**：`recognitionStore` 与 `detectionStore` 同范式，突发表按「站 + 段号」把标签 / 后验 / 结论接在检测段后面；探针 `app.results.recognitions`。
**文档**：10 报告 §11 加「C-4 实施时的修正」（两套谱、`merge_gap_frames`、行自带 trace、接受门限、平坦度区间、`SlotDef.group`、调度约定、验收数）；
`chain-view-guide.md` §5.6 改写并加一条「结果降级、原因是 ADC 削顶」的处置；`tests/regression/README.md` 计数与跑法。

**验证（macOS，原型阶段验证值）**：web 214 项；六套 e2e 在私有的 8090 端口服务上跑（用户自己的服务占着 8080，不动它）：
scene 14、slice1 39、slice2 29、slice4 108（新增子环节、派生只读、识别行、突发表标签列、探针七项）、slice6 67（新增按站实例化的识别与特征行）、slice8 42，合计 **299 项**全绿。
`scene-smoke` 第一遍报 `fetch failed` 是上一套的无头 Chrome 还没退干净、9333 端口被占（记忆里已有此坑），`pkill` 后单独重跑即过。
提交：415fe5f（步骤 0）、b932cff（步骤 1–2）、9204bfe（步骤 3）、本步一个提交；回填 CLAUDE.md D-066 与里程碑 C 行、06 §0.3 / §9G。
**下一步 C-5 真值与评价**（④b 收口）：`Evaluator` 绑站、吃 det + rec 双输入，靠「消费即产出」的约定才不会被跳过；`truth.jsonl` / `metrics.json` / `task.json.metrics_summary`；`evaluate.py` 逐值对拍；跨层算例 ① 典型链路实例差 ≤ 0.05。


### 2026-09-14 · C-5 第零步：调度器接受部分输入（`IComponent::accepts_partial_inputs()`，D-067）

**起因**。C-5 的 `Evaluator` 吃 det + rec（+ scene）多路输入。追踪 `graph.cpp:206-250` 的尾段：多输入节点在「上游全结束且自己的输入没齐」时直接 flush 并标结束，缓冲里已到的那一半被丢；而且**已连的可选口在调度器眼里同样必须有数据**（`:214` 只跳过没连线的可选口）。`FeatureExtractor::flush` 才把仍开着的最后一段吐出去 → `TemplateClassifier` 出该段的识别行 → 到不了双输入的评价器。demo-01 的单音**唯一一段**恰好持续到结束（识别行 `t_s 3.014656 → t_end_s 5.998592`），不修调度器就没有识别准确率可算；C-4 靴带出来的「消费即产出」约定也只治得了中段、治不了尾段。

**做了什么**。`IComponent` 加虚函数 `accepts_partial_inputs()`，缺省 false。为真的节点：任一已连输入有数据即运行 `process()`，缺席的口以 `has_data = false` 的空 `PortData` 给入；消费本身计作本轮进展（它不产出，否则「本轮没有任何节点产出」的停滞判据会误报）；上游全结束且所有已连输入缓冲为空时才走既有的收尾路径。非 partial 节点的判据 `run_now == inputs_ready`，代码路径一字不动。

**验证（macOS）**。新增两条单测（`test_types_graph.cpp`）：A 三块 + 尾块直连、B 一块 + 尾块经压一轮的中继——缺省的双输入汇聚器只收到 `a1+b0 / a2+b1`，**a0 被 a1 覆盖、A 的尾块 a3 在收尾时丢**（把既有行为写成断言）；开启后 `a0 | a1+b0 | a2+b1 | a3` 四次调用一块不丢、每块只消费一次、flush 一次。引擎 236 项 doctest + 11 项 ctest 全绿。**既有产品逐字节不变**：`slice1`、`slice2`（demo-01 场景）与 6 s 的缺省典型链路三份运行、20 个产品文件（`.f32`、索引、`detections / features / recognitions / track / links`）改前改后 sha256 逐一相同。

### 2026-09-14 · C-5 第一步：评价纯函数层 `evaluate()` + Python 参考 `evaluate.py` + 黄金基准 `metrics.json`

**做了什么**。`engine/include/cuav/evaluation.h` / `evaluation_json.h` / `src/evaluation.cpp`：`TruthRow`（一段真值：`[t_s, t_end_s)`、源、标签、波形、中心频率与带宽、`in_band`）、`EvalParams`（真值来源、匹配重叠比、ROC 点数、帧时长、工作点门限、有没有识别器）、`EvaluationMetrics`（帧级 / 突发级 / ROC / 识别 / 质量 / 四态与原因）与纯函数 `evaluate(检测行, 识别行, 真值行, 参数)`，落盘形状 `metrics_section_json()` 单独一个头以免把 nlohmann 拖进 observer.h 的包含链。口径（两侧契约，改动即基准变化）：帧真值按**帧中点**落在 in_band 真值区间并集内判；突发匹配 `overlap / min(len_det, len_truth) ≥ match_overlap`，发现时延钳到 ≥ 0；识别真值取重叠最大的真值段（并列取 `t_s` 早者、再 `emitter_id` 字典序）；ROC 门限取排序统计量的 `⌊i·(n−1)/(P−1)⌋` 位去重、判决严格大于；分母为零一律 NaN → `null`；`noise_stale_frames` 端口上拿不到，写 `null`。`algos/reference/evaluate.py`：同序的纯 Python 实现（显式循环累加）、`<run_dir>` 逐节对拍（整数逐位、浮点 rel ≤ 1e-9、null 对 null）、`--write-golden` 用 64 位 LCG 造一组确定性行——含漏检、虚警、一段频段外真值、库里没有的额外标签 `noise`、unknown 与 ambiguous 各若干、四个不与帧边界对齐的突发窗、两段重叠的同类真值。

**实测**。黄金基准 600 帧：tp 285 / fp 24 / fn 27 / tn 264，pd 0.9135、pfa 0.0833、f1 0.9179；真值段 8（频段外 1）、检测段 19、匹配 8、虚警段 12；识别评价 7 行、准确率 0.5714、未知率 0.1429、存疑率 0.2857；ROC 32 点，工作点与帧级 pd / pfa 逐位相同。C++ 侧经 `metrics_section_json()` 与 `expected` 逐值比（`test_evaluation.cpp`，5 例 222 条断言）：第一遍红了 25 处——识别行的匹配标志查在了匹配前的那份段表副本上（`segmap` 与 `segs` 两份），全部识别行都成了「未匹配」；另一处是测试自己的边界期望踩了 1 ulp（`0.03 + 0.005` 不等于字面 `0.035`），改用二进制可精确表示的时刻。引擎 241 项 doctest 全绿。

### 2026-09-14 · C-5 第二步：`Evaluator` 组件、观察者、运行器、装载器与夹具

**做了什么**。`engine/include/cuav/components/evaluator.h` / `src/evaluator.cpp`：`algorithm` / M2 / E2 / `model_id eval-baseline` / 0.1.0 / 绑站；输入 `det`（必连）、`rec`（可选）、`scene1..scene8`（可选，`check_wiring` 在 scenario 模式下要求至少一路）；参数 `truth_source ∈ {scenario, manifest, none}`、`data_id`、`nfft`（须等于检测器）、`match_overlap`、`roc_points`，内部 `manifest_path / scenario_path / scenario_id / site_id`；`accepts_partial_inputs() = true`，`process()` 只攒行（检测行核对 `frame_index` 连续，断档即报错），场景帧按 `emitter_id + valid_from_s` 去重、按 `tx_on` 连续段（`tx_center_Hz` 变即切）记区间；`flush()` 把区间与场景文件 `emission.waveform` 的门控相交生成真值行（`burst` 每个导通窗一行，样点域 `period_n = ⌊period·fs + 0.5⌋` 与源同式）、判 `in_band`、调 `evaluate()`、经 `on_truth / on_evaluation` 上报；`init()` 不抽随机流。观察者加 `TruthReport / EvaluationReport` 两报告与两回调；运行器 `truth_json()`、惰性写 `truth.jsonl`、`write_metrics()` 把各评价器分节按 `node_id` 序拼成 `metrics.json`（`cuav-metrics/1`，`sites[]` + 顶层 `localization: null`，无时间戳）、终态 `task.state` 加 `truth_rows / evaluations`；装载器 `check_feature_alignment` 扩到 `Evaluator.det`（只核 `nfft`），组件构造失败的错误码由「声明了 `data_id` 即 `data_id`」收紧为「`data_id` 必填或报文点名清单」——评价器的 `data_id` 选填，缺场景路径不是数据问题。注册表 21 → 22，目录黄金基准重生成（只多 `Evaluator` 一条，既有条目、`port_types`、`port_compat` 逐字不变）；夹具 `engine/tests/diagrams/slice4_evaluate{,_scene}.json`、`engine/tests/fixtures/eval-*.json`，ctest 11 → 13。

**实测（macOS，原型阶段验证值）**。demo-01 缺省链 6 s 加评价器：`truth.jsonl` 一行 `[3.0, 6.0) uav-1 cw_beacon in_band`；`metrics.json` 一节——2929 帧 tp 1457 / fp 1 / **fn 7** / tn 1464，pd 0.9952、pfa 0.00068、f1 0.9973；一段真值匹配、发现时延 **0.014656 s**——7 帧漏检与 14.7 ms 时延正是源按块起点门控 `tx_on`（第 23 块 3.014656 s 才开）的后果，与规划阶段的预测逐字相符；识别评价 1 行、准确率 1.0（持续到结束的单音段的识别行**到了**，步骤 0 的实地验收）；ROC 32 点、工作点与帧级逐位相同；`evaluate.py` 逐值一致。`none` 模式：无 `truth.jsonl`、分节 `not_applicable`、7812 帧 pfa 0.744（单音段全算虚警，没有真值就是这样）、逐值一致。组件级单测（`test_evaluator.cpp`，5 例）：`tx_on` 连续段成区间、`tx_off` 断开、中心频率变化切段、频段外只标记、不在场景里的源记降级、突发按门控切成导通窗、manifest 两类、配置约束与映射表、接不接评价器噪声源帧数相同。既有产品 20 个文件仍逐字节相同。引擎 249 项 doctest + 13 项 ctest 全绿。

### 2026-09-14 · C-5 第三步：跨层一致性算例 ① 的典型链路实例（`tests/regression/crosslayer_pd_chain.py`）

**做了什么**。场景 `tests/regression/scenarios/crosslayer-pd.scenario.json`（参数在跑之前按解析预测冻结并记 `trace.notes`：demo-01 的站、定点正东 2.9 km 的 `burst` 源，周期 0.512 s = 250 帧、导通 50 帧、边沿在帧边界、无开关事件，−16 dBm 使带内信噪比 ≈ −9 dB、解析 Pd ≈ 0.75）；脚本从前端内置缺省链路改出框图（换场景、40 s、ADC 16 bit、加评价器）→ `cuav_run --run` → 读 `links.jsonl` 路损、`detections.index.json` 的 η / nfft / 频段 → 链路预算 → D-026 两式 → 对 `metrics.json` 帧级 Pd，`|Δ| ≤ 0.05` 为过，并顺带跑 `evaluate.py` 对拍；接进 `scripts/build-all.sh`（无 uv 或未构建即明说跳过）。用突发不用连续单音：连续信号从 t=0 起会被滑动删截的环吸收，Pd≈0.7 时 30% 漏检帧持续进环、估计上漂，与「噪声已知」的解析式不可比。

**实测（macOS，原型阶段验证值，首跑即过、未调参）**。路损 109.457 dB、接收电平 −100.46 dBm、带内噪声 −91.47 dBm（M 921 bin）、信噪比 −8.99 dB；η 1.104928（与 `threshold_for_pfa` 反解逐位相同）；解析确定型 Pd **0.7164**、随机型 0.7151；蒙特卡洛 19531 帧、真值帧 3931、tp 2762 / fn 1169 / fp 23 / tn 15577，Pd **0.7026**、Pfa 0.00147、F1 0.8225；**|Δ| = 0.0138**，偏低方向与预测一致（漏检导通帧进环 → 噪声估计略偏高）。突发级：79 段真值检出 78（0.9873）、虚警段 22、发现时延均值 1.1 ms。识别准确率 0.533（72 对 / 27 判成 cw_beacon / 36 未知）只作信息——−9 dB 下特征本来就差，不是本算例判据。`evaluate.py` 逐值一致。`tests/regression/README.md` §二 ① 销项并加 §五。墙钟 4.0 s。

### 2026-09-14 · C-5 第四步：服务端——`truth` 抽稀键与过滤、`task.json.metrics_summary`

`server/src/products/routes.ts`：`truth` 的抽稀键 = `node_id:emitter_id`（多站下每站一个 `eval__<site>` 各写自己看到的真值；manifest 模式 `emitter_id` 为 null，键退化成节点），过滤 `site_id / node_id / emitter_id / label`。`store.ts` 加 `MetricsSummaryEntry` 与 `TaskRecord.metrics_summary?`；`manager.ts` 的 `finish()` 在终态为 `finished` 时接在写链上读 `metrics.json`（`readMetricsSummary()`：缺文件不写键、坏文件记一条 warning、都不动终态；数值只认有限数，`null` 照抄）再 `persist`。假引擎加 `metrics` / `metricsbad` 两模式（终态事件之前写文件，与真引擎同序）。测试：`routes.test.ts` 的 `metrics` 夹具改成 `sites[]` 形状、`truth` 段改为三条曲线的抽稀与两种过滤；`manager.test.ts` 新增摘要三支。`docs/api-versions.md` §3.1d 两行与 §4.1 终态字段（`truth_rows / evaluations`）。服务 127 项全绿。

### 2026-09-14 · C-5 第五步：框图页评价子环节、结果页评价卡、夹具再生成、e2e

**框图页**。`SlotId` 加 `eval`（`group: 'det'`，检测识别评价卡片里的第三个子环节，`bind: site`），`DERIVED_PARAMS.eval = [truth_source, nfft, data_id]`——真值来源随信号源模式（全合成 / 混合 → `scenario`，回放 → `manifest`）、`nfft` 随检测器、`data_id` 随信号源（回放）或背景片段（混合），三项只读派生、用户只改匹配重叠比与 ROC 点数；`compile()` 在 det 块里追加 `eval__<site>`：`det.out → eval.det`、识别器编译了才 `rec.out → eval.rec`、有场景时 `scn.link:<源> → eval.scene{i}` 按源扇入；`parseChain` 无需改（派生键随下次 compile 覆盖，与 feat 同法）；`UNAVAILABLE_REASON` 删 `Evaluator`；面板派生说明改成按槽位与参数的小映射。**结果页最小集**：`metricsStore`（只在终态取；409 等下一轮）+ 右栏「评价」卡（每节一行：站 · Pd · Pfa · F1 · 突发 · 识别准确率，`null` 写「—」，只摆数，D-039）+ 探针 `app.results.metrics`；`getMetrics()` 改四态并给 `MetricsSection` 类型；`TaskRecord.metrics_summary`。**再生成**：`_gen.ts` 加模式参数，`DEFAULT_CHAIN_TEXT` 10 → 11 节点 / 12 → 15 连线；两份 3×3 夹具经真实 `parseChain → compile` 路径重生成，55 / 93 → **58 / 108**、58 / 105 → **61 / 120**（与规划阶段的预测一致），既有节点逐字不变、`cuav_run --validate` 过。

**验证（macOS，原型阶段验证值）**。web 216 项（既有八条按节点数 / 可选口 / 槽位数改期望；新增 eval 槽位一条、metricsStore 一条；`catalog.test` 内部参数 28 → 32、可绑定 8 → 9）。六套 e2e 在 8090 端口的私有服务上按顺序跑（scene → slice1 → slice2 → slice4 → slice6 → slice8：slice4 的「缺省是 demo-01」依赖最近一个任务是 demo-01，乱序会踩到共享 `data/runs` 里别的场景——第一遍就是这样红了三条）：scene 14、slice1 39、slice2 29、**slice4 108 → 117**（三个子环节、评价器派生行、truth 一行、metrics 一节、帧级 / 突发级 / 识别断言、task.json 摘要逐位相等、右栏评价卡与探针一致、回放模式 manifest 真值 Pfa 为 null）、**slice6 67 → 71**（三站各一评价器、`uav-2` 一段 `[0, 20)`、`uav-3` 40 个导通窗、三节分节、摘要三行）、slice8 42，合计 **312 项**全绿。slice4 里 demo-01 缺省链的读数：Pd 0.9952、Pfa 0.00068、时延 14.7 ms、识别准确率 1。


### 2026-09-14 · C-5 第六步：回填——模型卡、文档、10 §11、06、CLAUDE.md D-067

`models/evaluation/README.md`（新：定位与溯源声明、绑站与部分输入、三种真值来源、两张映射表、指标口径、六条已知边界、验证数字）；`data/iq/measured/README.md` 改正「目前为空」并写清单类别 → `signal_role` 表；`docs/display-products.md` §1 树加两文件、§1.1 加 `metrics_summary`、§3.4 改八种并补三种抽稀键与过滤、新 §5.4 真值行与 §5.5 `metrics.json`；`docs/component-catalog.md` §6 加 C-5 表与装载器错误码收紧、§7 销 `Evaluator`；`docs/chain-view-guide.md` §5.6 改写为一张卡四个环节与评价卡读数口径；`models/README.md` 模型卡清单、`models/detection/README.md` §8 销 C-4 / C-5；`algos/README.md` 表补六个脚本；`tests/regression/README.md` §二 ① 销项、§五 新增、夹具计数；10 报告 §11 加「C-5 实施时的修正」十一行；06 §0.3 追加一行、§9G C-5 `[x]`、§9H L-9 注顶层预留；CLAUDE.md D-067、里程碑 C 行、「环境与命令」加评价一条。提交：5d808f3（步骤 0）、827be84（步骤 1）、91f4227（步骤 2）、ba3176b（步骤 3）、5363430（步骤 4）、038f189（步骤 5）、本步一个提交。
**`build-all.sh` 顺带抓到一处自食其果**：跨层算例脚本从前端缺省链路改出框图，而步骤 5 把评价器加进了缺省链路，脚本再追加一个即「节点 id 重复：eval」。改为「已有就不追加」并把 `link:<源>` 端口按本算例的源改写；两侧数字逐位不变（0.7026 对 0.7164，差 0.0138）。全量 `build-all.sh`（web、server、engine 249 + 13、geo、跨层算例 ①、legacy 常数守卫、路径与 ASCII 检查）全过。
**下一步 C-9 结果页两页签**（检测识别页签的时间线三行 + 评价页签的指标卡、混淆矩阵热表、ROC 曲线，`?dev=1` 叠解析预测；V-8 标签条同批），然后切片 ⑨（V-4 方位图、V-5 精度场）；C-8 `demo-02` 与 G-6 待排。

---

## 2026-09-15 C-9：结果页两个页签（检测识别时间线 + 评价），D-068

**背景**。C-5（D-067）收口之后，检测 / 特征 / 识别 / 真值 / 评价五种产物全部有生产者、有端点、有前端小 store，
但界面上只兑现了最小集：「检测识别」页签是摘要行加一张突发表，「评价」还不是页签而是右栏三行读数，
`metrics.json` 里的混淆矩阵、32 个 ROC 点、per_class、发现时延一个都没用上，
`truth.jsonl` 有端点但前端从未封装过，所以设计里说了快一年的「真值那一行」一次都没画出来。
C-9 就是把这些已经在盘上的数据摆出来。用户 2026-09-14 说「按路线图执行 C-9」，
并在计划阶段拍板三条：`?dev=1` 的解析叠加取**曲线族**而不是一条；时间线三行放在页签中栏顶部、自带轴，
全宽时间轴条不动；V-8（瀑布检测框按识别标签着色）随本步做掉。

**做了什么**（八步，实施顺序即验证顺序）：

0. **引擎只动一处**：`DetectionSummary` 加 `m_bins`（检测频段内的 bin 数 M），落进 `detections.index.json`。
   解析检出率与门限反解都要 M；读端按 `f_lo / f_hi / nfft / fs` 自己复算会在边界上差一个 bin，
   而 `metrics.json` 的 `detector` 块没有 `center_Hz`，根本算不出。
   **实测证明产品没变**：拿改动后的 `cuav_run` 重跑 demo-03 那次三站运行（同种子、同框图），
   12 个 `.f32` 与 12 份观测点索引**逐字节相同**，四个 JSONL 相同，`metrics.json` 只差 `task_id`，
   `detections.index.json` 只多出三行 `"m_bins": 921`。
1. `getTruth()` 与 `web/src/results/truthStore.ts`（与既有三个 store 同一范式）。服务端一行没改。
2. `web/src/results/analytic.ts`：把 `algos/reference/energy_detector.py` 的正则化上不完全伽马 `Q(a,x)`
   与 D-026 两式移植到 TypeScript（JavaScript 没有 `lgamma`，用 Lanczos g=7 补上）。
   **这是第二份实现，所以必须对第一份守基准**：新生成器 `algos/reference/gen_analytic_golden.py`
   出 `tests/golden/analytic-pd.json`，135 个取值点（`a` 取到 2000、跨级数支与连分式支的切换点），
   前端单测逐点比 rel ≤ 1e-9，首跑即过（铁律 10）。
3. `timeline3.ts`（纯函数：三种行 → 三行条带）+ `DetectionTimeline.tsx`（指针交互照抄 `shell/Timeline.tsx`）。
4. `DetectionView.tsx`：时间线 + 检测列表（新增「突发 / 帧」粒度切换）+ 识别列表。
   C-3 / C-4 的 `data-det-*` 钩子一个没改名。
5. `EvaluationView.tsx` + `ConfusionMatrix.tsx` + `RocPlot.tsx` + 页签接线（`ResultsTab` 只增一个值）。
6. V-8：`renderer.ts` 的瀑布检测框改三道描边并写标签名。
7. e2e：`slice4-smoke` 117 → 130 项，`slice6-smoke` 71 → 75 项。
8. 回填：10 §11、09 §7.1 与修订记录、06 §0.3 / §9G / §9J、`docs/{display-products, display-route, api-versions}.md`、
   `models/evaluation/README.md`、CLAUDE.md D-068 与三处里程碑。

**三处实测纠正了计划里的假设**（都是先按计划写出来、在浏览器里看见不对才改的）：

1. **解析曲线族的档位不能固定**。计划写 `s = −12…+6 dB` 七条。画出来整族贴在 `Pd = 1` 上，一条也读不出来。
   缘由是 H0 下 Λ 的均值是 1、标准差是 `1/√M`，所以「能不能检出」看的是 `s·√M` 而不是 `s`；
   M = 921 时 −12 dB 已经远在检测边沿之上。改为随 M 走：中心取 `−5·log10(M)`（即 `s ≈ 1/√M` 的那一档）。
   **改完仍不对**：以中心对称的 −6…+4 dB 这一族，最高一档在 pfa = 1e-3 上的 Pd 只有 0.235。
   在 M = 5 / 32 / 921 / 2000 四档上逐个量了一遍，转换区实际落在 `中心 + (−2…+8) dB`，才定下来。
   单测把四档 M 的「最低一档 < 0.05、最高一档 > 0.65」钉住。
2. **ROC 横轴必须取对数**。本系统的目标虚警率是 1e-3，线性轴上实测曲线与整族解析曲线全挤在左缘一条竖线里。
   改成 1e-6…1 的对数轴之后两者都读得出来。`pfa = 0` 的点（门限扫到最高、一个虚警都没有）贴左缘，轴标写明下限。
3. **瀑布上任何单一颜色都不可能对整条 viridis 达到 3:1**。背景从 `#440154` 深蓝到 `#fde725` 亮黄整条都有。
   所以判据分两条：边界的可见性交给外白 4 px 与内墨 1 px 这一对（十个 viridis 锚点上 `max(白, 墨)` 最低 **3.33:1**，
   过 3:1），中间 2 px 的类别色只管区分类别、量的是标签**文字**对它自己那块浅色底衬的对比度
   （按 WCAG 文字的 4.5:1，十个锚点最低 **5.37:1**，三个偏浅的色值为此压深了一档）。实测值记 `docs/display-route.md` §4.1。

**两处照实说而不是编数**（铁律 15）：

- **`pfa` 整列是 `null` 的那一支**。真值覆盖全程时 `tn + fp = 0`，`metrics.json` 里 32 个 ROC 点的 `pfa` 全是 `null`
  ——demo-03 三站持续发射、回放清单的整片真值都是这样。界面只画轴与一行「Pfa —（tn + fp = 0）」，指标卡写「—」。
  在 demo-03 上实测走到了这一支（`data-roc-points` 为 0、`data-roc-note` 有文字），`slice6-smoke` 把它钉住。
- **帧粒度的表**只渲染以时间游标为中心的 400 行，页脚写「共 9765 行 · 第 9366–9765 行」。
  几万个 `<tr>` 滚不动也读不完，但不能假装列全了。

**另外三条实现层的事实**：

- **焦点站是一个概念不是两处状态**。时间线一次只画一个站（K 站 × 3 行一屏放不下），评价页签一次看一节，
  两处都取「检测页签那个站下拉的值，没选就取第一个站」。多加一个站选择器就是多一个入口（D-057、D-062）。
- **`fmtDelay` 恒用 µs** 是给到达时间差的（那是 ns–µs 量级）。发现时延是 ms 量级，
  写成 `14656.0 µs` 读不出来，新加 `fmtDuration()` 按量级选单位。
- **`per_class` 比 `labels` 少一项**（没有 `unknown`），且会出现 `support = 0` 配 `precision = 0.0`
  （真值里没有这一类、却被误判成这一类）。demo-03 上就有：87 段 `cw_beacon` 真值里 47 段判成了 `telemetry_burst`。
  混淆矩阵按 `labels` 的长度画（库外标签会让它从 5×5 变 6×6），per_class 按它自己的表画，不按下标对齐。

**e2e 踩到的两件**：① `?dev=1` 是查询参数不是 hash，重新导航会重新采用「最近一个任务」，
未必还是前面那一个（slice4 一路跑了好几次），断言得按当前采用的任务取索引；
② 多站下焦点站来自 `detections.index.json` 里的站号，只等真值与指标就绪会拿到 `site = null`，
站下拉也还没渲染（它要 `sites.length > 1`），等待条件必须带上 `detections.status === 'final'`。

**量化结果**（macOS，原型阶段验证值，D-028）。缺省链 demo-01 跑 20 s：Pd 0.99916、Pfa 0.00068、F1 0.99952、
发现时延 14.7 ms（`fn = 7` 仍是源按块起点门控 `tx_on` 的那 7 帧，D-067 已记）、识别 1 段准确率 1、`m_bins = 921`。
引擎 249 项 doctest + 13 项 ctest、服务 127 项、web **228** 项、六套 e2e 14 + 39 + 29 + **130** + **75** + 42 = **329** 项全绿。

**留下的**：L-9 定位评价（`metrics.json` 顶层 `localization` 仍是 `null`）；U-4 的任务页签与数据中心；
V-8 后半的频段标签条（宽带多段，随 ④c）；09 §7.4 的观测点对比模式。下一步按路线图是 C-8（`demo-02` 宽带场景）
与 G-6（活动时间线），然后切片 ⑨（V-4 站点方位图、V-5 精度场）。

## 2026-09-15 C-8 宽带场景 `demo-02` 与 G-6 活动时间线施加（D-069）

用户指示「开工 C-8」。备忘录 §9G 的 C-8 行把两件事绑在一起：宽带场景 `demo-02` 与 G-6
活动时间线施加。开工前的调研得到一条与预期不同的事实：**G-6 不是从零做**——`hop` 与
`tx_on` 从 JSON 解析、`geo` 状态机、施加到 IQ、写进参数帧、特征的 `hop_from_prev_Hz`、
真值的 `has_hop → rc_hopping`，整条通路早已在仓库里，缺的只有五件：粒度是块不是样点、
跳频点不过铁律 4 的闸、`noise` 不带限也不搬移频率、没有任何场景带 `hop`、编辑器写不出它。

计划模式下拍板四条：`noise` 的带限在本期做（不推到 C-10）；`demo-02` 的两个源在时间上
先错开、末尾叠一段；界面缺省框图**不换**成 demo-02；跳频进场景编辑器。

### 做了什么（十一个提交）

1. **`geo::ActivitySchedule`（样点域）与 `EmitterRuntime`（帧域与航迹域）并存**。后者一个字符
   没动，因此两份航迹黄金基准逐点逐位不变。两域的一致性靠一条稠密对拍单测钉住（遍历
   500000 个样点，例外只许落在事件边界且每处不超过一个样点），不靠代码共用。
   `sample_at(t_s, fs)` 是全仓唯一的时刻 → 样点取整口径，源与评价器都调它，两边不可能各 round 各的。
2. **铁律 4 的闸覆盖跳频点**，三处同一口径：`Scenario::cross_check`（基频那句报文逐字不动）、
   `SceneEmitterSource::configure`（报文给出算得出来的 `fs_Hz` 建议）、前端 `plan.ts`
   （`FreqPlan` 加 `df_max`，`f_tx` 仍是 `emission.center_Hz` 不变；无 hop 时文案逐字不变）。
3. **`SceneEmitterSource::process` 按子段推进**。内层循环体逐字不变；相位累加器跨子段不重置
   （DDS 型相位连续跳频）；「无 hop 且全程 tx_on」时全流只有一个子段、`dphi` 的表达式与操作数
   与改动前完全一致，所以无活动的场景逐位不变——跨层算例 ① 的 Pd 0.7026 与差 0.0138 一个数没动。
4. **基准变更单独一步收口**（铁律 10）。见下。
5. **`Evaluator` 的真值改「帧定范围、schedule 定边界与频点」**。`take_frame()` 一行未改，
   但先把帧域按 `tx_center_Hz` 切开的相邻段并回去（跳频快于帧率时那些边界是混叠抽样），
   再按 schedule 细分。既有三条用例的期望值一个没改，只换了夹具。
6. **`noise` 加带限与频率搬移**。4 阶巴特沃斯、状态跨块保持、按冲激响应的 `Σ|h|²` 归一、
   `init()` 里推掉冷启动瞬态；不加任何新用户参数。Python 参考与黄金基准
   `engine/tests/golden/scene_noise_bandlimit.json`。
7. **`demo-02`** 与航迹黄金基准、引擎对拍（517 项断言）。
8. **demo-02 跑通**、模型卡 `models/radiator/README.md`。
9. **场景编辑器支持 hop**（事件按钮 + 跳频点与停留两项输入）。
10. **`slice9-smoke.mjs` 18 项**。
11. 回填。

### 实测（macOS，原型阶段验证值，D-028）

| 项 | 实测 | 判据 |
|---|---|---|
| 跳频突发起点对活动时间线 | **0.0 ns** | 一帧 102.4 µs（06 §9C G-6） |
| demo-02 识别准确率 | **1.0**（152 段，`rc_hopping` 180 行） | ≥ 0.9（10 报告 §8） |
| demo-02 突发级 | 真值 252 段全部匹配，帧级 Pd 1.0 | — |
| demo-02 墙钟 | 12 s（6 s / 10 MS/s / 双源，实时因子 0.5） | — |
| 带限对 Python 参考 | 系数与功率增益 rel ≤ 1e-9、4096 样点 rel ≤ 1e-6 | 黄金基准 |
| 带限 −3 dB 点 | 差 < 0.01 dB；六个探测频率对解析式差 < 0.02 dB | — |
| 带限归一后功率 | 1e6 个样点平均功率差 < 0.5% | 3σ |
| ADC 削顶 | 0 个样点 | 低于降级阈值 |

### 两处「先怀疑自己的判据」

- **带限的降级判据我一开始定错了**。原定「奈奎斯特处带边衰减 < 40 dB 即降级」，它把 demo-02 的
  图传判成降级。带边衰减不是该看的量：实测带边 39 dB 抑制对应的**绕折功率只有 2.4e-5**。
  改成对绕折功率做积分（4096 格中点法），超过 1e-3 才降级。**先查判据、再改场景**，
  没有为了让场景通过去挪频点。
- **「`metrics_summary` 恒为 null」是个假故障**。排查半天才发现上一次会话遗留的服务进程仍占着
  8080，新起的服务因 `EADDRINUSE` 静默退出，于是端到端打到的是旧二进制。
  **起服务前先 `lsof -ti tcp:8080 | xargs -r kill`。**

### 基准变更清单（铁律 10）

| 项 | 旧 | 新 | 根因 |
|---|---|---|---|
| demo-01 缺省链 `fn` | 7 | **0** | 源不再晚 14.656 ms 开（块 65536 @ 500 kS/s） |
| 同 Pd / F1 | 0.99916 / 0.99952 | **1.0** / 0.99988 | 同上 |
| 同 Pfa | 0.00068 | 0.001365 | truth-on 区间前移 7 帧，一帧由 tn 变 fp |
| 同 发现时延 | 14.7 ms | **0** | 同上 |
| demo-03 `uav-2` 支路产品字节 | `tx_off/on` 落 30.0155 / 40.0097 s | 精确 30.0 / 40.0 s | 同上 |
| 组件目录黄金基准 | — | 重生成一次 | **差异只有 `SceneEmitterSource` 的 `description` 一行文字**；`ports` / `params` / `port_compat` 逐字未变，对拍判据零变化 |

逐位不变并当场核过：两份航迹黄金基准、`DEFAULT_CHAIN_TEXT`、两份 3×3 夹具、
`energy_detector*.json` / `features.json` / `recognition.json` / `metrics.json` / `product-window.json` /
`analytic-pd.json` / `propagation.json` / 两份定位基准、跨层算例 ① 的 0.0138。

### 三条照实说的边界（写进 `models/radiator/README.md`，D-039）

1. **重叠段里单频段能量检测分不开两个源**。demo-02 的 5–6 s 两者同时发，检测器把整段并成
   **一个**突发（实测段号 181 跨 `[5.0, 6.0]`），100 个跳频突发被吞掉。场景**有意**留出这一段
   来展示它，不是把它藏起来。
2. **「突发级 252/252 全匹配」与「识别准确率 1.0」说的不是同一件事**。后者的分母只含产出了
   识别行并匹配上真值的段。准确率 1.0 的含义是「产出的每一行识别都对」，不是「把它们都分开了」。
3. **第一个跳频突发能判对是场景顺序带来的**。图传先发，于是跳频的第一个突发拿图传那一段
   当「上一段」；换一个跳频先发的场景，第一段仍会落 `unknown_ambiguous`。

### 一条新的跑序耦合（给下一次）

`slice9` 跑完会把「最近一个任务」留成 demo-02，而 `slice4` 断言「缺省绑定 demo-01」，
所以 **`slice9` 必须放在最后**；`slice4` 中途保存的框图会把传播档位的残留写进
`data/diagrams/chain-default.diagram.json` 的 `inactive_slots`，`slice6` 启动时载入它会让
「传播档位自洽」检查报冲突——`slice4` 异常中断后要先删掉这个文件再跑后面的。

## 2026-09-16 M-2：DDC 落地，并放弃 MATLAB Coder 路线（D-070）

**十四个提交**。做出来的是 `DDC` 组件（04 §7.7 的五步：数控振荡、复混频、抗混叠低通、整数抽取、更新元数据），
兑现 04 §15.2 标准算例第 7 项，并把 08 报告 §8 先行规定的四条多速率口径第一次落到代码上。

**最大的一件事不是写代码，是发现路线走不通。** 规划期实跑了一遍 `codegen -config coder.config('lib')`：
产物本身干净（93 行 C、无绝对路径、无动态内存、结构体类型清楚），但**每个 `.c` / `.h` 首部都盖着**
「Academic License — … Not for government, commercial, or other organizational use.」——
开发机的 MATLAB 是学术许可，而 08 §13 要求这些产物入库并进交付包。这与 D-036 / M-0 记的
「Coder 产物随交付不受许可条款限制」直接冲突。用户据此拍板：**放弃 Coder，DSP 件改手写 C++**（D-070）。
顺带一条更细的：用学术许可的 MATLAB 为交付件**设计**滤波器也在该许可的用途范围外，即使输出只是一串数，
所以系数表的主设计改走 Python（scipy，BSD），MATLAB 退居可选校验。

**成果与实测数字**（macOS，原型阶段验证值）：

- 与 Python 独立参考四个算例（D = 1 / 2 / 4 / 20）**最坏相对差 0**——float32 输出逐位相同，
  没有落到 1e-6 的容差上。功臣是「NCO 相位以圈计不以弧度计」那条契约：`2π` 不精确、每次回卷注入舍入，
  而 `[1,2)` 减 `1.0` 是精确的。
- 阻带抑制 **91.1 dB**（设计线 60）；冲激对齐偏差 **0 样点**；块长 1 / 7 / 997 / 整块逐位相同；`reset()` 后逐位复现。
- MATLAB `firpm` 对冻结表 **1.5e-14**，两家 Parks-McClellan 实现吻合到机器精度。
- `chain-demo-02-ddc` 六秒双源：60000000 入 @ 10 MS/s → **29999973 出 @ 5 MS/s**（合闭式），
  中心 2441 → 2438.5 MHz；S4 谱上图传 −74.93 dBm、**通带内那一跳 −93.75 dBm**（底噪 −147 dBm）、另四跳进阻带。
- **零基准变更**：产品 / 航迹 / 检测 / 评价基准与 `DEFAULT_CHAIN_TEXT`、两份 3×3 夹具全部逐字节不变，
  跨层算例 ① 仍是 0.0138。唯一变的是组件目录 22 → 23（逐条核对只有这一条新增）。

**三处实测发现，每一处都改了做法**：

1. **系数带边差了一个 2**。第一版把「归一化到奈奎斯特」与「周/样点」混起来，每个滤波器窄一半
   （D = 2 的通带边 1.0 MHz 而不是 2.0 MHz），白白滤掉半个输出带。**设计脚本的自检查不出来**——
   它是拿自己那套错的带边去量阻带的，只能确认内部自洽。是画出 S4 的谱、看见本该落在通带里的
   跳频点读 −157 dBm 才发现的。改对后抽头数减半，回到 ≈ 27·D。
   教训直白：自检只能证明内部一致，**证明「对不对」得拿物理量去看**。
2. **跳频真值边界的取整落差随跳数累积**。源端按宽带 fs 建活动时间线、评价器按窄带 fs 重建，
   我原以为落差恒定在一个样点内；实际跳频边界是 `start_n + k·dwell_n`，`dwell_n` 的取整误差乘 k。
   实测 D = 20、`dwell` 12.3456 ms 时每跳 400 ns、8 跳 2500 ns，已超过一个窄带样点。
   改成按累积上界断言，并报出「要累积满一个检测帧需 1365 / 1638 / 1950 跳」。
   demo-02 的 10 ms `dwell` 在 5 MS/s 下整除，本期夹具落差恒为零——正因为如此才更要把一般情形测出来。
3. **原写的「块长与抽取比不相容」护栏不可达**。抽取相位每块减少一个块长，至多 `ceil(gd/块长)` 轮
   必出第一个输出。删掉不留死代码（同 D-057），换成钉住这个上界的单测——它才是「返回 `Idle` 而不是
   空块」的前提。

另两处早就错、被 DDC 顺带照出来的前端缺陷：`plan.ts` 判 DDC 是否参与只看 `bypass` 而 `compile.ts` 看
`slotState`（于是「组件不在目录里但填过 decim」会算出不存在的 `fs_s4` 并据此收窄检测器频段）；
`detectionStore.ts` 取 `index.nodes` 第一个节点的 `dt_s` 给所有站用。两处都修了，各配一条会红的回归用例。

全量：引擎 **270** 项 doctest + **14** 项 ctest、web **232**、server 127、
**七套 e2e 14 + 39 + 29 + 132 + 75 + 42 + 18 = 349 项**全绿；`build-all.sh` 全过。

## 2026-09-16（补记）M-2 的许可前提被澄清，Coder 路线恢复并交给 M-3

同日稍晚，用户澄清：**本项目属学术用途，MATLAB 学术版许可没有问题**。这推翻了我当天做判断时
所依据的前提——我看到 codegen 产物带「不得用于政府 / 商业 / 组织用途」，就据此认为它与
08 报告 §13「产物入库并进交付包」冲突，进而建议放弃 Coder。前提不成立，结论要跟着改。

**改了什么**：

| 项 | 改前 | 改后 |
|---|---|---|
| D-036 的 Coder 路线 | 划掉、作废 | **恢复有效**，首个使用者是 M-3 |
| P1-8「走通一条 Coder 链路」 | 改 OPEN、待合规许可 | **由 M-3 兑现** |
| M-3 信道化与接收滤波 | 同样手写 C++ | **走 MATLAB Coder** |
| 08 §13 Coder 封装规范 | 整节标「本期未启用」 | 有效，首个使用者改 M-3 |
| `matlab/coder/` | 撤掉 | 随 M-3 重建 |

**没改什么**：`DDC` 保持手写 C++，代码一行没动。这一条现在的理由是工程取舍而不是许可——
08 §13 规定的封装层五项职责本来就得手写，Coder 只出算法核，而 DDC 的算法核（数控振荡 +
抽取型 FIR）约 60 行，省不下多少；而且手写版已经跑出与独立 Python 参考**逐位相同**的结果，
回退没有收益只有风险。多相 FFT 滤波器组与接收滤波才是「重写等于重新承担一次验证成本」
真正成立的地方。

系数表继续走 Python（scipy）主设计、MATLAB `firpm` 作独立校验。这一条**与许可无关**：
目的是让表在没有 MATLAB 的机器上也能复算，同时两家等波纹实现互为佐证（实测吻合 1.5e-14）。

**留了一条待核**（写进 D-070 ③、`matlab/README.md` 与 08 §13）：产物文件头会带学术许可条款，
而本项目文档里写着交付包、甲方数据与 Windows 单机一体化包。若 Coder 产物将来随交付物出去，
需按实际用途另行核实。这不是代码能决定的事，记着备查。

CLAUDE.md 的 D-070 **当天改写过一次，改写前后的判断都留在条目里**，没有把第一版悄悄抹掉——
这一条本身就是「前提错了要看得出来」的例子。代码与测试零改动，全量仍然全绿。

---

## 2026-09-16 M-3：信道化与接收滤波落地，首条 MATLAB Coder 链路走通（D-071）

用户指示「进行下一步 M-3」。规划阶段拍板三条：范围只做**信道化 + 接收滤波**两件
（多级抽取 `decim > 20` 推迟）；`chan` 槽位本步放开但**缺省仍旁路**，另加演示夹具；
Coder **生成 C**，编成独立静态库链进引擎。十四个提交，分步做完。

### 这一步真正交付的东西

**首条 Coder 链路整条走通**，08 报告 §13 定的三层分工第一次被真正执行：
`matlab/ref/*.m`（人工编写，带 `%#codegen`）→ `codegen` → `models/{channelizer,receiver}/coder/*.{c,h}`
（Coder 生成，25 + 11 个文件，**入库**）→ `engine/src/{channelizer,rx_filter}.cpp`（人工编写的封装层）。
产物编成独立静态库 `cuav_coder`，**单独设警告级别**——生成的代码不该按 `-Wall -Wextra` 去挑。
`engine/src/catalog.cpp:65-69` 那道「`implementation = coder` 则 `source_ref` 必填」的闸，
从 B-1 建注册表那天起就写在那里，这才第一次被真正用上。P1-8「走通一条 Coder 链路」由此销项。

两件组件：`Channelizer`（多相 FFT 滤波器组，临界抽取，本期只输出一路）与
`RxFilter`（不抽取的线性相位 FIR，群时延照扣）。组件目录 23 → 25。

### 一个必须先解开的死结：抽头数

08 §8 口径二要群时延是**整数个输入样点**（`gd = (N−1)/2`，故 N 必须奇数），
而多相分解要 N 是 **M 的倍数**（M 是偶数）。两者直接冲突。

解法是 **`N = M·T + 1` 且 T 取偶数**：N 是奇数（口径二满足），`N − 1 = M·T` 是 M 的倍数
（多相满足），并且额外带来一件好事——`gd = M·T/2` 是 **M 的整数倍**，于是多相 FFT 形式里
那个逐信道常数相位 `exp(−j2πk·gd/M)` **恒为 1**，FFT 组的输出不需要任何修正。
写代码之前先用 numpy 实算验过：`max|exp(−j2πk·gd/M) − 1| = 6.9e-15`，且多相 + `M·ifft`
与直接式的最大相对差 1.1e-12。T = 28 对六档（M = 2 / 4 / 8 / 16 / 32 / 64）通用，
每支路恒 29 个抽头——**每输出样点每子信道 29 次乘加，与 M 无关**，与 M-2 的「恒为 ≈27」同一句话。

设计脚本 `scripts/design_pfb_fir.py` 因此多一条本表独有的断言：`gd % M == 0`。

### 判据分两个尺度，这是本步最重要的一条口径

06 §9D 写的验收判据是「Coder 组件与 MATLAB 黄金向量 rel ≤ 1e-9」。落地时发现这句话必须拆开：

- **算法核尺度**：double 进 double 出，判据就是 **1e-9**。实测 Coder 内核对 MATLAB **1.38e-16**、
  MATLAB 对 Python 参考 **5.70e-13**（信道化）；接收滤波三方都在 **1e-15** 以内。
- **组件尺度**：输出存 `complex64`（`cuav::Complex` 是 `std::complex<float>`，eps 就是 1.2e-7），
  判据只能是 **1e-6**。实测两件都与 Python 参考**逐位相同**。

**拿组件输出去套 1e-9 是套不上的，那不是算法不准，是存储精度的下限**，不是放宽铁律 10。
黄金文件里两个尺度各写各的容差与理由（`tolerance.note`），单测两处断言各写各的消息。

**逐位相同这一件不作承诺**（与 M-2 的 DDC 不同）：MATLAB 的 `fft` 走 FFTW、Coder 生成的是
自带的基 2 实现、numpy 走 pocketfft，三家的蝶形次序与旋转因子求值各不相同。组件尺度这次恰好
逐位相同，那是 float32 舍入把 5.7e-13 吃掉了，不是承诺。

### 最贵的一课：共享输入必须共享比特，不能共享公式

第 5 步做三方对拍时，某一条算例的相对差是 **1.5e-9**——刚好越过判据。查了很久，根因**不在产品里，
在我自己搭的测试脚手架里**：两侧都按同一个闭式 `sin(0.00021n² − 0.07n + 0.9)` 各算一遍输入，
n = 4026 处相位约 3123 rad，clang `-O2` 把多项式收缩成 FMA，两侧算出
3122.9019600000001446 与 3122.9019599999996899（差 4.5e-13）；这两个相位的 `sin` 恰好跨了 float32
的舍入边界，转成 float32 后**差一个完整的 ulp（1.49e-8）**；这一个样点经滤波器 3823 倍的相消放大，
在输出上就是 1.5e-9。232 个样点里只有这一个不同。

拿 `Fraction` 做精确有理数求和验过：Python 一侧离真值 2.7e-18，C 一侧离 1.53e-9 ——
**错的是脚手架不是产品**。处置不是放宽判据，而是改掉共享方式：黄金文件把核对窗口与输入块写成
**显式数据**，三方谁也不再各自按闭式算一遍；组件尺度的输入流则由复刻的 xoshiro256++ 生成，
并记下它 float32 字节的 sha256。改完之后同一条对拍从 1.5e-9 降到 **5.7e-13**。

### 四件 codegen 实测才知道的事（已记进 08 §13.1）

1. **`rtwtypes.h` 默认会 `#include "tmwtypes.h"`**——那是 MATLAB 安装目录里的文件，
   入库的产物在没有 MATLAB 的机器上**编不过**。解法是
   `cfg.HardwareImplementation.ProdHWDeviceType = 'Generic->Custom'` 加显式位宽，
   `rtwtypes.h` 变成自足的 169 行。**`long` 故意取 32**：于是 `int64_T = long long`，
   在 Windows 的 LLP64 与 Linux/macOS 的 LP64 上都是 64 位，三平台同一份头（铁律 16）。
2. **默认文件头带生成时间戳**，入库后每次重生成都会 diff、读起来就像一次基准变更（铁律 10）。
   解法是 `cfg.CodeTemplate = coder.MATLABCodeTemplate('cuav_banner.cgt')`——照抄默认模板、
   删掉 `%<SourceGeneratedOn>` 那一行即可，**不需要 Embedded Coder**。
   去掉之后连跑两次，产物**逐字节相同**。
3. **`FilePartitionMethod` 要取 `'MapMFileToCFile'`**：取 `'SingleFile'` 会把六个 M 的实现
   全塞进 `cuav_pfb_m2.c`（里面装着 `cuav_pfb_m64`），读的人会以为搞错了。
4. **`_initialize` / `_terminate` 在 `lib` 目标下是 API 合同的一部分**，要入库并调用。
   `'MapMFileToCFile'` 下它们是独立文件，`'SingleFile'` 下是同一个 `.c` 里的空定义。

另两处用法陷阱：`cfg.CodeTemplate` 要 `coder.MATLABCodeTemplate` **对象**、不是路径字符串；
`codegen -d outd` 的命令形式把 `outd` 当**字面目录名**，要传变量必须用函数式
`codegen('-config', cfg, name, '-args', args, '-d', outdir)`。Coder 版本要遍历 `ver` 找
`'MATLAB Coder'` 取，`ver('coder')` 返回空。

### 溯源：产物是否被动过，只能靠哈希说话

`scripts/gen_coder_provenance.py` 把来源 `.m` 的集合哈希、MATLAB 与 Coder 版本、
codegen 参数的规范化哈希、以及**每个产物文件的 sha256** 编进 `engine/src/coder_provenance.cpp`。
引擎**不在 `describe()` 时读文件**——组件不该依赖部署目录布局，与 `ddc_taps.cpp` 不在运行时读 JSON
是同一条理由。`engine/tests/test_coder_provenance.cpp` 把 36 个文件重算一遍：
「手改了生成物」与「改了 `.m` 忘了重生成」都会当场红。`source_ref` 的实际取值与三条口径
写进 `docs/component-catalog.md` §6.1——那是 M-3 欠 D-070 ② 的那个填写示例。

MATLAB 一方的黄金向量也带**防陈旧字段**：`.matlab.json` 记着生成那一刻读到的来源 `.m` 与
冻结表的 sha256，引擎单测重算一遍比对。这两份文件是**必需**的（M-1 的
`spectrum_welch.matlab.json` 是可选的，这两份不是：P1-8 的验收判据只有 MATLAB 一方能兑现）；
文件入库，所以这条守卫在没有 MATLAB 的机器上照样成立——CI 与 `build-all.sh` 照旧不调用 MATLAB。

### 解析锚点（不只看脚本自检）

M-2 踩过的坑在这里也防着：scipy 的 `remez` 用周/样点、MATLAB 的 `firpm` 用归一化到奈奎斯特，
**差一个 2**，而设计脚本的自检是拿自己那套带边去量阻带的、照样全绿。所以验收里必须有物理量的锚点。

标准算例第 8 项（宽带 IQ 到信道化 IQ）：子信道中心单音增益 **0.00 dB**、
邻道抑制 **63.64 dB**（八路里最差 62.56 dB）、冲激对齐**偏差 0 样点**（M = 2/4/8/16 各验）、
`fs_out` 与中心频率逐路合闭式、输出条数合闭式、块长无关与 `reset()` 复现逐位相同。

标准算例第 5 项（接收滤波和群时延）：通带最大偏差 **0.148 dB**、阻带最差抑制 **61.13 dB**、
**七档逐档**验冲激峰值就落在输入的那个样点上、偏差 0 样点，输出条数 = 输入条数 − 群时延。
顺带钉住一条顺序口径：`bw_rel = bw_Hz / fs_in` 要到第一块才查得了表，所以 `configure()` 之后
`group_delay()` 还是 0，跑过一块才是表里那个数。

### 演示夹具：它演示的是「把两个辐射源分开」

`tests/regression/diagrams/chain-demo-02-chan.json`：demo-02 宽带场景 10 MS/s @ 2441 MHz，
`channels = 4`、`select_channel = 2`（零频那一路）→ S5 = 2.5 MS/s @ 2441 MHz。
取零频那一路而不是图传那一路，是因为它能演示的更多：图传整个落进相邻子信道，
五个跳频点里三个落在本路通带内——S5 瀑布上 0.5–2.5 s（只有图传）是底噪、
3–4.5 s（只有跳频）是突发串。

实跑 6 s（墙钟 8.4 s）。**通带内逐带一致到 0.1 dB**：±1 MHz 带内平均功率 S3 对 S5 差
+0.07 / +0.04 / +0.10 dB（全静默 / 只有图传 / 只有跳频三段各一次）。
识别 `rc_hopping` 准确率 0.978、突发级 150/150 全匹配。

**但有三条必须照实说，不能吹**：

1. **图传那 25.6 dB 是源端定的，不是信道化定的**。它自己那 2 MHz 带内 −47.10 dBm，
   落进本路的 −72.71 dBm；而 **S3 在同一 ±1 MHz 带里读到的就是同一个数（差 0.04 dB）**——
   落进来的是图传自己 4 阶巴特沃斯带限的裙边（D-069 记过「阻带非砖墙」），
   信道化在自己的通带里一进一出没有任何额外贡献。
2. **带外那两跳的真实阻带抑制在这里量不出来**：S3 在同一时隙、同一（混叠后）频点上已经有
   −94.4 / −97.8 dBm，那是 3 ms 门控突发自己的 sinc 裙边；S5 读到的比它还低
   （差值主要是 4 倍 RBW 的 6 dB）。量到的是突发的谱不是滤波器的谱。
   **纯单音下的 63.64 dB 才是滤波器的数。**
3. **帧级 Pfa 0.52，照实记**：图传裙边把本路带内功率抬高 10.5 dB（−83.15 → −72.68 dBm），
   能量检测器照样触发，而图传的真值在带外（`truth_out_of_band = 102`，D-067 ④）不计入分母，
   于是这些帧算虚警。这是源端带限不是砖墙的真实后果，不是信道化的缺陷，**不调参去掉它**。

### 基准变更与零变化的证明

**唯一的基准变更是组件目录 23 → 25**，单独一个提交，且 diff 是**纯插入**
（109 行新增、零删除；`port_types` / `port_compat` / `engine_version` 与既有 23 条逐条未变）。

零变化的那一面，拿 M-3 之前那一版提交（`1d3da3a`）另建一次工作树、各建一次引擎、各跑一遍：
五份框图（slice1 / slice2 / 缺省链 / demo-02 / demo-02-ddc）的 **50 个产品文件逐字节相同**；
三份航迹黄金基准逐字节相同；`DEFAULT_CHAIN_TEXT` 与五份夹具重跑 `_gen.ts` 逐字节相同；
跨层一致性算例 ① 仍是 **0.0138**。

### 顺带修的五处（都是别的闸自己红出来的，不是去找的）

1. `tests/e2e/slice9-smoke.mjs` 的「频率计划全项通过」读的是 `e.dataset.checkOk`，
   而 DOM 写的是 `data-ok="1|0"`——`dataset.checkOk` 恒为 `undefined`，再比 `=== 'false'`
   恒不成立，**这条断言从来不可能失败**。slice4 与 slice6 都写对了，slice9 是唯一的例外。
   改对后它真的在看东西（实测读到 8 项检查、全通过），并加一条「至少读到一项」的守卫。
2. `scripts/check-paths.sh` 的文件名单里**没有 `*.c` 也没有 `*.m`**（铁律 17 对生成代码是盲的），
   补上；`rtwtypes.h` 那个坑正是这一类。
3. `web/src/chain/examples/_gen.ts` 的缺省链抬头只写一行，而文档里记的重跑命令是
   `_gen.ts > default.ts`——重跑一次就把入库文件里那段「为什么增益取 20 dB」的缘由静默删掉
   （铁律 15）。抬头补齐后这条命令才是幂等的。
4. `engine/tests/test_registry.cpp` 的「只给必填项即可构造」当场逮住 `RxFilter` 少一份夹具取值
   （它的 `bw_Hz` 按 `bw_Hz / fs` 查表、没有缺省）。补夹具，不改组件。
5. `ddc.h`、`design_ddc_fir.py`、`check_ddc_fir.m` 三处仍写着「手写是因为学术许可」这个
   **已被推翻的前提**——D-070 当天已改成「工程取舍，与许可无关」，但 `1d3da3a` 只改了文档没动代码。

### 对上游文档的两处修正

10 报告 §3.7 写 `select_channel` 缺省 `0`、`channels` 是「2 的幂」。落地时都改了，理由记在报告里：

- 按低→高编号，**`j = 0` 指的是带边（±fs/2 绕回）那一路**——临界抽取里它合法，但作缺省最差；
  `channels/2` 是零频那一路，与 S4 中心重合，「S5 中心 = S4 中心」肉眼可核。
- `channels` 不是「2 的幂」而是「冻结原型表里那六档」：原型是等波纹设计、抽头数受 `N = M·T+1`
  且 T 偶数约束，表外的 M 没有系数可用。

另有一条**临界抽取的物理限制**写进模型卡：相邻子信道的过渡带折进本路外侧 20%，
**可用子带是 ±0.4·fs_out 不是 ±0.5·fs_out**。要无混叠全带就得过采样滤波器组，本期不做。

### 全量

引擎 **291 项 doctest + 15 项 ctest**、web **232**、server **127**、
七套 e2e 14 + 39 + 29 + 134 + 75 + 42 + 18 = **351 项**全绿（按记录的顺序跑、一套一台服务）；
`scripts/build-all.sh` 全过。

**一件跑 e2e 时踩到的环境坑**（不是代码问题，但值得记）：slice4 单独跑时有四项失败，
读数离谱。根因是**上一节会话留下的服务还占着 8080**，新起的服务 `EADDRINUSE` 退出、
测试打到的是那台旧服务；杀掉之后仍失败，第二个根因是**页面采用了最近的任务**，
而 `data/runs/` 里最新那个是上一节跑 demo-02 留下的——于是框图跟着载入 demo-02，
而 slice4 的链路预算断言是按 demo-01 写的。按记录的顺序（slice9 放最后）整轮跑，七套全绿。
CLAUDE.md 里「一套一台服务」这句话有它的来历。

**剩下的**：C-10「DSP 厚化接入」——`DDC` 与 `Channelizer` 进缺省链、`RxFilter` 串进前端
（要新开一个 `group: 'rx_fe'` 的子槽位）、频率计划按 `fir_version` 对齐过渡带，切片 ④c 由它收口。

---

## 2026-09-17 C-10：DSP 厚化接入框图页，切片 ④c 收口（D-072）

M-2 与 M-3 把三件 DSP 组件做出来了，但框图页上只有 DDC 与信道化两个槽位（都缺省旁路），
`RxFilter` 根本没有位置，频率计划也不知道冻结抽头表的存在。本步把这三件事补齐。两个提交。

### 做了什么

**`RxFilter` 串进链路**。新槽位 `rx_flt`，`group: 'rx_fe'` —— 挂在「接收机前端」卡片里的一行子环节，
不另立一张卡（链条仍是十一张），但在 `SITE_SLOTS` 里**排在前端之前**。这个顺序不是随手定的：
前端注入的等效热噪声因此不被它整形，**S2 的底噪仍是 −174 + nf + 10·log10(fs)**，
而那个等式有三处别的地方在依赖（频率计划的 `adc_floor` 检查、接收机模型卡 §2、slice4 的 S2 断言）。
它的通带 `bw_Hz` 由场景 `sites[].receiver.bw_Hz` 逐站带出（`FROM_SCENE`，D-054）——
那个字段自 G-2 起就被引擎解析、校验，然后一直没有任何模型用它；三份示例场景的
`bw_Hz / fs` 都正好是 0.8，落在冻结抽头表里。

**频率计划学会读冻结表**。过渡带那条检查里的 `0.4` 原来是写死的常数，与实际用的抽头版本毫无联系。
新增 `web/src/chain/firSpecs.ts` 作规格副本（只抄档位取值与通带 / 阻带边缘，不抄任何一个系数），
`firSpecs.test.ts` 直接读 `models/**/fir_*.json` 逐项对拍 —— 与引擎侧 `*_taps.cpp` 是同一套办法：
副本可以有，但必须有一道闸把它钉在真理源上。另加两条检查项：`rx_filter`（`bw_rel` 在不在档位表里）
与 `channelization`（档位、整除、路号范围）。

### 三处自己红出来 / 自己想出来的东西

**其一：S4 与 S5 会落在同一个节点上。** 「DDC 未启用时主产品落在链尾」那段兜底，注释写的是
「未启用时」，代码却是**无条件覆盖**。只开 DDC 时链尾恰好就是 DDC 那个节点，覆盖前后是同一个位置，
所以从 C-7 至今一直看不出来；DDC 与信道化**同时**启用时链尾是信道化，S4 就被挪到 S5 那一点上了——
而 CLAUDE.md 开篇把本系统的主产品定义为「DDC 后、信道化前」。是加了三级全开的夹具、
看见 `taps` 里 s4 与 s5 指着同一个节点才发现的。改成「没被锚定过才兜底」；
混合增强仍走兜底（背景在 S4 处相加，主产品是相加之后那一点）。

**其二：`select_channel` 不跟着 `channels` 走。** 编译时只写 `channels` 不写路号，
而组件目录里的缺省路号是 4（配缺省的 `channels = 8`）。用户把子信道数改成 4 却没动路号时，
编译出来的框图带着越界的 `select_channel = 4` 出去，引擎当场拒。计划里「缺省取 `channels/2`」
那条口径（M-3 对 10 §3.7 的修正）**必须写进框图**，否则它只在界面上成立。

**其三：「目标装不装得进本路」不该判错。** 第一版把它写成了红叉，然后拿演示夹具一跑就发现不对——
`chain-demo-02-chan` 选的正是「图传落进相邻子信道被压掉、跳频留在本路」那种配置，
选一路子信道本来就是一次**有意的取舍**。把「装不下」判成红叉，等于让界面替用户否掉一个合法而且
正是要演示的配置（D-039：只摆事实，不替用户下结论）。改成：硬错只留三条（档位、整除、路号范围，
都是引擎 `configure()` 会拒的），装不装得下写进说明。说明里写的是**占用**不是中心距——
demo-01 切 4 条时中心正落在本路中心上，可 400 kHz 的占用比 ±50 kHz 的子带宽得多，
只写中心距会读成「离本路 0.0 Hz，落在本路之外」这种自相矛盾的话。

顺带还有一处：可旁路的**子环节**此前没有旁路勾选框（开关只画在卡片上），
于是缺省旁路的接收滤波在界面上根本打不开。子环节行尾补一个。

### 标准算例 5 / 7 / 8 从保存的典型链路跑通

新夹具 `tests/regression/diagrams/chain-demo-02-dsp.json`：三级全开，10 → 5 → 2.5 MS/s，
S3 / S4 / S5 三个观测点一次跑出三种采样率。单测里这三个算例各自过了，
但那是直接调组件；C-10 的验收要的是「从框图跑通」。

实跑 6 s（墙钟 10.8 s，原型阶段验证值）：
`60000000 → 59999973（接收滤波扣 27 个群时延）→ 29999973 @ 5 MS/s → 14999973 @ 2.5 MS/s`，
`parameter_version` 依次 `receiver-chain-v1` / `ddc-lp_v1-d2` / `chan-pfb_v1-m2-j1`。

**图传 2 MHz 带内功率沿链：−47.163 → −47.227 → −47.161 dBm** ——
算例 7（DDC）差 **−0.064 dB**、算例 8（信道化）差 **+0.065 dB**、整条三级链累计 **+0.002 dB**。

算例 5（接收滤波）不好从一条链里单独量，因为它排在前端之前、而前端的噪声在它之后注入。
办法是拿**只差一个接收滤波**的两次运行对拍 S3：

- **全静默窗口 3906 帧 × 1024 bin 逐位相同** —— 前端噪声果然碰不到它，这正是它排在前端之前的理由；
  顺带证明加一个节点没有把随机子流挪位（否则两次运行的噪声实现就不同了，对拍也就不成立）。
- 通带内（|f| < 3.5 MHz）逐 bin 中位差 **−0.048 dB**，图传带内总功率差 **−0.059 dB** —— 通带是透明的。
- 阻带（|f| > 4.6 MHz）中位 **−1.897 dB**。**滤掉的是图传自己那条 4 阶巴特沃斯裙边，不是前端噪声**
  ——同样两次运行在静默窗口上是逐位相同的，有信号才有差别。

### 缺省链不接这三级

demo-01 是 500 kS/s：`decim` 只能取 1（抽取无从谈起），切 4 条子信道后可用子带只剩 ±50 kHz
而目标占用 400 kHz。接上去没有物理意义，只会把每一条既有基准改一遍。
**这是有意的取舍，写进 D-072 与备忘录，不是悄悄不做**；可见性由三份 demo-02 夹具兑现。

### 全量

`DEFAULT_CHAIN_TEXT` 与五份既有夹具重跑 `_gen.ts` **逐字节相同**。
引擎 291 项 doctest + **16** 项 ctest、web **245**、server 127、
七套 e2e 14 + 39 + 29 + **138** + 75 + 42 + 18 = **355 项**全绿。

**剩下的**：C-11 收口（三种模式的回归夹具、标准算例 1/2/3/6/9/10/11 从典型链路生成、
规范定稿与 06 / CLAUDE 回填），切片 ④d。

---

## 2026-09-17 C-11：标准算例从保存的典型链路跑通（D-073）

04 §15.2 列了十二项标准算例，`tests/regression/README.md` 里那张表从 2026-09-04 建起来之后
一直是十二个空框。M-2 / M-3 / C-10 顺手交了第 5、7、8 项，本步把 1、2、3、6、9、10、11 补齐，
并且**一律从保存的典型链路跑** —— 不另写测试链，跑的就是框图页真的编译得出来的文档。
两个提交。

### 三份模式夹具

`chain-synthetic.json` / `chain-replay.json` / `chain-mixed.json`，由 `_gen.ts` 经真实的编译路径
生成，`compile(parse(doc)) === doc` 逐字节（web 单测守着）。

- **全合成**那份就是内置缺省链，只多勾 S0–S3 四个观测点 —— 10 报告 §9 的「全合成链的电平链」
  要求从**保存的**链路上量，不是拿单测凑。
- **实测回放**那份前六个环节不适用（卡片上标「回放数据已含」），检测频段只能由用户给。
- **混合增强**那份要一条新场景。`AddMixer` 要求两路采样率与中心频率一致，而实测背景片段
  `dronerfb_0_CH0_S4` 是 80 MS/s @ 2440 MHz、4000000 个样点 —— 所以
  `tests/regression/scenarios/mixed-wideband.scenario.json` 就按它配成 80 MS/s、0.05 s
  （恰好等长）。这正是 04 §12.1 说的「100 MS/s 仅文件兼容与短窗」那一档用法，不是常用工作点。
  前端增益取 0：给合成支路加 20 dB 而背景不加，两者相对电平就拉开了，背景会被淹没。
  合成单音放在 +2.5 MHz（bin 宽 78125 Hz，2.5 MHz 恰好 32 个 bin，精确落在 bin 中心不吃扇贝损耗），
  设计上比背景高约 5 dB，实测 **+5.70 dB**。

### 电平链第一次从保存的链路上量全

| 点 | 实测 | 应当是 |
|---|---|---|
| S0 辐射源输出 | **27.0000 dBm** | 场景写的 `tx_power_dBm` = 27 |
| S1 接收天线端 | −51.065 dBm | 链路预算 27 + 2 + 3 − 83.146 = −51.146（差 **+0.081 dB**）|
| S2 − S1 | **20.000 dB** | 前端增益 20 dB |
| S3 量化后 | 削顶 609142 / 10000000 | 见下 |

### 三处测法是实测逼出来的

**其一：缺省链本来就削顶，所以「跑完是 valid」是个错的期望。** demo-01 缺省参数下削顶 6.09%，
D-066 ⑨ 明确记过「不为了好看去改缺省参数，它是引擎的真实判断」。第一版脚本写的是期望 valid，
当场红。改成期望 `degraded`，并顺势把 D-051 ⑥ 的两件事分开验：**计数是数据标记**
（进产品索引、观测点状态仍 `valid`——观测点不作判断），**比例超 `degrade_clip_ratio` 才把组件标降级**
（ADC 节点 `degraded`，理由写在 notes 里）。这比原来打算写的「clip_count > 0」有意义得多。

**其二：量化噪声的抬升要从量化器自己的步长算，不能拍一个容差。** 第一版写「抬升应当可忽略、
容差 0.1 dB」——那是猜的，实测 1.23 dB 当场打回。正确做法是按满量程复单音幅度
`A = 10^(FS/20)`、步长 `Δ = 2A/2^bits`、复信号量化噪声 `Δ²/6` 算出解析预期，
按带宽比例折算，再扣掉 hann 的 1.5 倍等效噪声带宽（D-049 ⑪）：
算出 **+1.194 dB**，实测 **+1.229 dB**，差 **0.035 dB**。

**其三：混合增强的目标电平要逐行相减再取中位，不能取均值。** 两次运行读的是同一段背景，
所以逐行差就是合成支路的贡献（加一个均值为零的交叉项）。但这段**真实**背景里有 WiFi 与蓝牙突发，
同一段频率上功率的**均值是中位的 87 倍** —— 均值差因此是两个大噪声量的小差值，
实测 **18% 的行差出来是负的**，按均值算偏 **1.7 dB**。改用逐行差的中位数后是 **−0.189 dB**。
这与 D-063 记过的「DS-6 分半标定的失配在干扰尾部而非噪声底」是同一件事的另一个侧面。

### 十项的实测值

| 算例 | 实测 |
|---|---|
| 1 单音频移 | 峰值频点差 **142 Hz**（容差半个 bin 244 Hz；已含多普勒 +142.4 Hz）|
| 1 功率标度 | 见上电平链三行 |
| 2 带限噪声 | −3 dB 带宽 **1.9727 MHz**（设定 2 MHz）；带内信噪比 **36.11 dB** |
| 3 突发占空比 | 导通帧占比 **0.3043**（设定 0.3）|
| 6 量化与削顶 | 削顶 6.09% 进索引且组件降级；量化抬升 **+1.229** 对解析 +1.194 dB |
| 9 实测回放 | 4000000 个样点、80 MS/s @ 2440 MHz 均取自清单；`degraded` 如实传出 |
| 10 背景加目标 | 背景原样通过 **±0.0007 dB**；合成目标对链路预算 **−0.189 dB** |
| 11 同种子复现 | **24 个产品文件逐字节相同** |

第 5、7、8 项由 C-10 的三级夹具交（通带 0.148 dB / 阻带 61.13 dB / 冲激 0 样点；
S3 → S4 −0.064 dB；S4 → S5 +0.065 dB）。第 4 项（多信号同频邻频叠加）与第 12 项（用户插件）随后续 ——
第 4 项的同频叠加其实 L-2 已经量过（对预算和差 −0.001 dB），只是没按标准算例的形式立起来。

### 性能记录（10 §9 欠的那一行）

demo-02 全合成 **20 s、10 MS/s、DDC 启用**：墙钟 **30.86 s**、实时因子 **0.648**，
3053 轮、366212 行产品。高于 0.2 的线，08 §15 ① 的多线程评估**未触发**。
（M-2 时在 6 s 的运行上量到 0.38，差别是启动开销在长任务里摊薄了。）

### 全量

引擎 291 项 doctest + **17** 项 ctest、web **246**、标准算例脚本 **18** 项全过；
`build-all.sh` 全过（`standard_cases.py` 已接进去）。既有夹具与黄金基准零改动。

**剩下的**：切片 ④d 的余项 —— P1-7（首批标准算例入回归，本步已把主体交了）、P1-9a、
U-4 的任务列表与数据中心、规范定稿。之后是切片 ⑨（V-4 站点方位图、V-5 精度场）或 L-9 定位评价。

## 2026-09-17 · D3-0：07 号报告，两项 OPEN 拍板（D-074）

**做了什么**：写出 `07.遮挡落点与坐标基座_v1.0.md`（693 行），把两项从 2026-09-02 挂到今天的
未决事项交给用户拍板，并定清楚建筑遮挡怎么接进已经跑起来的 `geo/` 帧生产端。

**用户的两条决定**（两条都与实施方的推荐不同，选项、代价与推荐理由原样留档在报告 §2.1 与 §3.1）：

1. **遮挡计算落点 = 浏览器 TypeScript 复算，并与 C++ 黄金基准对拍**。实施方推荐的是「引擎算、
   浏览器只显示」，理由是切片 ⑤ 在那条路上前端一行不用写、且少一份要长期同步的物理实现。
2. **坐标基座 = vendored GeographicLib**。实施方推荐的是「自写闭式转正，拿 foundation 冻成
   黄金基准」，理由是实测两家差 1e-9 米、换掉换不回任何物理量。

**写报告之前跑了真实数据，四件事是量出来的，不是推出来的**：

- **性能不是这一步的约束**。把 emcore 快照的遮挡原码直接编出来，喂北京亚运村的 47546 栋真实
  建筑（`buildings.geojson` 里 47582 个要素中 47546 个是单多边形，另 36 个是多重多边形，
  本次先跳过），站点置于观测区域中心、目标绕圈 720 个方位：单次射线 0.6 微秒（0.5 千米）到
  6.7 微秒（9.5 千米），桶网格建一次 10.5 毫秒，GeoJSON 用 nlohmann 解析 144 毫秒。
  按最多 8 源 × 8 站、20 赫兹帧率、每帧算两次（帧发生器的 `frame(k)` 对同一个 k 会被调两次）
  折算，整条任务的遮挡预算约是单核的 1.7%。将来覆盖场 200 × 200 个格点约 0.27 秒。
  **这个结论写进报告是为了挡住后面为它做无谓优化的冲动。**
- **非视距这条显示链路早就通到界面了，缺的只是生产者**。`line_of_sight` 从链路帧一路经
  `links.jsonl`、WebSocket 事件、`sceneStore` 通到链路线着色（`web/src/scene/layers/situation.ts:186`）
  与焦点卡的圆点徽标；非视距红 `#b91c1c` 在 2026-09-06 的色值标定里量过对比度（对陆地 5.79:1），
  **至今一次没画出来过**——因为 `geo/src/link_budget.cpp:33` 是字面量 `true`。这与 D-061 记的
  「链路线因标识拆分错从未画出」是同一类事。
- **换坐标基座是一次真正的基准变更**。GeographicLib 还没在盘上，就拿 foundation 的
  `UtEllipsoidalEarth` 当权威实现的替身实测（补 `MODEL_API` 与 `UT_DEPRECATED` 两个快照里
  根本不存在的宏就能编过，闭包 153 个文件、1886299 字节、66 个 `.cpp`，捎带轨道预报与太阳系历）：
  两家的地心地固坐标差 1e-9 米，demo-01 两条航段的弦长差 1.19e-10 与 2.65e-10 米，**相对 1e-13**。
  而航迹黄金基准是按双精度往返精度打印的（`116.40356690036484`），相对 1e-13 的扰动必然翻动
  最后两三位，**三份航迹基准都要重冻**，判据改成逐点相对差不超过 1e-12；产品的单精度浮点那一侧
  1e-13 远低于单精度的 1.2e-7，**大概率逐字节不变，但必须拿换基座前那一版的 `cuav_run` 字节比对
  去证，不能预先声明**。这一步因此单独成一个提交。
- **E3 打开之后这个演示会变成什么样，先量了再说**。站点天线 30 米（三份示例场景的现值）时，
  观测区域内 76% 到 100% 的几何判为非视距、刀口衍射损耗均值 29 到 35 分贝；抬到 60 米，
  比例降到 11% 到 96%、均值降到 8.6 到 28 分贝——**站点天线高度是这里最敏感的参数**，
  而它是场景参数不是模型参数，本步不动示例场景。demo-01 那条航线会呈现三段式：
  起飞点被挡 37.0 分贝、过顶时进入视距窗口、远去后 32.4 分贝，末端的带内信噪比从 33.8 分贝
  掉到 1.4 分贝。**这正是切片 ⑤ 想要的可见效果，而且是算出来的不是编出来的。**
  E1 仍是缺省（D-058），评价基准零变化。

**报告另外定下的接线口径**（这些是实施方的判断，不是待拍板项）：

- 遮挡**直接写在 `frame(k)` 里**，不像统计阴影那样在 `init()` 里预算。理由是它是几何的纯函数，
  满足帧发生器「可任意乱序、任意次数调用，结果逐位相同」的硬不变量；统计阴影要预算是因为它
  有序列状态。代价是射线数翻倍，**不做记忆化**——记忆化要引入可变状态，为 1.7% 不值得。
- `line_of_sight = !blocked`，与损耗大小无关：掠射只损 0.2 分贝也算非视距。这个布尔量的含义是
  「几何上有没有被楼切断」，界面上它只决定链路线颜色，颜色要摆的是事实，不是实施方替用户挑的
  一个门限。
- 损耗只进 `extra_loss_dB`，`path_loss_dB = free_space_dB + extra_loss_dB` 的恒等式照旧成立，
  `link_budget.h` 头注承诺的「只改 `extra_loss_dB` 与 `line_of_sight` 两处」在这一步逐字兑现，
  参数帧一个字段不加。**单程算一份、不乘 2**——emcore 的头注写着「雷达双程 ×2、单向链路 ×1」，
  这是从雷达代码移植过来时最容易顺手抄错的地方。
- **补第三、第四道闸**（传播那条线已有两道）：**E3 与统计阴影互斥**、**E3 与城市经验互斥**。
  两者都是建筑遮挡的统计等效，与确定性绕射同时开就是同源双计；`configure()` 报错并说明，
  不静默禁用。顺带消掉一个隐患：阴影标准差按「环境类别 × 视距」查表，而视距此前恒真、
  非视距那一列从来没被取到过；闸三禁掉 E3 加阴影之后，那一列在 E2 档下仍然恒视距，行为不变——
  但这一条要在实施时用字节比对确认，不能只靠推理。
- 投影常数照定位那条线的先例分家：`110540 / 111320·cos(纬度)` 只留在 `geo::legacy` 里守黄金基准，
  引擎喂严格的站心地平坐标，`LocalSceneAdapter` 的接口改吃平面米。**偏差已经量了**：
  亚运村处纬向 −0.445%、经向 −0.138%；把常数换成真值重跑 demo-01 整条航线，
  **命中的楼一栋没变、刀口损耗最大差 0.1 分贝**。
- `fresnelV` 里的光速 `3e8` 保留，而且引擎路径也调这一版。理由是刀口衍射是被 148 例黄金基准
  整体钉住的一个模块，只换其中一个常数会让它内部自相矛盾（几何用严格站心坐标、波长用旧光速）。
  R-2 当时预判「常数冲突要到 D3 移植 `fresnelV` 时才出现」，出现了，按 D-009 的原则处理。
- 建筑数据通路：`engine/src/buildings_json.cpp` 解析（`geo/` 不碰 JSON，与场景文件同法），
  核对区域清单里的哈希，**懒加载**——只有档位选到 E3 才读，校验路径的 9 毫秒不受影响。
  `buildings.geojson` 不入版本库，但黄金基准文件自带那 12 栋楼，所以黄金对拍是自足的；
  要真实建筑集的回归与端到端测试在缺数据时**明说跳过、不当作通过**。
- 八条声明过的简化原样进模型卡，并逐条标注它偏保守还是偏乐观。**其中「多楼只取侵入最深的一栋」
  偏乐观、「建筑的孔忽略」偏保守，方向相反且量级未测**，所以模型卡只列清单、不给合成结论。

**D3 线随之重排为 D3-0 至 D3-8**：0 本报告 → 1 vendored GeographicLib → 2 换基座的基准变更
（单独提交）→ 3 移植遮挡三件与 `fresnelV` / `knifeEdgeLoss_dB` → 4 建筑进引擎 →
5 接进帧生产端 → 6 浏览器复算 → 7 界面与点选探测 → 8 双编译器构建 → D4 同源验收。
旧的 D3-5（参数帧生产者）早在 2026-09-06 移交 G-2 完成，编号腾给新表并标注。

**回填**：CLAUDE.md 的文档表、已定表（新增遮挡落点与坐标基座两行）、未决事项表（删两行）、
D-035（标注已回收）、D3 里程碑行、决策日志 D-074；06 备忘录的 §0.3、§7 全表、§9E 切片 ⑤ 行、
§12 未决清单、§14 修订记录；`docs/display-route.md` 第 5 节与第 6.3 节待写清单。

**下一步**：D3-1 vendored GeographicLib。它需要一次联网动作取源码（与拉原始 OSM 标签同一性质，
铁律 6 允许建库阶段联网），取来时要记版本号与压缩包哈希；**动手前先与用户确认取哪个版本**。
与 D3 并行可做的小步：切片 ④d 余项里 P1-7 与 P1-9a 实质已完成、只差在备忘录上销项，
L-9 定位评价是半步（`metrics.json` 顶层 `localization` 至今是 `null`）。

## 2026-09-17 · 坐标基座的复核：维持 vendored GeographicLib

07 号报告交出去之后，用户问「拿 foundation 当权威实现也没问题吧？需要换成 GeographicLib 吗」。
这一问把已经拍板的第二项重新打开了一次，所以把 foundation 的**链接**代价也量准了
（上一轮量的是头文件传递闭包，不是链接闭包）。

**量出来的**：从 `UtEllipsoidalEarth.cpp` 起，按链接器报的未定义符号逐轮补源文件——
**它不收缩**。加到 35 个 `.cpp` 时仍剩 57 个未定义符号，一路要补到 66 个才链得上。
这 66 个里真正与坐标或椭球有关的只有 16 个（327297 字节），其余是轨道与天体历 12 个
（408956 字节，含太阳、月亮、木星、日食、Lambert 问题）、输入文件解析器 19 个
（348808 字节）、回调与实体 8 个（280796 字节）、日志异常与栈回溯 8 个（98771 字节）。
也就是说，为一次大地坐标到地心地固坐标的换算，要把一个会开文件、写日志、抓栈回溯的
子系统拖进交付包。

**答复的要点是把 foundation 的两个角色分开**：当**离线对拍件**（在开发机上编一次、
生成黄金基准、不进构建也不进交付包）代价近零，且权威性与「编进产品」一模一样——
权威性来自「一个独立的第三方实现给出同样的数」，不来自它被编进了我们的产品；
当**运行时基座**才要付上面那笔，而且只有换运行时基座才会触发那次相对 1e-13 的基准变更。

同时补记了一条上次没说的、对 GeographicLib 有利的理由：**垂直基准那项未决事项将来若要算
大地水准面高（EGM96 / EGM2008），GeographicLib 有现成的 `Geoid` 类，foundation 没有**。
首期是显式平地假设用不到，但这是唯一一条「将来可能真的需要」的功能性理由。

**用户的决定：维持 vendored GeographicLib，不改。** 报告与 CLAUDE.md 因此一个字不用改，
只把这一轮新量到的链接闭包数字与那条前瞻理由补进报告 §3.1 与 §3.2，并在报告开头的拍板栏
留了一条复核痕迹（699 行）。

**另外把一句话写进了报告，免得日后被误用**：三家实现（自写闭式、foundation、GeographicLib）
在本项目的尺度上互相差 1e-9 米量级，**谁也不比谁更准**；选 vendored 的第三方件换来的是
出处叙事与将来的功能余量，不是精度。不要把「用了 GeographicLib」当成精度论据。

## 2026-09-17 · D3-1：坐标基座 vendored GeographicLib 2.5.2（D-074）

**做了什么**：把 GeographicLib 的坐标基座抽取入库，写出 `GeographicLibGeodesy`，
立黄金基准把它与自写闭式钉在一起。**缺省工厂仍返回自写闭式**——换基座是一次真正的
基准变更，按铁律 10 单独成一步（D3-2），不夹在引入第三方件这一步里。

**取哪个版本是一个要动脑子的地方，不是取最新**。上游当时最新是 2.7。先照 2.7 试，
抽取的子集在 C++14 下**能编但只是靠编译器扩展**（`Math.hpp` 用了 C++17 的 `inline` 变量，
clang 只给警告不报错）。这在本项目是不能接受的：Windows 是优先平台，MSVC 严格模式下
大概率不收。翻上游的 `NEWS` 查到分界——**2.4 起要求 C++14、2.6 起要求 C++17**
（2.6 的条目原文写着「C++17 is now required. Minimum version of Visual Studio supported
is Visual Studio 15 2017」）。本项目的标准是 C++14，而「是否提升 C++ 标准」是一项独立的
未决事项，要单独拍板，**不能为了编过一个第三方件就顺手提**。于是取 **2.5.2**——
它是最后一个只要求 C++14 的版本，正好卡在本项目的标准上。实测确认：抽取的子集在
`-std=c++14 -pedantic-errors -Wall -Wextra` 下零警告编过。

**抽取范围**：上游共 53 个源文件、53 个头文件，本项目**只取 `Geocentric`（大地坐标与
地心地固互转）与 `LocalCartesian`（地心地固与站心地平互转）及其最小闭包，七个文件、
78105 字节**。不取大地线求解、投影、通用横轴墨卡托与军用格网、大地水准面、磁场与重力——
本系统的距离口径是地心地固弦长（决策 D-049 第一条），坐标交换一律经纬度（铁律 1），
用不着它们。唯一一件将来可能回来取的是大地水准面，那要等垂直基准这项未决事项有结论。

作为对照，上一轮为 07 号报告量过 foundation 的同一件事：那边要 66 个源文件、
153 个文件、1886299 字节，而且其中只有 16 个与坐标有关。两者差 23 倍。

**`Config.h` 是七件里唯一不是上游原件的**。上游那一份由它自己的 CMake 从 `Config.h.in`
生成，本项目不引上游的 CMake 工程，所以按抽取需要手写，每一项取值的理由写在文件里。
其中一项值得单独说：**`GEOGRAPHICLIB_HAVE_LONG_DOUBLE` 取 0**。这一项在本项目的取值下
（精度取 double）本来是惰性的，之所以仍然设成 0，是为了从源头堵掉一个跨平台隐患——
`long double` 在 MSVC 上是 64 位、在 clang 与 gcc 的 x86 上是 80 位，一旦将来有代码路径
走到它，同一份输入在两个目标平台上就会给出不同的位。交付是 Windows 与 Linux 双平台
（铁律 16），这个口子不留，堵死的代价是零。其余六件保留上游文件头、一个字节未改（铁律 13）。

**站心旋转自己写而不经 `LocalCartesian`，并且把理由验实了**。上游的 `LocalCartesian`
只暴露「大地坐标 ↔ 站心」，没有「地心地固进、站心出」的入口，经它走要多绕一次
地心地固到大地坐标再回来的往返，反而引入误差；而站心旋转矩阵是原点经纬度的纯三角函数、
不含任何椭球参数，各家一模一样。**这句话原本只能写在注释里当声称**，所以在冒烟测试里加了
一条用例：把本实现的站心换算与上游 `LocalCartesian::Forward` 在 210 个点上逐点对拍，
**最差 9.313e-10 米**（判据 1e-7）。两条代码路径给出同一个答案，那句话才算立住。

**新黄金基准 `tests/golden/geodesy.json`**，210 例（观测区域尺度为主，另加赤道、中纬、
高纬与南半球的锚点防止只在一个纬度上对），数值来自 GeographicLib，一律按 `%.17g` 打印。
生成器就是冒烟测试自己：`cuav_geo_smoke_test --write-golden tests/golden/geodesy.json`——
它是全仓唯一既链接 `cuav_geo`、又拿得到第三方私有头的目标。**判据分两个尺度**，
理由与 M-3 把算法核和组件分成两个尺度是同一条（决策 D-071）：GeographicLib 自身对本表
**1e-9 米**（同机实测是 0，即逐位；不写「逐位」是因为换平台时标准库的三角函数可能差一个
最低位，D-049 第十一条记过同一件事），自写闭式对它 **1e-6 米**（两家的反算算法不同，
自写是 Bowring 闭式不迭代，本就不该期望逐位相同）。

**实测**：两家一致性地心地固最差 **1.679e-9 米**、站心最差 **1.779e-9 米**，
比判据好三个数量级；两家往返分别是 2.28e-9 与 1.26e-6 米，都远在 G-1 立的 1 毫米之内。

**零基准变更，已证**：三份航迹黄金基准重新生成后与入库文件**逐字节相同**，
`cuav_run --catalog` 与组件目录黄金基准**逐字节相同**。缺省工厂没动，所以本该如此——
但这是量出来的，不是推出来的。

**构建**：三个第三方源文件直接编进 `cuav_geo`，不引上游 CMake，零网络。第三方头设成
PRIVATE，不出现在 `cuav_geo` 的公开头里，引擎与其余调用方对它无感。第三方源文件不按
本项目的 `-Wall -Wextra` 去挑（同引擎对 Coder 产物的处置，决策 D-071）。`geo/` 仍可独立构建。

**改掉了三处过时表述**：`geo/include/cuav_geo/scenario.h` 与 CLAUDE.md 的引擎条里
「`geo/` 零第三方依赖」现在不成立了，逐处改准；`propagation.h` 那句是**文件级**的
（「本文件零第三方依赖」）仍然成立，只把「与 geo/ 其余部分同」这半句改掉。
这是上一轮写 07 号报告时就点名要主动改的，不能留着自相矛盾。

**署名**：`THIRD-PARTY-NOTICES` 增一节，`third_party/README.md` 增一节，
`third_party/geographiclib/README.md` 记来源、版本、下载地址、压缩包字节数与 sha256
（`b3b4ce39…`）、抽取了什么、没抽什么、为什么不是 2.7、怎么复核。取源码这一步需要联网，
属建库阶段动作；交付与运行一律不联网（铁律 6）。

**全量**：引擎 **293** 项 doctest（新增 2 项）+ **17** 项 ctest、`geo/` 独立构建与冒烟测试、
标准算例 20 项、常数守卫、路径检查、ASCII 检查，`scripts/build-all.sh` 全绿。

**下一步**：D3-2，换基座的基准变更（单独提交）。要做的是把缺省工厂改成 GeographicLib，
然后**用字节比对去证后果**：三份航迹基准按逐点相对差不超过 1e-12 重冻、样点数
361 / 122 / 723 不变；全部产品文件拿换基座前那一版的 `cuav_run` 逐字节比对并如实记录结果。
07 号报告 §3.4 预估的扰动是相对 1e-13（拿 foundation 作替身量的）；换成 GeographicLib
本尊之后实测航段弦长差 1.3e-9 与 3.9e-10 米、斜距差 1.0e-9 米，**相对量级 1e-12，
比预估大约一个数量级**，所以航迹基准末几位会动这件事更是确定的了。

## 2026-09-17 · D3-2：把坐标基座真的换过去（D-074）

**做了什么**：把 `default_geodesy()` 从自写闭式改成 GeographicLib。**只改了这一处，
调用方一行没动**——D-035 当初把 `IGeodesy` 接口留出来就是为了这一刻。然后按铁律 10 的
要求，用字节比对去证后果，而不是宣称后果。

**先留证据再动手**：在换之前，用当时那版（还在用自写闭式）把八条链各跑一遍存下来——
`slice1`（无场景）、`slice2`、`chain-synthetic`、`chain-demo-02`、`chain-demo-02-ddc`、
`chain-demo-02-dsp`、`chain-3x3-aoa`、`chain-3x3-tdoa`，合计 146 个文件 1.7 GB。
换完重建、重跑、逐字节比。

### 两处预测被实测推翻

**其一：07 号报告 §3.4 说产品 `.f32`「大概率逐字节不变」——错了。**

实测 76 个文件变化，其中 42 个是 `.f32`。当时的推理是「路损相对变化 2.3e-12 → 幅度相对
1e-12 → 远低于单精度的 1.2e-7 → 应当不变」。这条推理**只对谱上那些有实际电平的频点成立**，
漏掉了另一大片：功率谱是取过对数之后存的，而一条纯单音的谱里有一大片频点落在**数值相消底**
上（加窗单音的泄漏底，电平 −200 到 −300 dBm，数值上就是零）。在那种地方，输入的 1e-12
相对扰动会把该点的值整个换掉——不是因为不准，是因为那里本来就是两个几乎相等的数相减的残渣，
折成分贝就成了几十分贝的「差异」。

分开量才看得清（九个观测点）：**峰下 20 dB 以内（真正读得到的那部分）最大差 7.9e-4 分贝**，
高于 −160 dBm 时最大 0.03 分贝，把 −200 dBm 以下的相消底算进来才是 58 分贝。
`chain-demo-02/s4` 是个干净的对照：6000 万个值里只有 **1021 个**变了（1.7e-5），
那正好是「1e-12 的扰动撞上单精度舍入边界」的概率量级——那才是我当初推理里想的那种情形。

**更要紧的是下游的结论一个都没变**，这一条是专门去查的：五份夹具的检测行数、命中帧数、
突发段数、识别标签序列、识别准确率**逐项相同**；跨层一致性算例 ① 也一个数没动，
解析检出率 0.7164 对蒙特卡洛 0.7026，差仍是 **0.0138**。

**其二：报告 §3.4 定的判据「逐点相对差不超过 1e-12」超差。** demo-01 实测 2.312e-12、
demo-02 实测 1.929e-12，demo-03 通过。按铁律 10 先查根因，**结论是判据不对，不是数据不对**：

顶到的是 demo-01 第 0 个样点的**航向角**，39.48970142375679 变成 39.489701423848075，
绝对差 9.128e-11 度。航向由航段两端点之差算出，1.7e-9 米的地心地固差落在 1009 米的第一
航段上，角度扰动应为 1.7e-9 除以 1009 弧度，即 **9.653e-11 度**，与实测的 9.128e-11 度
吻合。判据有两处不对：① 那个 1e-12 是拿 AFSIM 抽取库当权威实现的**替身**量出来的 1e-13
推的（当时 GeographicLib 还没在盘），换成本尊之后扰动是 1e-12 量级，**大一个数量级**——
这一点 D3-1 收尾时已经记过，但没有回头改这条判据；② **对角度用「相对差」本来就不是好判据**，
同样 9.1e-11 度的绝对变化，航向 39.5 度时是 2.3e-12，航向 0.4 度时就成了 2.3e-10，
判据会跟着无人机朝哪飞而变。

**处置是不放宽同一条判据，换成按量的物理判据**，每条从「地心地固差 1.7e-9 米除以该量的
尺度」推出来：经度与纬度不超过 1e-12 度（约 0.1 微米，实测最差 2.842e-14 与 1.421e-14，
余量 35 倍与 70 倍）、高度不超过 1e-8 米（实测 7.870e-11，余量 127 倍）、航向不超过
1e-8 度（推导上界 4.6e-10，实测 9.128e-11，余量 110 倍）、速度**严格为零**（位移除以时间，
时间轴没变，实测确实是零）。样点数 361 / 122 / 723 不变，实体与时刻逐条对齐。
三份基准据此重冻，**重冻之后再生成即逐字节相同**，回到平时的判据。

### 物理量到底动了多少

| 量 | 最大变化 |
|---|---|
| 链路距离 | 2.7 纳米 |
| 路径损耗 | 3.2e-10 分贝 |
| 方位角 | 9.1e-9 度 |
| 传播时延 | 9.1e-18 秒 |
| 航迹经纬度 | 1.4e-14 度（约 1.5 纳米） |
| 测向报告的方位 | 5.6e-10 度，测向标准差**严格不变** |
| 交叉定位解的经纬度 | 1.4e-13 度；圆概率误差 7.1 纳米 |
| 时差定位解的经纬度 | 4.3e-14 度；圆概率误差 7.8e-10 米 |

**一处顺带验到的事**：`chain-3x3-aoa` 与 `chain-3x3-tdoa` 的测向标准差 `sigma_deg` 与
`rms_trace_m` 两列**绝对差严格为零**。它们只依赖信噪比与几何构型的分档，不直接依赖坐标的
末几位，所以本该如此——但这是量出来的。

### 那条哨兵按设计红了

D3-1 时故意留了一条断言「缺省工厂仍是自写闭式」，并在注释里写明「这条在 D3-2 会被有意改掉，
改动本身就是那一步的标志」。这次全量跑下来，293 条用例里**只有它一条红**，改掉即通过。
这比不写这条、然后靠人记得去改要可靠。

**全量**：引擎 **293** 项 doctest + **17** 项 ctest、web **246**、服务端、
七套端到端 14 + 39 + 29 + 138 + 75 + 42 + 18 = **355** 项，`scripts/build-all.sh` 全绿。

**下一步**：D3-3，把遮挡那三件从 emcore 移植进 `geo/`（地图查询接口、场景适配器、刀口衍射），
接口改吃平面米、旧常数进 `geo::legacy`。收口条件是 148 例黄金基准相对误差不超过 1e-9，
外加解析锚点「掠射时刀口损耗 6.03 分贝」。

## 2026-09-17 · D3-3：遮挡三件移植进 geo/，148 例黄金基准全过（D-074）

**做了什么**：把建筑遮挡从 emcore 快照移植进 `geo/`——地图查询接口、本地场景适配器
（100 米桶网格加线段遍历）、单刀口衍射，外加 Fresnel 参数与刀口损耗两式。
**遮挡还没有接进链路预算**，`line_of_sight` 仍然恒为真，接线是 D3-5。

**落点**：`geo/include/cuav_geo/map.h`（接口 + 适配器）、`geo/src/local_scene_adapter.cpp`、
`geo/include/cuav_geo/occlusion.h` + `geo/src/occlusion.cpp`、
`geo/src/propagation.cpp` 里的 `legacy::fresnel_v` 与 `legacy::knife_edge_loss_dB`。

**两处有意偏离 emcore 原件**（都在 07 号报告 §6.3 定过）：

一、**接口改吃平面米，不吃经纬度**。原件的射线求交收的是经纬度，投影常数写死在适配器的
构造函数里。本项目把投影挪到调用方：黄金基准回放走 `legacy::local_frame_occlusion()` 的旧常数
（纬向 110540、经向 111320 乘纬度余弦），引擎实际运行走严格的站心地平坐标。这与 D-053 把
定位求解器改成平面坐标接口是同一套做法——一份数学两处用，旧常数只留在 `legacy` 里。

二、命名改成本项目的蛇形风格（同 R-2 移植大气与降雨两式时的处置）。**数值与算法一字未改。**

**`legacy` 里现在有两套常数不同的投影并存**，这件事要记住：定位那套纬向取 111320，
遮挡这套纬向取 110540，它们来自 emcore 的不同模块、各守各的黄金基准，**谁也不许去统一谁**。
两处头注都写了这句话。

### 常数守卫开了一条窄例外，并且验证过它还拦得住

`legacy::fresnel_v` 与 `legacy::knife_edge_loss_dB` 是**引擎实际运行也会调的**——
刀口衍射这一整套（两式加适配器几何）是被那 148 例整体钉住的一个模块，只把其中的光速常数
换成精确值，会让模块内部自相矛盾：几何走严格站心坐标、波长却走旧光速。

但 `scripts/build-all.sh` 里有一条守卫，专门拦 `legacy::` 渗进新代码（决策 D-009）。
于是给它加了一条**写明理由的窄例外**：只放行 `geo/src/occlusion.cpp` 这一个文件里的
这两个符号。加完之后**专门验证了它没有把守卫关掉**：在 `link_budget.cpp` 里写一句
`legacy::fresnel_v`，拦下；在 `occlusion.cpp` 里写一句**别的** `legacy::` 符号，也拦下；
恢复后通过。例外确实是一个文件乘两个符号这么窄。

### 顺带逮到 07 号报告自己写错的一个量级

给刀口衍射写解析锚点时，顺手加了一条断言把「两个光速常数差多少」也钉住——本来只打算
写在注释里。它当场就红了：实测 **2.99e-3 分贝**，而报告 §6.4 与 §8 写的是「1e-5 分贝量级」。

根因是把 `3e8` 与 `299792458` 的相对差看成了 6.9e-7，实际是 **6.9e-4**——`3e8` 比精确值
大 0.069%，我少算了三个数量级。往下推：Fresnel 参数与光速的平方根成反比，故相对变化
3.46e-4；大参数时损耗约为 `6.9 + 20·log10(2v)`，对 v 的导数是 `8.686/v`，于是损耗变化约
`8.686 × 3.46e-4 = 3.0e-3` 分贝，与实测吻合。

**结论不变、数字要改**：3e-3 分贝物理上仍然无关紧要，保留旧光速守黄金基准的理由一字不改；
改的只是「量级」这个数。修正记进 07 报告 §14.3、代码注释与单测。

**这个错之所以被逮住，是因为把量级写成了断言而不是注释**——注释里的数错了没人知道，
断言里的数错了当场就红。与 D3-1 把「站心旋转各家一样」从注释改成对拍用例是同一条经验，
这一步算是第二次吃到它的好处。

### 验收

| 项 | 结果 |
|---|---|
| 遮挡黄金基准 | **148 例全过**，相对误差 ≤ 1e-9；其中 29 例判为非视距 |
| Fresnel 参数 | **108 例全过**，≤ 1e-9 |
| 刀口损耗 | **14 例全过**，≤ 1e-9 |
| 解析锚点（掠射 v = 0） | **6.0329 分贝** |
| 模型截止 | v ≤ −0.78 判无遮挡；v 增大时损耗单调增 |
| 适配器行为 | 楼下穿过判非视距、楼顶掠过判视距、端点落在楼的投影内即排除该楼、无效要素剔除并计数不静默丢、空场景优雅降级、单程取一份不乘 2 |

黄金基准文件 `tests/golden/occlusion.json` 自 emcore 拷入（148 例加 12 栋楼），`_meta` 里
记了来源、去处、投影口径与光速常数这四件事；`tests/golden/propagation.json` 补上了
`fresnelV` 与 `knifeEdgeLoss_dB` 两组，`_meta.kept` 相应改写——这两组正是 R-2 当时明说
「留给 D3 移植刀口衍射时再启用」的。

独立冒烟测试也加了一条遮挡自检（那个文件的职责就是保证本库脱离引擎也能自检）：
一栋 40 米高的楼，楼下穿过 30.6 分贝、楼顶掠过 0 分贝。

**全量**：引擎 **297** 项 doctest（新增 4 项）+ **17** 项 ctest、`geo/` 独立构建与冒烟测试、
标准算例 20 项、常数守卫、路径与 ASCII 检查，`scripts/build-all.sh` 全绿。

**下一步**：D3-4，建筑数据进引擎——`engine/src/buildings_json.cpp` 解析
`buildings.geojson`、核对区域清单里的哈希、多重多边形按外环拆、懒加载（只有档位选到 E3 才读）。
收口条件是真实建筑集 47546 栋加载不超过 200 毫秒，且 `--validate` 仍然约 9 毫秒。

## 2026-09-17 · D3-3 收尾：两套旧投影并到一处，并把「不许统一」写成断言

**起因**：用户问「老常数那个角落里有两套不一样的换算系数，这个问题需要解决吗」。

**先核实再回答**。查下来两套旧投影各自被谁调：

| | 定位那套（纬向 111320） | 遮挡这套（纬向 110540） |
|---|---|---|
| 谁在调 | 只有 `geo/src/locate_aoa.cpp` 里那一句，而它在 `namespace legacy` 内部，函数头注写着「只供黄金基准回放」 | 只有 `engine/tests/test_occlusion.cpp` |
| 系统实际运行调不调 | **不调**：`engine/src/locate.cpp` 喂给求解器的是 `to_enu(to_ecef(...))`，严格站心地平 | **不调**：D3-5 接线时同样喂严格站心地平 |

所以**答案是不需要解决，而且不应该解决**：两套常数不是「同一个问题的两个答案」，系统真正
计算时一把都不用；它们只是两盘老录音带各自的播放参数。统一它们等于用我方常数重新生成
黄金基准，把「基准独立于我方代码产生」这个唯一的价值抹掉——正是铁律 10 禁止的事。

**但有两处真的值得收紧，用户同意后做掉**：

**一、两套并到一个文件里。** 新建 `geo/include/cuav_geo/legacy_frames.h` 与
`geo/src/legacy_frames.cpp`，把原先散在 `fix_geometry.h` 与 `map.h` 两处的投影挪进来并排放着。
顺带解决三件事：① 原来两个形状完全相同的结构体叫着两个名字（`LocalFrame` 与我在 D3-3 里
起的 `LocalFrame2`），现在合成一个；② `local_frame_111320` 这个名字其实分不清两者——
**遮挡那套的经向也用 111320**，只有纬向不同，所以按常数命名本来就会误导，改成按来源模块
命名 `local_frame_locate` / `local_frame_occlusion`；③ 警告以前在两个文件里各写一遍，
现在只在这一个文件的头注里写一次，而且读者一眼能同时看见两把尺子。

**二、把「不许统一」从注释变成断言。** `engine/tests/test_geo.cpp` 新增一条用例，钉住四件事：
两套的纬向常数各自的值；经向两套同式而**纬向必须不同**（这条就是「不许统一」的可执行版本）；
两套都刻意偏离 WGS-84 真值且**偏的方向相反**——实测在 39.99°N 定位那套纬向偏 **+0.257%**、
遮挡那套偏 **−0.445%**、两套经向同偏 −0.138%；以及参考纬度变化时经向跟着变、纬向不变。
谁哪天真去「统一」了，这条用例当场红。

这是同一条经验的第三次兑现（D3-1 把「站心旋转各家一样」从注释改成对拍用例、
D3-3 把光速常数的量级写成断言当场逮出报告里的错）：**注释里的话错了没人知道，断言里的话
错了立刻报警**。

**顺带清掉两处随移动产生的未用符号**（`fix_geometry.cpp` 的 `deg2rad`、
`local_scene_adapter.cpp` 的圆周率常量），编译器的未用告警当场指出来的。

**验证**：改完重跑常数守卫，确认它**没有被这次搬家弄松**——在 `link_budget.cpp` 里写一句
`legacy::local_frame_locate`，照样拦下。

**全量**：引擎 **298** 项 doctest（新增 1 项）+ 17 项 ctest、`geo/` 独立构建与冒烟测试、
标准算例 20 项、三项守卫，`scripts/build-all.sh` 全绿。黄金基准零改动——这次只搬家与改名，
一个数没动。

---

## 2026-09-18 D3-4：建筑几何进引擎（决策 D-074，07 报告 §7 与 §14bis）

从 2026-09-02 建库至今，`data/scene/beijing-yayuncun/buildings.geojson` 的四万多栋楼只被前端
拿去做三维拉伸渲染，引擎一侧从未读过。本步把这条通路打通到「取到手」为止：**建筑读进引擎、
建成桶网格、挂在 `ScenarioSource` 上**。把它接进链路预算（让 `line_of_sight` 不再恒真）是下一步
D3-5，本步一个物理数都没改。

### 做了什么

**一、`geo::SceneFrame`：遮挡几何的平面工作帧。** D3-3 把 `IMapQuery` 的接口改成吃平面米、
把投影挪给调用方之后，就留下一个没人回答的问题：**建筑是按哪个原点投到平面米上的，站点和目标
又是按哪个原点投进去的**。两边差一米，整份建筑集就相对视线平移一米，而这种错不会报警、只会让
遮挡结果悄悄偏掉。所以不把原点写成几个散着的常数，而是封成一个对象放进 `cuav_geo/map.h`：

- **原点取观测区域清单里的 `aoi.center`**（用户在 D-021 定的那个点）。它是数据包的属性，
  与场景无关、与站点无关，于是同一个数据包的所有站、所有场景共用同一把尺子。
- **换算是严格站心地平的东 / 北分量**（铁律 1），走缺省坐标基座（D3-2 起是 GeographicLib）。
- **`z` 不由这个帧产生**。`to_plane()` 只给 `x / y`，高度一律用离地高差另给。拿站心坐标的
  `up` 当高度是错的——10 km 外的地面点，`up` 已经因地球曲率低了 7.8 m，而建筑的
  `base_m / height_m` 是离地高差，两者不是一回事、更不许相加（铁律 2）。这条写成了断言。

**二、`engine/src/buildings_json.cpp`：GeoJSON → `std::vector<geo::Building>`。** 放引擎侧而不是
`geo/`，与 `scenario_json.cpp` 同一分工（`geo/` 不碰 JSON）。四条规则：闭合环去掉重复的末点
（`geo::Building` 约定首尾不闭合）；`MultiPolygon` 的每个子多边形的外环各当一栋、`id` 加
`#<序号>` 后缀；内环（孔）忽略并计数；`height_m ≤ 0` 与顶点不足的剔除并计数。
几何类型不认识**直接报错**，不静默跳过——本项目的建筑集只会有 `Polygon` 与 `MultiPolygon`，
出现别的说明上游变了，那是要当场知道的事。

**三、信任链与懒加载。** 场景声明观测区域清单的字节哈希（`check_aoi_manifest` 已经在核），
清单里记着 `buildings.geojson` 的 sha256，本步读进来再核一道，于是从场景到建筑字节是一条完整
的链（铁律 8）。加载**只在 `prop_level == E3` 时发生**，且发生在 `ScenarioSource::init()`；
`shared_scene_map()` 按 `(scene_root, aoi_id)` 在进程内缓存，K 个站共用一份桶网格而不是 K 份。
装载器只注入一个内部参数 `scene_root`——**框图里不写路径、浏览器见不到**（D-037）。

### 实测与三处与报告不符的地方

**其一，栋数不是 47546，是 47662。** 07 §7.3 的表把「`MultiPolygon` 36」写在计数栏里，那是
**要素数**；按「每个子多边形的外环各当一栋」拆完是 116 栋。§1.4 的调研实测是 47546 栋，
因为那一次明写了「`MultiPolygon` 36 件本次先跳过」。实际进适配器的是 47546 + 116 = **47662**。
忽略内环 **345** 个，`height_m ≤ 0` 与顶点不足的各 **0** 件。这些数已经写成单测里的断言。

**其二，加载 210–242 ms，过了报告写的 200 ms 线——那条线定错了。** 逐段量出来：

| 段 | 实测 |
|---|---|
| 读 16 MB 进内存 | 38–61 ms |
| sha256 核对 | 38–50 ms |
| nlohmann 解析（建 DOM） | 103–118 ms |
| **投影到平面米（严格 ENU，344712 个外环顶点）** | **29–32 ms** |
| 桶网格 | 13–15 ms |

§1.4 的调研只量了「解析 144 ms」与「桶网格 10.5 ms」两段（合 155 ms），既没算把文件读进内存，
也没算 §7.1 自己要求的 sha256 核对——这两段合起来 80–110 ms，200 ms 从一开始就装不下报告
自己要求的动作。**处置是把线改成 250 ms，不优化**：这笔钱一次 E3 任务只付一次，而整任务的遮挡
预算是单核 1.7%（§11 已经写死「性能不是这一步的约束」）。顺带记一个备查的数：DOM 解析的
**峰值常驻内存 193 MB**（瞬态，函数返回就还回去，留下的只有 47662 栋楼约 8 MB）；唯一有量级
收益的改法是 SAX 流式解析，本期不做——写在这里是为了让它是一个**决定**而不是一次疏忽。

值得说明的是，我原本担心的是投影：344712 个顶点各做一次严格 ENU 换算，听起来像是新引入的大头。
实测它只占 **29–32 ms**，是五段里第二小的。先量再决定，省下了一次为了性能去做投影近似的改动——
那种改动会把「严格 ENU」这句话变成一句不准确的话。

**其三，加载点不在装载器里。** 07 §9.2 的表写「`src/diagram_json.cpp`：E3 时按观测区域清单
加载建筑并注入」，而 §7.4 写「校验路径不读建筑」。**这两条不能同时成立**：装载器正是
`cuav_run --validate` 唯一走的那条路（它只调 `configure()`，不调 `init()`），在装载器里加载就等于
每次校验都付这 240 ms。按 §7.4 做。实测 `--validate` 十次的最小 / 中位 / 最大是
**2.8 / 3.1 / 3.7 ms**，一个字节也没多读。

### 零回归与基准变更

**零回归**：拿上一版提交（`f30d4c7`）另建一个工作树编出 `cuav_run`，与本版各跑五份夹具
（`slice1` / `slice2` / `slice4_evaluate_scene` / `chain-demo-02` / `chain-demo-02-dsp`），
**54 个产品文件逐字节相同**——`.f32`、观测点索引、四份 JSONL、`metrics.json` 全部在内。
`events.jsonl` 不同，差别只有墙钟时间戳与随 `progress` 条数（117 对 119）平移的 `seq`，
按 D-041 ③ 事件流本来就不承诺逐字节复现。

**唯一基准变更**是组件目录：`ScenarioSource` 多一个内部参数 `scene_root`，差异是**纯插入的
8 行**，`port_types` / `port_compat` / `engine_version` 与其余 24 个组件逐条未变（脚本核对）。
web 侧那条「全库内部参数 32 处」的计数断言随之改成 33 并加了一条 `ScenarioSource.scene_root`
的点名断言。

**全量**：引擎 **306** 项 doctest（新增 8 项）+ 17 项 ctest、web **246**、server **127**、
七套 e2e 14 + 39 + 29 + 138 + 75 + 42 + 18 = **355** 项、标准算例 20 项、跨层算例 ① 仍
**0.0138**，`scripts/build-all.sh` 全绿。

**顺带记一条环境上的坑**：第一轮跑 e2e 时 `slice9` 报 `TypeError: fetch failed`，查下去发现
8080 端口上是**一天半前的会话留下的服务进程**，我新编的服务因 `EADDRINUSE` 根本没起来，
七套 e2e 全跑在旧构建上。杀掉重起之后 355 项全绿。教训是起服务之后要确认端口上的进程是不是
自己刚起的那个（`ps -o pid,etime`），只看 `/api/v1/health` 返回 200 是不够的。

### 已知边界

- **`prop_level = E3` 仍被 `PropagationConfig::validate()` 拒**。建筑取到手了，但还没接进
  链路预算，此时放开 E3 等于让用户选了建筑遮挡却拿到自由空间的结果（铁律 15）。因此
  `load_scene_map()` 的 E3 分支目前由 `shared_scene_map()` 的直接单测覆盖，还没有走过
  一次完整的框图；注入这一环另有单测钉着（`scene_root` 的值确实到了组件里、E1 下确实不读）。
  D3-5 打开 E3 时这条路径才第一次端到端跑通。
- **建筑集不入 git**，缺数据的机器上相关用例**明说跳过、不当作通过**（D-073 ③ 的先例）；
  148 例遮挡黄金基准自带 12 栋楼，因此 golden 对拍在任何机器上都自足。

---

## 2026-09-18 D3-5：遮挡接进帧生产端（决策 D-074，07 报告 §5、§9 与 §14ter）

`geo/src/link_budget.cpp:33` 那句 `out.line_of_sight = true;` 从 2026-09-06 写下起就是个字面量。
本步把它换掉：视距由建筑几何给出，刀口衍射损耗进链路预算。切片 ⑤ 想要的可见效果到此出现。

### 做了什么

**一、`link_geometry()` 收一个可选的 `OcclusionQuery`**（地图 + `geo::SceneFrame` + 频率）。
三条口径写进头文件并各有断言：

- **`line_of_sight = !blocked`，与损耗大小无关**。掠射只损几分贝也算非视距。这个布尔量的含义
  只有一个——楼挡没挡住；界面据它给链路线上色，颜色要摆事实、不摆实施方挑的门限（D-039）。
- **损耗只进 `extra_loss_dB`，单程乘一不乘二**。emcore 的原注写的是「雷达双程乘二、单向链路
  乘一」，这是从雷达代码移植过来时最容易顺手抄错的地方，所以单测里直接拿 `segment_occlusion()`
  的返回值逐位对，并且断言它小于两倍。
- **频率逐帧取**。菲涅尔参数与波长有关，而跳频源每次停留都在变，所以用的是那一帧的
  `center_Hz`（瞬时频点，不是跳频序列，D-069）。

于是 `path_loss_dB = free_space_dB + extra_loss_dB` 的恒等式在任何档位下照旧成立，
`SceneParamFrame` 一个字段不加——`link_budget.h` 头注承诺的「只改 `extra_loss_dB` 与
`line_of_sight` 两处」逐字兑现。

**二、补闸三与闸四**（D-058 已有两道）。`E3` 与统计阴影互斥、`E3` 与城市经验互斥：两者都是
建筑遮挡的**统计等效**，与按几何算出来的确定性绕射同开就是同源双计（EM-P-13 §10.9）。
报错并说明，不静默禁用其中一个。E3 与地面双径、大气降雨可以同开——那不是同一效应。

**三、`included_loss_terms` 增取值 `diffraction`**，`link` 事件与 `links.jsonl` 增**可选键**
`diffraction_dB`（只在非零时写）。可选是为了 E1 / E2 的产品逐字节不变。

### 三处实施中才想清楚的事

**其一，地图必须与 `build()` 分两步给。** 建筑是懒加载的（D3-4），只有 E3 才读、读一次 240 ms，
而链路是在组件的 `configure()` 里搭的——那正是 `cuav_run --validate` 唯一走的那条路。
把地图作为 `build()` 的参数，就等于逼着 `configure()` 先加载建筑，D3-4 好不容易保住的
「校验不读建筑」当场作废。拆成 `build()` + `set_scene_map()`，另加一个 `needs_scene_map()`
让调用方在产帧之前查得出来；`ScenarioSource::init()` 查它，查到就报错——**E3 少了地图就是
「选了建筑遮挡却按自由空间算」，而界面上分不出来**（铁律 15）。

**其二，`included_loss_terms` 按帧的挡没挡来声明，是错的。** 第一版写成「这一帧没被挡就不声明
`diffraction`」，写完才想明白它回答错了问题：下游看这份清单是要判断「**这条路损里算没算过
建筑遮挡、还能不能再叠一个统计等效**」，而不是「这一帧挡没挡住」——后者是 `line_of_sight`
的事。按帧声明会让同一条链路的清单在视距与非视距之间来回变，下游据它做不了任何判断。
改成 E3 档每帧都声明。

**其三，前端的相容判据从「与引擎一一对应」变成「比引擎严一档」。** `propConflict()` 是引擎
`PropagationConfig::validate()` 的前端镜像，单测名字就叫「一一对应」。E3 接进引擎之后这句话
不再成立：引擎接受，前端仍拦。拦是对的——浏览器这一侧还没有同源的遮挡复算（D3-6），放开会让
场景页的链路预览与实际跑出来的结果对不上。**但报文里那句「E3 需要逐建筑几何，待 D3 接入」
现在是假的**，改成照实说，单测跟着改名并加一条 `assert.doesNotMatch(e3, /待 D3/)` 钉住措辞。
闸三闸四**没有**同步加进前端镜像：E3 在前端根本到不了，加了就是死代码（D-057 的处置），
D3-7 去置灰时一并补。

### 实测

**07 §1.5 的调研表逐行复现了，而且这一次是从保存的典型链路上跑出来的**（调研那次是拿 emcore
快照的原码另编一个基准程序量的，用的是别的投影常数、别的驱动方式）：

| 位置 | 调研（§1.5） | D3-5 实测 |
|---|---|---|
| 起飞点 | 210 m / 37.0 dB | **211.1 m / 36.98 dB** |
| 过顶 | 34 m / 0（视距窗口） | **33 m 起 / 视距窗口 t 7.2 至 26.4 s** |
| 第一航段末 | 799 m / 28.4 dB | **801.2 m / 28.39 dB** |
| 航线末端 | 3155 m / 32.4 dB | **3035 m / 32.30 dB** |
| 末端 S3 带内信噪比 | 33.8 至 1.4 dB | **33.7 至 1.4 dB** |

整条 180 秒航线 **89.3% 的帧判非视距**，刀口损耗均值 30.3 dB、最大 37.0 dB；过顶那一段两档
读数相同（35.8 dB）。瀑布上因此是「被挡、过顶拔起三十几分贝、远去重新沉下去」的三段式。
掠射那条性质由单测里**特意构造**的几何验（墙高扫到刚擦视线，7.38 dB 仍判非视距）——
demo-01 这条航线上被挡的帧全都挡得很深（最小 33.8 dB），一帧掠射也没有，拿它当证据
等于拿碰巧没有反例的数据说事，所以回归脚本里那一条改成只把这个事实打印出来，不当作断言。

**零回归**：拿 D3-4 那一版（`d56ed63`）另建工作树编出 `cuav_run` 对拍——

- E1 八份夹具（slice1 / slice2 / slice4_evaluate_scene / synthetic / demo-02 / demo-02-dsp /
  3x3-aoa / 3x3-tdoa）共 **134 个产品文件逐字节相同**；
- **E2 全开**（双径 + 阴影 + 天气 + 25 mm/h 雨）另跑一份，**24 个文件逐字节相同**，
  且 `diffraction_dB` 这个键确实没出现；
- 三份航迹黄金基准与组件目录黄金基准**逐字节不变**——本步**零基准变更**。

`cuav_run --validate` 对 E3 框图中位 **4.2 ms**（懒加载没被破坏，校验路径仍不读建筑）。

### 顺带修掉一个折腾过两次的端到端假失败

`slice9` 单独跑必过，紧跟 `slice8` 跑偶尔在第一步就挂，报 `TypeError: fetch failed`。
**上一次（D3-4）我把它归给了 8080 端口上那个一天半前的旧服务进程，那个判断是错的**——
旧服务确实存在也确实该杀，但这个失败是另一回事，换了新服务照样复现。

真正的原因在 `tests/e2e/cdp.mjs`：七套用例都用写死的调试端口 9333。接连跑时上一套的 Chrome
可能还没退干净，新起的那个绑不上端口，而就绪探针 `/json/version` **探到的是正在退出的那一个**
——返回 200，随后 `Page.open` 就 `fetch failed`。改成 `--remote-debugging-port=0` 让 Chrome
自己挑，从 `<userDataDir>/DevToolsActivePort` 读回实际端口（各套的 userDataDir 本来就是各自
`mkdtemp` 出来的，零冲突）。改完 slice8 接 slice9 连跑三轮全过。

记在这里是因为它示范了一类错误的代价：**一个与被测代码无关的偶发失败，如果归错了因，
下次还会照样浪费时间**——而且第一次的错误结论已经写进了记忆，这次得连记忆一起改。

### 全量

引擎 **310** 项 doctest（新增 4 项）+ 17 项 ctest、web **246**、server **127**、
七套 e2e 14 + 39 + 29 + 138 + 75 + 42 + 18 = **355** 项、标准算例 20 项、
**新增 E3 回归 7 项**（`tests/regression/e3_occlusion_chain.py`，已接进 `build-all.sh`，
缺建筑集时明说跳过）、跨层算例 ① 仍 **0.0138**，`scripts/build-all.sh` 全绿。

### 已知边界

- **界面上 E3 还选不到**，所以这一档目前只能从保存的框图或回归脚本里跑。D3-6 补浏览器侧的
  TS 复算、D3-7 才去置灰并把链路线第一次画红。
- **E3 一开，demo-01 这个演示会变暗**：站点天线 30 m 时 89.3% 的时间非视距。这是真实城市几何
  的结果不是缺陷；站点高度是**场景参数**（抬到 60 m 能把损耗均值砍一半），本步不动示例场景。
- 跨层一致性算例 ②（建筑遮挡）**还没写成算例**。已有的量是「E1 → E3 链路预算差 32.30 dB、
  S3 带内信噪比差 32.3 dB」，两者之差 0.6 dB 里有多少是抽象误差还没拆开。

---

## 2026-09-18 D3-6：浏览器侧遮挡复算（决策 D-074，07 报告 §2.2、§2.3 与 §14quater）

D-074 那次拍板定的是「遮挡两边都算」：C++ 是真理源，浏览器用 TypeScript 复算，两侧同守
同一份黄金基准。本步把浏览器那一半补上。

### 做了什么

`web/src/scene/occlusion/` 五个文件，逐件对着 C++ 写：

| TS | 对应的 C++ | 内容 |
|---|---|---|
| `frame.ts` | `geo::SceneFrame` + `geo::legacy` | 两套投影：严格站心地平（对引擎结果）与 legacy（回放黄金基准），**按用途选、别混** |
| `adapter.ts` | `geo::LocalSceneAdapter` | 100 m 桶网格 + Amanatides-Woo 线段遍历 |
| `occlusion.ts` | `geo::segment_occlusion` + `legacy::{fresnel_v, knife_edge_loss_dB}` | ITU-R P.526 单刀口，光速同样留 `3e8` |
| `geojson.ts` | `engine/src/buildings_json.cpp` | 闭合环去末点、`MultiPolygon` 拆外环、孔忽略并计数 |
| `store.ts` | （无，浏览器独有） | 建筑几何的懒加载小 store，不进主 store |

祖本是 em-demo 的 `src/models/occlusion.ts`（C++ 当年就是从它移植的，这次是回迁）。
§2.2 预告的两处照办：外环由闭合改成首尾不闭合、投影原点由写死的西安常数改成参数化。

**148 例首跑即过**，`rel ≤ 1e-9`，其中 29 例判非视距——与 C++ 侧报的是同一个数。
`fresnelV` 108 例、`knifeEdgeLoss_dB` 14 例同样全过。

### 一个核实到的问题：148 例不够用

跑绿之后我去想「这 148 例到底覆盖了什么」，结论是**没覆盖这一步最容易出错的三样东西**：
它用的是黄金基准自带的 **12 栋合成楼**加 **legacy 投影**，所以完全测不到 GeoJSON 的解析规则、
严格站心地平的平面帧、四万多栋楼上的桶网格。两侧都把 148 例跑绿，**仍然可能在真实数据上
分叉而没人知道**——而分叉不会报错，只会让遮挡结果悄悄不同。

补了一张表：`tests/golden/occlusion-aoi.json`，北京亚运村真实建筑集上的**五条射线**，
C++ 与浏览器各守一遍（表里的数由 C++ 产生，它是真理源）。实测两侧最差相对差
**9.5e-13（损耗）/ 1.3e-12（侵入深度）**，判据取 1e-9，余量约一千倍。

**不该期望逐位相同**，判据也就不能写成「相等」：椭球换算一边是 vendored GeographicLib、
一边是 `preview.ts` 的自写闭式，两家本身就差 1.7e-9 m（07 §3.2）。把 GeographicLib 也搬进
浏览器不值得。

这张表顺带把**解析计数**也钉成两侧同守：47582 要素 → 47546 `Polygon` + 36 `MultiPolygon`
拆出 116 → **47662 栋**、忽略内环 345、剔除 0，九项逐项相同。

### 三处值得记的判断

**其一，「懒加载」懒的是数据不是代码。** 07 §11 风险 8 写「模块被顶层 import 拖进首屏」。
实施下来这句话要说得更准：**代码**确实进了首屏——探针要读加载状态，`AppShell` 就得 import
那个 store；实测整个遮挡模块在产物里只占 **835 字节**（1562360 → 1563195 B，gzip +0.57 kB），
这点体积不值得为它做动态 import。**数据**则一个字节都没取：15.9 MB 的文件、88 ms 的
`JSON.parse`、41 MB 的堆，只有真要算遮挡时才付。`slice2` 新增一条断言场景页首屏
`app.occlusion.status === 'idle'` 且 `buildings === null`——**那是「懒住了」唯一的自动化证据**，
把 `ensureOcclusion()` 顺手挪到模块顶层或某个 effect 里，它当场红。

**其二，与 C++ 有意留了一处不同，并写明理由。** 几何类型不认识时，C++ **报错**（本项目的
建筑集只会有 `Polygon` 与 `MultiPolygon`，出现别的说明上游变了，要当场知道）；浏览器
**计数并跳过**——同一份文件还要交给 MapLibre 渲染，一个坏要素不该让整幅图白掉。
计数进 `stats.skippedGeometry`，照样说得出来，不是静默丢。这不违反「TS 不得有 C++ 没有的
分支」那条纪律：分支两边都有，只是处置不同。

**其三，`SceneFrame` 那条「正东走一段北向不是零」的断言在这边也写了一遍。** 纬线圈朝极点弯，
切平面上偏北 `d²·tanφ/(2N)`，1.02 km 上是 69 mm；legacy 那套在同一处给恒零。这条断言是
「本帧是真切平面、不是等距圆柱投影」的分界线——两套投影拿错了它会红。

### 实测

| 项 | 实测（macOS，原型阶段验证值） |
|---|---|
| 148 例黄金基准 | `rel ≤ 1e-9`，29 例判非视距（与 C++ 同数） |
| 真实建筑集五条射线对 C++ | 最差 **9.5e-13 / 1.3e-12**（判据 1e-9） |
| 解析计数对 C++ | 九项逐项相同 |
| 浏览器 `JSON.parse` 15.9 MB | **88 ms**（07 §2.3 调研值 72 ms，同量级） |
| 投影 344712 个顶点 | **30 ms** |
| 桶网格 | **24 ms** |
| 遮挡模块的产物体积 | **835 B**（gzip +0.57 kB） |

### 全量

web 246 → **259**（新增 13 项）、引擎 310 → **311**（新增那条五射线对拍）+ 17 ctest、
server 127、七套 e2e 355 → **356**（`slice2` 29 → 30）、标准算例 20 项、E3 回归 7 项、
跨层算例 ① 仍 **0.0138**，`scripts/build-all.sh` 全绿。**黄金基准零改动**，新增一份
`occlusion-aoi.json`；产品与航迹基准一个字节没动（本步不碰引擎的计算路径）。

### 已知边界

- **界面上仍看不到遮挡**：E3 在前端还拦着，`ensureOcclusion()` 目前没有任何调用方。
  D3-7 去置灰、接点选探测、把链路线第一次画红。
- 五条射线是**手挑的**，不是覆盖性采样。它们能钉住「两侧没分叉」，钉不住「所有配置都对」；
  后者靠 148 例的构造覆盖与 D4 的同源验收。

---

## 2026-09-18（续三）D3-7：界面上放开 E3，点选探测，链路线第一次变红（D-074 ⑰）

切片 ⑤ 的最后一件界面活。三件事：框图页把 E3 的置灰去掉并列出这一档包含哪几项；
场景页加「视距探测」，点地图任一点对焦点站算视距与刀口损耗；跑一条 E3 的链，
把 2026-09-06 就标定过对比度却一次没画出来过的非视距红 `#b91c1c` 真的画出来。

### 一、放开 E3 之前，先得把引擎那句话改掉

`ScenarioSource` 的 `prop_level` 参数描述里还写着「**需逐建筑几何，待 D3（切片 ⑤），
本版本收到即报错**」。D3-5 让引擎支持了 E3，但没人去改这句话——而它就在组件目录里，
也就是框图页那个下拉的悬停提示里。**放开置灰而不改它，用户选中 E3 时读到的仍是「收到即报错」。**

改它是一次组件目录基准变更。实测**只有这一行 description 变**（`ports` / `params` /
`port_compat` / `engine_version` 与其余 24 条逐字未变）。

这是这条线上第二次踩到同一件事：**代码改了、面向用户的那句话没改**。
第一次是 D3-5 的前端报文（07 §14ter.4），那次的处置是加一条
`assert.doesNotMatch(e3, /待 D3/)` 把措辞钉住。这次也照办了——单测里钉的是
「E3 本身不再被拦」（`assert.equal(propConflict(V({prop_level:'E3'})), null)`），
因为那句报文前后写过两版**都已不成立**的话。

### 二、前端：撤三处拦截，补两道闸，加一项检查

| 位置 | 改动 |
|---|---|
| `effects.ts` 的 `LEVEL_UNAVAILABLE` | **整个删掉**。它只有 E3 一条，删完是空表，留着就是死代码（同 D-057 的处置） |
| `effects.ts` 的 `propConflict()` | 拦 E3 那条撤掉；补闸三（E3 × 统计阴影）与闸四（E3 × 城市经验），报文与引擎 `PropagationConfig::validate()` 的两条逐句对齐 |
| `effects.ts` 的 `propView().terms` | 增 `diffraction`，位置与 C++ `combine()` 的 `included` 一致（`urban_mean` 之后、`shadow` 之前）。**E3 档每帧都声明**，与这一帧挡没挡住无关（07 §14ter.3）。卡片上于是写「E3 · 自由空间 + 建筑遮挡」 |
| `plan.ts` | 新增第 14 项 `prop_scene`「E3 有观测区域建筑几何」，**只在 E3 时出现** |
| `ChainView.tsx` 的 `LevelField` | 去掉 `disabled`，三档都可选 |

`prop_scene` 这一项**前端能查的比引擎少，这是有意的、也写明了**：前端查「场景引用的观测区域
与当前载入的数据包是不是同一个、清单里有没有建筑」，引擎查 `<scene_root>/<aoi_id>/manifest.json`
的字节哈希。前端这一层只是预检（08 §8 口径三的分工），把「数据包还没载进来」「载的是别的观测区域」
这两种在浏览器里就能看出来的情形挡在提交之前。数据包还没载入时它报不通过并说明在等，
**既不假装通过也不假装失败**（铁律 15）。

### 三、服务端一行没改——`--scene-root` 的缺省值恰好就是对的

原以为要给 `server/src/tasks/manager.ts` 的 `--run` 参数补一个 `--scene-root`
（`--scenario-track` 那一处是显式传的）。核实下来不用：`Options::scene_root` 的缺省值就是
`"data/scene"`，而引擎子进程的 cwd 恒为仓库根（D-042 ②）。实测从浏览器提交一条 E3 的链，
引擎照常找到建筑、跑完 `finished / valid`。

**记下来是因为它容易反过来错**：若将来把那个缺省值改成空串（「跳过清单哈希核对」的那档语义），
从浏览器提交的 E3 任务会当场失败，而单机跑 `cuav_run` 的人不会察觉——两条路的参数不是同一套。

### 四、视距探测：一个交互，三条克制

`web/src/scene/losProbe.ts`（算与存）+ `layers/losProbe.ts`（画）+ 工具条一个按钮 + 右栏一张卡。
点地图任一点，对**焦点站**算视距与刀口绕射损耗。

三条克制，都写在文件头注里：

1. **不新造物理**。视距与刀口损耗全部来自 D3-6 那份复算（与 C++ 同守 148 例与五条射线），
   这里只做「把场景里的量取出来、投到平面帧、把结果摆出来」。
   **卡上有意不写自由空间路损**——浏览器里没有第二份 `fspl_dB`，为一行读数新造一份
   无人守着的物理不划算。
2. **不新标定颜色**。探测线与探测点用链路线那一对（视距绿 `#15803d` / 非视距红 `#b91c1c`），
   `docs/display-route.md` 第 4 节标过对比度。画成虚线，因为它是推演不是实测。
3. **不并进态势图层**。态势画的是「某一帧真实发生了什么」，关掉那个开关就该整层清空；
   探测画的是「假设目标在这儿会怎样」，不该被它连坐。

与 C++ `link_geometry()` 逐句对应的三处：端点的 z 取**离地高差**不是 ENU 的 up（铁律 2）；
`line_of_sight = !blocked`，与损耗大小无关；刀口损耗**单程 ×1**。

**假设高度必须黏住**（这一条是写完第一版才想清楚的）。它缺省取焦点目标此刻的离地高——
探测回答的是「把这架无人机挪到这儿会怎样」，贴地和 120 米是两个完全不同的答案。
第一版每次点击都重新取，于是**楼两侧各点一次，两次用的高度不一样**（目标在飞，
两次点击之间它已经升了几十米），而画面上看不出来变过：两个答案不可比，还看不出不可比。
改成卡片上改过一次就固定住，旁边给一个「跟随目标」把它放开。

建筑几何仍是懒加载的：**第一次点击才取那 15.9 MB**（实测取 + 解析 + 建桶网格 **130 ms**），
之后每次点击都是本地几微秒。取不到就写「建筑几何没取到，视距算不了」，
**不退回「一律视距」**（铁律 15）。

### 五、「点选建筑两侧一致」怎么验才不是循环论证

这条验收（06 §9E 切片 ⑤）容易写成循环：用探测自己去找一对合适的点，再用探测去验它们。
分法是：

- **物理**由 `tests/golden/occlusion{,-aoi}.json` 两侧各守一遍（D3-3 / D3-6），探测不重复验；
- **交互与渲染—物理同源**由 `slice2` 验：楼是从**渲染图层** `aoi-buildings-3d` 上
  `queryRenderedFeatures` 挑出来的（铁律 11 的同一份 GeoJSON），两个点按它的轮廓算，
  点击是真的鼠标事件；
- 断言分两条，**一条无条件、一条存在性**：楼后那一侧**所有候选楼**都必须非视距
  （视线在楼那儿只有 30–40 m，候选楼都高过 60 m，这是无条件成立的）；楼前视距只要求
  **至少一栋**候选楼成立（楼前开不开阔取决于别的楼，不是无条件的）。
  写成「至少一栋」是照实说，不是放宽判据。

`web/src/scene/losProbe.test.ts` 另有一条同名用例，走同一套办法但不经浏览器。两条都要真实
建筑集，**缺数据时明说跳过、不当作通过**（D-073 ③）。

### 六、链路线第一次变红

07 §1 第 2 行记着「非视距红 `#b91c1c` 在 D2-4 标定过对比度，至今一次没画出来过」。
要让这句话作废，只有跑一条 E3 的链。`slice2` 于是在场景编辑那一段之前插了一次：
把 `chain-demo-01-e3.json` 的时长改成 3 秒存进服务端、从界面提交、回场景页断言
`links[0].los === false` 且 `cuav-link-line` 渲染出的要素 `los` 全是 `false`，跑完即删。

3 秒这个窗口不是随手取的：demo-01 起飞点外 25 m 就有一栋 62 m 的楼，**头 7.2 秒全程非视距**
（D3-5 实测的视距窗口是 t 7.2–26.4 s），所以短窗口里这条线一定是红的。

### 实测

| 项 | 实测（macOS，原型阶段验证值） |
|---|---|
| 楼两侧一致（浏览器里） | 楼 `35185865451587`，62 m、143 m 外：**楼前视距、楼后 37.1 dB**、侵入 24.7 m |
| 楼两侧一致（单测，四十栋候选） | 同一栋楼；四十栋候选的**楼后无一视距** |
| 第一次点击取建筑几何 | **130 ms**（取 + 解析 + 建桶网格），47662 栋 |
| E3 的 3 秒 demo-01 链（从界面提交） | `finished / valid`，`links[0].los === false`、路损 120.65 dB、距离 159.7 m |
| 组件目录基准 | **只变一行 description** |
| 前端产物 | 1576.81 kB（视距探测这一整套 + 卡片 + 图层） |

### 全量

web 259 → **263**（新增 4 项：`losProbe.test.ts` 三条 + `prop_scene` 一条）、
引擎 **311** doctest + 17 ctest、server 127、
七套 e2e 356 → **368**（`slice2` 30 → 42，新增 12 条）、标准算例 20 项、E3 回归 7 项、
跨层算例 ① 仍 **0.0138**，`scripts/build-all.sh` 全绿。

**顺带改掉三条已经不成立的旧断言**（不是失败，是事实变了）：
`scene-smoke` 的图层数 76 → **78**（探测的线与点两层）；
`slice4` 的「E3 保留在下拉里但置灰」→「三档都可选」；
`slice8` 的「工具条只露测量」→「观察组两件：测量 / 视距」。

### 已知边界

- **视距探测只算焦点站一条**。多站场景下要看别的站，得先在右栏或对象树里选中那个站。
  一次算 K 条、右栏出一张表是更好用，但那会让这张卡从「详情一次只看一个」变成一张表格（D-062）。
- **探测点的高度是一个假设，不是场景里的任何一架无人机的真实高度**。卡上把它写成可改的输入
  就是为了这一点——藏起来就会被读成「这架机现在通不通」。
- **单刀口的老边界照旧**：多楼连续遮挡只取侵入最深的那一栋，偏乐观；材质不穿透；平地假设。
  八条声明过的简化在 `models/channel/README.md` §3.6。
- 下一步 **D3-8**：MSVC / GCC 构建（铁律 16，C++ 侧首次在目标平台验证），之后是 D4 同源验收。
