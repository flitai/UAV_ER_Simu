# 工作过程日志（804 C-UAV 无人机电子信号侦察仿真系统）

本文件记录每个任务节点的实际过程、核实到的事实和由此产生的待办。写作要求：用完整句子，
不使用未在本文件内定义过的内部简称，每个结论都写清楚是"实测得到"还是"引自文档"。

新条目追加在文件末尾，按日期正序排列。文件顶部的「当前状态快照」在每次追加后同步更新。

---

## 当前状态快照（更新于 2026-09-05）

**所处阶段**：P0「需求冻结与数据摸底」，进行中。**整体处于原型系统阶段**（D-028）：
公开数据集只用于验证技术途径、软件架构与功能实现，**其量化结论一律不作交付指标**，
甲方数据到货后按同样脚本重跑修订。

**代码进度**：工程骨架已建立（06 备忘录 §2 的 S-1 至 S-7 全部完成），`web`、`server`、
`engine`、`geo`、`scene` 五个子工程均可空跑，`scripts/build-all.sh` 全绿（2026-09-04 路线图修订后实测：web、server、
engine 单测、geo 冒烟、路径与 ASCII 检查全部通过；D1 条目记录的那项引擎组件测试失败已不复现），主链仍无业务功能。
**已有实际功能的代码**：`tools/` 的 IQ 转换脚本与摸底工具（DS-2/DA-2、DS-5/DA-5）、
`algos/reference/` 的能量检测器参考实现（含检测概率解析式）与 DS-6、DS-7 两个标定脚本，
合计 49 项单测全绿，均已在两个数据集的真实文件上跑通。骨架提交 `8fe8e85` 时纳入版本控制的文件
75 个、470 KB（此前快照写的"692 KB"有误，已按 `git ls-tree -r -l` 更正）。

**数据进度**：已获得两个公开数据集，均已完成摸底核实，但都尚未转换格式、尚未入库、
许可条款尚未确认。

| 数据集 | 本地规模 | 关键特征（均为实测核实） | 状态 |
|---|---|---|---|
| DroneRFb-DIR | 64 GB / 4690 片（全集） | 单通道 80 兆样点每秒；固定 10 米；摆放不动；出版方已划分训练测试集；62 片纯背景；正交性近乎完美 | 已摸底 |
| DroneRFa | 21 GB / 22 片（全集约 560 GB） | 双通道 100 兆样点每秒；三档距离区间；飞行中 5–15 米每秒；24 型 7 品牌；正交支路幅度不平衡 0.54–0.95 | 已摸底并完成路损验证 |

DroneRFa 上得到的量化结果（DA-6 已完成重算，见同日第四条日志）：**2.4 吉赫兹城市户外链路的
路损指数约为 2**——`T0010` 六片给出 n = 1.93（逐对组合极差 0.12、标准差 0.05；三档最小二乘
1.95），`T0011` 三片给出 1.69，两机型宽基线一致，验证了路损与机型无关。**但主导不确定度是
距离区间标注本身**：真实距离比只能确定在 2.0 至 7.5 之间，同一实测降幅对应 n = 1.29 至 3.75，
比片间抖动大一个数量级以上，所以只能冻结粗档结论。片间重复性 0.3 至 1.3 分贝；底噪 −24.3
分贝经三条独立路径互相验证；5.8 吉赫兹通道在 7 个文件上突发帧占比全部为 0.0%，该频段放弃。

**两个数据集已全部转换入库**：DroneRFb-DIR 4690 个产物 70 GB、DroneRFa 24 个产物 14 GB，
4714 份清单逐一核对，回读对拍失败 0、质量状态异常 0（全部 `degraded`，原因一致：采集参数
来自论文而非设备记录）。格式规范 `docs/iq-format.md` 第 3、4 节已定稿。
**验收子集已冻结**：DroneRFb-DIR 2487 片（出版方测试集减去 26 片已用过的），含 6 架从未在
训练集出现的个体 1580 片；**DroneRFa 没有可用验收集**，本地 22 片全部已用于拟合，只能从
后续补下的片里划并当天冻结。清单 `data/iq/measured/holdout.manifest.json`。

**显示子线进度**（更新于 2026-09-04）：观测区域定为北京亚运村周围（用户指定，D-021），范围经实测比较后
由用户定为 20 × 20 km（`116.288,39.900,116.522,40.080`，D-024），区内 50201 栋建筑（按 id 去重）；底图与 DEM 按用户"128 GB
没有问题"的指示整份作为共享资产使用（D-022），并已**整份拷入本项目自持**（约 145 GB，D-023），
`data/basemap/{planet.pmtiles, dem/}` 是真实文件而非软链，本项目运行时不再依赖 Airports 工程的路径。
副本与来源逐字节一致（底图全文件 sha256、DEM 索引哈希两项均已核对）。
**建筑数据的两项实测**（D-025）：z15 要素带稳定 id，跨瓦片合并按 id 做；但瓦片 `height` 字段把真实标注与
planetiler 按层数推导的值混在一起，区内 76.9% 无高度、19.4% 是推导值、真实标注上界仅 3.7%，干净的来源标注
只能靠建库阶段拉 Overpass 原始标签。
**D0 场景数据包已完成**（2026-09-04，七步全过）：`buildings.geojson` 47582 栋、15909168 字节；高度来源
`osm:height` 1.74% / `osm:levels` 19.59% / `est:area` 78.55%；与原始 OSM 按编号精确对齐 99.72%、面积中位差
+0.008%，与底图自带建筑层逐像素交并比 0.993 至 0.999。数据包入口 `data/scene/beijing-yayuncun/manifest.json`。
**几何可用，高度不可用于验收指标**（近八成是估算值）。下一步是显示子线 D1。用 Airports 查看器在无头浏览器里实测：
包内底图经 Range 服务可加载并渲染出亚运村一带的道路、建筑拉伸与标签，切片、整份来源文件、包内副本三者的
画布像素逐位相同。

**已经拿到的、只有真实数据才能给出的结论**（详见 2026-09-04 两条日志）：真实城市 2.4 GHz
背景下，按白噪声假设设的门限虚警率超标 232 倍；同样虚警率要求下，能检出的最弱目标要比
白噪声环境强 23.6 至 38.9 分贝，频段选择本身值 15 分贝；门限在一半背景上标定换到另一半就
失效 7 倍，故静态门限不成立。

**最大的未决变量**：甲方（用户方）自己的实测 IQ 数据仍未提供，八项摸底问题只在公开数据集
上回答过。两个公开数据集都**没有绝对功率标定**（缺馈线损耗与一次标定常数），因此分贝毫瓦
类验证一律做不了；也都**没有视距与非视距标签、没有建筑几何与高度**，因此跨层一致性算例
② 建筑遮挡与 ③ 双径仍然不可做。这两块只能由甲方自采数据或补采计划补上。

**显示子线 D1 已完成**（2026-09-04）：前端可离线渲染观测区域的底图、山体阴影与建筑三维拉伸。
底图样式与 Airports 逐层等价（黄金基准守着），并排比对容差 8 内 99.83%。应用服务实现了 PMTiles 依赖的
HTTP Range（30 项测试），端到端 14 项断言全过。下一步是 D2 态势图层。

**路线图已于 2026-09-04 修订为 v1.1**（见同日「路线图修订」条目）：用户定后续重点为人机交互界面、
真实地理环境下的场景与目标活动生成、B/S 框图模型、波形与频谱可视化、后端计算，并追问 MATLAB 的
优势是否用上（此前没有：参考实现全为 Python，P1-8 未排期）。06 备忘录新增 B 桥接、U 界面、G 场景、
M MATLAB 四条步骤线与五个纵向切片；P1-5、P1-6 撤销，P1-8 由 M-2 兑现，D2 并入 G-5。四份规范骨架
已落地（框图、场景、显示产品、组件目录，字段已冻结）。

**当前位置与下一步**（2026-09-05）：切片 ①「一条链到频谱」的引擎侧已全部完成：B-1 注册表与目录、P1-4a 频谱分析、
B-3 观察者与观测点产品、B-2 框图 JSON 装载器（D-040）、**B-4 `cuav_run` 可执行**（`--catalog` 生成目录黄金基准、`--validate` 只校验、
`--run` 跑通切片 ① 框图并输出带序号的 JSON 事件与 `events.jsonl` 镜像，D-041）、M-1 `matlab/` 骨架与 M-4 首个三方互证；
引擎 57 项单测 27388 项断言 + 3 项可执行冒烟全绿。下一步是服务侧 B-5 任务管理器（`spawn cuav_run`、`data/runs/<task_id>/`、写解析旁挂、
`POST /api/v1/tasks`、`GET /api/v1/components`），再 B-6 WS 补取、B-7 视窗抽取，然后前端 U-1 / U-3。09 报告已升 v1.3。
公开数据集这条线已全部做完；**跨层一致性算例 ① 在两批独立数据上都通过**（检出率偏差 0.0016–0.0050，容差 0.05–0.10），
引擎侧整链复现待切片 ④。待用户出面的事项：甲方实测数据。Coder 产物许可已于 2026-09-04 确认不受限；DS-1 / DA-1 两个公开数据集的
许可已于 2026-09-05 由用户确认无任何使用与署名约束。

---

## 日志条目

### 2026-09-03 · 公开数据集 DroneRFb-DIR 摸底

#### 1. 本次做了什么

用户把一个公开数据集放进了项目根目录，路径是 `Datasets1-DroneRFb-DIR/`，并询问在拿到
甲方实测数据之前能否先用这个数据集推进项目。

本次工作是对该数据集做实际核实（不是照抄它自带的说明文件），具体包括：遍历目录统计文件
数量与体积、读取标签文件统计各类样本分布、用 HDF5 库打开 `.mat` 文件查看内部结构、抽取
若干文件计算数值统计量与功率谱、以及从随附论文中提取采集参数。

核实所用环境：macOS，`uv run --with h5py --with numpy python`。数据集本身没有被修改。

#### 2. 这个数据集是什么（以下均为实测核实结果）

数据集全名 DroneRFb-DIR，出自浙江大学，论文发表于《电子与信息学报》2025 年第 47 卷第 3 期，
论文数字对象标识符（DOI）为 `10.11999/JEIT240804`，数据托管于 SciDB 平台，数据集编号
`84cf9101e739402784b1396783881202`。其用途是"非合作无人机个体识别"，即不仅要判断有没有
无人机、是什么型号，还要区分同型号的不同架次个体。

| 核实项 | 实测结果 |
|---|---|
| 总容量 | 64 GB |
| 文件数量 | 4690 个，其中训练集 2177 个、测试集 2513 个 |
| 文件格式 | MATLAB v7.3 格式，其实体是 HDF5，内部用 gzip 压缩 |
| 文件内部结构 | 根节点下只有两个数据集，名为 `I` 和 `Q`，各自形状为 1 行 4000000 列，数据类型为 32 位浮点数 |
| 元数据 | **完全没有**。文件内既无采样率、无中心频率、无时间戳，也无接收机增益等任何属性 |
| 数值的本质 | 抽查 7 个文件（训练集 4 个、测试集 3 个），全部样点都是 2 的负 15 次方的整数倍，且绝对值不超过 32767。这说明底层是 16 位整数，只是以浮点形式存放 |
| 信号电平 | 峰值在满量程以下 15.4 分贝到 30.2 分贝之间，有效值在满量程以下 39.1 分贝到 47.2 分贝之间。没有出现削顶（饱和） |
| 正交支路质量 | 同相支路与正交支路的标准差之比在 1.0000 到 1.0014 之间，互相关系数接近 0。说明正交性良好，没有明显的镜像干扰或直流泄漏 |
| 突发结构 | 以 51.2 微秒为一帧统计，无人机信号文件中约有 10% 的帧功率高于中位数 6 分贝以上；纯背景文件中几乎没有高于中位数 10 分贝的帧 |

以下参数**只写在论文里，数据文件本身不携带**，引用时必须标明来源是论文而非设备记录：

| 参数 | 论文给出的值 |
|---|---|
| 接收设备 | USRP-2955 软件无线电设备 |
| 采样率 | 80 兆样点每秒 |
| 中心频率 | 2.44 吉赫兹（覆盖 2.4 到 2.48 吉赫兹） |
| 每个片段时长 | 50 毫秒（与 4000000 个样点、80 兆样点每秒相互吻合） |
| 收发距离 | 固定 10 米 |
| 采集姿态 | 无人机与其遥控器放在同一位置，**摆放不动，没有飞行**；接收机位置固定 |
| 视距与非视距的差别 | 论文称两者信噪比大致相差 20 分贝 |
| 无人机信号构成 | 图传信号带宽 20 兆赫兹、频率位置接近 2.48 吉赫兹；飞控信号逐帧跳频，但带宽与持续时长固定 |
| 片段之间的时间关系 | 论文明确说明：每个片段内部的采样点时间连续，但**片段之间时间不连续** |

样本的分类情况：共 6 种机型，每种 3 架个体，外加 1 类纯背景（现场没有无人机开机）。
机型对应关系为 A 代表大疆 Mavic 3 Pro，C 代表 Mini 2 SE，D 代表 Mini 4 Pro，E 代表 Mini 3，
F 代表 Air 3，G 代表 Air 2S，B 代表背景。训练集只含每种机型的第 1、2 架（共 12 架）加背景；
测试集含全部 18 架加背景，也就是多出 A3、C3、D3、E3、F3、G3 这 6 架**从未在训练集中出现过
的个体**。纯背景片段合计 62 个（训练集 37 个、测试集 25 个）。

#### 3. 这个数据集接在本项目仿真链条的哪一个位置

本项目的仿真链条按先后顺序可以展开成十个环节：

```
① 波形模板与业务活动   ② 复基带波形生成   ③ 时变复信道（把传播参数施加到波形上）
                                    ↓
④ 样本级噪声与干扰注入   ⑤ 接收机采样链（噪声系数、增益、自动增益控制、量化）   ⑥ 模数转换与数字下变频
                                    ↓
                    ★ S4 观测点：数字下变频之后、信道化之前的宽带复基带 IQ ★
                                    ↓
⑦ 信道化   ⑧ IQ 观测量提取（功率谱、瀑布图、能量检测、突发统计、参数估计）
                                    ↓
⑨ 信号级裁决（检测、特征提取、识别）   ⑩ 批量蒙特卡洛统计（检测概率曲线、混淆矩阵）
```

DroneRFb-DIR 的每个文件，本身就是 ★ 这一点的产物：USRP 设备已经把 2.44 吉赫兹的射频信号
下变频到复基带，带宽 80 兆赫兹，且没有做信道化。这与 CLAUDE.md 铁律第 4 条定义的主产品
（数字下变频之后、信道化之前的宽带复基带 IQ）完全一致。

因此它的接入位置是确定的：**从 ★ 往右，即第 ⑦ 到第 ⑩ 环节**。

反过来说，它无法用于验证第 ① 到第 ⑥ 环节。原因不是数据质量不好，而是逻辑上不成立：
这批数据本身就是①到⑥的运算结果，而产生它的输入参数（真实距离、各项损耗、发射功率、
接收增益）恰恰是这个数据集没有记录的。用一个结果去验证产生它的过程，前提是知道过程的
输入，这里不满足。第 ① 到 ⑥ 环节仍然只能用合成数据验证，即自行设定参数、生成 IQ、
再检查输出是否符合解析公式。

#### 4. 它能支撑什么，不能支撑什么

**能支撑的（因为只需要"IQ 本身长什么样"就够）**

- 第 ⑦ 到 ⑩ 环节的算法验证，包括频谱与瀑布图、能量检测、突发统计、参数估计、识别。
- 用真实文件尺寸对读取与流水线做压力测试。单个文件 16 兆字节、对应 50 毫秒，总量 64 GB，
  这种规模能压出分块大小、内存映射、背压、按视窗抽取降采样等工程问题；用几百千字节的
  合成文件永远压不出来。
- 提供真实的城市电磁背景。原计划的算例用的是高斯白噪声，而真实的 2.4 吉赫兹城市环境里
  充满 WiFi 与蓝牙的突发信号，是非平稳、非高斯的。这 62 个纯背景片段可以直接用作背景，
  叠加我们自己合成的目标信号——目标是自己造的，所以信噪比等"标准答案"完全可控，而背景
  是真的。这个组合恰好绕开了该数据集"上游参数未知"的短板。
- 作为 IQ 文件格式规范的第一个真实样本。它还提供了一条反面教材：采集参数必须外挂在元数据
  里，否则一旦与论文分离，数据就无法解释。

**不能支撑的（因为需要知道"这段 IQ 是怎么来的"）**

| 做不了的事 | 具体原因 |
|---|---|
| 任何关于绝对功率的验证 | 文件里的数字是模数转换器的量化码，不是分贝毫瓦。要换算成分贝毫瓦，需要天线增益、馈线损耗、接收机增益设置，再加一次标定，这四样一个都没有。因此只能比较"这片比那片强 6 分贝"，无法给出"接收功率是负 70 分贝毫瓦"。而链路预算校验（发射功率减去路径损耗减去遮挡损耗等于接收功率）要求等式两边都是绝对功率，所以做不了 |
| 任何关于几何的验证 | 全部 4690 个片段是同一个几何：距离固定 10 米、接收机固定、无人机摆放不动。数据集里根本不存在"几何变化导致信号变化"这一维度的信息，而这正是本系统要模拟的核心因果关系。因此建筑遮挡的移植验证（里程碑 D3、D4）用不上它 |
| 双径传播的验证 | 没有高度信息，没有距离变化，双径特征无从体现 |
| 多普勒与运动估计 | 无人机没有飞行，不存在多普勒 |
| 跨片段的时间分析 | 片段之间时间不连续且没有绝对时间戳。单个片段内部做的事（频谱、突发检测、跳频跟踪）都成立；跨片段做的事（长时间活动规律、多片拼接、行为模式）都不成立 |
| 覆盖甲方关心的目标集 | 只有 2.4 吉赫兹一个频段，机型全部是大疆消费级无人机，覆盖不到 5.8 吉赫兹与其他机型 |
| P0 阶段的冻结事项 | 观测区域范围、甲方接收机的实际参数、首批频段与机型清单，一个都没有解决 |

**一处需要修正的说法**：遮挡相关的验证并非"完全用不上"。论文给出了"视距与非视距信噪比
相差约 20 分贝"这一个数字，可以用作量级合理性检查——我们的建筑遮挡模型在类似的城市短距
场景下算出的附加损耗，应当落在十几到二十几分贝这个量级。若算出 3 分贝或 60 分贝，说明
模型或参数有问题。这属于"体检"而非"校准"，但成本几乎为零，值得做。

#### 5. 命名与文档中的陷阱（务必记住，极易搞反）

| 陷阱 | 说明 |
|---|---|
| `IN` 与 `OUT` 的含义与直觉相反 | 文件名中的 `IN` 代表**视距**采集，`OUT` 代表**非视距**采集。不是室内与室外 |
| `S0` / `S1` / `S2` 不是场景编号 | 它是**采集时间序号**。文件名格式为「机型与个体编号 + 视距标志 + 采集时间序号 + 片段序号」，例如 `A1_IN_S0_slice_2.mat` |
| 论文中的一处笔误 | 论文第 2.2 节称数据"存储为单精度浮点型(2字节)"。单精度浮点是 4 字节，实测也确认文件中是 32 位浮点。推测"2 字节"指的是原始的 16 位整数 |
| 测试集的答案是公开的 | `test_labels.txt` 第一列是带标签的原始文件名，第二列是测试集编号 0 到 2512。也就是说测试集的真值可以直接从这个文件读出 |
| 目录名与数据集名不符 | 解压出来的目录叫 `twin_droneRFa`，与数据集名 DroneRFb-DIR 对不上。来源不明，入库时应改名 |

#### 6. 五项可立即开工的任务（不依赖甲方数据）

**第 1 项：打通读取与显示的完整通路**
读取一个 `.mat` 文件，转换为复数 16 位整数交织格式，送入引擎做分块流读取，输出功率谱与
瀑布图，再由前端按视窗抽取后显示。
验收标准：屏幕上能辨认出论文图 3 标注的几样东西——接近 2.48 吉赫兹处一块 20 兆赫兹宽的
图传信号、随机跳频的窄带飞控信号、WiFi 的 20 兆赫兹驼峰、蓝牙的窄条。能看出来即说明
读取、解码、抽取、显示这条通路是通的。

**第 2 项：核对观测量提取的正确性**
论文给出了两个可核对的量：图传信号带宽 20 兆赫兹且频率接近 2.48 吉赫兹；飞控信号逐帧
跳频但带宽与持续时长固定。用我们的参数估计组件跑出中心频率、带宽、突发时长，与这两个量
比对。
验收标准：估计出的图传带宽与 20 兆赫兹的偏差在可接受范围内，频率位置落在 2.48 吉赫兹
附近。这是难得的**外部真值**——不是我们自己造的标准答案，而是第三方标注的。对不上就说明
我们的估计器有问题。

**第 3 项：在真实城市背景下标定虚警率（建议最先做）**
用 62 个纯背景片段测量能量检测器的虚警率。
预期结果：按高斯白噪声假设推导出的检测门限，在真实城市背景上虚警率会明显超标，因为真实
背景中的 WiFi 与蓝牙是突发的、非平稳的、非高斯的。
验收标准：给出超标的具体倍数，并给出修正后的门限。这个数字只有真实数据能得到，是"合成
数据会给出乐观结论"这一判断的第一个硬证据。

**第 4 项：跨层一致性算例的真实背景版本**
原计划的算例是"自由空间加高斯白噪声，比对功能级解析检测概率公式与 IQ 级蒙特卡洛结果"。
升级为"真实背景加合成目标"：目标信号由我们自己生成（信噪比、带宽、突发时长等参数完全
可控），背景用真实数据。
验收标准：产出真实背景下的检测概率曲线，与白噪声版本的曲线并排比较，给出两者的差值。

**第 5 项：性能压力测试**
80 兆样点每秒属于 CLAUDE.md 中"仅做文件兼容与短窗"的那一档（常用档是 5 到 50 兆样点每秒）。
用它测量端到端吞吐量与延迟。
验收标准：给出兆字节每秒的吞吐数字与端到端延迟，据此判断准实时模式的能力上限。

依赖顺序：第 1 项是第 2、3、4 项的前提；第 3 项与第 4 项之间，第 3 项在前（先有可信的门限，
再谈检测概率曲线）；第 5 项可与其他任务并行。

#### 7. 入库之前必须完成的事项

1. **格式转换**。放到 `data/iq/measured/dronerfb-dir/`，转换为复数 16 位整数交织格式，
   并配一份符合 SigMF 规范的外挂元数据。转换脚本中必须加断言，检查每个样点确实是 2 的
   负 15 次方的整数倍（本次抽查 7 个文件全部通过），转换完成后逐位回读对拍，证明无损。
2. **元数据标明来源**。采样率、中心频率、设备型号这些参数来自论文而不是数据文件，
   元数据中必须标注来源为论文，不能伪装成设备自带记录。时间连续性标志要写明"片段内部
   连续、片段之间不连续"。
3. **冻结验收子集**。直接采用出版方划分好的测试集 2513 个片段作为验收子集，另外把
   A3、C3、D3、E3、F3、G3 这 6 架训练集中从未出现的个体单独标出，记录文件哈希。
   出版方已划分好的好处是：不必自己划分，也就不会在划分时不小心把同一架次的数据同时
   放进训练集和验收集。
4. **文件名改为纯 ASCII**。目录名 `twin_droneRFa` 与随附的中文文件名 PDF 都要改。
5. **确认许可条款**（唯一可能影响能否使用的硬性风险）。SciDB 的数据集页面是动态渲染的，
   本次未能取到许可文本。内部研发使用并引用论文 DOI 应无问题，但**能否随交付系统一起
   分发必须先确认**。

#### 8. 本条目产生的待办

- [ ] 确认 DroneRFb-DIR 的许可条款，特别是能否随交付系统分发（阻塞项，需人工访问 SciDB 页面）
- [ ] 编写格式转换脚本，含无损断言与回读对拍
- [ ] 冻结验收子集并记录哈希
- [ ] 在 CLAUDE.md 的「资源清单 · 只读边界 · 复用边界」表中增加本数据集一行
- [ ] 在 CLAUDE.md 的里程碑表中更新 P0 状态，并考虑是否需要新增一条决策记录
- [ ] 甲方实测数据与 8 项数据摸底问题仍未回复，继续跟进

---

### 2026-09-03 · 工程骨架 S-1 至 S-7

#### 1. 本次做了什么

按 06 备忘录 §2 的步骤表建立工程骨架。S-3（建立本文件）在上一个任务节点已完成，本次做了
其余六步。每一步都做到「能实际跑起来」，不是只建空目录。

#### 2. 产物

| 步骤 | 产物 | 结果 |
|---|---|---|
| S-1 | 目录树，全部纯 ASCII 无空格 | 共 40 个目录 |
| S-2 | `.gitignore` | 用 `git check-ignore` 逐条验证过忽略与放行规则 |
| S-4 | `docs/` 八份文件 | `README`、`iq-format`、`api-versions`、`scene-package`、`display-route`、`concept-model-layers`、`concept-model-extensions`、`decisions/README`，合计 442 行 |
| S-5 | `tests/` 五级目录与回归清单 | `tests/regression/README.md` 列全 04 §15.2 十二项与跨层一致性四项，并逐项标注公开数据集能否支撑 |
| S-6 | 五个子工程 | `web`、`server`（npm）、`engine`、`geo`（CMake）、`scene`（uv） |
| S-7 | 两个脚本 | `scripts/check-ascii.sh`、`scripts/build-all.sh` |
| 附加 | 每个顶层目录一份 README | 写明该目录的用途、适用的铁律、已冻结项与未决项 |

纳入版本控制的共 75 个文件、692 KB。已用 `git diff --cached` 逐项确认：64 GB 的公开数据集、
1.5 GB 的只读参考快照、`node_modules`、`dist`、`build`、`.DS_Store` 全部未进入暂存区。

#### 3. 实测验证结果（不是"应该能跑"，是实际跑过）

| 子工程 | 命令 | 结果 |
|---|---|---|
| `web` | `npm run build` | 通过。类型检查加打包，产物 190.63 KB（gzip 后 60.20 KB），耗时 49 毫秒 |
| `server` | `npm run build` | 通过 |
| `server` | 启动后请求 `/api/v1/health` | 返回 200 与 `{"status":"ok",...}`；未知路径返回 404 |
| `engine` | `cmake --build` 加 `ctest` | 通过，1 个测试全过 |
| `geo` | `cmake --build` 加 `ctest` | 通过，1 个测试全过 |
| 全部 | `scripts/build-all.sh` | 全绿 |
| ASCII | `scripts/check-ascii.sh` | `web/dist` 与 `data/scene` 均合规 |

#### 4. 核实过的引用（工作协议 §13.2 要求）

**Airports 的行号**（`/Users/zhiyu/CC/Airports/index.html`，共 1883 行）：CLAUDE.md 引用的
七处全部存在——`protomapsStyle()` 在第 810 行，山体阴影在 1218 行，`addBldg3d()` 在 1296 行，
`newMap()` 在 1375 行，`probePmBldg()` 在 1281 行，`window.__probe` 在 1769 行，
`localglyphs://` 在 726 与 729 行。已抄录进 `docs/display-route.md`。

**依赖版本**：全部在 npm 源上核实存在后才写入，且写死精确版本号不用范围前缀。

| 包 | 锁定版本 | 上游最新 | 说明 |
|---|---|---|---|
| `maplibre-gl` | 5.24.0 | 6.7.0 | 决策 D-008 有意锁定，与 em-demo 保持同版本 |
| `pmtiles` | 4.4.1 | 4.5.0 | 同上 |
| `react` / `react-dom` | 19.2.8 | 19.2.8 | — |
| `vite` | 8.2.2 | 8.2.2 | — |
| `typescript` | 6.0.3 | 7.0.2 | 与 em-demo 的 6.0 对齐。上游已到 7.x，属低风险可提升项 |

#### 5. 遇到的问题与处理

**TypeScript 6 下 `server` 的 Node 全局类型不自动生效。** 装了 `@types/node` 之后，
`tsc` 仍报找不到 `process`、`console` 与 `node:http`。在 `tsconfig.json` 里显式写
`"types": ["node"]` 后通过。记下来是因为 `engine`、`geo` 之外的任何新 Node 工程都会踩同一个坑。

#### 6. 需要注意的风险

1. **开发机的 Node 是 25.8.2，不是长期支持版本**（主版本号为奇数）。两个 `package-lock.json`
   都是在这个版本下生成的。交付平台基线冻结 Node 长期支持版本之后，必须重新生成并重新验证。
2. **全部构建只在 macOS 上验证过**。按决策 D-015 与 D-016，macOS 结果**不作为验收依据**。
   MSVC 与 GCC 的首次构建验证分别安排在 D3-4 与 P2 阶段。
3. **git 仓库还没有第一次提交**。75 个文件目前只是暂存状态。

#### 7. 偏离步骤表之处（已登记进 06 备忘录 §14）

`.gitignore` 在步骤表列出的排除项之外，增加排除了 `Datasets1-DroneRFb-DIR/`。原因是该
公开数据集 64 GB，在步骤表编写时尚未放入项目根目录。

#### 8. 本条目产生的待办

- [ ] git 第一次提交（等待确认后执行）
- [ ] Node 长期支持版本冻结后重新生成两个 `package-lock.json` 并重新验证
- [ ] `server/src/range.ts` 实现 HTTP Range 文件服务（D1 阶段的前置条件）
- [ ] `docs/` 各份规范中标记「待写」的章节，随对应实现阶段补齐

---

### 2026-09-03 · 按公开数据集入手的事实修订 06 备忘录

#### 1. 起因

06 备忘录全篇有一个隐含前提：**手上没有任何真实 IQ**。因此凡涉及实测的步骤，措辞一律是
「数据到位后」，位置一律排在最后。2026-09-03 入手 DroneRFb-DIR 之后，这个前提只对了一半：
甲方数据仍未提供，但已有一批可解析的真实 S4 观测点 IQ。

**改的是顺序，不是技术路线。** 阶段 0 的退出条件与 P1 的退出条件均未变动。

#### 2. 改了哪九处

| # | 位置 | 改动 |
|---|---|---|
| 1 | §0.5 | 实测数据行的阻塞对象由 P1-9 改指 P1-9b |
| 2 | §0.5 | 新增「DroneRFb-DIR 许可条款」一行；表后新增**防线一**：不得因为有了公开数据集就放松索取甲方数据 |
| 3 | §3 | 阶段 0 的「实测数据」行：摸底工具 `tools/iq_survey` 解除阻塞，可先在公开数据集上开发验证，状态改 `[~]` |
| 4 | §9 P1-1 | 拆成两批定稿：容器、样点编码与元数据结构现在可定；功率与标定口径单列，仍被甲方数据阻塞。状态改 `[~]` |
| 5 | §9 P1-9 | 拆成 P1-9a（公开数据集回放，不阻塞）与 P1-9b（甲方数据回放，仍等数据） |
| 6 | §11.2 | 标题由「入库当天必做」改为「任何 IQ 数据集入库当天必做」，并补充质检结果必须落到四态语义、元数据缺失一律判 `degraded` |
| 7 | §11.3 | 参与时点拆成两条并行数据线：公开数据集线（现在起）与甲方数据线（待到货）；新增 §11.4 |
| 8 | §12 | 「实测等效」层注明公开数据集对跨层一致性四项算例的支撑范围 |
| 9 | §14 | 补一条修正记录，说明本次调整的原因与范围 |

#### 3. 新增的 §11.4

新增七个编号步骤 DS-1 至 DS-7，使数据集接入进入「每完成一步更新状态」的机制：
许可确认、转换脚本、外挂元数据、冻结验收子集、摸底工具跑通、虚警率标定、真实背景加合成
目标。除 DS-1 需要用户确认许可外，其余六步均不需要用户输入。

同时写入两项内容：

**转换体积与子集策略。** 全部 4690 片转成复 int16 交织需 75 GB（每片 16 MB），加原始
64 GB 共 139 GB。先转最小可用子集约 200 片、约 3.2 GB（62 片全部背景 + 训练集每个个体
5 片 + 测试集抽样 80 片），管线跑通后再按需扩大。

**防线二：该数据集不得与任何地理场景绑定展示。** 它没有位置信息，摆到地图上任一地点都是
编造，违反铁律 14。附带说明：它采自杭州，而临时观测区域用的是西安，本来也对不上。

#### 4. 顺手修掉的一处原有缺陷

§9 的 P1-1 行内 `|Δf| + B/2 + 保护带 < Fs/2` 中的竖线未转义，会把该表格行切成 6 列而不是
4 列，导致表格渲染错位。改成 `\|Δf\|` 后全篇表格列数一致（已用脚本逐表校验）。

这是备忘录原文就有的问题，不是本次改动引入的。

#### 5. 一个已核实的判断依据

把 DroneRFb-DIR 对着 04 §10.6 的八项质检逐项核对：八项中六项可跑通，两项（第 1 项元数据
一致性、第 7 项元数据必填项）必然失败，因为该数据集文件内没有任何元数据。

这正是把 DS-5 排在前面的理由：**它是比「完美数据集」更好的第一个测试用例**，因为它强制
`tools/iq_survey` 走「缺元数据判 degraded 而不是崩溃、也不是拿默认值顶上」这条路径，
恰好是铁律 15 要求的四态语义。若第一个测试数据集是完整的，这条路径就永远没被验证过。

#### 6. 本条目产生的待办

- [ ] DS-1 许可确认（需用户出面，阻塞该数据集能否进入交付物）
- [ ] 其余待办并入 06 备忘录 §11.4 的 DS 步骤表，不在此重复

---

### 2026-09-03 · 公开数据集 DroneRFa 摸底、路损验证与补数清单

#### 1. 本次做了什么

用户在同一天下午把第二个公开数据集的一部分放进项目根目录，路径是 `Datasets2-DroneRFa/`，
并询问它与前一个数据集的差异、对本项目是否更有价值。此后用户分四批补充文件，每批之后
都做一次实测核实与"还缺什么"的判断。

核实所用环境：macOS，`uv run --quiet --with h5py --with numpy python`，论文用 `pdftotext -layout`
抽取。数据集本身没有被修改（例外见第 7 节，有两片文件被误删）。

本条目是这一路工作的补记。工作发生在 2026-09-03 中午到下午，当时未按 D-006 的纪律当场落盘，
会话末尾因服务端 529 错误中断。本条目的所有数字均从当次会话记录中逐条取回，不是凭印象重写。

#### 2. 这个数据集是什么

数据集全名 DroneRFa，与前一个数据集 DroneRFb-DIR 出自同一课题组（浙江大学）的前期工作，
论文数字对象标识符为 `10.11999/JEIT230570`，数据托管于 SciDB 平台，数据集编号
`34f0a91e8a544904998b8fdc44477380`。用途是低空无人机的检测与型号识别（不是个体识别）。

以下是**实测核实结果**：

| 核实项 | 实测结果 |
|---|---|
| 本地体积与片数 | 21 GB，22 片（用户分四批补充，另有两片一度被误删后重下，见第 7 节） |
| 文件格式 | MATLAB v7.3 格式，其实体是 HDF5，内部用 gzip 压缩 |
| 文件内部结构 | 根节点下有四个数据集，名为 `RF0_I`、`RF0_Q`、`RF1_I`、`RF1_Q`，各自形状为 1 行 1.5 亿列，数据类型为 64 位浮点数 |
| 元数据 | **完全没有**，与前一个数据集相同 |
| 数值的本质 | 量化步长为 2 的负 15 次方，说明 64 位浮点底下仍是 16 位整数，转成复 int16 交织**无损** |
| 信号电平 | 峰值在满量程以下 34 到 43 分贝。折算有效位数约 9 到 10 位，比前一个数据集（满量程以下 15 到 30 分贝、约 11 到 12 位）窄，这是收发距离从 10 米拉到 40 至 150 米的直接后果 |
| 正交支路质量 | **同相支路与正交支路的标准差之比为 0.54 到 0.95**（三个文件、五个抽样位置均存在），互相关接近 0，直流偏置很小。详见第 5 节 |
| 单文件解压体积 | 约 4.8 GB；转成复 int16 交织后约 1.2 GB（双通道全转）或约 600 兆字节（只转 RF0） |

以下参数**只写在论文里，数据文件本身不携带**，引用时必须标明来源是论文而非设备记录：

| 参数 | 论文给出的值 |
|---|---|
| 接收设备 | USRP-2955 软件无线电设备 |
| 采样率 | 100 兆样点每秒 |
| 通道 | **双通道同时采集**，中心频率 2440 兆赫兹与 5800 兆赫兹 |
| 每片时长 | 1.5 秒（与每通道 1.5 亿点、100 兆样点每秒相互吻合） |
| 接收增益 | **50 分贝** |
| 接收天线 | **全向垂直，增益 3 dBi** |
| 收发距离 | **三档区间标注**：`D00` 为 20 至 40 米、`D01` 为 40 至 80 米、`D10` 为 80 至 150 米 |
| 采集姿态 | **无人机在飞行中**，速度 5 至 15 米每秒 |
| 时间连续性 | 每 1000 万点（即 0.1 秒）内连续，块之间有连续性损伤（采—存交替模式） |
| 类别 | 24 种机型加背景，覆盖 7 个品牌；其中只有 **9 类是城市户外运动采集**并带有效距离标注，其余 15 类是城市室内固定采集、距离约 2 米。论文未给出这 9 类的名单 |
| 训练测试划分 | **没有**，须自行划分 |
| 全集体积 | 约 560 GB；论文自己建议用不小于 1 TB 的硬盘 |

文件命名规则（引自论文表 3，已用本地文件逐一验证）：户外飞行类为
`T<机型码>_D<距离码>_S<频段状态与片段序号>.mat`，室内固定类**没有 `D` 字段**，形如
`T<机型码>_S<频段状态与片段序号>.mat`。`S` 是 4 位：最高位是频段状态标志，`0` 表示初始通信
在 915 兆赫兹或 2.4 吉赫兹、`1` 表示切换到 2.4 或 5.8 吉赫兹；低 3 位是片段序号 `000` 到 `111`。

**做距离对照时 `S` 的最高位不能变**，否则频段状态跟着变了、对照不成立。用户最初提供的三片
之所以不构成对照组，正是这个原因：近的那片是 `S1110`（无人机已切到 5.8 吉赫兹，2.4 吉赫兹上
只剩微弱飞控信号），远的那片是 `S0100`（无人机在 2.4 吉赫兹上工作），实测结果因此是反的——
近的突发帧占比只有 2%，远的有 15.2%。

#### 3. 与 DroneRFb-DIR 的差异

| 维度 | DroneRFb-DIR（Datasets1） | DroneRFa（Datasets2） |
|---|---|---|
| 定位 | 个体识别（同型号分架次） | 检测与型号识别 |
| 采样率 | 80 兆样点每秒 | 100 兆样点每秒 |
| 通道 | 单通道 | 双通道同时采集 |
| 中心频率 | 2.44 吉赫兹 | 2440 兆赫兹加 5800 兆赫兹 |
| 每片时长 | 50 毫秒（400 万点） | 1.5 秒（每通道 1.5 亿点） |
| **距离** | 固定 10 米 | **三档区间标注** |
| **运动状态** | 摆放不动 | **飞行中，5 至 15 米每秒** |
| 机型 | 6 型 18 架，全是 DJI | 24 型加背景，7 个品牌 |
| 接收增益与天线 | 无记录 | 50 分贝、3 dBi（均引自论文） |
| 时间连续性 | 片内连续、片间不连续 | 每 0.1 秒内连续、块间有损伤 |
| 训练测试划分 | 出版方已划好 | 无，须自行划分 |
| 正交支路质量 | 标准差比 1.0000 至 1.0014 | **0.54 至 0.95，同相支路系统性偏小** |
| 体积 | 64 GB（全集，本地已有全部） | 全集约 560 GB（本地 22 片、21 GB） |

**它补上的缺口**：一是距离维度，二是运动与多普勒，三是部分标定量（接收增益与天线增益）。
其中距离维度直接改变了 06 备忘录 §11.1 关于 P3 的 B 组「多距离场景」的结论，见第 6 节。

**它比前一个差的地方**：一是信号电平低约 15 分贝、有效位数少约 2 位；二是正交支路幅度
不平衡明显；三是没有出版方划好的训练测试划分，须自行按机型、距离档、采集批次分层，
且按 CLAUDE.md 的规矩入库即冻结、记哈希。

**仍然不够做绝对功率标定。** 虽然论文记录了接收增益与天线增益，但缺馈线损耗与一次功率
标定常数，量化码换算到分贝毫瓦这条路依然走不通，只是比前一个数据集近了一步。

#### 4. 四项实测量化结果

##### 4.1 路损指数 n = 1.90，片间重复性 0.3 至 1.3 分贝

这是本项目到目前为止唯一一个来自真实数据的定量物理结果，因此把算法口径完整记下。

**算法**（可复现的口径，与当次会话所用脚本一致）：

1. 读取：每个文件取 6 个采样偏移 `[0, 3000万, 6000万, 9000万, 1.2亿, 1.46亿]`，每处取 200 万点（0.02 秒）。这样做是因为单文件 1.5 亿点、解压 4.8 GB，整片读入不现实，而单点抽样又可能撞上突发间隙。
2. 帧化：FFT 长度 8192，汉宁窗，不重叠。单帧功率取帧内功率谱之和的 10 倍常用对数。
3. 平均功率谱：各偏移的功率谱线性域累加后除以总帧数，再取 10 倍常用对数。
4. **底噪估计：对平均功率谱在频率维取第 10 百分位。**
5. 带内功率：取相对中心频率 +2 至 +12 兆赫兹，即 2442 至 2452 兆赫兹，该频段已先行确认为无人机图传信号。
6. 突发帧占比：帧功率高于其中位数 6 分贝以上的帧所占比例。
7. 距离取区间中点：`D00` 取 30 米、`D01` 取 60 米、`D10` 取 115 米。
8. 路损指数：`n = −Δ(dB) / (10 · lg(d2/d1))`。

**样本清单**（`T0010` 即 DJI Phantom 4 Pro，全部取 `S0xxx` 即 2.4 吉赫兹频段状态）：

| 距离档 | 文件 | 图传带内功率高于底噪 |
|---|---|---|
| `D00` 20–40 米 | `T0010_D00_S0101.mat` | 27.2 分贝（**当时该档只有这 1 片**） |
| `D01` 40–80 米 | `T0010_D01_S0001.mat` / `T0010_D01_S0011.mat` | 17.4 / 18.8 分贝（片间相差 1.3 分贝） |
| `D10` 80–150 米 | `T0010_D10_S0000.mat` / `T0010_D10_S0100.mat` | 15.9 / 16.3 分贝（片间相差 0.3 分贝） |

`D00` 到 `D10`（30 米到 115 米）实测降幅 11.1 分贝，自由空间预期降幅 11.7 分贝，
折算**路损指数 n = 1.90**（自由空间为 2.00）。片间重复性 0.3 至 1.3 分贝。

**局限，必须与结论一起引用**：

- 相邻两档之间的比较不可靠。跨机型对照给出的数字是矛盾的：`T0010` 的 `D01` 到 `D10` 变化为 −2.0 分贝，而 `T0011`（DJI MATRICE 200，样本 `T0011_D01_S0000.mat` 与 `T0011_D10_S0000.mat`）为 −6.8 分贝，折算 n = 2.42，自由空间预期为 −5.7 分贝。两者相差 4.8 分贝。原因不是机型（路损是传播特性，与机型无关），而是**距离标注是区间且无人机在飞行**：`D01` 与 `D10` 的中点比只有 1.9 倍，而区间自身宽度就有 2 倍，相邻档的比较被区间的不确定性淹没。
- 因此这批数据**支持"路损指数约为 2"这个粗档结论，不支持逐米的距离—损耗曲线拟合**。
- 计算 n 时 `D00` 只有 1 片，分母只有单片支撑，给不出完整误差范围。当时判定「差 `T0010_D00_S0111.mat` 这一片」，用户已于同日补入，**该缺口现已补齐但尚未重算**（见第 9 节待办）。

##### 4.2 底噪 −24.3 分贝，两条独立路径互相验证

| 来源 | RF0 通道底噪估计 |
|---|---|
| 三片纯背景实测（`T0000_D00_S0000` / `S0100` / `S0111`） | −24.3 / −24.2 / −24.3 分贝 |
| 之前用含无人机的片子按第 4.1 节算法估算 | −24.3 / −24.3 / −24.1 / −24.2 分贝 |

两条路径完全一致，说明「对平均功率谱在频率维取第 10 百分位」这个底噪估计方法是对的，
可以放心用在有强信号的片子上。

**这里记一个踩过的坑**：早先曾用时间维度的帧功率中位数当底噪，在近距离档被信号污染了
8.5 分贝。频率维分位数与时间维中位数不是一回事，`tools/iq_survey` 实现时必须用前者。

另有一项提醒：这三片"背景"并不是纯噪声，峰值高出底噪 39.6 至 40.4 分贝，突发帧占比 0.0%
至 2.5%，里面有 WiFi 与蓝牙。`S0111` 最干净（0.0%），`S0000` 最脏（2.5%）。做虚警率标定时
要按干净程度分别处理，不能混成一批。

##### 4.3 5.8 吉赫兹通道零突发，该频段可以放弃

在 7 个文件上测过 RF1 通道（`T0010` 的三档距离共 6 片，加 `T0011_D10_S1100` 1 片），
每个文件 6 个抽样位置：

| 文件 | 距离档 | RF1 帧功率跨度 | RF1 突发帧占比 |
|---|---|---|---|
| `T0010_D00_S1000` | 20–40 米（最近） | 3.1 分贝 | **0.0%** |
| `T0010_D00_S1111` | 20–40 米（最近） | 4.6 分贝 | **0.0%** |
| `T0010_D01_S1010` | 40–80 米 | 1.5 分贝 | 0.0% |
| `T0010_D01_S1110` | 40–80 米 | 0.4–0.7 分贝 | 0.0% |
| `T0010_D10_S0100` | 80–150 米 | 0.4–0.7 分贝 | 0.0% |
| `T0010_D10_S1111` | 80–150 米 | 1.0 分贝 | 0.0% |
| `T0011_D10_S1100` | 80–150 米 | 0.4–0.7 分贝 | 0.0% |

**连最近的 20 至 40 米档都收不到 5.8 吉赫兹信号**，而同样抽样的 RF0 通道帧功率跨度达 18.4
分贝、突发帧占比达 15.2%。RF1 的峰值高出底噪 32 至 36 分贝但完全不随时间变化，是本振泄漏
或杂散，不是通信信号。

两个后果：一是不为 5.8 吉赫兹下载全集，"双频段"这个卖点在这套采集配置下不成立；二是转换
时只转 RF0，每片从 1.2 GB 降到约 600 兆字节。**注意这条取舍的理由是数据本身没有内容，不是
省存储**（后者见项目记忆 `no-space-saving-tradeoffs`）。

证据边界：实测覆盖的是 `T0010`（另有 `T0011` 一片）。推断到其他 DJI 机型属推断不是实测，
元数据里要这样标。若要复核，最省的做法是取任一其他机型的 `D00` 加 `S1xxx` 单片验证。

##### 4.4 `T10010` 的通道映射是反的，文件名有两种形态

`T10010`（FrSky X20 飞控器）实测确认了论文的说明，其通道与频率的映射与其余文件相反：

| 通道 | 中心频率 | 突发帧占比 | 帧功率跨度 |
|---|---|---|---|
| RF0 | **915 兆赫兹** | 0.0% | 4.4 分贝 |
| RF1 | **2440 兆赫兹** | **40.0%** | **29.6 分贝** |

信号在 RF1，RF0 是空的。**元数据的中心频率必须按机型编号判断，不能统一套用**：`T10010`
与 `T10100`（Taranis Plus）是 915 加 2440 兆赫兹，其余是 2440 加 5800 兆赫兹。

`T10010` 的 RF1 峰值高出底噪 50.8 分贝、突发帧占比 40%，是两个数据集里**信号最强、最活跃
的通道**，做波形模板与观测量提取的调试样本比任何一片无人机数据都好用。

同时确认文件名有两种形态：`T10010_S0000.mat` 与 `T10110_S0000.mat` 没有 `D` 字段。这印证了
论文所说的 15 类室内固定采集省掉了 `D`。**转换脚本必须处理这两种形态。**

#### 5. 实测发现的一处数据缺陷：正交支路幅度不平衡

同相支路与正交支路的标准差之比实测为 0.54 至 0.95（三个文件、五个抽样位置均存在，
并已排除短窗假象），互相关接近 0，直流偏置很小，所以是**纯幅度不平衡，不是相位误差**。
按 04 §10.6 第 3 项，这在质检中必须判 `degraded`。

**根因未查明，不编。** 但这里有一个反转值得记住：P3 明确要建模接收机非理想特性（相噪、
正交不平衡、自动增益控制、削顶）。前一个数据集的正交性太完美（标准差比 1.0000 至 1.0014），
无法用于校准正交不平衡模型；这一份有实测的不平衡量，**可能正是该模型的校准素材**——前提
是先确认它是接收链的真实特性，而不是数据生产环节的错误。

#### 6. 对 P3 四组标定数据的影响

前一个条目判定 P3 的 B 组「多距离场景」在 DroneRFb-DIR 上"完全不能"，并称之为硬伤。
DroneRFa 把这个缺口部分补上了：

| 组 | 用途 | DroneRFb-DIR | DroneRFa |
|---|---|---|---|
| A 高信噪比参考 | 参考波形与特征模板 | 部分可以（10 米视距） | 部分可以（`T10010` RF1 峰值高出底噪 50.8 分贝） |
| B **多距离场景** | 标定路损、遮挡与距离相关的模型 | 完全不能 | **粗档可以**：三档距离实测 n = 1.90；但只支持"路损指数约为 2"，不支持逐米曲线 |
| C 背景同邻频 | 真实干扰环境下的检测门限 | 可以（62 片纯背景） | 可以（本地 3 片背景，含 WiFi 与蓝牙） |
| D 独立验证 | 未参与调参的验收集 | 可以（出版方已划分） | 须自行划分，入库即冻结 |

**仍然只能由甲方数据或补采计划补上的部分**：绝对功率标定（缺馈线损耗与一次标定常数）、
逐米的路损曲线（需精确 GPS 距离记录）、视距与非视距标签与建筑几何（DroneRFa 无 LOS/NLOS
标签、无高度、无航迹记录，故跨层一致性算例 ② 建筑遮挡与 ③ 双径仍然不可做）。

#### 7. 一次不可逆的数据损失

用户在 13:59 说"先把 `T10010_S0000.mat` 和 `T10110_S0000.mat` 去掉吧"，我据此执行了
`rm`，随后用户说明本意并非删除原文件（"我也不差存储空间啊……别删了"）。核查本地时间机器
快照只有系统更新快照，没有覆盖这两个文件存在期间的快照，macOS 的 `rm` 也不进废纸篓，
**两片文件无法恢复，只能重下**。用户已于当日 15:13（本地时间）重新下载这两片。

由此产生的两条纪律已写入项目记忆 `no-space-saving-tradeoffs`：不以省存储为理由建议删除、
抽样或切片；删除原始数据前一律先确认，即使用户上一句说了"去掉"。

删除前生成了一份补数清单文件 `data/iq/measured/dronerfa/download-list.md`，按论文的命名
规则枚举命名空间（不是"确认存在"的文件列表，SciDB 上实际提供的片段序号是稀疏的，下载时
与实际文件列表取交集）。清单在本条目写作时按当前本地 22 片重新生成了一次。

#### 8. 一份格式预演切片与一处 `.gitignore` 漏洞

删除前从 `T10010_S0000.mat` 的 RF1 通道切出 2000 万点（0.2 秒）存为项目规定的复 int16
交织格式，作为 DS-2 与 DS-3 的一次小规模预演：先扫 7 个候选窗口取突发帧占比最高的一个
（偏移 2000 万点处，占比 43.4%），断言全部样点为 2 的负 15 次方整数倍（峰值码 2468，
即满量程以下 22.5 分贝），写盘后逐位回读对拍通过。同时配一份 `*.manifest.json` 外挂元数据，
把采集参数标 `source=paper`、通道映射反转写入备注、时间连续性写"每 1000 万点内连续、块间
有损伤"、绝对功率标"未标定"并写明原因、质量状态标 `degraded` 并列三条理由。

**格式预演的结论：外挂元数据这条路走得通，逐位无损与四态语义都能落地。**

同时修掉一处真实漏洞：`.gitignore` 原先只排除 `Datasets1-DroneRFb-DIR/`，
`Datasets2-DroneRFa/` 一直处于未跟踪状态，下一次 `git add -A` 会把 21 GB 吸进仓库。
已改为通配 `Datasets*/`，并验证：两个数据集目录下任意文件被忽略、切片 `.iq` 被忽略、
切片的 `.manifest.json` 不被忽略（是元数据，应入库）。

用户随后说明切片数据不必保留（原文件已重下）。**2026-09-03 补记时用户确认删除**，切片
`T10010_S0000_RF1_excerpt.iq` 与其 `*.manifest.json` 已删除（删除前核实过 `T10010_S0000.mat`
在盘、切片不是唯一副本），本节记的预演结论即该切片留下的全部价值。

#### 9. 本条目产生的待办

- [ ] 重算路损指数并给出误差范围：`D00` 档现已有两片（`S0101`、`S0111`），`T0011` 现已有三档齐全（`D00`/`D01`/`D10` 的 `S0000`），4.1 节的两处局限（`D00` 单片、跨机型矛盾）都可以再测一次。这是 P3 版本冻结的前置。
- [x] 确认 `data/iq/measured/dronerfa/` 那份 80 兆字节切片与其清单的去留 —— 2026-09-03 用户确认删除，已删（见第 8 节）。
- [ ] DroneRFa 的许可条款确认，与 DroneRFb-DIR 的 DS-1 并列，需用户出面。
- [ ] 其余步骤并入 06 备忘录 §11.5 的 DA 步骤表，不在此重复。

---

### 2026-09-03 · DA-6 路损指数重算：粗档冻结值与它真正的不确定度来源

#### 1. 起因与做法

上一条日志记的路损指数 n = 1.90 有两处明确局限：`D00` 档当时只有 1 片，给不出误差范围；
跨机型对照用的是相邻档（`D01` 到 `D10`），两个机型差了 4.8 分贝、彼此矛盾。用户随后补入
`T0010_D00_S0111.mat` 与 `T0011_D00_S0000.mat`，两处缺口都已补齐，本次重算。

**算法口径与上一条日志 4.1 节完全一致，一个参数都没改**：每文件 6 个采样偏移、每处 200 万点；
FFT 长度 8192、汉宁窗、不重叠；平均功率谱由各偏移在线性域累加后除以总帧数；底噪取平均功率谱
在**频率维**的第 10 百分位；带内取相对中心频率 +2 至 +12 兆赫兹（2442 至 2452 兆赫兹）。
本次新增的只有统计处理：片间抖动、逐对组合、最小二乘拟合、以及距离区间的敏感性分析。

脚本落库为 `scripts/da6_pathloss.py`，样本清单写死在脚本里，可原样复跑。

#### 2. 实测结果

`T0010`（DJI Phantom 4 Pro），RF0 通道，全部取 `S0xxx` 即 2.4 吉赫兹频段状态，6 片：

| 距离档 | 文件 | 图传带内功率高于底噪 | 片间极差 |
|---|---|---|---|
| `D00` 20–40 米 | `T0010_D00_S0101` / `T0010_D00_S0111` | 27.2 / 27.6 分贝 | **0.4 分贝** |
| `D01` 40–80 米 | `T0010_D01_S0001` / `T0010_D01_S0011` | 17.4 / 18.8 分贝 | 1.3 分贝 |
| `D10` 80–150 米 | `T0010_D10_S0000` / `T0010_D10_S0100` | 15.9 / 16.3 分贝 | 0.3 分贝 |

`T0011`（DJI MATRICE 200），同样条件，三档各 1 片：`D00` 22.7 分贝、`D01` 19.7 分贝、
`D10` 12.8 分贝。

九个文件的底噪估计全部落在 −24.2 至 −24.3 分贝，这是继背景片与含信号片之后**第三次**
独立验证「频率维第 10 百分位」这一底噪估计方法。

| 计算方式 | `T0010` | `T0011` |
|---|---|---|
| `D00`→`D10` 逐对组合（4 组 / 1 组，距离取区间中点 30→115 米） | n = 1.87 / 1.93 / 1.94 / 1.99，**均值 1.93**，极差 0.12，标准差 0.05 | n = **1.69** |
| 三档最小二乘（中点口径） | n = 1.95，残差均方根 1.7 分贝 | n = 1.68，残差均方根 1.0 分贝 |
| 三档最小二乘（区间几何均值口径） | n = 1.93 | n = 1.67 |
| 实测降幅 / 自由空间预期降幅 | −11.3 / −11.7 分贝 | −9.8 / −11.7 分贝 |

#### 3. 三个结论

**结论一：片间抖动很小，不是不确定度的主要来源。** 四种配对组合给出的 n 只差 0.12，
标准差 0.05；换成区间几何均值口径，n 从 1.95 变到 1.93，几乎无差别。

**结论二：主导不确定度是距离标注本身，比片间抖动大一个数量级以上。** `D00` 是 20 至 40 米、
`D10` 是 80 至 150 米，两者的真实距离比只能确定在 **2.0 到 7.5** 之间。同一个实测降幅
（`T0010` 为 −11.3 分贝）在这个比值范围内对应的 n 是 **1.29 到 3.75**；`T0011` 的
−9.8 分贝对应 **1.12 到 3.27**。

因此可以冻结的只有一句话：**这批数据支持"2.4 吉赫兹城市户外链路的路损指数约为 2"这个粗档
结论**。写进 P3 模型卡时必须同时记明：1.93 这个数字的前提是"距离取区间中点"这一计算假设，
它的精度不代表数据的精度；要一条带真实置信区间的路损曲线，仍然只能靠带精确 GPS 距离记录的
甲方自采数据。

**结论三：上一条日志记的跨机型矛盾，已经查明是相邻档比较的产物，不是真矛盾。**
上次只能用 `D01`→`D10` 比较，两个机型给出 0.72 与 2.42，差 4.8 分贝。现在两个机型都有了
宽基线（`D00`→`D10`），给出 1.93 与 1.69，差 0.24——**路损与机型无关这一物理预期得到验证**。
相邻档比较依然不可靠，本次两个机型的相邻档结果仍然发散（`T0010` 为 3.07 与 0.72，
`T0011` 为 1.00 与 2.42），原因是相邻档中点比只有 1.9 至 1.92 倍，而区间自身宽度就有 2 倍，
比较量被区间不确定性淹没。**做距离标定一律用宽基线，不用相邻档**，这一条写进 DA-3 的元数据
说明与 P3 模型卡。

#### 4. 与上一条日志的差异

上一条记的 n = 1.90 基于 `D00` 单片；补入第二片后 `D00` 档均值由 27.2 升到 27.4 分贝，
n 相应变为 1.93。**这不构成基准变化**（铁律 10），因为上一条记的是"首轮实测值"并已注明
`D00` 单片的局限，本次是按预定的 DA-6 步骤补齐样本后的重算，两个数字对应的样本集不同，
都如实保留在日志里。

#### 5. 本条目产生的待办

- [ ] P3 模型卡起草时，把「n ≈ 2 粗档、中点假设、区间边界 1.1–3.8」三件事一并写入，不得只写 1.93。
- [ ] `T0011` 三档各只有 1 片，其片间抖动未知。若要给第二机型也带上误差范围，按下载清单优先级 3 补片。
- [ ] 相邻档不可用于标定这一条，写进 DA-3 的元数据说明。

---

### 2026-09-03 · DS-2/DA-2 转换脚本与 DS-5/DA-5 摸底工具

#### 1. 本次做了什么

按用户指定的顺序做三件事：先把 `docs/iq-format.md` 的第 3、4 节定稿（否则转换脚本写出来的
格式就成了事实标准，规范反而要跟着代码走），再写转换脚本与摸底工具，最后在两个公开数据集
的真实文件上跑通并做验证。

产出：

| 路径 | 内容 |
|---|---|
| `docs/iq-format.md` 第 3、4 节 | 容器、样点编码、分段与索引、文件命名；十四个顶层键的元数据字段表、来源标注机制、八项质检键名与取最差规则、SigMF 对应 |
| `tools/iq_format/{manifest,writer,readers}.py` | 清单 schema 与校验、流式分段写盘器、两个数据集的源适配器 |
| `tools/iq_convert.py` | DS-2 / DA-2 |
| `tools/iq_survey.py` | DS-5 / DA-5，04 §10.6 八项质检 |
| `tests/unit/test_iq_tools.py` | 32 项单测，用合成 HDF5 夹具，不依赖几十 GB 数据集，可进 CI |

#### 2. 规范定稿时定的几件事

- **容器用「裸样点文件加旁挂清单」**，不用自带文件头。理由是铁律 7 要求分片续传，服务端按 HTTP Range 取任意区间时，样点偏移到字节偏移必须是常数乘法（`byte = sample × 4`），文件头会让每次换算都要先解析；另外这与 SigMF 的两文件结构同构，互转不搬样点。
- **字节序固定小端**，清单必须显式写并在读取时校验，**不做自动探测**——探测会在数据损坏时给出看似合理的错误解释。
- **单段上限 67108864 复样点（256 MiB）**；分段是存储行为，不得与采集的时间不连续混为一谈。
- **来源标注 `field_sources`**：从字段路径到 `measured / paper / derived / assumed / absent` 的映射，必须覆盖采样、频率、时间、通道、功率五类下的每个叶子字段。这是两个数据集"参数只在论文里"这一现实的直接产物。
- **`not_applicable` 与 `valid` 严格分开**：单通道数据的第 6 项（多通道对齐）是"不适用"，不是"通过"。整体状态取八项中最差，`not_applicable` 不参与。

#### 3. 单测覆盖了什么

32 项，其中这几项是刻意写的反面用例：

- 源数据不是 2 的负 15 次方整数倍时**必须抛错中止**，且不留下任何半成品文件（铁律 10）。
- 清单空骨架**必须验不过**——必填项为空正是要拦的东西（铁律 15）。
- 缺旁挂清单时摸底工具**判 degraded 而不是崩溃**，也不拿默认值顶替。
- 篡改清单里的哈希 → 第 8 项 `invalid`；改样点数 → 第 1 项 `invalid`。
- 同内容不同文件名 → 第 8 项报"完全重复"。
- 文件名含中文或空格 → 写盘器拒绝（铁律 15）。
- `IN` 解析为视距、`OUT` 解析为非视距（这个陷阱在 06 §11.4 里已经写明，单测把它钉死）。
- DroneRFa 的距离必须是**区间**，`distance_point_m` 为 `null`（D-019）。
- `T10010` 的 RF0 是 915 兆赫兹、RF1 是 2440 兆赫兹，与其余文件相反。

#### 4. 真实数据上的结果

转了 6 个产物：DroneRFb 三片（一片有信号的训练集、一片背景、一片测试集），
DroneRFa 三片（`T0010_D00_S0101` 的 RF0、`T10010_S0000` 的 RF1、`T0000_D00_S0111` 的 RF0）。
合计 1.8 GB，全部逐段回读对拍通过、无损断言通过。

| 产物 | 样点数 | 段数 | 峰值 dBFS | 耗时 |
|---|---|---|---|---|
| `dronerfb_A1_IN_S0_slice_1_CH0_S4` | 4000000 | 1 | −18.1 | 0.2 s |
| `dronerfb_background_slice_1_CH0_S4` | 4000000 | 1 | −30.8 | 0.1 s |
| `dronerfb_0_CH0_S4`（测试集） | 4000000 | 1 | −19.4 | 0.1 s |
| `dronerfa_T0010_D00_S0101_RF0_S4` | 150000000 | 3 | −22.9 | 5.1 s |
| `dronerfa_T10010_S0000_RF1_S4` | 160000000 | 3 | −18.1 | 5.3 s |
| `dronerfa_T0000_D00_S0111_RF0_S4` | 140000000 | 3 | −36.9 | 4.3 s |

**测试集的真值解析验证通过**：DroneRFb 测试集文件名是纯数字（`0.mat`），真值只存在于
`test_labels.txt`。工具反查到 `0.mat` 对应原始文件 `D1_IN_S2_slice_47`，即 DJI Mini 4 Pro
第 1 架、视距、第 2 次采集、第 47 片。**这条路径要是没实现，测试集 2513 片就全是无真值数据。**

八项质检的结论在两个数据集上一致：六项 `valid`、第 6 项 `not_applicable`、
**第 7 项 `metadata_required_units` 一律 `degraded`**，原因是十个采集类字段的来源是
`paper` 或 `absent`（采样率、中心频率、有效带宽、时间基准、连续性、站点、天线、增益、
AGC、标度）。整体状态因此一律 `degraded`。这正是 DS-5 当初被排在前面的理由：
**"缺元数据判 degraded 而不崩溃"这条路径，在真实数据上第一次被走通了。**

#### 5. 两个新的实测事实

##### 5.1 DroneRFa 的片长不是固定的 1.5 秒

上一条日志按 3 个抽样文件与论文记的是"每片 1.5 秒、每通道 1.5 亿点"。本次把 22 片全查了
一遍，实际是：

| 片长 | 片数 | 对应时长 |
|---|---|---|
| 1.3 亿点 | 1 | 1.3 s |
| 1.4 亿点 | 6 | 1.4 s |
| 1.5 亿点 | 8 | 1.5 s |
| 1.6 亿点 | 5 | 1.6 s |
| 1.7 亿点 | 2 | 1.7 s |

**全部是 1000 万点的整数倍，即 13 到 17 个采集块。** 这与论文所说"每 1000 万点内连续、
块间有损伤"的采—存交替模式吻合：片长取决于该次采集攒够了多少块。RF0 与 RF1 的长度在
每个文件里都相同。

修正上一条日志的表述：**1.5 秒是标称值，不是实测值**；任何按固定片长做的批处理假设都会出错，
转换脚本因此按实际形状读，不写死长度。

##### 5.2 正交幅度不平衡在**噪声路径**上，信号路径是平的

上一条日志记的是"标准差比 0.54 至 0.95，根因未查明"。本次用整片统计跑出来的结果很反常：
背景片 0.7335 判 degraded，而两片有信号的（0.9732、0.9981）都判通过——**同一台设备、同一次
采集，含信号的片反而"更平衡"，这不合理**，所以做了下面这个判别实验。

把同一个文件的帧按功率排序，分别取最安静的一成与最强的一成，各自算标准差比：

| 产物 | 整片 | 安静帧 | 强帧 |
|---|---|---|---|
| `T0000_D00_S0111`（背景） | 0.719 | 0.736 | 0.816 |
| `T0010_D00_S0101`（无人机） | 0.984 | 0.804 | 0.994 |
| `T10010_S0000` RF1（最强信号） | 0.999 | 0.881 | 0.999 |
| `dronerfb_A1_IN_S0_slice_1`（对照） | 1.000 | 1.001 | 1.000 |
| `dronerfb_background_slice_1`（对照） | 0.998 | 0.998 | 0.996 |

**结论**：DroneRFa 的不平衡只出现在噪声占优的帧上，信号越强整片比值越接近 1。这说明
不平衡在**接收/存储链路的噪声路径**上，被测信号本身是平衡的。DroneRFb 同为 USRP-2955
采集，两种帧上都是 1.000，**所以这不是设备型号的固有特性**，而与 DroneRFa 那次采集的
配置或数据生产环节有关。

**这个发现直接改了工具的判据（D-020）**：第 3 项质检改用安静帧的标准差比，整片值降为
参考量报出。改判据之后，三片 DroneRFa 全部被正确标出（安静帧 0.69 至 0.90），而按整片
判会漏掉两片。同一形态写成了单测 `test_survey_judges_imbalance_on_quiet_frames_not_whole_file`，
以后不会退回去。

对 DA-7 的意义：根因仍未查明，但**范围缩小了一大截**——要查的是噪声路径而不是整条链路，
且可以拿 DroneRFb 当"同型号设备、无此现象"的对照组。若最终确认是接收链特性，它就是 P3
正交不平衡模型的实测校准素材；若是数据生产环节的问题，则只能标记并弃用。

#### 6. 判据里的一个通用处理

直流偏置、标准差比、互相关这三个量在短文件上本身就有统计涨落（零直流假设下样本均值的
标准差是 σ/√N，互相关是 1/√N，标准差比是 1/√(2N)）。固定阈值会把短窗的正常涨落误报成
设备缺陷——写单测时就撞上了：24576 点的随机数据直流比达 0.9%，逼近 1% 的固定阈值。

处理办法是**判据取「固定阈值」与「5 倍统计标准差」中较宽的一个**。文件越长下限越小，到真实
数据的量级（1 亿点以上）下限约为万分之零点五，判据实际就退化成固定阈值。这样短窗不误报、
长文件不放水。

#### 7. 本条目产生的待办

- [ ] 批量转换尚未做。当前只转了 6 个产物用于验证工具。全量转换是一次批处理作业（DroneRFb 4690 片、DroneRFa 22 片×每片约 600 MB），**决定因素是时间不是空间**，等用户定何时跑。
- [ ] DS-4 / DA-4 冻结验收子集尚未做，须在批量转换的同时记 sha256。
- [ ] DS-6 真实背景虚警率标定：现在背景片已可转换、观测量统计已在摸底工具里，可以开工。
- [ ] DA-7 噪声路径不平衡的根因判定，按 5.2 节缩小后的范围查。
- [ ] `docs/iq-format.md` 第 5 节（功率与标定口径）仍被甲方数据阻塞，不动。

---

### 2026-09-03 · D0-1 / D0-2 北京亚运村观测区域与底图：两条用户指示改变了产物形态

#### 1. 本次做了什么

按 06 备忘录 §4 执行 D0-1（定观测区域）与 D0-2（底图）。计划阶段原定沿用 em-demo 的西安范围，
用户在批准计划时否决，改为**北京亚运村周围**；执行到一半用户又指出**"我可以用大的呀，128 GB 没有
问题"**，底图因此不再按观测区域裁切，而是把整份全球文件登记为共享资产。两条指示都已落实并记为
决策 D-021、D-022（`CLAUDE.md`），06 备忘录 §14 记了一条修正。D0-5（DEM）因同一决策一并完成。

| 用户指示 | 改变了什么 |
|---|---|
| "不放在西安，放在北京亚运村周围" | 观测区域中心改为 (116.405, 39.990)；em-demo 的 13,594 栋西安 Overpass 建筑集不再能作 D0-4 对拍基准 |
| "我可以用大的呀，128 GB 没有问题" | 底图与 DEM 不按区域拆分，整份登记到 `data/basemap/`；裁切件降为测试夹具；"缩小到区域之外一片空白"这个继承的坑随之消失；交付包体积因此多出约 145 GB，交付介质须在阶段 0 平台基线里一并定 |

#### 2. 产物

| 路径 | 内容 | 入 git |
|---|---|---|
| `scene/aoi/beijing-yayuncun.json` | 观测区域定义：bbox `116.345,39.945,116.465,40.035`（约 10.23 × 9.99 km），中心、坐标系、地名核实证据、替换条件 | 是 |
| `scene/register_basemap.py` | 登记整份全球底图与 DEM：软链、身份清单 | 是 |
| `scene/fetch_tiles.py` | 按观测区域裁切 zoom 0 至 15 的切片，六项自检，写清单；默认拒绝覆盖已有产物 | 是 |
| `data/basemap/planet.manifest.json` | 底图身份：137370745450 字节，全文件 sha256 `b4c4674234ae75c8…`，planetiler 0.10.2，OSM 快照 2026-08-17T04:00:00Z（序号 122085），9 个矢量图层 | 是 |
| `data/basemap/dem.manifest.json` | DEM 索引：zoom 0 至 8 共 87381 个文件、7442617745 字节，九层均完整 | 是 |
| `data/basemap/{planet.pmtiles, dem}` | 指向 Airports 工程的软链（目标机上放真实文件） | 否 |
| `data/scene/beijing-yayuncun/basemap-slice.pmtiles` | 切片：206 块瓦片、4342046 字节，sha256 `a0a40f550bea6d1e…` | 否 |
| `data/scene/beijing-yayuncun/basemap-slice.manifest.json` | 切片清单，含源文件哈希与六项自检结果 | 是 |
| `data/scene/beijing-yayuncun/verify-airports-view-z14p5.png` | 本条第 4 节的渲染验证图（真实 OSM 数据） | 否 |
| `docs/scene-package.md` | 目录布局改为"共享资产 + 区域目录"，三份清单字段表，建库脚本调用方式 | 是 |

#### 3. 观测区域中心的核实与范围的取法

中心位置不是凭记忆定的。用 `pmtiles tile` 从全球底图取 z12、z13、z14 三块瓦片，gzip 解压后
提取矢量瓦片里的中文串，`亚运村街道`、`亚运村全民健身园`、`安慧桥`、`奥林匹克公园`、`大屯`、
`慧忠路`、`北辰路`、`安立路` 全部落在 z12 瓦片 (3372, 1550) 的范围 116.367–116.455 E、
39.977–40.044 N 内，中心取 (116.405, 39.990) 与之相符。

范围大小 04 号方案附录 B 没有规定，取了与原西安范围同量级的约 10 × 10 km，标 PROVISIONAL。
三档候选的切片体积（用整份底图时与范围无关）：

| 范围 | 尺寸 | 切片瓦片数 | 切片体积 |
|---|---|---|---|
| `116.355,39.955,116.455,40.025` | 8.5 × 7.8 km | 142 | 3.4 MB |
| **`116.345,39.945,116.465,40.035`（采用）** | 10.2 × 10.0 km | 206 | 4.3 MB |
| `116.325,39.93,116.485,40.05` | 13.7 × 13.3 km | 334 | 6.0 MB |

#### 4. 实测验证结果（不是"应该能跑"，是实际跑过）

| 项目 | 命令或方法 | 结果 |
|---|---|---|
| 估算 | `fetch_tiles.py --aoi beijing-yayuncun --estimate` | 206 块、4.3 MB，与手工 dry-run 一致 |
| 切片自检 | 脚本内六项 | bounds 覆盖 bbox；zoom 0–15；条目数 206 等于 dry-run；`buildings` 层 zoom 11–15；中心 z15 瓦片 (26979, 12406) 解压 22177 字节且含 `buildings` 层名。全部通过 |
| 切片可复现 | 同参数裁两次 | 两次 sha256 相同（`a0a40f55…`） |
| 整份底图 Range 服务 | Airports `serve.py` 拷贝件，`curl -r 0-15` | `206 Partial Content`，`Content-Range: bytes 0-15/137370745450` |
| 渲染 | 无头 Chrome 加载 Airports 查看器拷贝件（初始视角改亚运村、强制离线、开 3D 建筑与山体阴影） | 样式 67 层，瓦片加载完成，可见要素 1756 个，无错误；画布导出 1114 × 813，可辨认「亚运村街道」「大屯街道」「国家体育场」「奥体中心体育场」「北四环中路」「安立路」「北辰中心花园」等标签，建筑按 Airports 外观拉伸 |
| 切片与整份一致 | 两种底图各导出一次画布 | 两张 PNG 的 sha256 相同（`f0ba7dac…`），证明区域之内二者无差别 |
| ASCII 检查 | `scripts/check-ascii.sh data/scene data/basemap scene` | 通过 |

无头验证只在 macOS 开发机上做过，按 D-015 / D-016 不作验收依据；D1 的正式验收截图另做。
Airports 工程本身未被修改，查看器拷贝件与打过补丁的服务器都在会话临时目录。

#### 5. 无头浏览器截图的三个坑（D1 的端到端测试会再遇到）

1. **`load` 事件太早**：Chrome 的 `--screenshot` / `--dump-dom` 在页面 `load` 时就动手，此时
   MapLibre 还没开始拉瓦片，截出来是空白。解决：让服务器提供一个延时 25 秒才返回的脚本，页面末尾
   引用它，把 `load` 推迟到瓦片加载完成之后。
2. **`--virtual-time-budget` 不加载瓦片**：探针显示样式挂上了但 `loaded=false`，网络请求在虚拟时间下不推进。
3. **合成器截图与 `toDataURL` 都拿不到 WebGL 内容**：`requestAnimationFrame` 正常在跑（约 57 帧/秒），
   但 MapLibre 每帧后缓冲被清空。解决：建图时开 `preserveDrawingBuffer: true`，导出前调
   `map.redraw()` 做一次同步重绘，再 `getCanvas().toDataURL()`。

#### 6. 与并行会话的协调

同一时间另一个会话在做 DS-2 / DA-2 转换脚本与 `tools/`，已占用决策编号 D-020 并修改了
`CLAUDE.md`、06 备忘录 §11 与本文件。本任务的两条决策因此编为 D-021、D-022；提交时对三份共享
文档只暂存本任务的改动，另一会话的改动留在工作区由其自行提交。

#### 7. 本条目产生的待办

- [ ] 观测区域**范围大小**待用户确认或调整（改 `scene/aoi/beijing-yayuncun.json` 后 `--force` 重跑即可）。
- [ ] D0-3 建筑解码：首先验证 z15 建筑要素有无稳定 id；顺带实测北京亚运村范围的 OSM 高度覆盖率。
- [ ] D0-4 对拍基准：建库阶段用 em-demo `fetch-buildings.mjs` 同样的 Overpass 查询拉北京 bbox。
- [ ] D0-6 区域总清单须按 sha256 引用 `data/basemap/` 两份清单。
- [ ] D1-1 Node 版 Range 文件服务，语义对齐 Airports `serve.py`；D1 端到端测试沿用第 5 节的截图方法。
- [ ] 交付介质：整份底图与 DEM 约 145 GB 随包，阶段 0 平台基线里一并定。

---

### 2026-09-04 · 底图与数字高程模型拷入本项目自持

#### 1. 本次做了什么

用户指示："底图与 DEM 约 145 GB 也拷贝到本项目目录中，避免后续出现资源路径的问题。"

在此之前，`data/basemap/planet.pmtiles` 与 `data/basemap/dem` 是两条指向
`/Users/zhiyu/CC/Airports/tiles/` 的软链，本项目因此在运行时依赖另一个工程的绝对路径。
本次把两份资源整份拷入，改为本项目自持的真实文件，并把脚本默认路径、三份清单与全部文档
一并改到包内路径。记为决策 D-023。

**动手前先确认了一件事**：那两条路径当时确实是软链。如果直接对软链路径 `cp`，写入会顺着
软链落到 Airports 的原文件上，等于覆盖来源。因此脚本里先 `test -L` 判定、`rm` 掉软链、
再拷贝；两次拷贝的第一行输出都记录了来源文件仍是 137370745450 字节。

#### 2. 实测结果（全部实际跑过）

| 项目 | 方法 | 结果 |
|---|---|---|
| 底图拷贝 | `cp`，137370745450 字节 | 1 分 52 秒（约 1.2 GB/s） |
| DEM 拷贝 | `cp -R`，87381 个文件 | 1 分 31 秒 |
| 是真拷贝而非克隆 | 可用空间、inode、块数 | 可用空间由 1.1 TiB 降到 1.0 TiB；inode 48880276 → 53666467 不同；块数各 268302240。是各自独立的副本，不是 APFS 克隆 |
| 底图内容一致 | `shasum -a 256` 全文件 | `b4c4674234ae75c8fea8a10c23f1cce49626155f56a437eda89b006f1e02b2b7`，与来源一致 |
| DEM 内容一致 | 索引哈希（"相对路径 + 字节数"有序列表的 sha256） | `13b213a4781971e24c1752c5835a1966bd575d90cc951b23d18b41d71dede52d`，与来源一致；87381 个文件、7442617745 字节，zoom 0 至 8 九层均完整 |
| 来源完好 | 拷贝后复查 | 底图仍 137370745450 字节，DEM 仍 87381 个文件 |
| 磁盘占用 | `df` | 本机剩余 1.0 TiB，两份副本合计约 145 GB |
| git 未受影响 | `git status` 计时 | 0.056 秒（新增 87381 个被忽略的文件后仍是毫秒级），无需改 `.gitignore` |
| 切片仍可复现 | 用包内底图重跑 `fetch_tiles.py --force` | 206 块瓦片、4342046 字节，sha256 `a0a40f55…`，与用来源文件裁出的完全相同 |
| 包内路径可服务 | 对 `data/basemap/planet.pmtiles` 发 `Range: bytes=0-15` | `206 Partial Content`，`Content-Range: bytes 0-15/137370745450` |
| 包内路径可渲染 | 无头浏览器加载 Airports 查看器拷贝件，源指向包内文件 | 67 层样式加载完成、瓦片加载完成、1756 个可见要素、无错误；导出画布与 2026-09-03 用来源文件渲染的那张 sha256 相同（`f0ba7dac…`） |

#### 3. 代码与清单的改动

- `scene/fetch_tiles.py`：默认底图源由 `/Users/zhiyu/CC/Airports/tiles/planet.pmtiles` 改为
  包内 `data/basemap/planet.pmtiles`。
- `scene/register_basemap.py`：原来的 `link_or_note()` 只会建软链，遇到真实文件会报错退出。
  改为 `place()`，判定三种存放形态并写入清单的新字段 `storage`：`in_place`（包内自持真实
  文件）、`symlink`（包内路径是软链）、`external`（资源在包外）；另加 `--origin` 与
  `--dem-origin` 两个参数，把副本的来源路径记进清单备查（铁律 8 的溯源要求）。
- 三份清单重新生成：`data/basemap/planet.manifest.json`、`data/basemap/dem.manifest.json`
  的 `dev_link_target` 由 Airports 路径变为空、`storage` 为 `in_place`；
  `data/scene/beijing-yayuncun/basemap-slice.manifest.json` 的 `source.path` 改为包内路径。

#### 4. 一处如实记录：文件权限

副本忠实继承了来源的权限位：底图与每个 DEM 瓦片都是 `-rwx------`（属主可读写执行、其他人
无权限），目录是 `drwx------`。数据文件带可执行位是来源本身的历史遗留，不是本次造成的。
本次**没有**改动权限，因为开发机上服务与文件同属一个用户，不影响使用。若将来内网集中部署
时应用服务以另一个账号运行，需要在部署脚本里统一放开读权限；Windows 交付不受影响。

#### 5. 本条目产生的待办

- [ ] 交付介质须装得下这约 145 GB，阶段 0 冻结平台基线时一并定（此条已在 2026-09-03 条目里
      提出，本次资源进包后成为硬约束）。
- [ ] 内网集中部署若以独立账号运行服务，部署脚本需统一放开 `data/basemap/` 的读权限。
- [ ] 此后代码、脚本、配置一律不得再引用 `/Users/zhiyu/CC/Airports/` 路径；`CLAUDE.md`
      资源清单里那两行只作来源留档。

---

### 2026-09-04 · AOI 定为 20 × 20 km，并测出建筑 id 与高度来源两项事实

#### 1. 本次做了什么

用户问「AOI 范围大小按 20 × 20 km 如何」。没有凭经验答，先对两个候选范围各解码一遍 z15
`buildings` 图层比较代价，据此给出建议；用户拍板后改 AOI 定义、重裁切片、更新联动文档。
过程中顺带销掉了 06 备忘录里标了很久的「首个技术未知点」，并发现瓦片高度字段的一个陷阱。

产出：决策 D-024（范围）、D-025（两项实测事实）；脚本 `scene/probe_buildings.py` 落库。

#### 2. 范围比较的实测数据

| 项目 | 10 × 10 km | 20 × 20 km | 倍数 |
|---|---|---|---|
| z15 瓦片 | 132 | 529 | 4.01 |
| `buildings` 要素（含跨瓦片重复） | 15706 | 58647 | 3.73 |
| 按 id 去重后的建筑 | 13564 | 50201 | 3.70 |
| 切片（zoom 0 至 15，全部层级） | 206 块、4342046 字节 | 749 块、11147792 字节 | 2.57 |

面积是 4.0 倍，建筑只多 3.7 倍。底图体积完全不受影响，因为底图已是整份全球文件（D-022）。
按 em-demo 西安那份 13594 栋对 6.9 MB 的比例外推，20 × 20 km 的 `buildings.geojson` 约 25 MB。
遮挡桶网格（100 米桶）由 100 × 100 变成 200 × 200 个桶。真正按面积 4 倍增长的是覆盖场的
格点数，同分辨率下由 25 万点变成 100 万点，这一条记给 D3。

**取 20 × 20 km 的理由不是代价而是物理**：单站置于中心时，10 × 10 km 到边界只有 5 公里、
到角上 7 公里，多半小于 2.4 吉赫兹图传链路的可探测距离，那样画出来的覆盖包络会被 AOI 边界
截断，等于用框子冒充物理边界。20 × 20 km 给到轴向 10 公里、角向 14 公里。

新范围 `116.288,39.900,116.522,40.080`，实测 19.96 × 19.97 km，中心 (116.405, 39.990) 不变。

#### 3. 两项实测事实

**其一：z15 建筑要素带稳定 id。** 20 × 20 km 内 58647 个要素**无一缺 id**，按 id 去重后
50201 栋（跨瓦片重复 8446 个）。06 备忘录 §4 把「Protomaps z15 建筑 feature 有无稳定 id」
列为首个技术未知点、要求 D0-3 首先验证，现在销项：跨瓦片合并直接按 id 做，原定的备用路径
（共享边或缓冲相交合并）不必再写。

**其二：瓦片的 `height` 字段把两种来源混在一起，比表面数字差得多。** 23.1% 的要素带
`height`，乍看远好于西安的 1.0%。但取值分布露了马脚：5.0 米一个值就占了全部 height 值的
47.6%，而且 5、8、11、14、17、20、23、26、29、32 是一串公差为 3 的等差数列——这正是
planetiler 按 `building:levels × 3 + 2` 推导的结果（1 层 → 5 米，2 层 → 8 米，依此类推）。
按「不小于 5 的整数且模 3 余 2」把它们挑出来：

| 高度来源 | 要素数 | 占全部 58647 个要素 |
|---|---|---|
| 无任何高度信息 | 45103 | 76.9% |
| 疑似由 `building:levels` 推导 | 11364 | 19.4% |
| 其它取值（真实标注的上界） | 2180 | 3.7% |

**这个判据只是启发式**，一栋真高 20 米的楼会被误判成推导值，反过来也可能有真实标注恰好落在
序列上。所以瓦片本身给不出干净的 `src`：原定 D0-3 里「`height` 有 → `src=osm:height`」这条
规则如果照写，会把 19.4% 的推导值当成实测标注，违反铁律 14（合成与估算必须标记）。已把 D0-3
的规则改为先标 `src=tile:height`，真实来源等 D0-4 拉 Overpass 原始标签后再定。D0-4 那次拉取
因此有了第二个用途，不只是重建对拍基准。

#### 4. 验证

| 项目 | 结果 |
|---|---|
| 探测脚本可复跑 | `scene/probe_buildings.py` 落库后复跑，58647 / 50201 / 0 无 id / 11364 推导，与临时脚本逐项相同 |
| 切片自检 | 六项全过；749 块与 dry-run 一致；`buildings` 层 zoom 11 至 15；中心瓦片含该层 |
| 渲染回归 | 用 20 km 切片重跑无头渲染，画布 sha256 `f0ba7dac…`，与 10 km 切片、整份底图两次渲染的结果**三者逐位相同**（视窗在两个范围内都被完整覆盖，本应如此，作为回归判据） |
| ASCII 检查 | `data/scene`、`data/basemap`、`scene` 均合规 |

#### 5. 本条目产生的待办

- [ ] D0-3 建筑解码：按 id 合并；`src` 先标 `tile:height`，不得直接标 `osm:height`。
- [ ] D0-4 拉 Overpass 时必须同时取 `height` 与 `building:levels` 原始标签，用于把 `src`
      分清「真实标注 / 层数推导 / 面积估算」三档。
- [ ] 覆盖场格点数按面积 4 倍增长，D3 设计报告里给出分辨率与耗时的取舍。

---

### 2026-09-04 · DS-6 真实背景虚警率标定

#### 1. 本次做了什么

先把两个数据集的全部背景片转换入库（DroneRFb-DIR 62 片：训练集 37 片加测试集 25 片，
测试集的背景片文件名是纯数字，靠 `test_labels.txt` 反查出来；DroneRFa 3 片），
再写能量检测器的参考实现与标定脚本，最后跑出虚警率。

| 路径 | 内容 |
|---|---|
| `algos/reference/energy_detector.py` | 能量检测器**参考实现**：检测量定义、H0 分布与门限、噪声估计。P1-4 的 C++ 版本须复现它的结果 |
| `algos/reference/ds6_false_alarm.py` | DS-6 标定脚本，含合成对照与逐频段扫描 |
| `tools/iq_format/store.py` | 本项目格式的读取端（写在 `writer.py`，读在这里，别处不再手写 `np.fromfile`） |
| `tests/unit/test_energy_detector.py` | 12 项单测 |
| `data/iq/measured/ds6-false-alarm-report.md` | 报告 |

#### 2. 检测器的口径（改动即为基准变化）

按帧长 1024 切帧、**不加窗、不重叠**；每帧 DFT 后在 M 个频点上取能量，
除以噪声估计得归一化检测量 Λ，H0 下 Λ ~ Gamma(M,1)/M、均值为 1，
虚警率 Pfa(η) = Q(M, M·η)，Q 是正则化上不完全伽马函数。

**不加窗是刻意的**：汉宁窗会让相邻频点相关，H0 的卡方自由度就不再是 2M，解析虚警率随之失效。
摸底工具算功率谱用汉宁窗是为了看谱形，两处口径不同且各自写明了理由。

Q 函数自己实现，不引入 scipy。整数 a 有精确闭式 `Q(n,x) = e^{-x}·Σ_{k<n} x^k/k!`，
单测拿它对拍：级数与连分式两条路径在 a = 4、16、128 上的相对误差都是 1e-10 量级。

噪声估计取**逐频点的帧维中位数除以 ln2**（指数分布的中位数是 σ²·ln2）。

#### 3. 一处推错了又改正的地方

原先在文档与单测里写「突发会把中位数估计拉低」，**方向反了**。被抬高的帧排到分布顶端，
剩余样本的中位数落在原分布的 0.5/(1−d) 分位上，所以是**拉高**：

| 突发占空比 | 中位数落在原分布的分位 | 估计偏差 |
|---|---|---|
| 5% | 0.5263 | **高估 0.33 dB** |
| 10% | 0.5556 | 高估 0.68 dB |
| 20% | 0.6250 | 高估 1.51 dB |

单测实测 1.0780，与解析值 1.0780 吻合。方向要紧：**高估噪声意味着门限偏高、虚警率偏低**，
所以下面测到的超标是保守的，真实情况只会更差。

#### 4. 结果

先看合成复高斯白噪声对照——这一项验证的是实现，不是数据：

| 目标虚警率 | DroneRFb 口径实测 | DroneRFa 口径实测 |
|---|---|---|
| 1e-2 | 9.67e-3 | 1.00e-2 |
| 1e-3 | 9.04e-4 | 1.05e-3 |
| 1e-4 | 1.11e-4 | 8.81e-5 |

全部落在目标值的两成以内，实现与门限公式没问题。**真实背景**（全频段合并）：

| 数据集 | 目标虚警率 | 实测虚警率 | **超标倍数** | 门限需抬高 |
|---|---|---|---|---|
| DroneRFb-DIR（62 片，242172 帧/频段） | 1e-2 | 2.47e-1 | **24.7 倍** | +22.7 dB |
| | 1e-3 | 2.32e-1 | **231.7 倍** | +32.6 dB |
| | 1e-4 | 2.22e-1 | **2222.7 倍** | +34.3 dB |
| DroneRFa（3 片，419895 帧/频段） | 1e-2 | 4.85e-2 | **4.8 倍** | +7.3 dB |
| | 1e-3 | 2.65e-2 | **26.5 倍** | +24.5 dB |
| | 1e-4 | 2.08e-2 | **207.7 倍** | +30.2 dB |

06 备忘录 §11.4 DS-6 当初的预期是「按高斯白噪声假设推出的门限会明显超标」，
**结论方向对，量级比预期大得多**：不是几倍，是两个到三个数量级，且实测虚警率在两成上下饱和。

饱和是重尾造成的：目标定得越严，白噪声假设给出的门限越贴近噪声均值，而真实背景里
「高出噪声十几分贝」的帧比比皆是，判决几乎全部落在门限之上，于是实测值不再随目标下降。

#### 5. 逐频段：干扰不是均匀铺满的

DroneRFb 背景，中心 2.44 GHz，10 MHz 一段（Λ 均值在 H0 下应为 1）：

| 绝对频率 | 频段内 WiFi 信道跨度 | Λ 均值 | 实测虚警率（目标 1e-3） |
|---|---|---|---|
| 2400–2410 | 信道 1 | 14.78 | 3.34e-1 |
| 2410–2420 | 信道 1 | **43.12** | 3.18e-1 |
| 2420–2430 | 信道 1、6 | 9.87 | 2.73e-1 |
| 2430–2440 | 信道 6 | **5.19** | 1.87e-1 |
| 2440–2450 | 信道 6 | 6.77 | 1.67e-1 |
| 2450–2460 | 信道 11 | 11.99 | 1.70e-1 |
| 2460–2470 | 信道 11 | 15.34 | 2.40e-1 |
| 2470–2480 | 信道 11 | 9.03 | 1.64e-1 |

**不是「三个信道都最差」**：信道 1 跨度内最高（43.1），信道 11 跨度次之，而信道 6 跨度内
反而最低（5.2）。合理解释是那次采集环境里信道 1 与 11 有活跃接入点、信道 6 没有。

这正是真实背景与白噪声假设的区别：**干扰有具体的占用格局，而这个格局只能实测得到，
不能从模型假设推出**。若虚警率与频率完全无关，反倒说明实现或标定出了问题。

#### 6. 三条必须与结论一起引用的限制

1. 这里的「虚警」指**检测器在没有无人机时判为有信号**。背景里确实有 WiFi 与蓝牙在发射，
   所以这些判决在物理上不是无中生有，而是把别的发射源当成了目标。能量检测本身不区分信号
   种类，这是它的固有局限，也正是 M2 层要靠特征与识别来补的原因。
2. 噪声估计用逐频点帧维中位数。**若某频段占空比超过一半，中位数估的就不再是噪声而是
   「信号加噪声」**，门限被抬高、虚警率被低估。所以本次的超标倍数是**下界**。
3. 加上第 3 节那条 0.33 至 0.68 dB 的高估，两条都使虚警率偏低。真实情况只会更差。

#### 7. 这个数字往下游怎么用

- **修正门限比理论门限高 20 至 35 dB**，意味着在这两批真实背景里，纯能量检测要达到设定
  虚警率，只能对比 WiFi 还强 20 dB 以上的信号动作。这不是「调一下门限就解决了」，
  而是该频段上能量检测的**可用灵敏度被环境抬高了这么多**。
- 跨层一致性算例 ① 因此要分两种背景各做一遍：白噪声背景（解析 Pd 曲线可对拍）与真实背景
  （只能给经验曲线）。两者的差距就是本条日志的数字，**这个差距本身是 P3 要交付的结论之一**。
- DS-7（真实背景加合成目标）现在具备全部前置：背景源已入库、检测器与门限口径已定。

#### 8. 本条目产生的待办

- [ ] DS-7：把合成目标按可控信噪比注入真实背景，出检测概率曲线，与白噪声版本并排比较。依赖 P1-3 的合成源，或先用参考实现直接生成。
- [ ] 逐频段结论只在这两次采集上成立。甲方数据到货后必须在其自己的环境里重测——**电磁环境不可移植**。
- [ ] 检测器目前只有单帧判决，没有多帧累积与门限自适应（CFAR 的常见做法）。P1-4 实现时要一并考虑，届时本条的数字是对照基线。

---

### 2026-09-04 · DS-7 真实背景加合成目标：检测概率曲线与跨层算例 ① 首次通过

#### 1. 本次做了什么

把可控强度的合成目标注入真实背景，测检测概率随信噪比的变化，并与白噪声背景并排比较。
一次覆盖两件事：04 §15.2 标准算例第 10 项「实测背景加合成目标」，以及**跨层一致性算例 ①**
（解析检测概率公式对 M3 蒙特卡洛）。

新增 `algos/reference/ds7_pd_curves.py`，检测概率的解析式加进 `energy_detector.py`，
单测由 12 项增至 17 项。报告 `data/iq/measured/ds7-pd-curves-report.md`。

#### 2. 公式侧是自己选定的，不是现成的

EM-S-02 概念模型只要求模型输出检测概率，并允许「解析近似或查表」（K-EM-CM-S-002-002），
**没有钉死闭式**。所以跨层算例 ① 的公式侧由本项目显式选定（D-026），与检测量定义严格配套：

| 目标类型 | 解析式 | 说明 |
|---|---|---|
| 随机型（带限噪声，类图传） | `Pd = Q(M, M·η/(1+s))` | 信号把每个频点功率等比放大 (1+s)，分布只是 H0 的 (1+s) 倍 |
| 确定型（单音） | `Pd = Σ_k Poisson(k; M·s)·Q(M+k, M·η)` | 非中心卡方写成中心卡方的泊松混合，即广义 Marcum Q 的级数形式 |

第二式这样写的好处是**不引入第二套特殊函数**，只依赖已经对精确闭式验证过的 Q。
两式在 s → 0 时都精确退化为虚警率，单测钉住。

#### 3. 三个口径上的讲究

**信噪比的分母取「安静时底噪」而不是「背景平均功率」。** 真实背景的平均功率被 WiFi 突发主导，
用它作分母会把环境的恶劣程度悄悄折进信噪比里，两种背景就不可比了。用安静底噪作分母，
同一个信噪比数值在两种背景下代表同样强度的目标，环境差异就干净地落在门限与检测概率上。

**门限分三档，把两种代价拆开**：① 解析门限；② 在测试数据自身上标定的门限（理想上界，
代表纯环境代价）；③ 在另一半背景上标定、拿到测试半区用的门限（现实做法，额外含标定迁移代价）。

**标定与测试各用一半帧、互不重叠**，否则门限会拟合到测试数据上。

#### 4. 跨层一致性算例 ① 通过

白噪声背景下，解析公式与蒙特卡洛的检出率最大绝对偏差：

| 频段 | 随机型 | 确定型 |
|---|---|---|
| 2410–2420 MHz | 0.0022 | 0.0016 |
| 2430–2440 MHz | 0.0050 | 0.0040 |

04 §16.3 建议的检出率容差是 5 至 10 个百分点，**实测偏差比它小两个数量级**。

这是 D-014 记的 P1 退出条件之一第一次真的跑出来。范围要说清楚：**通过的是参考实现侧**
（Python 参考检测器 + 解析式）；P1 退出条件要求的是引擎侧（C++ 分块流组件）复现同一结果，
那要等 P1-2、P1-4 落地后重跑。本次结果是届时的对照基线。

#### 5. 环境代价：23.6 到 38.9 分贝

达到检出率 0.9 所需的信噪比（目标虚警率 1e-3）：

| 频段 | 目标波形 | 白噪声背景 | 真实背景（理想标定） | **环境代价** |
|---|---|---|---|---|
| 2430–2440 MHz（最静） | 随机型 | −3.3 dB | +20.3 dB | **+23.6 dB** |
| | 确定型 | −3.4 dB | +19.9 dB | **+23.2 dB** |
| 2410–2420 MHz（最吵） | 随机型 | −3.3 dB | +35.6 dB | **+38.9 dB** |
| | 确定型 | −3.4 dB | +34.9 dB | **+38.3 dB** |

**同一台接收机、同一个检测器、同样的虚警率要求，换到真实城市背景，能检出的最弱目标要强
23 到 39 分贝。** 频段选得好不好，本身就值 15 分贝。

顺带一个结果：随机型与确定型只差 0.3 至 0.7 分贝。能量检测本来就只看总能量、不看能量怎么
分布，所以「把能量集中在一个频点」这件事对它几乎没有好处——这也解释了为什么要往特征检测走。

#### 6. 一个没预料到的发现：门限标定换一批数据就失效

第 ③ 档门限（在另一半背景上标定）拿到测试半区，实测虚警率是目标值的 **7.0 到 7.2 倍**。

做了一个判别实验：把同一批帧**随机打散**后再分半标定，虚警率回到 9.83e-4 与 1.05e-3，
基本达标。两种做法的唯一差别是分法——按时间与文件顺序分，还是打散分。

**所以失配来自背景本身随时间与地点漂移，不是统计涨落。** 这条结论直接影响工程实现：
静态门限在真实环境里不成立，必须用滑动噪声估计或恒虚警率处理。P1-4 实现能量检测时，
「单帧固定门限」只能作基线，不能作交付形态。

还要注意代价出现的位置：迁移失配**不体现在灵敏度上，而体现在虚警率上**。第 ③ 档门限比理想
门限低，所以检出率反而略高（所需信噪比低 0.7 至 1.5 分贝），代价是虚警率高出目标 7 倍。
两者是同一件事的两面：门限定低了。

#### 7. 为什么不单列突发一档

对「按帧取总能量」的检测器，**同一帧平均信噪比下突发与连续等价**：帧内能量只取决于总能量，
与它在帧内怎么分布无关。单测 `test_burst_equals_continuous_at_same_frame_snr` 实测验证了
这一点（占空比 0.1 的突发与等平均功率的连续信号，检出率差 < 0.03）。

突发的真实影响是另外两条，都可解析给出，不需要单列曲线：只有含突发的帧才有检出机会；
突发短于帧长时能量被摊薄，等效帧平均信噪比下降 10·lg(占空比)。

#### 8. 本条目产生的待办

- [ ] 引擎侧实现后重跑跨层算例 ①（P1-2、P1-4 之后），与本次的参考基线对拍。
- [ ] P1-4 的能量检测组件要按第 6 节的结论做成滑动噪声估计或恒虚警率，不能是静态门限。
- [ ] 跨层算例 ② ③ 仍不可做（两个公开数据集都无视距/非视距标签、无高度与航迹），等甲方数据。
- [ ] 本次只用了 DroneRFb 背景。DroneRFa 背景（100 MS/s、另一次采集）应当重跑一遍，看环境代价的量级是否随采集环境变化——DS-6 显示它比 DroneRFb 缓和。

---

### 2026-09-04 · D0-3 建筑解码：47582 栋，几何与底图逐像素对拍

#### 1. 本次做了什么

按 06 备忘录 §4 执行 D0-3：从本地全球底图的 z15 瓦片解码 `buildings` 图层，跨瓦片按 id 合并，
产出 `data/scene/beijing-yayuncun/buildings.geojson`。这份文件是建筑几何的**单一来源**，
同时驱动三维拉伸渲染与遮挡桶网格（铁律 11），所以几何对不对必须验到底，不能只看"渲染出来
像那么回事"。

产物：`scene/decode_buildings.py`、`buildings.geojson`（47582 栋、15919279 字节、
sha256 `65ae4dce…`）、`buildings.manifest.json`。

#### 2. 四条处理规则与它们的依据

| 规则 | 依据 |
|---|---|
| 跨瓦片按 id 合并，同 id 的碎片做**并集还原** | D-025 实测 id 稳定。不能任选一片：跨瓦片的建筑在每块瓦片里都被裁过，任选会沿瓦片边界留下一条直边，那是假墙，会直接污染遮挡计算 |
| 只保留 `kind=building` | `building_part` 是同一栋楼的子体量，保留会重复计入；`address` 实测是点要素（几何类型 1），不是轮廓 |
| 瓦片 `height` 一律标 `src=tile:height` | D-025：瓦片的 `height` 把真实标注与 planetiler 按 `building:levels x 3 + 2` 推导的值混在一起，标 `osm:height` 会把 19.4% 的推导值说成实测（铁律 14） |
| 无高度的按占地面积估高，标 `src=est:area` | 分档沿用 em-demo `fetch-buildings.mjs` 的 `building=yes` 面积档。瓦片没有用途标签（实测 `kind_detail` 对 `building` 全为空），用不上它的用途档 |

与 em-demo 参考实现有两处**有意偏离**，已写进脚本文档字符串以免被当成缺陷：估高的伪随机
种子取质心而不是轮廓首顶点（并集后顶点顺序可能随几何库版本变化，质心不会，产物因此可复现）；
本产物有 id 而 em-demo 那份没有，id 是后续"只升级高度、不动几何"的前提。

#### 3. 产物统计

| 项目 | 值 |
|---|---|
| 建筑数 | 47582（合并前 54735 片，其中 6447 个 id 跨瓦片） |
| 体积 / 顶点 | 15919279 字节 / 347045 个顶点，平均每栋 7.3 个 |
| 高度来源 | `tile:height` 10195（21.4%）、`est:area` 37387（78.6%） |
| 高度分位 | q10 5 m、q50 20 m、q90 38 m、p99.9 100 m、最高 528 m；超 100 m 的 47 栋、超 200 m 的 16 栋 |
| 占地面积 | q50 667 m²，合计 49.7 km²（占瓦片对齐覆盖区 464 km² 的 10.7%） |
| 多边形形态 | 带洞 254 栋，多重多边形 36 栋 |
| `base_m` | **全部为 0** |

`base_m` 恒为 0 不是缺陷而是规则的结果：瓦片里带 `min_height` 的 824 个要素**全部**是
`building_part`，而 `building_part` 按规则剔除。这一条已写进 `docs/scene-package.md`，
真正的架空层高度要等 D0-4 的 Overpass 标签。

#### 4. 几何验证：与底图自带建筑层逐像素对拍

先做了两项分析检查：投影往返误差为 0.0000 m；最高的那栋 528 m 建筑底面实测 80.6 × 76.9 m，
与中信大厦的实际尺寸吻合。**但不能用记忆里的地标坐标当真值**——我最初拿来对照的中信大厦
经纬度与产物差了约 300 m，方向与量级恰好是 GCJ-02 相对 WGS-84 的偏移特征，说明可疑的是那个
参考坐标而不是数据。这类核对一律只能用同源或已知基准，不能凭记忆。

真正的判据是把解码结果与**底图自己渲染的建筑层**在同一相机下逐像素比。做法：把两层都设成
纯红色填充，各渲染一帧，取画布像素算交并比。

第一轮得到覆盖率 77.9%、外溢 10.0%，看着像是几何偏小。查根因发现是**测试本身的缺陷**：
Airports 样式给建筑层单独设了描边色 `PM.bldgCase`，我只把填充改成红色，描边仍是原色，于是
每栋楼一圈描边像素被判成"只在解码结果"。把两层描边色统一后重测：

| 视角 | 交并比 | 只在瓦片层 | 只在解码结果 |
|---|---|---|---|
| 亚运村中心 z16 | 0.9928 | 248 | 909 |
| 亚运村中心 z19 | 0.9964 | 241 | 336 |
| 中信大厦 z18 | 0.9994 | 151 | 50 |

三个视角的双向残差都在几百像素、约 16 万交集像素的千分之几量级，是抗锯齿边的量级。**结论：
解码几何与底图自带建筑层一致。** 证据图 `verify-buildings-overlay-z16.png` 留在产物目录（不入 git）。

#### 5. 一条方法上的教训

第一轮那个 77.9% 如果当成"基本吻合"接受，或者反过来当成"几何有偏移"去调参，都会出错。
差异的根因是测试的渲染配置不对等，不是被测对象。**指标不对先查测量方法，再查被测物**——
这与铁律 10「超差 = 发现，查根因，禁止调参使其通过」是同一条纪律。

#### 6. 本条目产生的待办

- [ ] D0-4 拉 Overpass 原始标签（需联网）：既是对拍基准，也是把 `tile:height` 拆成
      `osm:height` / `osm:levels` 和填 `base_m` 的唯一途径。
- [ ] D0-6 区域总清单：按 sha256 引用共享底图、DEM 与本产物。
- [ ] D0-7 质量报告。
- [ ] 15.9 MB 的 GeoJSON 直接进浏览器在 D1 要实测帧率；若吃力则考虑按瓦片切分，但**几何单一
      来源不变**（铁律 11），切分只是传输形式。

---

### 2026-09-04 · D0-4 / D0-6 / D0-7：拉原始标签对拍，D0 场景数据包完成

#### 1. 本次做了什么

把 D0 剩下的三步做完：D0-4 从 Overpass 拉原始 OSM 标签并与瓦片解码结果对拍，D0-7 出质量报告，
D0-6 出数据包入口清单。**这是本项目至今唯一一次联网动作**，只发生在 `scene/` 的建库脚本里
（铁律 6），运行阶段与交付环境仍然不联网。

新增三个脚本：`scene/fetch_osm_buildings.py`、`scene/quality_report.py`、`scene/make_manifest.py`。

#### 2. 对齐方式：按编号，不是按几何

对拍的前提是两侧要素能一一对上。瓦片要素的 id 是个很大的整数（例如 35184971636750），
实测其编码为 `(类型 << 44) | OSM 编号`，类型 2 是 way、3 是 relation：47231 个 way 去掉类型位
后落在 4974233 至 1549914082，351 个 relation 落在 563404 至 21091607，都在 OSM 编号的合理
区间。**因此可以按 (类型, 编号) 精确对齐，不需要几何近邻匹配**，也就不会有匹配阈值这种可调
参数混进来。

#### 3. 对拍结果

拉到 47572 个原始要素、34.4 MB。第一个镜像返回 504，第二个 6.1 秒完成。

| 项目 | 结果 |
|---|---|
| 精确对上 | 47449，瓦片侧 99.72%、OSM 侧 99.74% |
| 只在一侧 | 瓦片侧 133，OSM 侧 123 |
| 面积相对差 | 中位 +0.008%，绝对值 q90 1.19%、q99 4.01%，超 5% 的 297 栋（0.63%） |
| 总占地 | 47979536 对 48207452 平方米，相差 −0.473% |
| 顶点数 | 338768 对 332902，即 101.8% |

**两侧各百余个对不上，根因是数据时间不同**：瓦片来自 OSM 快照 `2026-08-17T04:00:00Z`，
原始标签是 `2026-09-04T02:57:36Z` 的实时数据，中间 18 天的编辑。这条写进了报告的"三条不可比
因素"一节，放在所有差异数字之前——不先说清不可比因素，后面的差异就会被误读成管线缺陷。

#### 4. 一处写错又改对的结论

质量报告的模板里我预先写了一句"顶点数比原始少的部分是瓦片简化的代价"，而实测是 **101.8%**，
比原始**多**。数字与结论矛盾，说明结论是我预设的，不是算出来的。

查根因：81.6% 的建筑顶点数与原始完全相同；多出顶点的 4666 栋中位多 2 个，少顶点的 3978 栋。
抽样取证——**多出顶点的建筑里横跨两块瓦片的占 56.6%，而顶点数相同的对照组只占 1.7%**，所以
多出来的顶点来自跨瓦片合并的接缝，少掉的才是瓦片简化。已把报告改成由数据算出结论并附上这组
对照，不再写死。

教训与前一条日志同源：**报告里的每句结论都必须由当次数据算出来，模板不能预设结论。**

#### 5. 高度来源定案（D-027）

| 来源 | 数量 | 占比 |
|---|---|---|
| `osm:height` 真实标注 | 828 | 1.74% |
| `osm:levels` 层数折算 | 9322 | 19.59% |
| `tile:height` 对不上原始标签 | 56 | 0.12% |
| `est:area` 面积估算 | 37376 | 78.55% |

D-025 用瓦片启发式判据估的是"推导值约 19.4%、真实标注上界 3.7%"，实际是 19.59% 与 1.74%：
**方向正确、上界成立**，但只有原始标签能给出可用于标注 `src` 的定论。

层数折算固定用 **层数 × 3 + 2 米**，与 planetiler 一致（+2 计入女儿墙与屋面构筑物，对绕射
是有效遮挡高度），**与 em-demo 的 × 3.0 有意差 2 米**，已在脚本与规范里写明。层数标注为 0 或
负数的（实测 9 栋）视为无效，退回面积估算，不按 planetiler 那样当成 1 层。

`base_m` 仍然几乎全零（47582 栋里只有 9 栋非零）：原始 OSM 在该区域只有 7 个要素带
`min_height`、4 个带 `building:min_level`。架空层信息在数据源里就没有，这不是管线问题。

#### 6. 端到端验证

用产物的 `height_m` 驱动三维拉伸，压在离线底图上渲染（`verify-buildings-3d-z15p4.png`，
不入 git）：安慧里一带的高层住宅、北四环沿线的低层、奥体一带的大底盘建筑层次分明，按 `src`
分色后可以一眼看出近八成是浅色的估算件。加上此前的两项几何验证（与底图建筑层交并比
0.993 至 0.999、与原始 OSM 面积中位差 +0.008%），几何与高度都走通了端到端。

#### 7. D0 收口

七步全部完成。数据包入口是 `data/scene/beijing-yayuncun/manifest.json`，它把区域定义、共享
资产（按 sha256 引用，不复制）、7 项区域产物的哈希、两个 OSM 时刻的溯源链、以及复跑命令串成
一条可核对的链。ASCII 检查全部合规。

**这个数据包能支撑什么、不能支撑什么**：几何可用于视距与遮挡计算；**高度不可用于验收指标**，
近八成是估算值。要做建筑遮挡的跨层一致性算例（算例 ②），仍然缺的是甲方提供的真实建筑高度，
或者在小范围内人工核对的高度。这一条与数据线上"实测 IQ 仍缺绝对功率标定"是同一性质的欠项：
可以推进管线，不能据此下定量结论。

#### 8. 顺手修掉的一处忽略规则缺口

暂存时发现数据包入口文件 `manifest.json` **没有进版本控制**。根因是 `.gitignore` 的放行规则写的是
`!data/**/*.manifest.json`，这个模式匹配不到不带前缀的 `manifest.json`——`*` 后面必须还有内容。
已补一条 `!data/**/manifest.json` 并加注释说明为什么不能省。

这是本项目第二次在 `.gitignore` 上踩坑（上一次是公开数据集目录未排除）。教训是**放行规则必须
逐个用 `git check-ignore -v` 实测**，不能靠读模式判断。

#### 9. 本条目产生的待办

- [ ] D1-1 Node 版 Range 文件服务，语义对齐 Airports `serve.py`。
- [ ] D1 阶段实测 15.9 MB 建筑数据进浏览器的帧率；吃力则按瓦片切分，几何单一来源不变。
- [ ] 建筑高度的真值来源（甲方数据或小范围人工核对），这是算例 ② 的前置。
- [ ] 若 AOI 或 OSM 快照更新，按 `manifest.json` 的 `reproduce` 字段整条重跑。

---

### 2026-09-04 · DS-7 在 DroneRFa 背景上重跑：环境代价可迁移，但"平均占用度"不可用

#### 1. 为什么要重跑

DroneRFb 上测出的环境代价是 23.6 至 38.9 分贝。这个数字要能写进设计指标，先得回答一个问题：
**它是"城市 2.4 吉赫兹的普遍量级"，还是"那一次采集地点的特殊情况"？** 只有在另一次独立采集
上重跑才能分辨。两个数据集用**同一对绝对频段**，结果才可直接比较。

#### 2. 结果：两次采集相差约 6.4 分贝，但结论的形状完全一致

达到检出率 0.9 所需的信噪比（目标虚警率 1e-3，理想标定门限）：

| 频段 | 目标波形 | DroneRFb 环境代价 | DroneRFa 环境代价 | 差 |
|---|---|---|---|---|
| 2410–2420 MHz | 随机型 | +38.9 dB | +32.4 dB | 6.5 |
| | 确定型 | +38.3 dB | +31.8 dB | 6.5 |
| 2430–2440 MHz | 随机型 | +23.6 dB | +17.3 dB | 6.3 |
| | 确定型 | +23.2 dB | +16.8 dB | 6.4 |

**两个频段、两种波形，差值都是 6.3 到 6.5 分贝，一致得出乎意料。** 可以这样用：
环境代价的量级（十几到近四十分贝）是共性，具体数值随采集环境整体平移，平移量在本次两批
数据之间是 6.4 分贝。**写设计指标时应当给区间并注明它随环境平移，不能给单值。**

其余三项结论在 DroneRFa 上同样成立：

| 项 | DroneRFb | DroneRFa |
|---|---|---|
| 跨层一致性算例 ① 最大偏差 | 0.0016–0.0050 | 0.0027–0.0044 |
| 解析门限在真实背景的实际虚警率（目标 1e-3） | 2.07e-1 至 2.74e-1 | 1.01e-2 至 5.83e-2 |
| 门限跨半区标定的失配 | 7.0–7.2 倍 | 1.4–2.4 倍 |
| 随机型与确定型之差 | 0.3–0.7 dB | 0.6–0.7 dB |

**算例 ① 在一批独立数据上再次通过**，这比单批通过更有意义：它说明对拍成立与具体背景无关。
门限迁移失配在 DroneRFa 上明显缓和（1.4–2.4 倍对 7 倍），与它背景更干净一致，但方向相同，
所以"静态门限不成立"这条结论不变。

#### 3. 一处表面矛盾，查清了

DS-6 报的逐频段 Λ 均值里，DroneRFa 的 2430–2440 一格（8.68）比 2410–2420 一格（4.36）**更高**，
即"更吵"；而 DS-7 测出的环境代价却是 2430–2440 明显**更低**（17.3 对 32.4 分贝）。两者看似矛盾。

逐层查下来的结果：

1. 先怀疑底噪估计的窗口。实测**探针（开头 8192 帧）与整片估计完全一致（差 0.0 分贝）**，
   不是这个原因。
2. 再怀疑分段读取有 bug。逐 1000 万点扫描该文件，**读取正常**。
3. 真正的原因：那一格的 Λ 均值 8.68 由**一个文件独自拉高**——三片背景的 Λ 均值分别是
   1.32、2.51、**22.64**；而那个文件的高值又集中在**两段各约 0.1 秒的强发射**上
   （该处 Λ 均值 165、峰值 1611，其余时段只有 1.2）。

所以两个数字都没错，只是量的东西不同：**Λ 均值被极少数强事件主导，而检测门限由分布的高分位
决定。** 结论是一条方法学纪律：

> **不要用平均占用度预测检测代价。** 频段选择应当看高分位（99 分位、虚警率），
> 不看均值。DS-6 报告已补上 99 分位一列与这段说明。

顺带补了另一个原先没量的东西：**逐频段的安静底噪本身也不同**（同数据集内可比）。
DroneRFa 的 2430–2440 底噪比 2410–2420 高 2.0 分贝，DroneRFb 高 0.7 分贝——说明前者的干扰
更"平稳"，一部分被吸收进了底噪。把底噪差与门限抬升加在一起，两个数据集里 2430–2440 依然
都是更好的频段，**排序结论不受影响**。

#### 4. 本条目产生的待办

- [ ] 环境代价写进任何设计指标时，必须带区间与"随环境平移"的说明，不得给单值。
- [ ] 频段选择的判据统一改用高分位，均值只作参考。这条要带进 P1-4 的组件设计。

---

### 2026-09-04 · 批量转换全部入库，并冻结验收子集（DS-4 / DA-4）

#### 1. 批量转换

| 数据集 | 产物数 | 体积 | 说明 |
|---|---|---|---|
| DroneRFb-DIR | 4690 | 70 GB | 训练集 2177 + 测试集 2513，全部单通道 |
| DroneRFa | 24 | 14 GB | 22 片的 RF0，外加 `T10010` 两片的 RF1（该机型通道映射相反，信号在 RF1） |

**4714 份清单逐一核对：回读对拍失败 0，质量状态异常 0**（全部为 `degraded`，原因一致，
都是采集参数来自论文而非设备记录）。实测速度与之前外推吻合：DroneRFb 单片约 0.15 秒。

#### 2. 冻结验收子集

清单 `data/iq/measured/holdout.manifest.json`（入库，工具 `tools/freeze_holdout.py`）。

**DroneRFb-DIR：验收集 2487 片。** 用出版方划好的测试集（2513 片），减去 26 片：

| 排除的片 | 数量 | 理由 |
|---|---|---|
| 测试集里的纯背景 | 25 | DS-6 虚警率标定与 DS-7 检测概率测量用它们做过背景源与门限标定 |
| `D1_IN_S2_slice_47` | 1 | 转换脚本首次跑通时的验证样本。它并未参与任何参数选择，仍按保守原则排除 |

2487 + 26 = 2513，与出版方测试集片数核对一致。其中**从未在训练集出现的 6 架个体
（A3/C3/D3/E3/F3/G3）共 1580 片**，单列出来作个体识别泛化能力的关键子集——出版方给这 6 架
的片数（236 至 313）明显多于已见个体（51 至 106），显然是有意为之。

**DroneRFa：没有可用的验收集，一片都没有。**

这不是疏忽，是入手时还没有冻结机制的直接后果：本地 22 片**全部**已经用于探索与拟合——
DA-6 路损指数、DA-7 正交不平衡、DS-6 与 DS-7 的背景源。按项目记忆 `holdout-freeze-at-ingest`
的规矩，**用过的数据不能追认为验收集**，否则验收结果不作数。

补救办法只有一条：验收集从后续按 `download-list.md` 补下的片里划，**下载当天立即冻结，
冻结前不得用于任何分析**。这条已写进冻结清单的 `next_action` 字段。

#### 3. 检查工具

`--check` 双向验证过：
- 拿训练集的 171 个产物检查 → 「无交集，可用于调参」。
- 故意拿验收集里的 `dronerfb_1000_CH0_S4` 去检查 → 「相交 1 个，不得用于调参」。

拦截同时按 `data_id` 与 `content_sha256` 两条，**换个文件名也拦得住**。

冻结工具对已存在的清单直接拒绝覆盖，并提示「冻结只做一次，要重做须先说明理由并留档」。

#### 4. 入库方式随之改了一处（D-027）

原先 `.gitignore` 把所有 `*.manifest.json` 都放进版本控制。产物只有几个时没问题，
到 4714 个就是**近五千个小文件、约 19 MB**，既不好查也不好比。改成：

| 入库 | 不入库 |
|---|---|
| 每批一份合并索引 `index.manifest.json`（每产物一行：标识、来源、真值摘要、样点数、`content_sha256`、质量状态） | 逐产物清单 `<data_id>.manifest.json` |
| 冻结清单 `holdout.manifest.json` | 样点文件 `<data_id>.iq` |

逐产物清单由 `tools/iq_convert.py` 确定性重生成，**索引里的 `content_sha256` 足以证明盘上的
产物与入库时是同一份**。这更贴合 CLAUDE.md「大文件不入 git，只入索引与元数据」的原意。
工具 `tools/build_batch_index.py`。

#### 5. 本条目产生的待办

- [ ] DroneRFa 的验收集：补下新片时当天冻结。这件事必须在下一次动 DroneRFa 数据之前做。
- [ ] 后续任何拟合、门限标定、模型训练，跑之前先过一遍 `--check`，把结果记进 WORKLOG。

---

### 2026-09-04 · P1-2 引擎核心：分块流与调度落地，能量检测与参考实现逐帧对拍通过

#### 1. 本次做了什么

按 D-028 定的顺序（先打通薄的端到端切片，再补厚设计），从引擎这一端开工。
`engine/` 从空骨架变成可跑的分块流引擎，23 项单测全绿。

| 文件 | 内容 |
|---|---|
| `include/cuav/types.h` | 样点块与块元数据、端口类型、四态、溯源八件套、`SceneParamFrame` |
| `include/cuav/random.h` | `IRandom` 接口与 xoshiro256++ 实现（铁律 9：随机性显式注入） |
| `include/cuav/dsp.h` | 基 2 FFT、fftshift、正则化上不完全伽马、门限求解 |
| `include/cuav/graph.h` | 有向无环图、连线校验、拓扑排序、单线程调度 |
| `include/cuav/components/sources.h` | `ToneSource`、`NoiseSource`、`FileReplaySource` |
| `include/cuav/components/processing.h` | `AddMixer`、`EnergyDetector`、`DetectionSink` |

第三方件 vendored 自 emcore（doctest 与 nlohmann/json），保留原文件头，构建零网络（铁律 13）。
占位文件 `src/placeholder.cpp` 与冒烟测试按其自身注释的约定删除，版本接口挪进 `types.h`。

#### 2. 几条规矩落成了可执行的代码，不再只是文档里的话

- **D-013 的连线规则在 `connect()` 里执行**：`IQStream` 与 `SceneParamFrame` / `ChannelPathSet` 直连会被当场拒绝并给出理由，而不是等运行时出怪结果。单测覆盖。
- **四态沿块元数据向下传播**：`degrade()` 累积理由，且**降级不能把已判 `invalid` 的块"治好"**（单测钉住）。文件回放源把清单里的 `degraded` 与"量化码未标定"两条理由都带给下游。
- **缺必填参数一律报错，不拿默认值顶替**（铁律 15）：`ToneSource` 缺 `total_samples`、`FileReplaySource` 缺 `manifest_path`、清单字节序不是小端，都是配置期失败。
- **末尾不足一帧的样点直接丢弃并记状态说明，不补零**——补零会在频谱上造出假信号。
- **输入口必须全连**：留空口会让组件收到空输入而静默产出错误结果，所以 `validate()` 直接拒绝。

#### 3. 调度器有个真 bug，测试逼出来了

第一版跑三组件链路时报"调度停滞"。原因是**上游结束后没有把"结束"传给下游**：源停产后，
下游的输入缓冲永远空着，既不会被调度也不会收尾，于是全图卡死。

修法是给每个节点算出上游集合，当"上游全部结束且本节点输入缓冲已排空"时，本节点也收尾。
这是数据流调度的标准终止条件，第一版漏了。**如果当时只测单组件，这个 bug 会一直留到接服务时才炸。**

#### 4. 一条被测试逼出来的真实局限

原本写了个"单音加噪声必然检出"的用例，结果检出率是 0。查下来不是 bug：
**噪声估计取逐频点的帧维中位数，若信号从第一帧起就一直在，它就成了"底噪"的一部分。**

这不是缺陷而是这套判据的固有性质，于是拆成两个用例把行为钉住：

| 用例 | 结果 |
|---|---|
| 连续满带信号（从第一帧就在） | 检出率 < 0.05，**信号被中位数估计吸收** |
| 信号在探针窗口之后才出现 | 检出率 = 有信号帧占比 0.75，误差 2% 以内 |

工程含义写进了代码注释与 P1-4 的待办：**估底噪必须有一段无信号的窗口，或者用带外参考频段**。
这与 DS-7 得到的"静态门限不成立、要做恒虚频率处理"是同一件事的两个侧面。
`ToneSource` 因此加了起止样点参数，突发算例（04 §15.2 第 3 项）也要用它。

#### 5. 与参考实现的对拍：跨层算例 ① 的引擎侧第一次跑通

黄金基准由 `algos/reference/gen_engine_golden.py` 生成。**两侧从同一个种子各自生成输入**，
所以不需要往版本库塞二进制夹具——代价是要在 Python 里复刻 xoshiro256++ 与 Box-Muller，
这份复刻与 `engine/src/random.cpp` 逐行对应，改动即为基准变化。

| 项目 | 参考实现（Python，float64） | 引擎（C++，内部 float32） | 相对偏差 |
|---|---|---|---|
| 频段内频点 | 51 | 51 | 一致 |
| 门限 η | 1.354259521075 | 1.354259521075 | **2.3e-15** |
| 帧数 | 4000 | 4000 | 一致 |
| **命中帧数** | 39 | 39 | **逐帧一致** |
| 检测量最大值 | 1.5238738912 | 1.5238738962 | **3.3e-9** |

判据分三档并写进黄金文件：门限 1e-9（两侧同为 float64 同算法）、逐帧检测量 1e-5
（引擎内部按规范用 float32）、判决结果逐帧一致。**实测门限已到机器精度，检测量偏差比判据
严 4 个数量级**；判据仍留在 1e-5，因为不同编译器与平台的 float32 累加次序可能不同，
CI 要在 Windows 与 Linux 上也过。

范围要说清楚：这验证的是**组件级**复现。P1 退出条件要求的是整条链路在引擎里跑通并与
M1/M2 公式对拍，那还需要 P1-4 的观测量提取与 P1-5 的服务接口。

#### 6. 现在还没有的东西

背压与多线程调度、共享内存抽象、`SceneParamFrame` 的"施加"类组件、功率谱输出组件、
MATLAB Coder 链路。这些不是遗漏，是薄切片刻意不做的部分——接口里给它们留了位置
（组件不假设块大小、`Step::Idle` 表示"本轮没产出但没结束"、端口类型系统已含参数流）。

#### 7. 本条目产生的待办

- [ ] P1-5 服务最小接口：提交框图 JSON、查任务、WebSocket 状态、按视窗抽取谱数据。
- [ ] 功率谱输出组件（`SpectrumFrame`），前端最小页要靠它。
- [ ] 引擎在 Windows（MSVC）与 Linux（GCC）上首次构建验证——目前只在 macOS 上跑过，按 D-015 不作验收依据。

---

### 2026-09-04 · D1 离线底图上前端：样式逐层照搬并与 Airports 逐像素对拍

#### 1. 本次做了什么

把 D0 产出的场景数据包接到自己的前端上，完成 D1 的五个步骤。结束时浏览器里已经能离线看到
北京亚运村的底图、山体阴影和 47582 栋建筑的三维拉伸，数据全部经本地服务的 Range 请求取。

| 步骤 | 产物 |
|---|---|
| D1-1 Range 文件服务 | `server/src/{range,static,index}.ts`，零运行时依赖，30 项测试 |
| D1-2 web 骨架 | `web/src/scene/`，MapLibre 5.24.0 + pmtiles 4.4.1 随包 |
| D1-3 样式移植 | `style/{colors,protomaps,glyphs}.ts`、`layers/{hillshade,buildings3d}.ts`、`init.ts` |
| D1-4 精灵图 | 确认不需要：Airports 样式无 `sprite` 字段 |
| D1-5 探针 | `probe.ts` 的只读 `window.__probe()`，加 `tests/e2e/` 三件套 |

#### 2. 样式移植怎么验的：不看截图，看求值结果

"照搬不改"这件事不能靠肉眼比截图。做法是把 Airports `index.html` 里 `var PM = {` 到
`protomapsStyle()` 结束的整段抽出来在 Node 里求值，再把本项目移植的 TypeScript 模块求值，
两个样式对象逐层比 JSON 字符串：

| 项目 | 结果 |
|---|---|
| 图层数量 | 60 对 60 |
| 图层 id 与顺序 | 完全一致（顺序决定压盖关系，不能只比集合） |
| 逐层 JSON 不同的层数 | **0** |
| 数据源、字形地址、版本 | 一致 |

结果固化成黄金基准 `tests/golden/airports-basemap-style.json`，由 `protomaps.test.ts` 守着
（铁律 10：基准不得静默变）。以后谁改了色值或线宽档位，测试立刻挂。

#### 3. 与 Airports 的并排比对：99.83%，且不该是 100%

同一底图文件、同一相机、同一 1200 × 713 视口，两边都只留底图，逐像素比较：

| 指标 | 结果 |
|---|---|
| 容差 8 以内 | 99.83% |
| 逐位完全相同 | 93.72% |
| 平均绝对差 | 0.15（满量程 255） |

**不逐位相同是预期之内**：本项目用 MapLibre GL 5.24.0，Airports 用 5.6.1（决策 D-008 有意
锁定与 em-demo 同版本），两版在文字排布与抗锯齿上有亚像素差别；参照页还多 5 个它自己的机场
要素图层。判据取"看起来是不是同一张图"。

第一次跑出来只有 24.39%，原因是参照页 286 像素宽的侧栏没被隐藏，整幅地图右移了。隐藏侧栏、
搜索框、署名条与控件后才是可比的。**又一次是量法的问题，不是被测物的问题**——这已经是本项目
第三次踩同一类坑（前两次是建筑对拍的 77.9% 与顶点数结论），所以把"指标不对先查量法"写进了
`tests/e2e/README.md`。

#### 4. Range 服务的语义与三条路由规则

Range 按 RFC 9110 §14 实现，语义对齐 Airports `serve.py`。**多区间请求不支持**，按无 Range
处理返回 200 与完整内容——这是允许的降级，但必须写明，不能让调用方以为拿到的是部分内容。

三条路由规则都是有意为之，各有测试：

1. `/data/` 与 `/api/` 下的未知路径**一律 404，绝不回退首页**。第一版没有这条，结果请求
   `data/iq/`（本服务按铁律 7 不暴露原始 IQ）拿到的是 200 与一页 HTML，既掩盖错误又让人
   误以为该路径存在。
2. 单页应用回退**只对没有扩展名的路径生效**。缺失的静态资源要 404，不能返回 HTML 让浏览器
   在解析脚本时才报出莫名其妙的错误。
3. 路径穿越防护的安全契约写成"要么 null，要么落在根内"。原先的单元测试断言 `..` 一律返回
   null，实测发现绝对路径里的 `..` 会被规范化折掉、结果仍在根内——同样安全。**断言应该写
   性质而不是具体返回值**，写死返回值等于把实现细节当成契约，已改。

#### 5. 一个因为目录名有空格而静默失败的坑

服务编译后 `node dist/index.js` 起不来，日志一个字都没有，进程直接退出。根因是入口判定写成
`import.meta.url === \`file://${process.argv[1]}\``：仓库根目录名 `804 C-UAV` 含空格，
`import.meta.url` 把它编码成 `%20`，字符串比较永远不相等，于是 `start()` 从未被调用。
改用 `pathToFileURL(process.argv[1]).href` 后正常。凡是拼 `file://` 的地方都要注意。

#### 6. 端到端测试用自建调试协议客户端（D-029）

`tests/e2e/cdp.mjs` 一百余行，零依赖，只用 Node 内置的 `fetch` 与 `WebSocket`，覆盖开页面、
轮询探针、取截图三件事。理由是交付环境不联网、依赖须随包，为三件事引入一整套浏览器自动化
框架不划算。三个无头截图的坑一并写进了 `tests/e2e/README.md`。

`scene-smoke.mjs` 的 14 项断言全过：探针可用、WebGL2 可用、62 个图层（底图 60 + 建筑 + 山体
阴影）、三个数据源都挂上、瓦片加载完成、5613 个可见要素、地图无错误、相机落在观测区域内、
侧栏显示区域名、**估算高度有显式标注**（铁律 14，侧栏写明"高度中 78.6% 为按占地面积的估算值，
不得用于验收指标"）。

#### 7. 测试结果

| 范围 | 结果 |
|---|---|
| `server` 单元与集成 | 30 项全过 |
| `web` 样式黄金基准 | 6 项全过 |
| 端到端 | 14 项全过 |
| `web` 构建 | 通过，产物 1.26 MB（gzip 346 KB），ASCII 检查合规 |

`scripts/build-all.sh` 当前不全绿，失败的是 `engine` 的一项组件测试
（`test_components.cpp:210`，命中率 0.7505 对期望 0.6667）。那是并行会话尚未提交的工作，
不属于本次改动，未去动它。

#### 8. 本条目产生的待办

- [ ] D2 态势图层：状态流 schema、图层模块、Canvas 叠加层、**浅色底图上的色值重标定**。
- [ ] 建筑数据进浏览器的帧率实测。当前 15.9 MB 的 GeoJSON 一次性加载，端到端跑下来没有卡顿，
      但没有量过帧率；若吃力则按瓦片切分传输，几何单一来源不变（铁律 11）。
- [ ] 前端产物 1.26 MB 未做代码分割，Vite 已警告。单机模式下从本地加载不算问题，
      但打包给 Windows 时应复核首屏时间。
- [ ] Node 长期支持版本冻结后，`web` 与 `server` 的 `package-lock.json` 要重新生成并重验。

---

### 2026-09-04 · 清掉入库文件里的机器相关绝对路径（新增铁律 17）

#### 1. 起因

用户问仓库目录名 `804 C-UAV` 带空格会不会影响后面的工作。查证时发现空格本身影响有限——远端
仓库名是 `UAV_ER_Simu`，克隆、持续集成检出与交付包都不带空格，所以它只存在于开发机上——但
顺带扫出了一个更严重的问题：**入库文件里有写死的绝对路径**。用户随即定下规矩：路径必须是
项目内的相对路径，否则项目整体迁移或跨平台就会出问题。这条写成了铁律 17。

#### 2. 扫出了什么

写了 `scripts/check-paths.sh` 作判据：出现 `/Users/`、`/home/<用户名>`、`X:\Users\` 即不合规；
系统目录（`/Applications/`、`/usr/bin/`、`C:/Program Files/`）不算，那是查找浏览器、编译器
这类系统件的正当位置。首次运行命中 7 处，分三类：

| 类别 | 处数 | 后果 | 改法 |
|---|---|---|---|
| 脚本写死仓库路径 | 2 | **在任何别的机器上都跑不了**，与空格无关 | 从 `__file__` 推导根目录；数据集位置另给环境变量 `CUAV_DRONERFA_DIR` |
| 产物清单存绝对路径 | 4 | 迁移后清单不再可核对，也不可复跑 | 新增 `repo_path()`，仓库内路径一律存相对形式 |
| 注释引用外部工程的绝对路径 | 3 | 换台机器读者跟不过去 | 只写工程名与文件名，实际位置由 CLAUDE.md 资源清单单点记录 |

还顺手修了一处清单不该有的东西：`extract.command` 记的是 `/opt/homebrew/bin/pmtiles`，
换平台同样失效，改成只记工具名 `pmtiles`，工具版本另有 `tool_version` 字段。

#### 3. 例外只有一个，并且写进了检查器

数据清单里记录**外部历史来源**的 `origin` 与 `dev_link_target` 保留绝对路径。它们回答的是
"这份副本当初从哪里拷来"，是留档而不是可解析的位置；副本与来源逐字节相同这件事由 sha256
保证，换机器后哈希仍然有效。检查器对这两个字段名显式放行，清单里也写明"任何代码都不得解析它"。

#### 4. 顺带修掉的一个生成器缺陷

`scripts/gen_dronerfa_download_list.py` 改完路径后复跑，产出的 `download-list.md` 把一段
**手工更新过的说明覆盖回了脚本里内嵌的旧文字**。根因是生成器内嵌了叙事性段落，而那段话在
提交 `1867d67` 里被人工改过，脚本没跟着改。

改法不是把新文字再抄进脚本，而是**让生成器只陈述它能核实的事实**：那段现在按目录里实际的
样点文件数与清单数生成（当前 71 个与 25 份），叙事性的过程记录一律留在 WORKLOG。复跑两次
验证幂等。这条教训与本项目此前几次同源：**凡是能过期的结论都不能预先写死在生成器里。**

#### 5. 关于目录名里的空格

结论是**保留，并当成探针**。理由已写进 CLAUDE.md 的「继承的坑」：远端仓库名不含空格，所以
空格不会传播到流水线与交付物；而 Windows 上的真实路径常含空格与中文，本机带空格能让"路径
没加引号、`file://` 没做编码"这类问题在开发阶段就暴露。2026-09-04 服务静默不启动正是这样被
抓到的。代价是两条纪律：脚本里路径一律加引号；拼 `file://` 一律用 `pathToFileURL`。

#### 6. 验证

| 项目 | 结果 |
|---|---|
| `scripts/check-paths.sh` | 通过，已接入 `scripts/build-all.sh` |
| 三份场景清单重新生成 | `source.path` 现为 `data/basemap/planet.pmtiles` |
| 建筑产物 | 重新解码后仍为 47582 栋，统计量不变 |
| 生成器幂等 | 连跑两次，产物一致 |
| ASCII 检查 | 通过 |

#### 7. 本条目产生的待办

- [ ] 新写任何脚本前先想一遍路径来源；提交前 `scripts/check-paths.sh` 会拦。
- [ ] 其它目录（`tools/`、`algos/`、`engine/`）由并行会话维护，本次未逐个复核其内部约定，
      但检查器覆盖全仓库，有问题会在下次全量构建时暴露。

---

### 2026-09-04 · 路线图修订 v1.1：把界面、场景生成、框图、信号可视化与 MATLAB 提到当前工作面

#### 1. 起因

用户定后续工作重点：前端人机交互界面的设计、真实地理环境下的反无电子侦察场景与目标活动的生成、
面向信号级仿真的 B/S 框图模型设计、信号波形与频谱的可视化、后端模型计算，并问计划是否需要修订。
中途追问「MATLAB 的优势发挥出来了吗」。

#### 2. 核实到的事实

- 06 备忘录的步骤表只细化到 P1；P2（画布、组件库、参数表单、WS 补取、SQLite、上传、Windows 包）
  只有一段话没有编号。按 06 的「下一步」本应做 D2 态势图层。
- 代码现状（实测盘点）：`web/src` 只有场景视图，`App.tsx` 硬返回 `<SceneView/>`，无路由、无状态
  管理、无 WebSocket 客户端、无框图画布、无任何波形或频谱绘制、无态势图层，`api/ diagram/ signal/`
  三个目录是 `export {}` 占位。`server/src` 零运行时依赖，只有 Range 静态服务与三个只读端点，无
  WebSocket 服务端。`engine/` 只有静态库与测试可执行，没有任何可执行入口，与服务零接口；
  `IComponent` 没有参数描述与按名构造的注册表，`configure()` 只收两个 `map`。`geo/` 纯占位，
  `models/` 六个子目录全空。
- 参考实现 em-demo 的场景是写死的常量（`simulation/scenario.ts`），`simulation/engine.ts` 在浏览器里
  按 20 Hz 沿航点插值推进，`EquipmentPanel` 只能原地改设备参数，没有在地图上布站或画航线的编辑功能。
- 04 对场景生成几乎没有条款，只在 §3.1 提到「配置无人机工作状态、位置、航迹和传播条件」；04 §8.1
  引用的主界面交互原型 html 不在盘上（全盘搜索无结果）。
- MATLAB：参考实现与黄金向量全部是 Python numpy；P1-8「MATLAB Coder 链路」在 06 里有条目但没排进
  任何步骤；`matlab/` 目录空；缺的 DDC、信道化、接收滤波原计划手写 C++。开发机有
  `/Applications/MATLAB_R2025a.app`，工具箱 coder、dsp、comm、signal、phased、rf、fixedpoint、compiler
  齐全，`matlab` 不在 PATH。03 §11.3.1 与 04 §8.5 都把「Coder 生成 C/C++」定为最终产品主路线，
  03 §11.8 明确不建议 MATLAB Engine 作最终运行方式。

#### 3. 判断：计划需要修订，六处

| 用户重点 | 原计划的处理 | 差距 |
|---|---|---|
| 人机交互界面 | 无步骤；P1-6 是「任务 JSON 提交表单」 | 界面被压到 P2，且 P2 无步骤表 |
| 场景与目标活动生成 | 04 与 06 都无归属：D2 只显示状态流，D3-5 消费航迹但没人生产 | 整条线缺失 |
| B/S 框图模型 | 画布留到 P2 报告 09；框图 JSON 无规范；引擎无注册表 | 既无数据模型也无 UI |
| 波形与频谱可视化 | 只提 PSD 与瀑布 | 缺时域包络、观测点、视窗抽取的产品格式 |
| 后端模型计算 | 引擎与服务零接口；P1-3 缺 5 个组件 | 浏览器提交到结果没有桥 |
| MATLAB 优势 | 未发挥 | 新增 M 线，DSP 组件改走 Coder 主路线 |

前端与运行时不出现 MATLAB 是 04 §8.5 与 03 §11.8 有意为之；MATLAB 的优势在模型生产与验证侧，
而那一侧此前没有开工。

#### 4. 用户拍板与新决策

四项拍板均取推荐项：框图画布 React Flow；引擎桥接子进程 `cuav_run`（不做 N-API，不做引擎内嵌
HTTP）；场景运动学与链路预算放 `geo/` C++ 库加引擎组件 `ScenarioSource`；切片顺序链路 → 场景 →
画布 → 合流。写入 CLAUDE.md 决策日志 D-030 至 D-036。顺手修掉决策日志里两条 D-027 重号，后者改
D-027b。

#### 5. 修订内容

- 06 备忘录升 v1.1：§0.2 路线图加 B 桥接、U 界面、G 场景、M MATLAB 四条线与五个纵向切片；新增
  §9A 至 §9E 步骤表；P1-5、P1-6 撤销由 B / U 替代；P1-8 由 M-2 兑现；P1-3 的 DDC、信道化、接收滤波
  改走 Coder；D2 并入 G-5；D3-5 移交 G-2；D3-3 的自由空间路损锚点前移到 G-1；§10 P2 缩为剩余项；
  §14 记修正。P1 退出条件只把「从浏览器提交」改为「从框图画布提交」，技术路线与阶段 0 退出条件不变。
- CLAUDE.md：文档体系表加 07/08/09 报告与四份新规范；已定表加引擎运行形态、框图画布、WS 库、
  场景运行时、DSP 组件生产路线；OPEN 表删框图画布、加 MATLAB Runtime 与 Coder 许可；里程碑表加
  B / U / G / M 四行并改 P1、P2、D2、D3；环境与命令补 `cuav_run` 与 MATLAB；对上游文档修正加两条。
- 四份规范骨架落地，字段已冻结：`docs/diagram-format.md`（节点、端口、连线、参数、观测点、场景
  绑定、运行参数）、`docs/scenario-format.md`（站点、辐射源、航线、活动时间线、到参数帧的契约）、
  `docs/display-products.md`（产品目录、定长行二进制与索引、抽取端点、WS 二进制帧）、
  `docs/component-catalog.md`（六类、端口兼容矩阵、参数描述、实现形态），以及 `docs/schemas/` 下
  两份 JSON Schema。`docs/api-versions.md` 3.3 端点表与第 4 节 WS 事件信封冻结。
- `matlab/README.md`、`models/README.md`、`tests/regression/README.md` 补实现形态与算例输入约定。
- 本次只动计划文件与规范骨架，不动业务代码。修订后实测 `scripts/build-all.sh` 全绿：web、server、engine 单测
  （`cuav_engine_unit` 通过）、geo 冒烟、路径检查、ASCII 检查全部通过；D1 条目记录的那项引擎组件测试失败已不复现。
- 顺手修一处检查器缺陷：`scripts/check-paths.sh` 提交后会扫到自己（注释与判据里有 `/Users/` 字样，
  而 `*.sh` 在扫描范围内），此前只在它未入库时跑过所以没暴露；改为 `git grep` 的 pathspec 排除自身。

#### 6. 风险与坑（先记，切片实施时逐条销）

- Windows：`spawn` 路径含空格与中文（数组参数不走 shell）；子进程 stdout 按 UTF-8 字节写与解析；
  服务崩溃留孤儿进程（任务目录写 pid，启动时对账）；读正在追加的产品文件（行定长、索引最后写、
  读端只读已索引行）。`cuav_run` 与 `cuav_geo` 新代码的 MSVC 首次构建提前到切片 ② 末尾。
- 离线：React Flow 与 `ws` 锁版本进 `package-lock` 与 THIRD-PARTY-NOTICES；e2e 保留「页面无外网请求」断言。
- 性能：15.9 MB 建筑常驻加 20 Hz 实体事件加瀑布 Canvas。三视图共享单个地图实例；信号页不挂载时
  瀑布不绘；实体更新合并到 rAF；频谱行走 WS 二进制（约 80 KB/s）；瀑布用离屏 Canvas 位移追加。
  单线程引擎跑 5 MS/s 加 FFT 不承诺实时，界面同时显示逻辑时间与墙钟。
- 复现性：参数帧必须按样点序号推进，按墙钟则同种子不可复现。
- 基准：组件目录黄金基准只比「已有条目不变」，改动条目即记决策。
- 合规：合成场景徽标常驻并进截图（铁律 14）；回放源 `scene_bindable=false`，画布与 `--validate`
  双重拒绝（防线二、三）。
- 需求：09 报告顶替缺失的原型 html，标 PROVISIONAL，原件到手后对账。
- 架构：`SceneParamFrame` 每链路一个输出口，多辐射源时端口数随场景变，目录用 `dynamic_ports` 描述。

#### 7. 本条目产生的待办

- [ ] 切片 ① 从 B-1 组件注册表与参数描述开始；并行 M-1 `matlab/` 骨架与 `pwelch` 黄金向量。
- [x] 2026-09-04 用户确认：Coder 生成的 C 代码随交付不受单位许可条款限制，M-0 销项。DS-1 / DA-1 许可仍未确认。
- [ ] 08 报告在切片 ① 后、切片 ② 前定稿；09 报告在切片 ③ 前。
- [ ] `data/scene/beijing-yayuncun/quality-report.md` 里的「D-027」指建筑高度分档，即改号后的 D-027b；
      该文件由生成器产出，下次重跑生成器时同步改引用，本次不手改生成物。

---

### 2026-09-04 · 09 报告前半、M-0 销项、B-1 组件注册表

#### 1. 起因

用户批准路线图修订后指示：先出 09 报告的前半部分，再开 B-1；并确认 Coder 生成的 C 代码随交付不受
单位许可条款限制。

#### 2. 09 报告前半

`09.主界面三视图与框图画布交互设计_v1.0.md`，382 行，§0 至 §14。定了：一个窗口、一条顶栏、三个视图、
一个底部抽屉；04 §8.1 的「左组件库 / 中画布 / 右参数」是框图视图内部布局而不是全应用布局；顶部试验
上下文的八个字段与联动规则；三视图只隐藏不卸载、地图实例不重建；场景视图、框图视图、结果视图各自的
三栏线框；底部抽屉四页签；store 形状与数据流；探针 `app` 子对象与切片 ① 的六条 e2e 断言；键鼠与
视口基线；视觉与文案约定；四态与丢帧在界面上的表现；与 04 §8.1、03 §11.9 的逐条对照；八项 PROVISIONAL
待确认。§15 列了后半（画布与信号视图交互）要写的内容，切片 ③ 前补。

#### 3. M-0 销项

工具链上一条目已核实；许可由用户确认不受限。CLAUDE.md OPEN 表删该项，06 §0.5 删输入行，§9D M-0 记
`[x]`，附录 A 改为「已确认」。

#### 4. B-1 组件注册表与参数描述（切片 ① 第一步）

做了什么：

- `engine/include/cuav/param_spec.h`：`ParamSpec`（名、四种类型、单位、必填、上下限含开闭、默认值、枚举、
  说明、跨参数约束文字）与链式构造。
- `engine/include/cuav/component.h`：`ComponentInfo`（类型、六类类别、显示名、说明、M/E、`model_id`、版本、
  端口、参数、`scene_bindable`、`stateful`、`implementation`、`source_ref`、动态端口）；`IComponent::describe()`
  带默认实现，六个组件各自重写。
- `engine/include/cuav/registry.h`、`src/registry.cpp`：`Registry` 按名构造，重复注册拒绝；`validate_params()`
  按描述挡未知参数、类型错位、越界、枚举取值、缺必填，报错一定含参数名；`create_configured()` 三步合一；
  `builtin_registry()` 注册六个内置组件。
- `engine/include/cuav/catalog.h`、`src/catalog.cpp`：`catalog_json()` 按 `docs/component-catalog.md` 输出
  `cuav-catalog/1`，`port_compat` 由 `can_connect()` 六乘六全枚举导出并附拒绝理由；`validate_catalog_entry()`
  做目录自检。
- `engine/tests/test_registry.cpp`：7 个用例。

实测：

| 项 | 结果 |
|---|---|
| 引擎单测 | 30 个用例、523 项断言全绿（原 23 个不变） |
| 目录导出 | 六个组件按类型名排序；兼容矩阵 36 行、6 行为真、30 行带 D-013 拒绝理由；两次导出逐字节相同 |
| 一致性 | 五个组件只给必填项即可构造；去掉任一必填，描述与 `configure()` 两道闸都拒绝且报错含参数名；所有默认值在自己的范围内 |
| 越界样例 | `amplitude = -1`、`pfa = 1.0`、`sample_rate_Hz = 0`、未知键 `amplitud`、文本参数按数值给，全部被拒且报错含参数名 |
| 经注册表构造的链 | 单音加噪声到能量检测跑通，后半段强单音的检出率大于 0.3 |

两点设计取舍：

- 注册表是显式对象不是全局单例，`builtin_registry()` 每次返回一份新的；用户插件将来往自己的 `Registry` 里
  `add()`，与 04 §15.2 第 12 项「用户算法插件」的接口方向一致。
- `describe()` 与 `configure()` 是同一套规则的两道闸而不是合并成一道：前者按表挡住可静态判断的错误，后者做
  跨参数约束（奈奎斯特、频段上下限、2 的幂）。合并会让组件的校验逻辑迁到描述表里，描述表就不再是纯数据。

#### 5. 本条目产生的待办

- [ ] P1-4a `SpectrumAnalyzer`（Welch → `SpectrumFrame`，用 `dsp::fft_inplace`），以 M-4 的 `pwelch` 黄金向量验收。
- [ ] B-3 观察者与 `ObservationTap`；随后 B-2 框图 JSON 装载器（`docs/diagram-format.md`）。
- [ ] M-1 `matlab/` 骨架与 `pwelch` 黄金向量导出，与上两项并行。
- [ ] B-4 生成首版目录黄金基准 `tests/golden/component-catalog.json`。
- [ ] 09 报告后半（画布与信号视图交互）在切片 ③ 前补；前半的八项 PROVISIONAL 待用户确认。

---

### 2026-09-04 · 09 报告评审与 v1.1；回放源改用 data_id

#### 1. 起因

用户要求以电磁仿真人机交互专家的角度评审 09 报告前半。评审给出十二条「必须改」与十二条「建议改」，
用户指示「必须改，改到位」。

#### 2. 评审结论

v1.0 立住了应用壳（三视图不卸载、上下文单例、目录驱动组件库、四态不静默、探针可断言），但还不是电磁
仿真的人机交互设计：场景视图没有「链路」对象，只是带航点的地图编辑器；信号视图没有频谱仪的仪表约定；
没有用户与任务流。另有两处硬伤会直接返工：`Ctrl+1/2/3` 是 Chromium 保留的标签页切换键，页面拦不住；
回放源以 `manifest_path` 作用户参数，服务器路径会进框图 JSON 与参数面板，违反 04 §8.6，且数据中心「只列
索引」没有把数据接进框图的途径，回放流程是断的。

#### 3. v1.1 的改动（09 §17 有逐条记录）

| 条 | 改法 | 落点 |
|---|---|---|
| ① 保留键 | `Alt+1/2/3`；列出不绑定的浏览器保留键；`Ctrl+S` 须 `preventDefault` | §4.2、§11 |
| ② 路径 | 回放源只写 `data_id`，服务端装载时解析成内部参数；数据中心加「用于回放」 | §6、§7.3；D-037 |
| ③ 顶栏 | 面包屑「项目 › 试验 › 任务」加运行组；场景、框图、种子、模式收进试验详情；1366 折叠顺序 | §2、§3 |
| ④ 徽标 | 运行态（图标）与结果四态（形状加文字加颜色）两个正交徽标，定组合规则 | §13.1 |
| ⑤ 链路 | 对象树加派生的「链路」组；地图画链路线按视距着色；右栏读数距离、方位、俯仰、路损、时延、多普勒、帧有效期、四态，随游标变化 | §5.4 |
| ⑥ 地图要素 | AOI 边界常显不可关、比例尺、指北针、鼠标经纬度 | §5.2 |
| ⑦ 仪表约定 | 纵轴 `dBFS（未标定）`、参考电平与动态范围、射频绝对频率轴、RBW、迹线模式、marker、瀑布方向、色带图例、导出 | §7.2 |
| ⑧ 双游标 | 频率 marker 与时间游标在频谱、瀑布、包络上联动；框选缩放 | §7.2 |
| ⑨ 检测叠加 | 命中帧画成时频框叠在瀑布上 | §7.1、§7.2 |
| ⑩ 撤销重做 | 场景与框图各一个 50 步撤销栈，`Ctrl+Z / Ctrl+Y` | §4.3 |
| ⑪ 溯源 | 任务、节点、观测点、链路帧共用的溯源面板，六件套加 M/E/V，可导出 JSON | §9 |
| ⑫ 用户与流程 | 三类用户；全合成、实测回放、混合增强三条主流程的视图路径与反馈；四种空状态 | §1 |

随手并入的建议：工程计数法显示与输入、状态条基准 `WGS-84 · AGL · LogicalSim`、图层开关去重、通知策略、
示例框图下拉、保存带版本说明、模式中文标签、实现细节移附录 A。未并入的建议留后半：对比模式细节。

#### 4. 引擎与规范的连带改动

- `ParamSpec` 加 `internal` 标志与 `internal_only()`；目录 JSON 输出 `"internal": true`。
- `FileReplaySource`：用户参数 `data_id`（必填），`manifest_path` 转内部参数；`configure()` 两者都收，缺
  `manifest_path` 时报「应由装载器按 data_id 解析注入」；`data_id` 进块溯源的 `trace_id`。直接构造的测试与
  工具不受影响。引擎 30 项单测、535 项断言全绿。
- `docs/diagram-format.md` 加「参数值不得是服务器路径」规则与回放节点写法；`docs/component-catalog.md`
  加 `internal` 字段；`docs/api-versions.md` 任务提交端点写明解析与 400 规则、数据中心两个端点、WS 加
  `link` 事件并把 `task.state` 拆成运行态与结果态、日志路径替换；`docs/display-products.md` 加
  `links.jsonl` 与 `/links` 端点。06 §9A B-2、B-3、B-5 与 §9B U-1、U-3、§9C G-2 对应修订。

#### 5. 本条目产生的待办

- [ ] 09 §15 的十三项 PROVISIONAL 待用户确认，尤其瀑布方向、`dBFS（未标定）` 一刀切、验收集片段允许回放。
- [x] B-2 装载器实现 `IDataResolver` 与 `internal` 参数拒绝（2026-09-05，见同日「B-2」条目）；B-5 服务端解析 `data_id` 待做。
- [ ] G-2 观察者加 `on_link`；`links.jsonl` 产品与 `/links` 端点进 B-3、B-7。
- [ ] 切片 ① 继续：P1-4a `SpectrumAnalyzer`、B-3 `ObservationTap`、M-1。

---

### 2026-09-05 · 切片 ①：频谱分析组件、观测点产品、MATLAB 黄金向量三方互证

#### 1. 起因

用户指示「继续下一步」。按 06 §9E 切片 ① 与上一条目的待办：P1-4a `SpectrumAnalyzer`、B-3 观察者与
`ObservationTap`、并行 M-1 `matlab/` 骨架与 M-4 的 `pwelch` 黄金向量。

#### 2. P1-4a 频谱分析组件

- 口径：Welch 平均的**功率谱**（不是功率谱密度）。每段加周期窗，DFT 后取 |X|²，各段平均，除以 (Σw)²，
  零频居中；复单音峰值等于其功率 A²，满量程单音读 0 dBFS；未标定一律 dBFS（D-020）。四种周期窗
  hann / hamming / blackman / rect，重叠比例与每帧段数可配。
- 计算在 double 里做：`dsp::fft_inplace` 加了 double 版本，旋转因子逐点直接求值不递推；float 版本原样保留，
  能量检测的黄金基准依赖它（铁律 10）。`SpectrumFrame.psd_dB` 改为 double，`PortData` 改为一次交出多帧
  `spectra`，因为调度器只在有新输入时调 `process()`，组件必须把当前块能切出的帧一次全交出。
- 块长无关：同一输入按 1 块与按 97 样点小块喂，四帧逐位相同。收尾时不满一帧的段平均后交出并标降级，
  丢弃的尾样点只算「从未被任何一段覆盖」的（有重叠时 head 之后的 nfft − hop 个样点已进过上一段）。

#### 3. 三方互证（M-4 首项）

输入是闭式确定性信号（三个单音、一个直流、48 个按黄金分割摆放的低幅单音），量化成 float32 后以精确十进制
写进 `engine/tests/golden/spectrum_welch.json`，公式只作说明；这样避免在 MATLAB 里复刻 xoshiro256++。

| 一方 | 实现 | 对拍结果 |
|---|---|---|
| Python | `algos/reference/gen_spectrum_golden.py`，numpy.fft 手写 Welch | 基准 |
| MATLAB | `matlab/golden/gen_spectrum_golden.m`：`pwelch(x, hann(nfft,'periodic'), nfft−hop, nfft, fs, 'twosided', 'power')` 再 `fftshift`；并与 `matlab/ref/cuav_welch_power.m` 手写参考互证 | 与 Python 逐频点差在 1e-12 × 峰值以内（相对误差为 0）；pwelch 与手写参考最大相对误差 3.7e-16；R2025a Update 1 |
| C++ 引擎 | `SpectrumAnalyzer` | 与 Python、MATLAB 均 rel ≤ 1e-9；峰值 bin 160 三方一致，−5.4177 dBFS |

**踩到的坑**：MATLAB `pwelch` 的 `'centered'` 对偶数 nfft 给 `(−fs/2, fs/2]`，奈奎斯特频点放正端，与 `fftshift`
（numpy 与 MATLAB 自身）的 `[−fs/2, fs/2)` 差一格。首次对拍相对误差 7.0e4 就是这一格。改为 `'twosided'` 再
`fftshift`，已写进 CLAUDE.md「继承的坑」与 `matlab/README.md`。

另一个事实：JSON 交换时 MATLAB 一方不能改写 Python 写的文件，`jsonencode` 只保留 15 位有效数字，会破坏
float32 精确值的十进制表示；MATLAB 结果写到 `spectrum_welch.matlab.json`。

#### 4. B-3 运行观察者与观测点产品

- `observer.h`：`IRunObserver{on_progress, on_entity, on_link, on_log, on_product_row}` 与 `ProgressInfo`、
  `EntityState`、`LinkFrame`；`Graph::run(rng, observer)` 重载，init 前对每个节点 `attach()`，每轮回调进度。
- `platform.{h,cpp}`：目录逐级创建、文件原子替换、字节序探测、路径拼接；`#ifdef _WIN32` 只在这一处（铁律 16）。
- `components/tap.h`：`ObservationTap` 接任一 IQ 输出口，写 `spectrum.f32`（每行 nfft 个 float32 dBFS）与
  `envelope.f32`（每行 min、max、rms）及索引 JSON（`docs/display-products.md` §2 的字段，含 `notes`、
  `producer`、被观测信号的 `trace`）；索引每 64 行刷一次、收尾原子替换；每行同时回调 `on_product_row`。
  与 `SpectrumAnalyzer` 共用 `WelchAccumulator`，写出的谱行与其输出转 float32 逐位相同。
- 设计取舍：末行段数不足、末桶样点不足、丢弃尾样点是流结束的自然结果，**记进索引 `notes`，不降级**；
  产品级 `state` 只取被观测信号的四态。`iq/` 产品本步未做，组件暂不声明该参数（目录只增不改）。
- 实测：单音加噪声 10340 样点 → 40 行谱、21 行包络（末桶 340/500 样点）；同种子两次运行两种产品文件
  逐字节相同，换种子即不同；观察者收到 40 + 21 行回调，谱行逻辑时间单调。

#### 5. M-1 `matlab/` 骨架

`ref/`、`golden/`、`coder/`（空，待 M-2）、`run_all.m`（路径从文件位置推导）、`run_matlab.sh`（经环境变量
`MATLAB_ROOT`，铁律 17）。开发机实测 `matlab -batch` 一次约一分钟。

#### 6. 状态

引擎 37 个用例、12157 项断言全绿（含三方对拍）。路径与 ASCII 检查通过。八个组件进注册表。

#### 7. 本条目产生的待办

- [x] B-2 框图 JSON 装载器：按 `docs/diagram-format.md` 装 `Graph`，`observation_points[]` 并联成 `ObservationTap`，
      `IDataResolver` 解析 `data_id`，`internal` 参数出现即拒（2026-09-05 完成，见同日「B-2」条目）。
- [x] B-4 `cuav_run`：`--catalog`（生成首版目录黄金基准）、`--validate`、`--run`；stdout JSON 事件带 `seq`（2026-09-05 完成，见同日「B-4」条目）。
- [ ] M-4 余项：能量检测与 ADC 量化的 MATLAB 参考与黄金向量。
- [ ] 观测点 `iq/` 产品（按 `docs/iq-format.md` 写 `.iq` 与旁挂清单）。
- [ ] 产品行的 `t_s` 目前只对逻辑时间基准成立；回放源接入后要按块元数据的时间基准写索引。

---

### 2026-09-05 · 公开数据集许可确认（DS-1 / DA-1 销项）

用户确认：DroneRFb-DIR 与 DroneRFa 两个公开数据集**没有任何使用约束与署名约束**，可随交付系统分发。
06 §0.5 许可行、§11.4 DS-1、§11.5 DA-1 均记为完成。实施方此前未能自动取到 SciDB 页面的条款原文
（动态渲染），本条以用户确认为准。数据中心页仍按惯例显示数据集来源与编号，这是溯源要求（铁律 8），
不是许可要求。D-028 的口径不变：公开数据集只用于验证技术途径，量化结论不作交付指标。

---

### 2026-09-05 · 09 报告十三项暂定拍板（v1.2）

#### 1. 拍板

用户对 09 §15 的十三项暂定逐项表态：五项改定，八项采纳原案。

| # | 改定 | 报告落点 |
|---|---|---|
| 3 | 数据中心是独立页面，顶栏直达（`#/data`，`Alt+4`），不并入结果视图 | §3.3、§4.1、§7.3 |
| 9 | 视口基线按 2K 考虑：取 2560 × 1440 为基线，最小 1920 × 1080，折叠规则从 1920 起 | §2、§11 |
| 10 | 瀑布最新行在顶部、向下滚；频谱图紧贴瀑布上方，共用频率轴且列像素对齐 | §7.1、§7.2、§10 |
| 11 | 纵轴以 dBm 为目标：先按可得依据估一个标定常数并带来源徽标（实测 / 论文 / 假定 / 模型），没有任何常数才退到 `dBFS（未标定）`，绝不显示不带来源的 dBm | §7.2 标定常数行、§7.3、§13.2 |
| 13 | 验收集片段允许回放，界面提示（原案） | §7.3 |

「2K」按国内常用含义取 QHD 2560 × 1440；若指 DCI 2K 2048 × 1080 需另说。

#### 2. 连带改动

- `docs/display-products.md`：产品索引 `scale ∈ {dBm, dBFS}`，新增 `calibration{offset_dB, source, note}`；行值在有常数时已含偏移。
- `docs/api-versions.md`：数据中心端点返回 `calibration`。
- 06：U-3 验收改为「纵轴以 dBm 开头并带来源，合成链为『标定：模型』」；U-4 改为独立页；§11.2 加「入库当天估标定常数」规则，两个公开数据集的估算列为 DS-8 / DA-8 待办。
- CLAUDE.md：D-038。D-020 的「未标定」判据不变，显示层多了「带来源的估算常数」这一档。

#### 3. 同日销项

用户确认 DroneRFb-DIR 与 DroneRFa 没有任何使用与署名约束，DS-1 / DA-1 完成（见上一条目）。

#### 4. 本条目产生的待办

- [ ] DS-8 / DA-8：按论文参数（DroneRFa 接收增益 50 dB、天线 3 dBi；DroneRFb-DIR 论文所给参数待查）与满量程假设，为两批数据估算 `calibration_offset_dB`，来源标 `paper`，写进 `index.manifest.json` 并在 `docs/iq-format.md` §5 补字段定义。
- [ ] 引擎侧：`SpectrumAnalyzer` 与 `ObservationTap` 增加 `calibration_offset_dB` 与来源参数，产品索引写 `scale = dBm` 与 `calibration`；合成链定内部功率约定并标 `model`。排在 U-3 之前。
- [ ] 09 后半（切片 ③ 前）按 v1.2 的布局写画布与信号视图交互。

---

### 2026-09-05 · 系统定位再确认（D-039）

用户再次强调：本系统是技术验证的原型系统，用于技术路线的验证和功能演示，目的是证明本方在反无人机电子
侦察方面的行业理解和技术实力。这是 D-028「原型阶段」的延续，新增的是**目的**：不只是把链路跑通，还要
让每个环节能讲出背后的模型与依据。写进 CLAUDE.md 开头与决策 D-039、06 §0.1。

对后续工作的直接含义：切片 ①–④ 的可见链路仍是第一优先；09 报告里的链路读数、频谱仪约定、溯源面板从
「锦上添花」升为「行业理解的证据」，不得为省事砍掉；P2 的权限审计、分片上传、并发只做演示所需的最小集；
合成徽标、估算高度警示、「原型阶段验证值」这些标注保留并在演示时主动讲出来。

---

### 2026-09-05 · D-039 修正：可解释性对内、界面不展示溯源、标注低调

用户补充：「每个环节讲得出模型与依据」对内很必要，可在设计文档里强调，但系统里不给用户展示，这样更像交付
系统而不是实验系统；真实数据与合成标注同理，演示时不主动讲。

落法：铁律 8 的溯源数据仍随每个产物与事件走，不变；界面只在开发者模式（地址加 `?dev=1`，顶栏出「DEV」小标）
才有「溯源」按钮，默认 DOM 里没有任何溯源入口，e2e 加断言。合成场景标注由地图右上角的醒目徽标改为底图署名旁
的小字，估算高度比例改为默认折叠的数据包属性，铁律 14、15 的「显式标注、不静默」仍满足，只是呈现强度降低。
09 报告升 v1.3，CLAUDE.md 开头与 D-039 ②③ 相应改写（上一提交 `879da99` 只含 CLAUDE.md，其说明所述的
09 与 06 改动由本提交补上），06 §0.1 与 G-5 行同步。

---

### 2026-09-05 · B-2 框图 JSON 装载器

#### 1. 本次做了什么

新建 `engine/include/cuav/diagram_json.h`（130 行）与 `engine/src/diagram_json.cpp`（735 行），把 `docs/diagram-format.md`
定义的框图文件（`cuav-diagram/1`）装成可运行的 `Graph`：

- **结构校验**手写等价于 `docs/schemas/diagram.schema.json`：未知键在顶层、节点、连线、观测点、`run`、`scenario_ref`、`trace`
  七处一律拒绝；id 与端口名按正则；参数值只许 number / string / bool；`run` 的种子、时长、时间基准按规范取值。
- **节点**按注册表 `describe()` 分流参数（bool → 数值表 0/1，enum/string → 文本表），内部参数出现即拒（D-037）；
  `total_samples` 按 `run.duration_s × sample_rate_Hz` 四舍五入补齐、显式值超时长即拒；`run.block_size` 给未写块长的节点；
  `data_id` 经 `IDataResolver` 换成 `manifest_path` 注入；然后 `Registry::create_configured()` 构造。
- **连线**交给 `Graph::connect`；`graph.h` 新增 `LinkFault` / `GraphFault` 失败分类与两个重载，装载器据此把失败映射成带
  `{code, node_id, port, message}` 的 `DiagramError`，连线规则仍只在 Graph 一处解释，装载器不猜报文。
- **观测点**在 `IQStream` 输出口后并联 `ObservationTap`（图内名字 `op:<id>`），不改用户的边；`products` 映射到
  `spectrum` / `envelope` 开关，`iq` 明确拒绝。
- **两种解析器**：`MapDataResolver`（内存表或解析旁挂 `cuav-resolved/1`）、`IndexDataResolver`（读 `index.manifest.json`，
  按 `<索引目录>/<data_id>.manifest.json` 定位并核对存在，多份索引里同一 `data_id` 位置不同即拒）。
- `ObservationTap` 缺 `out_dir` 的检查从 `configure()` 挪到 `init()`：装载器不给 `out_dir` 即「只校验」模式，观测点照常构造
  并校验参数，运行到 `init` 才拒且报文含 `out_dir`。`test_tap.cpp` 相应改一处断言。
- 夹具 `engine/tests/diagrams/slice1_tone_noise_psd.json`：`docs/diagram-format.md` §8 示例的逐字副本。测试
  `engine/tests/test_diagram_json.cpp`（620 行，13 个用例）。

#### 2. 核实到的事实与踩到的坑

- 示例框图的参数名与端口名（`ToneSource.offset_Hz`、`AddMixer.a/b`、`SpectrumAnalyzer.in`、`window` 枚举）与现有目录一致，
  装载运行通过；`docs/diagram-format.md` §10 第一条待办的引擎侧一半销项，目录黄金基准仍待 B-4。
- **踩坑一（悬空指针）**：`IComponent::outputs()` 按值返回，`find_port(node->outputs(), port)` 拿到的是临时对象里的指针，
  表达式结束即失效。后果是观测点挂在 `SpectrumFrame` 口上没被 `observation_port` 拦住，读到垃圾类型后一路走到并联连线才被
  Graph 拒成 `graph` 兜底码。首轮测试抓到，改为先落局部变量再取指针。C++14 对「按值返回容器再取内部指针」没有语言级保护，
  后续评审要专门盯这一类写法。
- **踩坑二（检查顺序）**：`Graph::connect` 先查自环再查端口与占用，所以 `mix.out → mix.a` 报 `cycle` 而非 `input_occupied`。
  顺序本身合理（自环是更根本的错误），测试按引擎实际顺序改写，`input_occupied` 用 `noise.out → mix.a` 触发。
- 首批组件里没有 `SceneParamFrame` 输入口的组件，D-013 拒连测试用测试专用 `FakeApplier` 注册进临时 `Registry`；场景绑定
  分支用 `FakeBindable`。这也证明装载器只读注册表、不硬编码组件清单（D-030）。
- 数据索引记录里没有路径字段，`data_id → 旁挂清单` 的规则只由索引顶层 `directory` 与 `note` 的命名约定隐式给出；
  `IndexDataResolver` 把它写成代码并核对文件存在。
- 工作树在开工时是干净的（`git status` 0 项），快照里「约 50 个文件未提交」是过时描述，已删。

#### 3. 拍板（D-040，用户批准 B-2 计划即拍板）

| # | 口子 | 处置 |
|---|---|---|
| a | `data_id` 解析结果怎么到引擎 | 解析旁挂 `diagram.resolved.json`（`cuav-resolved/1`），框图副本永远不含内部参数；引擎对内部参数无条件拒绝 |
| b | `--validate` 时观测点缺 `out_dir` | `out_dir` 检查挪到 `init()`；装载器不给 `out_dir` 即只校验模式，不落盘 |
| c | `products` 含 `iq` | 本版本拒绝（`product_unsupported`），不静默忽略 |
| d | 错误码 | 首版 17 个，表进 `docs/diagram-format.md` §4；`Graph` 增加失败分类供映射 |

连带改动：`docs/diagram-format.md`（状态行、§3 补齐规则、§4 错误码表、§5 `iq` 与禁写参数、新 §9 解析旁挂、§10 待办）；
`docs/api-versions.md` B-5 行改为「写解析旁挂」；06 §0.3、§9A B-2 `[x]`、B-4 加 `--resolved / --data-index`、B-5 措辞、§14 记修正；
`engine/README.md` 现状表；CLAUDE.md 里程碑 B 行与 D-040。

#### 4. 量化结果（原型阶段验证值）

- 引擎单测 37 → 50 个用例，12157 → 12564 项断言，全绿；`scripts/build-all.sh`、`check-paths.sh`、`check-ascii.sh` 全过。
- 新增 13 个用例覆盖 17 个错误码中的 16 个（`graph` 是兜底码，正常路径不可达）。
- 示例框图：`total_samples` 补齐 2000000；谱 1953 行（1024 点一段，尾巴 448 样点记备注）、包络 489 行（4096 样点一桶）；
  只校验模式运行到 `init` 被拒，报文含 `out_dir` 与 `op:s4`。
- 回放夹具 5000 样点经 `MapDataResolver` 装载运行，产品索引 `trace_id = FileReplaySource:fx_replay_1`，谱 4 行。

#### 5. 本条目产生的待办

- [x] B-4 `cuav_run`：`--catalog`、`--validate <框图>`、`--run <框图> --out <dir> [--seed N] [--resolved <旁挂> | --data-index <索引>...]`；
      错误事件直接 `to_json(DiagramError)`；`--seed` 覆盖 `run.seed` 须写进事件；`--catalog` 生成 `tests/golden/component-catalog.json`。（2026-09-05 完成，见同日「B-4」条目）
- [ ] 回放节点的时长截断：`FileReplaySource` 的采样率读清单后才知道，`run.duration_s` 对它暂不生效（整片回放或显式 `max_samples`），B-4 或 P1-3 余项补。
- [ ] B-5：服务端写解析旁挂而不是含内部参数的框图副本（`docs/api-versions.md` 已改）；`docs/schemas/resolved.schema.json` 用到时一并写。
- [ ] 代码评审关注项：按值返回的容器再取内部指针（本条目踩坑一）。

---

### 2026-09-05 · B-4 `cuav_run` 可执行

#### 1. 本次做了什么

新建 `engine/tools/cuav_run/{runner.h, runner.cpp, main.cpp}`。逻辑放在静态库 `cuav_runner`，`main.cpp` 只做 argv 与进程退出码，
这样 doctest 能直接驱动 `run()` 检查事件流，不必起子进程。三个子命令：

- `--catalog`：输出 `catalog_json()`（`dump(2)`），先对每个组件跑 `validate_catalog_entry()`，自检不过就不输出。
  用它首次生成 `tests/golden/component-catalog.json`（8 组件、21763 字节）；新测试 `test_catalog_golden.cpp` 按
  `docs/component-catalog.md` §5 的「已有条目不变」比对：端口类型与兼容矩阵相同、基准里每个组件的 `type / ports / params` 相同，新增组件允许。
- `--validate <框图>`：`load_diagram_file()` 只校验模式（不给 `out_dir`，不落盘）；成功一条 `validate` 事件（节点、连线数、观测点、`run`），
  失败一条 `error` 事件（`to_json(DiagramError)`），退出码 2。
- `--run <框图> --out <目录> [--task-id] [--seed N] [--resolved <旁挂> | --data-index <索引>...] [--progress-interval-ms N]`：
  建目录、打开 `events.jsonl`、建解析器、装载、运行。事件依次为 `task.state running`（含种子与来源、节点、观测点、`started_utc`）、
  `log`、`progress`（墙钟节流，默认 100 ms）、每行一条 `product_row`（不带数据）、结束 `task.state finished`（结果四态、轮数、墙钟、
  实时因子、逐节点状态、`ended_utc`）；运行失败发 `error {code: run_failed, node_id}` 与 `task.state failed`，退出码 3。
- 事件信封与 WS 文本帧同构 `{seq, task_id, type, t_s, payload}`；stdout 与 `events.jsonl` 逐行 flush、逐字节相同。诊断走 stderr。
- 配套改动：`ObservationTap` 每写一行就 `fflush`（此前只在每 64 行更新索引时刷），服务端才能按 `product_row` 事件的 `row_index`
  立刻从文件读到这一行；`platform::utc_now_iso8601()`（`gmtime_s` / `gmtime_r` 放在平台层）；`LoadedDiagram.edge_count`。
- ctest 新增三项可执行冒烟：`cuav_run --catalog`、`--validate` 切片 ① 框图、`--run` 切片 ① 框图到构建目录。

#### 2. 核实到的事实与踩到的坑

- 三个测试写错、运行器本身没改：`--catalog` 的输出是多行目录 JSON 不是事件流，测试助手按行解析当然炸；把回放源塞进示例框图时
  没清掉单音的参数，装载器按键名顺序先撞到未知参数 `amplitude` 报 `param`，轮不到 `manifest_path` 的 `internal_param`；
  观测点用的是自己的 `nfft`（缺省 1024）不是下游分析器的 256，3000 样点只有 2 行谱。三处都改测试的预期。
- `progress` 回调每轮一次，运行 2 MS 样点默认块长约 31 轮，默认 100 ms 节流后只剩 1 到 2 条；`--progress-interval-ms 0` 时条数 ≥ 轮数
  （收尾轮也算）。因为节流按墙钟，**事件流不承诺逐字节复现，产品文件才承诺**（D-041 ③）。
- 运行失败的报文以节点名开头（`graph.cpp` 的「X 初始化失败 / 处理失败 / 收尾失败」），运行器据此把 `node_id` 找回来给画布高亮；
  匹配要求名字后紧跟空格，避免 `mix` 撞上 `mix2`。
- `task_id` 缺省取 `--out` 的末级目录名，与 `data/runs/<task_id>/` 的约定吻合；校验模式取 `diagram_id`，装载失败时取文件名主干。

#### 3. 约定（D-041）

事件信封与 `events.jsonl` 镜像、`product_row` 不带数据 + 观测点逐行刷盘、`progress` 墙钟节流、退出码 0–4 与结果四态正交、
`--seed` 覆盖须写来源、`--catalog` 不加 `generated_at`、`--scenario-track` 留 G-2。全文见 CLAUDE.md D-041 与 `docs/api-versions.md` §4.1。

#### 4. 量化结果（原型阶段验证值）

- 引擎单测 50 → 57 个用例，12564 → 27388 项断言，全绿；ctest 4 项（单测 + 3 冒烟）全过；`scripts/build-all.sh`、路径与 ASCII 检查全过。
- 切片 ① 框图 `--run`：2442 条 `product_row`（谱 1953 + 包络 489），`events.jsonl` 与 stdout 逐字节相同，序号 1 起连续，
  `task_id` 取目录名；同种子两次运行 `spectrum.f32` 逐字节相同；`--seed 7` 时 `seed_source = cli` 并有覆盖日志。
- 回放框图经 `--data-index` 与 `--resolved` 两种入口产品逐字节相同；两者都不给时 `error data_id` + `task.state failed`，退出码 2，
  失败也镜像到 `events.jsonl`。
- 目录黄金基准 `tests/golden/component-catalog.json`：8 组件、6 端口类型、36 行兼容矩阵、21763 字节。

#### 5. 本条目产生的待办

- [ ] B-5 服务任务管理器：`spawn('cuav_run', [...])` 数组参数不走 shell；任务目录 `data/runs/<task_id>/`；提交时写解析旁挂
      `diagram.resolved.json` 并以 `--resolved` 传入；读 stdout 事件转 WS，`product_row` 按偏移读文件转二进制帧；
      `GET /api/v1/components` 缓存 `--catalog` 输出；Windows 上路径含空格与中文、子进程 stdout 编码、孤儿进程三项风险（06 §9E）要专门验。
- [ ] `cuav_run` 的取消：目前没有信号处理，服务端取消只能杀进程；产品索引 `rows` 最后一次更新可能落后不到 64 行，B-5 收尾时按文件长度修正或让引擎收 SIGTERM 后写索引。
- [ ] `--scenario-track`（G-2）；`detection` 事件（P1-4d）；回放节点的时长截断（B-2 遗留）。

---

### 2026-09-05 · D-042：界面不解释建筑高度来源与估算

用户看到当前场景视图后指出：本系统本来就是演示系统，并不保证建筑物高度是实际值，用户与本方都不关心，
没必要在界面上过多解释；只要有比较合理的数据可用于后续计算即可。

落法：`SceneView` 侧栏删去黄色估算警示框与来源占比行，「按高度来源分色」开关与占比只在 `?dev=1` 开发者模式出现
（带 `data-dev`，与 D-039 的开发者模式同一入口）；端到端断言由「估算高度有显式标注」改为「界面不解释估算高度且无
`[data-dev]`」，默认页 14 项全过，带 `?dev=1` 时同一断言按预期报出 1 个开发者元素；死样式 `.scene-warn` 删除。
铁律 14 改写为「标记留在数据层，界面不解释」：`buildings.geojson` 的 `src` 字段、场景包清单与 `docs/scene-package.md`
的记录一律不动，任何计算与统计仍能区分估算值；「不得用于验收指标」写在文档与模型卡里。09 报告升 v1.4（§0 第 9 条、
§5 示意图、§5.1、§14）。合成场景的小字署名暂按 D-039 保留，用户若也不要再去掉。

前端 6 项单测不变；`web/dist` 已重建，运行中的服务直接托管新产物。

---

### 2026-09-05 · D-043：去掉「合成场景」小字署名

用户问清「合成场景的小字署名」是什么（09 报告为切片 ② 预留的、底图署名旁的三个字，`synthetic = true` 时出现）后指示去掉。
这是 D-042 的延伸：演示系统不必向用户解释站点与航迹是编的还是录的。落法：场景文件的 `synthetic` 字段保留并随产物走
（数据层，铁律 14）；界面不做任何标记。改动全在文档：09 报告 v1.5（§0 第 9 条、§3 试验详情、§5 示意图与 §5.3、§12、
§14 探针去掉 `badges.synthetic`），06 §9C G-5 验收项、§9E 切片 ② 与风险摘要、§13「不做」第 5 条改为「不把合成结果当实测
结论写进文档」，`docs/scenario-format.md` 的 `synthetic` 字段说明，CLAUDE.md 定位段、G 行与 D-043。代码尚未写到 G-5，无代码改动。
至此界面上不再有任何「向用户解释数据有多不完美」的元素；后续这类元素默认不做，除非用户点名要。
