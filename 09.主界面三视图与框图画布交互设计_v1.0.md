# 09. 主界面、三视图与框图画布交互设计

> **版本** v1.8（前半 + 后半，全篇），2026-09-07。v1.8 写出后半「框图画布交互」：§6 扩为完整的框图视图设计（§6.1 至 §6.11），新增 §7.4 对比模式与检测叠加，§11 增补画布键鼠，§16 改为「尚未规定的内容」。后半是切片 ③ 的设计前置，U-2 按它实现。**§0 至 §15 的既有编号未变动**，因为 `web/src/` 有八处注释按 §2、§3、§4.4、§6、§8 引用本报告。v1.7 前半（主界面框架与三视图），2026-09-06。v1.7 按 U-3 实现回填：§7.2 加色图、统计量、包络条、结束收口、未做四行，§10 探针 `signal` 字段与切片 ① 信号视图断言，§1.2 / §7.1 线框去掉 D-047 之前遗留的「标定：论文」（D-048）。v1.6 按 D-047：纵轴对用户只标 dBm，标定来源徽标只在开发者模式；U-1 实现后按实际回填 §10（63 层、`?dev=1` 钩子）。v1.5 按 D-043：不做「合成场景」标记，界面不解释数据是否合成。v1.4 按 D-042b：界面不解释建筑高度来源与估算，数据包组不再列估算比例。v1.3 按 D-039 修正：溯源只在开发者模式显示，场景与估算标注改为低调形式。v1.0 于 2026-09-04 写出，经电磁仿真人机交互评审后按「必须改」十二条修订为 v1.1；用户 2026-09-05 对 §15 十三项暂定逐项拍板后修订为 v1.2（数据中心独立页、2K 基线、瀑布最新行在顶、dBm 优先）。修订记录见 §17。后半「框图画布与信号视图交互」已于 2026-09-07 写出（§6.3 至 §6.11、§7.4）；仍未规定的内容见 §16。
>
> **v1.9（2026-09-07，D-051）**：框图页的主形态改为固定的典型链路视图（`10.典型链路框图与检测识别评价实施方案_v1.0.md`），本报告 §6 的画布设计整体降为高级模式（入口 `#/diagram/canvas`）；§1.2 / §1.3 的三个模板即典型链路的三种模式；§7.1 页签改为 信号 / 检测识别 / 评价 / 任务。正文编号与内容不改，只在 §6 与 §7.1 开头各加一段说明，以 §17 的修订行与 10 号方案为准。
> **状态** PROVISIONAL。本报告顶替 04 §8.1 引用而不在盘上的《无人机信号侦察仿真系统_主界面交互原型_v1.0.html》（决策 D-034）；原件到手后按 §15 逐条对账，差异记入 §15 并另记修正。
> **依据** 04 §8.1（前台主要功能）、§8.2（框图能力，含撤销重做、版本说明、观测点、错误定位）、§8.3（浏览器断开不得终止任务）、§6.4（展示数据按视窗抽取）、§8.6（稳定标识符、不暴露服务器路径）、§21（三个验收场景）；03 §11.9（产品呈现）；01 §10.2、§10.3（运行模式与 M3 触发单元）；`CLAUDE.md` 决策 D-001、D-013、D-020（未标定状态）、D-030 至 D-034、D-037，铁律 1、2、3、6、7、8、14、15；06 备忘录 §9B U-0 / U-1、§9E；`docs/{api-versions, display-products, diagram-format, scenario-format, component-catalog}.md`。
> **用途** U-1 三视图壳的实现依据；`tests/e2e` 断言的判据来源；与用户对齐界面的沟通稿。本报告不定字段（字段在 `docs/` 已冻结），只定布局、交互与状态表现。实现细节（store 形状、数据流）收在附录 A。

---

## 0. 一页结论

1. **一个窗口、一条顶栏、三个视图加一个数据中心页、一个底部抽屉。** 三视图 = 场景 / 框图 / 结果（D-001）；数据中心是独立页面，顶栏直达（用户 2026-09-05 拍板）。顶栏是一条**面包屑**「项目 › 试验 › 任务」加一组运行控制，不再是一排并列胶囊。
2. **用户先看到流程，再看到视图。** 首期服务两类人：算法工程师（画框图、看频谱、比检测器）与系统分析员（布场景、看链路）。三条主流程对应 04 §21 的三个验收场景：全合成、实测回放、混合增强；首次打开系统看到的是这三个入口，不是一张空地图（§1）。
3. **场景视图是电磁态势图，不只是地图编辑器。** 站点到辐射源的「链路」是一等对象：视距线、视距 / 非视距、距离、方位、俯仰、路损、时延、多普勒随逻辑时间变化并可读数（§5.4）。AOI 边界常显，框外无建筑、视距不可信。
4. **结果视图的信号页是一台频谱分析仪**：频谱在上、瀑布在下，共用频率轴且列像素对齐，瀑布最新行在顶部向下滚；有参考电平、动态范围、分辨率带宽、迹线模式、峰值与差值 marker、频率与时间双游标联动、框选缩放、检测叠加（§7.2）。纵轴以 **dBm** 为目标：有标定常数即显示 dBm（用户 2026-09-05 拍板）；**常数来源（实测 / 论文 / 假定 / 模型）不给用户看**，只随索引、探针与溯源走，开发者模式才显示（用户 2026-09-06 指示，D-047）；索引里没有任何常数时才退到 `dBFS（未标定）`。
5. **运行态与结果四态是两个正交徽标**，分别用图标与「形状 + 文字 + 颜色」表达，不共用一个位置（§13）。
6. **浏览器永不见服务器路径。** 实测数据在框图里只以 `data_id` 引用，装载时由服务端解析成文件位置（04 §8.6，D-037）。
7. **撤销 / 重做是一等功能**：场景编辑与框图编辑各一个撤销栈。**溯源不对用户展示**：六件套加 M/E/V 随产物与事件走（铁律 8），界面上只在隐藏的开发者模式里有「溯源」面板（§9），正常界面看起来像交付系统而不是实验系统（D-039）。
8. **地图实例只建一次**，随视图切换隐藏或显示；快捷键避开浏览器保留键。
9. 界面语言中文，数值用工程计数法（`2.44 GHz`、`-108 dBm`），标识符 ASCII，浅色主题，字体走系统字体，不加载任何网络资源（铁律 6、15）。**数据来源与是否合成不在界面上解释**：建筑高度来源、合成场景标记只留在数据字段与文档里，来源分色只在开发者模式（D-042b、D-043）；演示时不主动讲这些。

## 1. 用户与三条主流程

### 1.1 用户

| 角色 | 首期是否服务 | 主要停留的视图 | 关心什么 |
|---|---|---|---|
| 算法工程师 | 是 | 框图、结果 | 这一级组件对信号做了什么；检测器在真实背景下的表现；同种子能否复现 |
| 系统分析员 | 是 | 场景、结果 | 站点布在哪、无人机怎么飞、链路何时被楼挡住、接收功率随距离怎么变 |
| 试验操作员 | 部分（P2 批量运行后完整） | 结果、数据中心 | 跑批、导数据、查记录、出报告 |

### 1.2 三条主流程（04 §21）

| 流程 | 步骤 | 视图 | 界面动作 | 反馈 |
|---|---|---|---|---|
| **全合成** | 1 布站与航线 | 场景 | 「布站」点地图；选辐射源后「航点」连点；右栏改高度、速度、悬停 | 对象树出站点、辐射源、航线；链路线出现并显示距离、方位 |
| | 2 建链 | 框图 | 「新建 › 全合成模板」得到 S0 → S4 链，辐射源与接收机节点绑定到场景实体 | 节点上出现绑定标记；校验通过 |
| | 3 运行 | 顶栏 | 「运行」 | 任务徽标转为运行；逻辑时间走动 |
| | 4 看结果 | 结果 | 信号页：频谱峰值随距离下降、多普勒偏移；「叠加检测」 | 频谱与瀑布实时下滚 |
| | 5 回放 | 场景 | 拖逻辑时间游标 | 无人机与链路线回到该时刻，右栏链路读数随之变化 |
| **实测回放** | 1 选数据 | 数据中心（顶栏直达） | 找到片段，点「用于回放」 | 当前框图的回放节点写入 `data_id`；无场景时顶栏标「无场景绑定」 |
| | 2 建链 | 框图 | 「新建 › 实测回放模板」：回放源 → 观测点 → 能量检测 | 校验通过 |
| | 3 运行与看结果 | 顶栏、结果 | 同上 | 纵轴按数据集的标定常数显示 dBm（来源只在开发者模式，D-047）；没有常数才标 `dBFS（未标定）`；数据集四态为「降级」时原因直接显示 |
| **混合增强** | 1 选背景 | 数据中心（顶栏直达） | 「用于回放」 | 同上 |
| | 2 建链 | 框图 | 「新建 › 混合增强模板」：回放源与合成源经加法混合 → 观测点 → 检测 | 合成源可绑定场景，回放源不可绑定（画布拒绝） |
| | 3 运行与看结果 | 顶栏、结果 | 同上；对比两个观测点（背景、混合后） | 频谱叠加两条迹线 |

三条流程都以「运行 → 结果 → 改参数 → 再运行」循环收尾；同种子重跑必须逐位一致（铁律 9），结果视图的任务页签提供「用同一种子重跑」按钮。

### 1.3 空状态

| 状态 | 中栏显示 |
|---|---|
| 首次打开，没有试验 | 「开始」面板：三个入口按钮（全合成 / 实测回放 / 混合增强，各带一句话说明与示意图）、最近试验列表、「打开示例试验」 |
| 场景视图，没有场景文件 | 地图正常显示数据包；左栏对象树处显示「新建场景」与「载入场景」，工具条只有「俯仰」「图层」可用 |
| 框图视图，没有框图 | 画布中央显示三个模板卡片与「空白框图」 |
| 结果视图，没有任务 | 「还没有运行过任务」，附「去框图视图」链接 |
| 数据中心页，没有数据集 | 「没有登记的数据集」，附建库脚本名与索引文件位置说明（相对仓库根，不显示绝对路径） |

## 2. 总体框架（线框）

基线视口 2560 × 1440（用户 2026-09-05 拍板「按 2K 考虑」，取 QHD），最小 1920 × 1080（§11）。尺寸单位为 CSS 像素。

```
┌──────────────────────────────────────────────────────────────────────────────────────────────┐
│ 顶栏 48px                                                                                    │
│ ▣ │ 默认项目 › 亚运村单机直飞 ● › t-0007 ▶ 运行中 ▲ 降级(暂定) │ ⏵ 运行 ■ 停止 │ t 12.3 s / 120 s ×0.8 │ [场景][框图][结果] [数据] │ ⋯ │
├──────────────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                              │
│                        视图区（三选一；未显示的视图隐藏但不卸载）                                 │
│                                                                                              │
├──────────────────────────────────────────────────────────────────────────────────────────────┤
│ 底部抽屉 收起 28px / 展开 240px  [日志][告警][数据流][资源]   116.4052°E 39.9901°N │ WGS-84 · AGL · LogicalSim │ ● WS seq 1842 │
└──────────────────────────────────────────────────────────────────────────────────────────────┘
```

| 区域 | 内容 | 出处 | 首期切片 |
|---|---|---|---|
| 顶栏左段 | 系统图标（点开：项目列表、关于） | 04 §8.1「项目」 | ① |
| 顶栏面包屑 | 项目 › 试验 › 任务；试验胶囊点开是试验详情（场景、框图、种子、模式、参数版本、M3 触发单元）；任务胶囊带运行态与结果态两个徽标（§13） | D-001；01 §10.2 | ① |
| 顶栏运行组 | 「运行 / 停止」、逻辑时间与实时因子、运行结束后的时间游标 | 04 §8.1「运行」、§8.2 | ① |
| 顶栏右段 | 三视图切换与「数据」页直达；「⋯」菜单（模型目录、试验中心占位、帮助） | D-001；用户拍板 | ① |
| 视图区 | 场景（§5）/ 框图（§6）/ 结果（§7） | D-001 | ①：场景 + 框图（JSON 编辑框）+ 结果（信号页签） |
| 底部抽屉 | 日志、告警、数据流、资源；状态条常显鼠标经纬度、坐标与时间基准、WS 状态 | 04 §8.1「下部」；铁律 1、2、3 | ①：日志、状态条；其余页签占位 |

1920 宽度下的折叠顺序（从先到后）：项目名折成「…」；逻辑时间只留 `t` 与一条细进度条；视图切换退化为图标；系统图标隐藏。**任务徽标、运行 / 停止、视图切换永不隐藏。** 低于 1920 时左右栏自动收起。2560 宽度下不折叠，左右栏取默认宽度的 1.25 倍（350 / 400）。

不用的东西：没有左侧全局导航栏，没有多窗口、多标签页，没有可拖拽的浮动面板。栏宽可拖但有最小值，布局与抽屉高度只存浏览器本地。

## 3. 顶部：面包屑与运行控制

### 3.1 层级

项目 › 试验 › 任务。一个试验 = 一个场景 + 一个框图 + 种子 + 模式 + M3 触发单元；一个试验可以运行多次，每次是一个任务。首期只有一个默认项目。

| 层 | 胶囊显示 | 点开后 | 变化时的全局效果 |
|---|---|---|---|
| 项目 | 名称（1366 下折成「…」） | 项目列表（P2；首期只有默认项目） | 清空其余上下文 |
| 试验 | 名称；场景或框图任一未保存时标 ●，悬停显示是哪个 | **试验详情面板**：场景（`scenario_id`，「在场景视图打开」）、框图（`diagram_id`，「在框图视图打开」）、种子（可改）、模式（「算法验证」/「工程分析」，副标 `M2+M3` / `M1+M2`，01 §10.2；首期只影响试验记录与结果页指标解释）、参数版本、M3 触发单元（站点集合、空间区域、时间窗口、频率窗口、信号集合、验证指标，01 §10.3）、备注 | 场景视图与框图视图载入；框图里场景绑定节点的可选实体随场景更新 |
| 任务 | `task_id` + 运行态徽标 + 结果态徽标（§13）+ 细进度条 | 任务列表；「取消」「用同一种子重跑」 | 结果视图切到该任务；场景视图态势图层切到该任务的实体与链路流 |

### 3.2 运行组

- 「运行」发 `POST /api/v1/tasks`，随后一切状态来自 WS；运行中变「停止」，调 `POST .../cancel`，取消前弹一次确认。运行前先做校验，校验失败时按钮禁用并把原因写在悬停提示与告警页签。
- 逻辑时间 `t 12.3 s / 120 s ×0.8`：当前逻辑时间、总时长、实时因子（墙钟相对逻辑时间的速度，不承诺实时，04 §12.1）。运行结束后同一位置变为可拖**时间游标**，三视图联动：场景回放位置、信号页时间窗中心、数据流页签。
  > **2026-09-12 修订（D-061）**：可拖时间游标不放在顶栏这一格（170 px 放不下活动标记），改为**全宽时间轴条**（`.app` 网格第四行，场景页与结果页可见）；顶栏保留读数与「跟随实时」按钮。时基、回放缓冲与联动见 13 报告 §5。
- 任务运行中关闭浏览器，任务不受影响（04 §8.3）；重新打开时从 `GET /api/v1/tasks` 取最近任务并按 `since = 0` 补取事件恢复。
- 关闭页面前若试验有 ●，浏览器原生确认框拦一次。

### 3.3 04 §8.1 六个顶部菜单的去向

| 04 菜单 | 去向 | 理由 |
|---|---|---|
| 项目 | 系统图标菜单与面包屑首段 | 项目是层级根，不需要单独菜单 |
| 运行 | 顶栏运行组，以及框图视图工具条 | 运行控制必须贴着任务状态 |
| 数据 | 独立页面 `#/data`，顶栏右段直达按钮 | 用户 2026-09-05 拍板：数据中心是独立页面，不并入结果视图 |
| 模型 | 框图视图左栏组件库；「⋯」菜单「模型目录」只读页 | 组件库就是模型的浏览入口 |
| 试验 | 面包屑试验胶囊与试验详情面板；参数扫描、任务队列、结果对比留 P2 | 试验是层级中段 |
| 帮助 | 「⋯」菜单 | 首期只有「关于」与版本信息 |

## 4. 三视图、切换与撤销

### 4.1 路由

手写 hash 路由（D-032）：`#/scene`（默认，保留 `?aoi=`；自 D-061 起另认 `?scenario=`，打开时载入指定场景，优先于最近任务绑定的场景）、`#/diagram`、`#/results`（默认信号页签）、`#/results/detections`、`#/results/tasks`；`#/data` 数据中心独立页（§7.3）。路由只表达「看哪个页面」；上下文在 store 里，刷新后从服务端恢复。数据中心页不持有地图或画布实例，按需挂载即可。

### 4.2 切换规则

1. 三个视图首次访问时挂载，此后只隐藏不卸载。隐藏用 `visibility: hidden` 加绝对定位，不用 `display: none`（后者让地图容器尺寸归零，MapLibre 要重建画布，WebGL 上下文可能丢失）；显示时调一次 `map.resize()`。
   **仅靠 `visibility: hidden` 不够**：该属性虽然继承，后代却可以把自己改回 `visible`。React Flow 量到节点尺寸后会给每个节点写行内 `visibility: visible`，框图节点因此穿透隐藏浮到场景地图上（2026-09-07 实测）。隐藏的视图另加 `opacity: 0`——opacity 加在祖先上，后代无法覆盖；`display` 仍然不动。
2. 框图画布同理保留实例；信号页的瀑布在隐藏时停止绘制，只累积数据，显示时一次性补画。
3. 切换不改变上下文，不中断 WS。
4. 快捷键 `Alt+1 / Alt+2 / Alt+3`，数据中心页 `Alt+4`。**不用 `Ctrl+1/2/3`**：那是 Chromium 保留的标签页切换键，页面拦不住（§11）。

### 4.3 撤销与重做（04 §6.3、§8.2）

| 项 | 约定 |
|---|---|
| 范围 | 场景编辑与框图编辑各一个撤销栈，互不影响；信号页的视窗操作不进栈 |
| 深度 | 50 步；载入文件时清空；保存不清空 |
| 键 | `Ctrl+Z` 撤销，`Ctrl+Y` 或 `Ctrl+Shift+Z` 重做 |
| 显示 | 各视图工具条上「↶ ↷」两个按钮，悬停显示将撤销的动作名（「移动航点 3」「删除连线 e2」） |
| 粒度 | 一次表单「应用」一步；地图上一次拖动一步；画布上一次拖放、连线、删除、改参数各一步 |

### 4.4 三栏规则（三视图共用）

| 栏 | 宽度 | 内容 | 可收起 |
|---|---|---|---|
| 左栏 | 默认 280，最小 220 | 对象树或列表：场景视图是数据包信息与场景对象树（含链路）；框图视图是组件库六类；结果视图是任务与观测点选择器 | 可 |
| 中栏 | 剩余 | 主画面：地图 / 画布 / 图表 | 否 |
| 右栏 | 默认 320，最小 260 | 选中对象的属性或读数：场景对象表单与链路读数 / 节点参数表单 / 仪表与视窗控制 | 可 |

## 5. 场景视图

```
┌ 左栏 280 ────────┬ 中栏 ────────────────────────────────────────────┬ 右栏 320 ─────────────┐
│ ▾ 数据包          │ [选择][布站][航点][测量] │ [俯仰][图层 ▾] │ ↶ ↷                │ 链路 site-1 → uav-1   │
│  beijing-yayuncun │                                                    │  状态  ● 视距          │
│  19.96 × 19.97 km │      ┌ AOI 边界 ───────────────────────────┐        │  距离  1.83 km         │
│  建筑 47,582 栋    │      │        ◉ site-1                     │        │  方位  228.4°          │
│  高度中位 20 m     │      │           ╲ 1.83 km  视距            │        │  俯仰  1.6°            │
│ ▾ 场景 demo-01 ●  │      │            ╲                        │        │  路损  105.3 dB        │
│  ▸ 站点 (1)        │      │             ✈ uav-1 ─ ─ ─ 航线       │        │  时延  6.1 µs          │
│  ▸ 辐射源 (1)      │      │                                     │        │  多普勒 −97 Hz         │
│  ▸ 航线 (1)        │      └─────────────────────────────────────┘        │  帧   12.30–12.31 s    │
│  ▸ 活动 (3)        │                                                    │  更新率 100 Hz  ● 有效 │
│  ▸ 链路 (1) 派生   │  N↑  ├───┤ 500 m   底图 OSM 2026-08-17 · © OSM            │                        │
└──────────────────┴────────────────────────────────────────────────────┴───────────────────────┘
```

### 5.1 左栏

- 「数据包」组沿用现有 `SceneView` 侧栏信息（数据包名、范围、建筑数、高度中位与最高），**默认折叠**。**不列高度来源与估算比例**：来源分色与占比只在开发者模式 `?dev=1` 出现，e2e 断言默认 DOM 里没有这类文字（D-042b）。图层开关不再放这里，统一在工具条「图层 ▾」（去重）。
- 「场景」对象树：站点、辐射源、航线、活动四组来自场景文件；**链路组是派生对象**，站点 × 辐射源自动生成，不入文件。点击对象即选中，右栏出属性或读数，地图飞到对象。未保存标 ●；标题行有「保存场景」「另存」。

### 5.2 中栏地图

| 元素 | 约定 | 切片 |
|---|---|---|
| AOI 边界 | 常显、不可关的细边框图层；鼠标在框外时状态条提示「AOI 外：无建筑数据，视距判定不可信」；框外布站给出告警但不禁止 | ① |
| 比例尺、指北针 | MapLibre 自带 `ScaleControl` 与带指南针的 `NavigationControl`，指北针随方位角旋转 | ① |
| 鼠标经纬度 | 状态条左段，`116.4052°E 39.9901°N`，六位小数（约 0.1 m） | ① |
| 场景标注 | 不做「合成场景」标记：`synthetic` 只留在场景文件字段里随产物走（D-043）；无场景绑定的回放任务显示「无场景绑定」 | ① |
| 底图署名 | 左下角，现有 `scene-attrib` | ① |
| 站点、辐射源、航线 | 站点图标 ◉，辐射源图标随机型，规划航线虚线，航点小方块 | ② |
| 链路线 | 站点到辐射源当前位置的直线；视距绿、非视距红（色值 D2-4 标定）；线旁标距离；运行中随实体移动，回放时随游标移动 | ② |
| 目标与航迹 | G-5：图标随航向旋转，航迹抽稀；点击图标右栏显示实体状态 | ② |

工具条：

| 工具 | 行为 | 切片 |
|---|---|---|
| 选择 | 默认；点击选中；拖动站点或航点移动它（一次拖动一步撤销） | ② |
| 布站 | 点击地图放一个站点，默认参数取 `docs/scenario-format.md` §3；放完回到「选择」 | ② |
| 航点 | 先在左栏选中辐射源，然后连续点击添加航点，双击或 `Esc` 结束 | ② |
| 测量 | 两点距离与真北顺时针方位 | ② |
| 俯仰 | pitch 55 与 0 之间切换 | ① |
| 图层 ▾ | 弹出菜单：山体阴影、建筑三维、按高度来源分色、站点、航线、链路、目标与航迹 | ① |
| ↶ ↷ | 撤销 / 重做（§4.3） | ② |

### 5.3 右栏：对象表单

> **2026-09-12 修订（D-061）**：场景页右栏默认不再是空态，而是**常驻的目标列表 + 目标卡片栈 + 站点卡**（13 报告 §3）；选中对象时本节的表单叠在栈顶并带「返回」，取消选中或 `Esc` 回到栈。本节的表单字段不变。

- 数值一律工程计数法显示与输入（§12）：`2.44 GHz`、`20 MHz`、`30 m`、`12 m/s`；输入接受 SI 前缀（`2.44G`、`20M`），底层存 SI 基本单位。
- 站点：名称、经纬度（六位小数，「从地图拾取」按钮）、离地高（标签写明 AGL，铁律 2）、天线增益、接收机四参数（采样率、中心频率、带宽、噪声系数）。
- 辐射源：名称、机型、中心频率、带宽、发射功率、天线增益、波形类型与子字段。
- 航点：经纬度、离地高、速度、悬停秒数；「插入航点」「删除航点」。
- 活动：时刻、事件、参数；列表按时刻排序；切片 ② 用表单增删，不做拖拽时间线（D-030 薄版）。
- 表单底部「应用」「撤销」；应用即写入 store、标 ●、入撤销栈。范围校验取 schema，越界红框加一句原因。

### 5.4 右栏：链路读数

选中链路时右栏显示当前逻辑时间的链路状态，来源是引擎按样点序号出的 `SceneParamFrame`（`docs/scenario-format.md` §7），经 WS `link` 事件到达；运行结束后按游标从 `track` 与 `link` 产品回放。

| 读数 | 来源 | 未运行时 |
|---|---|---|
| 视距 / 非视距 | `line_of_sight`；D3 落地前平地假设恒为视距，读数旁注「平地假设」 | 浏览器几何预览可给 |
| 距离、方位、俯仰 | 几何 | 浏览器几何预览可给（只做直线插值，不做物理） |
| 路损 | `path_loss_dB`；D3 前为自由空间路损，注「自由空间」 | 显示「运行后由引擎给出」 |
| 时延、多普勒 | `delay_s`、`doppler_Hz` | 同上 |
| 帧有效期、更新率 | `valid_from_s / valid_to_s`、`update_rate_Hz` | — |
| 四态 | 帧的 `state`，形状加文字（§13） | — |
| 溯源 | 只在开发者模式显示（§9） | — |

链路线上的标注只放距离与视距状态，其余读数在右栏，避免地图上文字堆叠。

## 6. 框图视图

> **2026-09-10（D-060）：本章 §6.3–§6.11 描述的自由画布已整体删除。** 用户判定它
> 「不重要、用户操作起来也很难控制，有点华而不实」，框图页只留典型链路一种形态（§6.11 之后
> 由 10 报告 §5 接手，当前操作说明见 `docs/chain-view-guide.md`）。本章保留不删——
> 它记的是当时的设计过程，其中三条经验在典型链路视图里仍然成立并仍在执行：
> **界面不自行解释规则**（组件、参数、单位、范围全部来自组件目录）、
> **参数控件由 `ParamSpec` 生成**、**报错四元组落到具体那张卡片上**。
> §6.2「切片 ① 的过渡形态」描述的 JSON 编辑页同样已删（它在 U-2 之后就没有入口了）。


本章规定框图视图的全部交互。§6.1 至 §6.2 是前半（2026-09-04）已定的框架与过渡形态，
§6.3 起是后半（2026-09-07，U-0 后半）补写的画布设计，即 U-2 的实现依据。

**2026-09-07（v1.9，D-051；C-7 已实现）**：本章的画布自 10 号方案起降为**高级模式**——路由 `#/diagram/canvas`，从典型链路视图工具条「展开为自由画布」进入、画布工具条「回到典型链路」回去（两个方向都通，2026-09-08 补；顶栏「框图」与 `Alt+2` 回的是框图页默认形态），`?dev=1` 时顶栏直达；框图页 `#/diagram` 的主形态是 10 号方案 §5 的典型链路视图（九环节固定、三种信号源模式、只改参数）。本章各节对画布的约定继续有效，不改。§6.11 与 §16 的「模板」专指用户把自建链存为模板（P2），系统预置的典型链路不在其列（10 号方案 §1.3）。

框图视图要解决的问题是：让用户以拖放和连线的方式，构造出一份满足 `docs/diagram-format.md`
的框图文件，并在提交前就得知它是否成立。本章的核心原则只有一条：
**画布不自行解释任何规则**。组件有哪些、参数叫什么、什么单位、什么范围、哪两个端口能连、
连不上的理由是什么，全部来自 `GET /api/v1/components` 返回的组件目录，
而该目录由引擎 `cuav_run --catalog` 生成（08 报告 §4.4）。画布、应用服务与引擎三处的校验结果
因此不可能不一致，画布只是引擎规则的投影。

### 6.1 三栏框架

```
┌ 左栏 280 ────────┬ 中栏 ────────────────────────────────────────────┬ 右栏 320 ─────────────┐
│ 组件库 [搜索    ] │ [新建 ▾][校验][运行 ⏵][保存][另存] ↶ ↷   slice1 ●   │ 节点 replay           │
│ ▾ 辐射源          │                                                    │  类型 FileReplaySource│
│   ToneSource      │             画布（React Flow，U-2）                  │  数据 dronerfb_0_CH0_S4│
│   NoiseSource     │                                                    │       [从数据中心选…] │
│   AddMixer        │       切片 ① 此处为 JSON 编辑框 + 示例框图下拉         │  每块样点 65536       │
│ ▾ 信道            │             + 错误列表                              │  最多读取 全部         │
│ ▾ 天线            │                                                    │  端口 out IQStream →  │
│ ▾ 接收机          │                                                    │  验证 ✓ 通过          │
│ ▾ 数据            │                                                    │  参数版本 …           │
│   FileReplaySource│                                                    │                       │
│ ▾ 算法            │                                                    │                       │
└──────────────────┴────────────────────────────────────────────────────┴───────────────────────┘
```

- 左栏组件库只由组件目录驱动（`GET /api/v1/components`，`docs/component-catalog.md`），
  六类分组来自 `category`；目录中 `internal: true` 的参数不出现在参数面板。
- 中栏工具条：「新建 ▾」提供三个模板（全合成、实测回放、混合增强）与空白；「校验」经服务返回
  `--validate` 结果；「保存」弹出版本说明输入框，服务端执行版本检查以防静默覆盖（04 §8.2、§8.6）；
  另有「另存」与撤销、重做。
- 右栏是 04 §8.1 要求的「参数、单位、范围、说明和验证状态」，另加参数版本；
  溯源只在开发者模式显示（§9）。
- 回放源的数据引用只用 `data_id`（例 `dronerfb_0_CH0_S4`），参数面板显示标识与「从数据中心选…」
  按钮，服务端在任务装载时解析为文件位置；框图文件中出现 `manifest_path` 即校验失败（D-037）。

### 6.2 切片 ① 的过渡形态

切片 ① 与切片 ② 期间，中栏为 JSON 编辑框，顶部一个「示例框图」下拉，下方错误列表每条带
`node_id` 与 `port`；左右栏容器保留，内容显示为「切片 ③ 启用」。该形态实现于
`web/src/diagram/DiagramJsonView.tsx`，在 U-2 完成后由画布取代，但**保留为开发者模式下的
「源码」页签**：框图文件是唯一的真相，能直接读写它对排查问题有价值。

### 6.3 节点外观

节点是画布上的基本对象，其外观需同时表达五项信息：类别、标识、端口、参数摘要与状态。

```
        ┌──────────────────────────────┐
        │ ◆ 辐射源            ✓        │  ← 类别条（色）+ 类别名 + 验证徽标
        │ 单音源                        │  ← 显示名（label，缺省取 display_name）
        │ tone                          │  ← 节点 id（等宽，次要色）
   ○ ── │                              │
        │ 2.4405 GHz · −70 dBm         │  ← 参数摘要（至多两行，工程计数法）
        │                          out ○│  ← 输出口把手
        └──────────────────────────────┘
                    S4 ◉                   ← 该输出口已打观测点
```

节点宽度固定 200 px，高度随参数摘要行数在 72 至 104 px 之间。固定宽度使自动布局与对齐可预期。

**类别色**取自 04 §8.1 的六类，色值在 §12 的浅色底色板上标定，判据同 `docs/display-route.md` 第 4 节
（非文本图形元素对相邻背景不低于 3:1）。类别色只用于节点顶部 4 px 的色条与左侧图标，
不用于整块底色，以免与四态徽标的颜色语义冲突。

| 类别 | 中文名 | 色值 | 最低对比度 | 首期组件 |
|---|---|---|---|---|
| `source` | 辐射源 | `#1e40af` | 7.74:1 | `ToneSource`、`NoiseSource`、`AddMixer`、`SceneEmitterSource` |
| `channel` | 信道 | `#7c2d12` | 8.32:1 | `SceneBoundChannel`、`FreeSpaceChannel` |
| `antenna` | 天线 | `#0f766e` | 4.86:1 | 首期为空 |
| `receiver` | 接收机 | `#6d28d9` | 6.31:1 | 首期为空 |
| `data` | 数据 | `#334155` | 9.19:1 | `FileReplaySource`、`ScenarioSource` |
| `algorithm` | 算法 | `#15803d` | 4.45:1 | `EnergyDetector`、`SpectrumAnalyzer`、`ObservationTap`、`DetectionSink` |

对比度取三种相邻背景中的最低者：画布底 `#f4f1ec`、节点白 `#ffffff`、警示底 `#fdf6e3`
（2026-09-07 实算，判据 WCAG 2.1 SC 1.4.11 的 3:1）。最低的一项是算法绿 4.45:1。

天线与接收机两类首期没有组件。组件库仍显示这两个分组并标注「本期无组件」，
不隐藏它们，因为 04 §8.1 规定的是六类，隐藏会让用户以为系统只支持四类。

**端口把手的形状按端口类型区分**，不只靠颜色（§12 的色弱可辨要求）：

| 端口类型 | 把手形状 | 说明 |
|---|---|---|
| `IQStream` | 实心圆 ● | 样点流，链路主干 |
| `SceneParamFrame` | 实心菱形 ◆ | 慢变参数流 |
| `ChannelPathSet` | 空心菱形 ◇ | 首期无生产者，把手不出现 |
| `SpectrumFrame` | 实心方 ■ | 观测量 |
| `DetectionList` | 实心三角 ▲ | 观测量 |
| `FeatureVector` | 空心方 □ | 首期无生产者，把手不出现 |

输入口在节点左侧，输出口在右侧。把手旁显示端口名，节点被选中或悬停时才显示端口类型全名。

**四态徽标**沿用 §13.1 的两个正交徽标规则。节点上只显示校验态：未校验为空，
通过为 `✓`，失败为 `✕` 并把节点边框改为警示色。运行期的结果四态不落到节点上，
因为节点不是数据产物，四态属于块与产品（08 报告 §2）。

### 6.4 组件库到画布的拖放

组件库中每一项可拖到画布。拖放的约定如下。

1. **拖动时显示幽灵节点**，其尺寸与真实节点一致，使用户在放下之前即可判断是否与既有节点重叠。
2. **放下即创建节点**，`id` 由类型名派生并追加序号（`tone`、`tone-2`），保证符合
   `[a-z0-9_-]{1,64}` 且在框图内唯一；用户可在参数面板改 `id`，改名时同步更新引用该节点的
   连线与观测点。
3. **参数取目录缺省值**，框图文件中不写缺省值（`docs/diagram-format.md` §3），
   面板以浅色显示缺省值，用户改动后转为正常色。必填且无缺省的参数（如 `band_lo_Hz`）
   置空并标为待填，节点显示未校验态。
4. **双击画布空白**弹出组件搜索框，选中后在该位置创建节点。此路径服务键盘操作，
   不要求鼠标拖动。

**对齐与吸附**：画布背景为 16 px 网格，节点位置吸附到网格。拖动时若节点的水平或垂直中心线
与另一节点对齐，显示一条对齐参考线。`position` 只由画布使用，引擎忽略（`docs/diagram-format.md` §3）。

**不重排作者已摆好的节点**，因为框图的空间安排本身携带作者意图。但 `position` 在框图文件中是可选字段，
手写的框图与引擎自带的示例都没有该字段，此时若不给初始位置，全部节点将重叠于同一点。
因此约定：**只对没有 `position` 的节点排一次初始位置**，已有位置的一律不动。
排法是按拓扑深度分层，深度取自源节点算起的最长路径，列内按出现次序纵向排开；
该顺序正是信号链的阅读顺序。排位结果只用于渲染，不回写文档，
否则会把「用户没有摆过」变成「用户摆过」。实现见 `web/src/diagram/layout.ts`。

### 6.5 连线与拒绝提示

连线是画布上最容易出错的操作，也是最能体现「画布是引擎规则的投影」这一原则的地方。

**合法性判据只有一处来源**：组件目录中的 `port_compat`，它是 6×6 全枚举的 36 条，
每条不兼容项自带一句理由。画布不复制规则，只查表。

拖出一条连线时的反馈分三档：

| 情形 | 反馈 |
|---|---|
| 目标端口合法且空闲 | 把手放大、连线变为类别色实线、目标端口显示吸附环 |
| 目标端口类型不兼容 | 连线变为灰色虚线，光标旁显示目录给出的理由原文 |
| 目标输入口已被占用 | 连线变为灰色虚线，提示「一个输入口只能连一条边」 |

松开鼠标时若不合法，连线消失并在画布底部的错误条显示同一句理由，保留 5 秒。
不使用弹窗，因为连线是高频操作，弹窗会打断节奏。

**`IQStream` 与参数流不得直连**这一条需要专门的提示文案，因为它是本系统最容易被误解的规则。
目录给出的理由原文是「端口类型不同；跨类型必须由显式组件承担，IQ 流与参数流不得直连（决策 D-013）」。
画布在此基础上补一句可操作的建议：「请在两者之间放置一个施加类组件，例如场景绑定信道」。
建议文案由画布依据目标类型给出，属显示层的便利，不改变判据本身。

连线的视觉：贝塞尔曲线，颜色取源端口类型的类别色，宽度 2 px。
选中时加粗至 3 px 并显示删除按钮。`SceneParamFrame` 连线画为虚线，
以在视觉上把慢变参数流与 IQ 主干区分开。

### 6.6 参数面板

右栏参数面板的每一项完全由目录的 `ParamSpec` 生成，包括控件类型、单位、范围与说明。
`ParamSpec` 的字段与控件的对应关系如下。

| `type` | 控件 | 约定 |
|---|---|---|
| `number` | 数值输入框 | 接受工程计数法输入（`2.44G`、`20M`、`1e6`），底层存 SI 基本单位；单位取 `unit` 显示在框右；`min` / `max` 越界即红框并给出范围 |
| `enum` | 下拉框 | 选项取 `enum[]`，不允许自由输入 |
| `bool` | 开关 | 显示为「是 / 否」而非勾选框，避免与观测点的产品勾选混淆 |
| `string` | 文本框 | `data_id` 与 `scenario_id` 另有专门控件，见 §6.8 |

四条通用约定：

1. **`internal: true` 的参数不出现**，面板与框图文件里都没有。这是 D-037 在画布侧的执行点。
2. **互斥参数成组显示**：目录 `excludes[]` 声明的两个参数（如 `level_dBm` 与 `amplitude`）
   并排显示并共用一个单选开关，用户只能填其中一个。填了一个则另一个置灰。
   这样在界面上就不可能构造出触发 `param_conflict` 的框图。
3. **跨参数约束即时校验**：目录 `constraint` 声明的约束（如铁律 4 的
   `|Δf| + B/2 + 保护带 < Fs/2`）在输入变化时立即求值，不通过则在面板顶部显示一行原因，
   同时节点转为未通过态。这类约束在提交时由引擎再校验一次，画布的即时校验是便利，不是权威。
4. **缺省值回填**：清空一个有缺省值的输入框即回到缺省，面板以浅色显示，框图文件中不写该键。

面板顶部显示节点的 `id`、`type`、`label` 与验证状态，底部显示参数版本。
`model_id`、`model_version`、`model_layer`、`credibility` 等溯源字段只在开发者模式显示（§9、D-039）。

### 6.7 观测点

观测点不是节点，而是挂在某个 `IQStream` 输出口上的抽头（`docs/diagram-format.md` §5）。
画布上的表现相应地不是一个方框，而是端口下方的一个标记。

**打观测点**：在任一 `IQStream` 输出口上右键，菜单第一项为「打观测点」。
非 `IQStream` 输出口的右键菜单中该项置灰并注明「观测点只能挂在 IQ 输出口上」，
对应引擎错误码 `observation_port`。

创建后弹出观测点面板，含四项：

| 项 | 说明 |
|---|---|
| `id` | 产品目录名，缺省按端口派生（`s4`、`s4-2`）；它同时是结果视图左栏的观测点名 |
| `label` | 显示名，可含中文 |
| 产品 | `spectrum` / `envelope` / `iq` 三个开关，至少选一项 |
| 参数 | `nfft`、窗、重叠、每帧段数、桶长，由目录 `ObservationTap` 的 `ParamSpec` 生成 |

**`iq` 开关本版本置灰**并注明「本版本未实现」。引擎对 `products` 含 `iq` 的框图返回
`product_unsupported` 而不是静默忽略（D-040 ③），画布因此不能让用户勾上它。
待观测点 IQ 产品的落盘格式定稿后放开（08 报告 §16.1）。

**画布标记**：已打观测点的输出口下方显示一个 `◉` 与观测点 `id`。
标记可点击以打开面板，可按 `Delete` 删除。

**与结果视图联动**：结果视图左栏列出本任务的全部观测点，点击其中一项跳到框图视图并选中
对应的输出口；反向地，在画布上点击观测点标记时，若当前任务已有该观测点的产品，
则在标记旁显示一个「看频谱」按钮，点击跳到结果视图并切到该观测点。
两个方向都用 `op_id` 作为关联键。

### 6.8 场景绑定节点

目录中 `scene_bindable = true` 的组件（首期为 `ScenarioSource`、`SceneEmitterSource`、
`SceneBoundChannel`）可绑定到场景中的对象。绑定在框图文件中表现为 `scene_binding`
字段（`{scenario_id, entity_id}` 或 `{scenario_id, site_id}`）。

**绑定操作**：这类节点的参数面板顶部有一个「场景绑定」区，含两个下拉：
第一个选场景，取自 `GET /api/v1/scenarios`；第二个选实体或站点，取自所选场景的对象树。
未绑定时节点显示未校验态并提示「未绑定场景对象」。

**动态端口**：`ScenarioSource` 的输出口在绑定之前是未知的。目录里它只给出一条声明
`{pattern: "link:<emitter_id>", source: "scene_binding", type: "SceneParamFrame"}`，
具体端口要到引擎 `configure()` 取得场景后才确定（08 报告 §4.1）。
画布按同一规则在本地推导：选定场景后，按该场景的辐射源列表生成 `link:<emitter_id>` 输出口。
推导结果与引擎必然一致，因为两者读的是同一份场景文件。
绑定变更时，已连出的连线若其端口不再存在，则连同该连线一并删除并提示。

**场景引用与哈希**：框图顶层的 `scenario_ref` 含 `scenario_id` 与 `sha256`。
该哈希必须是场景文件落盘字节的哈希，由服务端 `PUT /api/v1/scenarios/{id}` 的响应头
`X-CUAV-Sha256` 给出，前端写回框图（D-049 ⑧）。**画布不得自行序列化场景再计算哈希**，
两端各自序列化必然对不上。场景保存后，画布应更新所有引用该场景的框图的 `scenario_ref.sha256`，
并把受影响的框图标为未保存。

**反向高亮**：在画布上选中一个场景绑定节点时，切到场景视图后对应的站点或实体高亮显示；
在场景视图选中一个对象时，切到框图视图后绑定该对象的节点高亮显示。

### 6.9 引擎报错到画布

引擎与服务的错误报文是四元组 `{code, node_id, port, message}`（08 报告 §5.3，共 19 个错误码）。
画布据此定位，规则如下。

| 报文内容 | 画布表现 |
|---|---|
| 有 `node_id` 且有 `port` | 该节点边框转警示色，该端口把手加警示环 |
| 有 `node_id` 无 `port` | 该节点边框转警示色 |
| 二者皆无（如 `json_parse`、`graph`） | 不定位到节点，只进错误列表并在画布底部显示 |

错误列表位于画布下方，每条显示错误码、定位与报文。点击一条即把画布平移到对应节点并选中它；
反向地，选中一个有错的节点时，错误列表滚动到对应条目并高亮。
错误在下一次校验或运行前保持显示，节点参数被修改时该节点的错误标记立即清除，
因为此时旧的报文已不再描述当前状态。

**校验的两个来源要区分显示**：画布本地的即时校验（类型不兼容、必填未填、范围越界）
用「待修正」措辞；服务端 `--validate` 返回的用「校验失败」措辞并附错误码。
两者的判据同源，但前者是提交前的便利，后者是权威结论。

### 6.10 撤销、重做与保存

框图编辑与场景编辑各自持有一个撤销栈（§4.3）。框图栈记录的操作包括：
创建与删除节点、移动节点、连线与断线、修改参数、打观测点与删观测点、绑定场景对象。
连续的同一参数输入合并为一个撤销步（500 ms 静默窗口），避免逐字符入栈。

保存时前端把画布状态序列化为 `docs/diagram-format.md` 规定的形式。
序列化的两条要求：**键序固定**、**缺省值不写入**。前者使同一张框图两次保存的字节相同，
后者使目录缺省值变更时既有框图自动跟随。
序列化结果与黄金基准的等价性是 U-2 的验收判据之一（06 §9B）。

### 6.11 后半暂不规定的内容

以下两项属 04 §8.2 中 P2 阶段的能力，本报告只留占位，不在切片 ③ 实现：

- **子系统封装与模板**：将若干节点折叠为一个可复用的子系统，以及把常用链路存为模板。
- **模型启用 / 旁路 / 替换**：在不改变连线的前提下临时旁路某个节点，或将其替换为同端口的
  另一实现，用于 A/B 对比。

两项都需要框图文件格式增加字段，届时先改 `docs/diagram-format.md` 并记决策。

## 7. 结果视图

### 7.1 页签与布局

**2026-09-07（v1.9，D-051）**：页签改为 信号 / 检测识别 / 评价 / 任务（10 号方案 §5.7）。下图与下表里的「检测」页签内容并入「检测识别」（时间线 + 检测列表 + 识别列表），「评价」页签新增（指标卡、混淆矩阵、ROC），「任务」页签加指标摘要；信号页与右栏仪表不变。

```
┌ 左栏 280 ────────┬ 中栏 ────────────────────────────────────────────┬ 右栏 320 ─────────────┐
│ [信号][检测][任务]                                                       │ 迹线  单帧 ▾  平均 16  │
│ 任务 t-0007 ▶ ▲    │ 频谱   M1 2.4401 GHz −68.2 dBm  Δ +100.0 kHz −12.4 dB │ 参考电平 −30 dBm      │
│                   │        dBm                                          │                       │
│ 观测点            │ ┃▁▂▃▅▇▅▃▂▁         ┆M1        ┆M2               │ 动态范围  80 dB [自动] │
│  ● s4  S4 观测点  │ 2.430   2.435   2.440   2.445   2.450 GHz  RBW 977 Hz │ 频率轴  射频 ▾        │
│  ○ s0  S0 辐射源  │ 瀑布  最新行在顶部，与频谱共用频率轴、列对齐              │ 分辨率  nfft 1024 hann │
│ [对比：s0 ⊕ s4]   │ ░░░▒▒▓▓█▓▒░░  ┆         ┆     ── t 12.30 s ──     │ 时间窗 t0 [0 s] t1 [2 s]│
│                   │ 包络 min / max / rms                               │ 频段 f0 [−10M] f1[+10M]│
│                   │ 散点 S4 IQ n = 4096                                 │ 统计  max ▾            │
│                   │ [叠加检测 ☑] [导出 PNG][导出 CSV]                      │ ☑ 跟随实时  色带 viridis│
└──────────────────┴────────────────────────────────────────────────────┴───────────────────────┘
```

| 页签 | 内容 | 步骤 |
|---|---|---|
| 信号 | 频谱、瀑布、包络、散点四块，按观测点切换；仪表约定见 §7.2；「对比」把第二个观测点的频谱叠成虚线迹线（切片 ③） | U-3 |
| 检测 | 检测列表（`detections.jsonl`）、命中率、门限、噪声估计摘要；点击一条把游标移到该帧；信号页「叠加检测」开关把命中帧画成时频框 | U-4 |
| 任务 | 任务列表与摘要（框图哈希、场景哈希、种子、运行态、结果态、起止、实时因子、引擎版本）；「用同一种子重跑」 | U-4 |

### 7.2 频谱与瀑布的仪表约定

| 项 | 约定 |
|---|---|
| 纵轴单位 | 目标是 **dBm**（用户 2026-09-05 拍板）。取产品索引 `scale` 与 `calibration`：有标定常数即显示 `dBm`；**来源徽标「标定：实测 / 论文 / 假定 / 模型」、常数值与出处只在开发者模式 `?dev=1` 显示**（用户 2026-09-06 指示，D-047 ④）；索引里没有任何常数时才退到 **`dBFS（未标定）`**。两种绝不混画；数据层仍绝不产生不带来源的 dBm（D-020、铁律 15）——索引写了 `dBm` 却没有 `calibration` 时按未标定显示并记一条日志 |
| 参考电平与动态范围 | 右栏「参考电平」「动态范围」两个数；「自动」按当前视窗的最大值与噪声底定；频谱与瀑布共用同一对上下限 |
| 横轴 | 默认射频绝对频率（`center_Hz + offset`），刻度工程计数法；可切「相对中心」；轴下角常显 RBW = `bin_width_Hz` |
| 分辨率 | 显示 `nfft` 与窗函数名；改分辨率是引擎参数不是显示参数，面板里只读并给「去框图改」链接 |
| 标定常数 | 索引 `calibration{offset_dB, source ∈ {measured, paper, assumed, model}, note}`。引擎内部功率单位是 mW，合成链常数为 0 标「模型」；回放数据取清单 `power.calibration`（2026-09-06 估算：DroneRFa −50.0 dBm「论文」、DroneRFb −1.6 dBm「模型」，原型阶段验证值；甲方数据到货后换实测值）。常数进产品索引与开发者模式的信号页头 / 溯源面板，不进用户可见的轴标（D-047） |
| 迹线 | 单帧 / 平均 N 帧 / 最大保持 / 最小保持；N 默认 16；最大保持有「清除」按钮 |
| Marker | M1 峰值自动跟踪，M2 手动放置（点击频谱或瀑布），顶部读数栏显示各自频率与电平、Δ 频差与 Δ 电平；`M` 键放置，`Delete` 删除 |
| 双游标联动 | 频率 marker 在频谱与瀑布上是同一条竖线；时间游标在瀑布与包络上是同一条横线，并与顶栏时间游标同源 |
| 布局与方向 | 频谱图紧贴瀑布上方，两者**共用频率轴且列像素对齐**，频率 marker 是贯穿两图的一条竖线；瀑布时间向下增长，**最新行在顶部**，新行进入时旧行下移（用户 2026-09-05 拍板）；色带图例在瀑布右侧，刻度用与频谱相同的 dB 上下限 |
| 缩放 | 瀑布拖框缩放（时间 × 频率），双击复位；滚轮时间缩放，`Shift` + 滚轮频率缩放，拖动平移；频谱只在频率方向缩放，和瀑布联动 |
| 实时与回看 | 「跟随实时」勾选时新行由 WS 二进制帧追加；拖框、拖动或取消勾选即转为按视窗 REST 抽取（`docs/display-products.md` §3）；新任务开始时自动回到跟随 |
| 检测叠加 | 命中帧在瀑布上画时频框，颜色随检测量深浅；关闭开关时不画 |
| 丢帧 | `dropped{from, to}` 的行画斜纹，不留白、不静默跳过 |
| 导出 | 「导出 PNG」出当前频谱或瀑布图像（带轴与标注）；「导出 CSV」出当前视窗的抽取数据；文件名含 `task_id`、`op_id`、时间窗 |
| 像素 | `px` 等于中栏画布的设备像素宽；`py` 为可见行数；两者都不插值 |
| 色图 | viridis（10 个标准锚点分段线性 256 级，随包内嵌）；色带在瀑布右侧，刻度同频谱量程；自动量程 `ref = ceil10(最大值)`、`floor = floor10(10% 分位)`、只扩不缩并回写面板（D-048） |
| 统计量 | 右栏「统计」`max / mean / min` 决定回看抽取的 `stat`；跟随模式的像素列归约同式（`mean` 线性域），两模式画面一致，前端归约同守黄金基准（D-048） |
| 包络 | 瀑布右侧竖条，与瀑布共时间轴（一像素行一组，回看用包络端点 `px = 可见行数`），`20·log10` 后画 min–max 带与 rms 线，`sqrt_mW` → dBm；时间游标贯穿瀑布与包络条（D-048） |
| 结束收口 | 任务到终态、索引收尾、结束事件已折叠三条同时成立后，自动把当前画面转成回看窗口重取：突发下 WS 丢的行帧从文件补齐（D-048 ②）；采用已结束的任务直接回看 |
| 未做 | IQ 散点（观测点无 `iq` 产品，D-040 ③）、检测叠加、对比观测点：控件不出现，留 U-4 / 切片 ③ |

### 7.3 数据中心（独立页面 `#/data`，顶栏直达）

布局：全宽列表在中，右栏是选中片段的详情（索引里的非路径字段、真值摘要、标定常数与来源）；左栏是数据集与批次过滤。

| 列 | 说明 |
|---|---|
| `data_id` | 可复制 |
| 数据集、通道、中心频率、样点数、片长 | 来自索引 |
| 四态 | 形状加文字；公开数据集全部为「降级」，原因「采集参数来自论文」直接显示 |
| 验收集 | `holdout` 标记显眼；用于回放不禁止，但顶栏任务徽标悬停提示「使用了验收集片段」（用户 2026-09-05 拍板） |
| 标定常数 | `calibration_offset_dB` 与来源；没有时显示「无，纵轴将退到 dBFS」 |
| 动作 | **「用于回放」**：把 `data_id` 写入当前框图选中的回放节点，没有回放节点时提示「先在框图里放一个回放源」；「详情」显示索引里的非路径字段 |

只列索引不给下载，不显示任何文件路径（铁律 7、04 §8.6）。

### 7.4 对比模式与检测叠加

本节是后半补写的部分，规定结果视图中两个观测点的对比以及检测结果的叠加显示。
两项均为切片 ③ 的范围，其前提是画布可以打多个观测点（§6.7）。

**对比的对象是同一任务内的两个观测点。** 典型用法是混合增强场景：一个观测点挂在背景回放源之后，
另一个挂在混合之后，用以直观比较合成目标加入前后的差别。跨任务对比不在本期范围，
因为两次运行的时间轴与标定常数未必可比。

对比模式的三条约定：

1. **频谱叠加，瀑布并排。** 频谱是一维曲线，两条迹线叠在同一坐标系内即可比较，
   第一条用主色、第二条用次色并加虚线，图例给出观测点 `id` 与 `label`。
   瀑布是二维图像，叠加会互相遮挡，因此左右并排、共用时间轴与频率轴，色带与量程也共用，
   否则两幅图的颜色不可比。
2. **量程取两者的并集**，并在面板上标明当前量程由哪一个观测点决定。若各自取自动量程，
   两幅瀑布的同一颜色会代表不同的功率，比较即失去意义。
3. **两个观测点的几何参数必须一致**才允许对比，即 `nfft`、窗、采样率与中心频率相同。
   不一致时禁用对比并说明原因，不作重采样对齐。判据取自各自产品索引 `index.json` 的字段。

**检测叠加**把 `EnergyDetector` 的输出画到频谱与瀑布上。检测结果经 B-7 的
`GET /api/v1/results/{task}/{op}/detections` 读取，该端点的读取层已就绪，生产者待 P1-4d。
叠加的表现：频谱上把检测到的频段画为半透明色带，瀑布上把对应的时间与频率区间画为矩形边框。
检测叠加是一个可关闭的图层，缺省关闭，因为它会遮挡原始数据。
检测结果非 `valid` 时按 §13.2 在图上叠一行原因。

**marker 表格**：信号页右栏在仪表面板之下增加一张 marker 表，每行为一个 marker，
列出编号、频率、时间、功率读数，以及与 M1 的差值。表格与图上的 marker 双向选中。
该表使 marker 的读数可以直接复制，也使差值不必靠目测。

## 8. 底部抽屉与通知

| 页签 | 内容 | 来源 | 切片 |
|---|---|---|---|
| 日志 | 环形缓冲 2000 行；级别过滤；搜索；「跟随末尾」；每行带 `seq` 与逻辑时间；点击带 `node_id` 的错误行，框图视图高亮该节点 | WS `log`、`error` | ① |
| 告警 | 任务、场景、链路、产品的四态变化与原因 | WS `task.state`、`link`、产品索引 | ① 任务级 |
| 数据流 | 每节点 `blocks_in / out`、`samples_in / out`、最近一次 `Step` | WS `progress` | ② |
| 资源 | 实时因子、产品目录大小、WS 丢帧计数 | WS `progress`、`dropped` | ② |

状态条（常显，从左到右）：鼠标经纬度；**坐标与时间基准 `WGS-84 · AGL · LogicalSim`**（回放任务时时间基准显示 `FileAcquisition`；铁律 1、2、3 显式化）；WS 状态 `● 已连接 seq 1842` / `● 重连中（第 2 次，2 s 后）` / `● 已断开`，点击展开抽屉到日志。

通知策略：任务完成、失败、结果降级、WS 断开用右上角非阻塞 toast（5 s 自动消失，失败类不自动消失），同一事件同时进抽屉；toast 不用于进度。

## 9. 内部诊断面板：溯源（铁律 8；仅开发者模式）

溯源数据（六件套加 M/E/V）随每个产物与事件走，这是铁律 8 的数据层要求，不变。**正常界面不展示它**：用户 2026-09-05 指出，对外它让系统像实验系统而不像交付系统；它的价值在内部，写进设计文档、模型卡与证据包（D-039）。实现上保留一个**开发者模式**：地址加 `?dev=1`，开启后任务、节点、观测点、链路帧才出现「溯源」按钮，打开同一种面板；顶栏出现一个小的「DEV」标记提醒别在演示时开着。

| 行 | 内容 |
|---|---|
| 模型 | `model_id`、`model_version`、`model_layer`（M）、`model_level`（E）、`credibility`（V） |
| 参数 | `parameter_version`；节点级另列参数快照 |
| 追溯 | `trace_id`；任务级另列框图哈希、场景哈希、种子、引擎版本、`data_id`（回放时） |
| 状态 | 四态与 `state_reasons` |
| 导出 | 「导出 JSON」，作为模型卡与证据包（P3）的原始材料 |

面板只读；任何字段缺失显示「缺失」，不显示默认值（铁律 15）。默认状态下 DOM 里不存在任何 `[data-dev]` 元素，e2e 有专门断言（§10）。

## 10. 探针与 e2e 断言

现有 `window.__probe()` 的顶层字段全部保留，`tests/e2e/scene-smoke.mjs` 只改一处（图层数 62 → 63，因为切片 ① 新增了 `aoi-boundary`）。新增 `app` 子对象：

```ts
app: {
  view: 'scene' | 'diagram' | 'results' | 'data';
  context: { projectId, experimentId, scenarioId, diagramId, taskId, seed, mode };
  task: { runState, result, t_s, duration_s, realtimeFactor };
  ws: { status, lastSeq, reconnects, dropped };
  drawer: { open, tab };
  unsaved: { scene: boolean; diagram: boolean };
  undo: { scene: { depth, redo }, diagram: { depth, redo } };
  links: Array<{ id, los, distance_m, pathLoss_dB }>;
  signal: { opId, viewport, rows, cols, peakBin, scaleLabel, calibration: { offset_dB, source } | null,
            waterfallNewestRow: 'top', markers: Array<{ id, freq_Hz, level_dB }>,
            // U-3：模式与画面（D-048）
            mode: 'follow' | 'browse', follow, cursor_t_s, stat, trace,
            geom: { nfft, bw, dt, center_Hz, t0_s } | null,
            lastFetch: { rows, cols, px, py, t0, t1, f0, f1, stat, state, envRows } | null, fetchStatus,
            bounds: { spectrum: [left, right] | null, waterfall: [left, right] | null }, canvas: { W, H, dpr } | null,
            drawnRows, hatchedRows, liveFrames, liveRows, shown: { t0, t1, f0, f1 } | null, envelopeRows };
  perf: { longTasks: { count, maxMs } | null } | null;   // 仅 ?dev=1
  badges: { noScene: boolean };
  mapInstanceId: number;
}
```

切片 ① 断言（`tests/e2e/slice1-smoke.mjs`）：

| 断言 | 判据 |
|---|---|
| 三视图切换不重建地图 | 切到框图再切回场景，`mapInstanceId` 不变且 `tilesLoaded` 为真 |
| 任务跑完 | `app.task.runState === 'finished'` 且 `result === 'valid'` |
| 单音峰值 | `app.signal.peakBin` 与理论 bin 差 ≤ 1 |
| 纵轴标注 | `app.signal.scaleLabel` 以 `dBm` 开头：默认地址为 `dBm`，`?dev=1` 为 `dBm · 标定：模型`（切片 ① 合成链）；`app.signal.calibration.source === 'model'`；默认 DOM 无「标定：」文字，`?dev=1` 时它只在 `[data-dev]` 内；索引无常数时为 `dBFS（未标定）`（D-047） |
| WS 序号连续 | `?dev=1` 下调 `window.__cuav.ws.dropForTest()` 断开（走真实重连与 `since` 补取），`ws.reconnects ≥ 1`，`ws.lastSeq` 追平 `GET /api/v1/tasks/{id}` 的 `last_seq`，日志无「缺号」告警 |
| 页面无外网请求 | CDP 网络事件里所有请求的 host 都是本机 |
| 标注 | 无场景时 `badges.noScene` 为真；默认 DOM 里没有「合成场景」「估算」「est:」「osm:」等数据来源文字（D-042b、D-043） |
| 无溯源入口 | 默认状态下 DOM 里不存在 `[data-dev]`，任何面板没有「溯源」按钮；带 `?dev=1` 打开时才有 |
| 快捷键 | 发 `Alt+2`，`app.view === 'diagram'`；发 `Alt+4`，`app.view === 'data'` |
| 瀑布方向 | `app.signal.waterfallNewestRow === 'top'`，且频谱与瀑布画布左右边界像素相同（`bounds.spectrum` 与 `bounds.waterfall`） |
| 隐藏期累积 | 任务在框图页跑完后切到结果页，`liveFrames ≥ 1`（从环补画），随后 `mode === 'browse'` 且 `lastFetch.t1` 等于数据末尾（结束收口，D-048 ②） |
| 缩放重取 | `?dev=1` 钩子 `window.__cuav.signal.zoomTo({t0, t1, f0, f1})` 等价于框选：`lastFetch.rows === py === canvas.H`、`cols === min(px, 窗内列数)`、`stat === 'max'` |
| 键 | `←` 把 `cursor_t_s` 前移一个 `dt`；`M` 后 `markers.length === 2` 且 M2 在 M1 处；`Delete` 后回 1 |
| 二次运行 | 新任务 `liveFrames` 增加（实时画过）、结束后 `follow === false`；地图空闲后 `perf.reset()` 再跑，`perf.longTasks.count === 0` |
| 导出 | `window.__cuav.signal.csv()` 的文件名含任务、观测点、时间窗，数据行数 = rows + 列头 |
| 刷新回看 | 默认页采用已结束任务后 `mode === 'browse'`、`lastFetch.t0 === 0`；探针无 `perf` |
| AOI 边界 | `layers` 含 `aoi-boundary` |
| 状态条基准 | DOM 里存在 `[data-basis="WGS-84 AGL LogicalSim"]` |

## 11. 键鼠约定与窗口基线

| 项 | 约定 |
|---|---|
| 视口 | 基线 2560 × 1440（2K，QHD）；最小 1920 × 1080（折叠顺序见 §2） |
| 浏览器 | Edge / Chromium 内核，WebGL2 必需（D-016）；不支持触屏手势 |
| 设备像素比 | 1 至 2；瀑布画布按 DPR 分配像素 |
| **保留键不用** | `Ctrl+1…9`、`Ctrl+Tab`、`Ctrl+W`、`Ctrl+T`、`Ctrl+N`、`Ctrl+D`、`F5`、`F11`、`F12` 一律不绑定；`Ctrl+S` 与 `Ctrl+A` 可绑但必须 `preventDefault` |
| `Alt+1 / 2 / 3 / 4` | 切到场景 / 框图 / 结果 / 数据中心 |
| `Ctrl+S` | 保存当前视图的对象（场景或框图） |
| `Ctrl+Enter` | 校验并运行 |
| `Ctrl+Z` / `Ctrl+Y` | 撤销 / 重做（当前视图的栈） |
| `Esc` | 结束当前地图工具、取消选择、关闭弹出面板 |
| `` Ctrl+` `` | 收起 / 展开底部抽屉 |
| `←` `→` | 结果视图：时间游标前后移一帧；`Shift` 十帧 |
| `M` | 结果视图：放置 marker |
| `Delete` / `Backspace` | 框图视图：删除选中的节点、连线或观测点；结果视图：删除选中 marker |
| `Ctrl+A` | 框图视图：全选节点 |
| `Ctrl+Shift+D` | 框图视图：复制选中节点（参数一并复制，`id` 追加序号）。**不用 `Ctrl+D`**，它是 Chromium 的「收藏此标签页」 |
| `Tab` | 框图视图：在节点间按拓扑序移动选择 |
| 鼠标 | 地图沿用 MapLibre 默认；画布沿用 React Flow 默认（左键框选、中键或空格加左键平移、滚轮缩放）；输出口右键出观测点菜单（§6.7）；瀑布见 §7.2 |

## 12. 视觉与文案约定

- **主题**：浅色，沿用 `web/src/app.css` 色板：背景 `#f4f1ec`、文字 `#48423a`、边框 `#ddd7cd`、次要文字 `#7b7367`、警示底 `#fdf6e3` 与线 `#dfbe83`。与 Airports 底图同一色系。不做深色主题（D-017）。
- **徽标不只靠颜色**：运行态用图标，结果态用「形状 + 文字 + 颜色」（§13），色弱可辨；色值随态势色在浅色底图上标定（D2-4），进 `docs/display-route.md` 第 4 节。
- **场景标注**：不做「合成场景」标记，界面不解释数据是否合成（D-043）。
- **工程计数法**：所有物理量显示为 `数值 + SI 前缀 + 单位`（`2.44 GHz`、`−108 dBm`、`6.1 µs`），三到四位有效数字；输入框接受 `2.44G`、`20M`、`1e6`，底层存 SI 基本单位；不接受的输入红框加一句原因。
- **字体**：系统字体栈；数值用 `tabular-nums`；不加载 Web 字体；图标用内联 SVG（铁律 6）。
- **文案**：界面中文；模式用「算法验证」「工程分析」，`M2+M3` / `M1+M2` 作副标；标识符（`scenario_id`、`diagram_id`、`task_id`、`op_id`、`data_id`）原样显示 ASCII。
- **不暴露服务器路径**（04 §8.6）：任何面板、日志行、导出文件名都不出现磁盘路径；日志里引擎报的路径由服务端替换为 `data_id` 或相对名。
- **不出现 MATLAB**（04 §8.5、03 §11.9）：Coder 产物组件与手写组件外观一致，`implementation` 只在「模型目录」只读页显示为「实现来源」。

## 13. 运行态、结果态与降级的界面表现

### 13.1 两个正交徽标

| 运行态 | 图标 | 说明 |
|---|---|---|
| 排队 | ○ | 已提交未开始 |
| 运行 | ▶ | 逻辑时间走动 |
| 完成 | ■ | 正常结束 |
| 失败 | ✕ | 引擎报错 |
| 取消 | ⊘ | 用户取消 |

| 结果态 | 形状 + 文字 | 颜色（占位，D2-4 标定） |
|---|---|---|
| 有效 | ● 有效 | 绿 |
| 降级 | ▲ 降级 | 琥珀 |
| 无效 | ✖ 无效 | 红 |
| 不适用 | — 不适用 | 灰 |

组合规则：结果态徽标在运行态为「完成」「失败」时定稿显示；运行中显示到目前为止的中间结果态并加「（暂定）」，样式为空心；「失败」强制结果态为「无效」；「取消」的结果态为「不适用」。

### 13.2 各对象的表现

| 对象 | 四态从哪来 | 表现 |
|---|---|---|
| 任务 | WS `task.state.payload.result` | 顶栏任务胶囊两个徽标；告警页签列 `state_reasons` |
| 场景 | `aoi.manifest_sha256` 核对、schema 校验 | 试验详情里场景行徽标；无效时「运行」禁用并给原因 |
| 框图 | `--validate` 结果 | 试验详情里框图行徽标；错误列表每条带 `node_id` 与 `port` |
| 链路帧 | `SceneParamFrame.state` | 右栏读数旁徽标；地图链路线加斜纹 |
| 观测点产品 | `spectrum.index.json` 的 `state` | 信号页标题旁徽标；降级时图上叠一行原因 |
| 数据集 | `index.manifest.json` 质量状态 | 数据中心每行徽标与原因 |
| WS 丢帧 | `dropped{from, to}` | 瀑布对应行斜纹；资源页签计数 |
| WS 断开 | 心跳超时 | 状态条变色；任务状态显示「最后已知」并带时间；重连后自动补取 |
| 引擎失败 | `error` 事件 | 运行态 ✕、结果态无效；日志滚到错误行；框图视图高亮节点 |

禁止的表现：用默认值顶替缺失数据、把「不适用」画成 0、把丢帧画成空白、把索引里没有标定常数的功率标成 dBm（铁律 15、D-020；来源本身不给用户看是 D-047 的显示层决定，数据层的来源字段不可缺）。

## 14. 与 04 §8.1、§8.2、03 §11.9 逐条对照

| 上游条款 | 落点 | 切片 |
|---|---|---|
| 04 顶部：项目、运行、数据、模型、试验、帮助 | §3.3 | ① |
| 04 左侧：六类组件库 | §6 左栏，目录驱动 | ③ |
| 04 中部：框图画布 | §6 中栏 | ① JSON 编辑框；③ 画布 |
| 04 右侧：参数、单位、范围、说明、验证状态 | §6 右栏 | ③ |
| 04 下部：日志、告警、数据流、资源 | §8 | ① 日志；② 其余 |
| 04 结果页：时域、频谱、瀑布图、星座图、统计特征、算法指标 | §7（时域 = 包络，星座 = S4 散点，D-034；仪表约定 §7.2；dBm 与标定常数） | ① 频谱瀑布；③ 包络散点 marker 标定徽标；④ 检测指标 |
| 04 数据中心 | §7.3 独立页面 `#/data`，顶栏直达 | ④ |
| 04 试验中心 | 试验详情面板占位；任务队列 = 任务页签 | P2 |
| 04 §8.2 撤销重做（经 §6.3）、保存加载另存与版本说明 | §4.3、§6 | ②③ |
| 04 §8.2 任务脱离页面继续运行、重连恢复 | §3.2 | ① |
| 04 §8.2 错误定位到模型和端口 | §8 日志、§13 | ① 列表；③ 高亮 |
| 04 §8.2 中间观测点 | §7 左栏；打法在后半 | ① 固定一个；③ 可编辑 |
| 04 §8.6 不暴露服务器路径、版本检查 | §6、§7.3、§12 | ① |
| 04 §21 三个验收场景 | §1.2 三条主流程 | ①–④ |
| 03 §11.9 顶部场景配置、运行控制 | 场景视图；§3.2 | ① |
| 铁律 8 溯源 | 数据层随产物走；界面只在开发者模式显示（§9） | ② |
| D-001 三视图 + 顶部试验上下文 | §2、§3、§4 | ① |

## 15. 暂定项与拍板记录

v1.1 的十三项暂定已由用户 2026-09-05 逐项拍板；五项改定，八项采纳报告原案。原型 html 到手后仍按本表对账。

| # | 项 | v1.1 暂定 | 拍板 | 落点 |
|---|---|---|---|---|
| 1 | 顶栏面包屑「项目 › 试验 › 任务」，场景与框图作为试验属性 | 面包屑 | 采纳 | §2、§3 |
| 2 | 三视图切换放顶栏右段，不做左侧导航栏 | 顶栏右段 | 采纳 | §2 |
| 3 | 数据中心的位置 | 结果视图的页签 | **改定：独立页面，顶栏直达** | §3.3、§4.1、§7.3、`#/data`、`Alt+4` |
| 4 | 试验中心首期只占位 | 占位 | 采纳 | §3.3 |
| 5 | 逻辑时间游标三视图联动 | 联动 | 采纳 | §3.2、§7.2 |
| 6 | 星座图首期 = S4 IQ 散点 | 散点 | 采纳（D-034） | §7.1 |
| 7 | 活动时间线用表单不做拖拽 | 表单 | 采纳（D-030 薄版） | §5.3 |
| 8 | 运行模式切换首期只影响记录不影响引擎 | 只记录 | 采纳 | §3.1 |
| 9 | 视口基线 | 1920 × 1080，最小 1366 × 768 | **改定：按 2K 考虑**，取 2560 × 1440 为基线，最小 1920 × 1080 | §2、§11 |
| 10 | 瀑布方向与布局 | 最新行在底部、向上滚 | **改定：最新行在顶部、向下滚；频谱紧贴瀑布上方，共用频率轴、列对齐** | §7.1、§7.2、§10 |
| 11 | 纵轴单位 | 未标定一律 `dBFS（未标定）`，绝不显示 dBm | **改定：以 dBm 为目标，先按可得依据估一个标定常数并标明来源；没有任何常数才退到 dBFS** | §7.2 标定常数行、§7.3、§13.2；数据线补公开数据集的估算常数 |
| 12 | 链路线视距绿、非视距红 | 待 D2-4 标定 | 采纳，色值仍待 D2-4 | §5.2 |
| 13 | 验收集片段允许用于回放但提示 | 允许并提示 | 采纳 | §7.3 |

## 16. 尚未规定的内容

后半（§6.3 至 §6.11、§7.4）于 2026-09-07 补写后，本报告仍未规定以下内容。前两项属 04 §8.2 的
P2 阶段能力，第三项待引擎补齐数据源，第四项待画布落地后按实测回填。

- **子系统封装与模板**：把若干节点折叠为可复用的子系统，以及把常用链路存为模板。
  需要框图文件格式增加字段，届时先改 `docs/diagram-format.md` 并记决策（§6.11）。
- **模型启用 / 旁路 / 替换**（04 §8.2）：在不改动连线的前提下临时旁路某节点，
  或替换为同端口的另一实现，用于 A/B 对比。同样需要新字段（§6.11）。
- **IQ 散点**：观测点本版本不产出 `iq` 产品（D-040 ③），前端无数据源。
  待 08 报告 §16.1 的落盘格式定稿后，在 §7 补写散点面板与它同频谱、包络的联动。
- **画布性能基线**：节点数与连线数达到多少时需要虚拟化渲染，待 U-2 实现后按实测填入 §11。
  切片 ① 与切片 ② 的示例框图分别为 4 节点与 6 节点，远未触及该问题。

## 17. 修订记录

| 版本 | 日期 | 修订 | 依据 |
|---|---|---|---|
| v1.10 | 2026-09-12 | **时间游标改到全宽时间轴条，场景页右栏改为目标卡片栈**。不改正文：§3.2 与 §5.3 各加一段引用块，指向 13 报告 §5 与 §3；§2 的「没有可拖拽的浮动面板」不变（时间轴条是网格行不是浮层） | 用户 2026-09-12 拍板（全宽条、卡片常驻右栏）；D-061；13 报告 |
| v1.9 | 2026-09-07 | **画布降为高级模式，结果页页签改为四个**。不改正文：在 §6 与 §7.1 开头各加一段说明；§1.2 / §1.3 的「新建 › 三个模板」即典型链路的三种模式，成为框图页本身；§6.11 与 §16 的「模板」限定为用户自存模板。典型链路视图的设计在 10 号方案 §5，检测识别与评价页签在 §5.7 | 用户 2026-09-07 指示；D-051；10 报告 |
| v1.8 | 2026-09-07 | **后半写出（U-0 后半，切片 ③ 的设计前置）**。§6 由「只定框架」扩为完整的框图视图设计，原内容保留为 §6.1（三栏框架）与 §6.2（切片 ① 过渡形态），新增 §6.3 节点外观（六类类别色在浅色底上标定、端口把手按类型区分形状、节点只显示校验态）、§6.4 拖放与对齐吸附、§6.5 连线与三档拒绝提示（判据只查目录 `port_compat`，理由用目录原文并补一句可操作建议）、§6.6 参数面板（四种控件、互斥参数成组、约束即时校验、缺省值回填、`internal` 不出现）、§6.7 观测点（右键打点、`iq` 开关置灰、与结果视图双向联动）、§6.8 场景绑定节点（动态端口本地推导、哈希由服务端给出不自行序列化）、§6.9 引擎报错到画布（四元组定位三档、本地校验与服务端校验措辞区分）、§6.10 撤销重做与序列化两条要求、§6.11 P2 占位；新增 §7.4 对比模式与检测叠加（频谱叠加瀑布并排、量程取并集、几何参数须一致、marker 表格）；§11 增补画布键鼠四行（`Ctrl+D` 因与 Chromium 收藏冲突改为 `Ctrl+Shift+D`，并入保留键行）；§16 由「后半部分待写」改为「尚未规定的内容」四项。文体按技术设计文档写法，与 08 报告 v2.1 一致 | 用户 2026-09-07「从 U-0 后半开工」；06 §9B U-0、§9E 切片 ③；D-013、D-030、D-032、D-037、D-039、D-040 ③、D-049 ⑧；`docs/{diagram-format, component-catalog}.md`；08 报告 §3、§4.1、§5.3 |
| v1.0 | 2026-09-04 | 前半初稿，§0–§15 | U-0 |
| v1.7 | 2026-09-06 | 按 U-3 实现回填：§7.2 新增色图、统计量、包络、结束收口、未做五行；§10 探针 `signal` 字段扩展与切片 ① 信号视图断言七行；§1.2、§7.1 线框去掉 D-047 之前遗留的「标定：论文」。包络布局按「时间游标是同一条横线」定为瀑布右侧竖条（线框中的横向「包络 min / max / rms」行作废） | U-3 实施；D-048 |
| v1.6 | 2026-09-06 | 按 D-047：纵轴对用户只标 `dBm`，来源徽标、常数与出处只在开发者模式（§0 第 4 条、§7.2 两行、§13.2 禁止项）；U-1 实现后回填 §10：`scene-smoke` 图层数改 63，WS 断线断言用 `?dev=1` 钩子 `window.__cuav.ws.dropForTest()`，纵轴断言区分默认与开发者模式 | 用户 2026-09-06 指示；U-1 实现 |
| v1.5 | 2026-09-05 | 按 D-043：去掉「合成场景」小字署名——§0 第 9 条、§3 试验详情、§5 示意图与 §5.3 场景标注、§12、§14 探针 `badges.synthetic` 与断言改写 | 用户 2026-09-05 指示 |
| v1.4 | 2026-09-05 | 按 D-042b：界面不解释建筑高度的来源与估算——§0 第 9 条、§5 示意图与 §5.1 数据包组、§14 标注断言改写；来源分色与占比只在开发者模式。现有 `SceneView` 与 e2e 已同步改 | 用户 2026-09-05 指示 |
| v1.3 | 2026-09-05 | 按 D-039 修正：可解释性是对内要求，溯源面板降为开发者模式（`?dev=1`）的内部诊断面板，正常界面无任何「溯源」入口（§0、§3.1、§5、§6、§7、§9、§10、§14）；合成场景标注由右上角醒目徽标改为底图署名旁小字，估算高度警示改为默认折叠的数据包属性，铁律 14、15 的标注要求仍满足，只是呈现强度降低，演示时不主动讲 | 用户 2026-09-05 修正 |
| v1.2 | 2026-09-05 | 用户对 §15 十三项逐项拍板，五项改定：③ 数据中心独立页面顶栏直达（`#/data`、`Alt+4`）；⑨ 视口基线 2560 × 1440、最小 1920 × 1080；⑩ 瀑布最新行在顶部向下滚，频谱紧贴其上共用频率轴列对齐；⑪ 纵轴以 dBm 为目标，标定常数带来源徽标，无常数才退 dBFS；⑬ 验收集允许回放并提示。其余八项采纳原案。§0、§1、§2、§3.3、§4、§7、§10、§11、§13.2、§14、附录 A 相应修订 | 用户 2026-09-05 拍板 |
| v1.1 | 2026-09-04 | 按评审「必须改」十二条修订：① `Ctrl+1/2/3` 改 `Alt+1/2/3`，列出保留键（§4.2、§11）；② 回放源改用 `data_id`，数据中心加「用于回放」，浏览器永不见路径（§6、§7.3、D-037）；③ 顶栏改面包屑加运行组，定 1366 折叠顺序（§2、§3）；④ 运行态与结果态拆成两个正交徽标（§13.1）；⑤ 场景视图加链路对象与读数（§5.4）；⑥ 加 AOI 边界、比例尺、指北针、鼠标经纬度（§5.2）；⑦ 新增频谱与瀑布仪表约定（§7.2）；⑧ 频率 marker 与时间游标双联动、框选缩放（§7.2）；⑨ 检测叠加到瀑布（§7.1、§7.2）；⑩ 撤销重做（§4.3）；⑪ 溯源面板（§9）；⑫ 新增用户与三条主流程、空状态（§1）。随手并入的建议：工程计数法、徽标形状加文字、状态条基准、图层开关去重、通知策略、导出、示例框图下拉、版本说明、模式中文标签、瀑布方向、实现细节移附录 | 2026-09-04 评审 |

---

## 附录 A. 状态模型与数据流（实现细节，供 U-1）

### A.1 store 形状（`web/src/state/store.ts`，零依赖 `useReducer` + context，D-032）

```ts
interface AppState {
  ui: { view: 'scene' | 'diagram' | 'results' | 'data'; resultsTab: 'signal' | 'detections' | 'tasks'; devMode: boolean;   // ?dev=1，默认 false
        drawer: { open: boolean; tab: 'log' | 'alerts' | 'flow' | 'resources' }; leftCollapsed: boolean; rightCollapsed: boolean;
        toasts: Toast[] }
  context: { projectId: string; experimentId: string | null; scenarioId: string | null; diagramId: string | null;
             taskId: string | null; mode: 'M2+M3' | 'M1+M2'; seed: number; parameterVersion: string | null }
  scene: { packageId: string | null; summary: SceneSummary | null; scenario: Scenario | null; dirty: boolean;
           editor: { tool: 'select' | 'site' | 'waypoint' | 'measure'; selection: { kind: string; id: string } | null };
           undo: UndoStack<SceneEdit> }
  diagram: { json: Diagram | null; dirty: boolean; validation: { ok: boolean; errors: DiagramError[] } | null;
             undo: UndoStack<DiagramEdit> }
  task: { id: string | null; runState: 'queued' | 'running' | 'finished' | 'failed' | 'cancelled' | null;
          result: 'valid' | 'degraded' | 'invalid' | 'not_applicable' | null; resultProvisional: boolean;
          t_s: number; duration_s: number; realtimeFactor: number; error: TaskError | null; provenance: Provenance | null }
  entities: { byId: Record<string, EntityState>; trails: Record<string, [number, number, number][]> }
  links: { byId: Record<string, LinkFrame> }          // 每条链路最近一帧 SceneParamFrame 的读数
  signal: { opId: string | null; compareOpId: string | null;
            viewport: { t0: number; t1: number; f0: number; f1: number; stat: 'max' | 'mean' | 'min' };
            display: { refLevel_dB: number; range_dB: number; auto: boolean; trace: 'single' | 'avg' | 'maxhold' | 'minhold'; avgN: number;
                       freqAxis: 'rf' | 'offset'; overlayDetections: boolean; unit: 'dBm' | 'dBFS';
                       calibration: { offset_dB: number; source: 'measured' | 'paper' | 'assumed' | 'model' } | null };
            markers: Marker[]; follow: boolean; cursor_t_s: number | null }
  log: { ring: LogLine[]; filter: 'all' | 'warn' | 'error'; query: string; followTail: boolean }
  ws: { status: 'connected' | 'reconnecting' | 'closed'; lastSeq: number; reconnects: number; dropped: number }
}
```

规则：频谱、包络、散点的 Float32 数据不进 store，放在信号页自己的缓冲对象里；store 只存视窗、仪表设置与元信息。切换任务时缓冲整体释放。撤销栈只存编辑差分，不存整份文件。

### A.2 数据流

| 触发 | 调用 | 更新 |
|---|---|---|
| 页面打开 | `GET /api/v1/scenes`、`GET /api/v1/components`、`GET /api/v1/tasks?limit=1` | `scene.packageId`、组件库、`context.taskId`；若有任务则 WS 订阅并 `since = 0` 补取 |
| 点「运行」 | 先本地 schema 校验，再 `POST /api/v1/tasks`（框图 JSON 只含 `data_id`，`Idempotency-Key` = 框图哈希 + 时间戳） | `context.taskId`、`task.runState = queued`；WS 订阅 |
| WS 文本帧 | 按 `seq` 递增校验；缺号即 `GET .../events?since` 补取 | `task`、`entities`、`links`、`log`、`ws.lastSeq`；`task.state`、`error`、`dropped` 触发 toast |
| WS 二进制帧 | 写入信号缓冲；`follow` 时追加绘制 | `ws.dropped` |
| 拖视窗、游标、marker | `GET /api/v1/results/{task}/{op}/spectrum?…`（防抖 150 ms） | 信号缓冲；`signal.*` |
| 「用于回放」 | 无网络调用；写当前框图回放节点的 `data_id` | `diagram.json`、`diagram.dirty`、撤销栈 |
| 场景保存 / 框图保存 | `PUT /api/v1/scenarios/{id}`；框图保存走任务提交或 P2 的框图端点，带版本说明 | `dirty = false` |

WS 客户端（`web/src/api/ws.ts`）：单连接；重连退避 1 / 2 / 4 / 8 s 封顶，重连报文带 `since = ws.lastSeq`；心跳 15 s 未收到即判断开。实体、链路与进度事件先进队列，`requestAnimationFrame` 时一次性提交到 store（每帧一次状态提交，沿用 em-demo 的做法）。
