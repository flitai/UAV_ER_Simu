# **本仓库不做任何换行转换。**
#
# 理由不是风格偏好，是这个项目的验证手段：溯源与基准大量建立在**文件的字节哈希**上——
# 场景文件对观测区域清单的 sha256、框图的 `scenario_ref.sha256`、Coder 产物的 36 个哈希、
# 建筑集的 sha256、黄金基准文件。git 在 Windows 上默认把 LF 换成 CRLF，**换的是字节**，
# 于是这些哈希在 Windows 上一个都对不上。
#
# D3-8 第一次在 Windows CI 上跑时当场验实了：`manifest.json` 场景里写 4e1ed6a8…、
# Windows 上实际算出 7e73ad02…，而把本地这份按 CRLF 重算一遍，得到的正是 7e73ad02…。
# 57 个失败断言里有 50 个是这一个原因。
#
# `-text` = 这个文件不是「文本」，检出与入库都不碰换行。对全部文件生效。
* -text
